## Agent skills

### Issue tracker

Issues live in [stoasystem/stoa-backend](https://github.com/stoasystem/stoa-backend) GitHub Issues, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
