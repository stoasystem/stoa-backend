---
phase: 05-verification-and-test-data
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/05-verification-and-test-data/05-TEST-DATA.md
  - .planning/phases/05-verification-and-test-data/05-VERIFICATION.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-10
must_haves:
  truths:
    - "Backend verification evidence is recorded for parent ownership, cross-parent denial, non-parent rejection, empty child list, and missing report behavior."
    - "Frontend verification evidence is recorded for real response rendering, no-child state, missing-report state, and service path alignment."
    - "Test data documentation identifies one parent account, one linked student account, and enough activity to verify summary/history."
    - "Requirements status reflects completed evidence."
  artifacts:
    - path: ".planning/phases/05-verification-and-test-data/05-TEST-DATA.md"
      provides: "Test account and activity data documentation"
      contains: "parent@test.com"
    - path: ".planning/phases/05-verification-and-test-data/05-VERIFICATION.md"
      provides: "Milestone verification evidence"
      contains: "tests/test_parent_children.py"
---

<objective>
Record final milestone verification and test data evidence, then update requirement statuses.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run final backend and frontend verification</name>
  <action>Run backend parent tests and ruff, frontend build/lint, focused parent Playwright, and frontend demo backend py_compile. Record exact results in `05-VERIFICATION.md`, including any non-failing warnings.</action>
  <verify>
    <automated>uv run --extra dev pytest tests/test_parent_children.py -q</automated>
    <automated>uv run --extra dev ruff check src/stoa/routers/parents.py src/stoa/db/repositories/report_repo.py tests/test_parent_children.py</automated>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
    <automated>npx playwright test tests/e2e/parent-dashboard.spec.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document test data and update requirement statuses</name>
  <action>Create `05-TEST-DATA.md` with local/demo test account and child/activity data. Update `.planning/REQUIREMENTS.md` checkboxes and traceability statuses for completed Phase 3, Phase 4, and Phase 5 requirements. Keep v2 REPORT requirements out of v1 traceability completion.</action>
  <verify>
    <automated>node ~/.codex/get-shit-done/bin/gsd-tools.cjs validate health</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Final backend and frontend verification evidence is recorded.
- Test data documentation exists.
- Requirements are marked complete where evidence exists.
</success_criteria>

<output>
Create `.planning/phases/05-verification-and-test-data/05-02-SUMMARY.md` when done.
</output>
