---
phase: 13-frontend-report-state-verification
plan: 01
subsystem: frontend
status: ready
requirements: [TEST-07, TEST-08]
depends_on:
  - 13-CONTEXT.md
---

# Phase 13 Plan 01: Frontend Report State Verification

## Goal

Ensure frontend tests prove parents can see generated report details, missing report state, and email-failed report state through the real parent report page.

## Tasks

### 1. Strengthen generated report detail assertions

File:

- `/Users/zhdeng/stoa-frontend/tests/e2e/parent-dashboard.spec.ts`

Work:

- Assert generated report detail content beyond summary:
  - week range,
  - stats/metric values,
  - weak topic note,
  - multiple recommendations,
  - teacher note or strength text.
- Keep assertions stable across browser locale/timezone.

Acceptance:

- `TEST-07` has direct Playwright evidence for generated report detail rendering.

### 2. Confirm missing and email-failed states

File:

- `/Users/zhdeng/stoa-frontend/tests/e2e/parent-dashboard.spec.ts`

Work:

- Preserve missing-report test.
- Preserve email-failed generated-state test.
- Keep pending/failed tests as supporting coverage.

Acceptance:

- `TEST-08` has direct Playwright evidence for missing and email-failed states.

### 3. Run frontend verification

Commands:

- `/Users/zhdeng/stoa-frontend`: `npm run test:e2e -- tests/e2e/parent-dashboard.spec.ts`
- `/Users/zhdeng/stoa-frontend`: `npm run lint`

Acceptance:

- Focused Playwright spec passes.
- Frontend lint passes.

## Verification Criteria

- `TEST-07` is satisfied when the generated report e2e test asserts report detail content from the API response.
- `TEST-08` is satisfied when e2e tests cover missing and email-failed states.

## Risks

- Date formatting can vary by browser locale/timezone; avoid exact generated timestamp assertions unless the app provides a stable format.
