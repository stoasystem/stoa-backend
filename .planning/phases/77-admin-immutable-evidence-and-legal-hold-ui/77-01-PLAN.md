# Plan: Phase 77 Admin Immutable Evidence And Legal Hold UI

**Phase:** 77
**Milestone:** v2.7 Immutable Audit Storage And Legal Hold Foundation
**Status:** Complete
**Created:** 2026-06-07

## Objective

Expose Phase 76 immutable evidence and legal hold backend APIs in the admin report operations UI without weakening metadata-only privacy boundaries or implying immutable storage is configured before CDK evidence exists.

## Work Items

1. Add frontend API types and client calls for immutable evidence status/persist and legal hold status/apply.
2. Add React Query mutation hooks.
3. Add an Immutable evidence panel to `/admin/report-operations`.
4. Reuse existing report/job/release evidence reference selection.
5. Add Playwright route mocks and assertions for immutable status, persistence refusal, legal hold controls, copy/download, and privacy denylist.
6. Run frontend lint, build, and targeted e2e.

## Acceptance Criteria

- `UI-14`: admin UI exposes immutable evidence status and manifest persistence status.
- `UI-14`: admin UI exposes legal hold status and apply/release controls with explicit reason fields.
- UI shows CDK-gated/not-configured state when immutable storage config is absent.
- UI does not render private report artifacts, S3 keys, presigned URLs, raw JSON/HTML, auth tokens, cookies, passwords, or AWS secrets.
- Focused Playwright coverage passes.

## Verification

- `npm run lint`
- `npm run build`
- `npm run test:e2e -- tests/e2e/admin-report-operations.spec.ts`

## Production Safety

Phase 77 modifies frontend code only. It does not deploy, mutate production resources, delete audit rows, mutate customer report artifacts, write immutable objects, or perform external support-system writes.
