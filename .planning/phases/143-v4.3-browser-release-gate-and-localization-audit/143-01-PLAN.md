# Plan 143-01: Verify v4.3 And Update Release Documentation

## Tasks

1. Confirm the frontend worktree is clean before release-gate verification.
2. Run frontend lint and production build.
3. Run targeted Playwright evidence:
   - mobile responsive student, parent, tutor, and admin flows
   - authenticated English/German language preference persistence
4. Update roadmap, requirements, project state, milestone index, next milestone queue, and feature gap audit.
5. Create v4.3 archive snapshots for roadmap, requirements, and milestone audit.
6. Run GSD roadmap consistency checks and whitespace checks.
7. Commit the final planning closeout.

## Verification Commands

- `npm run lint`
- `npm run build`
- `npx playwright test tests/e2e/mobile-responsive.spec.ts tests/e2e/localization-preferences.spec.ts --reporter=line`
- `node ~/.codex/get-shit-done/bin/gsd-tools.cjs query roadmap.update-plan-progress 143`
- `node ~/.codex/get-shit-done/bin/gsd-tools.cjs query roadmap.analyze`
- `git diff --check`

