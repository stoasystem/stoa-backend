---
phase: 11-generated-report-api-and-frontend-display
plan: 01
subsystem: backend-frontend
status: ready
requirements: [API-01, API-02, API-03, FRONT-01, FRONT-02, FRONT-03, FRONT-04]
depends_on:
  - 11-CONTEXT.md
---

# Phase 11 Plan 01: Generated Report API and Parent Display

## Goal

Expose generated weekly reports through the real parent portal contract and render generated, missing, and failure states in the parent report page.

## Tasks

### 1. Extend backend parent report response

Files:

- `src/stoa/routers/parents.py`
- `tests/test_parent_children.py`

Work:

- Add response fields for week end, stats, summary, strengths, weak topics with notes, recommendation list, teacher note, generated timestamp, email status, report status, and error fields.
- Preserve existing fields and missing state.
- Map generation/email failure statuses without leaking cross-student data.
- Add focused route/helper tests.

Acceptance:

- Parent report endpoint returns generated detail fields for owning parent/child.
- Missing state remains stable.
- Failed states are visible to owning parent only.

### 2. Update frontend report types and API consumption

Files in `/Users/zhdeng/stoa-frontend`:

- `src/types/parentReport.ts`
- `src/pages/parent/ChildReportPage.tsx`
- `tests/e2e/parent-dashboard.spec.ts`

Work:

- Extend TypeScript report types to match backend response.
- Render generated summary, week range, stats, weak topics, recommendations, generated timestamp, and email status.
- Preserve missing state.
- Update existing e2e mocks/assertions for generated report and email-failed state.

Acceptance:

- Page no longer depends on mock-only rich report assumptions.
- Email failed/pending/sent status is visible where useful.

### 3. Verify

Commands:

- Backend: `uv run --extra dev pytest tests/test_parent_children.py -q`
- Backend lint: `uv run --extra dev ruff check src/stoa/routers/parents.py tests/test_parent_children.py`
- Frontend: `npm run build`
- Frontend e2e focused if practical: `npm run test:e2e -- tests/e2e/parent-dashboard.spec.ts`

## Verification Criteria

- `API-01` is satisfied when generated report detail contains week range, stats, summary, weak topics, recommendations, generated timestamp, and email status.
- `API-02` is satisfied when missing state remains clear.
- `API-03` is satisfied when failure states are exposed only after ownership checks.
- `FRONT-01` is satisfied when the report page renders generated summary, week range, stats, weak topics, recommendations, and generated timestamp.
- `FRONT-02` is satisfied when email status is visible.
- `FRONT-03` is satisfied when missing report state remains visible.
- `FRONT-04` is satisfied when the page uses the real parent report response, not legacy rich mock-only data.

## Risks

- Frontend edits are in sibling repo `/Users/zhdeng/stoa-frontend`; patch application requires escalated filesystem access.
- Existing e2e setup may require a dev server or browser dependencies; record if verification cannot run.
