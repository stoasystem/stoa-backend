---
phase: 04-frontend-parent-portal-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - /Users/zhdeng/stoa-frontend/src/components/parent/ChildCard.tsx
  - /Users/zhdeng/stoa-frontend/src/components/parent/ChildLearningHistoryList.tsx
  - /Users/zhdeng/stoa-frontend/src/pages/parent/ParentDashboardPage.tsx
  - /Users/zhdeng/stoa-frontend/src/pages/parent/ParentReportsPage.tsx
  - /Users/zhdeng/stoa-frontend/src/pages/parent/ChildSummaryPage.tsx
  - /Users/zhdeng/stoa-frontend/src/pages/parent/ChildLearningHistoryPage.tsx
  - /Users/zhdeng/stoa-frontend/src/pages/parent/ChildReportPage.tsx
autonomous: true
requirements:
  - FRONT-02
  - FRONT-03
  - FRONT-04
  - FRONT-05
user_setup:
  - "Frontend repository write access is required because the active Codex writable root is /Users/zhdeng/stoa-backend."
must_haves:
  truths:
    - "Parent dashboard renders backend `subjects` and no-child state without hard-coded child fallback."
    - "Child summary renders Phase 3 counters, weak-topic strings, and recent activity with stable empty states."
    - "History renders backend events and `{ items: [] }` cleanly."
    - "Weekly report page renders both `available` and `missing` report states."
  artifacts:
    - path: "/Users/zhdeng/stoa-frontend/src/pages/parent/ParentDashboardPage.tsx"
      provides: "Dashboard backend child-list rendering without hard-coded child fallback"
      contains: "useParentChildrenQuery"
    - path: "/Users/zhdeng/stoa-frontend/src/pages/parent/ChildSummaryPage.tsx"
      provides: "Phase 3 child summary rendering"
      contains: "questionsAskedThisWeek"
    - path: "/Users/zhdeng/stoa-frontend/src/pages/parent/ChildReportPage.tsx"
      provides: "Available/missing weekly report rendering"
      contains: "missing"
---

<objective>
Update parent portal pages and components to render Phase 3 backend data shapes with explicit empty and error states.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update dashboard and child summary surfaces</name>
  <action>Change child cards and report hub cards to use `subjects` instead of `primarySubjects`. Remove `children[0]?.id ?? "user-student"` behavior from the dashboard; only query child-specific practice summary when a real child exists. Update the child summary page to render summary counters, weak-topic strings, recent activity, and empty states directly from Phase 3 response data.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update history and report surfaces</name>
  <action>Update the history list/page to accept Phase 3 activity events with nullable subject and type labels. Update the weekly report page to consume `ParentChildReportState`: render compact available report data and an explicit missing-report state using the backend message. Keep existing page chrome and navigation intact; avoid broad visual redesign.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Parent pages compile against Phase 3 service types.
- Empty, missing, and error states are visible instead of replaced by mock data.
- Existing parent navigation remains intact.
</success_criteria>

<output>
Create `.planning/phases/04-frontend-parent-portal-integration/04-02-SUMMARY.md` when done.
</output>
