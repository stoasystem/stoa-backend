---
phase: 04-frontend-parent-portal-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/zhdeng/stoa-frontend/src/types/parent.ts
  - /Users/zhdeng/stoa-frontend/src/types/parentReport.ts
  - /Users/zhdeng/stoa-frontend/src/services/parent/parentApi.ts
  - /Users/zhdeng/stoa-frontend/src/services/parent/parentReportApi.ts
  - /Users/zhdeng/stoa-frontend/backend/app/main.py
  - /Users/zhdeng/stoa-frontend/src/data/phase11MockData.ts
autonomous: true
requirements:
  - FRONT-01
  - FRONT-04
  - FRONT-05
  - FRONT-06
user_setup:
  - "Frontend repository write access is required because the active Codex writable root is /Users/zhdeng/stoa-backend."
must_haves:
  truths:
    - "Parent service calls for children, summary, history, and weekly report use `/parents/me/...` without `withDemoFallback`."
    - "Frontend types match Phase 3 backend response shapes."
    - "Local demo backend parent endpoints return Phase 3-compatible shapes for Playwright verification."
  artifacts:
    - path: "/Users/zhdeng/stoa-frontend/src/types/parent.ts"
      provides: "Phase 3 child, summary, activity, and history types"
      contains: "ParentChildActivity"
    - path: "/Users/zhdeng/stoa-frontend/src/types/parentReport.ts"
      provides: "Phase 3 available/missing report state types"
      contains: "ParentChildReportState"
    - path: "/Users/zhdeng/stoa-frontend/src/services/parent/parentApi.ts"
      provides: "Parent API calls without silent demo fallback"
      contains: "getParentChildren"
    - path: "/Users/zhdeng/stoa-frontend/backend/app/main.py"
      provides: "Local demo backend contract aligned with Phase 3 parent routes"
      contains: "/parents/me/children/{child_id}/report"
---

<objective>
Align frontend service contracts and the local demo backend to the Phase 3 parent route shapes.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update parent frontend types and services</name>
  <action>Replace old parent service response types with Phase 3 backend shapes. Remove `withDemoFallback` from normal parent child, summary, history, and weekly report service functions. Keep service URLs under `/parents/me/...`. Add a report state type with `status: "available" | "missing"`, compact report detail, optional message, and nullable report. Do not change unrelated billing, practice, admin, organization, or monthly report fallback behavior in this plan.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align the local frontend demo backend parent endpoints</name>
  <action>Update `/Users/zhdeng/stoa-frontend/backend/app/main.py` parent children, summary, history, and report endpoints to return Phase 3-compatible shapes. Report missing state should return 200 with `status: "missing"`, not 404, for `/parents/me/children/{child_id}/report`. Preserve ownership checks and unrelated demo backend routes.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>npx playwright test tests/e2e/parent-dashboard.spec.ts</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Frontend parent service contracts compile against Phase 3 backend shapes.
- Parent-critical service functions no longer mask backend failures with demo data.
- Local demo backend supports the same response shapes for focused Playwright verification.
</success_criteria>

<output>
Create `.planning/phases/04-frontend-parent-portal-integration/04-01-SUMMARY.md` when done.
</output>
