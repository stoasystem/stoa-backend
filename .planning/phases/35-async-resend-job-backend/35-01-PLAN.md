# Phase 35 Plan: Async Resend Job Backend

**Milestone:** v1.6 Report Recovery Operations Hardening
**Phase:** 35
**Plan:** 35-01
**Status:** Complete

## Scope

Deliver JOB-01 through JOB-09 and finish AUDIT-05 cancelled recovery-path coverage.

## Tasks

1. Add repository helpers for recovery job summary rows, target snapshots, result updates, cancellation claims, and job pagination tokens.
2. Add `report_recovery_job_service` for dry-run preview, preview confirmation, async job creation, Lambda invocation, worker execution, and cooperative cancellation.
3. Add admin APIs for preview/create/list/detail/results/cancel.
4. Route `report_recovery_resend_email` events in the weekly report Lambda handler.
5. Reuse `report_recovery_service.resend_report_email` for actual sends and audit evidence.
6. Add CDK permission for `stoa-api` to invoke `stoa-weekly-report` and configure the function name in API Lambda env.
7. Verify admin authorization, metadata-only boundaries, idempotent claims, cancellation, pagination, and CDK synth.

## Verification

- `uv run pytest -q`
- `uv run ruff check ...`
- `python scripts/build_lambda_dist.py`
- `python scripts/build_lambda_dist.py --verify-only`
- `python -m compileall stacks/api_stack.py`
- `uv run cdk synth StoaApiStack --context env=dev`
