---
phase: 224
plan: 01
status: complete
---

# Plan 224-01: Parent Account Operations UI

## Goal

Let parents see the account operations state exposed by v5.9 without demo fallback.

## Tasks

1. Add account operations types and API client.
   - Define parent account operations, billing, child row, usage, verification, and support state types.
   - Add `getParentAccountOperations`.
   - Add `parentQueryKeys.accountOperations()`.
   - Add `useParentAccountOperationsQuery`.

2. Add UI helpers/components.
   - Add code-to-copy helpers for blockers/warnings and status formatting.
   - Add compact support state rendering.
   - Add child operations row rendering.

3. Add parent detail page and route.
   - Create `ParentAccountOperationsPage`.
   - Add `/parent/account-operations` route under parent role route.
   - Render loading/error/ready/attention/blocked/no-child states.

4. Add dashboard entry.
   - Add a compact dashboard card that links to the account operations page.
   - Reuse the same query without hiding API failures behind fallback.

5. Add focused e2e coverage.
   - Add account operations route intercept helpers.
   - Cover ready, attention, blocked, no-child, API-error, and dashboard link states.

6. Verify and document.
   - Run frontend lint/build and focused parent e2e.
   - Write summary and verification.
   - Update roadmap, requirements, and state.

## Verification

- `npm run lint` — passed
- `npm run build` — passed with existing Vite large chunk warning
- `npx playwright test tests/e2e/parent-account-operations.spec.ts` — passed, 5 tests
