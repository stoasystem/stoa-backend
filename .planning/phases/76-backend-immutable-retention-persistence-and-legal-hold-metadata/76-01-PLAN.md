# Plan: Phase 76 Backend Immutable Retention Persistence And Legal Hold Metadata

**Phase:** 76
**Milestone:** v2.7 Immutable Audit Storage And Legal Hold Foundation
**Status:** Complete
**Created:** 2026-06-07

## Objective

Add backend metadata foundations for immutable evidence persistence and legal hold administration while preserving fail-closed CDK gates and metadata-only privacy boundaries.

## Work Items

1. Add immutable audit storage settings with disabled defaults.
2. Add repository methods for immutable manifest references and legal hold metadata/audit rows.
3. Extend audit retention service with immutable status, immutable manifest persistence attempt, legal hold status, and legal hold apply/release functions.
4. Add admin-only router request models and endpoints.
5. Add focused backend tests for authorization, missing CDK config refusal, configured persistence, legal hold metadata, audit rows, and privacy denylist behavior.
6. Run focused tests and ruff.

## Acceptance Criteria

- `IMMUTABLE-02`: backend can produce immutable evidence status and persist sanitized manifest references when CDK-managed config is present.
- `IMMUTABLE-02`: backend refuses immutable persistence with `not_configured` when CDK-managed storage config is absent.
- `LEGALHOLD-01`: backend can apply and inspect metadata-only legal hold state with append-only audit rows.
- Admin APIs never return private report artifacts, S3 keys, presigned URLs, raw JSON/HTML, auth tokens, cookies, passwords, or AWS secrets.
- Focused backend tests cover the new APIs and privacy boundaries.

## Verification

- `ruff check src/stoa/services/report_audit_retention_service.py src/stoa/routers/admin.py src/stoa/db/repositories/report_repo.py tests/test_admin_report_ops.py`
- `PYTHONPATH=src .venv/bin/pytest tests/test_admin_report_ops.py -k "audit_retention or immutable or legal_hold"`

## Production Safety

Phase 76 does not deploy, mutate production resources, delete audit rows, mutate customer report artifacts, or write immutable objects in production. Immutable persistence remains disabled until CDK-managed storage configuration exists.
