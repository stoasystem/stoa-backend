---
phase: 474-deterministic-verification-and-gated-delivery
plan: 87
type: execute
wave: 11
depends_on: [474-23, 474-82, 474-83, 474-84, 474-86]
files_modified:
  - "/Users/zhdeng/stoa-frontend/scripts/verify-release.mjs"
  - "/Users/zhdeng/stoa-frontend/schemas/release/web-gate-run-v1.schema.json"
  - "/Users/zhdeng/stoa-frontend/tests/release/verify-release.test.mjs"
  - "/Users/zhdeng/stoa-frontend/package.json"
autonomous: true
requirements: [V9QUAL-01]
must_haves:
  truths:
    - "The Web subordinate verifier starts only in an exact checkout with no node_modules or dist and installs only from the committed package-lock.json with npm ci."
    - "Node 20 lint, typecheck, production build, and release-contract checks emit one closed subordinate input-bound receipt without serializing command output or environment values; Plan 88 adds candidate binding."
    - "The verifier cannot use npx, a warm dependency tree, an alternate lock, mobile/native source, or a successful partial gate as PASS."
  artifacts:
    - path: "/Users/zhdeng/stoa-frontend/scripts/verify-release.mjs"
      provides: "Fresh exact Web verification runner"
    - path: "/Users/zhdeng/stoa-frontend/schemas/release/web-gate-run-v1.schema.json"
      provides: "Closed Web gate-run receipt contract"
    - path: "/Users/zhdeng/stoa-frontend/tests/release/verify-release.test.mjs"
      provides: "Fresh-install, ordering, tamper, and privacy regressions"
---

<objective>
Create one exact fresh-install Web subordinate verifier.

Purpose: make the committed frontend lock and source executable as a single backend-callable obligation without trusting developer node_modules.
Output: one Node 20 verifier, one closed receipt schema, and adversarial tests.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Verify Web source from one fresh locked install</name>
  <files>/Users/zhdeng/stoa-frontend/scripts/verify-release.mjs, /Users/zhdeng/stoa-frontend/schemas/release/web-gate-run-v1.schema.json, /Users/zhdeng/stoa-frontend/tests/release/verify-release.test.mjs, /Users/zhdeng/stoa-frontend/package.json</files>
  <behavior>
    - Reject non-Node-20 runtimes, pre-existing/symlinked node_modules or dist, absent/changed package-lock bytes, alternate locks, unknown CLI fields, relative/in-repository outputs, and any failed/omitted/reordered command.
    - Run exact ordered step IDs `frontend-locked-install`, `frontend-eslint`, `frontend-typecheck`, `frontend-build`, and internal `web-release-contracts`; install uses `npm ci --ignore-scripts --no-audit --no-fund`, and the last explicitly enumerates the six pre-Plan-87 release contract files without claiming Plan 24's canonical `frontend-contract` result.
    - Require package-lock bytes unchanged after install and checks, require a nonempty dist free of source maps and forbidden compile-time contact/monitoring configuration, and hash the canonical artifact tree.
    - Emit only Node/npm/platform identity, source/artifact hashes, exact logical argv, exit/status/counts, stdout/stderr hashes, and exact production `NOT RUN`; atomically replace an absolute external output only on a complete result.
  </behavior>
  <read_first>
    - /Users/zhdeng/stoa-frontend/package.json
    - /Users/zhdeng/stoa-frontend/package-lock.json
    - /Users/zhdeng/stoa-frontend/eslint.config.js
    - /Users/zhdeng/stoa-frontend/tsconfig.json
    - /Users/zhdeng/stoa-frontend/scripts/vite.mjs
    - /Users/zhdeng/stoa-frontend/tests/release/runtime-config.test.mjs
    - /Users/zhdeng/stoa-frontend/tests/release/runtime-startup-barrier.test.mjs
    - /Users/zhdeng/stoa-frontend/tests/release/runtime-monitoring-flag.test.mjs
    - /Users/zhdeng/stoa-frontend/tests/release/contact-backend-only.test.mjs
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-86-SUMMARY.md
  </read_first>
  <action>Implement a dependency-light ESM verifier with injectable operations for tests. Pin Node major 20, resolve the active distribution's npm-cli.js, and invoke it through `process.execPath` so no ambient PATH or npm shebang supplies the runtime. Refuse warm/generated trees, alternate package managers/locks, unknown options, internal outputs, or lock drift. Scrub NODE_OPTIONS/NODE_PATH, npm user/global configuration, and VITE_/AWS_ policy or credential variables from subprocesses; set deterministic locale/timezone. Run `npm ci --ignore-scripts --no-audit --no-fund` from the exact committed lock, then the reviewed package scripts for lint, typecheck, build, and the explicitly enumerated six existing release contracts in fixed order. Hash output bytes rather than serializing them; validate every result and the closed receipt before atomic publication. Keep Playwright, OpenAPI adapter semantics, aggregate registration, CI, mobile/native, and production delivery outside this one plan.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; cd /Users/zhdeng/stoa-frontend; /private/tmp/node-v20.20.2-darwin-arm64/bin/node --test tests/release/verify-release.test.mjs; /private/tmp/node-v20.20.2-darwin-arm64/bin/node --test tests/release/*.test.mjs; /private/tmp/node-v20.20.2-darwin-arm64/bin/npm exec -- tsc -b'</automated>
  </verify>
  <acceptance>
    - One fresh lock-derived Web run produces a closed PASS receipt; every partial, warm, drifted, or wrong-runtime run fails closed.
  </acceptance>
  <done>The frontend repository has one exact fresh-install verifier ready for registration by the backend authority.</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Warm developer dependencies substitute for committed lock resolution | Require absent node_modules and run only npm ci before any other gate. |
| npx or alternate lock fetches unreviewed tools | Invoke only installed package scripts/local binaries and reject alternate locks. |
| Partial checks or stale output are mistaken for PASS | Require the fixed complete ordered result set, invalidate prior output, and atomically publish only a validated receipt. |
| Command diagnostics expose secrets or source | Retain only bounded counts and SHA-256 digests of stdout/stderr. |
| Mobile/native source broadens the Web launch gate | Bind only the stoa-frontend root and explicitly reject mobile/native inputs. |
</threat_model>

<verification>
  <automated>bash -lc 'set -euo pipefail; cd /Users/zhdeng/stoa-frontend; /private/tmp/node-v20.20.2-darwin-arm64/bin/node --test tests/release/verify-release.test.mjs; /private/tmp/node-v20.20.2-darwin-arm64/bin/node --test tests/release/*.test.mjs; /private/tmp/node-v20.20.2-darwin-arm64/bin/npm exec -- tsc -b'</automated>
</verification>

<success_criteria>
- The Web verifier proves a fresh npm-ci-derived Node 20 build and release-contract run.
- No developer dependency tree, alternate lock, npx path, mobile source, or partial result can pass.
- Production operations remain exact `NOT RUN`.
</success_criteria>

<output>Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-87-SUMMARY.md` when done.</output>
