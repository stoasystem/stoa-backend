---
phase: 474-deterministic-verification-and-gated-delivery
plan: 86
type: execute
wave: 11
depends_on: [474-85]
files_modified:
  - "scripts/release_gate.py"
  - "tests/test_release_gate.py"
autonomous: true
requirements: [V9QUAL-01]
must_haves:
  truths:
    - "Every gate invocation rejects a candidate that does not exactly match all three live checkouts."
    - "A clean candidate is derived from live HEAD, read-only HEAD/index mode-OID-path projection equality, raw tracked bytes/types/modes, committed/worktree lock bytes, and exact untracked inventory rather than a retained evidence file or filter-sensitive porcelain."
    - "Gate commands execute only from isolated exact-tree checkouts of the candidate commits, never from mutable worktrees."
  artifacts:
    - path: "scripts/release_gate.py"
      provides: "Live three-repository candidate issuer and verifier"
    - path: "tests/test_release_gate.py"
      provides: "Stale, dirty, lock-drift, and wrong-root candidate regressions"
---

<objective>
Reject stale candidate identities before any registered gate command runs.

Purpose: close the gap where an old candidate remained acceptable merely because its commits still existed.
Output: one live candidate issuer and one mandatory exact-checkout validator.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Bind candidates to exact live repository state</name>
  <files>scripts/release_gate.py, tests/test_release_gate.py</files>
  <behavior>
    - A candidate generated from three clean roots passes immediately and remains host-path-free.
    - Any HEAD, read-only HEAD/index projection, committed lock blob, worktree lock bytes, raw tracked byte/type/mode, or untracked path drift rejects before the gate command executes.
    - Backend, frontend, and infra roots cannot be swapped; only a never-tracked literal infra-root `.DS_Store` is excluded from porcelain, while either HEAD or index tracking rejects, and no broader exception exists.
    - `verify` and `self-test` require an explicit candidate and cannot fall back to checked-in historical evidence.
    - Ignored files and concurrent worktree/index/HEAD changes cannot enter command execution because capture is end-bracketed and commands run from exact candidate commit trees.
    - Ambient `GIT_*`, user/system or repository-local Git configuration, hooks, fsmonitor, external index/worktree/object routing, and partial-clone lazy fetch cannot influence candidate capture, checkout materialization, or gate commands.
    - Candidate and receipt outputs must be absolute paths outside all source roots; failed runs remove any previous output and successful writes replace atomically.
  </behavior>
  <read_first>
    - scripts/release_gate.py
    - tests/test_release_gate.py
    - evidence/phase-474/candidate-identity.json
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-01-PLAN.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-85-SUMMARY.md
  </read_first>
  <action>Add a `candidate` CLI subcommand that derives the closed logical candidate object from execution-only roots. Capture one exact HEAD, derive tree and committed lock bytes from that OID, reject nonstandard index flags, require read-only HEAD/index mode-OID-path projection equality, compare every tracked worktree path's raw bytes/type/executable mode against the index through no-follow directory descriptors without invoking filters, compare the worktree lock, and require stable HEAD/index/raw-worktree/untracked/lock observations. Preserve only the already approved exact infra-root `.DS_Store` exclusion. Reject dirty repositories instead of issuing a candidate. Make the public registered-gate entry independently rederive live state, then materialize every committed blob by Git plumbing into an isolated checkout, preserve executable/safe-symlink modes, reject gitlinks, and install an isolated copied candidate-bound object context for Git-dependent tests. Scrub ambient Git routing/configuration from every Git and gate subprocess, explicitly bind worktree-sensitive Git operations to the supplied root, and disable hooks/fsmonitor/untracked-cache shortcuts. Do not use `git archive`, `git status`, `git write-tree`, checkout filters, `export-ignore`, or the mutable source worktree for command execution/input hashing. Require explicit `--candidate`, remove the historical candidate artifact from self-test, require absolute outputs outside all source roots, invalidate an old output before work, and replace successful JSON atomically. Keep production and repository mutations exact `NOT RUN`.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; .venv/bin/python -m pytest -q tests/test_release_gate.py; .venv/bin/ruff check scripts/release_gate.py tests/test_release_gate.py; git diff --check'</automated>
  </verify>
  <acceptance>
    - A stale but reachable commit cannot authorize a gate.
    - Candidate issuance and verification use the same exact live-state projection.
  </acceptance>
  <done>Every registered gate starts only from an explicitly supplied candidate matching all three current clean checkouts.</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Reachable historical commit is presented as current | Recompute and compare exact live HEAD/tree before command execution. |
| Lock bytes differ from committed source | Hash both committed blob bytes and worktree bytes and require equality. |
| Index flags, ignored files, or concurrent changes evade porcelain | Reject nonstandard index flags and execute only an exact plumbing materialization of the captured commit trees. |
| Repository-local symlink or clean-filter configuration hides raw tracked changes or executes during status | Avoid status/diff conversion entirely; compare raw filesystem bytes, types, and modes to stage-zero index blobs and require its read-only projection to equal HEAD. |
| Candidate inspection writes tree objects or index cache state | Compare `ls-tree` and `ls-files --stage` projections; never call `write-tree` or another mutating Git command. |
| Missing promisor objects trigger lazy network fetch or credential helpers | Require no-lazy-fetch on every Git process and deny all Git transport protocols and credential helpers. |
| Repository or user excludes files hide otherwise-untracked source | Force `core.excludesFile` empty while retaining repository-versioned ignore rules and the reviewed `.git/info/exclude` behavior. |
| Case-folding or Unicode-precomposition config changes tracked/untracked classification across hosts | Force exact-case, non-precomposed Git traversal and retain the raw path grammar. |
| Ambient Git environment or repository configuration reroutes the index, worktree, objects, hooks, or fsmonitor | Remove inherited `GIT_*`, isolate global/system configuration, bind each operation to the requested worktree, override repository-local execution features, and test hostile environment/configuration injection. |
| A failed run leaves an earlier PASS output | Remove an allowed external output before validation and atomically replace only on success. |
</threat_model>

<verification>
  <automated>bash -lc 'set -euo pipefail; .venv/bin/python -m pytest -q tests/test_release_gate.py; .venv/bin/ruff check scripts/release_gate.py tests/test_release_gate.py; git diff --check'</automated>
</verification>

<success_criteria>
- Candidate issuance and gate use share one exact live source projection.
- Stale, dirty, swapped, or lock-drifted state fails before command execution.
- Production operations remain exact `NOT RUN`.
</success_criteria>

<output>Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-86-SUMMARY.md` when done.</output>
