---
phase: 474-deterministic-verification-and-gated-delivery
plan: 90
type: execute
wave: 11
depends_on: [474-89]
files_modified:
  - .github/workflows/deploy.yml
  - tests/test_backend_workflow_contract.py
autonomous: true
requirements: [V9QUAL-01, V9QUAL-02]
must_haves:
  truths:
    - "The backend workflow accepts exactly three immutable repository SHAs and invokes only the fixed formal aggregate."
    - "The former direct main-to-Lambda path has no push trigger, credential authority, build, upload, deploy, smoke, or rollback command."
    - "Provider execution is not claimed by local workflow-contract tests and remains exact NOT RUN until separately run."
  artifacts:
    - path: .github/workflows/deploy.yml
      provides: "Thin manual backend formal-verification caller"
    - path: tests/test_backend_workflow_contract.py
      provides: "Closed workflow trigger, ref, action, command, permission, and no-deploy contract"
---

<objective>
Replace the unsafe backend direct deployment workflow with one thin exact-ref caller of the fixed formal aggregate.

Purpose: close the backend CI side of V9QUAL-01 without introducing a second quality graph or authorizing any delivery operation.
Output: one verification-only workflow and one adversarial local contract test. Frontend, infra, source handoff, provider execution, deployment, mobile/native, and production remain outside this plan.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-89-SUMMARY.md
@.github/workflows/deploy.yml
@scripts/release_gate.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace backend deployment with one exact formal caller</name>
  <files>.github/workflows/deploy.yml, tests/test_backend_workflow_contract.py</files>
  <behavior>
    - Permit only manual `workflow_dispatch` with required `backend_sha`, `frontend_sha`, and `infra_sha` strings. Each has no default and must match lowercase 40-hex before checkout; each checkout HEAD must equal the supplied SHA.
    - Pin checkout `de0fac2e4500dabe0009e67214ff5f5447ce83dd`, setup-python `a309ff8b426b58ec0e2a45f0f869d46889d02405`, and setup-uv `08807647e7069bb48b6ef5acd8ec9567f424441b` by full commit; fix Python `3.12.13` and uv `0.11.16`; disable credential persistence and dependency caches.
    - Checkout `stoasystem/stoa-backend`, `stoasystem/stoa-frontend`, and `stoasystem/stoa-infra` into exact sibling paths and verify all three detached identities before execution.
    - Prepare only the ephemeral Linux namespace controls needed by the formal gate and one private `$RUNNER_TEMP` evidence directory. The workflow must not duplicate, weaken, skip, or reorder formal child gates.
    - Invoke exactly `scripts/release_gate.py candidate` followed by `scripts/release_gate.py formal` with all explicit roots and private outputs. Do not invoke pytest, Ruff, mypy, npm, build, synth, audit, or any alternate quality command in the workflow.
    - Use only `contents: read`. Reject id-token/package/action writes, credentials, secrets, AWS/GCP/Azure/provider commands, artifacts, container builds, environment gates, staging, deployment, smoke, rollback, mobile, and native behavior.
    - Local tests parse the workflow structurally and fail on mutable actions/refs, defaults, expression-to-shell injection, missing HEAD checks, alternate commands, broad permissions, new jobs, or mutation vocabulary. They do not claim a provider run.
  </behavior>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_backend_workflow_contract.py &amp;&amp; .venv/bin/ruff check tests/test_backend_workflow_contract.py &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance>
    - Backend CI has one fixed candidate/formal graph and no direct deployment path.
    - No caller input or workflow step can select a weaker gate or a mutable repository ref.
    - Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` with zero calls.
  </acceptance>
  <done>The backend repository exposes only a thin exact-ref formal-verification workflow.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| Mutable or substituted source ref | Three required lowercase 40-hex inputs plus post-checkout exact HEAD equality |
| Mutable or compromised third-party action | Three reviewed full action commit pins and no other actions |
| Workflow reimplements a weaker quality graph | Exactly one candidate command and one formal command; all alternate quality commands forbidden |
| Verification acquires provider or delivery authority | Read-only contents permission and closed no-provider/no-deploy vocabulary |
| Shell interpolation executes an input | Inputs enter shell only through fixed environment variables and are regex-validated before use |
| Provider result is fabricated locally | Contract evidence records workflow structure only; provider execution stays NOT RUN |
</threat_model>

<success_criteria>
- Backend workflow dispatch requires three exact immutable repository commits.
- One pinned runtime runs one checked-in fixed formal aggregate.
- No backend workflow path builds or mutates staging, production, cloud, mobile, or native state.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-90-SUMMARY.md` when done.
</output>
