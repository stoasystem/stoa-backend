---
phase: 474-deterministic-verification-and-gated-delivery
plan: 07
type: execute
wave: 5
depends_on: [474-05]
files_modified:
  - "src/stoa/security/admin_authorization.py"
  - "src/stoa/security/authorization.py"
  - "src/stoa/security/identity.py"
  - "src/stoa/security/jwks.py"
  - "src/stoa/security/public_auth_errors.py"
  - "src/stoa/security/reconciliation.py"
  - "src/stoa/security/route_authorization.py"
  - "src/stoa/security/route_inventory.py"
  - "src/stoa/security/tokens.py"
autonomous: true
requirements: [V9QUAL-04]
must_haves:
  truths:
    - "The auth and security roots slice is fully typed without broad suppression."
    - "Runtime regression behavior is unchanged."
  artifacts:
    - path: "src/stoa/security/admin_authorization.py"
      provides: "Auth and security roots"
    - path: "src/stoa/security/authorization.py"
      provides: "Auth and security roots"
    - path: "src/stoa/security/identity.py"
      provides: "Auth and security roots"
    - path: "src/stoa/security/jwks.py"
      provides: "Auth and security roots"
    - path: "src/stoa/security/public_auth_errors.py"
      provides: "Auth and security roots"
    - path: "src/stoa/security/reconciliation.py"
      provides: "Auth and security roots"
    - path: "src/stoa/security/route_authorization.py"
      provides: "Auth and security roots"
    - path: "src/stoa/security/route_inventory.py"
      provides: "Auth and security roots"
    - path: "src/stoa/security/tokens.py"
      provides: "Remaining owned artifacts for Auth and security roots"
  key_links:
    - from: "474-05"
      to: "src/stoa/security/admin_authorization.py"
      via: "declared dependency and immutable receipt"
      pattern: 'admin_authorization\.py'
---

<objective>
Auth and security roots (D-09, D-10).

Purpose: Close one real mypy root-cause family while retaining existing behavior and direct producer/consumer ordering.
Output: `src/stoa/security/admin_authorization.py`, `src/stoa/security/authorization.py`, `src/stoa/security/identity.py`, `src/stoa/security/jwks.py`, `src/stoa/security/public_auth_errors.py`, `src/stoa/security/reconciliation.py`, `src/stoa/security/route_authorization.py`, `src/stoa/security/route_inventory.py`, `src/stoa/security/tokens.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `src/stoa/security/admin_authorization.py` — Auth and security roots.
- `src/stoa/security/authorization.py` — Auth and security roots.
- `src/stoa/security/identity.py` — Auth and security roots.
- `src/stoa/security/jwks.py` — Auth and security roots.
- `src/stoa/security/public_auth_errors.py` — Auth and security roots.
- `src/stoa/security/reconciliation.py` — Auth and security roots.
- `src/stoa/security/route_authorization.py` — Auth and security roots.
- `src/stoa/security/route_inventory.py` — Auth and security roots.
- `src/stoa/security/tokens.py` — Auth and security roots.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type identity, JWKS, token, and public-auth boundaries</name>
  <files>src/stoa/security/identity.py, src/stoa/security/jwks.py, src/stoa/security/tokens.py, src/stoa/security/public_auth_errors.py</files>
  <behavior>
    - Previously measured diagnostics in the owned file set become zero.
    - Malformed/untrusted values remain explicitly narrowed before use.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - src/stoa/security/identity.py
    - src/stoa/security/jwks.py
    - src/stoa/security/tokens.py
    - src/stoa/security/public_auth_errors.py
  </read_first>
  <action>Repair the measured full-scope mypy errors in the identity, JWKS, token, and public-auth boundaries family, limited to src/stoa/security/identity.py, src/stoa/security/jwks.py, src/stoa/security/tokens.py, src/stoa/security/public_auth_errors.py. Start from the exact 435-error/113-file command and preserve runtime authorization, privacy, idempotency, retry, fence, pagination, and stable-error semantics. Use closed TypedDict/dataclass records, explicit Optional/union narrowing, maintained stubs, or narrow project-owned Protocols at provider boundaries per D-09/D-10. Do not add or broaden Any, casts that hide unvalidated input, type: ignore, excludes, follow_imports=skip, ignore_missing_imports, skips, xfails, retries, or weaker assertions. This task implements D-09, D-10.</action>
  <verify>
    <automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/security/identity.py src/stoa/security/jwks.py src/stoa/security/tokens.py src/stoa/security/public_auth_errors.py &amp;&amp; .venv/bin/python -m pytest -q tests/test_auth_security.py tests/test_public_auth_error_boundary.py &amp;&amp; .venv/bin/ruff check src/stoa/security/identity.py src/stoa/security/jwks.py src/stoa/security/tokens.py src/stoa/security/public_auth_errors.py'</automated>
  </verify>
  <acceptance>
    - The identity, JWKS, token, and public-auth boundaries files pass focused mypy and Ruff with no semantic suppression.
    - Named runtime regressions remain green.
  </acceptance>
  <done>The identity, JWKS, token, and public-auth boundaries files pass focused mypy and Ruff with no semantic suppression. Named runtime regressions remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Type authorization metadata, route inventory, and reconciliation</name>
  <files>src/stoa/security/route_authorization.py, src/stoa/security/admin_authorization.py, src/stoa/security/authorization.py, src/stoa/security/route_inventory.py, src/stoa/security/reconciliation.py</files>
  <behavior>
    - Previously measured diagnostics in the owned file set become zero.
    - Malformed/untrusted values remain explicitly narrowed before use.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - src/stoa/security/route_authorization.py
    - src/stoa/security/admin_authorization.py
    - src/stoa/security/authorization.py
    - src/stoa/security/route_inventory.py
  </read_first>
  <action>Repair the measured full-scope mypy errors in the authorization metadata, route inventory, and reconciliation family, limited to src/stoa/security/route_authorization.py, src/stoa/security/admin_authorization.py, src/stoa/security/authorization.py, src/stoa/security/route_inventory.py, src/stoa/security/reconciliation.py. Start from the exact 435-error/113-file command and preserve runtime authorization, privacy, idempotency, retry, fence, pagination, and stable-error semantics. Use closed TypedDict/dataclass records, explicit Optional/union narrowing, maintained stubs, or narrow project-owned Protocols at provider boundaries per D-09/D-10. Do not add or broaden Any, casts that hide unvalidated input, type: ignore, excludes, follow_imports=skip, ignore_missing_imports, skips, xfails, retries, or weaker assertions. This task implements D-09, D-10.</action>
  <verify>
    <automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/security/route_authorization.py src/stoa/security/admin_authorization.py src/stoa/security/authorization.py src/stoa/security/route_inventory.py src/stoa/security/reconciliation.py &amp;&amp; .venv/bin/python -m pytest -q tests/test_authorization_audit.py tests/test_route_authorization_inventory.py &amp;&amp; .venv/bin/ruff check src/stoa/security/route_authorization.py src/stoa/security/admin_authorization.py src/stoa/security/authorization.py src/stoa/security/route_inventory.py src/stoa/security/reconciliation.py'</automated>
  </verify>
  <acceptance>
    - The authorization metadata, route inventory, and reconciliation files pass focused mypy and Ruff with no semantic suppression.
    - Named runtime regressions remain green.
  </acceptance>
  <done>The authorization metadata, route inventory, and reconciliation files pass focused mypy and Ruff with no semantic suppression. Named runtime regressions remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS / STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-07-01 | High | Tampering / Repudiation | Auth and security roots | mitigate/block | Bind exact source, inputs, command, result, and artifact identities; reject missing, duplicate, unknown, dirty, or mismatched values. |
| T-474-07-02 | High | Spoofing / Elevation | verification-to-delivery boundary | mitigate/block | Apply least privilege, protected environment checks, and dependency-closed eligibility before authority is exposed. |
| T-474-SC | High | Tampering | audited package installs | mitigate/block | Use only packages marked OK in RESEARCH.md Package Legitimacy Audit; [ASSUMED]/[SUS] requires a blocking human checkpoint and [SLOP] is forbidden. |
</threat_model>

<verification>
<automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/security/route_authorization.py src/stoa/security/admin_authorization.py src/stoa/security/authorization.py src/stoa/security/route_inventory.py src/stoa/security/reconciliation.py &amp;&amp; .venv/bin/python -m pytest -q tests/test_authorization_audit.py tests/test_route_authorization_inventory.py &amp;&amp; .venv/bin/ruff check src/stoa/security/route_authorization.py src/stoa/security/admin_authorization.py src/stoa/security/authorization.py src/stoa/security/route_inventory.py src/stoa/security/reconciliation.py'</automated>
</verification>

<success_criteria>
- The auth and security roots slice is fully typed without broad suppression.
- Runtime regression behavior is unchanged.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless a later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-07-SUMMARY.md` when done.
</output>
