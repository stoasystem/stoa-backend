---
phase: 474-deterministic-verification-and-gated-delivery
plan: 17
type: execute
wave: 8
depends_on: [474-12, 474-13, 474-14]
files_modified:
  - "src/stoa/routers/conversations.py"
  - "src/stoa/routers/notifications.py"
  - "src/stoa/routers/parents.py"
autonomous: true
requirements: [V9QUAL-04]
must_haves:
  truths:
    - "The parent conversation and notification routers slice is fully typed without broad suppression."
    - "Runtime regression behavior is unchanged."
  artifacts:
    - path: "src/stoa/routers/conversations.py"
      provides: "Parent conversation and notification routers"
    - path: "src/stoa/routers/notifications.py"
      provides: "Parent conversation and notification routers"
    - path: "src/stoa/routers/parents.py"
      provides: "Parent conversation and notification routers"
  key_links:
    - from: "474-14"
      to: "src/stoa/routers/conversations.py"
      via: "declared dependency and immutable receipt"
      pattern: "conversations\.py"
---

<objective>
Parent conversation and notification routers (D-09, D-10).

Purpose: Close one real mypy root-cause family while retaining existing behavior and direct producer/consumer ordering.
Output: `src/stoa/routers/conversations.py`, `src/stoa/routers/notifications.py`, `src/stoa/routers/parents.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `src/stoa/routers/conversations.py` — Parent conversation and notification routers.
- `src/stoa/routers/notifications.py` — Parent conversation and notification routers.
- `src/stoa/routers/parents.py` — Parent conversation and notification routers.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type parent and conversation routes</name>
  <files>src/stoa/routers/parents.py, src/stoa/routers/conversations.py</files>
  <behavior>
    - Previously measured diagnostics in the owned file set become zero.
    - Malformed/untrusted values remain explicitly narrowed before use.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - src/stoa/routers/parents.py
    - src/stoa/routers/conversations.py
  </read_first>
  <action>Repair the measured full-scope mypy errors in the parent and conversation routes family, limited to src/stoa/routers/parents.py, src/stoa/routers/conversations.py. Start from the exact 435-error/113-file command and preserve runtime authorization, privacy, idempotency, retry, fence, pagination, and stable-error semantics. Use closed TypedDict/dataclass records, explicit Optional/union narrowing, maintained stubs, or narrow project-owned Protocols at provider boundaries per D-09/D-10. Do not add or broaden Any, casts that hide unvalidated input, type: ignore, excludes, follow_imports=skip, ignore_missing_imports, skips, xfails, retries, or weaker assertions. This task implements D-09, D-10.</action>
  <verify>
    <automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/routers/parents.py src/stoa/routers/conversations.py &amp;&amp; .venv/bin/python -m pytest -q tests/test_parent_children.py tests/test_conversations.py &amp;&amp; .venv/bin/ruff check src/stoa/routers/parents.py src/stoa/routers/conversations.py'</automated>
  </verify>
  <acceptance>
    - The parent and conversation routes files pass focused mypy and Ruff with no semantic suppression.
    - Named runtime regressions remain green.
  </acceptance>
  <done>The parent and conversation routes files pass focused mypy and Ruff with no semantic suppression. Named runtime regressions remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Type notification routes</name>
  <files>src/stoa/routers/notifications.py</files>
  <behavior>
    - Previously measured diagnostics in the owned file set become zero.
    - Malformed/untrusted values remain explicitly narrowed before use.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - src/stoa/routers/notifications.py
  </read_first>
  <action>Repair the measured full-scope mypy errors in the notification routes family, limited to src/stoa/routers/notifications.py. Start from the exact 435-error/113-file command and preserve runtime authorization, privacy, idempotency, retry, fence, pagination, and stable-error semantics. Use closed TypedDict/dataclass records, explicit Optional/union narrowing, maintained stubs, or narrow project-owned Protocols at provider boundaries per D-09/D-10. Do not add or broaden Any, casts that hide unvalidated input, type: ignore, excludes, follow_imports=skip, ignore_missing_imports, skips, xfails, retries, or weaker assertions. This task implements D-09, D-10.</action>
  <verify>
    <automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/routers/notifications.py &amp;&amp; .venv/bin/python -m pytest -q tests/test_phase473_notification_deletion.py tests/test_phase473_private_delivery_fencing.py &amp;&amp; .venv/bin/ruff check src/stoa/routers/notifications.py'</automated>
  </verify>
  <acceptance>
    - The notification routes files pass focused mypy and Ruff with no semantic suppression.
    - Named runtime regressions remain green.
  </acceptance>
  <done>The notification routes files pass focused mypy and Ruff with no semantic suppression. Named runtime regressions remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS / STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-17-01 | High | Tampering / Repudiation | Parent conversation and notification routers | mitigate/block | Bind exact source, inputs, command, result, and artifact identities; reject missing, duplicate, unknown, dirty, or mismatched values. |
| T-474-17-02 | High | Spoofing / Elevation | verification-to-delivery boundary | mitigate/block | Apply least privilege, protected environment checks, and dependency-closed eligibility before authority is exposed. |
| T-474-SC | High | Tampering | audited package installs | mitigate/block | Use only packages marked OK in RESEARCH.md Package Legitimacy Audit; [ASSUMED]/[SUS] requires a blocking human checkpoint and [SLOP] is forbidden. |
</threat_model>

<verification>
<automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/routers/notifications.py &amp;&amp; .venv/bin/python -m pytest -q tests/test_phase473_notification_deletion.py tests/test_phase473_private_delivery_fencing.py &amp;&amp; .venv/bin/ruff check src/stoa/routers/notifications.py'</automated>
</verification>

<success_criteria>
- The parent conversation and notification routers slice is fully typed without broad suppression.
- Runtime regression behavior is unchanged.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless a later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-17-SUMMARY.md` when done.
</output>
