---
phase: 474-deterministic-verification-and-gated-delivery
plan: 82
type: execute
wave: 9
depends_on: [474-73, 474-81]
files_modified:
  - "/Users/zhdeng/stoa-frontend/src/bootstrap.ts"
  - "/Users/zhdeng/stoa-frontend/src/main.tsx"
  - "/Users/zhdeng/stoa-frontend/tests/release/runtime-startup-barrier.test.mjs"
autonomous: true
requirements: [V9QUAL-03, V9QUAL-06]
must_haves:
  truths:
    - "No React tree, API/auth client, monitoring, or route module initializes before descriptor and runtime-config validation succeeds."
    - "Startup failure renders a bounded safe failure state and publishes no partial config."
  artifacts:
    - path: "/Users/zhdeng/stoa-frontend/src/bootstrap.ts"
      provides: "Descriptor-to-config startup barrier"
    - path: "/Users/zhdeng/stoa-frontend/src/main.tsx"
      provides: "Barrier-first browser entry point"
    - path: "/Users/zhdeng/stoa-frontend/tests/release/runtime-startup-barrier.test.mjs"
      provides: "Import-order and failure-state tests"
  key_links:
    - from: "474-73"
      to: "/Users/zhdeng/stoa-frontend/src/bootstrap.ts"
      via: "served descriptor supplies exact runtime config expectations"
      pattern: 'loadServedRelease|initializeRuntimeConfig'
---

<objective>
Install a fail-closed Web startup barrier before application/service imports.

Purpose: Make validated descriptor and runtime configuration a hard prerequisite for the browser application rather than an asynchronous side effect.
Output: one bootstrap coordinator, a barrier-first entry point, and import-order tests.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Gate Web startup on descriptor and config validation</name>
  <files>/Users/zhdeng/stoa-frontend/src/bootstrap.ts, /Users/zhdeng/stoa-frontend/src/main.tsx, /Users/zhdeng/stoa-frontend/tests/release/runtime-startup-barrier.test.mjs</files>
  <behavior>
    - Startup fetches/validates the served descriptor, then fetches/validates its exact runtime config, then dynamically loads the application graph.
    - Descriptor/config failure, mismatch, timeout, or duplicate initialization renders a generic actionable failure view without importing or starting App/API/auth/monitoring modules.
    - A successful second candidate cannot replace the installed release during one page lifetime.
  </behavior>
  <read_first>
    - /Users/zhdeng/stoa-frontend/src/lib/servedRelease.ts
    - /Users/zhdeng/stoa-frontend/src/lib/runtimeConfig.ts
    - /Users/zhdeng/stoa-frontend/src/bootstrap.ts
    - /Users/zhdeng/stoa-frontend/src/main.tsx
    - /Users/zhdeng/stoa-frontend/tests/release/runtime-startup-barrier.test.mjs
  </read_first>
  <action>Implement one startup coordinator that resolves `/served-release.json`, passes its exact release identities and runtime-config SHA to the Plan 72 loader, and only after registry installation dynamically imports/render-starts the application graph. Keep diagnostics generic and credential-free. Do not activate Amplify/Cognito, change environment projection semantics, decide monitoring policy, alter contact delivery, or touch mobile.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail &amp;&amp; cd /Users/zhdeng/stoa-frontend &amp;&amp; node --version | grep -Eq "^v20\." &amp;&amp; node --test tests/release/runtime-startup-barrier.test.mjs &amp;&amp; npm exec -- tsc -b'</automated>
  </verify>
  <acceptance>
    - Static startup cannot import the application/service graph before validated installation.
    - Failure leaves no partially usable application or configuration.
  </acceptance>
  <done>Browser startup is blocked until the exact served release configuration is installed.</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Service initializes with default or stale configuration | Dynamic application import occurs only after exact descriptor/config installation. |
| Failure leaks provider or object coordinates | Render only stable safe startup errors. |
</threat_model>

<verification>
  <automated>bash -lc 'set -euo pipefail &amp;&amp; cd /Users/zhdeng/stoa-frontend &amp;&amp; node --version | grep -Eq "^v20\." &amp;&amp; node --test tests/release/runtime-startup-barrier.test.mjs &amp;&amp; npm exec -- tsc -b'</automated>
</verification>

<success_criteria>
- Descriptor/config validation precedes all application services.
- Node 20 tests and TypeScript pass.
- Production operations remain exact `NOT RUN`.
</success_criteria>

<output>Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-82-SUMMARY.md` when done.</output>
