---
phase: 474-deterministic-verification-and-gated-delivery
plan: 22
type: execute
wave: 7
depends_on: [474-07, 474-08, 474-09, 474-10, 474-39, 474-40, 474-41, 474-42]
files_modified:
  - pyproject.toml
  - scripts/source_handoff.py
  - scripts/release_gate.py
  - schemas/release/quality-repair-report-v1.schema.json
  - tests/test_quality_gate_policy.py
autonomous: true
requirements: [V9QUAL-04]
must_haves:
  truths:
    - "Ruff and the exact full-repository mypy command reach zero without a baseline, broad Any, exclusions, or suppressions."
    - "The canonical release gate runs the same source scope and fails closed on any diagnostic or scope drift."
  artifacts:
    - path: schemas/release/quality-repair-report-v1.schema.json
      provides: "Closed current-source Ruff/mypy result contract"
    - path: tests/test_quality_gate_policy.py
      provides: "Zero-result, scope-drift, and semantic-weakening controls"
    - path: scripts/release_gate.py
      provides: "Canonical repository-wide quality gate registration"
  key_links:
    - from: scripts/release_gate.py
      to: tests/test_quality_gate_policy.py
      via: "exact registered command and result contract"
      pattern: "mypy"
---

<objective>
Close the current repository-wide Ruff and mypy gate in one bounded plan.

Purpose: replace the obsolete 40-plan decomposition based on the historical
435-error snapshot with one source-current zero-error obligation.
Output: a reproducible full-scope zero gate and a source-bound failure report
for any remaining diagnostic.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CLEANUP.md
</context>

<tasks>

<task type="auto" id="474-22-01" tdd="true">
  <name>Close the current full-repository Ruff and mypy gate</name>
  <files>pyproject.toml, scripts/source_handoff.py, scripts/release_gate.py, schemas/release/quality-repair-report-v1.schema.json, tests/test_quality_gate_policy.py</files>
  <read_first>
    - pyproject.toml
    - scripts/source_handoff.py
    - scripts/release_gate.py
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CLEANUP.md
  </read_first>
  <behavior>
    - The exact full command checks `src/stoa`, `scripts`, and `tests` with no incremental cache.
    - Missing stubs, duplicate module identities, import-path drift, and every subsequently revealed diagnostic are repaired at source.
    - The gate rejects ignore growth, excluded files, alternate reduced scopes, or a documented nonzero baseline.
  </behavior>
  <action>
Run the exact command before editing and retain its result as the starting
receipt. On 2026-07-30 the command stopped at three structural blockers:
`release_gate` import resolution, missing PyYAML stubs, and duplicate test
module identity. Repair those without weakening policy, rerun to reveal the
complete current diagnostic set, and continue until Ruff and mypy both reach
zero. Register the identical commands and source scope in the canonical release
gate and add false-pass tests for exclusions, ignores, alternate paths, and
nonzero residual reports.
  </action>
  <verify>
    <automated>.venv/bin/python -m ruff check src tests scripts --no-cache &amp;&amp; MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa scripts tests &amp;&amp; .venv/bin/python -m pytest -q tests/test_quality_gate_policy.py</automated>
  </verify>
  <acceptance_criteria>
    - Ruff exits zero.
    - The exact full-repository mypy command exits zero.
    - No baseline, broad suppression, excluded family, or alternate reduced command can pass.
    - The release gate and standalone verification execute the same scope.
  </acceptance_criteria>
  <done>The current source tree has one reproducible Ruff/mypy-zero gate rather than forty stale file-family plans.</done>
</task>

</tasks>

<verification>
The exact Ruff, mypy, and quality-policy commands pass from a clean checkout.
</verification>

<success_criteria>
- The current full source scope reaches true zero.
- Any future diagnostic or scope drift blocks release.
- The obsolete historical error-family decomposition is no longer part of execution.
</success_criteria>

<output>Create `474-22-SUMMARY.md` after verification.</output>
