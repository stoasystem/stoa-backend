---
phase: 474-deterministic-verification-and-gated-delivery
plan: 22
type: execute
wave: 10
depends_on: [474-18, 474-56, 474-57, 474-19, 474-58, 474-59, 474-60, 474-61, 474-62, 474-20, 474-63, 474-64, 474-65, 474-66, 474-21, 474-67, 474-68, 474-69, 474-70, 474-71]
files_modified:
  - "schemas/release/quality-repair-report-v1.schema.json"
  - "tests/test_quality_gate_policy.py"
  - "scripts/release_gate.py"
autonomous: true
requirements: [V9QUAL-04]
must_haves:
  truths:
    - "Ruff and the exact full-repository mypy command both reach zero before delivery can continue."
    - "Any residual is source-bound, release-blocking, and returned to the owner without a preplanned baseline checkpoint."
  artifacts:
    - path: "schemas/release/quality-repair-report-v1.schema.json"
      provides: "Automated repository-wide mypy-zero gate and report (D-09, D-10)"
    - path: "tests/test_quality_gate_policy.py"
      provides: "Automated repository-wide mypy-zero gate and report (D-09, D-10)"
    - path: "scripts/release_gate.py"
      provides: "Automated repository-wide mypy-zero gate and report (D-09, D-10)"
  key_links:
    - from: "474-71"
      to: "schemas/release/quality-repair-report-v1.schema.json"
      via: "declared dependency and source-bound verification"
      pattern: 'quality\-repair\-report\-v1\.schema\.json'
---

<objective>
Automated repository-wide mypy-zero gate and report (D-09, D-10).

Purpose: Accept only Ruff zero and true repository-wide mypy zero; emit a blocking residual report otherwise.
Output: `schemas/release/quality-repair-report-v1.schema.json`, `tests/test_quality_gate_policy.py`, `scripts/release_gate.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `schemas/release/quality-repair-report-v1.schema.json` — Automated repository-wide mypy-zero gate and report (D-09, D-10)
- `tests/test_quality_gate_policy.py` — Automated repository-wide mypy-zero gate and report (D-09, D-10)
- `scripts/release_gate.py` — Automated repository-wide mypy-zero gate and report (D-09, D-10)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce exact full scope and reject semantic weakening</name>
  <files>schemas/release/quality-repair-report-v1.schema.json, tests/test_quality_gate_policy.py</files>
  <behavior>
    - True zero passes.
    - Suppression or unbound residual reports fail.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - schemas/release/quality-repair-report-v1.schema.json
    - tests/test_quality_gate_policy.py
  </read_first>
  <action>Create a closed report schema and tests that pin MYPYPATH=src:tests python -m mypy --no-incremental --explicit-package-bases src/stoa scripts tests, Ruff source scope, measured starting diagnostics, repaired file families, tool/lock/source identities, and exact residuals. Reject new/broadened Any, ignore, exclude, skip, retry, or weakened assertion per D-09/D-10. This task implements D-09, D-10.</action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_quality_gate_policy.py</automated>
  </verify>
  <acceptance>
    - The original 435-error zero attempt is represented as a closed obligation.
    - The gate cannot green by baseline or semantic omission.
  </acceptance>
  <done>The original 435-error zero attempt is represented as a closed obligation. The gate cannot green by baseline or semantic omission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register Ruff, mypy, and typing policy in the canonical gate</name>
  <files>scripts/release_gate.py, tests/test_quality_gate_policy.py</files>
  <behavior>
    - Full zero emits PASS.
    - Residual or scope drift emits a source-bound failure report.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - scripts/release_gate.py
    - tests/test_quality_gate_policy.py
  </read_first>
  <action>Run Ruff zero and the exact complete mypy command after every typing family, bind source/tool/lock/diagnostic identities, and produce PASS only at true zero. If residuals remain, produce exact FAILED_ZERO_ATTEMPT with root cause and concrete repair cost; never silently freeze the historical count per D-09. This task implements D-09, D-10.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m ruff check src tests scripts --no-cache &amp;&amp; MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa scripts tests &amp;&amp; .venv/bin/python -m pytest -q tests/test_quality_gate_policy.py'</automated>
  </verify>
  <acceptance>
    - True repository-wide zero is attempted and preferred.
    - Any exact residual remains release-blocking and is returned to the owner after execution.
  </acceptance>
  <done>True repository-wide zero is attempted and preferred. Any exact residual remains release-blocking and is returned to the owner after execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS V1-V14 Applicability

| ASVS | Control family | Disposition | Plan control |
|---|---|---|---|
| V1 | Architecture | apply | Keep the plan dependency-closed, source-bound, and independently verifiable. |
| V2 | Authentication | preserve | Do not introduce an alternate identity mechanism; smoke uses existing bounded auth. |
| V3 | Session Management | preserve | Do not broaden session lifetime or expose browser/session material in evidence. |
| V4 | Access Control | apply | Use least privilege and protected environment/OIDC boundaries before authority. |
| V5 | Validation | apply | Validate paths, refs, schemas, provider values, counts, and closed outcomes. |
| V6 | Cryptography | apply | Use SHA-256/AWS-managed encryption only; reject digest mismatch. |
| V7 | Error/Logging | apply | Emit stable redacted errors and identifiers without credentials or private payloads. |
| V8 | Data Protection | apply | Retain only required hashes, IDs, statuses, and policy evidence. |
| V9 | Communications | apply | Deny ambient network and constrain provider calls to reviewed endpoints/roles. |
| V10 | Malicious Code | apply | Use audited locks, immutable action SHAs, and tamper tests. |
| V11 | Business Logic | apply | Fail closed on missing prerequisites, stale identity, or partial outcome. |
| V12 | Files/Resources | apply | Use bounded paths, immutable blobs/objects, and resource limits. |
| V13 | API/Web Services | apply | Validate structured API/provider responses and preserve authorization semantics. |
| V14 | Configuration | apply | Use reviewed closed configuration with explicit production NOT RUN state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-474-22-S | Spoofing | source/provider identity | mitigate/block | Require exact execution-derived Git/provider identity and authorized actor/role. |
| T-474-22-T | Tampering | Automated repository-wide mypy-zero gate and report (D-09, D-10) | mitigate/block | Bind inputs, commands, results, and artifacts; reject mismatch, drift, or unknown fields. |
| T-474-22-R | Repudiation | evidence receipt | mitigate/block | Retain canonical source-bound command/result/request identities. |
| T-474-22-I | Information Disclosure | diagnostics/evidence | mitigate/block | Store redacted IDs, hashes, counts, and statuses only. |
| T-474-22-D | Denial of Service | bounded execution | mitigate/block | Apply bounded time/resource scopes and fail closed without partial PASS. |
| T-474-22-E | Elevation of Privilege | verification-to-delivery boundary | mitigate/block | Use least privilege, dependency-closed eligibility, and protected environments. |
| T-474-22-SC | Tampering | package/action supply chain | mitigate/block | Use only RESEARCH-approved packages and immutable reviewed Action SHAs; [SLOP] is forbidden. |
</threat_model>

<verification>
  <automated>.venv/bin/python -m pytest -q tests/test_quality_gate_policy.py</automated>
  <automated>bash -lc '.venv/bin/python -m ruff check src tests scripts --no-cache &amp;&amp; MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa scripts tests &amp;&amp; .venv/bin/python -m pytest -q tests/test_quality_gate_policy.py'</automated>
</verification>

<success_criteria>
- Ruff and the exact full-repository mypy command both reach zero before delivery can continue.
- Any residual is source-bound, release-blocking, and returned to the owner without a preplanned baseline checkpoint.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-22-SUMMARY.md` when done.
</output>
