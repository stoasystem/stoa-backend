---
phase: 474-deterministic-verification-and-gated-delivery
plan: 22
type: execute
wave: 10
depends_on: [474-18, 474-19, 474-20, 474-21]
files_modified:
  - "schemas/release/quality-repair-report-v1.schema.json"
  - "scripts/release_gate.py"
  - "tests/test_quality_gate_policy.py"
autonomous: false
requirements: [V9QUAL-04]
must_haves:
  truths:
    - "Ruff is zero and the complete repository mypy command is attempted after all families."
    - "Only the owner can accept an exact irreducible temporary residual."
  artifacts:
    - path: "schemas/release/quality-repair-report-v1.schema.json"
      provides: "Full mypy-zero quality gate and conditional owner disposition"
    - path: "scripts/release_gate.py"
      provides: "Full mypy-zero quality gate and conditional owner disposition"
    - path: "tests/test_quality_gate_policy.py"
      provides: "Full mypy-zero quality gate and conditional owner disposition"
  key_links:
    - from: "474-21"
      to: "schemas/release/quality-repair-report-v1.schema.json"
      via: "declared dependency and immutable receipt"
      pattern: "quality-repair-report-v1\.schema\.json"
---

<objective>
Full mypy-zero quality gate and conditional owner disposition (D-09, D-10).

Purpose: Complete the mandatory zero attempt and make any residual owner-visible rather than silently baselined.
Output: `schemas/release/quality-repair-report-v1.schema.json`, `scripts/release_gate.py`, `tests/test_quality_gate_policy.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `schemas/release/quality-repair-report-v1.schema.json` — Full mypy-zero quality gate and conditional owner disposition.
- `scripts/release_gate.py` — Full mypy-zero quality gate and conditional owner disposition.
- `tests/test_quality_gate_policy.py` — Full mypy-zero quality gate and conditional owner disposition.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce exact full scope and reject semantic weakening</name>
  <files>schemas/release/quality-repair-report-v1.schema.json, tests/test_quality_gate_policy.py</files>
  <behavior>
    - True zero passes.
    - Suppression or unbound residual reports fail.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
  </read_first>
  <action>Create a closed report schema and tests that pin MYPYPATH=src:tests python -m mypy --no-incremental --explicit-package-bases src/stoa scripts tests, Ruff source scope, measured starting diagnostics, repaired file families, tool/lock/source identities, and exact residuals. Reject new/broadened Any, ignore, exclude, skip, retry, or weakened assertion per D-09/D-10. This task implements D-09, D-10.</action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_quality_gate_policy.py</automated>
  </verify>
  <acceptance>
    - The original 435-error zero attempt is represented as a closed obligation.
    - The gate cannot green by baseline or semantic omission.
  </acceptance>
  <done>The original 435-error zero attempt is represented as a closed obligation. The gate cannot green by baseline or semantic omission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register Ruff, mypy, and typing policy in the canonical gate</name>
  <files>scripts/release_gate.py, tests/test_quality_gate_policy.py</files>
  <behavior>
    - Full zero emits PASS.
    - Residual or scope drift emits a source-bound failure report.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
  </read_first>
  <action>Run Ruff zero and the exact complete mypy command after every typing family, bind source/tool/lock/diagnostic identities, and produce PASS only at true zero. If residuals remain, produce exact FAILED_ZERO_ATTEMPT with root cause and concrete repair cost; never silently freeze the historical count per D-09. This task implements D-09, D-10.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m ruff check src tests scripts --no-cache &amp;&amp; MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa scripts tests &amp;&amp; .venv/bin/python -m pytest -q tests/test_quality_gate_policy.py'</automated>
  </verify>
  <acceptance>
    - True repository-wide zero is attempted and preferred.
    - Any exact residual remains release-blocking absent the next checkpoint.
  </acceptance>
  <done>True repository-wide zero is attempted and preferred. Any exact residual remains release-blocking absent the next checkpoint.</done>
</task>

<task type="checkpoint:decision">
  <name>Task 3: Decide only a proven irreducible residual</name>
  <files>schemas/release/quality-repair-report-v1.schema.json</files>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
  </read_first>
  <action>This checkpoint is NOT APPLICABLE when Task 2 reaches zero. Only after the exact full command produces a source/tool/lock-bound residual may the owner choose continue repair, accept that exact temporary residual with expiry/owner/target, or block release. An old count, broad baseline, or executor choice is ineligible. This task implements D-09, D-10.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m ruff check src tests scripts --no-cache &amp;&amp; MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa scripts tests'</automated>
  </verify>
  <how-to-verify>Review only the execution-generated residual report after the complete zero attempt. Select continue repair, accept the exact bounded residual, or block release.</how-to-verify>
  <resume-signal>Type approved only when the automated check is green; otherwise describe the discrepancy.</resume-signal>
  <acceptance>
    - Zero bypasses the decision as not applicable.
    - Any temporary acceptance is exact, expiring, owner-authored, and does not broaden scope.
  </acceptance>
  <done>Zero bypasses the decision as not applicable. Any temporary acceptance is exact, expiring, owner-authored, and does not broaden scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS / STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-22-01 | High | Tampering / Repudiation | Full mypy-zero quality gate and conditional owner disposition | mitigate/block | Bind exact source, inputs, command, result, and artifact identities; reject missing, duplicate, unknown, dirty, or mismatched values. |
| T-474-22-02 | High | Spoofing / Elevation | verification-to-delivery boundary | mitigate/block | Apply least privilege, protected environment checks, and dependency-closed eligibility before authority is exposed. |
| T-474-SC | High | Tampering | audited package installs | mitigate/block | Use only packages marked OK in RESEARCH.md Package Legitimacy Audit; [ASSUMED]/[SUS] requires a blocking human checkpoint and [SLOP] is forbidden. |
</threat_model>

<verification>
<automated>bash -lc '.venv/bin/python -m ruff check src tests scripts --no-cache &amp;&amp; MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa scripts tests'</automated>
</verification>

<success_criteria>
- Ruff is zero and the complete repository mypy command is attempted after all families.
- Only the owner can accept an exact irreducible temporary residual.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless a later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-22-SUMMARY.md` when done.
</output>
