---
phase: 474-deterministic-verification-and-gated-delivery
plan: 22
type: execute
wave: 11
depends_on: [474-19, 474-20]
files_modified:
  - /Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
  - tests/test_infra_workflow_contract.py
autonomous: true
requirements: [V9QUAL-01, V9QUAL-04, V9QUAL-06]
must_haves:
  truths:
    - "Infrastructure deployment invokes the canonical gate and cannot receive AWS credentials when any prerequisite fails per D-01/D-16"
    - "The workflow deploys only the reviewed Phase 474 staging substrate; production infrastructure remains exact NOT RUN"
    - "Every infrastructure source, action, manifest, and approval identity is immutable and auditable per D-13/D-15"
  artifacts:
    - path: /Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
      provides: "Pinned, manifest-bound staging-infrastructure workflow with separate protected production boundary"
    - path: tests/test_infra_workflow_contract.py
      provides: "Infrastructure workflow pinning, credential-order, target, and no-production contract"
  key_links:
    - from: /Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
      to: /Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py
      via: "explicit staging stack synth/diff/deploy selection"
    - from: /Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
      to: scripts/release_gate.py
      via: "exact backend checkout and common pre-credential gate invocation"
---

<objective>
Harden the infrastructure workflow around the minimum staging release substrate and the canonical release gate.

Purpose: Ensure infrastructure automation cannot bypass verification, substitute mutable code, or turn Phase 474 planning authority into a production mutation.
Output: A pinned infrastructure workflow that deploys only exact reviewed staging resources and records production as NOT RUN.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-19-SUMMARY.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-20-SUMMARY.md
@/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
@/Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py
@/Users/zhdeng/stoa-infra/tests/test_release_topology.py
@scripts/release_gate.py
</context>

## Artifacts this phase produces

- Rewritten `/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml` with full-SHA action pins, exact backend/infra candidates, common-gate validation, staging-scoped OIDC, and explicit staging stack selection.
- New `tests/test_infra_workflow_contract.py` parsing the workflow and rejecting mutable refs/actions, pre-gate OIDC, arbitrary targets, application artifact deployment, and production mutation.
- A credential-ordering contract: untrusted resolve and verification jobs have no OIDC/AWS access; only the post-gate staging job can assume the staging role.
- Exact `production_infrastructure`, `production_approval`, and `production_mutation` NOT RUN evidence for Phase 474.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| workflow input -> CDK context | Untrusted environment/stack/ref fields could select broader infrastructure. |
| common-gate receipt -> OIDC | A forged or stale receipt could unlock AWS credentials. |
| staging workflow -> production account | Role or stack selection could cross environment boundaries. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-22-01 | Critical | Elevation of privilege | OIDC role/session | mitigate/block | Exact audience/subject/environment conditions, staging role only, and no credentials before gate. |
| T-474-22-02 | Critical | Tampering | CDK stack/context | mitigate/block | Closed staging stack ID, reviewed manifest digest, exact commits, and diff evidence before deploy. |
| T-474-22-03 | Critical | Spoofing | gate receipt | mitigate/block | Revalidate schema, candidate, run, artifact, workflow, and receipt digests before role assumption. |
| T-474-22-04 | High | Repudiation | infra deployment | mitigate/block | Retain synth/diff/deploy IDs, CloudFormation stack events, and manifest/commit identities. |
| T-474-22-05 | High | Information disclosure | workflow evidence | mitigate/block | Redact outputs; evidence includes identifiers/status/hashes only. |
| T-474-22-SC | High | Tampering | action supply chain | mitigate/block | Freeze third-party actions to reviewed full commit SHAs; no new package install. |
</threat_model>

<tasks>

<task type="auto" id="474-22-01" tdd="true">
  <name>Fence infrastructure workflow inputs and credential ordering</name>
  <behavior>
    - Resolve and common-gate jobs have no OIDC, AWS credentials, secrets, or writable permissions.
    - The staging job is eligible only after an exact successful gate receipt.
    - Repository/action refs and target account/region/stack/environment are immutable and closed.
  </behavior>
  <read_first>
    - /Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
    - /Users/zhdeng/stoa-infra/app.py
    - /Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py
    - scripts/release_gate.py
    - schemas/release/gate-receipt-v1.schema.json
  </read_first>
  <files>/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml, tests/test_infra_workflow_contract.py</files>
  <action>Write the workflow contract tests first, then rewrite the workflow into unprivileged `resolve` and `verify-common-gate` jobs followed by one staging-environment job. Require full backend and infra commit SHAs, manifest/gate-receipt digests, fixed `staging` environment, and the closed Phase 474 stack ID. Pin all actions to full commits, set minimal top-level permissions, grant `id-token: write` only to the staging job, and acquire AWS credentials only after exact receipt revalidation. Reject expression interpolation of untrusted values into shell commands, arbitrary role/stack/account/region input, mutable refs, and pull-request secrets per D-01, D-13, and D-16.</action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_infra_workflow_contract.py -k credential_order</automated>
  </verify>
  <acceptance_criteria>
    - OIDC is unavailable to resolve/verify and available only to the staging environment job.
    - Stack, role, account, region, environment, commits, manifest, and receipt identities are closed and immutable.
    - A forged/failed gate cannot make the credential or deploy job eligible.
  </acceptance_criteria>
  <done>Infrastructure credentials and stack selection are fenced behind the exact common gate.</done>
</task>

<task type="auto" id="474-22-02" tdd="true">
  <name>Limit CDK automation to reviewed staging substrate and exact production NOT RUN</name>
  <behavior>
    - Synth/diff/deploy selects only the exact Phase 474 staging release stack.
    - Application artifacts remain outside the infrastructure workflow.
    - Production infrastructure, approval, and mutation are exact NOT RUN and cannot be enabled by input.
  </behavior>
  <read_first>
    - /Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
    - /Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py
    - /Users/zhdeng/stoa-infra/tests/test_release_topology.py
    - scripts/release_delivery.py
    - schemas/release/promotion-transaction-v1.schema.json
  </read_first>
  <files>/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml, tests/test_infra_workflow_contract.py</files>
  <action>Extend the contract tests first, then make the privileged job run a deterministic synth and retained diff for the exact Phase 474 staging stack before deploying only that stack. Pass the manifest ID/digest and infra commit into deployment tags/evidence. Do not deploy application artifacts here; Plan 20's coordinator owns immutable Lambda/Web delivery. Add a separate protected-production job shape with no Phase 474 activation condition and an evidence-only step that records `production_infrastructure`, `production_approval`, and `production_mutation` as exact NOT RUN with reason `production_mutation_not_authorized`. Keep `workflow_dispatch` unable to override that policy per D-05, D-06, D-08, and D-15.</action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_infra_workflow_contract.py</automated>
  </verify>
  <acceptance_criteria>
    - CDK synth/diff/deploy targets only the reviewed staging release-delivery stack.
    - Application artifact promotion remains exclusively in the manifest-bound delivery coordinator.
    - No Phase 474 event or input can enable a production infrastructure mutation.
  </acceptance_criteria>
  <done>The infrastructure workflow can create the staging substrate while production remains configured, protected, and unexecuted.</done>
</task>

</tasks>

<verification>
Run the workflow contract verifier with fixtures that forge the gate, choose production/arbitrary stacks, use mutable refs/actions, move OIDC before the gate, and add an application deploy; every fixture must fail.
</verification>

<success_criteria>
- The minimum release infrastructure can be deployed to staging only.
- Failed or forged verification cannot reach AWS credentials.
- Production infrastructure mutation is exact NOT RUN and cannot be enabled by dispatch input.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-22-SUMMARY.md` when done.
</output>
