---
phase: 474-deterministic-verification-and-gated-delivery
plan: 88
type: execute
wave: 11
depends_on: [474-86, 474-87]
files_modified:
  - scripts/release_gate.py
  - schemas/release/gate-receipt-v1.schema.json
  - tests/test_release_gate.py
autonomous: true
requirements: [V9QUAL-01]
must_haves:
  truths:
    - "The backend authority runs the exact Plan 87 Web bundle from the materialized frontend candidate and independently binds its evidence to that candidate."
    - "Ambient Node, npm, Vite, AWS, Git, preload, and lifecycle configuration cannot execute before or influence the Web verifier."
    - "A missing, partial, synthetic, stale, special-file, or source-mismatched Web receipt cannot become an outer PASS receipt."
  artifacts:
    - path: scripts/release_gate.py
      provides: "One registered frontend-web-fresh gate and its closed evidence adapter"
    - path: schemas/release/gate-receipt-v1.schema.json
      provides: "Closed Web gate-evidence branch"
    - path: tests/test_release_gate.py
      provides: "Adversarial registration, launcher, evidence, and candidate-binding coverage"
---

<objective>
Register the exact fresh-install Web bundle in the backend release-gate authority.

Purpose: turn Plan 87's subordinate receipt into candidate-bound outer gate evidence without claiming OpenAPI, dependency, Playwright, aggregate, CI, delivery, mobile, or production work.
Output: one selectable internal gate ID, `frontend-web-fresh`, validated by the existing canonical receipt authority.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-86-SUMMARY.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-87-SUMMARY.md
@scripts/release_gate.py
@schemas/release/gate-receipt-v1.schema.json
@tests/test_release_gate.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register candidate-bound fresh Web evidence</name>
  <files>scripts/release_gate.py, schemas/release/gate-receipt-v1.schema.json, tests/test_release_gate.py</files>
  <behavior>
    - Extend `GateSpec` with the closed evidence kinds `none`, `python-matrix`, and `web-gate-run`; register exactly one frontend gate, `frontend-web-fresh`, whose logical argv is `{node} scripts/verify-release.mjs verify --output {evidence_output}`.
    - Permit exactly one backend-generated `{evidence_output}` token only for `web-gate-run`. Create an unpredictable source-external mode-0700 directory, pass its absolute receipt path only to the real process, keep logical argv/path identities host-free, and always remove the inner evidence directory.
    - Resolve one absolute Node executable from the controlled toolchain, require Node 20, and launch the top-level verifier with a whitelist environment containing only private HOME/TMPDIR, the Node bin PATH, CI, UTC, and deterministic locale. Do not inherit NODE/NPM_CONFIG/VITE/AWS/GIT/preload/lifecycle/DYLD/LD configuration.
    - Read the inner output with no-follow/nonblocking semantics. Require a regular single-link private file of 1..1 MiB, strict UTF-8, no duplicate JSON keys, and exact canonical JSON plus one newline.
    - Validate the closed `stoa.web.gate-run.v1` shape, whole-receipt digest, Node/npm runtime, five fixed ordered PASS steps and argv, 5/5/0/0 counts, hashed diagnostics only, artifact identity, and four exact production `NOT RUN` values.
    - Independently recompute package.json, package-lock.json, Plan-87 source-tree, and dist artifact identities from the exact materialized frontend snapshot; bind package-lock to the live candidate lock identity. Recomputed mismatch must fail even when an attacker recomputes inner and outer digests.
    - Store the complete validated inner receipt only in the outer `gate_evidence` Web branch and project exactly five PASS outcomes. Keep the outer runtime as the Python orchestrator runtime and never serialize real Node paths, temporary paths, environment values, or diagnostics.
    - Keep `scripts/release_manifest.py` and its schema unchanged. Do not map the bundle to canonical `frontend-contract`, `frontend-dependencies`, or `frontend-playwright`, and do not add a formal aggregate or CI caller in this plan.
  </behavior>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_release_gate.py tests/test_release_manifest.py &amp;&amp; .venv/bin/ruff check scripts/release_gate.py tests/test_release_gate.py &amp;&amp; .venv/bin/mypy --strict scripts/release_gate.py &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance>
    - A valid Plan 87 receipt from the exact frontend snapshot creates one closed outer PASS receipt with five outcomes.
    - Hostile launcher environment, missing or malformed evidence, duplicate keys, oversized/symlink/FIFO/hardlink output, step or digest tamper, and source/lock/artifact/candidate drift all fail closed without stale output.
    - Live frontend source is never modified; only the exact materialized snapshot receives node_modules and dist.
    - Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN`.
  </acceptance>
  <done>The backend authority has one honest candidate-bound fresh Web gate ready for the later non-selectable formal aggregate.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| Ambient Node preload executes before Plan 87 scrub | Dedicated absolute Node20 launcher with a closed environment |
| Caller selects an output or substitutes stale evidence | Backend-only unpredictable private output path; stale invalidation; no-follow bounded read |
| Synthetic inner receipt recomputes its own digest | Independent candidate, package, lock, source, and artifact recomputation |
| One Web bundle is overclaimed as other release gates | Internal `frontend-web-fresh` ID only; manifest/OpenAPI/dependency/Playwright excluded |
| Evidence leaks host paths, secrets, or diagnostics | Logical argv plus hashes/counts only; closed schema and privacy tests |
</threat_model>

<success_criteria>
- `frontend-web-fresh` is the only new registry obligation.
- Every Web PASS is bound to one exact Plan 86 frontend snapshot and one complete Plan 87 execution.
- No aggregate, workflow, delivery, provider, mobile, or production mutation is introduced.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-88-SUMMARY.md` when done.
</output>
