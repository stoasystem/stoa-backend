---
phase: 474-deterministic-verification-and-gated-delivery
plan: 32
type: execute
wave: 17
depends_on: [474-25, 474-76, 474-27, 474-28, 474-29, 474-30]
files_modified:
  - "scripts/release_environment.py"
  - "tests/test_release_environment.py"
  - ".github/workflows/deploy.yml"
  - "tests/test_delivery_workflow_contract.py"
  - "docs/security/phase-474-workflow-policy.json"
autonomous: true
requirements: [V9QUAL-01, V9QUAL-02, V9QUAL-03, V9QUAL-04, V9QUAL-05, V9QUAL-06]
must_haves:
  truths:
    - "Backend CI is the single delivery authority and consumes exact sibling commits."
    - "Production topology is enforceable but production mutation remains exact NOT RUN."
  artifacts:
    - path: "scripts/release_environment.py"
      provides: "Backend delivery workflow and environment controller (D-01, D-05, D-06, D-14, D-15, D-16)"
    - path: "tests/test_release_environment.py"
      provides: "Backend delivery workflow and environment controller (D-01, D-05, D-06, D-14, D-15, D-16)"
    - path: ".github/workflows/deploy.yml"
      provides: "Backend delivery workflow and environment controller (D-01, D-05, D-06, D-14, D-15, D-16)"
    - path: "tests/test_delivery_workflow_contract.py"
      provides: "Backend delivery workflow and environment controller (D-01, D-05, D-06, D-14, D-15, D-16)"
    - path: "docs/security/phase-474-workflow-policy.json"
      provides: "Backend delivery workflow and environment controller (D-01, D-05, D-06, D-14, D-15, D-16)"
  key_links:
    - from: "474-30"
      to: "scripts/release_environment.py"
      via: "declared dependency and source-bound verification"
      pattern: 'release_environment\.py'
---

<objective>
Backend delivery workflow and environment controller (D-01, D-05, D-06, D-14, D-15, D-16).

Purpose: Join the exact three-repository handoffs to one enforceable staging path and implement the safe external-state controller.
Output: `scripts/release_environment.py`, `tests/test_release_environment.py`, `.github/workflows/deploy.yml`, `tests/test_delivery_workflow_contract.py`, `docs/security/phase-474-workflow-policy.json`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `scripts/release_environment.py` — Backend delivery workflow and environment controller (D-01, D-05, D-06, D-14, D-15, D-16)
- `tests/test_release_environment.py` — Backend delivery workflow and environment controller (D-01, D-05, D-06, D-14, D-15, D-16)
- `.github/workflows/deploy.yml` — Backend delivery workflow and environment controller (D-01, D-05, D-06, D-14, D-15, D-16)
- `tests/test_delivery_workflow_contract.py` — Backend delivery workflow and environment controller (D-01, D-05, D-06, D-14, D-15, D-16)
- `docs/security/phase-474-workflow-policy.json` — Backend delivery workflow and environment controller (D-01, D-05, D-06, D-14, D-15, D-16)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fail-closed environment inventory/config/staging controls</name>
  <files>scripts/release_environment.py, tests/test_release_environment.py</files>
  <behavior>
    - Exact reviewed staging plans can apply and reverify.
    - Drift, unknown/replacement/destructive/production targets fail before mutation.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - scripts/release_environment.py
    - tests/test_release_environment.py
  </read_first>
  <action>Implement injected read-only inventory plus exact plan/apply/verify commands for six GitHub environments and one allowlisted staging CDK substrate. Bind execution-derived source receipts, before-state/diff/confirmation digests, actor/repo/account/region/stack identities, sole-owner/self-approval policy, branch/OIDC constraints, and provider readback. Reject unavailable reads, drift, replacement/deletion, unknown resources, broadened IAM, partial success, wrong target, or any production apply command per D-05/D-06/D-15. This task implements D-01, D-05, D-06, D-14, D-15, D-16.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_environment.py &amp;&amp; .venv/bin/ruff check scripts/release_environment.py tests/test_release_environment.py'</automated>
  </verify>
  <acceptance>
    - Inventory/config/staging mutations are plan-bound and idempotently verified.
    - No controller command can apply production infrastructure or application state.
  </acceptance>
  <done>Inventory/config/staging mutations are plan-bound and idempotently verified. No controller command can apply production infrastructure or application state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the build-once staged delivery DAG and failure eligibility</name>
  <files>.github/workflows/deploy.yml, tests/test_delivery_workflow_contract.py, docs/security/phase-474-workflow-policy.json</files>
  <behavior>
    - Green exact receipts allow immutable staging delivery.
    - Any required gate failure or tamper cuts every delivery edge.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - .github/workflows/deploy.yml
    - tests/test_delivery_workflow_contract.py
    - docs/security/phase-474-workflow-policy.json
  </read_first>
  <action>Parse all three workflow contracts, bind frontend/infra receipts, and implement exact checkout -> common gate -> immutable build/upload -> staging substrate -> staging deploy/smoke -> protected production approval eligibility. Pin actions, withhold artifacts/OIDC after any test/Ruff/mypy/dependency/provenance/tamper failure, and never rebuild. Define production promotion/smoke/rollback jobs and automatic compensation semantics but disable real execution as exact NOT RUN in this phase per D-05/D-06/D-14/D-16. This task implements D-01, D-05, D-06, D-14, D-15, D-16.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_delivery_workflow_contract.py tests/test_frontend_workflow_contract.py tests/test_infra_workflow_contract.py &amp;&amp; .venv/bin/ruff check tests/test_delivery_workflow_contract.py'</automated>
  </verify>
  <acceptance>
    - One dependency-closed workflow owns main candidate through staging.
    - Every intentional pre-staging failure prevents artifact/credential/deploy eligibility.
  </acceptance>
  <done>One dependency-closed workflow owns main candidate through staging. Every intentional pre-staging failure prevents artifact/credential/deploy eligibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS V1-V14 Applicability

| ASVS | Control family | Disposition | Plan control |
|---|---|---|---|
| V1 | Architecture | apply | Keep the plan dependency-closed, source-bound, and independently verifiable. |
| V2 | Authentication | preserve | Do not introduce an alternate identity mechanism; smoke uses existing bounded auth. |
| V3 | Session Management | preserve | Do not broaden session lifetime or expose browser/session material in evidence. |
| V4 | Access Control | apply | Use least privilege and protected environment/OIDC boundaries before authority. |
| V5 | Validation | apply | Validate paths, refs, schemas, provider values, counts, and closed outcomes. |
| V6 | Cryptography | apply | Use SHA-256/AWS-managed encryption only; reject digest mismatch. |
| V7 | Error/Logging | apply | Emit stable redacted errors and identifiers without credentials or private payloads. |
| V8 | Data Protection | apply | Retain only required hashes, IDs, statuses, and policy evidence. |
| V9 | Communications | apply | Deny ambient network and constrain provider calls to reviewed endpoints/roles. |
| V10 | Malicious Code | apply | Use audited locks, immutable action SHAs, and tamper tests. |
| V11 | Business Logic | apply | Fail closed on missing prerequisites, stale identity, or partial outcome. |
| V12 | Files/Resources | apply | Use bounded paths, immutable blobs/objects, and resource limits. |
| V13 | API/Web Services | apply | Validate structured API/provider responses and preserve authorization semantics. |
| V14 | Configuration | apply | Use reviewed closed configuration with explicit production NOT RUN state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-474-32-S | Spoofing | source/provider identity | mitigate/block | Require exact execution-derived Git/provider identity and authorized actor/role. |
| T-474-32-T | Tampering | Backend delivery workflow and environment controller (D-01, D-05, D-06, D-14, D-15, D-16) | mitigate/block | Bind inputs, commands, results, and artifacts; reject mismatch, drift, or unknown fields. |
| T-474-32-R | Repudiation | evidence receipt | mitigate/block | Retain canonical source-bound command/result/request identities. |
| T-474-32-I | Information Disclosure | diagnostics/evidence | mitigate/block | Store redacted IDs, hashes, counts, and statuses only. |
| T-474-32-D | Denial of Service | bounded execution | mitigate/block | Apply bounded time/resource scopes and fail closed without partial PASS. |
| T-474-32-E | Elevation of Privilege | verification-to-delivery boundary | mitigate/block | Use least privilege, dependency-closed eligibility, and protected environments. |
| T-474-32-SC | Tampering | package/action supply chain | mitigate/block | Use only RESEARCH-approved packages and immutable reviewed Action SHAs; [SLOP] is forbidden. |
</threat_model>

<verification>
  <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_environment.py &amp;&amp; .venv/bin/ruff check scripts/release_environment.py tests/test_release_environment.py'</automated>
  <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_delivery_workflow_contract.py tests/test_frontend_workflow_contract.py tests/test_infra_workflow_contract.py &amp;&amp; .venv/bin/ruff check tests/test_delivery_workflow_contract.py'</automated>
</verification>

<success_criteria>
- Backend CI is the single delivery authority and consumes exact sibling commits.
- Production topology is enforceable but production mutation remains exact NOT RUN.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-32-SUMMARY.md` when done.
</output>
