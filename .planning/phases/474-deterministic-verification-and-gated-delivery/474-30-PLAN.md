---
phase: 474-deterministic-verification-and-gated-delivery
plan: 30
type: execute
wave: 14
depends_on: [474-26, 474-28]
files_modified:
  - "evidence/phase-474/infra-source-ref.json"
autonomous: true
requirements: [V9QUAL-01, V9QUAL-06]
must_haves:
  truths:
    - "The infrastructure release source is a clean exact commit/tree/lock receipt."
    - "All committed paths belong to the Phase 474 topology/workflow plans."
  artifacts:
    - path: "evidence/phase-474/infra-source-ref.json"
      provides: "Infrastructure repository source handoff"
  key_links:
    - from: "474-28"
      to: "evidence/phase-474/infra-source-ref.json"
      via: "declared dependency and immutable receipt"
      pattern: 'infra-source-ref\.json'
---

<objective>
Infrastructure repository source handoff (D-13, D-15).

Purpose: Make versioned release infrastructure reachable by an exact clean commit without absorbing the pre-existing infra file.
Output: `evidence/phase-474/infra-source-ref.json`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `evidence/phase-474/infra-source-ref.json` — Infrastructure repository source handoff.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit and receipt only the owned infrastructure changes</name>
  <files>evidence/phase-474/infra-source-ref.json</files>
  <behavior>
    - Owned infra changes produce one exact receipt.
    - Unexpected dirt blocks before staging authority is used.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
  </read_first>
  <action>Validate candidate-identity.json, require current infra HEAD equals its execution-time start, derive the closed allowed path set from Plans 26 and 28, and reject every undeclared porcelain entry. The known .DS_Store must already have been owner-resolved by Plan 01 and must never be deleted, moved, ignored, staged, or adopted here. Stage only owned paths, create repository-local commit feat(474): qualify release infrastructure, require clean state, and receipt parent/commit/tree/lock/path digest per D-13. This task implements D-13, D-15.</action>
  <verify>
    <automated>bash -lc 'test -z "$(git -C /Users/zhdeng/stoa-infra status --porcelain=v1 --untracked-files=all)" &amp;&amp; jq -e '"'"'.repository == "infra" and .clean_after == true'"'"' evidence/phase-474/infra-source-ref.json >/dev/null'</automated>
  </verify>
  <acceptance>
    - The infra handoff is one clean commit descended from the receipted start.
    - No undeclared file, including .DS_Store, is adopted.
  </acceptance>
  <done>The infra handoff is one clean commit descended from the receipted start. No undeclared file, including .DS_Store, is adopted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS / STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-30-01 | High | Tampering / Repudiation | Infrastructure repository source handoff | mitigate/block | Bind exact source, inputs, command, result, and artifact identities; reject missing, duplicate, unknown, dirty, or mismatched values. |
| T-474-30-02 | High | Spoofing / Elevation | verification-to-delivery boundary | mitigate/block | Apply least privilege, protected environment checks, and dependency-closed eligibility before authority is exposed. |
| T-474-SC | High | Tampering | audited package installs | mitigate/block | Use only packages marked OK in RESEARCH.md Package Legitimacy Audit; [ASSUMED]/[SUS] requires a blocking human checkpoint and [SLOP] is forbidden. |
</threat_model>

<verification>
<automated>bash -lc 'test -z "$(git -C /Users/zhdeng/stoa-infra status --porcelain=v1 --untracked-files=all)" &amp;&amp; jq -e '"'"'.repository == "infra" and .clean_after == true'"'"' evidence/phase-474/infra-source-ref.json >/dev/null'</automated>
</verification>

<success_criteria>
- The infrastructure release source is a clean exact commit/tree/lock receipt.
- All committed paths belong to the Phase 474 topology/workflow plans.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless a later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-30-SUMMARY.md` when done.
</output>
