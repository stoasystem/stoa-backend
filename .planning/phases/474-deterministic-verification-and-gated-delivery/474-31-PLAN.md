---
phase: 474-deterministic-verification-and-gated-delivery
plan: 31
type: execute
wave: 16
depends_on: [474-22, 474-27, 474-32]
files_modified:
  - "evidence/phase-474/backend-source-ref.json"
autonomous: true
requirements: [V9QUAL-01, V9QUAL-06, V9QUAL-07]
must_haves:
  truths:
    - "Backend, frontend, and infra now each have exact commit/tree/lock handoffs."
    - "The backend candidate remains stable under later evidence metadata commits."
  artifacts:
    - path: "evidence/phase-474/backend-source-ref.json"
      provides: "Backend source identity handoff"
  key_links:
    - from: "474-32"
      to: "evidence/phase-474/backend-source-ref.json"
      via: "declared dependency and immutable receipt"
      pattern: "backend-source-ref\.json"
---

<objective>
Backend source identity handoff (D-13, D-16).

Purpose: Complete the third repository clean-start/commit-SHA handoff without a circular current-HEAD identity.
Output: `evidence/phase-474/backend-source-ref.json`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `evidence/phase-474/backend-source-ref.json` — Backend source identity handoff.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seal the backend source candidate and publish its source receipt</name>
  <files>evidence/phase-474/backend-source-ref.json</files>
  <behavior>
    - A clean exact backend candidate can be selected and receipted.
    - Dirty, non-descendant, mutable, or research-time identity fails.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
  </read_first>
  <action>After all backend source/workflow/controller edits are committed by their owning tasks, require the backend worktree clean and current HEAD descended from the execution-time start. Select that clean pre-receipt HEAD as the backend source candidate, record commit/tree/uv.lock/requirements/path-set identities, then create only backend-source-ref.json as metadata so the task commit may become a later descendant without changing candidate blobs. Do not hardcode research SHAs or reset/adopt unrelated dirt. The release manifest consumes the recorded candidate, not mutable current HEAD, per D-13/V9QUAL-07. This task implements D-13, D-16.</action>
  <verify>
    <automated>bash -lc 'test -z "$(git status --porcelain=v1 --untracked-files=all -- . ":(exclude)evidence/phase-474/backend-source-ref.json")" &amp;&amp; test "$(jq -r .candidate evidence/phase-474/backend-source-ref.json)" = "$(git rev-parse HEAD)" &amp;&amp; jq -e '"'"'.repository == "backend" and .clean_candidate == true and (.candidate | test("^[0-9a-f]{40}$"))'"'"' evidence/phase-474/backend-source-ref.json >/dev/null'</automated>
  </verify>
  <acceptance>
    - The backend candidate is execution-derived, clean, and descendant of the preflight identity.
    - Later metadata does not alter the source candidate/tree/locks.
  </acceptance>
  <done>The backend candidate is execution-derived, clean, and descendant of the preflight identity. Later metadata does not alter the source candidate/tree/locks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS / STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-31-01 | High | Tampering / Repudiation | Backend source identity handoff | mitigate/block | Bind exact source, inputs, command, result, and artifact identities; reject missing, duplicate, unknown, dirty, or mismatched values. |
| T-474-31-02 | High | Spoofing / Elevation | verification-to-delivery boundary | mitigate/block | Apply least privilege, protected environment checks, and dependency-closed eligibility before authority is exposed. |
| T-474-SC | High | Tampering | audited package installs | mitigate/block | Use only packages marked OK in RESEARCH.md Package Legitimacy Audit; [ASSUMED]/[SUS] requires a blocking human checkpoint and [SLOP] is forbidden. |
</threat_model>

<verification>
<automated>bash -lc 'test -z "$(git status --porcelain=v1 --untracked-files=all -- . ":(exclude)evidence/phase-474/backend-source-ref.json")" &amp;&amp; test "$(jq -r .candidate evidence/phase-474/backend-source-ref.json)" = "$(git rev-parse HEAD)" &amp;&amp; jq -e '"'"'.repository == "backend" and .clean_candidate == true and (.candidate | test("^[0-9a-f]{40}$"))'"'"' evidence/phase-474/backend-source-ref.json >/dev/null'</automated>
</verification>

<success_criteria>
- Backend, frontend, and infra now each have exact commit/tree/lock handoffs.
- The backend candidate remains stable under later evidence metadata commits.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless a later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-31-SUMMARY.md` when done.
</output>
