---
phase: 474-deterministic-verification-and-gated-delivery
plan: 31
type: execute
wave: 18
depends_on: [474-22, 474-27, 474-32]
files_modified:
  - "evidence/phase-474/backend-source-ref.json"
autonomous: true
requirements: [V9QUAL-01, V9QUAL-06, V9QUAL-07]
must_haves:
  truths:
    - "Backend, frontend, and infra now each have exact commit/tree/lock handoffs."
    - "The backend candidate remains stable under later evidence metadata commits."
  artifacts:
    - path: "evidence/phase-474/backend-source-ref.json"
      provides: "Backend source identity handoff (D-13, D-16)"
  key_links:
    - from: "474-32"
      to: "evidence/phase-474/backend-source-ref.json"
      via: "declared dependency and source-bound verification"
      pattern: 'backend\-source\-ref\.json'
---

<objective>
Backend source identity handoff (D-13, D-16).

Purpose: Complete the third repository clean-start/commit-SHA handoff without a circular current-HEAD identity.
Output: `evidence/phase-474/backend-source-ref.json`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `evidence/phase-474/backend-source-ref.json` — Backend source identity handoff (D-13, D-16)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seal the backend source candidate and publish its source receipt</name>
  <files>evidence/phase-474/backend-source-ref.json</files>
  <behavior>
    - A clean exact backend candidate can be selected and receipted.
    - Dirty, non-descendant, mutable, or research-time identity fails.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - evidence/phase-474/backend-source-ref.json
  </read_first>
  <action>After all backend source/workflow/controller edits are committed by their owning tasks, require the backend worktree clean and current HEAD descended from the execution-time start. Select that clean pre-receipt HEAD as the backend source candidate, record commit/tree/uv.lock/requirements/path-set identities, then create only backend-source-ref.json as metadata so the task commit may become a later descendant without changing candidate blobs. Do not hardcode research SHAs or reset/adopt unrelated dirt. The release manifest consumes the recorded candidate, not mutable current HEAD, per D-13/V9QUAL-07. This task implements D-13, D-16.</action>
  <verify>
    <automated>bash -lc 'test -z "$(git status --porcelain=v1 --untracked-files=all -- . ":(exclude)evidence/phase-474/backend-source-ref.json")" &amp;&amp; test "$(jq -r .candidate evidence/phase-474/backend-source-ref.json)" = "$(git rev-parse HEAD)" &amp;&amp; jq -e '"'"'.repository == "backend" and .clean_candidate == true and (.candidate | test("^[0-9a-f]{40}$"))'"'"' evidence/phase-474/backend-source-ref.json >/dev/null'</automated>
  </verify>
  <acceptance>
    - The backend candidate is execution-derived, clean, and descendant of the preflight identity.
    - Later metadata does not alter the source candidate/tree/locks.
  </acceptance>
  <done>The backend candidate is execution-derived, clean, and descendant of the preflight identity. Later metadata does not alter the source candidate/tree/locks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS V1-V14 Applicability

| ASVS | Control family | Disposition | Plan control |
|---|---|---|---|
| V1 | Architecture | apply | Keep the plan dependency-closed, source-bound, and independently verifiable. |
| V2 | Authentication | preserve | Do not introduce an alternate identity mechanism; smoke uses existing bounded auth. |
| V3 | Session Management | preserve | Do not broaden session lifetime or expose browser/session material in evidence. |
| V4 | Access Control | apply | Use least privilege and protected environment/OIDC boundaries before authority. |
| V5 | Validation | apply | Validate paths, refs, schemas, provider values, counts, and closed outcomes. |
| V6 | Cryptography | apply | Use SHA-256/AWS-managed encryption only; reject digest mismatch. |
| V7 | Error/Logging | apply | Emit stable redacted errors and identifiers without credentials or private payloads. |
| V8 | Data Protection | apply | Retain only required hashes, IDs, statuses, and policy evidence. |
| V9 | Communications | apply | Deny ambient network and constrain provider calls to reviewed endpoints/roles. |
| V10 | Malicious Code | apply | Use audited locks, immutable action SHAs, and tamper tests. |
| V11 | Business Logic | apply | Fail closed on missing prerequisites, stale identity, or partial outcome. |
| V12 | Files/Resources | apply | Use bounded paths, immutable blobs/objects, and resource limits. |
| V13 | API/Web Services | apply | Validate structured API/provider responses and preserve authorization semantics. |
| V14 | Configuration | apply | Use reviewed closed configuration with explicit production NOT RUN state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-474-31-S | Spoofing | source/provider identity | mitigate/block | Require exact execution-derived Git/provider identity and authorized actor/role. |
| T-474-31-T | Tampering | Backend source identity handoff (D-13, D-16) | mitigate/block | Bind inputs, commands, results, and artifacts; reject mismatch, drift, or unknown fields. |
| T-474-31-R | Repudiation | evidence receipt | mitigate/block | Retain canonical source-bound command/result/request identities. |
| T-474-31-I | Information Disclosure | diagnostics/evidence | mitigate/block | Store redacted IDs, hashes, counts, and statuses only. |
| T-474-31-D | Denial of Service | bounded execution | mitigate/block | Apply bounded time/resource scopes and fail closed without partial PASS. |
| T-474-31-E | Elevation of Privilege | verification-to-delivery boundary | mitigate/block | Use least privilege, dependency-closed eligibility, and protected environments. |
| T-474-31-SC | Tampering | package/action supply chain | mitigate/block | Use only RESEARCH-approved packages and immutable reviewed Action SHAs; [SLOP] is forbidden. |
</threat_model>

<verification>
  <automated>bash -lc 'test -z "$(git status --porcelain=v1 --untracked-files=all -- . ":(exclude)evidence/phase-474/backend-source-ref.json")" &amp;&amp; test "$(jq -r .candidate evidence/phase-474/backend-source-ref.json)" = "$(git rev-parse HEAD)" &amp;&amp; jq -e '"'"'.repository == "backend" and .clean_candidate == true and (.candidate | test("^[0-9a-f]{40}$"))'"'"' evidence/phase-474/backend-source-ref.json >/dev/null'</automated>
</verification>

<success_criteria>
- Backend, frontend, and infra now each have exact commit/tree/lock handoffs.
- The backend candidate remains stable under later evidence metadata commits.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-31-SUMMARY.md` when done.
</output>
