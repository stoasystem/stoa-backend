---
phase: 474-deterministic-verification-and-gated-delivery
plan: 23
type: execute
wave: 5
depends_on: [474-01, 474-05]
files_modified:
  - "/Users/zhdeng/stoa-frontend/package.json"
  - "/Users/zhdeng/stoa-frontend/package-lock.json"
autonomous: true
requirements: [V9QUAL-03, V9QUAL-05]
must_haves:
  truths:
    - "The Web root lock has no unaccepted blocking advisory."
    - "The mobile skeleton is neither audited nor changed."
  artifacts:
    - path: "/Users/zhdeng/stoa-frontend/package.json"
      provides: "Web dependency remediation (D-11, D-12, D-18)"
    - path: "/Users/zhdeng/stoa-frontend/package-lock.json"
      provides: "Web dependency remediation (D-11, D-12, D-18)"
  key_links:
    - from: "474-05"
      to: "/Users/zhdeng/stoa-frontend/package.json"
      via: "declared dependency and source-bound verification"
      pattern: 'package\.json'
---

<objective>
Web dependency remediation (D-11, D-12, D-18).

Purpose: Make the real Web lockfile satisfy the exact release-blocking advisory policy without touching native/mobile dependencies.
Output: `/Users/zhdeng/stoa-frontend/package.json`, `/Users/zhdeng/stoa-frontend/package-lock.json`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `/Users/zhdeng/stoa-frontend/package.json` — Web dependency remediation (D-11, D-12, D-18)
- `/Users/zhdeng/stoa-frontend/package-lock.json` — Web dependency remediation (D-11, D-12, D-18)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair Web root advisories</name>
  <files>/Users/zhdeng/stoa-frontend/package.json, /Users/zhdeng/stoa-frontend/package-lock.json</files>
  <behavior>
    - Supported dependency fixes remove blockers.
    - The committed lock remains npm-ci exact.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - /Users/zhdeng/stoa-frontend/package.json
    - /Users/zhdeng/stoa-frontend/package-lock.json
  </read_first>
  <action>Run the machine-readable Web advisory policy, upgrade or replace the measured vulnerable Web-root packages, and retain exact package-lock identity. Do not add a D-11 exception while a supported fix exists, and do not inspect or mutate mobile/ per D-11/D-12/D-18.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail &amp;&amp; audit_path="$(mktemp /tmp/stoa-web-audit.XXXXXX)" &amp;&amp; trap "rm -f -- \"$audit_path\"" EXIT &amp;&amp; cd /Users/zhdeng/stoa-frontend &amp;&amp; npm ci &amp;&amp; (npm audit --package-lock-only --json &gt; "$audit_path" || test "$?" -eq 1) &amp;&amp; test -s "$audit_path" &amp;&amp; node -e "JSON.parse(require(\"fs\").readFileSync(process.argv[1], \"utf8\"))" "$audit_path" &amp;&amp; cd /Users/zhdeng/stoa-backend &amp;&amp; .venv/bin/python scripts/dependency_policy.py check-frontend --repo-root /Users/zhdeng/stoa-frontend --audit "$audit_path"'</automated>
  </verify>
  <acceptance>
    - The Web root lock installs exactly and passes the shared advisory policy.
    - No native/mobile dependency participates in the result.
  </acceptance>
  <done>The Web root lock installs exactly and passes the shared advisory policy. No native/mobile dependency participates in the result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS V1-V14 Applicability

| ASVS | Control family | Disposition | Plan control |
|---|---|---|---|
| V1 | Architecture | apply | Keep the plan dependency-closed, source-bound, and independently verifiable. |
| V2 | Authentication | preserve | Do not introduce an alternate identity mechanism; smoke uses existing bounded auth. |
| V3 | Session Management | preserve | Do not broaden session lifetime or expose browser/session material in evidence. |
| V4 | Access Control | apply | Use least privilege and protected environment/OIDC boundaries before authority. |
| V5 | Validation | apply | Validate paths, refs, schemas, provider values, counts, and closed outcomes. |
| V6 | Cryptography | apply | Use SHA-256/AWS-managed encryption only; reject digest mismatch. |
| V7 | Error/Logging | apply | Emit stable redacted errors and identifiers without credentials or private payloads. |
| V8 | Data Protection | apply | Retain only required hashes, IDs, statuses, and policy evidence. |
| V9 | Communications | apply | Deny ambient network and constrain provider calls to reviewed endpoints/roles. |
| V10 | Malicious Code | apply | Use audited locks, immutable action SHAs, and tamper tests. |
| V11 | Business Logic | apply | Fail closed on missing prerequisites, stale identity, or partial outcome. |
| V12 | Files/Resources | apply | Use bounded paths, immutable blobs/objects, and resource limits. |
| V13 | API/Web Services | apply | Validate structured API/provider responses and preserve authorization semantics. |
| V14 | Configuration | apply | Use reviewed closed configuration with explicit production NOT RUN state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-474-23-S | Spoofing | source/provider identity | mitigate/block | Require exact execution-derived Git/provider identity and authorized actor/role. |
| T-474-23-T | Tampering | Web dependency remediation (D-11, D-12, D-18) | mitigate/block | Bind inputs, commands, results, and artifacts; reject mismatch, drift, or unknown fields. |
| T-474-23-R | Repudiation | evidence receipt | mitigate/block | Retain canonical source-bound command/result/request identities. |
| T-474-23-I | Information Disclosure | diagnostics/evidence | mitigate/block | Store redacted IDs, hashes, counts, and statuses only. |
| T-474-23-D | Denial of Service | bounded execution | mitigate/block | Apply bounded time/resource scopes and fail closed without partial PASS. |
| T-474-23-E | Elevation of Privilege | verification-to-delivery boundary | mitigate/block | Use least privilege, dependency-closed eligibility, and protected environments. |
| T-474-23-SC | Tampering | package/action supply chain | mitigate/block | Use only RESEARCH-approved packages and immutable reviewed Action SHAs; [SLOP] is forbidden. |
</threat_model>

<verification>
  <automated>bash -lc 'set -euo pipefail &amp;&amp; audit_path="$(mktemp /tmp/stoa-web-audit.XXXXXX)" &amp;&amp; trap "rm -f -- \"$audit_path\"" EXIT &amp;&amp; cd /Users/zhdeng/stoa-frontend &amp;&amp; npm ci &amp;&amp; (npm audit --package-lock-only --json &gt; "$audit_path" || test "$?" -eq 1) &amp;&amp; test -s "$audit_path" &amp;&amp; node -e "JSON.parse(require(\"fs\").readFileSync(process.argv[1], \"utf8\"))" "$audit_path" &amp;&amp; cd /Users/zhdeng/stoa-backend &amp;&amp; .venv/bin/python scripts/dependency_policy.py check-frontend --repo-root /Users/zhdeng/stoa-frontend --audit "$audit_path"'</automated>
</verification>

<success_criteria>
- The Web root lock has no unaccepted blocking advisory.
- The mobile skeleton is neither audited nor changed.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-23-SUMMARY.md` when done.
</output>
