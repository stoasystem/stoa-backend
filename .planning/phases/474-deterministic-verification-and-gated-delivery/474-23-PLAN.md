---
phase: 474-deterministic-verification-and-gated-delivery
plan: 23
type: execute
wave: 14
depends_on: [474-20, 474-84, 474-85]
files_modified:
  - .github/workflows/deploy.yml
  - tests/test_delivery_workflow_contract.py
  - docs/security/phase-474-workflow-policy.json
autonomous: true
requirements: [V9QUAL-01, V9QUAL-02, V9QUAL-03, V9QUAL-04, V9QUAL-05, V9QUAL-06]
must_haves:
  truths:
    - "A main candidate can reach staging only through the canonical gate, one immutable build, and manifest-bound handoff per D-01/D-05"
    - "Failed tests, Ruff, mypy policy, dependency policy, provenance, or tamper scenarios make deploy jobs ineligible and deny artifacts/credentials per D-16"
    - "Production jobs require the protected production environment and exact staging-verified release, while Phase 474 records them as NOT RUN"
    - "Every third-party action, repository candidate, artifact, receipt, approval, and environment is immutable or closed"
  artifacts:
    - path: .github/workflows/deploy.yml
      provides: "Integrated verification, build-once, staging, smoke, protected-production, compensation, and evidence DAG"
    - path: tests/test_delivery_workflow_contract.py
      provides: "Static and simulated DAG/permissions/tamper contract tests"
    - path: docs/security/phase-474-workflow-policy.json
      provides: "Machine-readable action, permission, environment, and job-order allowlist"
  key_links:
    - from: .github/workflows/deploy.yml
      to: scripts/release_gate.py
      via: "single no-credential formal verification job"
    - from: .github/workflows/deploy.yml
      to: scripts/release_delivery.py
      via: "manifest-bound staging/smoke/promotion/rollback subcommands"
    - from: tests/test_delivery_workflow_contract.py
      to: all three repository workflows
      via: "cross-repository frozen-reference and no-bypass policy"
---

<objective>
Assemble the cross-repository delivery workflow DAG and prove its security invariants before any live staging exercise.

Purpose: Turn the verified gate, immutable artifacts, Web/infra callers, and two-pointer coordinator into one enforceable path from main candidate to staging while keeping production gated and unexecuted.
Output: Backend delivery workflow, machine-readable policy, and exhaustive workflow contract/failure tests.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-20-SUMMARY.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-21-SUMMARY.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-22-SUMMARY.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-84-SUMMARY.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-85-SUMMARY.md
@evidence/phase-474/frontend-source-ref.json
@evidence/phase-474/infra-source-ref.json
@.github/workflows/deploy.yml
@scripts/release_gate.py
@scripts/release_manifest.py
@scripts/release_delivery.py
@schemas/release/release-manifest-v1.schema.json
</context>

## Artifacts this phase produces

- New test file `tests/test_delivery_workflow_contract.py` parsing backend/frontend/infra workflows and simulating `needs`/`if` eligibility, environment/permission boundaries, action pins, artifact flow, and failure/tamper cases.
- New closed policy `docs/security/phase-474-workflow-policy.json` listing allowed events, jobs, edges, environments, permissions, action SHAs, artifact names, exact NOT RUN obligations, and forbidden commands/patterns.
- Rewritten `.github/workflows/deploy.yml` with jobs `resolve-candidate`, `verify-common-gate`, `build-once`, `publish-evidence`, `deploy-staging`, `smoke-staging`, `await-production-approval`, `promote-production`, `smoke-production`, `compensate-production`, and `finalize-evidence`.
- Explicit workflow outputs connecting one manifest, one backend artifact, one Web artifact, one runtime-config digest, one gate receipt, and one transaction identity across all jobs.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| trigger/resolution -> candidate manifest | Cross-repository SHAs and workflow inputs may be attacker-controlled. |
| verification -> artifact jobs | A failed or forged gate must not produce deployable artifact access. |
| artifact jobs -> staging credentials | Artifact, manifest, or receipt substitution could deploy different bytes. |
| staging evidence -> protected production | Approval could target a different or stale release set. |
| production smoke -> compensation | Partial failure or races could leave backend/Web pointers split. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-23-01 | Critical | Elevation of privilege | workflow DAG/permissions | mitigate/block | Least per-job permissions; OIDC only after gate in environment-scoped jobs. |
| T-474-23-02 | Critical | Tampering | artifact/manifest flow | mitigate/block | Exact digest outputs, attestations, one manifest, rehash at every download/effect boundary. |
| T-474-23-03 | Critical | Spoofing | production approval | mitigate/block | Protected production environment plus exact staging transaction/manifest/run identity. |
| T-474-23-04 | Critical | Tampering | rollback target | mitigate/block | Only prior durable verified transaction identity accepted; compensation verifies both pointers. |
| T-474-23-05 | High | Repudiation | workflow/evidence | mitigate/block | Retain run/attempt/job/environment/request IDs and signed digest-linked receipts. |
| T-474-23-06 | High | Information disclosure | logs/artifacts | mitigate/block | No secrets in evidence; redact outputs and prohibit credential dumps/debug tracing. |
| T-474-23-SC | High | Tampering | actions/packages | mitigate/block | Full-SHA actions and existing audited locks only; no new install task. |

## ASVS 4.0.3 L1 Control Mapping

| ASVS L1 category | Applicability and concrete control | Verification |
|---|---|---|
| V1 Architecture | Applicable — exclusive file ownership, explicit dependencies, and fail-closed boundaries are part of this plan. | Task 23-01 automated command and this plan's focused acceptance criteria. |
| V2 Authentication | Applicable — exact identity/provider inputs remain authenticated and are exercised by focused tests. | Task 23-01 automated command and this plan's focused acceptance criteria. |
| V3 Session Management | Applicable — session/token state is closed and expiry/replay behavior remains tested. | Task 23-01 automated command and this plan's focused acceptance criteria. |
| V4 Access Control | Applicable — authorization/release authority remains deny-first; any High/Critical failure blocks completion. | Task 23-01 automated command and this plan's focused acceptance criteria. |
| V5 Validation | Applicable — untrusted records, command inputs, and result unions are narrowed before use; malformed values fail. | Task 23-01 automated command and this plan's focused acceptance criteria. |
| V6 Cryptography | Applicable — SHA-256/content identities are recomputed and never substitute for secret-key controls. | Task 23-01 automated command and this plan's focused acceptance criteria. |
| V7 Error Handling and Logging | Applicable — stable bounded errors/results are verified and private/provider payloads are not logged. | Task 23-01 automated command and this plan's focused acceptance criteria. |
| V8 Data Protection | Applicable — tests preserve redaction and prevent secret, token, student-content, or provider-payload disclosure. | Task 23-01 automated command and this plan's focused acceptance criteria. |
| V9 Communications | Applicable — real/provider/network boundaries require approved origins, denial, or immutable transport identity. | Task 23-01 automated command and this plan's focused acceptance criteria. |
| V10 Malicious Code | Applicable — no unreviewed package/action is added; the SC threat and locked dependency policy remain blocking. | Task 23-01 automated command and this plan's focused acceptance criteria. |
| V11 Business Logic | Applicable — focused tests preserve exact state, race, retry, idempotency, and fail-closed outcomes relevant to the owned files. | Task 23-01 automated command and this plan's focused acceptance criteria. |
| V12 Files and Resources | Applicable — every modified path is enumerated and artifact/evidence bytes or source files are bounded by exact ownership. | Task 23-01 automated command and this plan's focused acceptance criteria. |
| V13 API and Web Services | Applicable — request/CLI/workflow contracts are closed and verified end to end. | Task 23-01 automated command and this plan's focused acceptance criteria. |
| V14 Configuration | Applicable — exact tool/scope/configuration is verified and broad ignores, mutable refs, or permissive defaults are forbidden. | Task 23-01 automated command and this plan's focused acceptance criteria. |

Any failed applicable ASVS L1 control, or any High/Critical STRIDE threat without its named mitigation and test, blocks plan completion.
</threat_model>

<tasks>

<task type="auto" id="474-23-01" tdd="true">
  <name>Specify cross-repository workflow policy and failure eligibility</name>
  <behavior>
    - Every deploy-capable job needs a passing canonical gate and exact manifest/artifact receipts.
    - A failed/tampered prerequisite makes build/upload/deploy jobs ineligible and unable to receive artifacts or credentials.
    - Staging and production roles/environments are disjoint; production has a distinct approval edge.
    - Phase 474 cannot execute production jobs and must emit four exact production NOT RUN obligations.
  </behavior>
  <read_first>
    - .github/workflows/deploy.yml
    - /Users/zhdeng/stoa-frontend/.github/workflows/frontend-ci.yml
    - /Users/zhdeng/stoa-frontend/.github/workflows/deploy.yml
    - /Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
    - scripts/release_gate.py
    - evidence/phase-474/frontend-source-ref.json
    - evidence/phase-474/infra-source-ref.json
  </read_first>
  <files>tests/test_delivery_workflow_contract.py, docs/security/phase-474-workflow-policy.json</files>
  <action>Consume the exact repository-local commit/tree receipts from Plans 84 and 85 and reject missing, dirty, mutable, uncommitted, or non-descendant sibling identities. Create the closed workflow policy and failing tests that parse all three repositories' delivery workflows. Assert exact allowed events/jobs/edges/environments/permissions/action SHAs/artifact names, no mutable refs, no alternate quality command, no shell use of untrusted expressions, no pre-gate OIDC/secrets, no Web rebuild, no root sync/delete, and no direct infrastructure/application production command. Simulate intentional failures for Python tests, Ruff, mypy, backend/Web advisories, provenance, source/artifact/config/receipt tampering, staging smoke, and production smoke. Prove every pre-staging failure prevents artifact publication and deploy eligibility per D-16; prove production is separately protected per D-05/D-06.</action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_delivery_workflow_contract.py</automated>
  </verify>
  <acceptance_criteria>
    - Policy is closed: unknown event/job/edge/permission/action/artifact/environment fails.
    - Each required intentional failure has an expected ineligible job set and zero credential/artifact reachability.
    - Production NOT RUN obligations are exactly approval, promotion, smoke, and rollback.
  </acceptance_criteria>
  <done>The complete workflow security and eligibility contract exists as failing tests and closed policy data.</done>
</task>

<task type="auto" id="474-23-02" tdd="true">
  <name>Implement the build-once staging delivery DAG</name>
  <behavior>
    - One resolved main candidate invokes one authoritative gate, then builds one immutable backend/Web release set.
    - Staging receives only artifacts and runtime config bound to the exact manifest and receipts.
    - Failed verification, build, attestation, upload, deployment, or smoke cannot advance.
  </behavior>
  <read_first>
    - .github/workflows/deploy.yml
    - tests/test_delivery_workflow_contract.py
    - docs/security/phase-474-workflow-policy.json
    - scripts/release_gate.py
    - scripts/release_manifest.py
    - scripts/release_delivery.py
    - evidence/phase-474/frontend-source-ref.json
    - evidence/phase-474/infra-source-ref.json
  </read_first>
  <files>.github/workflows/deploy.yml, tests/test_delivery_workflow_contract.py</files>
  <action>Rewrite the backend workflow using the exact jobs and outputs in the artifact inventory. On a main candidate, read the frontend and infrastructure commit SHAs from the validated Plan 84/85 receipts—never infer them from sibling worktrees or branch heads—and resolve those exact refs plus the backend ref and locks into the manifest. Run only the canonical gate in a fresh no-credential job, build once, attest/upload by digest, then enter the protected staging environment and call `release_delivery.py stage` and `smoke` for that same transaction. Use full-SHA actions, least permissions, candidate concurrency, bounded timeouts, explicit artifact retention, and exact `needs`/success conditions. Rehash and revalidate manifest/gate/source/lock/artifact/config identities after every download and before OIDC/effect. No later job may invoke npm, uv build, packaging, or source checkout to rebuild per D-02, D-04, D-05, D-13, and D-14.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_delivery_workflow_contract.py && .venv/bin/ruff check tests/test_delivery_workflow_contract.py'</automated>
  </verify>
  <acceptance_criteria>
    - The static DAG has one gate, one build, one immutable artifact flow, and one staging transaction.
    - OIDC and staging artifacts are unreachable after any prerequisite failure/tamper scenario.
    - No staging or production job rebuilds either artifact.
  </acceptance_criteria>
  <done>A passing main candidate can automatically reach staging and smoke only through the exact common gate and build-once manifest.</done>
</task>

<task type="auto" id="474-23-03" tdd="true">
  <name>Wire protected production, compensation, and evidence without authorizing mutation</name>
  <behavior>
    - Production approval is tied to exact successful staging transaction and manifest.
    - A production smoke failure invokes two-pointer compensation to the recorded prior verified set.
    - Without explicit operational authorization, production approval/promotion/smoke/rollback are exact NOT RUN and no provider call occurs.
  </behavior>
  <read_first>
    - .github/workflows/deploy.yml
    - tests/test_delivery_workflow_contract.py
    - docs/security/phase-474-workflow-policy.json
    - scripts/release_delivery.py
    - schemas/release/promotion-transaction-v1.schema.json
  </read_first>
  <files>.github/workflows/deploy.yml, tests/test_delivery_workflow_contract.py, docs/security/phase-474-workflow-policy.json</files>
  <action>Add the separately protected production job shape: exact staging evidence enters `await-production-approval`, the owner may self-approve per D-06, and only an additional explicit operational authorization can make promotion eligible. Bind approval to exact manifest/staging transaction/artifact/config/gate digests. Wire bounded production smoke and `always()` compensation that restores and verifies both prior pointer sets per D-08; never permit new/rebuilt emergency code per D-07. For Phase 474's actual invocation, make production jobs ineligible and finalize exact NOT RUN for approval, promotion, smoke, and rollback, with no AWS credentials/effects. Retain all success/failure/rollback identities per D-15.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_delivery_workflow_contract.py -k "production or rollback or evidence or policy" && .venv/bin/python -m json.tool docs/security/phase-474-workflow-policy.json >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - Protected owner approval cannot authorize a different/stale staging release.
    - The workflow contains a tested two-pointer compensation edge after failed production smoke.
    - The Phase 474 path records four exact production NOT RUN obligations and cannot acquire production credentials.
  </acceptance_criteria>
  <done>The full production/rollback contract is configured and testable while Phase 474 remains strictly non-production.</done>
</task>

</tasks>

<verification>
Run the frontend, infrastructure, and integrated workflow contract suites; inspect simulated failure reachability to confirm zero artifacts/credentials/effects after every failed gate or tamper case.
</verification>

<success_criteria>
- The backend workflow is the sole integrated path from candidate resolution through staging and evidence.
- Build once/promote unchanged and cross-repository manifest identity are enforced by DAG and executable tests.
- Protected production and verified rollback semantics exist, but Phase 474 cannot perform a production mutation.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-23-SUMMARY.md` when done.
</output>
