---
phase: 474-deterministic-verification-and-gated-delivery
plan: 24
type: execute
wave: 13
depends_on: [474-23]
files_modified:
  - scripts/release_environment.py
  - tests/test_release_environment.py
  - evidence/phase-474/environment-inventory.json
  - evidence/phase-474/staging-substrate.json
autonomous: false
requirements: [V9QUAL-06]
user_setup:
  - service: github
    why: "Create/verify protected staging and production environments and the sole-owner reviewer policy"
    env_vars:
      - name: GH_TOKEN
        source: "GitHub token with repository Actions/Environments administration for backend, frontend, and infra repositories"
    dashboard_config:
      - task: "Ensure the project owner can be selected as the protected production reviewer with self-approval allowed"
        location: "Repository Settings -> Environments -> production"
  - service: aws
    why: "Inventory release resources and deploy only the minimum staging release-delivery substrate"
    env_vars:
      - name: AWS_PROFILE
        source: "Authorized staging infrastructure profile or equivalent OIDC session; no production mutation permission is needed"
    dashboard_config:
      - task: "Confirm the staging account/region and owner-approved resource prefixes"
        location: "AWS account inventory / deployment configuration"
must_haves:
  truths:
    - "Live GitHub staging/production environments implement the sole-owner approval decision per D-05/D-06"
    - "The minimum versioned release infrastructure exists in staging and is recorded by immutable identities"
    - "Inventory and configuration fail closed on repository/account/region/resource drift or broader-than-declared permissions"
    - "Production application and infrastructure mutation remains exact NOT RUN"
  artifacts:
    - path: scripts/release_environment.py
      provides: "Read-first inventory and narrowly scoped GitHub/staging bootstrap commands with no production deploy command"
    - path: evidence/phase-474/environment-inventory.json
      provides: "Redacted live repository/AWS preflight facts and mismatches"
    - path: evidence/phase-474/staging-substrate.json
      provides: "GitHub protection and staging CloudFormation/resource identity receipts"
  key_links:
    - from: scripts/release_environment.py
      to: docs/security/phase-474-workflow-policy.json
      via: "closed expected repository/environment/account/region/resource policy"
    - from: scripts/release_environment.py
      to: /Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
      via: "exact staging stack workflow dispatch and run observation"
---

<objective>
Inventory live release surfaces, configure protected GitHub environments, and bootstrap only the staging release substrate.

Purpose: Close the live configuration gap needed for an executable staging proof without widening authority into production.
Output: Tested environment tool, redacted inventory receipt, protected-environment evidence, and staging-infrastructure receipt followed by human verification.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-23-SUMMARY.md
@scripts/release_environment.py
@docs/security/phase-474-workflow-policy.json
@/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
@/Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py
</context>

## Artifacts this phase produces

- New `scripts/release_environment.py` with testable commands `inventory`, `plan-github`, `apply-github`, `plan-staging`, `apply-staging`, `verify`, and `self-test`; no production deployment/mutation subcommand.
- New `tests/test_release_environment.py` with fake GitHub/AWS/CloudFormation clients, redaction assertions, closed account/region/repository/resource checks, least-privilege comparisons, idempotency, and production-denial tests.
- New `evidence/phase-474/environment-inventory.json` containing schema version, UTC/run/request IDs, exact repository/account/region/resource/environment facts, policy mismatches, and SHA-256 digest without credentials/secrets.
- New `evidence/phase-474/staging-substrate.json` containing exact GitHub environment protection, infra commit/workflow run, stack ID/change set, resource ARNs/versioning/retention/role-policy hashes, status, and production NOT RUN facts.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local tool -> GitHub administration | A broad token could alter unrelated settings or weaken protection. |
| local tool -> AWS/CloudFormation | Wrong account/region/stack could mutate production or unrelated resources. |
| live provider responses -> evidence | Secrets or mutable names could leak or masquerade as immutable proof. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-24-01 | Critical | Elevation of privilege | GitHub/AWS targets | mitigate/block | Closed owner/repo/account/region/environment/stack allowlists and explicit confirmation tokens. |
| T-474-24-02 | Critical | Tampering | production boundary | mitigate/block | No production apply subcommand; deny production account/stack/resource inputs in code and tests. |
| T-474-24-03 | High | Information disclosure | inventory/evidence | mitigate/block | Recursive redaction and allowlisted identifiers/hashes/status only. |
| T-474-24-04 | High | Repudiation | configuration actions | mitigate/block | Persist request/run/change-set/commit IDs and before/after policy hashes. |
| T-474-24-05 | High | Spoofing | live resources | mitigate/block | Verify provider-returned ARNs, account, region, versioning, retention, tags, trust/policy documents. |
| T-474-24-SC | High | Tampering | provider dependencies | mitigate/block | Use existing audited GitHub CLI/boto/CDK surfaces and project-owned narrow clients; no new package. |
</threat_model>

<tasks>

<task type="auto" id="474-24-01" tdd="true">
  <name>Build a read-first, staging-only environment controller</name>
  <behavior>
    - Inventory is read-only, redacted, deterministic, and rejects unexpected repositories/accounts/regions/resources.
    - GitHub apply changes only named staging/production environment protection settings.
    - AWS apply can dispatch/deploy only the named staging release stack and has no production mutation command.
  </behavior>
  <read_first>
    - docs/security/phase-474-workflow-policy.json
    - /Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
    - /Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py
    - scripts/release_delivery.py
  </read_first>
  <files>scripts/release_environment.py, tests/test_release_environment.py</files>
  <action>Write tests first, then implement the named commands using injected narrow GitHub/AWS/CloudFormation clients. `inventory` must perform only reads; `plan-*` must emit exact proposed deltas and confirmation digests; `apply-github` may create/update only `staging` and `production` environment policies for the three repositories; `apply-staging` may dispatch or execute only the exact reviewed infra commit/workflow/stack. Require explicit closed expected owner/repos/account/region/resource prefixes and refuse wildcards, mutable refs, unknown resources, broader role policies, production stack/account, or changed confirmation digest. Encode the owner as the sole production reviewer with self-approval allowed per D-06, while retaining a separate operational-authorization requirement in the workflow.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_environment.py && .venv/bin/ruff check scripts/release_environment.py tests/test_release_environment.py'</automated>
  </verify>
  <acceptance_criteria>
    - Fake-provider tests prove inventory has zero writes and evidence contains no secret-bearing values.
    - Apply commands are idempotent and cannot target production application/infrastructure resources.
    - Unexpected identity, policy broadening, or plan/apply drift fails before any mutation.
  </acceptance_criteria>
  <done>A narrow, tested controller can safely inspect and configure only the declared Phase 474 environment surfaces.</done>
</task>

<task type="auto" id="474-24-02">
  <name>Inventory live state, configure protection, and deploy the staging substrate</name>
  <read_first>
    - scripts/release_environment.py
    - tests/test_release_environment.py
    - docs/security/phase-474-workflow-policy.json
    - /Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
    - /Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py
  </read_first>
  <files>evidence/phase-474/environment-inventory.json, evidence/phase-474/staging-substrate.json</files>
  <action>With user-provided GitHub administrative and staging AWS authority, run the read-only inventory first and retain the redacted digest. Stop on any target/policy drift. Generate and inspect exact GitHub and staging plans, apply GitHub `staging`/`production` environment protection to all three repositories, then deploy only the reviewed Phase 474 staging stack through the hardened infra workflow. Verify bucket/table versioning, retention/lifecycle, role trust/permissions, Lambda alias substrate, Web pointer substrate, tags, and CloudFormation completion by immutable provider identities. Record `production_infrastructure`, `production_approval`, and `production_mutation` as NOT RUN; do not invoke any production AWS role, stack, alias, bucket, distribution, or application endpoint per D-05/D-06/D-15.</action>
  <verify>
    <automated>.venv/bin/python scripts/release_environment.py verify --inventory evidence/phase-474/environment-inventory.json --receipt evidence/phase-474/staging-substrate.json --policy docs/security/phase-474-workflow-policy.json</automated>
  </verify>
  <acceptance_criteria>
    - All three repositories expose protected staging and production environments with the declared owner policy.
    - The exact staging stack exists with versioned/retained artifacts/evidence, scoped roles, aliases, pointers, and rollback state.
    - Evidence contains immutable provider/workflow/commit/policy identities and exact production NOT RUN, with no secret material.
  </acceptance_criteria>
  <done>Live staging infrastructure and GitHub protection are configured and machine-verifiably recorded without production mutation.</done>
</task>

<task type="checkpoint:human-verify" id="474-24-03" gate="blocking">
  <name>Verify protected environments and staging-only infrastructure boundary</name>
  <read_first>
    - evidence/phase-474/environment-inventory.json
    - evidence/phase-474/staging-substrate.json
    - docs/security/phase-474-workflow-policy.json
  </read_first>
  <files>evidence/phase-474/environment-inventory.json, evidence/phase-474/staging-substrate.json</files>
  <action>Pause only after the automated inventory, protection configuration, staging deployment, and machine verification pass. Present direct GitHub environment settings links, the staging workflow run, CloudFormation stack link/ID, redacted resource list, policy diffs, and exact production NOT RUN fields for owner inspection.</action>
  <verify>
    <automated>.venv/bin/python scripts/release_environment.py verify --inventory evidence/phase-474/environment-inventory.json --receipt evidence/phase-474/staging-substrate.json --policy docs/security/phase-474-workflow-policy.json</automated>
    <human-check>Confirm the project owner is the sole production reviewer with self-approval allowed, staging and production roles are separate, only the named staging stack changed, retained stores are versioned, and every production mutation field is NOT RUN.</human-check>
  </verify>
  <acceptance_criteria>
    - Human verification agrees with the machine-readable receipt and live provider views.
    - No unrelated repository setting or AWS resource changed.
    - No production mutation or credential use occurred.
  </acceptance_criteria>
  <done>The owner approves the live protected-environment and staging-substrate boundary by replying `approved`, or reports exact discrepancies for correction.</done>
</task>

</tasks>

<verification>
Run unit tests, read-only inventory, plan/apply drift checks, live post-apply verification, canonical evidence validation, and owner inspection. Any unknown/broadened target or missing immutable identity blocks Plan 25.
</verification>

<success_criteria>
- GitHub protection matches the sole-owner policy without implying production mutation authority.
- The minimum release infrastructure is live in staging with retention and least-privilege controls.
- Production remains unmodified and exactly NOT RUN.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-24-SUMMARY.md` when done.
</output>
