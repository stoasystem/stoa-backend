---
phase: 474-deterministic-verification-and-gated-delivery
plan: 15
type: execute
wave: 4
depends_on: [474-04]
files_modified:
  - /Users/zhdeng/stoa-frontend/package.json
  - /Users/zhdeng/stoa-frontend/package-lock.json
  - /Users/zhdeng/stoa-frontend/public/runtime-config.json.template
  - /Users/zhdeng/stoa-frontend/schemas/release/runtime-config-v1.schema.json
  - /Users/zhdeng/stoa-frontend/src/lib/runtimeConfig.ts
  - /Users/zhdeng/stoa-frontend/src/lib/env.ts
  - /Users/zhdeng/stoa-frontend/src/lib/amplify.ts
  - /Users/zhdeng/stoa-frontend/src/lib/api.ts
  - /Users/zhdeng/stoa-frontend/src/main.tsx
  - /Users/zhdeng/stoa-frontend/src/vite-env.d.ts
  - /Users/zhdeng/stoa-frontend/vite.config.ts
  - /Users/zhdeng/stoa-frontend/tests/release/runtime-config.test.mjs
autonomous: true
requirements: [V9QUAL-03, V9QUAL-05, V9QUAL-06]
must_haves:
  truths:
    - "A clean `npm ci` uses the committed root Web lock and has zero unaccepted dependency blocker"
    - "Staging and production use byte-identical Web build artifacts per D-14"
    - "Only reviewed non-secret runtime configuration varies and production-facing config fails closed"
    - "The Expo mobile skeleton remains outside install/audit/build scope per D-12/D-18"
  artifacts:
    - path: /Users/zhdeng/stoa-frontend/src/lib/runtimeConfig.ts
      provides: "Closed runtime configuration loader/validator"
    - path: /Users/zhdeng/stoa-frontend/public/runtime-config.json.template
      provides: "Reviewed non-secret deployment-time config template"
    - path: /Users/zhdeng/stoa-frontend/tests/release/runtime-config.test.mjs
      provides: "Runtime schema, secret denial, digest, and build-byte tests"
  key_links:
    - from: /Users/zhdeng/stoa-frontend/src/main.tsx
      to: /runtime-config.json
      via: "load and validate before dynamically importing/rendering App"
---

<objective>
Repair the Web dependency baseline and separate reviewed runtime configuration from generic build bytes.

Purpose: Make `npm ci` reproducible and allow true build-once Web promotion without compile-time staging/production differences.
Output: Clean root lock/audit, closed runtime-config contract, fail-closed bootstrap, and byte-identity tests.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-04-SUMMARY.md
@/Users/zhdeng/stoa-frontend/package.json
@/Users/zhdeng/stoa-frontend/package-lock.json
@/Users/zhdeng/stoa-frontend/src/lib/env.ts
@/Users/zhdeng/stoa-frontend/src/lib/amplify.ts
@/Users/zhdeng/stoa-frontend/src/lib/api.ts
@/Users/zhdeng/stoa-frontend/src/main.tsx
@/Users/zhdeng/stoa-frontend/vite.config.ts
@/Users/zhdeng/stoa-frontend/.github/workflows/deploy.yml
</context>

## Artifacts this phase produces

- New script `typecheck` in frontend `package.json` using `tsc -b`; existing `build` continues to typecheck and run Vite.
- Repaired root `package-lock.json` with zero unaccepted Critical/High or production-reachable Medium advisory, including the measured production-reachable `form-data` path.
- New closed schema `schemas/release/runtime-config-v1.schema.json` and template `public/runtime-config.json.template` with `schema`, `appEnv`, `apiMode`, `apiBaseUrl`, `websocketBaseUrl`, Cognito public IDs, and closed boolean feature flags; no secret-shaped fields.
- New `RuntimeConfig`, `RuntimeConfigError`, `parseRuntimeConfig`, `loadRuntimeConfig`, `runtimeConfigDigest`, and `initializeRuntimeConfig` exports in `src/lib/runtimeConfig.ts`.
- Refactored live exports in `src/lib/env.ts`, `getAmplifyConfig` in `src/lib/amplify.ts`, and `createApiClient` in `src/lib/api.ts` initialized only after config validation.
- Refactored `src/main.tsx` bootstrap that loads config then dynamically imports `App`; invalid/missing production-facing config renders one bounded failure state and makes no business request.
- New Node test file `tests/release/runtime-config.test.mjs` covering schema closure, secret/unknown field denial, origin allowlist, demo/production contradiction, digest stability, and identical build bytes across config fixtures.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| runtime JSON -> browser services | Config may inject secrets, unreviewed origins, demo mode, or provider identities. |
| lockfile/audit -> Web build | Vulnerable transitive packages or lock drift can enter release bytes. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-15-01 | Critical | Tampering | runtime config | mitigate/block | Closed schema, origin/demo/secret denial, digest binding, and bootstrap-before-App. |
| T-474-15-02 | High | Information disclosure | public config | mitigate/block | Allowlist non-secret keys and reject token/key/password/secret-shaped names/values. |
| T-474-15-03 | High | Tampering | package lock | mitigate/block | `npm ci`, lock hash, machine audit policy, and no mobile subtree. |
| T-474-15-SC | Critical | Tampering | npm installs | mitigate/block | Existing npm ecosystem only; exact committed lock and Plan 04 policy must pass after upgrades. |
</threat_model>

<tasks>

<task type="auto" id="474-15-01">
  <name>Repair the root Web advisory baseline and freeze explicit scripts</name>
  <files>/Users/zhdeng/stoa-frontend/package.json, /Users/zhdeng/stoa-frontend/package-lock.json</files>
  <read_first>
    - /Users/zhdeng/stoa-frontend/package.json
    - /Users/zhdeng/stoa-frontend/package-lock.json
    - scripts/dependency_policy.py
    - tests/test_dependency_policy.py
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md measured Web baseline
  </read_first>
  <action>Add the explicit `typecheck` script and use targeted npm updates/overrides only where required to move the two High, one Moderate, one Low findings—including production-reachable `form-data`—to compatible patched versions. Regenerate only the root lock, run clean `npm ci`, and evaluate `npm audit --package-lock-only --json` through Plan 04. Do not audit/edit `mobile/`, add a wildcard exception, or leave a compatible fix unapplied; an actually unfixable blocker stops for exact D-11 owner decision.</action>
  <verify><automated>bash -lc 'cd /Users/zhdeng/stoa-frontend && npm ci && npm run lint && npm run typecheck && /Users/zhdeng/stoa-backend/.venv/bin/python /Users/zhdeng/stoa-backend/scripts/dependency_policy.py check-web --frontend-root /Users/zhdeng/stoa-frontend --lock package-lock.json'</automated></verify>
  <acceptance_criteria>
    - Root `npm ci`, lint, and standalone typecheck pass from the committed lock.
    - Zero unaccepted Critical/High/reachable-Medium advisory remains.
    - `mobile/` is untouched and not scanned as release scope.
  </acceptance_criteria>
  <done>The Web install/dependency baseline is clean and explicit.</done>
</task>

<task type="auto" id="474-15-02" tdd="true">
  <name>Create the closed non-secret runtime configuration contract</name>
  <files>/Users/zhdeng/stoa-frontend/public/runtime-config.json.template, /Users/zhdeng/stoa-frontend/schemas/release/runtime-config-v1.schema.json, /Users/zhdeng/stoa-frontend/src/lib/runtimeConfig.ts, /Users/zhdeng/stoa-frontend/tests/release/runtime-config.test.mjs</files>
  <behavior>
    - Only allowlisted non-secret fields and exact HTTPS/approved WebSocket origins are accepted.
    - Production/staging-pilot cannot enable mock/demo/fallback/internal surfaces.
    - Canonical config bytes produce a stable SHA-256 and any field change changes it.
  </behavior>
  <read_first>
    - /Users/zhdeng/stoa-frontend/public/runtime-config.json.template
    - /Users/zhdeng/stoa-frontend/schemas/release/runtime-config-v1.schema.json
    - /Users/zhdeng/stoa-frontend/src/lib/runtimeConfig.ts
    - /Users/zhdeng/stoa-frontend/src/lib/env.ts
    - /Users/zhdeng/stoa-frontend/src/lib/amplify.ts
    - /Users/zhdeng/stoa-frontend/src/lib/api.ts
    - /Users/zhdeng/stoa-frontend/tests/release/runtime-config.test.mjs
    - /Users/zhdeng/stoa-frontend/.github/workflows/deploy.yml
  </read_first>
  <action>Define the closed schema/template and implement the named typed loader/validator/digest functions using existing Zod plus Web Crypto/Node crypto as appropriate. Enforce exact enums, no additional properties, reviewed API/WebSocket origin allowlists, public Cognito identifiers, no secret-shaped keys, and production-facing no-demo/no-mock/no-fallback constraints. Fetch with `cache: no-store`, bounded status/content-type/size checks, and safe error codes.</action>
  <verify><automated>bash -lc 'cd /Users/zhdeng/stoa-frontend && node --test tests/release/runtime-config.test.mjs && npm run typecheck'</automated></verify>
  <acceptance_criteria>
    - Schema/template/TypeScript model agree exactly.
    - Secret, unknown, demo-production, invalid-origin, oversized, malformed, and digest-tamper cases fail.
    - Valid staging and production fixtures differ only in runtime-config bytes/digest.
  </acceptance_criteria>
  <done>Environment variance is a reviewed, non-secret, digest-bound runtime document.</done>
</task>

<task type="auto" id="474-15-03" tdd="true">
  <name>Bootstrap runtime config before any Web application/service module</name>
  <files>/Users/zhdeng/stoa-frontend/src/lib/env.ts, /Users/zhdeng/stoa-frontend/src/lib/amplify.ts, /Users/zhdeng/stoa-frontend/src/lib/api.ts, /Users/zhdeng/stoa-frontend/src/main.tsx, /Users/zhdeng/stoa-frontend/src/vite-env.d.ts, /Users/zhdeng/stoa-frontend/vite.config.ts, /Users/zhdeng/stoa-frontend/tests/release/runtime-config.test.mjs</files>
  <behavior>
    - Runtime config initializes before static App/service evaluation and API/Cognito client construction.
    - Production build bytes do not depend on release-varying VITE values.
    - Invalid/missing config fails closed before business requests or demo fallback.
  </behavior>
  <read_first>
    - /Users/zhdeng/stoa-frontend/src/lib/env.ts
    - /Users/zhdeng/stoa-frontend/src/lib/amplify.ts
    - /Users/zhdeng/stoa-frontend/src/lib/api.ts
    - /Users/zhdeng/stoa-frontend/src/main.tsx
    - /Users/zhdeng/stoa-frontend/src/vite-env.d.ts
    - /Users/zhdeng/stoa-frontend/vite.config.ts
    - /Users/zhdeng/stoa-frontend/src/lib/runtimeConfig.ts
    - /Users/zhdeng/stoa-frontend/tests/release/runtime-config.test.mjs
  </read_first>
  <action>Refactor compile-time exports to initialized live bindings/getters and change `main.tsx` to validate runtime config before dynamically importing `App`, ensuring modules that construct HTTP clients see initialized values. Remove release-varying VITE inputs from production build semantics; retain only build-invariant metadata. Make `getAmplifyConfig`/`createApiClient` consume validated config. Add tests that build twice with different staging/production runtime fixtures and compare the full `dist` tree digest byte-for-byte, while separately validating distinct runtime config digests.</action>
  <verify><automated>bash -lc 'cd /Users/zhdeng/stoa-frontend && npm run lint && npm run typecheck && npm run build && node --test tests/release/runtime-config.test.mjs'</automated></verify>
  <acceptance_criteria>
    - App/service modules are not evaluated before config initialization.
    - Missing/invalid production-facing config renders a bounded error and makes zero business requests.
    - Two config fixtures produce identical generic build-tree digest and different reviewed config digests.
  </acceptance_criteria>
  <done>The Web artifact can be built once and promoted unchanged with separately bound runtime config.</done>
</task>

</tasks>

<verification>
From a clean root install run audit, lint, typecheck, two generic builds, runtime-config Node tests, and tree-digest comparison; inspect built bytes for release-varying endpoints/Cognito IDs and secret-shaped material.
</verification>

<success_criteria>
- The measured Web advisories are repaired or execution stops for explicit owner decision.
- Runtime configuration is closed, reviewed, non-secret, and fail-closed.
- Staging/production generic Web artifacts are byte-identical per D-14.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-15-SUMMARY.md` when done.
</output>
