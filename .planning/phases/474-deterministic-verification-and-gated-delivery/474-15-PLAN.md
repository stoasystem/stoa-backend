---
phase: 474-deterministic-verification-and-gated-delivery
plan: 15
type: execute
wave: 8
depends_on: [474-07, 474-08, 474-12, 474-13]
files_modified:
  - "src/stoa/routers/admin.py"
  - "src/stoa/routers/auth.py"
  - "src/stoa/routers/teacher_applications.py"
  - "src/stoa/routers/teachers.py"
autonomous: true
requirements: [V9QUAL-04]
must_haves:
  truths:
    - "The auth admin and teacher routers slice is fully typed without broad suppression."
    - "Runtime regression behavior is unchanged."
  artifacts:
    - path: "src/stoa/routers/admin.py"
      provides: "Auth admin and teacher routers"
    - path: "src/stoa/routers/auth.py"
      provides: "Auth admin and teacher routers"
    - path: "src/stoa/routers/teacher_applications.py"
      provides: "Auth admin and teacher routers"
    - path: "src/stoa/routers/teachers.py"
      provides: "Auth admin and teacher routers"
  key_links:
    - from: "474-13"
      to: "src/stoa/routers/admin.py"
      via: "declared dependency and immutable receipt"
      pattern: 'admin\.py'
---

<objective>
Auth admin and teacher routers (D-09, D-10).

Purpose: Close one real mypy root-cause family while retaining existing behavior and direct producer/consumer ordering.
Output: `src/stoa/routers/admin.py`, `src/stoa/routers/auth.py`, `src/stoa/routers/teacher_applications.py`, `src/stoa/routers/teachers.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `src/stoa/routers/admin.py` — Auth admin and teacher routers.
- `src/stoa/routers/auth.py` — Auth admin and teacher routers.
- `src/stoa/routers/teacher_applications.py` — Auth admin and teacher routers.
- `src/stoa/routers/teachers.py` — Auth admin and teacher routers.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type auth and teacher-application routes</name>
  <files>src/stoa/routers/auth.py, src/stoa/routers/teacher_applications.py</files>
  <behavior>
    - Previously measured diagnostics in the owned file set become zero.
    - Malformed/untrusted values remain explicitly narrowed before use.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - src/stoa/routers/auth.py
    - src/stoa/routers/teacher_applications.py
  </read_first>
  <action>Repair the measured full-scope mypy errors in the auth and teacher-application routes family, limited to src/stoa/routers/auth.py, src/stoa/routers/teacher_applications.py. Start from the exact 435-error/113-file command and preserve runtime authorization, privacy, idempotency, retry, fence, pagination, and stable-error semantics. Use closed TypedDict/dataclass records, explicit Optional/union narrowing, maintained stubs, or narrow project-owned Protocols at provider boundaries per D-09/D-10. Do not add or broaden Any, casts that hide unvalidated input, type: ignore, excludes, follow_imports=skip, ignore_missing_imports, skips, xfails, retries, or weaker assertions. This task implements D-09, D-10.</action>
  <verify>
    <automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/routers/auth.py src/stoa/routers/teacher_applications.py &amp;&amp; .venv/bin/python -m pytest -q tests/test_auth_security.py tests/test_public_identity_lifecycle.py &amp;&amp; .venv/bin/ruff check src/stoa/routers/auth.py src/stoa/routers/teacher_applications.py'</automated>
  </verify>
  <acceptance>
    - The auth and teacher-application routes files pass focused mypy and Ruff with no semantic suppression.
    - Named runtime regressions remain green.
  </acceptance>
  <done>The auth and teacher-application routes files pass focused mypy and Ruff with no semantic suppression. Named runtime regressions remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Type admin and teacher routes</name>
  <files>src/stoa/routers/admin.py, src/stoa/routers/teachers.py</files>
  <behavior>
    - Previously measured diagnostics in the owned file set become zero.
    - Malformed/untrusted values remain explicitly narrowed before use.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - src/stoa/routers/admin.py
    - src/stoa/routers/teachers.py
  </read_first>
  <action>Repair the measured full-scope mypy errors in the admin and teacher routes family, limited to src/stoa/routers/admin.py, src/stoa/routers/teachers.py. Start from the exact 435-error/113-file command and preserve runtime authorization, privacy, idempotency, retry, fence, pagination, and stable-error semantics. Use closed TypedDict/dataclass records, explicit Optional/union narrowing, maintained stubs, or narrow project-owned Protocols at provider boundaries per D-09/D-10. Do not add or broaden Any, casts that hide unvalidated input, type: ignore, excludes, follow_imports=skip, ignore_missing_imports, skips, xfails, retries, or weaker assertions. This task implements D-09, D-10.</action>
  <verify>
    <automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/routers/admin.py src/stoa/routers/teachers.py &amp;&amp; .venv/bin/python -m pytest -q tests/test_authorization_audit.py tests/test_conversations.py &amp;&amp; .venv/bin/ruff check src/stoa/routers/admin.py src/stoa/routers/teachers.py'</automated>
  </verify>
  <acceptance>
    - The admin and teacher routes files pass focused mypy and Ruff with no semantic suppression.
    - Named runtime regressions remain green.
  </acceptance>
  <done>The admin and teacher routes files pass focused mypy and Ruff with no semantic suppression. Named runtime regressions remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS / STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-15-01 | High | Tampering / Repudiation | Auth admin and teacher routers | mitigate/block | Bind exact source, inputs, command, result, and artifact identities; reject missing, duplicate, unknown, dirty, or mismatched values. |
| T-474-15-02 | High | Spoofing / Elevation | verification-to-delivery boundary | mitigate/block | Apply least privilege, protected environment checks, and dependency-closed eligibility before authority is exposed. |
| T-474-SC | High | Tampering | audited package installs | mitigate/block | Use only packages marked OK in RESEARCH.md Package Legitimacy Audit; [ASSUMED]/[SUS] requires a blocking human checkpoint and [SLOP] is forbidden. |
</threat_model>

<verification>
<automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/routers/admin.py src/stoa/routers/teachers.py &amp;&amp; .venv/bin/python -m pytest -q tests/test_authorization_audit.py tests/test_conversations.py &amp;&amp; .venv/bin/ruff check src/stoa/routers/admin.py src/stoa/routers/teachers.py'</automated>
</verification>

<success_criteria>
- The auth admin and teacher routers slice is fully typed without broad suppression.
- Runtime regression behavior is unchanged.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless a later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-15-SUMMARY.md` when done.
</output>
