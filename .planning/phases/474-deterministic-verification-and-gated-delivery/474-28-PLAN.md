---
phase: 474-deterministic-verification-and-gated-delivery
plan: 28
type: execute
wave: 15
depends_on: [474-27, 474-78]
files_modified:
  - "/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml"
  - "tests/test_infra_workflow_contract.py"
autonomous: true
requirements: [V9QUAL-01, V9QUAL-06]
must_haves:
  truths:
    - "Infrastructure automation cannot substitute source or gate semantics."
    - "Production policy may be proven but production mutation remains absent."
  artifacts:
    - path: "/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml"
      provides: "Thin exact-ref infrastructure workflow (D-01, D-05, D-16)"
    - path: "tests/test_infra_workflow_contract.py"
      provides: "Thin exact-ref infrastructure workflow (D-01, D-05, D-16)"
  key_links:
    - from: "474-78"
      to: "/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml"
      via: "declared dependency and source-bound verification"
      pattern: 'deploy\.yml'
---

<objective>
Thin exact-ref infrastructure workflow (D-01, D-05, D-16).

Purpose: Make infrastructure automation a thin, reviewed staging-only consumer of the common release identity.
Output: `/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml`, `tests/test_infra_workflow_contract.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml` — Thin exact-ref infrastructure workflow (D-01, D-05, D-16)
- `tests/test_infra_workflow_contract.py` — Thin exact-ref infrastructure workflow (D-01, D-05, D-16)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fence infrastructure workflow identity and credentials</name>
  <files>/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml, tests/test_infra_workflow_contract.py</files>
  <behavior>
    - Exact receipted refs reach staging eligibility.
    - Mutable refs or early credentials cannot.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - /Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
    - tests/test_infra_workflow_contract.py
  </read_first>
  <action>Pin all actions by full SHA, accept only exact receipted backend/infra refs, run frozen infra tests/synth/diff before credentials, and expose OIDC only to reviewed environment jobs. Reject mutable refs, unrelated backend HEAD, credentials in verify, stale-dist flags, or an alternate quality command per D-01/D-16. This task implements D-01, D-05, D-16.</action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_infra_workflow_contract.py -k 'identity or permission or action or gate'</automated>
  </verify>
  <acceptance>
    - Infra CI is a thin exact-ref caller with no pre-gate authority.
    - Every mutable or bypass input fails contract tests.
  </acceptance>
  <done>Infra CI is a thin exact-ref caller with no pre-gate authority. Every mutable or bypass input fails contract tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Limit infrastructure automation to staging and exact production NOT RUN</name>
  <files>/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml, tests/test_infra_workflow_contract.py</files>
  <behavior>
    - Staging-only workflow passes.
    - Any production mutation command fails the contract.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - /Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
    - tests/test_infra_workflow_contract.py
  </read_first>
  <action>Allow only reviewed staging substrate apply after inventory/protected authority. Define production policy/config proof but issue no production CDK/application command in Phase 474. Emit exact NOT RUN for production infrastructure/deploy/smoke/rollback and reject any direct prod stack/function/bucket mutation. This task implements D-01, D-05, D-16.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_infra_workflow_contract.py &amp;&amp; .venv/bin/ruff check tests/test_infra_workflow_contract.py'</automated>
  </verify>
  <acceptance>
    - Staging apply is dependency-closed and environment-protected.
    - Production operations remain exact NOT RUN.
  </acceptance>
  <done>Staging apply is dependency-closed and environment-protected. Production operations remain exact NOT RUN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS V1-V14 Applicability

| ASVS | Control family | Disposition | Plan control |
|---|---|---|---|
| V1 | Architecture | apply | Keep the plan dependency-closed, source-bound, and independently verifiable. |
| V2 | Authentication | preserve | Do not introduce an alternate identity mechanism; smoke uses existing bounded auth. |
| V3 | Session Management | preserve | Do not broaden session lifetime or expose browser/session material in evidence. |
| V4 | Access Control | apply | Use least privilege and protected environment/OIDC boundaries before authority. |
| V5 | Validation | apply | Validate paths, refs, schemas, provider values, counts, and closed outcomes. |
| V6 | Cryptography | apply | Use SHA-256/AWS-managed encryption only; reject digest mismatch. |
| V7 | Error/Logging | apply | Emit stable redacted errors and identifiers without credentials or private payloads. |
| V8 | Data Protection | apply | Retain only required hashes, IDs, statuses, and policy evidence. |
| V9 | Communications | apply | Deny ambient network and constrain provider calls to reviewed endpoints/roles. |
| V10 | Malicious Code | apply | Use audited locks, immutable action SHAs, and tamper tests. |
| V11 | Business Logic | apply | Fail closed on missing prerequisites, stale identity, or partial outcome. |
| V12 | Files/Resources | apply | Use bounded paths, immutable blobs/objects, and resource limits. |
| V13 | API/Web Services | apply | Validate structured API/provider responses and preserve authorization semantics. |
| V14 | Configuration | apply | Use reviewed closed configuration with explicit production NOT RUN state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-474-28-S | Spoofing | source/provider identity | mitigate/block | Require exact execution-derived Git/provider identity and authorized actor/role. |
| T-474-28-T | Tampering | Thin exact-ref infrastructure workflow (D-01, D-05, D-16) | mitigate/block | Bind inputs, commands, results, and artifacts; reject mismatch, drift, or unknown fields. |
| T-474-28-R | Repudiation | evidence receipt | mitigate/block | Retain canonical source-bound command/result/request identities. |
| T-474-28-I | Information Disclosure | diagnostics/evidence | mitigate/block | Store redacted IDs, hashes, counts, and statuses only. |
| T-474-28-D | Denial of Service | bounded execution | mitigate/block | Apply bounded time/resource scopes and fail closed without partial PASS. |
| T-474-28-E | Elevation of Privilege | verification-to-delivery boundary | mitigate/block | Use least privilege, dependency-closed eligibility, and protected environments. |
| T-474-28-SC | Tampering | package/action supply chain | mitigate/block | Use only RESEARCH-approved packages and immutable reviewed Action SHAs; [SLOP] is forbidden. |
</threat_model>

<verification>
  <automated>.venv/bin/python -m pytest -q tests/test_infra_workflow_contract.py -k 'identity or permission or action or gate'</automated>
  <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_infra_workflow_contract.py &amp;&amp; .venv/bin/ruff check tests/test_infra_workflow_contract.py'</automated>
</verification>

<success_criteria>
- Infrastructure automation cannot substitute source or gate semantics.
- Production policy may be proven but production mutation remains absent.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-28-SUMMARY.md` when done.
</output>
