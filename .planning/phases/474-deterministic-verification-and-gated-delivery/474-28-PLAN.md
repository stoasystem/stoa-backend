---
phase: 474-deterministic-verification-and-gated-delivery
plan: 28
type: execute
wave: 2
depends_on: [474-05]
files_modified:
  - scripts/build_lambda_dist.py
  - tests/test_lambda_dist_build.py
autonomous: true
requirements: [V9QUAL-06]
must_haves:
  truths:
    - "Build a reproducible lock-bound Lambda artifact is independently complete and cannot be bypassed by another plan."
    - "Failure of the exact automated command blocks every dependent plan"
    - "D-09/D-10 typing integrity, Web-first scope, and production NOT RUN remain immutable where applicable"
  artifacts:
    - path: scripts/build_lambda_dist.py
      provides: "Exclusive artifact for build a reproducible lock-bound lambda artifact"
    - path: tests/test_lambda_dist_build.py
      provides: "Exclusive artifact for build a reproducible lock-bound lambda artifact"
  key_links:
    - from: scripts/build_lambda_dist.py
      to: "Task 474-28-01 verification"
      via: "exact automated command and acceptance criteria"
---

<objective>
Build a reproducible lock-bound Lambda artifact.

Purpose: Deliver one cohesive, independently verifiable outcome without overlapping another plan's file ownership.
Output: `scripts/build_lambda_dist.py`, `tests/test_lambda_dist_build.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-05-SUMMARY.md
@scripts/build_lambda_dist.py
@tests/test_lambda_dist_build.py
@requirements.txt
@uv.lock
@schemas/release/release-manifest-v1.schema.json
</context>

## Artifacts this phase produces

- `scripts/build_lambda_dist.py` is created or modified only for build a reproducible lock-bound lambda artifact.
- `tests/test_lambda_dist_build.py` is created or modified only for build a reproducible lock-bound lambda artifact.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted input/provider/source -> owned boundary | Malformed or substituted values can falsify this plan's independently verified outcome. |
| owned result -> downstream plan | A weakened, partial, or non-source-bound result could create false release eligibility. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-28-01 | Critical | Tampering | owned contract and result | mitigate/block | Exact file ownership, closed inputs/results, and Task 28-01 automated verification. |
| T-474-28-02 | High | Elevation of privilege | downstream eligibility | mitigate/block | Any missing/non-pass outcome blocks dependencies; no bypass, broad suppression, or inferred success. |
| T-474-28-03 | High | Information disclosure | tests/evidence/errors | mitigate/block | Preserve bounded redaction and reject credentials, tokens, private content, and provider payloads. |
| T-474-28-SC | High | Tampering | package/action supply chain | mitigate/block | No unreviewed install/action; frozen audited locks and the Package Legitimacy Audit remain authoritative. |

## ASVS 4.0.3 L1 Control Mapping

| ASVS L1 category | Applicability and concrete control | Verification |
|---|---|---|
| V1 Architecture | Applicable — exclusive file ownership, explicit dependencies, and fail-closed boundaries are part of this plan. | Task 28-01 automated command and this plan's focused acceptance criteria. |
| V2 Authentication | N/A — this plan neither issues nor validates credentials; upstream authentication behavior is unchanged and regression-tested. | Task 28-01 automated command and this plan's focused acceptance criteria. |
| V3 Session Management | N/A — no session creation, renewal, storage, or termination occurs in this scope. | Task 28-01 automated command and this plan's focused acceptance criteria. |
| V4 Access Control | Applicable — authorization/release authority remains deny-first; any High/Critical failure blocks completion. | Task 28-01 automated command and this plan's focused acceptance criteria. |
| V5 Validation | Applicable — untrusted records, command inputs, and result unions are narrowed before use; malformed values fail. | Task 28-01 automated command and this plan's focused acceptance criteria. |
| V6 Cryptography | Applicable — SHA-256/content identities are recomputed and never substitute for secret-key controls. | Task 28-01 automated command and this plan's focused acceptance criteria. |
| V7 Error Handling and Logging | Applicable — stable bounded errors/results are verified and private/provider payloads are not logged. | Task 28-01 automated command and this plan's focused acceptance criteria. |
| V8 Data Protection | Applicable — tests preserve redaction and prevent secret, token, student-content, or provider-payload disclosure. | Task 28-01 automated command and this plan's focused acceptance criteria. |
| V9 Communications | Applicable — real/provider/network boundaries require approved origins, denial, or immutable transport identity. | Task 28-01 automated command and this plan's focused acceptance criteria. |
| V10 Malicious Code | Applicable — no unreviewed package/action is added; the SC threat and locked dependency policy remain blocking. | Task 28-01 automated command and this plan's focused acceptance criteria. |
| V11 Business Logic | Applicable — focused tests preserve exact state, race, retry, idempotency, and fail-closed outcomes relevant to the owned files. | Task 28-01 automated command and this plan's focused acceptance criteria. |
| V12 Files and Resources | Applicable — every modified path is enumerated and artifact/evidence bytes or source files are bounded by exact ownership. | Task 28-01 automated command and this plan's focused acceptance criteria. |
| V13 API and Web Services | N/A — this plan does not expose or consume an API/Web-service contract; its typed boundary is exercised locally. | Task 28-01 automated command and this plan's focused acceptance criteria. |
| V14 Configuration | Applicable — exact tool/scope/configuration is verified and broad ignores, mutable refs, or permissive defaults are forbidden. | Task 28-01 automated command and this plan's focused acceptance criteria. |

Any failed applicable ASVS L1 control, or any High/Critical STRIDE threat without its named mitigation and test, blocks plan completion.
</threat_model>

<tasks>

<task type="auto" id="474-28-01" tdd="true">
  <name>Build a reproducible lock-bound Lambda artifact</name>
  <files>scripts/build_lambda_dist.py, tests/test_lambda_dist_build.py</files>
  <behavior>
    - Two builds from the same clean source/lock produce identical ZIP bytes and SHA-256.
    - Committed requirements must equal a fresh locked production export.
    - Both handlers, Python 3.12, manylinux2014_aarch64, and arm64 remain mandatory.
  </behavior>
  <read_first>
    - scripts/build_lambda_dist.py
    - tests/test_lambda_dist_build.py
    - requirements.txt
    - uv.lock
    - schemas/release/release-manifest-v1.schema.json
  </read_first>
  <action>Replace host-metadata-dependent `archive.write` behavior with explicit normalized `ZipInfo` construction over sorted paths, fixed timestamp/modes, stable compression, and no host extras. Before install/build, compare committed `requirements.txt` with `uv export --format requirements-txt --no-dev --no-emit-project --locked`. Hash final ZIP bytes and include the digest/bytes in the builder manifest. Preserve handler inventory and runtime constants, reject stale dist, and add two-directory/two-time builds proving exact equality plus one-byte/timestamp/mode/requirements drift rejection.</action>
  <verify><automated>bash -lc '.venv/bin/python -m pytest -q tests/test_lambda_dist_build.py tests/test_release_manifest.py && .venv/bin/ruff check scripts/build_lambda_dist.py tests/test_lambda_dist_build.py'</automated></verify>
  <acceptance_criteria>
    - Repeated fixture builds produce the same final ZIP digest and bytes.
    - Lock/export, handler, runtime/platform/architecture, source, and ZIP metadata drift fail.
    - The final digest is suitable for both staging and unchanged production promotion.
  </acceptance_criteria>
  <done>The backend artifact is content-addressed, reproducible, and bound to the frozen release identity.</done>
</task>

</tasks>

<verification>
Run `bash -lc '.venv/bin/python -m pytest -q tests/test_lambda_dist_build.py tests/test_release_manifest.py && .venv/bin/ruff check scripts/build_lambda_dist.py tests/test_lambda_dist_build.py'`. Treat any nonzero result, missing artifact, node loss, skip/xfail/retry, secret disclosure, or ownership drift as blocking.
</verification>

<success_criteria>
- Build a reproducible lock-bound Lambda artifact is complete as one independently testable outcome.
- Every owned file and required behavior passes the task's exact automated command.
- No other plan shares same-wave write ownership, and no locked decision or deferred boundary is weakened.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-28-SUMMARY.md` when done.
</output>
