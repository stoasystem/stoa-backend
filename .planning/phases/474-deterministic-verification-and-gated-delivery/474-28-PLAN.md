---
phase: 474-deterministic-verification-and-gated-delivery
plan: 28
type: execute
wave: 13
depends_on: [474-26, 474-27]
files_modified:
  - "/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml"
  - "tests/test_infra_workflow_contract.py"
autonomous: true
requirements: [V9QUAL-01, V9QUAL-06]
must_haves:
  truths:
    - "Infrastructure automation cannot substitute source or gate semantics."
    - "Production policy may be proven but production mutation remains absent."
  artifacts:
    - path: "/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml"
      provides: "Thin exact-ref infrastructure workflow"
    - path: "tests/test_infra_workflow_contract.py"
      provides: "Thin exact-ref infrastructure workflow"
  key_links:
    - from: "474-27"
      to: "/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml"
      via: "declared dependency and immutable receipt"
      pattern: 'deploy\.yml'
---

<objective>
Thin exact-ref infrastructure workflow (D-01, D-05, D-16).

Purpose: Make infrastructure automation a thin, reviewed staging-only consumer of the common release identity.
Output: `/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml`, `tests/test_infra_workflow_contract.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml` — Thin exact-ref infrastructure workflow.
- `tests/test_infra_workflow_contract.py` — Thin exact-ref infrastructure workflow.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fence infrastructure workflow identity and credentials</name>
  <files>/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml, tests/test_infra_workflow_contract.py</files>
  <behavior>
    - Exact receipted refs reach staging eligibility.
    - Mutable refs or early credentials cannot.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - /Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
  </read_first>
  <action>Pin all actions by full SHA, accept only exact receipted backend/infra refs, run frozen infra tests/synth/diff before credentials, and expose OIDC only to reviewed environment jobs. Reject mutable refs, unrelated backend HEAD, credentials in verify, stale-dist flags, or an alternate quality command per D-01/D-16. This task implements D-01, D-05, D-16.</action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_infra_workflow_contract.py -k 'identity or permission or action or gate'</automated>
  </verify>
  <acceptance>
    - Infra CI is a thin exact-ref caller with no pre-gate authority.
    - Every mutable or bypass input fails contract tests.
  </acceptance>
  <done>Infra CI is a thin exact-ref caller with no pre-gate authority. Every mutable or bypass input fails contract tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Limit infrastructure automation to staging and exact production NOT RUN</name>
  <files>/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml, tests/test_infra_workflow_contract.py</files>
  <behavior>
    - Staging-only workflow passes.
    - Any production mutation command fails the contract.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - /Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
  </read_first>
  <action>Allow only reviewed staging substrate apply after inventory/protected authority. Define production policy/config proof but issue no production CDK/application command in Phase 474. Emit exact NOT RUN for production infrastructure/deploy/smoke/rollback and reject any direct prod stack/function/bucket mutation. This task implements D-01, D-05, D-16.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_infra_workflow_contract.py &amp;&amp; .venv/bin/ruff check tests/test_infra_workflow_contract.py'</automated>
  </verify>
  <acceptance>
    - Staging apply is dependency-closed and environment-protected.
    - Production operations remain exact NOT RUN.
  </acceptance>
  <done>Staging apply is dependency-closed and environment-protected. Production operations remain exact NOT RUN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS / STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-28-01 | High | Tampering / Repudiation | Thin exact-ref infrastructure workflow | mitigate/block | Bind exact source, inputs, command, result, and artifact identities; reject missing, duplicate, unknown, dirty, or mismatched values. |
| T-474-28-02 | High | Spoofing / Elevation | verification-to-delivery boundary | mitigate/block | Apply least privilege, protected environment checks, and dependency-closed eligibility before authority is exposed. |
| T-474-SC | High | Tampering | audited package installs | mitigate/block | Use only packages marked OK in RESEARCH.md Package Legitimacy Audit; [ASSUMED]/[SUS] requires a blocking human checkpoint and [SLOP] is forbidden. |
</threat_model>

<verification>
<automated>bash -lc '.venv/bin/python -m pytest -q tests/test_infra_workflow_contract.py &amp;&amp; .venv/bin/ruff check tests/test_infra_workflow_contract.py'</automated>
</verification>

<success_criteria>
- Infrastructure automation cannot substitute source or gate semantics.
- Production policy may be proven but production mutation remains absent.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless a later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-28-SUMMARY.md` when done.
</output>
