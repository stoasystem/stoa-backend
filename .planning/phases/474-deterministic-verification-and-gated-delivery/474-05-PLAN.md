---
phase: 474-deterministic-verification-and-gated-delivery
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - schemas/release/release-manifest-v1.schema.json
  - tests/test_release_manifest.py
  - scripts/release_manifest.py
autonomous: true
requirements: [V9QUAL-06]
must_haves:
  truths:
    - "Define and validate the immutable cross-repository manifest is independently complete and cannot be bypassed by another plan."
    - "Failure of the exact automated command blocks every dependent plan"
    - "D-09/D-10 typing integrity, Web-first scope, and production NOT RUN remain immutable where applicable"
  artifacts:
    - path: schemas/release/release-manifest-v1.schema.json
      provides: "Exclusive artifact for define and validate the immutable cross-repository manifest"
    - path: tests/test_release_manifest.py
      provides: "Exclusive artifact for define and validate the immutable cross-repository manifest"
    - path: scripts/release_manifest.py
      provides: "Exclusive artifact for define and validate the immutable cross-repository manifest"
  key_links:
    - from: schemas/release/release-manifest-v1.schema.json
      to: "Task 474-05-01 verification"
      via: "exact automated command and acceptance criteria"
---

<objective>
Define and validate the immutable cross-repository manifest.

Purpose: Deliver one cohesive, independently verifiable outcome without overlapping another plan's file ownership.
Output: `schemas/release/release-manifest-v1.schema.json`, `tests/test_release_manifest.py`, `scripts/release_manifest.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
@schemas/release/release-manifest-v1.schema.json
@tests/test_release_manifest.py
@scripts/build_lambda_dist.py
@tests/test_lambda_dist_build.py
@scripts/release_evidence.py
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md D-13 through D-16
@scripts/release_manifest.py
</context>

## Artifacts this phase produces

- `schemas/release/release-manifest-v1.schema.json` is created or modified only for define and validate the immutable cross-repository manifest.
- `tests/test_release_manifest.py` is created or modified only for define and validate the immutable cross-repository manifest.
- `scripts/release_manifest.py` is created or modified only for define and validate the immutable cross-repository manifest.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted input/provider/source -> owned boundary | Malformed or substituted values can falsify this plan's independently verified outcome. |
| owned result -> downstream plan | A weakened, partial, or non-source-bound result could create false release eligibility. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-05-01 | Critical | Tampering | owned contract and result | mitigate/block | Exact file ownership, closed inputs/results, and Task 05-01 automated verification. |
| T-474-05-02 | High | Elevation of privilege | downstream eligibility | mitigate/block | Any missing/non-pass outcome blocks dependencies; no bypass, broad suppression, or inferred success. |
| T-474-05-03 | High | Information disclosure | tests/evidence/errors | mitigate/block | Preserve bounded redaction and reject credentials, tokens, private content, and provider payloads. |
| T-474-05-SC | High | Tampering | package/action supply chain | mitigate/block | No unreviewed install/action; frozen audited locks and the Package Legitimacy Audit remain authoritative. |

## ASVS 4.0.3 L1 Control Mapping

| ASVS L1 category | Applicability and concrete control | Verification |
|---|---|---|
| V1 Architecture | Applicable — exclusive file ownership, explicit dependencies, and fail-closed boundaries are part of this plan. | Task 05-01 automated command and this plan's focused acceptance criteria. |
| V2 Authentication | N/A — this plan neither issues nor validates credentials; upstream authentication behavior is unchanged and regression-tested. | Task 05-01 automated command and this plan's focused acceptance criteria. |
| V3 Session Management | N/A — no session creation, renewal, storage, or termination occurs in this scope. | Task 05-01 automated command and this plan's focused acceptance criteria. |
| V4 Access Control | Applicable — authorization/release authority remains deny-first; any High/Critical failure blocks completion. | Task 05-01 automated command and this plan's focused acceptance criteria. |
| V5 Validation | Applicable — untrusted records, command inputs, and result unions are narrowed before use; malformed values fail. | Task 05-01 automated command and this plan's focused acceptance criteria. |
| V6 Cryptography | Applicable — SHA-256/content identities are recomputed and never substitute for secret-key controls. | Task 05-01 automated command and this plan's focused acceptance criteria. |
| V7 Error Handling and Logging | Applicable — stable bounded errors/results are verified and private/provider payloads are not logged. | Task 05-01 automated command and this plan's focused acceptance criteria. |
| V8 Data Protection | Applicable — tests preserve redaction and prevent secret, token, student-content, or provider-payload disclosure. | Task 05-01 automated command and this plan's focused acceptance criteria. |
| V9 Communications | N/A — this plan performs no network or inter-process communication; existing transport controls are outside its ownership. | Task 05-01 automated command and this plan's focused acceptance criteria. |
| V10 Malicious Code | Applicable — no unreviewed package/action is added; the SC threat and locked dependency policy remain blocking. | Task 05-01 automated command and this plan's focused acceptance criteria. |
| V11 Business Logic | Applicable — focused tests preserve exact state, race, retry, idempotency, and fail-closed outcomes relevant to the owned files. | Task 05-01 automated command and this plan's focused acceptance criteria. |
| V12 Files and Resources | Applicable — every modified path is enumerated and artifact/evidence bytes or source files are bounded by exact ownership. | Task 05-01 automated command and this plan's focused acceptance criteria. |
| V13 API and Web Services | N/A — this plan does not expose or consume an API/Web-service contract; its typed boundary is exercised locally. | Task 05-01 automated command and this plan's focused acceptance criteria. |
| V14 Configuration | Applicable — exact tool/scope/configuration is verified and broad ignores, mutable refs, or permissive defaults are forbidden. | Task 05-01 automated command and this plan's focused acceptance criteria. |

Any failed applicable ASVS L1 control, or any High/Critical STRIDE threat without its named mitigation and test, blocks plan completion.
</threat_model>

<tasks>

<task type="auto" id="474-05-01" tdd="true">
  <name>Specify the closed cross-repository manifest and tamper matrix</name>
  <files>schemas/release/release-manifest-v1.schema.json, tests/test_release_manifest.py</files>
  <behavior>
    - Exact backend/Web/infra source, lock, artifact, runtime, config, verification, and gate identities are mandatory.
    - Any changed byte or identity invalidates the manifest.
    - PASS, FAIL, and exact NOT RUN production obligations remain distinct.
  </behavior>
  <read_first>
    - schemas/release/release-manifest-v1.schema.json
    - tests/test_release_manifest.py
    - scripts/build_lambda_dist.py
    - tests/test_lambda_dist_build.py
    - scripts/release_evidence.py
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md D-13 through D-16
  </read_first>
  <action>Create the closed schema and failing fixture tests for every field listed above. Require lowercase SHA-256, full Git SHAs, positive byte counts, exact runtime `python3.12`/`manylinux2014_aarch64`/`arm64`, complete gate IDs, and separate runtime-config digest. Derive release ID only from source/lock/runtime identities so it is non-circular; bind artifact bytes afterward. Reject branches, tags, `latest`, dirty flags, unknown gates, duplicate receipts, missing infra identity, and pass-shaped production NOT RUN.</action>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_release_manifest.py</automated></verify>
  <acceptance_criteria>
    - `additionalProperties` is false at every closed object boundary.
    - A single-axis mutation test exists for every identity/digest/status family.
    - Infra identity and exact production NOT RUN fields are mandatory.
  </acceptance_criteria>
  <done>The manifest contract is red until exact source/artifact hashing is implemented.</done>
</task>
<task type="auto" id="474-05-02" tdd="true">
  <name>Build and validate canonical cross-repository manifests</name>
  <files>scripts/release_manifest.py, tests/test_release_manifest.py</files>
  <behavior>
    - Clean detached repository identities and stable tree/lock hashes produce byte-identical manifests.
    - All artifact/config/receipt bytes are rehashed during validation.
    - Any mutable or missing identity returns a stable policy failure.
  </behavior>
  <read_first>
    - scripts/release_manifest.py
    - tests/test_release_manifest.py
    - schemas/release/release-manifest-v1.schema.json
    - scripts/build_lambda_dist.py
    - scripts/release_evidence.py
  </read_first>
  <action>Implement the named dataclasses/functions and `create`/`validate` subcommands. Hash files by streaming bytes and trees by sorted relative path plus NUL-delimited digest. Resolve exact Git commit/tree and clean status for all three repositories. Load canonical gate receipts, require the closed inventory, derive the non-circular release ID, bind final backend/Web/runtime-config digests, and compute canonical manifest digest. Never copy mutable source between repositories or infer identity from branch names.</action>
  <verify><automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_manifest.py && .venv/bin/ruff check scripts/release_manifest.py tests/test_release_manifest.py'</automated></verify>
  <acceptance_criteria>
    - Two creations from identical fixtures are byte-identical.
    - Dirty tree, moved ref, changed lock/source/artifact/config/receipt, and missing gate cases all fail.
    - Output contains no raw environment, credential, token, or private payload values.
  </acceptance_criteria>
  <done>One manifest deterministically joins all three source repositories to exact verified bytes.</done>
</task>

</tasks>

<verification>
Run `.venv/bin/python -m pytest -q tests/test_release_manifest.py`. Treat any nonzero result, missing artifact, node loss, skip/xfail/retry, secret disclosure, or ownership drift as blocking.
</verification>

<success_criteria>
- Define and validate the immutable cross-repository manifest is complete as one independently testable outcome.
- Every owned file and required behavior passes the task's exact automated command.
- No other plan shares same-wave write ownership, and no locked decision or deferred boundary is weakened.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-05-SUMMARY.md` when done.
</output>
