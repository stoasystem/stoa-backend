---
phase: 474-deterministic-verification-and-gated-delivery
plan: 05
type: execute
wave: 4
depends_on: [474-03]
files_modified:
  - "schemas/release/dependency-exceptions-v1.schema.json"
  - "scripts/dependency_policy.py"
  - "tests/test_dependency_policy.py"
  - "pyproject.toml"
  - "uv.lock"
  - "requirements.txt"
autonomous: true
requirements: [V9QUAL-04, V9QUAL-05]
must_haves:
  truths:
    - "Backend and Web dependency policy is exact, expiring, and lock-bound."
    - "Native mobile dependencies are outside the gate without excluding Web root dependencies."
  artifacts:
    - path: "schemas/release/dependency-exceptions-v1.schema.json"
      provides: "Dependency policy and measured advisory repair (D-09, D-10, D-11, D-12)"
    - path: "scripts/dependency_policy.py"
      provides: "Dependency policy and measured advisory repair (D-09, D-10, D-11, D-12)"
    - path: "tests/test_dependency_policy.py"
      provides: "Dependency policy and measured advisory repair (D-09, D-10, D-11, D-12)"
    - path: "pyproject.toml"
      provides: "Dependency policy and measured advisory repair (D-09, D-10, D-11, D-12)"
    - path: "uv.lock"
      provides: "Dependency policy and measured advisory repair (D-09, D-10, D-11, D-12)"
    - path: "requirements.txt"
      provides: "Dependency policy and measured advisory repair (D-09, D-10, D-11, D-12)"
  key_links:
    - from: "474-03"
      to: "schemas/release/dependency-exceptions-v1.schema.json"
      via: "declared dependency and source-bound verification"
      pattern: 'dependency\-exceptions\-v1\.schema\.json'
---

<objective>
Dependency policy and measured advisory repair (D-09, D-10, D-11, D-12).

Purpose: Turn both lockfiles into release-blocking dependency inputs and repair the measured backend red baseline.
Output: `schemas/release/dependency-exceptions-v1.schema.json`, `scripts/dependency_policy.py`, `tests/test_dependency_policy.py`, `pyproject.toml`, `uv.lock`, `requirements.txt`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `schemas/release/dependency-exceptions-v1.schema.json` — Dependency policy and measured advisory repair (D-09, D-10, D-11, D-12)
- `scripts/dependency_policy.py` — Dependency policy and measured advisory repair (D-09, D-10, D-11, D-12)
- `tests/test_dependency_policy.py` — Dependency policy and measured advisory repair (D-09, D-10, D-11, D-12)
- `pyproject.toml` — Dependency policy and measured advisory repair (D-09, D-10, D-11, D-12)
- `uv.lock` — Dependency policy and measured advisory repair (D-09, D-10, D-11, D-12)
- `requirements.txt` — Dependency policy and measured advisory repair (D-09, D-10, D-11, D-12)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement exact backend/Web advisory policy and exception schema</name>
  <files>schemas/release/dependency-exceptions-v1.schema.json, scripts/dependency_policy.py, tests/test_dependency_policy.py</files>
  <behavior>
    - Unaccepted blockers fail.
    - One exact unexpired exception matches only its bound advisory/version/lock.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - schemas/release/dependency-exceptions-v1.schema.json
    - scripts/dependency_policy.py
    - tests/test_dependency_policy.py
  </read_first>
  <action>Parse pip-audit and npm audit machine JSON against the authoritative backend export and frontend root lock. Block every backend runtime advisory when severity is unavailable, all Critical/High, and production-reachable Medium; require exact package/advisory/version/lock/reachability/owner/expiry/target for exceptions. Reject wildcards, expiry, drift, duplicates, unmatched blockers, and mobile-root substitution per D-11/D-12. Use only audited packages from RESEARCH.md. This task implements D-09, D-10, D-11, D-12.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_dependency_policy.py &amp;&amp; .venv/bin/ruff check scripts/dependency_policy.py tests/test_dependency_policy.py'</automated>
  </verify>
  <acceptance>
    - Both lockfiles have one fail-closed machine policy.
    - Exceptions cannot silently broaden or survive lock/advisory drift.
  </acceptance>
  <done>Both lockfiles have one fail-closed machine policy. Exceptions cannot silently broaden or survive lock/advisory drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair the measured backend runtime advisories and preserve frozen export</name>
  <files>pyproject.toml, uv.lock, requirements.txt</files>
  <behavior>
    - Fixed packages disappear from blocker output.
    - Lock/export drift fails before audit acceptance.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - pyproject.toml
    - uv.lock
    - requirements.txt
  </read_first>
  <action>Upgrade or replace the measured vulnerable runtime packages through uv while preserving supported behavior and exact export equality. Run focused regressions plus the policy evaluator. Do not add an exception while a supported fix exists; any genuinely unavailable fix remains release-blocking unless the owner later accepts the exact D-11 record. This task implements D-09, D-10, D-11, D-12.</action>
  <verify>
    <automated>bash -lc 'uv lock --check &amp;&amp; tmp=$(mktemp) &amp;&amp; uv export --format requirements-txt --no-dev --no-emit-project --locked > "$tmp" &amp;&amp; cmp "$tmp" requirements.txt &amp;&amp; .venv/bin/python scripts/dependency_policy.py check-backend --lock uv.lock --requirements requirements.txt'</automated>
  </verify>
  <acceptance>
    - The committed lock/export is internally consistent.
    - No measured backend blocker remains unaccepted.
  </acceptance>
  <done>The committed lock/export is internally consistent. No measured backend blocker remains unaccepted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS V1-V14 Applicability

| ASVS | Control family | Disposition | Plan control |
|---|---|---|---|
| V1 | Architecture | apply | Keep the plan dependency-closed, source-bound, and independently verifiable. |
| V2 | Authentication | preserve | Do not introduce an alternate identity mechanism; smoke uses existing bounded auth. |
| V3 | Session Management | preserve | Do not broaden session lifetime or expose browser/session material in evidence. |
| V4 | Access Control | apply | Use least privilege and protected environment/OIDC boundaries before authority. |
| V5 | Validation | apply | Validate paths, refs, schemas, provider values, counts, and closed outcomes. |
| V6 | Cryptography | apply | Use SHA-256/AWS-managed encryption only; reject digest mismatch. |
| V7 | Error/Logging | apply | Emit stable redacted errors and identifiers without credentials or private payloads. |
| V8 | Data Protection | apply | Retain only required hashes, IDs, statuses, and policy evidence. |
| V9 | Communications | apply | Deny ambient network and constrain provider calls to reviewed endpoints/roles. |
| V10 | Malicious Code | apply | Use audited locks, immutable action SHAs, and tamper tests. |
| V11 | Business Logic | apply | Fail closed on missing prerequisites, stale identity, or partial outcome. |
| V12 | Files/Resources | apply | Use bounded paths, immutable blobs/objects, and resource limits. |
| V13 | API/Web Services | apply | Validate structured API/provider responses and preserve authorization semantics. |
| V14 | Configuration | apply | Use reviewed closed configuration with explicit production NOT RUN state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-474-05-S | Spoofing | source/provider identity | mitigate/block | Require exact execution-derived Git/provider identity and authorized actor/role. |
| T-474-05-T | Tampering | Dependency policy and measured advisory repair (D-09, D-10, D-11, D-12) | mitigate/block | Bind inputs, commands, results, and artifacts; reject mismatch, drift, or unknown fields. |
| T-474-05-R | Repudiation | evidence receipt | mitigate/block | Retain canonical source-bound command/result/request identities. |
| T-474-05-I | Information Disclosure | diagnostics/evidence | mitigate/block | Store redacted IDs, hashes, counts, and statuses only. |
| T-474-05-D | Denial of Service | bounded execution | mitigate/block | Apply bounded time/resource scopes and fail closed without partial PASS. |
| T-474-05-E | Elevation of Privilege | verification-to-delivery boundary | mitigate/block | Use least privilege, dependency-closed eligibility, and protected environments. |
| T-474-05-SC | Tampering | package/action supply chain | mitigate/block | Use only RESEARCH-approved packages and immutable reviewed Action SHAs; [SLOP] is forbidden. |
</threat_model>

<verification>
  <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_dependency_policy.py &amp;&amp; .venv/bin/ruff check scripts/dependency_policy.py tests/test_dependency_policy.py'</automated>
  <automated>bash -lc 'uv lock --check &amp;&amp; tmp=$(mktemp) &amp;&amp; uv export --format requirements-txt --no-dev --no-emit-project --locked > "$tmp" &amp;&amp; cmp "$tmp" requirements.txt &amp;&amp; .venv/bin/python scripts/dependency_policy.py check-backend --lock uv.lock --requirements requirements.txt'</automated>
</verification>

<success_criteria>
- Backend and Web dependency policy is exact, expiring, and lock-bound.
- Native mobile dependencies are outside the gate without excluding Web root dependencies.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-05-SUMMARY.md` when done.
</output>
