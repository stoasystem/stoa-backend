---
phase: 474-deterministic-verification-and-gated-delivery
plan: 18
type: execute
wave: 4
depends_on: [474-01, 474-05, 474-28, 474-83]
files_modified:
  - /Users/zhdeng/stoa-infra/pyproject.toml
  - /Users/zhdeng/stoa-infra/uv.lock
  - /Users/zhdeng/stoa-infra/stacks/storage_stack.py
  - /Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py
  - /Users/zhdeng/stoa-infra/tests/test_release_topology.py
autonomous: true
requirements: [V9QUAL-06]
must_haves:
  truths:
    - "CDK owns immutable release/evidence stores and exact staging/production/rollback roles"
    - "Verification jobs have no AWS authority and release roles cannot build, overwrite, delete, or bypass retention"
    - "Failed/staging evidence retains at least 90 days; production/current/known-good is retained long term per D-15"
  artifacts:
    - path: /Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py
      provides: "Release stores, transaction state, OIDC roles, and policy outputs"
    - path: /Users/zhdeng/stoa-infra/tests/test_release_topology.py
      provides: "CDK assertions for immutability, trust, least privilege, and retention"
  key_links:
    - from: /Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py
      to: /Users/zhdeng/stoa-infra/stacks/storage_stack.py
      via: "existing immutable evidence bucket plus dedicated release artifact store"
---

<objective>
Create the minimum immutable storage, transaction, and scoped role substrate for gated delivery.

Purpose: Give later staging/promotion/rollback code durable content-addressed objects and environment-specific authority without exposing credentials to verification.
Output: Locked infra tests, release-delivery stack, immutable storage/retention, four scoped OIDC roles, and CDK outputs.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-05-SUMMARY.md
@/Users/zhdeng/stoa-infra/pyproject.toml
@/Users/zhdeng/stoa-infra/uv.lock
@/Users/zhdeng/stoa-infra/stacks/storage_stack.py
@/Users/zhdeng/stoa-infra/stacks/api_stack.py
@/Users/zhdeng/stoa-infra/stacks/frontend_stack.py
@/Users/zhdeng/stoa-infra/app.py
</context>

## Artifacts this phase produces

- Locked infra dev dependency `pytest==9.0.3` and new test package/file `tests/test_release_topology.py` using AWS CDK assertions.
- New `ReleaseDeliveryStack` in `stacks/release_delivery_stack.py` accepting existing evidence bucket, Lambda functions, Web bucket/distribution, and exact repository/environment settings.
- New constructs `StoaReleaseArtifactBucket` (private, encrypted, SSL, versioned, Object Lock), `StoaReleaseTransactionTable` (release ID primary key, point-in-time recovery), `StoaReleaseArtifactPublisherRole`, `StoaStagingPromotionRole`, `StoaProductionPromotionRole`, and `StoaRollbackRole`.
- Exact GitHub OIDC trust subjects for `stoasystem/stoa-backend` main publisher/staging and protected `production`/`rollback` environments; Web/infra repos cannot assume release mutation roles directly.
- Role outputs `ArtifactPublisherRoleArn`, `StagingPromotionRoleArn`, `ProductionPromotionRoleArn`, `RollbackRoleArn`, plus artifact/evidence bucket and transaction table identifiers.
- Lifecycle/object-lock classes for failed/staging minimum 90 days, production/approval/smoke/rollback long-term, and no automatic expiry for current/latest-known-good tags/prefixes.
- Extended `StorageStack` outputs/grants without replacing existing immutable evidence bucket logical identity.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub OIDC -> AWS roles | Repository/ref/environment claims are untrusted deployment authority inputs. |
| role -> artifact/evidence/transaction stores | Overbroad permissions could overwrite evidence or bypass the gate. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-18-01 | Critical | Spoofing | OIDC trust | mitigate/block | Exact `sub`/`aud` repository/ref/environment conditions and distinct roles. |
| T-474-18-02 | Critical | Tampering | artifact/evidence stores | mitigate/block | Versioning, Object Lock, no delete/overwrite/bypass permissions, content-addressed prefixes. |
| T-474-18-03 | High | Repudiation | transaction/evidence retention | mitigate/block | PITR plus retained immutable receipts and exact request/run identities. |
| T-474-18-04 | High | Elevation of privilege | verify jobs | mitigate/block | No verify role/credentials; CDK tests reject id-token/AWS authority in verification contract. |
| T-474-18-SC | High | Tampering | package install | mitigate/block | `pytest` is established and Approved in research; lock exact infra environment. |

## ASVS 4.0.3 L1 Control Mapping

| ASVS L1 category | Applicability and concrete control | Verification |
|---|---|---|
| V1 Architecture | Applicable — exclusive file ownership, explicit dependencies, and fail-closed boundaries are part of this plan. | Task 18-01 automated command and this plan's focused acceptance criteria. |
| V2 Authentication | Applicable — exact identity/provider inputs remain authenticated and are exercised by focused tests. | Task 18-01 automated command and this plan's focused acceptance criteria. |
| V3 Session Management | Applicable — session/token state is closed and expiry/replay behavior remains tested. | Task 18-01 automated command and this plan's focused acceptance criteria. |
| V4 Access Control | Applicable — authorization/release authority remains deny-first; any High/Critical failure blocks completion. | Task 18-01 automated command and this plan's focused acceptance criteria. |
| V5 Validation | Applicable — untrusted records, command inputs, and result unions are narrowed before use; malformed values fail. | Task 18-01 automated command and this plan's focused acceptance criteria. |
| V6 Cryptography | Applicable — SHA-256/content identities are recomputed and never substitute for secret-key controls. | Task 18-01 automated command and this plan's focused acceptance criteria. |
| V7 Error Handling and Logging | Applicable — stable bounded errors/results are verified and private/provider payloads are not logged. | Task 18-01 automated command and this plan's focused acceptance criteria. |
| V8 Data Protection | Applicable — tests preserve redaction and prevent secret, token, student-content, or provider-payload disclosure. | Task 18-01 automated command and this plan's focused acceptance criteria. |
| V9 Communications | Applicable — real/provider/network boundaries require approved origins, denial, or immutable transport identity. | Task 18-01 automated command and this plan's focused acceptance criteria. |
| V10 Malicious Code | Applicable — no unreviewed package/action is added; the SC threat and locked dependency policy remain blocking. | Task 18-01 automated command and this plan's focused acceptance criteria. |
| V11 Business Logic | Applicable — focused tests preserve exact state, race, retry, idempotency, and fail-closed outcomes relevant to the owned files. | Task 18-01 automated command and this plan's focused acceptance criteria. |
| V12 Files and Resources | Applicable — every modified path is enumerated and artifact/evidence bytes or source files are bounded by exact ownership. | Task 18-01 automated command and this plan's focused acceptance criteria. |
| V13 API and Web Services | Applicable — request/CLI/workflow contracts are closed and verified end to end. | Task 18-01 automated command and this plan's focused acceptance criteria. |
| V14 Configuration | Applicable — exact tool/scope/configuration is verified and broad ignores, mutable refs, or permissive defaults are forbidden. | Task 18-01 automated command and this plan's focused acceptance criteria. |

Any failed applicable ASVS L1 control, or any High/Critical STRIDE threat without its named mitigation and test, blocks plan completion.
</threat_model>

<tasks>

<task type="auto" id="474-18-01">
  <name>Lock CDK assertion testing and write failing release topology contracts</name>
  <files>/Users/zhdeng/stoa-infra/pyproject.toml, /Users/zhdeng/stoa-infra/uv.lock, /Users/zhdeng/stoa-infra/tests/test_release_topology.py</files>
  <read_first>
    - /Users/zhdeng/stoa-infra/pyproject.toml
    - /Users/zhdeng/stoa-infra/uv.lock
    - /Users/zhdeng/stoa-infra/tests/test_release_topology.py
    - /Users/zhdeng/stoa-infra/stacks/storage_stack.py
    - /Users/zhdeng/stoa-infra/stacks/api_stack.py
    - /Users/zhdeng/stoa-infra/stacks/frontend_stack.py
    - /Users/zhdeng/stoa-infra/app.py
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md Package Legitimacy Audit and CDK topology
  </read_first>
  <action>Consume Plan 83's clean-worktree receipt and require the infrastructure HEAD to remain `a4d5eb41cd41e1500319c42e5895cfcb943285f6` before the first edit. Any untracked `.DS_Store`, unrelated change, or SHA drift blocks execution; do not reset, delete, move, stage, or overwrite user-owned state. Add exact pytest dev dependency with `uv add --dev`, freeze the infra lock, and create CDK assertion tests before constructs. Require private encrypted SSL versioned/Object-Lock stores, 90-day/long-term/never-expire classes, PITR transaction state, exact OIDC conditions, four named roles, no artifact overwrite/delete/ObjectLock bypass/build permissions, and outputs. Add negative template mutations for public bucket, mutable artifact key, wildcard trust/resource, cross-repo subject, verify authority, missing retention, and role collapse.</action>
  <verify><automated>bash -lc 'cd /Users/zhdeng/stoa-infra; set +e; uv run pytest -q tests/test_release_topology.py; rc=$?; test "$rc" -eq 1'</automated></verify>
  <acceptance_criteria>
    - Infra lock is frozen and tests fail only because release constructs are absent.
    - Every trust/permission/retention invariant has positive and negative assertion.
  </acceptance_criteria>
  <done>The minimum release-control topology is specified before implementation.</done>
</task>

<task type="auto" id="474-18-02" tdd="true">
  <name>Implement immutable artifact, evidence, and transaction stores</name>
  <files>/Users/zhdeng/stoa-infra/stacks/storage_stack.py, /Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py, /Users/zhdeng/stoa-infra/tests/test_release_topology.py</files>
  <read_first>
    - /Users/zhdeng/stoa-infra/stacks/storage_stack.py
    - /Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py
    - /Users/zhdeng/stoa-infra/tests/test_release_topology.py
    - /Users/zhdeng/stoa-infra/stacks/api_stack.py
    - /Users/zhdeng/stoa-infra/stacks/frontend_stack.py
  </read_first>
  <action>Create the release artifact bucket and transaction table in `ReleaseDeliveryStack`, reuse/pass the existing immutable evidence bucket without changing its logical ID, and add exact outputs. Enforce block-public-access, SSL, managed encryption, versioning, Object Lock, retained removal, and lifecycle rules that cannot expire production/current/known-good. Define content-addressed `artifacts/<sha256>/`, `manifests/<release-id>/`, `failed/`, `staging/`, `production/`, `approvals/`, `smoke/`, and `rollback/` namespaces; mutation code will retain VersionIds.</action>
  <verify><automated>bash -lc 'cd /Users/zhdeng/stoa-infra && uv run pytest -q tests/test_release_topology.py -k "storage or retention or transaction"'</automated></verify>
  <acceptance_criteria>
    - Template proves private versioned Object-Lock storage and PITR transaction state.
    - Failed/staging minimum 90 days and production/current/known-good retention rules are executable.
    - Existing immutable evidence resource is extended, not replaced/destroyed.
  </acceptance_criteria>
  <done>Release bytes, receipts, and transaction state have durable immutable homes.</done>
</task>

<task type="auto" id="474-18-03" tdd="true">
  <name>Create least-privilege publisher, staging, production, and rollback roles</name>
  <files>/Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py, /Users/zhdeng/stoa-infra/tests/test_release_topology.py</files>
  <read_first>
    - /Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py
    - /Users/zhdeng/stoa-infra/tests/test_release_topology.py
    - /Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
    - /Users/zhdeng/stoa-backend/.github/workflows/deploy.yml
    - /Users/zhdeng/stoa-frontend/.github/workflows/deploy.yml
  </read_first>
  <action>Create/import GitHub OIDC provider and four separate roles. Publisher can write new content-addressed objects/manifests/evidence only after the gate; staging can publish versions/update staging aliases and bounded staging Web pointer plus write transaction/smoke; production can update production aliases/pointer and evidence only through `environment:production`; rollback can select only manifest-bound prior versions/pointers and write rollback results through protected `environment:rollback`. None can build, overwrite artifact keys, delete evidence, bypass Object Lock, mutate unqualified Lambda code, or access unrelated resources. Verification receives no role.</action>
  <verify><automated>bash -lc 'cd /Users/zhdeng/stoa-infra && uv run pytest -q tests/test_release_topology.py && uv run python -m compileall -q app.py stacks'</automated></verify>
  <acceptance_criteria>
    - Exact trust subjects/audience and least-privilege resources/actions pass assertions.
    - Wildcard/cross-repo/ref/environment/bypass mutations fail tests.
    - All named outputs exist for workflows.
  </acceptance_criteria>
  <done>The release control plane has separated, environment-bound authority with no verification credential path.</done>
</task>

</tasks>

<verification>
Run all CDK assertions and synth; inspect IAM/bucket/table templates for wildcard trust/resources, delete/bypass/build permissions, public access, replacement of existing evidence resources, and retention gaps.
</verification>

<success_criteria>
- Immutable stores and four scoped roles are CDK-owned and test-proven.
- Evidence/artifact retention satisfies D-15.
- Verification has zero AWS/deploy authority and no role can bypass build-once identity.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-18-SUMMARY.md` when done.
</output>
