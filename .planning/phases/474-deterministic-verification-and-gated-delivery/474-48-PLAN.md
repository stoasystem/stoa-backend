---
phase: 474-deterministic-verification-and-gated-delivery
plan: 48
type: execute
wave: 7
depends_on: [474-08, 474-10, 474-40]
files_modified:
  - "src/stoa/services/support_destination_service.py"
  - "src/stoa/services/support_handoff_service.py"
  - "src/stoa/services/support_sla_service.py"
autonomous: true
requirements: [V9QUAL-04]
must_haves:
  truths:
    - "The support routing and SLA services domain is fully typed without broad suppression."
    - "Runtime regression behavior is unchanged."
  artifacts:
    - path: "src/stoa/services/support_destination_service.py"
      provides: "Mypy-zero closure for support routing and SLA services (D-09, D-10)"
    - path: "src/stoa/services/support_handoff_service.py"
      provides: "Mypy-zero closure for support routing and SLA services (D-09, D-10)"
    - path: "src/stoa/services/support_sla_service.py"
      provides: "Mypy-zero closure for support routing and SLA services (D-09, D-10)"
  key_links:
    - from: "474-40"
      to: "src/stoa/services/support_destination_service.py"
      via: "declared dependency and source-bound verification"
      pattern: 'support_destination_service\.py'
---

<objective>
Mypy-zero closure for support routing and SLA services (D-09, D-10).

Purpose: Close one coherent measured typing domain without weakening runtime or verification semantics.
Output: `src/stoa/services/support_destination_service.py`, `src/stoa/services/support_handoff_service.py`, `src/stoa/services/support_sla_service.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `src/stoa/services/support_destination_service.py` — Mypy-zero closure for support routing and SLA services (D-09, D-10)
- `src/stoa/services/support_handoff_service.py` — Mypy-zero closure for support routing and SLA services (D-09, D-10)
- `src/stoa/services/support_sla_service.py` — Mypy-zero closure for support routing and SLA services (D-09, D-10)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type support routing and SLA services</name>
  <files>src/stoa/services/support_destination_service.py, src/stoa/services/support_handoff_service.py, src/stoa/services/support_sla_service.py</files>
  <behavior>
    - Previously measured diagnostics in this coherent domain become zero.
    - Malformed and provider-originated values remain explicitly narrowed before use.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - src/stoa/services/support_destination_service.py
    - src/stoa/services/support_handoff_service.py
    - src/stoa/services/support_sla_service.py
  </read_first>
  <action>Repair the measured full-scope mypy errors only in the support routing and SLA services domain: src/stoa/services/support_destination_service.py, src/stoa/services/support_handoff_service.py, src/stoa/services/support_sla_service.py. Start from the exact 435-error/113-file command and preserve runtime authorization, privacy, idempotency, retry, fence, pagination, and stable-error semantics. Use closed TypedDict/dataclass records, explicit Optional/union narrowing, maintained stubs, or narrow project-owned Protocols at provider boundaries per D-09/D-10. Do not add or broaden Any, casts that hide unvalidated input, type: ignore, excludes, follow_imports=skip, ignore_missing_imports, skips, xfails, retries, or weaker assertions. This task implements D-09 and D-10.</action>
  <verify>
    <automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/services/support_destination_service.py src/stoa/services/support_handoff_service.py src/stoa/services/support_sla_service.py &amp;&amp; .venv/bin/python -m pytest -q tests/test_support_handoff_sla.py &amp;&amp; .venv/bin/ruff check src/stoa/services/support_destination_service.py src/stoa/services/support_handoff_service.py src/stoa/services/support_sla_service.py'</automated>
  </verify>
  <acceptance>
    - The support routing and SLA services files pass focused mypy and Ruff with no semantic suppression.
    - Named runtime regressions remain green.
  </acceptance>
  <done>The support routing and SLA services files pass focused mypy and Ruff with no semantic suppression. Named runtime regressions remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS V1-V14 Applicability

| ASVS | Control family | Disposition | Plan control |
|---|---|---|---|
| V1 | Architecture | apply | Keep the plan dependency-closed, source-bound, and independently verifiable. |
| V2 | Authentication | preserve | Do not introduce an alternate identity mechanism; smoke uses existing bounded auth. |
| V3 | Session Management | preserve | Do not broaden session lifetime or expose browser/session material in evidence. |
| V4 | Access Control | apply | Use least privilege and protected environment/OIDC boundaries before authority. |
| V5 | Validation | apply | Validate paths, refs, schemas, provider values, counts, and closed outcomes. |
| V6 | Cryptography | apply | Use SHA-256/AWS-managed encryption only; reject digest mismatch. |
| V7 | Error/Logging | apply | Emit stable redacted errors and identifiers without credentials or private payloads. |
| V8 | Data Protection | apply | Retain only required hashes, IDs, statuses, and policy evidence. |
| V9 | Communications | apply | Deny ambient network and constrain provider calls to reviewed endpoints/roles. |
| V10 | Malicious Code | apply | Use audited locks, immutable action SHAs, and tamper tests. |
| V11 | Business Logic | apply | Fail closed on missing prerequisites, stale identity, or partial outcome. |
| V12 | Files/Resources | apply | Use bounded paths, immutable blobs/objects, and resource limits. |
| V13 | API/Web Services | apply | Validate structured API/provider responses and preserve authorization semantics. |
| V14 | Configuration | apply | Use reviewed closed configuration with explicit production NOT RUN state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-474-48-S | Spoofing | source/provider identity | mitigate/block | Require exact execution-derived Git/provider identity and authorized actor/role. |
| T-474-48-T | Tampering | Mypy-zero closure for support routing and SLA services (D-09, D-10) | mitigate/block | Bind inputs, commands, results, and artifacts; reject mismatch, drift, or unknown fields. |
| T-474-48-R | Repudiation | evidence receipt | mitigate/block | Retain canonical source-bound command/result/request identities. |
| T-474-48-I | Information Disclosure | diagnostics/evidence | mitigate/block | Store redacted IDs, hashes, counts, and statuses only. |
| T-474-48-D | Denial of Service | bounded execution | mitigate/block | Apply bounded time/resource scopes and fail closed without partial PASS. |
| T-474-48-E | Elevation of Privilege | verification-to-delivery boundary | mitigate/block | Use least privilege, dependency-closed eligibility, and protected environments. |
| T-474-48-SC | Tampering | package/action supply chain | mitigate/block | Use only RESEARCH-approved packages and immutable reviewed Action SHAs; [SLOP] is forbidden. |
</threat_model>

<verification>
  <automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/services/support_destination_service.py src/stoa/services/support_handoff_service.py src/stoa/services/support_sla_service.py &amp;&amp; .venv/bin/python -m pytest -q tests/test_support_handoff_sla.py &amp;&amp; .venv/bin/ruff check src/stoa/services/support_destination_service.py src/stoa/services/support_handoff_service.py src/stoa/services/support_sla_service.py'</automated>
</verification>

<success_criteria>
- The support routing and SLA services domain is fully typed without broad suppression.
- Runtime regression behavior is unchanged.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-48-SUMMARY.md` when done.
</output>
