---
phase: 474-deterministic-verification-and-gated-delivery
plan: 25
type: execute
wave: 14
depends_on: [474-24]
files_modified:
  - evidence/phase-474/staging-delivery.json
  - evidence/phase-474/controlled-rollback.json
autonomous: true
requirements: [V9QUAL-01, V9QUAL-02, V9QUAL-03, V9QUAL-04, V9QUAL-05, V9QUAL-06]
must_haves:
  truths:
    - "One exact candidate passes the complete common gate, builds once, deploys unchanged to staging, and passes real-service Web/backend smoke"
    - "A controlled non-production smoke failure automatically restores and verifies both prior pointer identities per D-08/D-16"
    - "Failed/staging artifacts and evidence are retained at least 90 days and current/known-good rollback sets indefinitely per D-15"
    - "Production approval, promotion, smoke, and rollback remain exact NOT RUN with zero production mutation"
  artifacts:
    - path: evidence/phase-474/staging-delivery.json
      provides: "Successful gate/build/stage/smoke manifest, workflow, artifact, provider, and browser evidence"
    - path: evidence/phase-474/controlled-rollback.json
      provides: "Retained controlled failure, prior/target identities, compensation actions, and restored-state verification"
  key_links:
    - from: evidence/phase-474/staging-delivery.json
      to: schemas/release/release-manifest-v1.schema.json
      via: "exact release/manifest/gate/artifact/config/transaction digest chain"
    - from: evidence/phase-474/controlled-rollback.json
      to: schemas/release/promotion-transaction-v1.schema.json
      via: "staging transaction state and verified two-pointer compensation"
---

<objective>
Execute the live staging delivery and controlled non-production rollback proofs.

Purpose: Demonstrate that the fully wired release path works with real staging services and fails safely before Phase 474 is declared enforceable.
Output: Durable successful staging evidence and durable controlled-failure/rollback evidence, with production exactly NOT RUN.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-24-SUMMARY.md
@.github/workflows/deploy.yml
@scripts/release_gate.py
@scripts/release_manifest.py
@scripts/release_delivery.py
@scripts/verify_frontend.py
@schemas/release/release-manifest-v1.schema.json
@schemas/release/promotion-transaction-v1.schema.json
</context>

## Artifacts this phase produces

- New `evidence/phase-474/staging-delivery.json` with exact backend/frontend/infra commits, locks, sources, runtimes, target Linux arm64, gate/run/collection/clock/seed IDs, Web route inventory result, artifact/config/manifest/receipt digests, workflow runs, staging role/session/provider versions, smoke facts, retention class, and four production NOT RUN obligations.
- New `evidence/phase-474/controlled-rollback.json` with exercise ID, intentional mismatch identity, prior/target API/weekly alias and Web pointer identities, workflow/request/run IDs, failed smoke evidence, transaction transitions, compensation calls, read-after-write restored identities/digests, retention class, and no-production assertion.
- Retained GitHub run/artifacts and staging provider objects for both the successful and failed candidates, with at least 90-day failed/staging retention and indefinite current/known-good rollback retention.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local invocation -> live workflow | A different manifest/ref or hidden input could be dispatched. |
| workflow artifacts -> staging AWS/Web | Bytes/config/receipts could be substituted between jobs. |
| smoke outcome -> transaction state | False-positive or attacker-controlled smoke could commit a bad candidate. |
| failed staging transaction -> rollback | Mutable/stale prior pointers or partial compensation could leave split state. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-25-01 | Critical | Tampering | dispatch/manifest/artifacts | mitigate/block | Echo and revalidate exact immutable identities at dispatch, download, and effect boundaries. |
| T-474-25-02 | Critical | Spoofing | real-service smoke | mitigate/block | HTTPS staging allowlist, release-identity headers/body, exact browser routes, bounded response assertions. |
| T-474-25-03 | Critical | Tampering | compensation target | mitigate/block | Durable previously verified pointer set only; read-after-write verification of all restored identities. |
| T-474-25-04 | High | Repudiation | live exercise | mitigate/block | Retain workflow/run/attempt/job/request/provider/transaction IDs and digest-linked receipts. |
| T-474-25-05 | High | Elevation of privilege | production boundary | mitigate/block | Staging-scoped credentials only; assert four production NOT RUN obligations and zero production calls. |
| T-474-25-SC | High | Tampering | workflow dependencies | mitigate/block | Execute only the frozen workflows/locks verified by Plan 23. |
</threat_model>

<tasks>

<task type="auto" id="474-25-01">
  <name>Run the complete gate, build once, and prove live staging smoke</name>
  <read_first>
    - .github/workflows/deploy.yml
    - scripts/release_gate.py
    - scripts/release_manifest.py
    - scripts/release_delivery.py
    - scripts/verify_frontend.py
    - evidence/phase-474/staging-substrate.json
  </read_first>
  <files>evidence/phase-474/staging-delivery.json</files>
  <action>Select exact clean backend/frontend/infra commits and invoke the hardened main-candidate workflow. Confirm the authoritative gate creates fresh Python 3.12/locked Web installs, runs the full Python suite twice at the two fixed clocks with ambient AWS/network denial and zero skip/xfail/xpass, runs Ruff/mypy policy/dependency/provenance/Web/OpenAPI/browser gates, and produces the one manifest. Build backend Linux arm64 and Web artifacts once, then deploy those exact digests with reviewed staging runtime config. Run bounded real-service API/weekly-job health and production-critical Playwright route inventory against staging without demo login or route interception per D-04/D-05/D-12/D-13/D-14/D-17/D-18. Persist and validate the complete staging receipt, retention facts, and exact production NOT RUN obligations.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m json.tool evidence/phase-474/staging-delivery.json >/dev/null && .venv/bin/python scripts/release_delivery.py self-test && .venv/bin/python -m pytest -q tests/test_release_delivery.py -k "receipt or staging or smoke"'</automated>
  </verify>
  <acceptance_criteria>
    - The receipt links one exact candidate, gate, build set, runtime config, transaction, staging provider state, and real-service smoke.
    - Provider artifact identities/digests equal the manifest and no post-gate rebuild occurred.
    - All four production obligations are NOT RUN and provider-call evidence shows no production access.
  </acceptance_criteria>
  <done>The exact verified release set is live and healthy in staging with durable machine-verifiable evidence.</done>
</task>

<task type="auto" id="474-25-02">
  <name>Exercise controlled staging smoke failure and verify two-pointer rollback</name>
  <read_first>
    - evidence/phase-474/staging-delivery.json
    - scripts/release_delivery.py
    - schemas/release/promotion-transaction-v1.schema.json
    - .github/workflows/deploy.yml
    - docs/security/phase-474-workflow-policy.json
  </read_first>
  <files>evidence/phase-474/controlled-rollback.json</files>
  <action>Through the same frozen workflow and staging role, stage a second fully gated immutable candidate or restage an independently identified gated set, record the exact prior/target pointer sets, and intentionally make the bounded staging smoke expectation mismatch after both pointer updates. Do not tamper with provider state out of band. Prove the smoke failure prevents commit and automatically enters compensation; restore API/weekly Lambda aliases and Web index/runtime-config VersionIds to the durable prior verified set, issue bounded Web invalidation, then read back and rehash every restored identity. Retain failed release/workflow/request/run/health/rollback/result evidence and 90-day artifacts per D-07/D-08/D-15/D-16. Assert production credentials/calls remain zero and all four production obligations remain NOT RUN.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m json.tool evidence/phase-474/controlled-rollback.json >/dev/null && .venv/bin/python scripts/release_delivery.py self-test && .venv/bin/python -m pytest -q tests/test_release_delivery.py -k "compensat or rollback or restored"'</automated>
  </verify>
  <acceptance_criteria>
    - The intentional failure occurs after both target pointers are written and before transaction commit.
    - The receipt proves both prior backend aliases and both prior Web objects were restored and reverified exactly.
    - Failed candidate evidence/artifacts have the required retention and no production action occurred.
  </acceptance_criteria>
  <done>The live non-production failure exercise proves automatic two-pointer rollback and durable evidence.</done>
</task>

</tasks>

<verification>
Validate both evidence files against their closed schemas, cross-check immutable GitHub/AWS/Web identities, independently read staging pointer state, and confirm production provider-call counts are zero.
</verification>

<success_criteria>
- A complete verified release reaches staging unchanged and passes real-service smoke.
- A controlled staging smoke failure restores and verifies the complete prior release set.
- Both runs are retained and production remains exact NOT RUN.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-25-SUMMARY.md` when done.
</output>
