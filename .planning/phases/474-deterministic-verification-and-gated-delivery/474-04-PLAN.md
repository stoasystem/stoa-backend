---
phase: 474-deterministic-verification-and-gated-delivery
plan: 04
type: execute
wave: 3
depends_on: [474-02]
files_modified:
  - schemas/release/dependency-exceptions-v1.schema.json
  - scripts/dependency_policy.py
  - tests/test_dependency_policy.py
  - pyproject.toml
  - uv.lock
  - requirements.txt
autonomous: true
requirements: [V9QUAL-05]
must_haves:
  truths:
    - "Backend and Web audits are parsed from machine JSON and evaluated with one closed policy per D-11"
    - "Critical/High and production-reachable Medium advisories block without an exact unexpired exception"
    - "The nine measured backend advisories are repaired rather than merely reported"
    - "Mobile dependencies remain outside the v9 Web gate per D-12 and D-18"
  artifacts:
    - path: scripts/dependency_policy.py
      provides: "Lock-bound pip-audit/npm-audit policy evaluator"
    - path: schemas/release/dependency-exceptions-v1.schema.json
      provides: "Exact expiring exception ledger contract"
    - path: tests/test_dependency_policy.py
      provides: "Severity, reachability, lock drift, expiry, wildcard, and abuse tests"
  key_links:
    - from: scripts/dependency_policy.py
      to: requirements.txt
      via: "hashed locked runtime audit"
    - from: scripts/dependency_policy.py
      to: /Users/zhdeng/stoa-frontend/package-lock.json
      via: "npm audit package-lock-only JSON policy"
---

<objective>
Create the closed backend/Web dependency policy and repair the measured backend runtime advisories per D-11 and D-12.

Purpose: Prevent an advisory or broadened exception from being hidden behind package-level allowlists or prose parsing.
Output: Policy/schema/tests plus an updated frozen backend lock/export with zero unaccepted advisory.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-02-SUMMARY.md
@pyproject.toml
@uv.lock
@requirements.txt
@/Users/zhdeng/stoa-frontend/package-lock.json
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
</context>

## Artifacts this phase produces

- New file `scripts/dependency_policy.py` with `DependencyPolicyError`, `Advisory`, `DependencyException`, `load_pip_audit_json`, `load_npm_audit_json`, `classify_reachability`, `validate_exception`, `evaluate_advisories`, `command_check_backend`, `command_check_web`, `command_validate_exceptions`, and `main(argv)`.
- New CLI subcommands `check-backend`, `check-web`, and `validate-exceptions`, each returning 0 only when no unaccepted blocker exists.
- New closed schema `schemas/release/dependency-exceptions-v1.schema.json` requiring exact ecosystem, package, advisory, installed version, lock SHA-256, scope, severity, reachability status/evidence digest, owner, UTC expiry, and upgrade/removal target.
- New stable reason codes `critical_or_high`, `production_reachable_medium`, `backend_runtime_advisory`, `exception_expired`, `exception_wildcard`, `exception_lock_drift`, `exception_version_drift`, `exception_advisory_drift`, and `reachability_unproven`.
- Repaired backend `pyproject.toml`, `uv.lock`, and verified `requirements.txt` resolving the measured `cryptography`, `ecdsa`, `pydantic-settings`, `python-multipart`, and `starlette` advisories.
- New file `tests/test_dependency_policy.py` with positive empty-audit and exact-exception cases plus single-axis abuse/tamper cases.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| ecosystem audit JSON -> policy | Advisory shape, severity, dependency path, and version are untrusted. |
| exception ledger -> release gate | Wildcards, stale evidence, or expiry could silently broaden release authority. |
| lock update -> runtime artifact | A scan of different bytes than the packaged lock/export is meaningless. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-04-01 | Critical | Tampering | exception ledger | mitigate/block | Closed exact schema and lock/package/advisory/version binding. |
| T-474-04-02 | High | Elevation of privilege | reachability claim | mitigate/block | Production reachable Medium blocks; unreachable requires exact evidence digest. |
| T-474-04-03 | High | Repudiation | dependency result | mitigate/block | Persist machine input digest, tool version, blocker list, and matched exception IDs. |
| T-474-04-04 | High | Information disclosure | audit receipt | mitigate/block | Package/advisory/version facts only; no registry credentials or environment output. |
| T-474-04-SC | Critical | Tampering | package installs | mitigate/block | Use only established packages and approved audit tooling; lock/export checks and audits must pass after every upgrade. |
</threat_model>

<tasks>

<task type="auto" id="474-04-01" tdd="true">
  <name>Define the exact dependency exception and blocker policy</name>
  <files>schemas/release/dependency-exceptions-v1.schema.json, tests/test_dependency_policy.py</files>
  <behavior>
    - Backend runtime advisories block unless one exact exception matches every identity and remains unexpired.
    - Web Critical/High and production-reachable Medium advisories block.
    - Wildcard, missing, broadened, expired, wrong-lock, wrong-version, wrong-advisory, and unsupported ecosystem records fail.
  </behavior>
  <read_first>
    - schemas/release/dependency-exceptions-v1.schema.json
    - tests/test_dependency_policy.py
    - pyproject.toml
    - uv.lock
    - requirements.txt
    - /Users/zhdeng/stoa-frontend/package-lock.json
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md Dependency Policy and exact exception shape
  </read_first>
  <action>Write a closed schema and failing tests for both ecosystems. Require every field listed in the artifacts section, lowercase 64-hex lock/evidence digests, one exact installed version and advisory ID, an ISO UTC expiry later than the gate clock, and a nonempty concrete target. For npm consume severity and dependency paths; for backend treat every runtime advisory as blocking when a uniform severity is unavailable. Exclude only `/Users/zhdeng/stoa-frontend/mobile/` per D-12/D-18, never frontend root production dependencies.</action>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_dependency_policy.py</automated></verify>
  <acceptance_criteria>
    - All D-11 exception fields are required and `additionalProperties` is false.
    - Every exception-abuse mutation has an executable rejection test.
    - Backend runtime and Web Critical/High/reachable-Medium blocker rules are distinct and deterministic.
  </acceptance_criteria>
  <done>The exception model cannot silently broaden or outlive its exact advisory/lock/reachability evidence.</done>
</task>

<task type="auto" id="474-04-02" tdd="true">
  <name>Implement lock-native pip and npm advisory evaluation</name>
  <files>scripts/dependency_policy.py, tests/test_dependency_policy.py</files>
  <behavior>
    - Machine JSON is parsed into one closed advisory model; prose is never scraped.
    - Audit input, lock, and exception digests are retained in canonical results.
    - Any malformed/unknown result or unmatched blocker exits 2.
  </behavior>
  <read_first>
    - scripts/dependency_policy.py
    - tests/test_dependency_policy.py
    - schemas/release/dependency-exceptions-v1.schema.json
    - scripts/release_gate.py
  </read_first>
  <action>Implement the named models, parsers, evaluators, reason codes, and three subcommands. Invoke `pip-audit --require-hashes -r requirements.txt --format json` for the verified backend export and `npm audit --package-lock-only --json` in the exact frontend checkout. Bind canonical results to lock SHA-256 and tool version. Reject malformed JSON, unknown advisory identity, changed lock after scan, missing reachability, multiple exceptions for one advisory, and any unmatched blocker. Emit package/advisory/version/status facts only.</action>
  <verify><automated>bash -lc '.venv/bin/python -m pytest -q tests/test_dependency_policy.py && .venv/bin/ruff check scripts/dependency_policy.py tests/test_dependency_policy.py'</automated></verify>
  <acceptance_criteria>
    - Synthetic pip/npm machine payloads produce stable blocker and accepted-exception results.
    - Prose or exit-code-only success cannot satisfy the evaluator.
    - Every malformed or unmatched blocker path returns exit 2.
  </acceptance_criteria>
  <done>Both ecosystem audits are evaluated by one lock-bound fail-closed policy.</done>
</task>

<task type="auto" id="474-04-03">
  <name>Upgrade the measured backend vulnerabilities and prove a clean locked audit</name>
  <files>pyproject.toml, uv.lock, requirements.txt</files>
  <read_first>
    - pyproject.toml
    - uv.lock
    - requirements.txt
    - scripts/dependency_policy.py
    - tests/test_dependency_policy.py
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md measured dependency baseline
  </read_first>
  <action>Use targeted `uv lock --upgrade-package` operations for `cryptography`, `ecdsa`, `pydantic-settings`, `python-multipart`, and `starlette`, selecting the first compatible versions not affected by every observed advisory; change direct constraints only where resolution requires it. Regenerate the hashed production export with the exact locked command and run the machine policy. Do not create an exception while a compatible fix exists, remove packages blindly, or weaken runtime tests. If one advisory truly has no compatible fix, stop for an explicit D-11 owner decision with its exact residual instead of committing a baseline.</action>
  <verify><automated>bash -lc 'uv lock --check; tmp=$(mktemp); uv export --format requirements-txt --no-dev --no-emit-project --locked > "$tmp"; cmp "$tmp" requirements.txt; .venv/bin/python scripts/dependency_policy.py check-backend --lock uv.lock --requirements requirements.txt'</automated></verify>
  <acceptance_criteria>
    - None of the nine measured advisory records remains unaccepted.
    - `uv.lock` and `requirements.txt` are exact and reproducible.
    - Backend focused/full tests remain runnable; no exception or broad package ignore is added silently.
  </acceptance_criteria>
  <done>The backend dependency gate is green because vulnerable versions were repaired, not hidden.</done>
</task>

</tasks>

<verification>
Run policy tests, Ruff, lock/export equality, and live machine-readable backend audit; retain exact tool/lock/input/result digests and zero unaccepted blockers.
</verification>

<success_criteria>
- D-11 and D-12 are implemented with exact expiring exceptions and the Web-first scope.
- The nine measured backend advisories are repaired or execution stops for the owner; no silent exception is accepted.
- Lock, export, audit, exception, and result identities are cryptographically bound.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-04-SUMMARY.md` when done.
</output>
