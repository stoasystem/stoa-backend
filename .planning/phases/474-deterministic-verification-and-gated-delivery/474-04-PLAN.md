---
phase: 474-deterministic-verification-and-gated-delivery
plan: 04
type: execute
wave: 3
depends_on: [474-02]
files_modified:
  - schemas/release/dependency-exceptions-v1.schema.json
  - tests/test_dependency_policy.py
  - scripts/dependency_policy.py
autonomous: true
requirements: [V9QUAL-05]
must_haves:
  truths:
    - "Enforce the exact cross-ecosystem dependency policy is independently complete and cannot be bypassed by another plan."
    - "Failure of the exact automated command blocks every dependent plan"
    - "D-09/D-10 typing integrity, Web-first scope, and production NOT RUN remain immutable where applicable"
  artifacts:
    - path: schemas/release/dependency-exceptions-v1.schema.json
      provides: "Exclusive artifact for enforce the exact cross-ecosystem dependency policy"
    - path: tests/test_dependency_policy.py
      provides: "Exclusive artifact for enforce the exact cross-ecosystem dependency policy"
    - path: scripts/dependency_policy.py
      provides: "Exclusive artifact for enforce the exact cross-ecosystem dependency policy"
  key_links:
    - from: schemas/release/dependency-exceptions-v1.schema.json
      to: "Task 474-04-01 verification"
      via: "exact automated command and acceptance criteria"
---

<objective>
Enforce the exact cross-ecosystem dependency policy.

Purpose: Deliver one cohesive, independently verifiable outcome without overlapping another plan's file ownership.
Output: `schemas/release/dependency-exceptions-v1.schema.json`, `tests/test_dependency_policy.py`, `scripts/dependency_policy.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-02-SUMMARY.md
@schemas/release/dependency-exceptions-v1.schema.json
@tests/test_dependency_policy.py
@pyproject.toml
@uv.lock
@requirements.txt
@/Users/zhdeng/stoa-frontend/package-lock.json
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md Dependency Policy and exact exception shape
@scripts/dependency_policy.py
@scripts/release_gate.py
</context>

## Artifacts this phase produces

- `schemas/release/dependency-exceptions-v1.schema.json` is created or modified only for enforce the exact cross-ecosystem dependency policy.
- `tests/test_dependency_policy.py` is created or modified only for enforce the exact cross-ecosystem dependency policy.
- `scripts/dependency_policy.py` is created or modified only for enforce the exact cross-ecosystem dependency policy.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted input/provider/source -> owned boundary | Malformed or substituted values can falsify this plan's independently verified outcome. |
| owned result -> downstream plan | A weakened, partial, or non-source-bound result could create false release eligibility. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-04-01 | Critical | Tampering | owned contract and result | mitigate/block | Exact file ownership, closed inputs/results, and Task 04-01 automated verification. |
| T-474-04-02 | High | Elevation of privilege | downstream eligibility | mitigate/block | Any missing/non-pass outcome blocks dependencies; no bypass, broad suppression, or inferred success. |
| T-474-04-03 | High | Information disclosure | tests/evidence/errors | mitigate/block | Preserve bounded redaction and reject credentials, tokens, private content, and provider payloads. |
| T-474-04-SC | High | Tampering | package/action supply chain | mitigate/block | No unreviewed install/action; frozen audited locks and the Package Legitimacy Audit remain authoritative. |

## ASVS 4.0.3 L1 Control Mapping

| ASVS L1 category | Applicability and concrete control | Verification |
|---|---|---|
| V1 Architecture | Applicable — exclusive file ownership, explicit dependencies, and fail-closed boundaries are part of this plan. | Task 04-01 automated command and this plan's focused acceptance criteria. |
| V2 Authentication | N/A — this plan neither issues nor validates credentials; upstream authentication behavior is unchanged and regression-tested. | Task 04-01 automated command and this plan's focused acceptance criteria. |
| V3 Session Management | N/A — no session creation, renewal, storage, or termination occurs in this scope. | Task 04-01 automated command and this plan's focused acceptance criteria. |
| V4 Access Control | Applicable — authorization/release authority remains deny-first; any High/Critical failure blocks completion. | Task 04-01 automated command and this plan's focused acceptance criteria. |
| V5 Validation | Applicable — untrusted records, command inputs, and result unions are narrowed before use; malformed values fail. | Task 04-01 automated command and this plan's focused acceptance criteria. |
| V6 Cryptography | N/A — no cryptographic key, primitive, or protected-secret handling is introduced by this plan. | Task 04-01 automated command and this plan's focused acceptance criteria. |
| V7 Error Handling and Logging | Applicable — stable bounded errors/results are verified and private/provider payloads are not logged. | Task 04-01 automated command and this plan's focused acceptance criteria. |
| V8 Data Protection | Applicable — tests preserve redaction and prevent secret, token, student-content, or provider-payload disclosure. | Task 04-01 automated command and this plan's focused acceptance criteria. |
| V9 Communications | N/A — this plan performs no network or inter-process communication; existing transport controls are outside its ownership. | Task 04-01 automated command and this plan's focused acceptance criteria. |
| V10 Malicious Code | Applicable — no unreviewed package/action is added; the SC threat and locked dependency policy remain blocking. | Task 04-01 automated command and this plan's focused acceptance criteria. |
| V11 Business Logic | Applicable — focused tests preserve exact state, race, retry, idempotency, and fail-closed outcomes relevant to the owned files. | Task 04-01 automated command and this plan's focused acceptance criteria. |
| V12 Files and Resources | Applicable — every modified path is enumerated and artifact/evidence bytes or source files are bounded by exact ownership. | Task 04-01 automated command and this plan's focused acceptance criteria. |
| V13 API and Web Services | N/A — this plan does not expose or consume an API/Web-service contract; its typed boundary is exercised locally. | Task 04-01 automated command and this plan's focused acceptance criteria. |
| V14 Configuration | Applicable — exact tool/scope/configuration is verified and broad ignores, mutable refs, or permissive defaults are forbidden. | Task 04-01 automated command and this plan's focused acceptance criteria. |

Any failed applicable ASVS L1 control, or any High/Critical STRIDE threat without its named mitigation and test, blocks plan completion.
</threat_model>

<tasks>

<task type="auto" id="474-04-01" tdd="true">
  <name>Define the exact dependency exception and blocker policy</name>
  <files>schemas/release/dependency-exceptions-v1.schema.json, tests/test_dependency_policy.py</files>
  <behavior>
    - Backend runtime advisories block unless one exact exception matches every identity and remains unexpired.
    - Web Critical/High and production-reachable Medium advisories block.
    - Wildcard, missing, broadened, expired, wrong-lock, wrong-version, wrong-advisory, and unsupported ecosystem records fail.
  </behavior>
  <read_first>
    - schemas/release/dependency-exceptions-v1.schema.json
    - tests/test_dependency_policy.py
    - pyproject.toml
    - uv.lock
    - requirements.txt
    - /Users/zhdeng/stoa-frontend/package-lock.json
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md Dependency Policy and exact exception shape
  </read_first>
  <action>Write a closed schema and failing tests for both ecosystems. Require every field listed in the artifacts section, lowercase 64-hex lock/evidence digests, one exact installed version and advisory ID, an ISO UTC expiry later than the gate clock, and a nonempty concrete target. For npm consume severity and dependency paths; for backend treat every runtime advisory as blocking when a uniform severity is unavailable. Exclude only `/Users/zhdeng/stoa-frontend/mobile/` per D-12/D-18, never frontend root production dependencies.</action>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_dependency_policy.py</automated></verify>
  <acceptance_criteria>
    - All D-11 exception fields are required and `additionalProperties` is false.
    - Every exception-abuse mutation has an executable rejection test.
    - Backend runtime and Web Critical/High/reachable-Medium blocker rules are distinct and deterministic.
  </acceptance_criteria>
  <done>The exception model cannot silently broaden or outlive its exact advisory/lock/reachability evidence.</done>
</task>
<task type="auto" id="474-04-02" tdd="true">
  <name>Implement lock-native pip and npm advisory evaluation</name>
  <files>scripts/dependency_policy.py, tests/test_dependency_policy.py</files>
  <behavior>
    - Machine JSON is parsed into one closed advisory model; prose is never scraped.
    - Audit input, lock, and exception digests are retained in canonical results.
    - Any malformed/unknown result or unmatched blocker exits 2.
  </behavior>
  <read_first>
    - scripts/dependency_policy.py
    - tests/test_dependency_policy.py
    - schemas/release/dependency-exceptions-v1.schema.json
    - scripts/release_gate.py
  </read_first>
  <action>Implement the named models, parsers, evaluators, reason codes, and three subcommands. Invoke `pip-audit --require-hashes -r requirements.txt --format json` for the verified backend export and `npm audit --package-lock-only --json` in the exact frontend checkout. Bind canonical results to lock SHA-256 and tool version. Reject malformed JSON, unknown advisory identity, changed lock after scan, missing reachability, multiple exceptions for one advisory, and any unmatched blocker. Emit package/advisory/version/status facts only.</action>
  <verify><automated>bash -lc '.venv/bin/python -m pytest -q tests/test_dependency_policy.py && .venv/bin/ruff check scripts/dependency_policy.py tests/test_dependency_policy.py'</automated></verify>
  <acceptance_criteria>
    - Synthetic pip/npm machine payloads produce stable blocker and accepted-exception results.
    - Prose or exit-code-only success cannot satisfy the evaluator.
    - Every malformed or unmatched blocker path returns exit 2.
  </acceptance_criteria>
  <done>Both ecosystem audits are evaluated by one lock-bound fail-closed policy.</done>
</task>

</tasks>

<verification>
Run `.venv/bin/python -m pytest -q tests/test_dependency_policy.py`. Treat any nonzero result, missing artifact, node loss, skip/xfail/retry, secret disclosure, or ownership drift as blocking.
</verification>

<success_criteria>
- Enforce the exact cross-ecosystem dependency policy is complete as one independently testable outcome.
- Every owned file and required behavior passes the task's exact automated command.
- No other plan shares same-wave write ownership, and no locked decision or deferred boundary is weakened.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-04-SUMMARY.md` when done.
</output>
