---
phase: 474-deterministic-verification-and-gated-delivery
plan: 91
type: execute
wave: 11
depends_on: [474-90]
files_modified:
  - /Users/zhdeng/stoa-frontend/.github/workflows/frontend-ci.yml
  - /Users/zhdeng/stoa-frontend/.github/workflows/deploy.yml
  - tests/test_frontend_workflow_contract.py
autonomous: true
requirements: [V9QUAL-01, V9QUAL-02]
must_haves:
  truths:
    - "Frontend automation contains exactly one manual exact-ref caller of the backend-owned fixed formal aggregate."
    - "The frontend workflow revision owns frontend_sha and no S3, CloudFront, build, alternate test, or deployment path remains."
    - "Provider execution remains exact NOT RUN until the committed source is pushed and separately dispatched."
  artifacts:
    - path: /Users/zhdeng/stoa-frontend/.github/workflows/frontend-ci.yml
      provides: "Thin manual frontend formal-verification caller"
    - path: tests/test_frontend_workflow_contract.py
      provides: "Closed cross-repository frontend workflow contract"
---

<objective>
Replace frontend CI and direct deployment with one thin exact-ref caller of the fixed backend formal aggregate.

Purpose: close only the frontend CI side of V9QUAL-01 while deleting the existing production deployment bypass.
Output: one verification-only frontend workflow, no frontend deploy workflow, and one backend-owned adversarial contract test. Business UI, infra workflow, source handoff, provider execution, production, mobile, and native remain outside this plan.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-90-SUMMARY.md
@/Users/zhdeng/stoa-frontend/.github/workflows/frontend-ci.yml
@/Users/zhdeng/stoa-frontend/.github/workflows/deploy.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace frontend automation with one exact formal caller</name>
  <files>/Users/zhdeng/stoa-frontend/.github/workflows/frontend-ci.yml, /Users/zhdeng/stoa-frontend/.github/workflows/deploy.yml, tests/test_frontend_workflow_contract.py</files>
  <behavior>
    - Keep exactly one nonsymlink workflow file, manual `workflow_dispatch`, and required no-default lowercase full `backend_sha`, `frontend_sha`, and `infra_sha` inputs; `frontend_sha` must equal the workflow `github.sha`.
    - Use the exact Plan 90 action commits, Python `3.12.13`, uv `0.11.16`, no persistent checkout credentials, no dependency caches, and exact sibling repository paths and post-checkout HEAD checks.
    - Reproduce the exact fail-closed Linux network/PID namespace preparation and private evidence-directory controls.
    - Invoke exactly backend `release_gate.py candidate` followed by backend `release_gate.py formal`; callers cannot select or duplicate child gates.
    - Delete the frontend direct deployment workflow completely. Reject push/PR triggers, Node setup, npm/lint/build, secrets, OIDC, AWS, S3, CloudFront, artifacts, alternate tests, deployment, smoke, rollback, mobile, and native behavior.
    - Parse YAML with duplicate-key rejection and close the complete ordered workflow shape so extra steps or `if` bypasses fail locally.
  </behavior>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_frontend_workflow_contract.py &amp;&amp; .venv/bin/ruff check tests/test_frontend_workflow_contract.py &amp;&amp; git -C /Users/zhdeng/stoa-frontend diff --check &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance>
    - Frontend automation has one fixed candidate/formal graph and no direct deployment path.
    - No caller input or inserted step can select a weaker gate, stale frontend workflow revision, or mutable repository ref.
    - Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` with zero calls.
  </acceptance>
  <done>The frontend repository exposes only one thin exact-ref formal-verification workflow.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| New workflow executes stale frontend source | `frontend_sha == github.sha` plus exact checkout HEAD equality |
| Direct S3/CloudFront mutation survives beside the caller | Workflow directory is closed to one nonsymlink file and deploy workflow is absent |
| CI recreates a weaker Web graph | Exactly candidate then fixed formal; Node/npm/build/test commands are forbidden |
| Extra step or conditional bypass evades tests | Exact ordered step names and complete mapping equality |
| Verification gains cloud authority | Read-only contents permission and closed secret/provider/deploy vocabulary |
</threat_model>

<success_criteria>
- Frontend workflow dispatch requires one exact immutable three-repository source set.
- The frontend-owned source SHA is inseparable from the workflow revision.
- No frontend workflow builds or mutates staging, production, cloud, mobile, or native state.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-91-SUMMARY.md` when done.
</output>
