---
phase: 474-deterministic-verification-and-gated-delivery
plan: 85
type: execute
wave: 13
depends_on: [474-22]
files_modified:
  - evidence/phase-474/infra-source-ref.json
autonomous: true
requirements: [V9QUAL-01, V9QUAL-06, V9QUAL-07]
must_haves:
  truths:
    - "All Phase 474 infrastructure changes exist in one repository-local commit descended from the receipted starting SHA."
    - "No unrelated infrastructure path is staged or committed, and the infrastructure worktree is clean afterward."
  artifacts:
    - path: evidence/phase-474/infra-source-ref.json
      provides: "Immutable infrastructure commit handoff"
  key_links:
    - from: evidence/phase-474/infra-source-ref.json
      to: /Users/zhdeng/stoa-infra/.git
      via: "commit SHA, parent SHA, tree SHA, and owned-path digest"
---

<objective>
Commit the complete Phase 474 infrastructure slice and hand off its immutable ref.

Purpose: Make sibling CDK/workflow changes reachable by the exact source identity consumed by the backend manifest and protected staging workflow.
Output: One infrastructure repository commit plus `infra-source-ref.json`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-83-SUMMARY.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-22-SUMMARY.md
@evidence/phase-474/infra-worktree-preflight.json
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- A repository-local infrastructure commit containing only paths owned by Plans 18, 19, 22, 77, and 78.
- `evidence/phase-474/infra-source-ref.json` binding the resulting commit/tree to the preflight SHA and exact owned-path set.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| dirty sibling tree -> immutable candidate ref | Unrelated or omitted CDK/workflow files could be hidden inside the release identity. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-85-01 | Critical | Tampering | infrastructure commit | mitigate/block | Stage only declared Phase 474 paths after exact status reconciliation. |
| T-474-85-02 | High | Repudiation | source-ref receipt | mitigate/block | Record commit, parent, tree, owned-path digest, and clean-after state. |
| T-474-85-03 | High | Elevation of privilege | unrelated files | mitigate/block | `.DS_Store`, unexpected path, or pre-existing commit drift blocks without reset/deletion. |
| T-474-85-SC | High | Tampering | supply chain | mitigate/block | No new install/action occurs; the locked infra environment is upstream evidence. |

## ASVS 4.0.3 L1 Control Mapping

| ASVS L1 category | Applicability and concrete control | Verification |
|---|---|---|
| V1 Architecture | Explicit sibling-repository finalization boundary. | Task 85-01. |
| V4 Access Control | Only declared paths are stageable. | Cached-path equality. |
| V5 Validation | Commit/parent/tree/path identities are closed and canonical. | Receipt validation. |
| V7 Error Handling | Dirty or drifted state blocks without destructive cleanup. | Exact command. |
| V10 Malicious Code | No new package/action is introduced here. | Upstream lock and staged-path review. |
| V12 Files and Resources | Owned path inventory is exact. | `git diff --cached --name-only`. |
| V14 Configuration | Mutable branch/tag identity is forbidden. | 40-hex commit receipt. |

Any failed applicable ASVS L1 control or unmitigated High/Critical threat blocks completion.
</threat_model>

<tasks>

<task type="auto" id="474-85-01">
  <name>Create and receipt the infrastructure repository-local commit</name>
  <files>evidence/phase-474/infra-source-ref.json</files>
  <read_first>
    - evidence/phase-474/infra-worktree-preflight.json
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-18-SUMMARY.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-19-SUMMARY.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-22-SUMMARY.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-77-SUMMARY.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-78-SUMMARY.md
  </read_first>
  <action>In `/Users/zhdeng/stoa-infra`, require HEAD still equals preflight SHA `a4d5eb41cd41e1500319c42e5895cfcb943285f6`. Derive the closed allowed path set from the `files_modified` fields of Plans 18, 19, 22, 77, and 78; require every porcelain entry to be in that set and every expected completed-plan change to be present. The pre-existing `.DS_Store` and any other undeclared path must already be absent; never delete, move, ignore, stage, or adopt it. Stage only the exact changed allowed paths, verify the cached path set, create repository-local commit `feat(474): qualify release infrastructure`, and require an empty worktree afterward. Do not reset, clean, amend unrelated history, or overwrite user changes. Write canonical `infra-source-ref.json` with repository, starting/parent/commit/tree SHA, sorted committed paths plus digest, lock digest, clean-after=true, and upstream summary identities.</action>
  <verify><automated>bash -lc 'test -z "$(git -C /Users/zhdeng/stoa-infra status --porcelain=v1 --untracked-files=all)" && .venv/bin/python -m json.tool evidence/phase-474/infra-source-ref.json >/dev/null && test "$(git -C /Users/zhdeng/stoa-infra rev-parse HEAD)" != a4d5eb41cd41e1500319c42e5895cfcb943285f6'</automated></verify>
  <acceptance_criteria>
    - The commit parent equals the exact preflight SHA and every committed path belongs to the closed Phase 474 infrastructure inventory.
    - No unrelated path, including `.DS_Store`, is committed; the worktree is empty afterward.
    - The receipt's commit/tree/path/lock identities recompute from Git and disk.
  </acceptance_criteria>
  <done>The complete infrastructure slice is reachable by one immutable repository-local SHA and clean handoff receipt.</done>
</task>

</tasks>

<verification>
Recompute the receipt from Git, require an empty infrastructure worktree, and reject any parent/path/tree/lock mismatch.
</verification>

<success_criteria>
- The infrastructure release ref is immutable, complete, clean, and contains no unrelated changes.
- Plans 23-25 can consume the exact SHA without assuming backend-root commits affect the sibling repository.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-85-SUMMARY.md` when done.
</output>
