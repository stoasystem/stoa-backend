---
phase: 474-deterministic-verification-and-gated-delivery
plan: 85
type: execute
wave: 11
depends_on: [474-02]
files_modified:
  - "scripts/release_gate.py"
  - "scripts/dependency_policy.py"
  - "schemas/release/gate-receipt-v1.schema.json"
  - "tests/test_release_gate.py"
  - "tests/test_dependency_policy.py"
  - "evidence/phase-474/candidate-identity.json"
autonomous: true
requirements: [V9QUAL-01]
must_haves:
  truths:
    - "The canonical gate runs from arbitrary backend, frontend, and infra checkout roots without serializing host paths."
    - "Every command and input is bound to one logical repository and repository-relative path."
  artifacts:
    - path: "scripts/release_gate.py"
      provides: "Portable logical-repository gate contract"
    - path: "scripts/dependency_policy.py"
      provides: "Portable authoritative Web lock selection"
    - path: "schemas/release/gate-receipt-v1.schema.json"
      provides: "Host-path-free signed receipt schema"
---

<objective>
Make the release gate repository-root contract portable.

Purpose: remove workstation-specific paths before any cross-repository or CI aggregation work.
Output: logical repository identities, execution-only root mapping, cwd-bound commands, and portable dependency policy.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Replace host paths with logical repository roots</name>
  <files>scripts/release_gate.py, scripts/dependency_policy.py, schemas/release/gate-receipt-v1.schema.json, tests/test_release_gate.py, tests/test_dependency_policy.py, evidence/phase-474/candidate-identity.json</files>
  <behavior>
    - Runtime callers provide backend/frontend/infra roots; receipts contain only logical repository names and relative paths.
    - GateSpec binds one logical repository, safe repository-relative cwd, and safe repository-relative inputs.
    - Symlink roots, path traversal, wrong-repository locks, absolute inputs, and a copied mobile lock fail closed.
    - The existing candidate is migrated from absolute `root` fields to the closed logical repository schema and a recomputed canonical identity; this does not assert that the historical candidate is current.
  </behavior>
  <read_first>
    - scripts/release_gate.py
    - scripts/dependency_policy.py
    - schemas/release/gate-receipt-v1.schema.json
    - tests/test_release_gate.py
    - tests/test_dependency_policy.py
    - evidence/phase-474/candidate-identity.json
  </read_first>
  <action>Add an execution-only workspace-root mapping and logical repository field to registered gates/input identities. Resolve and validate cwd/input paths beneath the selected root, execute each command in that cwd, and never place an absolute root in candidate/receipt canonical bytes. Make frontend dependency evaluation accept an explicit authoritative Web root and prove its lock is exactly `<root>/package-lock.json`. Preserve the exact Web root package identity and reject mobile/copy substitution. Do not add live HEAD validation or aggregate execution in this plan.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; ! rg -n "/Users/zhdeng" scripts/release_gate.py scripts/dependency_policy.py schemas/release/gate-receipt-v1.schema.json tests/test_release_gate.py tests/test_dependency_policy.py; .venv/bin/python -m pytest -q tests/test_release_gate.py tests/test_dependency_policy.py; .venv/bin/ruff check scripts/release_gate.py scripts/dependency_policy.py tests/test_release_gate.py tests/test_dependency_policy.py'</automated>
  </verify>
  <acceptance>
    - The same code and receipt schema work in a temporary Linux checkout tree.
    - No signed source, input, or command identity depends on a developer home path.
  </acceptance>
  <done>The gate and dependency policy resolve logical repositories only at execution time.</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| A path selects a sibling or mobile lock | Closed logical repository mapping plus resolved descendant checks. |
| Host paths make local and CI receipts incomparable | Absolute roots stay execution-only and never enter canonical receipt bytes. |
</threat_model>

<verification>
  <automated>bash -lc 'set -euo pipefail; ! rg -n "/Users/zhdeng" scripts/release_gate.py scripts/dependency_policy.py schemas/release/gate-receipt-v1.schema.json tests/test_release_gate.py tests/test_dependency_policy.py; .venv/bin/python -m pytest -q tests/test_release_gate.py tests/test_dependency_policy.py; .venv/bin/ruff check scripts/release_gate.py scripts/dependency_policy.py tests/test_release_gate.py tests/test_dependency_policy.py'</automated>
</verification>

<success_criteria>
- Gate execution is portable and repository-bound.
- Existing single-gate behavior and Python matrix evidence remain green.
- Production operations remain exact `NOT RUN`.
</success_criteria>

<output>Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-85-SUMMARY.md` when done.</output>
