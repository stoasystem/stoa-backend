---
phase: 474-deterministic-verification-and-gated-delivery
plan: 94
type: execute
wave: 11
depends_on: [474-93]
files_modified:
  - evidence/phase-474/linux-formal-run-1.json
  - evidence/phase-474/linux-formal-run-2.json
  - evidence/phase-474/linux-formal-admission.json
  - evidence/phase-474/linux-formal-execution-envelope.json
autonomous: true
requirements: [V9QUAL-01, V9QUAL-02]
must_haves:
  truths:
    - "Only the replacement owner-approved publication issued after both formal-snapshot root repairs and its handoff-bound frontend/infra commits are materialized."
    - "The complete fixed formal aggregate runs twice as two sequential processes in one no-host-mount Linux VM, with a fresh candidate check before each aggregate-owned child."
    - "Both raw receipts independently validate as complete PASS and their retained semantic projections are byte-identical."
  artifacts:
    - path: evidence/phase-474/linux-formal-run-1.json
      provides: "First raw Linux formal receipt"
    - path: evidence/phase-474/linux-formal-run-2.json
      provides: "Second raw Linux formal receipt"
    - path: evidence/phase-474/linux-formal-admission.json
      provides: "Machine-checked two-run P/F/I admission"
    - path: evidence/phase-474/linux-formal-execution-envelope.json
      provides: "No-host-mount VM and sequential-process provenance"
---

<objective>
Execute and seal two independent Linux formal PASS runs for the one owner-approved source tuple.

Purpose: close V9QUAL-01 and V9QUAL-02 with real Linux execution rather than caller configuration or local macOS inference.
Output: two raw formal receipts, one admission record, one execution envelope, and exact requirement-status updates. Production/provider, staging, mobile, and native operations remain excluded.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-93-SUMMARY.md
@evidence/phase-474/final-source-handoff.json
@scripts/release_gate.py
@scripts/source_handoff.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run and seal two Linux formal aggregates</name>
  <files>evidence/phase-474/linux-formal-run-1.json, evidence/phase-474/linux-formal-run-2.json, evidence/phase-474/linux-formal-admission.json, evidence/phase-474/linux-formal-execution-envelope.json</files>
  <behavior>
    - Transfer complete local Git bundles into the existing Ubuntu 26.04 ARM64 VM with `mounts: null`; do not bind, share, or execute from a host source path.
    - Clone non-shallow repositories, detach exactly at P/F/I, prove full object closure and clean ordinary checkouts, and prove no host filesystem mount or host source path exists.
    - Acquire exact Python 3.12.13 and use checked `uv` 0.11.16; prove user/network and PID namespace preflights before candidate issuance.
    - Create one private mode-0700 evidence directory and mode-0600 candidate/receipt files; execute formal run 1 to completion before starting formal run 2.
    - Admit both raw receipts with the P-owned source-handoff verifier in the same exact P/F/I workspace, preserving all retained semantic evidence.
    - Record VM boot/kernel/architecture, mount isolation, exact source commits, exact commands, exit codes, sequential windows, receipt/admission digests, and production `NOT RUN` in a canonical execution envelope.
  </behavior>
  <verify>
    <automated>Re-run `source_handoff.py admit` against the two preserved receipts in a fresh exact P/F/I VM checkout, then run the local source-handoff/formal/caller regression suite.</automated>
  </verify>
  <acceptance>
    - Both aggregate and all four child executions report PASS; each embedded Python matrix reports two fresh environments, fixed clocks/seed/collection, all tests passed, and zero skip/xfail/xpass.
    - Both Web runs use the fixed Node/npm runtime, clean locked install, and byte-identical retained artifact identity.
    - The second run starts only after the first aggregate ends and the two outer receipt digests differ.
    - Infrastructure, deploy, smoke, rollback, provider workflow, staging, production, mobile, and native remain exact `NOT RUN`.
  </acceptance>
  <done>V9QUAL-01 and V9QUAL-02 have durable, machine-revalidated two-run Linux PASS evidence for exact P/F/I.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| Host worktree contaminates execution | Lima `mounts: null`, bundle transfer, VM-local non-shallow clones |
| Runner executes later backend metadata HEAD | Detached exact P plus post-checkout SHA equality and P/F/I candidate validation |
| Same process/output is counted twice | Sequential separate formal CLI processes, distinct private files/windows/digests, execution envelope |
| Receipt JSON is copied or weakened | Full raw semantic validation before fixed-pointer projection and retained-field equality |
| Unavailable external work is called PASS | Production/provider/staging operations stay exact `NOT RUN`; only local Linux verification may pass |
</threat_model>

<success_criteria>
- Two separate exact-source Linux formal commands exit 0 and preserve complete PASS receipts.
- Admission proves identical retained semantics for both validated receipts.
- Durable evidence is sufficient to recheck V9QUAL-01 and V9QUAL-02 without access to mutable worktrees.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-94-SUMMARY.md` after evidence revalidation succeeds.
</output>
