---
phase: 474-deterministic-verification-and-gated-delivery
plan: 27
type: execute
wave: 14
depends_on: [474-06, 474-22, 474-75, 474-78]
files_modified:
  - "schemas/release/promotion-transaction-v1.schema.json"
  - "tests/test_release_delivery.py"
  - "scripts/release_delivery.py"
  - "scripts/release_gate.py"
  - "tests/test_release_manifest.py"
autonomous: true
requirements: [V9QUAL-06]
must_haves:
  truths:
    - "One verified set deploys to staging and can be compensated without rebuild."
    - "Production mutation is protected and exact NOT RUN absent later explicit authority."
  artifacts:
    - path: "schemas/release/promotion-transaction-v1.schema.json"
      provides: "Durable two-pointer delivery coordinator (D-05, D-06, D-07, D-08, D-13, D-14)"
    - path: "tests/test_release_delivery.py"
      provides: "Durable two-pointer delivery coordinator (D-05, D-06, D-07, D-08, D-13, D-14)"
    - path: "scripts/release_delivery.py"
      provides: "Durable two-pointer delivery coordinator (D-05, D-06, D-07, D-08, D-13, D-14)"
    - path: "scripts/release_gate.py"
      provides: "Durable two-pointer delivery coordinator (D-05, D-06, D-07, D-08, D-13, D-14)"
    - path: "tests/test_release_manifest.py"
      provides: "Durable two-pointer delivery coordinator (D-05, D-06, D-07, D-08, D-13, D-14)"
  key_links:
    - from: "474-78"
      to: "schemas/release/promotion-transaction-v1.schema.json"
      via: "declared dependency and source-bound verification"
      pattern: 'promotion\-transaction\-v1\.schema\.json'
---

<objective>
Durable two-pointer delivery coordinator (D-05, D-06, D-07, D-08, D-13, D-14).

Purpose: Coordinate backend/Web pointers as a durable compensating transaction rather than an atomicity claim.
Output: `schemas/release/promotion-transaction-v1.schema.json`, `tests/test_release_delivery.py`, `scripts/release_delivery.py`, `scripts/release_gate.py`, `tests/test_release_manifest.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `schemas/release/promotion-transaction-v1.schema.json` — Durable two-pointer delivery coordinator (D-05, D-06, D-07, D-08, D-13, D-14)
- `tests/test_release_delivery.py` — Durable two-pointer delivery coordinator (D-05, D-06, D-07, D-08, D-13, D-14)
- `scripts/release_delivery.py` — Durable two-pointer delivery coordinator (D-05, D-06, D-07, D-08, D-13, D-14)
- `scripts/release_gate.py` — Durable two-pointer delivery coordinator (D-05, D-06, D-07, D-08, D-13, D-14)
- `tests/test_release_manifest.py` — Durable two-pointer delivery coordinator (D-05, D-06, D-07, D-08, D-13, D-14)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify promotion transactions authorization races and compensation</name>
  <files>schemas/release/promotion-transaction-v1.schema.json, tests/test_release_delivery.py</files>
  <behavior>
    - Happy-path staging commits after both pointers and smoke.
    - Either pointer/smoke failure restores both prior verified identities.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - schemas/release/promotion-transaction-v1.schema.json
    - tests/test_release_delivery.py
  </read_first>
  <action>Define PREPARED/APPLYING/SMOKING/COMMITTED/COMPENSATING/ROLLED_BACK/PARTIAL_FAILURE states binding release/manifest, previous/target Lambda aliases, previous/target served-release descriptor key/VersionId/SHA-256, and every descriptor-selected Web/runtime-config key/VersionId/SHA-256. Bind actor/environment/run/request IDs and idempotency; reject any descriptor/config identity inferred from mutable names or body-only digests. Test stale reads, retries, partial writes, descriptor/config substitution, smoke failure, credential scope, and durable evidence per D-05/D-08. This task implements D-05, D-06, D-07, D-08, D-13, D-14.</action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_release_delivery.py</automated>
  </verify>
  <acceptance>
    - Every mutation begins from a durable exact previous/target transaction.
    - Partial failure always enters explicit compensation or retained partial failure.
  </acceptance>
  <done>Every mutation begins from a durable exact previous/target transaction. Partial failure always enters explicit compensation or retained partial failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement staging promotion smoke rollback and gate integration</name>
  <files>scripts/release_delivery.py, scripts/release_gate.py, tests/test_release_delivery.py, tests/test_release_manifest.py</files>
  <behavior>
    - Verified staging mutation can commit.
    - Failed smoke restores both pointers and retains failed identities/actions/results.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - scripts/release_delivery.py
    - scripts/release_gate.py
    - tests/test_release_delivery.py
    - tests/test_release_manifest.py
  </read_first>
  <action>Implement idempotent preconditioned Lambda-alias and served-descriptor VersionId updates, bounded smoke, two-pointer compensation, and read-back verification of the descriptor exact key/VersionId/SHA-256 plus its selected runtime-config and Web key/VersionId/SHA-256. Transaction evidence must retain all previous/target coordinates and reject cross-release substitution. Emergency restore may select only a previously verified descriptor/config/Web set. Register commands through the canonical gate. Hotfix/new code has no bypass; production commands require protected authority and default to exact NOT RUN in Phase 474 per D-05/D-06/D-07/D-08.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_delivery.py tests/test_release_manifest.py &amp;&amp; .venv/bin/ruff check scripts/release_delivery.py scripts/release_gate.py tests/test_release_delivery.py'</automated>
  </verify>
  <acceptance>
    - Staging deploy/smoke and controlled rollback share one immutable manifest.
    - Production cannot rebuild, bypass, or mutate without separate approval.
  </acceptance>
  <done>Staging deploy/smoke and controlled rollback share one immutable manifest. Production cannot rebuild, bypass, or mutate without separate approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS V1-V14 Applicability

| ASVS | Control family | Disposition | Plan control |
|---|---|---|---|
| V1 | Architecture | apply | Keep the plan dependency-closed, source-bound, and independently verifiable. |
| V2 | Authentication | preserve | Do not introduce an alternate identity mechanism; smoke uses existing bounded auth. |
| V3 | Session Management | preserve | Do not broaden session lifetime or expose browser/session material in evidence. |
| V4 | Access Control | apply | Use least privilege and protected environment/OIDC boundaries before authority. |
| V5 | Validation | apply | Validate paths, refs, schemas, provider values, counts, and closed outcomes. |
| V6 | Cryptography | apply | Use SHA-256/AWS-managed encryption only; reject digest mismatch. |
| V7 | Error/Logging | apply | Emit stable redacted errors and identifiers without credentials or private payloads. |
| V8 | Data Protection | apply | Retain only required hashes, IDs, statuses, and policy evidence. |
| V9 | Communications | apply | Deny ambient network and constrain provider calls to reviewed endpoints/roles. |
| V10 | Malicious Code | apply | Use audited locks, immutable action SHAs, and tamper tests. |
| V11 | Business Logic | apply | Fail closed on missing prerequisites, stale identity, or partial outcome. |
| V12 | Files/Resources | apply | Use bounded paths, immutable blobs/objects, and resource limits. |
| V13 | API/Web Services | apply | Validate structured API/provider responses and preserve authorization semantics. |
| V14 | Configuration | apply | Use reviewed closed configuration with explicit production NOT RUN state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-474-27-S | Spoofing | source/provider identity | mitigate/block | Require exact execution-derived Git/provider identity and authorized actor/role. |
| T-474-27-T | Tampering | Durable two-pointer delivery coordinator (D-05, D-06, D-07, D-08, D-13, D-14) | mitigate/block | Bind inputs, commands, results, and artifacts; reject mismatch, drift, or unknown fields. |
| T-474-27-R | Repudiation | evidence receipt | mitigate/block | Retain canonical source-bound command/result/request identities. |
| T-474-27-I | Information Disclosure | diagnostics/evidence | mitigate/block | Store redacted IDs, hashes, counts, and statuses only. |
| T-474-27-D | Denial of Service | bounded execution | mitigate/block | Apply bounded time/resource scopes and fail closed without partial PASS. |
| T-474-27-E | Elevation of Privilege | verification-to-delivery boundary | mitigate/block | Use least privilege, dependency-closed eligibility, and protected environments. |
| T-474-27-SC | Tampering | package/action supply chain | mitigate/block | Use only RESEARCH-approved packages and immutable reviewed Action SHAs; [SLOP] is forbidden. |
</threat_model>

<verification>
  <automated>.venv/bin/python -m pytest -q tests/test_release_delivery.py</automated>
  <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_delivery.py tests/test_release_manifest.py &amp;&amp; .venv/bin/ruff check scripts/release_delivery.py scripts/release_gate.py tests/test_release_delivery.py'</automated>
</verification>

<success_criteria>
- One verified set deploys to staging and can be compensated without rebuild.
- Production mutation is protected and exact NOT RUN absent later explicit authority.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-27-SUMMARY.md` when done.
</output>
