---
phase: 474-deterministic-verification-and-gated-delivery
plan: 27
type: execute
wave: 4
depends_on: [474-04]
files_modified:
  - pyproject.toml
  - uv.lock
  - requirements.txt
autonomous: true
requirements: [V9QUAL-05]
must_haves:
  truths:
    - "Repair the measured backend runtime advisories is independently complete and cannot be bypassed by another plan."
    - "Failure of the exact automated command blocks every dependent plan"
    - "D-09/D-10 typing integrity, Web-first scope, and production NOT RUN remain immutable where applicable"
  artifacts:
    - path: pyproject.toml
      provides: "Exclusive artifact for repair the measured backend runtime advisories"
    - path: uv.lock
      provides: "Exclusive artifact for repair the measured backend runtime advisories"
    - path: requirements.txt
      provides: "Exclusive artifact for repair the measured backend runtime advisories"
  key_links:
    - from: pyproject.toml
      to: "Task 474-27-01 verification"
      via: "exact automated command and acceptance criteria"
---

<objective>
Repair the measured backend runtime advisories.

Purpose: Deliver one cohesive, independently verifiable outcome without overlapping another plan's file ownership.
Output: `pyproject.toml`, `uv.lock`, `requirements.txt`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-04-SUMMARY.md
@pyproject.toml
@uv.lock
@requirements.txt
@scripts/dependency_policy.py
@tests/test_dependency_policy.py
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md measured dependency baseline
</context>

## Artifacts this phase produces

- `pyproject.toml` is created or modified only for repair the measured backend runtime advisories.
- `uv.lock` is created or modified only for repair the measured backend runtime advisories.
- `requirements.txt` is created or modified only for repair the measured backend runtime advisories.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted input/provider/source -> owned boundary | Malformed or substituted values can falsify this plan's independently verified outcome. |
| owned result -> downstream plan | A weakened, partial, or non-source-bound result could create false release eligibility. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-27-01 | Critical | Tampering | owned contract and result | mitigate/block | Exact file ownership, closed inputs/results, and Task 27-01 automated verification. |
| T-474-27-02 | High | Elevation of privilege | downstream eligibility | mitigate/block | Any missing/non-pass outcome blocks dependencies; no bypass, broad suppression, or inferred success. |
| T-474-27-03 | High | Information disclosure | tests/evidence/errors | mitigate/block | Preserve bounded redaction and reject credentials, tokens, private content, and provider payloads. |
| T-474-27-SC | High | Tampering | package/action supply chain | mitigate/block | No unreviewed install/action; frozen audited locks and the Package Legitimacy Audit remain authoritative. |

## ASVS 4.0.3 L1 Control Mapping

| ASVS L1 category | Applicability and concrete control | Verification |
|---|---|---|
| V1 Architecture | Applicable — exclusive file ownership, explicit dependencies, and fail-closed boundaries are part of this plan. | Task 27-01 automated command and this plan's focused acceptance criteria. |
| V2 Authentication | N/A — this plan neither issues nor validates credentials; upstream authentication behavior is unchanged and regression-tested. | Task 27-01 automated command and this plan's focused acceptance criteria. |
| V3 Session Management | N/A — no session creation, renewal, storage, or termination occurs in this scope. | Task 27-01 automated command and this plan's focused acceptance criteria. |
| V4 Access Control | Applicable — authorization/release authority remains deny-first; any High/Critical failure blocks completion. | Task 27-01 automated command and this plan's focused acceptance criteria. |
| V5 Validation | Applicable — untrusted records, command inputs, and result unions are narrowed before use; malformed values fail. | Task 27-01 automated command and this plan's focused acceptance criteria. |
| V6 Cryptography | N/A — no cryptographic key, primitive, or protected-secret handling is introduced by this plan. | Task 27-01 automated command and this plan's focused acceptance criteria. |
| V7 Error Handling and Logging | Applicable — stable bounded errors/results are verified and private/provider payloads are not logged. | Task 27-01 automated command and this plan's focused acceptance criteria. |
| V8 Data Protection | Applicable — tests preserve redaction and prevent secret, token, student-content, or provider-payload disclosure. | Task 27-01 automated command and this plan's focused acceptance criteria. |
| V9 Communications | N/A — this plan performs no network or inter-process communication; existing transport controls are outside its ownership. | Task 27-01 automated command and this plan's focused acceptance criteria. |
| V10 Malicious Code | Applicable — no unreviewed package/action is added; the SC threat and locked dependency policy remain blocking. | Task 27-01 automated command and this plan's focused acceptance criteria. |
| V11 Business Logic | Applicable — focused tests preserve exact state, race, retry, idempotency, and fail-closed outcomes relevant to the owned files. | Task 27-01 automated command and this plan's focused acceptance criteria. |
| V12 Files and Resources | Applicable — every modified path is enumerated and artifact/evidence bytes or source files are bounded by exact ownership. | Task 27-01 automated command and this plan's focused acceptance criteria. |
| V13 API and Web Services | N/A — this plan does not expose or consume an API/Web-service contract; its typed boundary is exercised locally. | Task 27-01 automated command and this plan's focused acceptance criteria. |
| V14 Configuration | Applicable — exact tool/scope/configuration is verified and broad ignores, mutable refs, or permissive defaults are forbidden. | Task 27-01 automated command and this plan's focused acceptance criteria. |

Any failed applicable ASVS L1 control, or any High/Critical STRIDE threat without its named mitigation and test, blocks plan completion.
</threat_model>

<tasks>

<task type="auto" id="474-27-01">
  <name>Repair the measured backend runtime advisories</name>
  <files>pyproject.toml, uv.lock, requirements.txt</files>
  <read_first>
    - pyproject.toml
    - uv.lock
    - requirements.txt
    - scripts/dependency_policy.py
    - tests/test_dependency_policy.py
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md measured dependency baseline
  </read_first>
  <action>Use targeted `uv lock --upgrade-package` operations for `cryptography`, `ecdsa`, `pydantic-settings`, `python-multipart`, and `starlette`, selecting the first compatible versions not affected by every observed advisory; change direct constraints only where resolution requires it. Regenerate the hashed production export with the exact locked command and run the machine policy. Do not create an exception while a compatible fix exists, remove packages blindly, or weaken runtime tests. If one advisory truly has no compatible fix, stop for an explicit D-11 owner decision with its exact residual instead of committing a baseline.</action>
  <verify><automated>bash -lc 'uv lock --check && tmp=$(mktemp) && uv export --format requirements-txt --no-dev --no-emit-project --locked > "$tmp" && cmp "$tmp" requirements.txt && .venv/bin/python scripts/dependency_policy.py check-backend --lock uv.lock --requirements requirements.txt'</automated></verify>
  <acceptance_criteria>
    - None of the nine measured advisory records remains unaccepted.
    - `uv.lock` and `requirements.txt` are exact and reproducible.
    - Backend focused/full tests remain runnable; no exception or broad package ignore is added silently.
  </acceptance_criteria>
  <done>The backend dependency gate is green because vulnerable versions were repaired, not hidden.</done>
</task>

</tasks>

<verification>
Run `bash -lc 'uv lock --check && tmp=$(mktemp) && uv export --format requirements-txt --no-dev --no-emit-project --locked > "$tmp" && cmp "$tmp" requirements.txt && .venv/bin/python scripts/dependency_policy.py check-backend --lock uv.lock --requirements requirements.txt'`. Treat any nonzero result, missing artifact, node loss, skip/xfail/retry, secret disclosure, or ownership drift as blocking.
</verification>

<success_criteria>
- Repair the measured backend runtime advisories is complete as one independently testable outcome.
- Every owned file and required behavior passes the task's exact automated command.
- No other plan shares same-wave write ownership, and no locked decision or deferred boundary is weakened.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-27-SUMMARY.md` when done.
</output>
