---
phase: 474-deterministic-verification-and-gated-delivery
plan: 27
type: execute
wave: 12
depends_on: [474-06, 474-22, 474-24, 474-26]
files_modified:
  - "schemas/release/promotion-transaction-v1.schema.json"
  - "scripts/release_delivery.py"
  - "scripts/release_gate.py"
  - "tests/test_release_delivery.py"
  - "tests/test_release_manifest.py"
autonomous: true
requirements: [V9QUAL-06]
must_haves:
  truths:
    - "One verified set deploys to staging and can be compensated without rebuild."
    - "Production mutation is protected and exact NOT RUN absent later explicit authority."
  artifacts:
    - path: "schemas/release/promotion-transaction-v1.schema.json"
      provides: "Durable two-pointer delivery coordinator"
    - path: "scripts/release_delivery.py"
      provides: "Durable two-pointer delivery coordinator"
    - path: "scripts/release_gate.py"
      provides: "Durable two-pointer delivery coordinator"
    - path: "tests/test_release_delivery.py"
      provides: "Durable two-pointer delivery coordinator"
    - path: "tests/test_release_manifest.py"
      provides: "Durable two-pointer delivery coordinator"
  key_links:
    - from: "474-26"
      to: "schemas/release/promotion-transaction-v1.schema.json"
      via: "declared dependency and immutable receipt"
      pattern: 'promotion-transaction-v1\.schema\.json'
---

<objective>
Durable two-pointer delivery coordinator (D-05, D-06, D-07, D-08, D-13, D-14).

Purpose: Coordinate backend/Web pointers as a durable compensating transaction rather than an atomicity claim.
Output: `schemas/release/promotion-transaction-v1.schema.json`, `scripts/release_delivery.py`, `scripts/release_gate.py`, `tests/test_release_delivery.py`, `tests/test_release_manifest.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `schemas/release/promotion-transaction-v1.schema.json` — Durable two-pointer delivery coordinator.
- `scripts/release_delivery.py` — Durable two-pointer delivery coordinator.
- `scripts/release_gate.py` — Durable two-pointer delivery coordinator.
- `tests/test_release_delivery.py` — Durable two-pointer delivery coordinator.
- `tests/test_release_manifest.py` — Durable two-pointer delivery coordinator.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify promotion transactions authorization races and compensation</name>
  <files>schemas/release/promotion-transaction-v1.schema.json, tests/test_release_delivery.py</files>
  <behavior>
    - Happy-path staging commits after both pointers and smoke.
    - Either pointer/smoke failure restores both prior verified identities.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
  </read_first>
  <action>Define PREPARED/APPLYING/SMOKING/COMMITTED/COMPENSATING/ROLLED_BACK/PARTIAL_FAILURE states binding release/manifest, previous/target Lambda aliases and Web pointer VersionIds/digests, actor/environment/run/request IDs, exact artifact/config bytes, and idempotency. Test stale reads, retries, partial writes, smoke failure, credential scope, and durable evidence per D-05/D-08. This task implements D-05, D-06, D-07, D-08, D-13, D-14.</action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_release_delivery.py</automated>
  </verify>
  <acceptance>
    - Every mutation begins from a durable exact previous/target transaction.
    - Partial failure always enters explicit compensation or retained partial failure.
  </acceptance>
  <done>Every mutation begins from a durable exact previous/target transaction. Partial failure always enters explicit compensation or retained partial failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement staging promotion smoke rollback and gate integration</name>
  <files>scripts/release_delivery.py, scripts/release_gate.py, tests/test_release_delivery.py, tests/test_release_manifest.py</files>
  <behavior>
    - Verified staging mutation can commit.
    - Failed smoke restores both pointers and retains failed identities/actions/results.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
  </read_first>
  <action>Implement idempotent preconditioned alias/pointer updates, bounded smoke, two-pointer compensation, read-back digest verification, emergency restore of only a previously verified set, and privacy-safe evidence. Register commands through the canonical gate. Hotfix/new code has no bypass; production commands require protected authority and default to exact NOT RUN in Phase 474 per D-05/D-06/D-07/D-08. This task implements D-05, D-06, D-07, D-08, D-13, D-14.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_delivery.py tests/test_release_manifest.py &amp;&amp; .venv/bin/ruff check scripts/release_delivery.py scripts/release_gate.py tests/test_release_delivery.py'</automated>
  </verify>
  <acceptance>
    - Staging deploy/smoke and controlled rollback share one immutable manifest.
    - Production cannot rebuild, bypass, or mutate without separate approval.
  </acceptance>
  <done>Staging deploy/smoke and controlled rollback share one immutable manifest. Production cannot rebuild, bypass, or mutate without separate approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS / STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-27-01 | High | Tampering / Repudiation | Durable two-pointer delivery coordinator | mitigate/block | Bind exact source, inputs, command, result, and artifact identities; reject missing, duplicate, unknown, dirty, or mismatched values. |
| T-474-27-02 | High | Spoofing / Elevation | verification-to-delivery boundary | mitigate/block | Apply least privilege, protected environment checks, and dependency-closed eligibility before authority is exposed. |
| T-474-SC | High | Tampering | audited package installs | mitigate/block | Use only packages marked OK in RESEARCH.md Package Legitimacy Audit; [ASSUMED]/[SUS] requires a blocking human checkpoint and [SLOP] is forbidden. |
</threat_model>

<verification>
<automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_delivery.py tests/test_release_manifest.py &amp;&amp; .venv/bin/ruff check scripts/release_delivery.py scripts/release_gate.py tests/test_release_delivery.py'</automated>
</verification>

<success_criteria>
- One verified set deploys to staging and can be compensated without rebuild.
- Production mutation is protected and exact NOT RUN absent later explicit authority.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless a later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-27-SUMMARY.md` when done.
</output>
