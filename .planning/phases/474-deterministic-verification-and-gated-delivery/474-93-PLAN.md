---
phase: 474-deterministic-verification-and-gated-delivery
plan: 93
type: execute
wave: 11
depends_on: [474-92]
files_modified:
  - scripts/source_handoff.py
  - schemas/release/source-handoff-v1.schema.json
  - schemas/release/source-handoff-admission-v1.schema.json
  - tests/test_source_handoff.py
  - evidence/phase-474/final-source-handoff.json
autonomous: true
requirements: [V9QUAL-01, V9QUAL-02]
must_haves:
  truths:
    - "One owner-approved metadata publication P freezes one exact backend implementation B, frontend F, and infrastructure I tuple without serializing P into itself."
    - "P is a direct child of B and changes only the fixed handoff, summary, ROADMAP, and STATE metadata paths with exact ordinary-file modes."
    - "Only a candidate headed by P/F/I can enter the two-run admission boundary; B/F/I and arbitrary generic workflow tuples are rejected."
  artifacts:
    - path: scripts/source_handoff.py
      provides: "Canonical handoff issuance, immutable publication verification, and two-receipt admission"
    - path: schemas/release/source-handoff-v1.schema.json
      provides: "Closed non-self-referential B/F/I handoff contract"
    - path: schemas/release/source-handoff-admission-v1.schema.json
      provides: "Closed two-run Linux admission record contract"
    - path: evidence/phase-474/final-source-handoff.json
      provides: "Owner-approved immutable implementation tuple"
---

<objective>
Freeze one exact cross-repository source tuple behind a non-circular, machine-verifiable owner handoff.

Purpose: close the remaining V9QUAL-01 source-authority gap and define the fail-closed admission boundary consumed by the two Linux runs in Plan 94.
Output: one implementation commit B, one metadata-only publication P, a canonical B/F/I handoff blob, and a verifier that derives all authority from explicit P. No provider, staging, production, mobile, or native operation is included.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-92-SUMMARY.md
@scripts/release_gate.py
@schemas/release/formal-gate-run-v1.schema.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seal and verify the exact source handoff</name>
  <files>scripts/source_handoff.py, schemas/release/source-handoff-v1.schema.json, schemas/release/source-handoff-admission-v1.schema.json, tests/test_source_handoff.py, evidence/phase-474/final-source-handoff.json</files>
  <behavior>
    - Issue canonical duplicate/nonfinite-free JSON containing only ordered B/F/I commit, tree, fixed lock, production `NOT RUN`, exact A/A/M/M publication policy, and a reproducible tuple digest; never include P, a branch, host path, time, user, or environment value.
    - Treat explicit full lowercase publication P as the sole external trust input; require its parent B object locally, one parent, direct ancestry, exact raw status/mode/path delta, regular Git blobs, and immutable handoff bytes at later HEAD without lazy fetch or replacement refs.
    - Verify each B/F/I commit, tree, lock blob, fixed repository marker, ordering, and absence of tracked infra `.DS_Store` from immutable Git objects.
    - Admit only a fully live-validated candidate P/F/I and exactly two distinct fully validated formal receipts from the same clean materialized workspace.
    - Require both receipts to be complete PASS on the same approved Linux platform and Python 3.12.13, with sequential non-overlapping windows and exact production `NOT RUN`.
    - Compare a fixed semantic projection only after full validation; remove only outer/child run clocks and times, receipt self-digests, child process output digests, and Web step output/self-digests while preserving all source, input, lock, collection, artifact, command, count, result, privacy, and production facts.
  </behavior>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_source_handoff.py tests/test_formal_release_gate.py tests/test_release_gate.py &amp;&amp; .venv/bin/ruff check scripts/source_handoff.py tests/test_source_handoff.py &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance>
    - B to P is non-circular and P cannot conceal a source, lock, verifier, workflow, schema, symlink, gitlink, mode, rename, or extra-path change.
    - A shallow or missing-parent object store fails closed and never fetches a mutable ref.
    - A copied, overlapping, non-Linux, non-PASS, wrong-candidate, partially validated, or semantically divergent receipt pair is rejected.
    - Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` with zero provider calls.
  </acceptance>
  <done>One explicit owner-approved P deterministically identifies the only source set eligible for Plan 94.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| Handoff authorizes itself | P is never serialized; the owner-supplied full P SHA is the only trust input |
| Metadata commit hides source drift | Exact A/A/M/M raw diff and ordinary blob-mode contract |
| Shallow clone silently accepts unverifiable B | Required local commit/tree/blob objects, no lazy fetch, no network protocol |
| Generic caller selects an arbitrary tuple | Admission derives B/F/I from P and requires live candidate P/F/I |
| Same or weak run is counted twice | Distinct receipt digests, ordered disjoint windows, two complete semantic validations |
| Recursive normalization hides evidence drift | Fixed JSON-pointer projection after raw receipt validation |
</threat_model>

<success_criteria>
- One explicit metadata publication uniquely freezes the eligible backend/Web/infra source set.
- Every source, publication, candidate, and receipt mismatch fails closed before admission.
- Plan 94 can execute only the fixed formal graph twice and cannot reinterpret unavailable work as PASS.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-93-SUMMARY.md` only in the metadata publication P after implementation commit B is complete.
</output>
