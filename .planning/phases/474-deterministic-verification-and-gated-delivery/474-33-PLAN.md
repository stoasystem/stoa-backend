---
phase: 474-deterministic-verification-and-gated-delivery
plan: 33
type: execute
wave: 18
depends_on: [474-93]
files_modified:
  - "scripts/release_environment.py"
  - "tests/test_release_environment.py"
  - "evidence/phase-474/environment-baseline-resolution.json"
autonomous: true
requirements: [V9QUAL-06]
must_haves:
  truths:
    - "The read-only before-state distinguishes the declared single-staging release targets that are safely absent from unknown or unsafe provider state without claiming absent targets are configured."
    - "Only exact source refs, repository, account 562923011260, region eu-central-2, readable before-state, and zero conflicting resources can yield PASS with READY or READY_TO_BOOTSTRAP."
    - "Unknown conflicts, unreadable required state, destructive drift, or ambiguous provider identity remain BLOCKED before mutation."
  artifacts:
    - path: "scripts/release_environment.py"
      provides: "Fail-closed SAFE_ABSENT and READY_TO_BOOTSTRAP baseline contract"
    - path: "tests/test_release_environment.py"
      provides: "Adversarial baseline and downstream-consumer contract coverage"
    - path: "evidence/phase-474/environment-baseline-resolution.json"
      provides: "Source-bound resolution of the historical read-only before-state (D-03, D-05, D-06, D-15)"
  key_links:
    - from: "evidence/phase-474/frontend-source-ref.json and evidence/phase-474/infra-source-ref.json"
      to: "evidence/phase-474/environment-baseline-resolution.json"
      via: "exact source-ref digests and provider identity"
      pattern: 'environment\-baseline\-resolution\.json'
    - from: "evidence/phase-474/environment-baseline-resolution.json"
      to: "474-34 and 474-79"
      via: "PASS plus READY or READY_TO_BOOTSTRAP eligibility"
      pattern: 'READY|READY_TO_BOOTSTRAP'
---

<objective>
Read-only pre-change release baseline (D-03, D-05, D-06, D-15).

Purpose: Resolve the complete GitHub/AWS/CloudFormation before-state into a safe bootstrap decision without treating declared missing targets as configured or requiring those targets to exist first.
Output: `scripts/release_environment.py`, `tests/test_release_environment.py`, and `evidence/phase-474/environment-baseline-resolution.json`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `scripts/release_environment.py` — Closed baseline verifier and downstream bootstrap-consumer boundary.
- `tests/test_release_environment.py` — SAFE_ABSENT/READY and unsafe-BLOCKED regression coverage.
- `evidence/phase-474/environment-baseline-resolution.json` — New immutable resolution that references, but does not overwrite, the historical blocked inventory.

## V9QUAL mapping

| Requirement | Plan evidence |
|---|---|
| V9QUAL-06 | A source-bound, read-only provider baseline permits only safe bootstrap of the single-staging GitHub/AWS control plane and blocks unknown or unsafe state before any mutation. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve the read-only provider before-state into a safe bootstrap baseline</name>
  <files>scripts/release_environment.py, tests/test_release_environment.py, evidence/phase-474/environment-baseline-resolution.json</files>
  <behavior>
    - Exact readable state with all declared targets already correct yields PASS plus READY.
    - Exact readable state where the declared `staging` GitHub Environment, main ruleset/protection, or StoaReleaseStaging is absent records each absence as SAFE_ABSENT and yields PASS plus READY_TO_BOOTSTRAP.
    - Missing `dist/.stoa-build-manifest.json`, synth output, or CDK diff does not block the pre-change baseline because Plan 474-34 owns those artifacts.
    - Unknown conflicting resources, unreadable required before-state, destructive drift, permission ambiguity, or source/repository/account/region mismatch yields BLOCKED.
    - A SAFE_ABSENT row never satisfies a post-configuration or post-deployment readback assertion.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - docs/security/phase-474-workflow-policy.json
    - evidence/phase-474/474-76-source-ref-resolution.json
    - evidence/phase-474/frontend-source-ref.json
    - evidence/phase-474/infra-source-ref.json
    - evidence/phase-474/environment-inventory.json
    - evidence/phase-474/protected-environments.json
    - scripts/release_environment.py
    - tests/test_release_environment.py
  </read_first>
  <action>Per D-03/D-05/D-06/D-15, update the closed release-environment schemas and focused tests to the superseding staging-only contract: exactly one GitHub Environment `staging`, main protection, one active main ruleset, one OIDC subject `repo:stoasystem/stoa-backend:environment:staging`, one `StoaReleaseStaging` target, and production release-control scope fixed to `DEFERRED_OUT_OF_SCOPE`. Make `verify-inventory` validate a new resolution receipt and require `verify-github`/`verify-staging` to reject a missing, BLOCKED, mismatched, or unvalidated baseline without allowing a SAFE_ABSENT row to satisfy post-change readback. Re-run authorized read-only GitHub, STS, CloudFormation, IAM, Lambda, S3, and CloudFront observations for `stoasystem/stoa-backend`, account `562923011260`, region `eu-central-2`, exact frontend/infra source-ref receipts, and only the declared staging targets. Classify only a missing declared target as `SAFE_ABSENT`; emit status `PASS` with readiness `READY` or `READY_TO_BOOTSTRAP` only when all required reads and exact identities succeed with no unknown collision or unsafe drift. Do not inspect or require `dist/.stoa-build-manifest.json`, synth output, a CDK diff, or any post-change resource as a baseline PASS prerequisite. Fail closed as `BLOCKED` on unknown resources, name collisions, destructive drift, unreadable required state, ambiguous permission, or source/repository/account/region mismatch. Write `environment-baseline-resolution.json` with a digest-bound `resolves` reference to the historical `environment-inventory.json`; preserve both historical blocked receipts byte-for-byte and do not claim any absent target is configured. Every provider mutation field remains `NOT RUN`; the production release control plane is `DEFERRED_OUT_OF_SCOPE` rather than an unexecuted current obligation.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_environment.py &amp;&amp; .venv/bin/python scripts/release_environment.py verify-inventory --receipt evidence/phase-474/environment-baseline-resolution.json --frontend-ref evidence/phase-474/frontend-source-ref.json --infra-ref evidence/phase-474/infra-source-ref.json &amp;&amp; jq -e '"'"'.status == "PASS" and (.readiness == "READY" or .readiness == "READY_TO_BOOTSTRAP") and .identity.repository == "stoasystem/stoa-backend" and .identity.account_id == "562923011260" and .identity.region == "eu-central-2" and .declared_targets.github_environments == ["staging"] and .declared_targets.stack == "StoaReleaseStaging" and .mutation.github_configuration == "NOT RUN" and .mutation.cdk_deploy == "NOT RUN" and .scope.production_release_control_plane == "DEFERRED_OUT_OF_SCOPE"'"'"' evidence/phase-474/environment-baseline-resolution.json >/dev/null'</automated>
  </verify>
  <acceptance>
    - A missing declared `staging` Environment, main protection/ruleset, or StoaReleaseStaging is represented only as SAFE_ABSENT and can authorize the corresponding bootstrap plan without being represented as configured.
    - Missing build manifest, synth, diff, and post-change resource state do not block this read-only baseline.
    - Exact source/provider identities and every required before-state read are bound to the new resolution; unknown, ambiguous, destructive, or conflicting state blocks.
    - The historical blocked evidence remains byte-unchanged, every provider mutation field remains NOT RUN, and production scope is DEFERRED_OUT_OF_SCOPE.
  </acceptance>
  <done>The source-bound read-only baseline is PASS with READY or READY_TO_BOOTSTRAP, safely classifies only declared single-staging absences, preserves historical evidence, leaves every provider mutation NOT RUN, and records production as DEFERRED_OUT_OF_SCOPE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS V1-V14 Applicability

| ASVS | Control family | Disposition | Plan control |
|---|---|---|---|
| V1 | Architecture | apply | Keep the plan dependency-closed, source-bound, and independently verifiable. |
| V2 | Authentication | preserve | Do not introduce an alternate identity mechanism; smoke uses existing bounded auth. |
| V3 | Session Management | preserve | Do not broaden session lifetime or expose browser/session material in evidence. |
| V4 | Access Control | apply | Use least privilege and protected environment/OIDC boundaries before authority. |
| V5 | Validation | apply | Validate paths, refs, schemas, provider values, counts, and closed outcomes. |
| V6 | Cryptography | apply | Use SHA-256/AWS-managed encryption only; reject digest mismatch. |
| V7 | Error/Logging | apply | Emit stable redacted errors and identifiers without credentials or private payloads. |
| V8 | Data Protection | apply | Retain only required hashes, IDs, statuses, and policy evidence. |
| V9 | Communications | apply | Deny ambient network and constrain provider calls to reviewed endpoints/roles. |
| V10 | Malicious Code | apply | Use audited locks, immutable action SHAs, and tamper tests. |
| V11 | Business Logic | apply | Fail closed on missing prerequisites, stale identity, or partial outcome. |
| V12 | Files/Resources | apply | Use bounded paths, immutable blobs/objects, and resource limits. |
| V13 | API/Web Services | apply | Validate structured API/provider responses and preserve authorization semantics. |
| V14 | Configuration | apply | Use the reviewed single-staging contract and explicit production `DEFERRED_OUT_OF_SCOPE` state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-33-S | Spoofing | source/provider identity | high | mitigate/block | Require exact execution-derived Git/provider identity and authorized actor/role. |
| T-474-33-T | Tampering | Read-only bootstrap baseline (D-03, D-05, D-15) | high | mitigate/block | Bind inputs, commands, results, and artifacts; reject mismatch, drift, or unknown fields. |
| T-474-33-R | Repudiation | evidence receipt | medium | mitigate/block | Retain canonical source-bound command/result/request identities. |
| T-474-33-I | Information Disclosure | diagnostics/evidence | medium | mitigate/block | Store redacted IDs, hashes, counts, and statuses only. |
| T-474-33-D | Denial of Service | bounded execution | medium | mitigate/block | Apply bounded time/resource scopes and fail closed without partial PASS. |
| T-474-33-E | Elevation of Privilege | baseline-to-bootstrap boundary | high | mitigate/block | SAFE_ABSENT authorizes only its declared bootstrap branch and never post-change authority. |
| T-474-33-SC | Tampering | package/action supply chain | high | mitigate/block | Use only RESEARCH-approved packages and immutable reviewed Action SHAs; [SLOP] is forbidden. |
</threat_model>

<verification>
  <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_environment.py &amp;&amp; .venv/bin/python scripts/release_environment.py verify-inventory --receipt evidence/phase-474/environment-baseline-resolution.json --frontend-ref evidence/phase-474/frontend-source-ref.json --infra-ref evidence/phase-474/infra-source-ref.json &amp;&amp; jq -e '"'"'.status == "PASS" and (.readiness == "READY" or .readiness == "READY_TO_BOOTSTRAP")'"'"' evidence/phase-474/environment-baseline-resolution.json >/dev/null'</automated>
</verification>

<success_criteria>
- The new baseline binds exact source refs, repository, account, region, and all readable before-state without claiming SAFE_ABSENT targets are configured.
- Only PASS plus READY or READY_TO_BOOTSTRAP can feed Plans 474-34 and 474-79; unknown, unreadable, ambiguous, conflicting, replacement, or destructive state blocks.
- The historical blocked receipts remain unchanged, all provider mutation remains NOT RUN, and production release-control scope is DEFERRED_OUT_OF_SCOPE.
- Every task command is green and its evidence is source-bound.
- Production GitHub/AWS release-control work is `DEFERRED_OUT_OF_SCOPE` to a separate future phase or milestone.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-33-SUMMARY.md` when done.
</output>
