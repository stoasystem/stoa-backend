---
phase: 474-deterministic-verification-and-gated-delivery
plan: 02
type: execute
wave: 2
depends_on: [474-01]
files_modified:
  - "tests/test_release_gate.py"
  - "schemas/release/gate-receipt-v1.schema.json"
  - "scripts/release_gate.py"
autonomous: true
requirements: [V9QUAL-01, V9QUAL-02, V9QUAL-03, V9QUAL-04, V9QUAL-05, V9QUAL-06]
must_haves:
  truths:
    - "Local and CI callers share one authoritative entry point."
    - "NOT RUN is never counted as PASS."
  artifacts:
    - path: "tests/test_release_gate.py"
      provides: "Canonical gate and receipt contract (D-01, D-03, D-16)"
    - path: "schemas/release/gate-receipt-v1.schema.json"
      provides: "Canonical gate and receipt contract (D-01, D-03, D-16)"
    - path: "scripts/release_gate.py"
      provides: "Canonical gate and receipt contract (D-01, D-03, D-16)"
  key_links:
    - from: "474-01"
      to: "tests/test_release_gate.py"
      via: "declared dependency and source-bound verification"
      pattern: 'test_release_gate\.py'
---

<objective>
Canonical gate and receipt contract (D-01, D-03, D-16).

Purpose: Create the sole command and evidence vocabulary used by every local and CI release obligation.
Output: `tests/test_release_gate.py`, `schemas/release/gate-receipt-v1.schema.json`, `scripts/release_gate.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `tests/test_release_gate.py` — Canonical gate and receipt contract (D-01, D-03, D-16)
- `schemas/release/gate-receipt-v1.schema.json` — Canonical gate and receipt contract (D-01, D-03, D-16)
- `scripts/release_gate.py` — Canonical gate and receipt contract (D-01, D-03, D-16)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the closed gate CLI and receipt schema</name>
  <files>tests/test_release_gate.py, schemas/release/gate-receipt-v1.schema.json</files>
  <behavior>
    - Complete closed receipts validate.
    - Single-field tampering and alternate command graphs fail closed.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - tests/test_release_gate.py
    - schemas/release/gate-receipt-v1.schema.json
  </read_first>
  <action>Write failing tests and a closed schema for verify/self-test commands, exact backend/frontend/infra source identities, canonical receipt hashing, closed gate IDs, outcome counts, artifact/config identities, privacy fields, PASS/FAIL/exact NOT RUN, and exit-code separation. Prove CI cannot substitute a second command graph per D-01/D-03/D-16. This task implements D-01, D-03, D-16.</action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_release_gate.py</automated>
  </verify>
  <acceptance>
    - Every missing, duplicate, unknown, tampered, or pass-shaped NOT RUN field fails.
    - The same argv/result vocabulary is usable locally and in CI.
  </acceptance>
  <done>Every missing, duplicate, unknown, tampered, or pass-shaped NOT RUN field fails. The same argv/result vocabulary is usable locally and in CI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dependency-light orchestration and canonical receipt hashing</name>
  <files>scripts/release_gate.py, tests/test_release_gate.py</files>
  <behavior>
    - Identical stable fields produce an identical digest.
    - Unexpected execution failures remain distinct from policy rejection.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - scripts/release_gate.py
    - tests/test_release_gate.py
  </read_first>
  <action>Implement injectable Git/subprocess/clock operations, typed registration, canonical compact JSON digesting, human-readable sorted JSON output, exact path/SHA validation, privacy-safe failure capture, and exit 0 only for complete PASS. Do not implement build, provider mutation, or workflow policy here; later plans register those capabilities through this one authority per D-01. This task implements D-01, D-03, D-16.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_gate.py &amp;&amp; .venv/bin/ruff check scripts/release_gate.py tests/test_release_gate.py'</automated>
  </verify>
  <acceptance>
    - The CLI emits deterministic digests and stable policy failures.
    - No secret/environment value is serialized.
  </acceptance>
  <done>The CLI emits deterministic digests and stable policy failures. No secret/environment value is serialized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS V1-V14 Applicability

| ASVS | Control family | Disposition | Plan control |
|---|---|---|---|
| V1 | Architecture | apply | Keep the plan dependency-closed, source-bound, and independently verifiable. |
| V2 | Authentication | preserve | Do not introduce an alternate identity mechanism; smoke uses existing bounded auth. |
| V3 | Session Management | preserve | Do not broaden session lifetime or expose browser/session material in evidence. |
| V4 | Access Control | apply | Use least privilege and protected environment/OIDC boundaries before authority. |
| V5 | Validation | apply | Validate paths, refs, schemas, provider values, counts, and closed outcomes. |
| V6 | Cryptography | apply | Use SHA-256/AWS-managed encryption only; reject digest mismatch. |
| V7 | Error/Logging | apply | Emit stable redacted errors and identifiers without credentials or private payloads. |
| V8 | Data Protection | apply | Retain only required hashes, IDs, statuses, and policy evidence. |
| V9 | Communications | apply | Deny ambient network and constrain provider calls to reviewed endpoints/roles. |
| V10 | Malicious Code | apply | Use audited locks, immutable action SHAs, and tamper tests. |
| V11 | Business Logic | apply | Fail closed on missing prerequisites, stale identity, or partial outcome. |
| V12 | Files/Resources | apply | Use bounded paths, immutable blobs/objects, and resource limits. |
| V13 | API/Web Services | apply | Validate structured API/provider responses and preserve authorization semantics. |
| V14 | Configuration | apply | Use reviewed closed configuration with explicit production NOT RUN state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-474-02-S | Spoofing | source/provider identity | mitigate/block | Require exact execution-derived Git/provider identity and authorized actor/role. |
| T-474-02-T | Tampering | Canonical gate and receipt contract (D-01, D-03, D-16) | mitigate/block | Bind inputs, commands, results, and artifacts; reject mismatch, drift, or unknown fields. |
| T-474-02-R | Repudiation | evidence receipt | mitigate/block | Retain canonical source-bound command/result/request identities. |
| T-474-02-I | Information Disclosure | diagnostics/evidence | mitigate/block | Store redacted IDs, hashes, counts, and statuses only. |
| T-474-02-D | Denial of Service | bounded execution | mitigate/block | Apply bounded time/resource scopes and fail closed without partial PASS. |
| T-474-02-E | Elevation of Privilege | verification-to-delivery boundary | mitigate/block | Use least privilege, dependency-closed eligibility, and protected environments. |
| T-474-02-SC | Tampering | package/action supply chain | mitigate/block | Use only RESEARCH-approved packages and immutable reviewed Action SHAs; [SLOP] is forbidden. |
</threat_model>

<verification>
  <automated>.venv/bin/python -m pytest -q tests/test_release_gate.py</automated>
  <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_gate.py &amp;&amp; .venv/bin/ruff check scripts/release_gate.py tests/test_release_gate.py'</automated>
</verification>

<success_criteria>
- Local and CI callers share one authoritative entry point.
- NOT RUN is never counted as PASS.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-02-SUMMARY.md` when done.
</output>
