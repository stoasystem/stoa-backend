---
phase: 474-deterministic-verification-and-gated-delivery
plan: 02
type: execute
wave: 2
depends_on: [474-01]
files_modified:
  - pyproject.toml
  - uv.lock
  - requirements.txt
  - scripts/release_gate.py
  - scripts/phase474_pytest_guard.py
  - tests/conftest.py
  - tests/test_deterministic_gate.py
autonomous: true
requirements: [V9QUAL-01, V9QUAL-02]
must_haves:
  truths:
    - "Each formal backend run uses a distinct fresh Python 3.12 frozen environment per D-02"
    - "The complete suite runs at 2026-07-01T12:00:00Z and 2035-01-15T12:00:00Z with seed 4740718 per D-04"
    - "AWS discovery and non-allowlisted sockets fail inside the test boundary, including child processes"
    - "Failed, error, skipped, xfail, or xpass counts force a nonzero gate"
  artifacts:
    - path: scripts/phase474_pytest_guard.py
      provides: "Strict deterministic node/outcome/clock/seed receipt plugin"
    - path: tests/test_deterministic_gate.py
      provides: "Fresh-environment, clock, AWS, network, and result-policy adversarial tests"
    - path: scripts/release_gate.py
      provides: "Two-environment Python 3.12 backend verifier"
  key_links:
    - from: scripts/release_gate.py
      to: uv.lock
      via: "uv lock check and fresh frozen sync"
    - from: scripts/release_gate.py
      to: scripts/phase474_pytest_guard.py
      via: "identical pytest argv with only clock identity changed"
---

<objective>
Implement the hermetic two-clock backend verification boundary required by D-02, D-03, and D-04.

Purpose: Replace ambient Python/time/cloud/network behavior with two independently fresh, source-bound Python 3.12 observations.
Output: Approved locked test tooling, a strict pytest plugin, central isolation fixtures, and backend-run orchestration in the canonical gate.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-01-SUMMARY.md
@pyproject.toml
@uv.lock
@requirements.txt
@scripts/release_gate.py
@scripts/phase473_pytest_guard.py
@tests/conftest.py
@tests/security/conftest.py
</context>

## Artifacts this phase produces

- Approved pinned dev dependencies `pip-audit==2.10.1`, `time-machine==3.2.0`, `pytest-socket==0.8.0`, `boto3-stubs==1.43.16`, and `types-python-jose==3.5.0.20260408`, with frozen lock/export identity.
- New file `scripts/phase474_pytest_guard.py` with `SCHEMA_VERSION = "stoa.release.pytest-nodes.v1"`, `pytest_addoption`, `pytest_configure`, `pytest_runtest_logreport`, `pytest_collection_finish`, and `pytest_sessionfinish`.
- New pytest flags `--stoa-gate-receipt`, `--stoa-fixed-clock`, and `--stoa-seed`.
- New `tests/conftest.py` autouse fixtures `stoa_fixed_clock`, `deny_ambient_aws`, and `deny_non_allowlisted_sockets`.
- New `scripts.release_gate.BackendRunSpec`, `build_backend_run_specs`, `create_fresh_backend_environment`, `run_backend_suite`, and `verify_backend_matrix`.
- Fixed formal values `4740718`, `2026-07-01T12:00:00Z`, and `2035-01-15T12:00:00Z`.
- New file `tests/test_deterministic_gate.py` with direct and subprocess clock/network/AWS escapes plus two-run identity tests.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host -> fresh test environment | Warm packages, AWS files, proxies, and ambient clocks are untrusted. |
| dependency acquisition -> denied execution | Network is allowed only before the test boundary; child processes must remain denied after entry. |
| pytest events -> gate receipt | Collection and phase outcomes must be accounted without skip/xfail/xpass ambiguity. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-02-01 | High | Spoofing | Python runtime | mitigate/block | Require recorded Python 3.12 patch identity from each newly created environment. |
| T-474-02-02 | Critical | Elevation of privilege | AWS/network boundary | mitigate/block | Clear AWS/proxy variables, disable metadata, use nonexistent credential paths, and execute tests in network-none OS isolation. |
| T-474-02-03 | High | Tampering | clock/seed/collection | mitigate/block | Fixed explicit values, sorted node IDs, collection SHA-256, and cross-run equality assertions. |
| T-474-02-04 | High | Repudiation | pytest outcomes | mitigate/block | Record setup/call/teardown and force failure for every non-pass category. |
| T-474-02-05 | High | Information disclosure | environment receipt | mitigate/block | Record names/digests only; never serialize credential/proxy/token values. |
| T-474-02-SC | High | Tampering | package installs | mitigate/block | Install only packages marked Approved in the research Package Legitimacy Audit and lock exact versions before use. |
</threat_model>

<tasks>

<task type="auto" id="474-02-01">
  <name>Lock the approved hermetic verification toolchain</name>
  <files>pyproject.toml, uv.lock, requirements.txt</files>
  <read_first>
    - pyproject.toml
    - uv.lock
    - requirements.txt
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md Package Legitimacy Audit and verification stack
  </read_first>
  <action>Add the five exact approved packages to the `dev` optional group using `uv add --optional dev`; preserve Python `>=3.12`, existing runtime dependencies, Ruff/pytest settings, and lock integrity. Run `uv lock --check`, export production dependencies with `uv export --format requirements-txt --no-dev --no-emit-project --locked`, and make committed `requirements.txt` byte-identical. Do not add an unreviewed package, global mypy suppression, or mobile dependency.</action>
  <verify><automated>bash -lc 'uv lock --check && tmp=$(mktemp); uv export --format requirements-txt --no-dev --no-emit-project --locked > "$tmp"; cmp "$tmp" requirements.txt'</automated></verify>
  <acceptance_criteria>
    - All five exact approved versions are present in `uv.lock` under the dev group.
    - Frozen production export equals `requirements.txt` byte for byte.
    - No native/mobile package or mypy weakening is introduced.
  </acceptance_criteria>
  <done>The hermetic test tools are reproducible from the committed lock.</done>
</task>

<task type="auto" id="474-02-02" tdd="true">
  <name>Enforce strict node outcomes, fixed clocks, and ambient denial</name>
  <files>scripts/phase474_pytest_guard.py, tests/conftest.py, tests/test_deterministic_gate.py</files>
  <behavior>
    - Sorted collection identity and every setup/call/teardown result are emitted.
    - Standard/future clocks are exact and `tick=False`; seed is explicit.
    - Direct and spawned network/AWS discovery fail after the execution boundary.
    - Skip, xfail, xpass, error, and failure each force nonzero exit.
  </behavior>
  <read_first>
    - scripts/phase474_pytest_guard.py
    - scripts/phase473_pytest_guard.py
    - tests/conftest.py
    - tests/security/conftest.py
    - tests/test_deterministic_gate.py
    - pyproject.toml
  </read_first>
  <action>Port the Phase 473 strict accounting pattern into the named Phase 474 plugin and add collection SHA-256, Python/runtime, seed, and clock fields. Integrate `time_machine.travel(..., tick=False)`, `pytest-socket`, cleared AWS credential/session/container/proxy variables, `AWS_EC2_METADATA_DISABLED=true`, and nonexistent shared config paths through central fixtures rather than individual tests. Add subprocess self-tests that try a real socket client and AWS credential discovery. The plugin must reject an empty collection and force `ExitCode.TESTS_FAILED` for failed/error/skipped/xfail/xpass counts.</action>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_deterministic_gate.py</automated></verify>
  <acceptance_criteria>
    - Each forbidden outcome has a subprocess test proving nonzero exit and an exact count of 1.
    - Direct and child-process network/AWS escape attempts fail without leaking ambient values.
    - Clock, seed, Python version, sorted nodes, and collection digest are present in the receipt.
  </acceptance_criteria>
  <done>The pytest boundary accounts for every node and denies ambient time/cloud/network behavior.</done>
</task>

<task type="auto" id="474-02-03" tdd="true">
  <name>Run two separate frozen Python 3.12 environments through the canonical gate</name>
  <files>scripts/release_gate.py, tests/test_deterministic_gate.py</files>
  <behavior>
    - Two unique temp environment paths are created and destroyed; a repository `.venv` is rejected as evidence.
    - Both run specs have identical command/seed/collection expectations and differ only in clock ID.
    - Dependency acquisition precedes network-none execution; unsupported isolation yields exact NOT RUN, never PASS.
  </behavior>
  <read_first>
    - scripts/release_gate.py
    - tests/test_deterministic_gate.py
    - scripts/phase474_pytest_guard.py
    - uv.lock
    - pyproject.toml
  </read_first>
  <action>Implement `BackendRunSpec` and the named orchestration functions. For each clock create a different validated temp `UV_PROJECT_ENVIRONMENT`, run `uv sync --frozen --python 3.12 --extra dev`, then enter a Linux `network=none`/network-namespace boundary and invoke the same complete pytest argv with only `--stoa-fixed-clock` changed. Clear ambient AWS/proxy variables and mount no credential directories/sockets. Reject warm env paths, lock/source change during run, collection mismatch, non-3.12 runtime, or any non-pass category. If the host cannot provide OS isolation, emit exact `NOT RUN` with reason `isolation_runtime_unavailable`; CI must provide the capable boundary.</action>
  <verify><automated>bash -lc '.venv/bin/python -m pytest -q tests/test_deterministic_gate.py tests/test_release_gate.py && .venv/bin/ruff check scripts/phase474_pytest_guard.py scripts/release_gate.py tests/conftest.py tests/test_deterministic_gate.py'</automated></verify>
  <acceptance_criteria>
    - Tests prove two distinct environment paths and exact clocks/seed/runtime/lock/collection fields.
    - Tests reject `.venv`, reused temp paths, collection drift, runtime drift, lock drift, network access, and AWS discovery.
    - Unsupported local isolation is exact NOT RUN while a simulated capable CI runner executes both complete commands.
  </acceptance_criteria>
  <done>The canonical gate can issue backend evidence only from two fresh frozen Python 3.12 isolated runs.</done>
</task>

</tasks>

<verification>
Run deterministic-gate tests, package lock/export equality, focused Ruff, and a simulated capable-runner matrix; inspect both receipts for distinct environments, fixed/future clocks, identical collection hash, and zero forbidden outcomes.
</verification>

<success_criteria>
- D-02 and D-04 are executable and adversarially tested.
- Warm environments, ambient AWS, non-allowlisted sockets, clock drift, collection drift, and forbidden pytest outcomes cannot issue release evidence.
- Local unavailable isolation remains exact NOT RUN while the CI contract requires both complete runs.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-02-SUMMARY.md` when done.
</output>
