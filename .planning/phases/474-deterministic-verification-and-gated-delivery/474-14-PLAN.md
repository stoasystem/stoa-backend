---
phase: 474-deterministic-verification-and-gated-delivery
plan: 14
type: execute
wave: 9
depends_on: [474-12, 474-13]
files_modified:
  - scripts/release_gate.py
  - schemas/release/quality-repair-report-v1.schema.json
  - tests/test_quality_gate_policy.py
autonomous: true
requirements: [V9QUAL-04]
must_haves:
  truths:
    - "Ruff is zero and exact full-repository mypy is zero after the concrete 435-error repair"
    - "The gate rejects global ignore_missing_imports, exclusions, follow-import skipping, blanket ignores, and broadened Any"
    - "No temporary baseline is created or requested when zero is achieved"
  artifacts:
    - path: tests/test_quality_gate_policy.py
      provides: "Exact scope and forbidden-weakening semantic guard"
    - path: schemas/release/quality-repair-report-v1.schema.json
      provides: "Measured before/after quality repair receipt"
    - path: scripts/release_gate.py
      provides: "Canonical Ruff and full-mypy gates"
  key_links:
    - from: scripts/release_gate.py
      to: tests/test_quality_gate_policy.py
      via: "exact argv and semantic weakening checks"
---

<objective>
Prove the full 435-error repair reaches Ruff zero and full-repository mypy zero without policy weakening.

Purpose: Turn the repository-wide repair into a permanent release-blocking quality gate per D-09 and D-10.
Output: Closed repair report, semantic guard, and canonical Ruff/mypy gate integration.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-12-SUMMARY.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-13-SUMMARY.md
@scripts/release_gate.py
@pyproject.toml
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md Mypy Closure Sequence
</context>

## Artifacts this phase produces

- New closed schema `schemas/release/quality-repair-report-v1.schema.json` requiring exact mypy/Ruff argv, source/lock identity, before counts `435 errors/113 files/221 checked files`, after counts, category deltas, UTC bounds, tool versions, forbidden-pattern scan, and receipt digest.
- New file `tests/test_quality_gate_policy.py` with AST/config/argv checks for full scope and forbidden `ignore_missing_imports`, `exclude`, `follow_imports=skip`, blanket `type: ignore`, broadened `Any`, and weaker alternate commands.
- New gate IDs `PYTHON-RUFF-ZERO`, `PYTHON-MYPY-ZERO`, and `PYTHON-TYPING-POLICY` registered by `scripts/release_gate.py`.
- Exact mypy argv `MYPYPATH=src:tests python -m mypy --no-incremental --explicit-package-bases src/stoa scripts tests` and Ruff argv `python -m ruff check src tests scripts --no-cache`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| config/argv -> claimed quality scope | A narrower command or suppression can forge zero. |
| source diff -> repair report | Broad Any/ignores can hide production-reachable type failures. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-14-01 | Critical | Tampering | mypy scope/config | mitigate/block | Exact argv/source roots and config semantic tests. |
| T-474-14-02 | High | Repudiation | repair result | mitigate/block | Before/after counts, tool/source/lock identity, and canonical report digest. |
| T-474-14-03 | High | Elevation of privilege | broad Any/provider modules | mitigate/block | AST/config guard and no provider/module exclusions. |
| T-474-14-SC | High | Tampering | package installs | mitigate/block | No package install. |
</threat_model>

<tasks>

<task type="auto" id="474-14-01" tdd="true">
  <name>Enforce exact full quality scope and forbid semantic weakening</name>
  <files>schemas/release/quality-repair-report-v1.schema.json, tests/test_quality_gate_policy.py</files>
  <behavior>
    - Only the exact full mypy/Ruff commands can satisfy quality receipts.
    - Global/module exclusions, missing-import suppression, follow-import skipping, blanket ignores, and broadened Any fail.
    - The report accounts for the measured 435/113/221 baseline and exact zero result.
  </behavior>
  <read_first>
    - schemas/release/quality-repair-report-v1.schema.json
    - tests/test_quality_gate_policy.py
    - pyproject.toml
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - all changed files listed in 474-06 through 474-13 summaries
  </read_first>
  <action>Create the closed repair schema and semantic/AST/config tests. Validate exact command tokens and roots; detect new/broadened `Any`, `type: ignore`, `ignore_missing_imports`, `exclude`, `follow_imports=skip`, per-module ignore blocks, and omitted provider/script/test roots. Allow only pre-existing narrow suppressions documented by exact file/code/line and reject any count increase. Require baseline and after counts with source/lock/tool identities.</action>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_quality_gate_policy.py</automated></verify>
  <acceptance_criteria>
    - Every forbidden weakening has a mutation test.
    - Exact source roots and before counts are schema-required.
    - A narrower command cannot validate a zero report.
  </acceptance_criteria>
  <done>The quality policy distinguishes real zero from suppressed/narrowed zero.</done>
</task>

<task type="auto" id="474-14-02" tdd="true">
  <name>Register Ruff, mypy, and typing-policy gates and capture true zero</name>
  <files>scripts/release_gate.py, tests/test_quality_gate_policy.py</files>
  <behavior>
    - Canonical gate executes exact Ruff, mypy, and semantic-policy commands in the fresh environment.
    - Zero errors and zero forbidden weakening are required for PASS.
    - Report is canonical, source/lock bound, and records the 435-error repair delta.
  </behavior>
  <read_first>
    - scripts/release_gate.py
    - tests/test_quality_gate_policy.py
    - schemas/release/quality-repair-report-v1.schema.json
    - pyproject.toml
  </read_first>
  <action>Register the three exact gate IDs and execute them inside the Plan 02 fresh Python 3.12 environment. Parse machine/stable mypy and Ruff results without treating console prose alone as evidence. Emit the closed repair report and reject any nonzero/error/unknown output or semantic weakening. Run the exact full command after all repairs; if errors remain, fix the owning source/test rather than writing a baseline. Only an actual irreducible residual after concrete repair may trigger the D-09 owner decision path; do not precreate it.</action>
  <verify><automated>bash -lc '.venv/bin/python -m pytest -q tests/test_quality_gate_policy.py; .venv/bin/python -m ruff check src tests scripts --no-cache; MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa scripts tests'</automated></verify>
  <acceptance_criteria>
    - Exact Ruff command reports zero.
    - Exact full mypy command reports `Success: no issues found in 221 source files` or the current exact full file count after legitimate additions.
    - Semantic guard reports zero new/broadened forbidden patterns.
    - No baseline, exclude, broad Any, or owner waiver exists when zero passes.
  </acceptance_criteria>
  <done>V9QUAL-04 is an executable, source-bound, non-weakenable zero gate.</done>
</task>

</tasks>

<verification>
Run the exact full Ruff/mypy commands twice in fresh Python 3.12 environments, semantic-policy mutation tests, and canonical report validation; compare source/lock/argv/count/digest facts.
</verification>

<success_criteria>
- The measured 435 errors across 113 files are repaired to zero.
- Ruff and full mypy are release-blocking under one exact command.
- D-09/D-10 suppression shortcuts are automatically rejected.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-14-SUMMARY.md` when done.
</output>
