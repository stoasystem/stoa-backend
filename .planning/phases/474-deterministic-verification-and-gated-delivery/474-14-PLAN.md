---
phase: 474-deterministic-verification-and-gated-delivery
plan: 14
type: execute
wave: 7
depends_on: [474-10]
files_modified:
  - "src/stoa/services/account_deletion_service.py"
  - "src/stoa/services/notification_service.py"
  - "src/stoa/services/notify_service.py"
  - "src/stoa/services/websocket_service.py"
autonomous: true
requirements: [V9QUAL-04]
must_haves:
  truths:
    - "The notification and account-deletion services slice is fully typed without broad suppression."
    - "Runtime regression behavior is unchanged."
  artifacts:
    - path: "src/stoa/services/account_deletion_service.py"
      provides: "Notification and account-deletion services"
    - path: "src/stoa/services/notification_service.py"
      provides: "Notification and account-deletion services"
    - path: "src/stoa/services/notify_service.py"
      provides: "Notification and account-deletion services"
    - path: "src/stoa/services/websocket_service.py"
      provides: "Notification and account-deletion services"
  key_links:
    - from: "474-10"
      to: "src/stoa/services/account_deletion_service.py"
      via: "declared dependency and immutable receipt"
      pattern: "account_deletion_service\.py"
---

<objective>
Notification and account-deletion services (D-09, D-10).

Purpose: Close one real mypy root-cause family while retaining existing behavior and direct producer/consumer ordering.
Output: `src/stoa/services/account_deletion_service.py`, `src/stoa/services/notification_service.py`, `src/stoa/services/notify_service.py`, `src/stoa/services/websocket_service.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `src/stoa/services/account_deletion_service.py` — Notification and account-deletion services.
- `src/stoa/services/notification_service.py` — Notification and account-deletion services.
- `src/stoa/services/notify_service.py` — Notification and account-deletion services.
- `src/stoa/services/websocket_service.py` — Notification and account-deletion services.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type WebSocket and notification delivery services</name>
  <files>src/stoa/services/websocket_service.py, src/stoa/services/notification_service.py, src/stoa/services/notify_service.py</files>
  <behavior>
    - Previously measured diagnostics in the owned file set become zero.
    - Malformed/untrusted values remain explicitly narrowed before use.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - src/stoa/services/websocket_service.py
    - src/stoa/services/notification_service.py
    - src/stoa/services/notify_service.py
  </read_first>
  <action>Repair the measured full-scope mypy errors in the WebSocket and notification delivery services family, limited to src/stoa/services/websocket_service.py, src/stoa/services/notification_service.py, src/stoa/services/notify_service.py. Start from the exact 435-error/113-file command and preserve runtime authorization, privacy, idempotency, retry, fence, pagination, and stable-error semantics. Use closed TypedDict/dataclass records, explicit Optional/union narrowing, maintained stubs, or narrow project-owned Protocols at provider boundaries per D-09/D-10. Do not add or broaden Any, casts that hide unvalidated input, type: ignore, excludes, follow_imports=skip, ignore_missing_imports, skips, xfails, retries, or weaker assertions. This task implements D-09, D-10.</action>
  <verify>
    <automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/services/websocket_service.py src/stoa/services/notification_service.py src/stoa/services/notify_service.py &amp;&amp; .venv/bin/python -m pytest -q tests/test_phase473_private_delivery_fencing.py tests/test_phase473_notification_deletion.py &amp;&amp; .venv/bin/ruff check src/stoa/services/websocket_service.py src/stoa/services/notification_service.py src/stoa/services/notify_service.py'</automated>
  </verify>
  <acceptance>
    - The WebSocket and notification delivery services files pass focused mypy and Ruff with no semantic suppression.
    - Named runtime regressions remain green.
  </acceptance>
  <done>The WebSocket and notification delivery services files pass focused mypy and Ruff with no semantic suppression. Named runtime regressions remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Type account-deletion orchestration</name>
  <files>src/stoa/services/account_deletion_service.py</files>
  <behavior>
    - Previously measured diagnostics in the owned file set become zero.
    - Malformed/untrusted values remain explicitly narrowed before use.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - src/stoa/services/account_deletion_service.py
  </read_first>
  <action>Repair the measured full-scope mypy errors in the account-deletion orchestration family, limited to src/stoa/services/account_deletion_service.py. Start from the exact 435-error/113-file command and preserve runtime authorization, privacy, idempotency, retry, fence, pagination, and stable-error semantics. Use closed TypedDict/dataclass records, explicit Optional/union narrowing, maintained stubs, or narrow project-owned Protocols at provider boundaries per D-09/D-10. Do not add or broaden Any, casts that hide unvalidated input, type: ignore, excludes, follow_imports=skip, ignore_missing_imports, skips, xfails, retries, or weaker assertions. This task implements D-09, D-10.</action>
  <verify>
    <automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/services/account_deletion_service.py &amp;&amp; .venv/bin/python -m pytest -q tests/test_phase473_account_deletion.py tests/test_phase473_account_deletion_claim_fencing.py &amp;&amp; .venv/bin/ruff check src/stoa/services/account_deletion_service.py'</automated>
  </verify>
  <acceptance>
    - The account-deletion orchestration files pass focused mypy and Ruff with no semantic suppression.
    - Named runtime regressions remain green.
  </acceptance>
  <done>The account-deletion orchestration files pass focused mypy and Ruff with no semantic suppression. Named runtime regressions remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS / STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-14-01 | High | Tampering / Repudiation | Notification and account-deletion services | mitigate/block | Bind exact source, inputs, command, result, and artifact identities; reject missing, duplicate, unknown, dirty, or mismatched values. |
| T-474-14-02 | High | Spoofing / Elevation | verification-to-delivery boundary | mitigate/block | Apply least privilege, protected environment checks, and dependency-closed eligibility before authority is exposed. |
| T-474-SC | High | Tampering | audited package installs | mitigate/block | Use only packages marked OK in RESEARCH.md Package Legitimacy Audit; [ASSUMED]/[SUS] requires a blocking human checkpoint and [SLOP] is forbidden. |
</threat_model>

<verification>
<automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/services/account_deletion_service.py &amp;&amp; .venv/bin/python -m pytest -q tests/test_phase473_account_deletion.py tests/test_phase473_account_deletion_claim_fencing.py &amp;&amp; .venv/bin/ruff check src/stoa/services/account_deletion_service.py'</automated>
</verification>

<success_criteria>
- The notification and account-deletion services slice is fully typed without broad suppression.
- Runtime regression behavior is unchanged.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless a later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-14-SUMMARY.md` when done.
</output>
