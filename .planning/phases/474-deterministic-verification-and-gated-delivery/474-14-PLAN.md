---
phase: 474-deterministic-verification-and-gated-delivery
plan: 14
type: execute
wave: 10
depends_on: [474-12, 474-59, 474-60, 474-61, 474-62, 474-63, 474-13, 474-64, 474-65, 474-66, 474-67, 474-68, 474-69, 474-70, 474-71]
files_modified:
  - scripts/release_gate.py
  - schemas/release/quality-repair-report-v1.schema.json
  - tests/test_quality_gate_policy.py
autonomous: false
requirements: [V9QUAL-04]
must_haves:
  truths:
    - "Ruff is zero and exact full-repository mypy is zero after the concrete 435-error repair"
    - "The gate rejects global ignore_missing_imports, exclusions, follow-import skipping, blanket ignores, and broadened Any"
    - "No temporary baseline is created or requested when zero is achieved"
  artifacts:
    - path: tests/test_quality_gate_policy.py
      provides: "Exact scope and forbidden-weakening semantic guard"
    - path: schemas/release/quality-repair-report-v1.schema.json
      provides: "Measured before/after quality repair receipt"
    - path: scripts/release_gate.py
      provides: "Canonical Ruff and full-mypy gates"
  key_links:
    - from: scripts/release_gate.py
      to: tests/test_quality_gate_policy.py
      via: "exact argv and semantic weakening checks"
---

<objective>
Prove the full 435-error repair reaches Ruff zero and full-repository mypy zero without policy weakening.

Purpose: Turn the repository-wide repair into a permanent release-blocking quality gate per D-09 and D-10.
Output: Closed repair report, semantic guard, and canonical Ruff/mypy gate integration.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-12-SUMMARY.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-13-SUMMARY.md
@scripts/release_gate.py
@pyproject.toml
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md Mypy Closure Sequence
</context>

## Artifacts this phase produces

- New closed schema `schemas/release/quality-repair-report-v1.schema.json` requiring exact mypy/Ruff argv, source/lock identity, before counts `435 errors/113 files/221 checked files`, after counts, category deltas, UTC bounds, tool versions, forbidden-pattern scan, and receipt digest.
- New file `tests/test_quality_gate_policy.py` with AST/config/argv checks for full scope and forbidden `ignore_missing_imports`, `exclude`, `follow_imports=skip`, blanket `type: ignore`, broadened `Any`, and weaker alternate commands.
- New gate IDs `PYTHON-RUFF-ZERO`, `PYTHON-MYPY-ZERO`, and `PYTHON-TYPING-POLICY` registered by `scripts/release_gate.py`.
- Exact mypy argv `MYPYPATH=src:tests python -m mypy --no-incremental --explicit-package-bases src/stoa scripts tests` and Ruff argv `python -m ruff check src tests scripts --no-cache`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| config/argv -> claimed quality scope | A narrower command or suppression can forge zero. |
| source diff -> repair report | Broad Any/ignores can hide production-reachable type failures. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-14-01 | Critical | Tampering | mypy scope/config | mitigate/block | Exact argv/source roots and config semantic tests. |
| T-474-14-02 | High | Repudiation | repair result | mitigate/block | Before/after counts, tool/source/lock identity, and canonical report digest. |
| T-474-14-03 | High | Elevation of privilege | broad Any/provider modules | mitigate/block | AST/config guard and no provider/module exclusions. |
| T-474-14-SC | High | Tampering | package installs | mitigate/block | No package install. |

## ASVS 4.0.3 L1 Control Mapping

| ASVS L1 category | Applicability and concrete control | Verification |
|---|---|---|
| V1 Architecture | Applicable — exclusive file ownership, explicit dependencies, and fail-closed boundaries are part of this plan. | Task 14-01 automated command and this plan's focused acceptance criteria. |
| V2 Authentication | Applicable — exact identity/provider inputs remain authenticated and are exercised by focused tests. | Task 14-01 automated command and this plan's focused acceptance criteria. |
| V3 Session Management | Applicable — session/token state is closed and expiry/replay behavior remains tested. | Task 14-01 automated command and this plan's focused acceptance criteria. |
| V4 Access Control | Applicable — authorization/release authority remains deny-first; any High/Critical failure blocks completion. | Task 14-01 automated command and this plan's focused acceptance criteria. |
| V5 Validation | Applicable — untrusted records, command inputs, and result unions are narrowed before use; malformed values fail. | Task 14-01 automated command and this plan's focused acceptance criteria. |
| V6 Cryptography | Applicable — SHA-256/content identities are recomputed and never substitute for secret-key controls. | Task 14-01 automated command and this plan's focused acceptance criteria. |
| V7 Error Handling and Logging | Applicable — stable bounded errors/results are verified and private/provider payloads are not logged. | Task 14-01 automated command and this plan's focused acceptance criteria. |
| V8 Data Protection | Applicable — tests preserve redaction and prevent secret, token, student-content, or provider-payload disclosure. | Task 14-01 automated command and this plan's focused acceptance criteria. |
| V9 Communications | Applicable — real/provider/network boundaries require approved origins, denial, or immutable transport identity. | Task 14-01 automated command and this plan's focused acceptance criteria. |
| V10 Malicious Code | Applicable — no unreviewed package/action is added; the SC threat and locked dependency policy remain blocking. | Task 14-01 automated command and this plan's focused acceptance criteria. |
| V11 Business Logic | Applicable — focused tests preserve exact state, race, retry, idempotency, and fail-closed outcomes relevant to the owned files. | Task 14-01 automated command and this plan's focused acceptance criteria. |
| V12 Files and Resources | Applicable — every modified path is enumerated and artifact/evidence bytes or source files are bounded by exact ownership. | Task 14-01 automated command and this plan's focused acceptance criteria. |
| V13 API and Web Services | Applicable — request/CLI/workflow contracts are closed and verified end to end. | Task 14-01 automated command and this plan's focused acceptance criteria. |
| V14 Configuration | Applicable — exact tool/scope/configuration is verified and broad ignores, mutable refs, or permissive defaults are forbidden. | Task 14-01 automated command and this plan's focused acceptance criteria. |

Any failed applicable ASVS L1 control, or any High/Critical STRIDE threat without its named mitigation and test, blocks plan completion.
</threat_model>

<tasks>

<task type="auto" id="474-14-01" tdd="true">
  <name>Enforce exact full quality scope and forbid semantic weakening</name>
  <files>schemas/release/quality-repair-report-v1.schema.json, tests/test_quality_gate_policy.py</files>
  <behavior>
    - Only the exact full mypy/Ruff commands can satisfy quality receipts.
    - Global/module exclusions, missing-import suppression, follow-import skipping, blanket ignores, and broadened Any fail.
    - The report accounts for the measured 435/113/221 baseline and exact zero result.
  </behavior>
  <read_first>
    - schemas/release/quality-repair-report-v1.schema.json
    - tests/test_quality_gate_policy.py
    - pyproject.toml
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - all changed files listed in 474-06 through 474-13 summaries
  </read_first>
  <action>Create the closed repair schema and semantic/AST/config tests. Validate exact command tokens and roots; detect new/broadened `Any`, `type: ignore`, `ignore_missing_imports`, `exclude`, `follow_imports=skip`, per-module ignore blocks, and omitted provider/script/test roots. Allow only pre-existing narrow suppressions documented by exact file/code/line and reject any count increase. Require baseline and after counts with source/lock/tool identities.</action>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_quality_gate_policy.py</automated></verify>
  <acceptance_criteria>
    - Every forbidden weakening has a mutation test.
    - Exact source roots and before counts are schema-required.
    - A narrower command cannot validate a zero report.
  </acceptance_criteria>
  <done>The quality policy distinguishes real zero from suppressed/narrowed zero.</done>
</task>

<task type="auto" id="474-14-02" tdd="true">
  <name>Register Ruff, mypy, and typing-policy gates and capture true zero</name>
  <files>scripts/release_gate.py, tests/test_quality_gate_policy.py</files>
  <behavior>
    - Canonical gate executes exact Ruff, mypy, and semantic-policy commands in the fresh environment.
    - Zero errors and zero forbidden weakening are required for PASS.
    - Report is canonical, source/lock bound, and records the 435-error repair delta.
  </behavior>
  <read_first>
    - scripts/release_gate.py
    - tests/test_quality_gate_policy.py
    - schemas/release/quality-repair-report-v1.schema.json
    - pyproject.toml
  </read_first>
  <action>Register the three exact gate IDs and execute them inside the Plan 02 fresh Python 3.12 environment. Parse machine/stable mypy and Ruff results without treating console prose alone as evidence. Emit the closed repair report and reject any nonzero/error/unknown output or semantic weakening. Run the exact full command after all repairs; if errors remain, fix the owning source/test rather than writing a baseline. Only an actual irreducible residual after concrete repair may trigger the D-09 owner decision path; do not precreate it.</action>
  <verify><automated>bash -lc '.venv/bin/python -m pytest -q tests/test_quality_gate_policy.py && .venv/bin/python -m ruff check src tests scripts --no-cache && MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa scripts tests'</automated></verify>
  <acceptance_criteria>
    - Exact Ruff command reports zero.
    - Exact full mypy command reports `Success: no issues found in 221 source files` or the current exact full file count after legitimate additions.
    - Semantic guard reports zero new/broadened forbidden patterns.
    - No baseline, exclude, broad Any, or owner waiver exists when zero passes.
  </acceptance_criteria>
  <done>V9QUAL-04 is an executable, source-bound, non-weakenable zero gate.</done>
</task>

<task type="checkpoint:decision" id="474-14-03" gate="blocking-human">
  <name>Resolve only a proven irreducible mypy residual</name>
  <files>scripts/release_gate.py, schemas/release/quality-repair-report-v1.schema.json, tests/test_quality_gate_policy.py</files>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md Open Questions (RESOLVED), question 3
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md D-09 and D-10
    - scripts/release_gate.py
    - schemas/release/quality-repair-report-v1.schema.json
    - tests/test_quality_gate_policy.py
  </read_first>
  <decision>Choose the disposition for the exact source-bound residual only if Task 14-02 still fails after every owning repair plan and concrete repair attempt.</decision>
  <context>If Task 14-02 reaches zero, record this checkpoint as NOT APPLICABLE and continue without pausing. Otherwise present the exact remaining diagnostics, files, tool/lock/source identities, repairs attempted, and why each diagnostic could not be narrowed with maintained stubs, explicit unions, or project-owned Protocols. Historical counts, time pressure, and invented baselines are ineligible per D-09/D-10.</context>
  <action>Run the automated precheck first. On zero, write no baseline and record NOT APPLICABLE. On a nonzero result, pause before changing the gate/schema/tests and present only the exact proven residual for the owner decision below; after the selection, implement only that selected disposition and rerun the full command. Never infer acceptance from silence or convert a broader/historical count into the source-bound maximum.</action>
  <options>
    <option id="continue-repair">
      <name>Continue exact repair (Recommended)</name>
      <pros>Preserves the locked zero target and release integrity.</pros>
      <cons>Requires another bounded owning-file repair before the gate can pass.</cons>
    </option>
    <option id="owner-accept-exact-baseline">
      <name>Accept exact residual</name>
      <pros>Uses the sole D-09 exception path for an evidenced irreducible residual.</pros>
      <cons>Creates a source/tool/lock-bound maximum that blocks every new or broadened error and requires explicit owner acceptance.</cons>
    </option>
    <option id="block-release">
      <name>Block release</name>
      <pros>Prevents a nonzero typing state from becoming eligible.</pros>
      <cons>Phase 474 remains incomplete until the residual is repaired or exactly accepted.</cons>
    </option>
  </options>
  <verify><automated>bash -lc '.venv/bin/python -m ruff check src tests scripts --no-cache && MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa scripts tests'</automated></verify>
  <acceptance_criteria>
    - Zero records the checkpoint as NOT APPLICABLE and creates no baseline.
    - A nonzero checkpoint presents only exact current diagnostics after all concrete repair attempts, with source/tool/lock identities and no historical padding.
    - Only the explicit owner selection is implemented; silence or automation cannot accept a baseline.
  </acceptance_criteria>
  <resume-signal>Select `continue-repair`, `owner-accept-exact-baseline`, or `block-release`; zero automatically records NOT APPLICABLE.</resume-signal>
  <done>Zero is retained, or the sole owner has explicitly selected a D-09 disposition for the exact proven residual.</done>
</task>

</tasks>

<verification>
Run the exact full Ruff/mypy commands twice in fresh Python 3.12 environments, semantic-policy mutation tests, and canonical report validation; compare source/lock/argv/count/digest facts.
</verification>

<success_criteria>
- The measured 435 errors across 113 files are repaired to zero.
- Ruff and full mypy are release-blocking under one exact command.
- D-09/D-10 suppression shortcuts are automatically rejected.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-14-SUMMARY.md` when done.
</output>
