---
phase: 474-deterministic-verification-and-gated-delivery
plan: 79
type: execute
wave: 19
depends_on: [474-33]
files_modified:
  - ".github/workflows/deploy.yml"
  - "docs/security/phase-474-workflow-policy.json"
  - "tests/test_delivery_workflow_contract.py"
  - "evidence/phase-474/protected-environments-resolution.json"
autonomous: true
requirements: [V9QUAL-06]
user_setup:
  - service: github
    why: "The authenticated actor must have Administration permission on stoasystem/stoa-backend before protected-environment, ruleset, or branch-protection writes are allowed."
    dashboard_config:
      - task: "Grant DengZhiyuan-math repository Administration permission, or supply an already-authorized repository administrator."
        location: "GitHub organization/repository access settings"
must_haves:
  truths:
    - "Exactly one GitHub Environment `staging` in stoasystem/stoa-backend allows only branch main, allows no tags, requires no reviewer, and binds one exact OIDC subject."
    - "Main protection and one active main ruleset enforce administrators while denying force-push, deletion, and non-fast-forward updates."
    - "Deploy, smoke, and rollback remain separate workflow jobs but all reference the same `staging` Environment and their three distinct staging roles."
    - "GitHub/workflow configuration makes no application, infrastructure, deploy, smoke, or rollback action; production is DEFERRED_OUT_OF_SCOPE."
  artifacts:
    - path: "evidence/phase-474/protected-environments-resolution.json"
      provides: "GitHub protected-environment apply/readback resolution (D-05, D-06, D-15)"
    - path: "docs/security/phase-474-workflow-policy.json"
      provides: "Closed staging-only GitHub Environment, workflow-job, role, and production-scope contract"
    - path: ".github/workflows/deploy.yml"
      provides: "One-environment staging deploy/smoke/rollback DAG without production jobs"
    - path: "tests/test_delivery_workflow_contract.py"
      provides: "Fail-closed workflow and policy regression coverage"
  key_links:
    - from: "evidence/phase-474/environment-baseline-resolution.json"
      to: "evidence/phase-474/protected-environments-resolution.json"
      via: "validated READY or READY_TO_BOOTSTRAP baseline plus exact GitHub post-change readback"
      pattern: 'protected\-environments\-resolution\.json'
---

<objective>
GitHub staging protection only (D-05, D-06, D-15).

Purpose: Consume the safe Plan 474-33 baseline, encode and configure exactly one staging Environment plus main protection/ruleset and the staging-only workflow contract, then preserve the prior six-environment blocked receipt as historical before-state.
Output: `.github/workflows/deploy.yml`, `docs/security/phase-474-workflow-policy.json`, `tests/test_delivery_workflow_contract.py`, and `evidence/phase-474/protected-environments-resolution.json`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `.github/workflows/deploy.yml` — Formal/build then separate deploy/smoke/rollback jobs, all under `environment: staging`, with no production job.
- `docs/security/phase-474-workflow-policy.json` — Exactly-one-Environment and three-role workflow contract; production fixed to `DEFERRED_OUT_OF_SCOPE`.
- `tests/test_delivery_workflow_contract.py` — Exact DAG/environment/role/OIDC and no-production-control-plane assertions.
- `evidence/phase-474/protected-environments-resolution.json` — Digest-bound GitHub apply/readback receipt that resolves but does not overwrite `protected-environments.json`.

## V9QUAL mapping

| Requirement | Plan evidence |
|---|---|
| V9QUAL-06 | Exactly one reviewer-free staging Environment, main protection/ruleset, one OIDC subject, and a three-job shared-Environment workflow are configured and provider-read back without executing deployment. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Configure and verify the staging-only GitHub release policy</name>
  <precondition>The Plan 474-33 baseline verifies as PASS with READY or READY_TO_BOOTSTRAP, and `gh api repos/stoasystem/stoa-backend --jq '.permissions.admin'` returns `true` for the authenticated actor.</precondition>
  <files>.github/workflows/deploy.yml, docs/security/phase-474-workflow-policy.json, tests/test_delivery_workflow_contract.py, evidence/phase-474/protected-environments-resolution.json</files>
  <behavior>
    - Exact single-staging Environment policy applies and provider readback matches.
    - Formal/build precede deploy/smoke/rollback; those three jobs all declare `environment: staging` and distinct least-privilege staging role names.
    - Any production job/Environment/role/OIDC subject, separate staging-smoke/staging-rollback Environment, manual reviewer, missing admin authority, branch/tag/protection/ruleset mismatch, or drift blocks.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - docs/security/phase-474-workflow-policy.json
    - .github/workflows/deploy.yml
    - tests/test_delivery_workflow_contract.py
    - scripts/release_environment.py
    - tests/test_release_environment.py
    - evidence/phase-474/environment-baseline-resolution.json
    - evidence/phase-474/protected-environments.json
  </read_first>
  <action>Per D-03/D-05/D-06/D-15, fail the external administration precondition before any write if the authenticated actor is not an administrator. Replace the workflow-policy contract with exactly one GitHub Environment `staging`, one OIDC subject `repo:stoasystem/stoa-backend:environment:staging`, required jobs `formal`, `immutable_build`, `staging_deploy`, `staging_smoke`, and `staging_rollback`, and role mapping `StoaStagingDeployRole`/`StoaStagingReadRole`/`StoaStagingRollbackRole`; set production release-control scope to `DEFERRED_OUT_OF_SCOPE`. Update `.github/workflows/deploy.yml` and its contract tests so formal/build carry no AWS authority, the three staging jobs are dependency-closed, all three declare `environment: staging`, each names only its corresponding role, and no production eligibility or operation job exists. These are checked-in configuration changes only: do not dispatch the workflow, assume a role, build, upload, synthesize, deploy, smoke, roll back, or touch application/infra state. Validate the Plan 474-33 baseline and configure only `stoasystem/stoa-backend`: create/update exactly `staging` with a custom deployment policy allowing branch `main`, no tag policy, and an empty reviewer list. Configure main branch protection with administrator enforcement, force pushes disabled, and deletion disabled; configure one active branch ruleset limited to `refs/heads/main` with deletion and non-fast-forward denial. Reject any extra release Environment, branch, tag, reviewer, team, subject, bypass, inactive ruleset, or drift. Read every object back through GitHub API, bind actor identity, request IDs, baseline digest, policy/workflow digests, and post-state digest, and write `protected-environments-resolution.json` whose `resolves` field references the historical six-environment blocked receipt without overwriting it. Every non-GitHub action remains `NOT RUN`; production scope remains `DEFERRED_OUT_OF_SCOPE`.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_delivery_workflow_contract.py tests/test_release_environment.py &amp;&amp; .venv/bin/python scripts/release_environment.py verify-github --baseline evidence/phase-474/environment-baseline-resolution.json --receipt evidence/phase-474/protected-environments-resolution.json --policy docs/security/phase-474-workflow-policy.json &amp;&amp; jq -e '"'"'.status == "PASS" and .actor.repository_admin == true and (.readback.environments | map(.name)) == ["staging"] and (.readback.environments[0].reviewers | length) == 0 and .readback.main_branch_protection.enforce_admins == true and .readback.main_branch_protection.allow_force_pushes == false and .readback.main_branch_protection.allow_deletions == false and .readback.rulesets[0].enforcement == "active" and .readback.oidc_subjects == ["repo:stoasystem/stoa-backend:environment:staging"] and .mutation.application == "NOT RUN" and .mutation.infrastructure == "NOT RUN" and .mutation.staging_deploy == "NOT RUN" and .mutation.staging_smoke == "NOT RUN" and .mutation.staging_rollback == "NOT RUN" and .scope.production_release_control_plane == "DEFERRED_OUT_OF_SCOPE"'"'"' evidence/phase-474/protected-environments-resolution.json >/dev/null'</automated>
  </verify>
  <acceptance>
    - Exactly one backend `staging` Environment, its main-only/no-tag/no-reviewer policy, main protection/ruleset, and one OIDC subject match provider readback.
    - The workflow has separate staging deploy/smoke/rollback jobs sharing that Environment and distinct role names, with no production node.
    - The historical blocked receipt remains unchanged, and no application, infrastructure, deploy, smoke, or rollback action is made.
  </acceptance>
  <done>The new resolution proves the exact single-staging GitHub/workflow contract, all non-configuration actions remain NOT RUN, and production release control is DEFERRED_OUT_OF_SCOPE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS V1-V14 Applicability

| ASVS | Control family | Disposition | Plan control |
|---|---|---|---|
| V1 | Architecture | apply | Keep the plan dependency-closed, source-bound, and independently verifiable. |
| V2 | Authentication | preserve | Do not introduce an alternate identity mechanism; smoke uses existing bounded auth. |
| V3 | Session Management | preserve | Do not broaden session lifetime or expose browser/session material in evidence. |
| V4 | Access Control | apply | Use least privilege and protected environment/OIDC boundaries before authority. |
| V5 | Validation | apply | Validate paths, refs, schemas, provider values, counts, and closed outcomes. |
| V6 | Cryptography | apply | Use SHA-256/AWS-managed encryption only; reject digest mismatch. |
| V7 | Error/Logging | apply | Emit stable redacted errors and identifiers without credentials or private payloads. |
| V8 | Data Protection | apply | Retain only required hashes, IDs, statuses, and policy evidence. |
| V9 | Communications | apply | Deny ambient network and constrain provider calls to reviewed endpoints/roles. |
| V10 | Malicious Code | apply | Use audited locks, immutable action SHAs, and tamper tests. |
| V11 | Business Logic | apply | Fail closed on missing prerequisites, stale identity, or partial outcome. |
| V12 | Files/Resources | apply | Use bounded paths, immutable blobs/objects, and resource limits. |
| V13 | API/Web Services | apply | Validate structured API/provider responses and preserve authorization semantics. |
| V14 | Configuration | apply | Use reviewed closed staging-only configuration with the production release-control plane explicitly `DEFERRED_OUT_OF_SCOPE`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-79-S | Spoofing | GitHub actor/repository identity | high | mitigate/block | Require exact authenticated administrator and backend repository identity. |
| T-474-79-T | Tampering | protected-environment apply/readback | high | mitigate/block | Bind baseline, policy, requests, and post-state; reject extra, missing, or drifted fields. |
| T-474-79-R | Repudiation | GitHub configuration receipt | medium | mitigate/block | Retain canonical actor/request/before/after identities in the new resolution. |
| T-474-79-I | Information Disclosure | diagnostics/evidence | medium | mitigate/block | Store redacted IDs, hashes, counts, and statuses only. |
| T-474-79-D | Denial of Service | bounded API execution | medium | mitigate/block | Bound API targets to one Environment, main protection, and one ruleset; fail without partial PASS. |
| T-474-79-E | Elevation of Privilege | GitHub protection boundary | high | mitigate/block | Require admin precondition and ensure configuration grants no application, infrastructure, or deploy authority. |
| T-474-79-SC | Tampering | package/action supply chain | high | mitigate/block | Use only RESEARCH-approved packages and immutable reviewed Action SHAs; [SLOP] is forbidden. |
</threat_model>

<verification>
  <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_delivery_workflow_contract.py tests/test_release_environment.py &amp;&amp; .venv/bin/python scripts/release_environment.py verify-github --baseline evidence/phase-474/environment-baseline-resolution.json --receipt evidence/phase-474/protected-environments-resolution.json --policy docs/security/phase-474-workflow-policy.json &amp;&amp; jq -e '"'"'.status == "PASS" and .scope.production_release_control_plane == "DEFERRED_OUT_OF_SCOPE"'"'"' evidence/phase-474/protected-environments-resolution.json >/dev/null'</automated>
</verification>

<success_criteria>
- Exactly one backend `staging` Environment matches the no-reviewer, main-only/no-tag, and exact-subject policy.
- Main protection and the active ruleset deny administrator bypass, force-push, deletion, and non-fast-forward updates as specified.
- Deploy, smoke, and rollback are separate jobs sharing `staging` and distinct staging role names; no production workflow job exists.
- GitHub/workflow configuration makes no application, infrastructure, deploy, smoke, or rollback action, and the historical blocked receipt remains unchanged.
- Every task command is green and its evidence is source-bound.
- Production GitHub/AWS release-control work is `DEFERRED_OUT_OF_SCOPE` to a separate future phase or milestone.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-79-SUMMARY.md` when done.
</output>
