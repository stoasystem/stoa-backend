---
phase: 474-deterministic-verification-and-gated-delivery
plan: 12
type: execute
wave: 7
depends_on: [474-08, 474-09, 474-10]
files_modified:
  - "src/stoa/services/ai_operations_service.py"
  - "src/stoa/services/ai_service.py"
  - "src/stoa/services/moderation_service.py"
  - "src/stoa/services/production_pilot_service.py"
  - "src/stoa/services/subscription_service.py"
  - "src/stoa/services/support_destination_service.py"
  - "src/stoa/services/support_handoff_service.py"
  - "src/stoa/services/support_sla_service.py"
autonomous: true
requirements: [V9QUAL-04]
must_haves:
  truths:
    - "The ai support moderation and subscription services slice is fully typed without broad suppression."
    - "Runtime regression behavior is unchanged."
  artifacts:
    - path: "src/stoa/services/ai_operations_service.py"
      provides: "AI support moderation and subscription services"
    - path: "src/stoa/services/ai_service.py"
      provides: "AI support moderation and subscription services"
    - path: "src/stoa/services/moderation_service.py"
      provides: "AI support moderation and subscription services"
    - path: "src/stoa/services/production_pilot_service.py"
      provides: "AI support moderation and subscription services"
    - path: "src/stoa/services/subscription_service.py"
      provides: "AI support moderation and subscription services"
    - path: "src/stoa/services/support_destination_service.py"
      provides: "AI support moderation and subscription services"
    - path: "src/stoa/services/support_handoff_service.py"
      provides: "AI support moderation and subscription services"
    - path: "src/stoa/services/support_sla_service.py"
      provides: "AI support moderation and subscription services"
  key_links:
    - from: "474-10"
      to: "src/stoa/services/ai_operations_service.py"
      via: "declared dependency and immutable receipt"
      pattern: 'ai_operations_service\.py'
---

<objective>
AI support moderation and subscription services (D-09, D-10).

Purpose: Close one real mypy root-cause family while retaining existing behavior and direct producer/consumer ordering.
Output: `src/stoa/services/ai_operations_service.py`, `src/stoa/services/ai_service.py`, `src/stoa/services/moderation_service.py`, `src/stoa/services/production_pilot_service.py`, `src/stoa/services/subscription_service.py`, `src/stoa/services/support_destination_service.py`, `src/stoa/services/support_handoff_service.py`, `src/stoa/services/support_sla_service.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `src/stoa/services/ai_operations_service.py` — AI support moderation and subscription services.
- `src/stoa/services/ai_service.py` — AI support moderation and subscription services.
- `src/stoa/services/moderation_service.py` — AI support moderation and subscription services.
- `src/stoa/services/production_pilot_service.py` — AI support moderation and subscription services.
- `src/stoa/services/subscription_service.py` — AI support moderation and subscription services.
- `src/stoa/services/support_destination_service.py` — AI support moderation and subscription services.
- `src/stoa/services/support_handoff_service.py` — AI support moderation and subscription services.
- `src/stoa/services/support_sla_service.py` — AI support moderation and subscription services.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type AI provider, operations, and subscription services</name>
  <files>src/stoa/services/ai_service.py, src/stoa/services/ai_operations_service.py, src/stoa/services/subscription_service.py</files>
  <behavior>
    - Previously measured diagnostics in the owned file set become zero.
    - Malformed/untrusted values remain explicitly narrowed before use.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - src/stoa/services/ai_service.py
    - src/stoa/services/ai_operations_service.py
    - src/stoa/services/subscription_service.py
  </read_first>
  <action>Repair the measured full-scope mypy errors in the AI provider, operations, and subscription services family, limited to src/stoa/services/ai_service.py, src/stoa/services/ai_operations_service.py, src/stoa/services/subscription_service.py. Start from the exact 435-error/113-file command and preserve runtime authorization, privacy, idempotency, retry, fence, pagination, and stable-error semantics. Use closed TypedDict/dataclass records, explicit Optional/union narrowing, maintained stubs, or narrow project-owned Protocols at provider boundaries per D-09/D-10. Do not add or broaden Any, casts that hide unvalidated input, type: ignore, excludes, follow_imports=skip, ignore_missing_imports, skips, xfails, retries, or weaker assertions. This task implements D-09, D-10.</action>
  <verify>
    <automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/services/ai_service.py src/stoa/services/ai_operations_service.py src/stoa/services/subscription_service.py &amp;&amp; .venv/bin/python -m pytest -q tests/test_subscription_operations.py tests/test_conversations.py &amp;&amp; .venv/bin/ruff check src/stoa/services/ai_service.py src/stoa/services/ai_operations_service.py src/stoa/services/subscription_service.py'</automated>
  </verify>
  <acceptance>
    - The AI provider, operations, and subscription services files pass focused mypy and Ruff with no semantic suppression.
    - Named runtime regressions remain green.
  </acceptance>
  <done>The AI provider, operations, and subscription services files pass focused mypy and Ruff with no semantic suppression. Named runtime regressions remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Type support routing, SLA, pilot, and moderation services</name>
  <files>src/stoa/services/support_destination_service.py, src/stoa/services/support_handoff_service.py, src/stoa/services/support_sla_service.py, src/stoa/services/production_pilot_service.py, src/stoa/services/moderation_service.py</files>
  <behavior>
    - Previously measured diagnostics in the owned file set become zero.
    - Malformed/untrusted values remain explicitly narrowed before use.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - src/stoa/services/support_destination_service.py
    - src/stoa/services/support_handoff_service.py
    - src/stoa/services/support_sla_service.py
    - src/stoa/services/production_pilot_service.py
  </read_first>
  <action>Repair the measured full-scope mypy errors in the support routing, SLA, pilot, and moderation services family, limited to src/stoa/services/support_destination_service.py, src/stoa/services/support_handoff_service.py, src/stoa/services/support_sla_service.py, src/stoa/services/production_pilot_service.py, src/stoa/services/moderation_service.py. Start from the exact 435-error/113-file command and preserve runtime authorization, privacy, idempotency, retry, fence, pagination, and stable-error semantics. Use closed TypedDict/dataclass records, explicit Optional/union narrowing, maintained stubs, or narrow project-owned Protocols at provider boundaries per D-09/D-10. Do not add or broaden Any, casts that hide unvalidated input, type: ignore, excludes, follow_imports=skip, ignore_missing_imports, skips, xfails, retries, or weaker assertions. This task implements D-09, D-10.</action>
  <verify>
    <automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/services/support_destination_service.py src/stoa/services/support_handoff_service.py src/stoa/services/support_sla_service.py src/stoa/services/production_pilot_service.py src/stoa/services/moderation_service.py &amp;&amp; .venv/bin/python -m pytest -q tests/test_authorization_audit.py &amp;&amp; .venv/bin/ruff check src/stoa/services/support_destination_service.py src/stoa/services/support_handoff_service.py src/stoa/services/support_sla_service.py src/stoa/services/production_pilot_service.py src/stoa/services/moderation_service.py'</automated>
  </verify>
  <acceptance>
    - The support routing, SLA, pilot, and moderation services files pass focused mypy and Ruff with no semantic suppression.
    - Named runtime regressions remain green.
  </acceptance>
  <done>The support routing, SLA, pilot, and moderation services files pass focused mypy and Ruff with no semantic suppression. Named runtime regressions remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS / STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-12-01 | High | Tampering / Repudiation | AI support moderation and subscription services | mitigate/block | Bind exact source, inputs, command, result, and artifact identities; reject missing, duplicate, unknown, dirty, or mismatched values. |
| T-474-12-02 | High | Spoofing / Elevation | verification-to-delivery boundary | mitigate/block | Apply least privilege, protected environment checks, and dependency-closed eligibility before authority is exposed. |
| T-474-SC | High | Tampering | audited package installs | mitigate/block | Use only packages marked OK in RESEARCH.md Package Legitimacy Audit; [ASSUMED]/[SUS] requires a blocking human checkpoint and [SLOP] is forbidden. |
</threat_model>

<verification>
<automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/services/support_destination_service.py src/stoa/services/support_handoff_service.py src/stoa/services/support_sla_service.py src/stoa/services/production_pilot_service.py src/stoa/services/moderation_service.py &amp;&amp; .venv/bin/python -m pytest -q tests/test_authorization_audit.py &amp;&amp; .venv/bin/ruff check src/stoa/services/support_destination_service.py src/stoa/services/support_handoff_service.py src/stoa/services/support_sla_service.py src/stoa/services/production_pilot_service.py src/stoa/services/moderation_service.py'</automated>
</verification>

<success_criteria>
- The ai support moderation and subscription services slice is fully typed without broad suppression.
- Runtime regression behavior is unchanged.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless a later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-12-SUMMARY.md` when done.
</output>
