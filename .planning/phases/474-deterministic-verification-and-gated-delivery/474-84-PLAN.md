---
phase: 474-deterministic-verification-and-gated-delivery
plan: 84
type: execute
wave: 10
depends_on: [474-82]
files_modified:
  - "/Users/zhdeng/stoa-frontend/src/services/contact/contactApi.ts"
  - "/Users/zhdeng/stoa-frontend/tests/release/contact-backend-only.test.mjs"
autonomous: true
requirements: [V9QUAL-03, V9QUAL-05]
must_haves:
  truths:
    - "Contact requests use only the authorized backend API path."
    - "Browser source contains no EmailJS endpoint, public key, service/template identity, direct email fallback, or fabricated success receipt."
  artifacts:
    - path: "/Users/zhdeng/stoa-frontend/src/services/contact/contactApi.ts"
      provides: "Backend-only contact submission"
    - path: "/Users/zhdeng/stoa-frontend/tests/release/contact-backend-only.test.mjs"
      provides: "No-browser-email fallback contract"
---

<objective>
Remove the browser EmailJS contact fallback.

Purpose: Keep contact delivery, idempotency, auditing, and credentials behind the authorized backend boundary.
Output: backend-only contact submission plus adversarial source/runtime tests.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Delete browser EmailJS fallback behavior</name>
  <files>/Users/zhdeng/stoa-frontend/src/services/contact/contactApi.ts, /Users/zhdeng/stoa-frontend/tests/release/contact-backend-only.test.mjs</files>
  <behavior>
    - Successful `/contact/requests` responses pass through unchanged.
    - Network, backend, validation, and authorization failures remain failures; none triggers a browser provider call or synthetic success response.
    - Source contains no EmailJS URL, key, service/template ID, inbox address, fallback flag, direct-email helper, or `VITE_CONTACT_*` value.
  </behavior>
  <read_first>
    - /Users/zhdeng/stoa-frontend/src/services/contact/contactApi.ts
    - /Users/zhdeng/stoa-frontend/src/services/api/httpClient.ts
    - /Users/zhdeng/stoa-frontend/tests/release/contact-backend-only.test.mjs
  </read_first>
  <action>Reduce contact submission to the existing authenticated/authorized backend API call and return only its server receipt. Delete every browser EmailJS credential/template/fallback path and do not replace it with another direct provider. Preserve stable backend error handling. Do not change startup, runtime projection, monitoring, backend endpoints, or mobile.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail &amp;&amp; cd /Users/zhdeng/stoa-frontend &amp;&amp; node --version | grep -Eq "^v20\." &amp;&amp; node --test tests/release/contact-backend-only.test.mjs &amp;&amp; npm exec -- tsc -b'</automated>
  </verify>
  <acceptance>
    - Browser contact code has one backend-only delivery path and cannot claim fallback success.
  </acceptance>
  <done>Contact submission never sends email or stores provider configuration in the browser.</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Browser provider identifiers enable unauthorized delivery/spam | Remove provider endpoint and all client configuration. |
| Network failure becomes fabricated success | Propagate the backend failure and accept only a server receipt. |
</threat_model>

<verification>
  <automated>bash -lc 'set -euo pipefail &amp;&amp; cd /Users/zhdeng/stoa-frontend &amp;&amp; node --version | grep -Eq "^v20\." &amp;&amp; node --test tests/release/contact-backend-only.test.mjs &amp;&amp; npm exec -- tsc -b'</automated>
</verification>

<success_criteria>
- EmailJS/direct browser delivery is absent.
- Node 20 tests and TypeScript pass.
- Production operations remain exact `NOT RUN`.
</success_criteria>

<output>Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-84-SUMMARY.md` when done.</output>
