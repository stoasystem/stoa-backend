---
phase: 474-deterministic-verification-and-gated-delivery
plan: 83
type: execute
wave: 1
depends_on: []
files_modified:
  - evidence/phase-474/candidate-identity.json
autonomous: true
requirements: [V9QUAL-01, V9QUAL-03, V9QUAL-06, V9QUAL-07]
must_haves:
  truths:
    - "Execution records the clean backend, Web, and infrastructure HEAD, tree, and lock identities that actually exist when Phase 474 starts."
    - "Planning-time measured SHAs are diagnostic context only and never become release identity."
    - "Any dirty repository blocks for owner action without reset, deletion, checkout, staging, or adoption of user work."
  artifacts:
    - path: evidence/phase-474/candidate-identity.json
      provides: "Canonical clean cross-repository starting identity receipt"
  key_links:
    - from: evidence/phase-474/candidate-identity.json
      to: /Users/zhdeng/stoa-backend/.git
      via: "execution-time HEAD/tree/status and uv.lock digest"
    - from: evidence/phase-474/candidate-identity.json
      to: /Users/zhdeng/stoa-frontend/.git
      via: "execution-time HEAD/tree/status and package-lock.json digest"
    - from: evidence/phase-474/candidate-identity.json
      to: /Users/zhdeng/stoa-infra/.git
      via: "execution-time HEAD/tree/status and uv.lock digest"
---

<objective>
Receipt the clean execution-time identity of the backend, Web, and infrastructure candidate.

Purpose: Give every cross-repository edit one immutable, non-stale starting identity without mutating or silently adopting user work per D-13.
Output: `evidence/phase-474/candidate-identity.json`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `evidence/phase-474/candidate-identity.json` records, for all three repositories, the exact root, execution-time commit OID, `HEAD^{tree}` OID, authoritative lock path/SHA-256, clean-status digest, and `clean: true` result.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| three Git worktrees -> candidate receipt | Mutable or dirty source must not be misrepresented as the clean release starting point. |
| candidate receipt -> sibling-repository plans | A stale, hard-coded, or partially checked identity could admit unrelated source. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-83-01 | Critical | Tampering | candidate source identity | mitigate/block | Recompute all three execution-time HEAD/tree/lock identities and require clean porcelain before writing the receipt. |
| T-474-83-02 | High | Repudiation | receipt provenance | mitigate/block | Canonical fields bind roots, commit/tree OIDs, lock digests, status digest, and receipt digest. |
| T-474-83-03 | High | Elevation of privilege | user-owned work | mitigate/block | Dirt blocks for owner action; no reset, clean, checkout, delete, move, stage, ignore, or adoption is authorized. |
| T-474-83-SC | High | Tampering | supply chain | mitigate/block | The task performs no fetch, install, checkout, package, or Action operation. |

## ASVS 4.0.3 L1 Control Mapping

| ASVS L1 category | Applicability and concrete control | Verification |
|---|---|---|
| V1 Architecture | Applicable — one receipt is the cross-repository starting-identity boundary. | Task 83-01 closed schema/root checks. |
| V2 Authentication | N/A — no user or provider credential is issued or validated. | No credential-bearing operation. |
| V3 Session Management | N/A — no application session is created or changed. | No session surface. |
| V4 Access Control | Applicable — only the three exact repository roots may enter the receipt. | Exact-root and clean-status checks. |
| V5 Validation | Applicable — OIDs, paths, lock digests, booleans, and repository keys are closed. | `jq` schema checks and Git/hash recomputation. |
| V6 Cryptography | Applicable — SHA-256 and Git object IDs are recomputed, not trusted from planning prose. | Dynamic hash/OID equality checks. |
| V7 Error Handling and Logging | Applicable — any dirt or missing identity blocks with a bounded repository label. | No PASS receipt on mismatch. |
| V8 Data Protection | Applicable — the receipt excludes diffs, file contents, remotes with credentials, and environment values. | Closed allowlisted receipt fields. |
| V9 Communications | N/A — no network access occurs. | No fetch or provider call. |
| V10 Malicious Code | Applicable — no package, Action, hook installation, or executable source acquisition occurs. | Read-only Git/hash commands only. |
| V11 Business Logic | Applicable — all three repositories must pass as one indivisible candidate identity. | Missing/dirty member blocks the whole task. |
| V12 Files and Resources | Applicable — only the one receipt is written. | Frontmatter/task path equality. |
| V13 API and Web Services | N/A — no API or Web service is invoked. | Local Git/filesystem verification only. |
| V14 Configuration | Applicable — branch names, tags, mutable refs, and research SHAs cannot substitute for execution-time OIDs. | Receipt fields are recomputed at execution. |

Any failed applicable ASVS L1 control or unmitigated High/Critical threat blocks completion.
</threat_model>

<tasks>

<task type="auto" id="474-83-01">
  <name>Record one clean cross-repository candidate identity</name>
  <files>evidence/phase-474/candidate-identity.json</files>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md observed checkout identities
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md cross-repository provenance
    - uv.lock
    - /Users/zhdeng/stoa-frontend/package-lock.json
    - /Users/zhdeng/stoa-infra/uv.lock
  </read_first>
  <action>Before writing any receipt, inspect exactly `/Users/zhdeng/stoa-backend`, `/Users/zhdeng/stoa-frontend`, and `/Users/zhdeng/stoa-infra`. Require each resolved top-level root to equal its declared root and each `git status --porcelain=v1 --untracked-files=all` to be empty. If any repository is dirty, stop for owner action and report only its root plus bounded path names; never reset, checkout, clean, delete, move, ignore, stage, commit, overwrite, or adopt the work. Do not compare against the research SHAs because those are planning measurements, not D-13 release identity. After all three pass, write one canonical JSON receipt containing schema, generated-at/run ID, and a closed `repositories` object whose backend/frontend/infra entries contain root, execution-time 40-hex HEAD, `HEAD^{tree}`, authoritative lock path and SHA-256, empty-status SHA-256, and `clean: true`; add a canonical receipt SHA-256 and no source contents, remotes, tokens, credentials, or environment values. Plans 15, 18, and 72 must consume these fields and reject drift.</action>
  <verify><automated>bash -lc 'jq -e '\'' .schema == "stoa.release.candidate-identity.v1" and (.repositories | keys == ["backend","frontend","infra"]) and ([.repositories[] | .clean] | all) and ([.repositories[] | .head, .tree] | all(test("^[0-9a-f]{40}$"))) '\'' evidence/phase-474/candidate-identity.json >/dev/null && test "$(jq -r .repositories.backend.head evidence/phase-474/candidate-identity.json)" = "$(git -C /Users/zhdeng/stoa-backend rev-parse HEAD)" && test "$(jq -r .repositories.frontend.head evidence/phase-474/candidate-identity.json)" = "$(git -C /Users/zhdeng/stoa-frontend rev-parse HEAD)" && test "$(jq -r .repositories.infra.head evidence/phase-474/candidate-identity.json)" = "$(git -C /Users/zhdeng/stoa-infra rev-parse HEAD)" && test -z "$(git -C /Users/zhdeng/stoa-frontend status --porcelain=v1 --untracked-files=all)" && test -z "$(git -C /Users/zhdeng/stoa-infra status --porcelain=v1 --untracked-files=all)"'</automated></verify>
  <acceptance_criteria>
    - The one receipt contains exact execution-time HEAD/tree/lock identities for backend, Web, and infrastructure.
    - All three worktrees were clean before the receipt write; any dirt blocks for owner action without repository mutation.
    - No planning-time SHA, branch, tag, or mutable pointer is asserted as the candidate identity.
  </acceptance_criteria>
  <done>The clean three-repository starting candidate is immutably receipted for dependent plans.</done>
</task>

</tasks>

<verification>
Validate the closed JSON shape, recompute every Git/lock identity, and require the sibling repositories remain clean. A missing member, malformed OID, digest mismatch, or unapproved dirt blocks every dependent cross-repository edit.
</verification>

<success_criteria>
- One independently verifiable receipt binds the actual clean backend/Web/infra starting candidate.
- User work is never mutated or silently included.
- D-13 identity is execution-derived; Web-first D-17/D-18 scope and production `NOT RUN` remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-83-SUMMARY.md` when done.
</output>
