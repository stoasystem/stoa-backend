---
phase: 474-deterministic-verification-and-gated-delivery
plan: 83
type: execute
wave: 10
depends_on: [474-81, 474-82]
files_modified:
  - "/Users/zhdeng/stoa-frontend/src/services/monitoring/frontendErrorMonitoring.ts"
  - "/Users/zhdeng/stoa-frontend/tests/release/runtime-monitoring-flag.test.mjs"
autonomous: true
requirements: [V9QUAL-03]
must_haves:
  truths:
    - "Frontend error reporting follows only the validated runtime `features.errorMonitoring` flag."
    - "Compile-time defaults cannot silently enable monitoring or broaden captured data."
  artifacts:
    - path: "/Users/zhdeng/stoa-frontend/src/services/monitoring/frontendErrorMonitoring.ts"
      provides: "Runtime-controlled monitoring decision"
    - path: "/Users/zhdeng/stoa-frontend/tests/release/runtime-monitoring-flag.test.mjs"
      provides: "Monitoring flag and redaction regressions"
---

<objective>
Move frontend monitoring enablement to the validated runtime feature flag.

Purpose: Remove the remaining compile-time monitoring policy without changing telemetry payload/redaction behavior.
Output: one runtime-backed monitoring decision and focused tests.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Read monitoring enablement from runtime config</name>
  <files>/Users/zhdeng/stoa-frontend/src/services/monitoring/frontendErrorMonitoring.ts, /Users/zhdeng/stoa-frontend/tests/release/runtime-monitoring-flag.test.mjs</files>
  <behavior>
    - `features.errorMonitoring=false` performs no report request; `true` uses the existing bounded backend endpoint after startup.
    - Missing runtime config fails closed and no `VITE_ENABLE_FRONTEND_MONITORING` or environment default decides policy.
    - Existing token/private-text redaction and bounded payload rules remain unchanged and tested.
  </behavior>
  <read_first>
    - /Users/zhdeng/stoa-frontend/src/lib/env.ts
    - /Users/zhdeng/stoa-frontend/src/services/monitoring/frontendErrorMonitoring.ts
    - /Users/zhdeng/stoa-frontend/tests/release/runtime-monitoring-flag.test.mjs
  </read_first>
  <action>Replace the compile-time/environment fallback in `isMonitoringEnabled` with the exact validated runtime flag. Preserve the existing backend endpoint, redaction, truncation, and failure behavior. Do not change startup, general environment projection, contact delivery, telemetry schema, or mobile.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail &amp;&amp; cd /Users/zhdeng/stoa-frontend &amp;&amp; node --version | grep -Eq "^v20\." &amp;&amp; node --test tests/release/runtime-monitoring-flag.test.mjs &amp;&amp; npm exec -- tsc -b'</automated>
  </verify>
  <acceptance>
    - Monitoring has one runtime policy source and retains all privacy bounds.
  </acceptance>
  <done>Frontend monitoring enablement follows only validated runtime configuration.</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Monitoring silently enabled by build environment | Remove VITE/default policy and require the installed runtime flag. |
| Error report exposes private data | Preserve and regression-test existing redaction and size bounds. |
</threat_model>

<verification>
  <automated>bash -lc 'set -euo pipefail &amp;&amp; cd /Users/zhdeng/stoa-frontend &amp;&amp; node --version | grep -Eq "^v20\." &amp;&amp; node --test tests/release/runtime-monitoring-flag.test.mjs &amp;&amp; npm exec -- tsc -b'</automated>
</verification>

<success_criteria>
- Monitoring reads one validated runtime flag.
- Node 20 tests and TypeScript pass.
- Production operations remain exact `NOT RUN`.
</success_criteria>

<output>Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-83-SUMMARY.md` when done.</output>
