---
phase: 474-deterministic-verification-and-gated-delivery
plan: 89
type: execute
wave: 11
depends_on: [474-86, 474-88]
files_modified:
  - scripts/release_gate.py
  - schemas/release/formal-gate-run-v1.schema.json
  - tests/test_formal_release_gate.py
autonomous: true
requirements: [V9QUAL-01]
must_haves:
  truths:
    - "One non-selectable formal command always executes backend-python-hermetic followed by frontend-web-fresh against one exact candidate."
    - "A child execution failure, policy rejection, or exact NOT RUN can never become aggregate PASS, and the second valid obligation still runs after the first valid non-PASS."
    - "The aggregate retains two independently materialized and validated child receipts, contains no host path or environment value, and publishes only through a private external atomic output."
  artifacts:
    - path: scripts/release_gate.py
      provides: "Fixed formal orchestration and semantic validation"
    - path: schemas/release/formal-gate-run-v1.schema.json
      provides: "Closed formal aggregate receipt contract"
    - path: tests/test_formal_release_gate.py
      provides: "Adversarial CLI, ordering, classification, drift, privacy, and publication coverage"
---

<objective>
Add one authoritative non-selectable formal aggregate over the existing Python hermetic and fresh Web gates.

Purpose: make local and later CI callers invoke one fixed verification graph without allowing a weaker gate selection.
Output: a `formal` CLI and one closed source-bound aggregate receipt. Workflow wiring, source handoff, provider CI, deployment, mobile/native, and production remain outside this plan.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-86-SUMMARY.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-88-SUMMARY.md
@scripts/release_gate.py
@schemas/release/gate-receipt-v1.schema.json
@tests/test_release_gate.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the fixed formal aggregate</name>
  <files>scripts/release_gate.py, schemas/release/formal-gate-run-v1.schema.json, tests/test_formal_release_gate.py</files>
  <behavior>
    - Add a `formal` command with required candidate, backend-root, frontend-root, infra-root, and private external output arguments. Reject duplicate arguments and every caller-supplied gate, skip, only, order, or custom argv option.
    - Keep formal outside `GateRegistry`. Execute exactly `backend-python-hermetic` then `frontend-web-fresh`, each as a registered `verify` child against the same frozen candidate and a different materialized three-repository snapshot.
    - Revalidate the live candidate before and after every materialization/child, after outer validation, and immediately before publication. Candidate drift, materialization failure, invalid child evidence, or publication failure emits no partial aggregate.
    - Never keep sibling child snapshots alive concurrently. Fully execute and semantically validate one child inside its snapshot, freeze a canonical deep copy of that receipt, delete the snapshot, and only then materialize the next child. The outer validator consumes only those same-process validated frozen receipts and rechecks their closed shape, digest, source, order, and result; it must not pretend to recompute a deleted Web artifact.
    - Run the second child after the first valid PASS, policy rejection, execution failure, or exact NOT RUN. Classify the outer result with strict priority: execution failure, policy rejection, NOT RUN, then complete PASS. Count exactly two obligations rather than child test nodes.
    - Add a closed `stoa.release.formal-gate-run.v1` schema with fixed command graph, source/input/runtime/privacy/time identities, exact two-child order, four production `NOT RUN` values, and a canonical whole-receipt SHA-256 that retains both child receipt digests.
    - Require an existing current-user mode-0700 external parent and publish one mode-0600 single-link regular file through exclusive no-follow creation, file fsync, same-directory atomic replace, parent fsync, final revalidation, and stale-output invalidation. Never publish a second formal receipt to stdout.
    - Preserve Web containment-unavailable as the exact child NOT RUN receipt; Python PASS plus Web NOT RUN makes formal NOT RUN. Do not add or execute deployment, staging, provider, mobile/native, or production behavior.
  </behavior>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_formal_release_gate.py tests/test_release_gate.py tests/test_release_manifest.py &amp;&amp; .venv/bin/ruff check scripts/release_gate.py tests/test_formal_release_gate.py &amp;&amp; .venv/bin/mypy --strict scripts/release_gate.py &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance>
    - No CLI input can remove, add, substitute, or reorder either formal child.
    - Two PASS children are the only aggregate PASS; every legal non-PASS combination returns the exact stable outer status and exit code.
    - Missing, extra, swapped, tampered, source-mismatched, stale, or privacy-leaking child/outer evidence fails closed without stale output.
    - Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` with zero calls.
  </acceptance>
  <done>Local and later CI callers have one fixed cross-repository formal verification entry point.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| Caller substitutes a weaker gate graph | No gate-list input; fixed checked-in two-child tuple and logical argv |
| A later child scans temporary storage and mutates an earlier snapshot | Strict sequential snapshot lifetime; child 1 is validated, frozen, and deleted before child 2 exists |
| Live source mutates during the long run | Repeated live candidate brackets and no partial aggregate |
| A valid non-PASS is overclaimed as PASS | Exact classification priority and two-obligation closed result matrix |
| A stale or hostile output target preserves evidence | Private dirfd/no-follow atomic publication with stale invalidation and fsync |
| Receipt leaks host paths, environment, or diagnostics | Logical placeholders, hashes/counts only, closed schema, and privacy rejection tests |
</threat_model>

<success_criteria>
- `formal` always owns exactly the Python hermetic and fresh Web obligations in fixed order.
- Both complete child receipts bind one candidate and survive independent semantic validation.
- Only two child PASS results produce aggregate PASS.
- No workflow, delivery, provider, mobile/native, or production mutation is introduced.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-89-SUMMARY.md` when done.
</output>
