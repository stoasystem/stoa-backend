---
phase: 474-deterministic-verification-and-gated-delivery
plan: 81
type: execute
wave: 8
depends_on: [474-72, 474-73]
files_modified:
  - "/Users/zhdeng/stoa-frontend/src/lib/env.ts"
  - "/Users/zhdeng/stoa-frontend/tests/release/runtime-env-projection.test.mjs"
autonomous: true
requirements: [V9QUAL-03, V9QUAL-06]
must_haves:
  truths:
    - "Release-varying Web environment values project only from the installed validated runtime config."
    - "Production-facing code has no VITE endpoint, release, demo, payment, realtime, or feature fallback."
  artifacts:
    - path: "/Users/zhdeng/stoa-frontend/src/lib/env.ts"
      provides: "Compatibility projection over validated runtime configuration"
    - path: "/Users/zhdeng/stoa-frontend/tests/release/runtime-env-projection.test.mjs"
      provides: "Projection and no-VITE regression contract"
  key_links:
    - from: "474-72"
      to: "/Users/zhdeng/stoa-frontend/src/lib/env.ts"
      via: "getRuntimeConfig-only projection"
      pattern: 'getRuntimeConfig'
---

<objective>
Project the installed runtime configuration through the existing Web environment interface.

Purpose: Remove release-varying compile-time truth without combining service startup, monitoring, or contact delivery changes.
Output: one runtime-backed `env.ts` projection and focused Node tests.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Replace VITE release values with a runtime projection</name>
  <files>/Users/zhdeng/stoa-frontend/src/lib/env.ts, /Users/zhdeng/stoa-frontend/tests/release/runtime-env-projection.test.mjs</files>
  <behavior>
    - Installed staging, staging-pilot, and production configs map exactly to existing API, environment, realtime, and non-demo feature consumers.
    - Import/read before installation fails with the runtime-config error; missing values never fall back to mock, demo, localhost, or a VITE release value.
    - Source and built projection contain no release-varying `VITE_*`, Cognito/Amplify, native/mobile, EmailJS, or monitoring policy.
  </behavior>
  <read_first>
    - /Users/zhdeng/stoa-frontend/src/lib/runtimeConfig.ts
    - /Users/zhdeng/stoa-frontend/src/lib/env.ts
    - /Users/zhdeng/stoa-frontend/tests/release/runtime-config.test.mjs
    - /Users/zhdeng/stoa-frontend/tests/release/runtime-env-projection.test.mjs
  </read_first>
  <action>Make `env.ts` a compatibility projection over `getRuntimeConfig()` only. Preserve names needed by current consumers while mapping environment, API origin, realtime endpoint/enabled state, and exact feature flags from the validated registry. Delete production-facing release defaults and VITE reads, and fail before access when no config is installed. Do not edit startup, monitoring, contact, auth-provider, or mobile code.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail &amp;&amp; cd /Users/zhdeng/stoa-frontend &amp;&amp; node --version | grep -Eq "^v20\." &amp;&amp; node --test tests/release/runtime-env-projection.test.mjs &amp;&amp; npm exec -- tsc -b'</automated>
  </verify>
  <acceptance>
    - Runtime config is the sole release-varying source for `env.ts`.
    - No mock/demo/local fallback can silently enter a release build.
  </acceptance>
  <done>The existing Web environment interface projects only installed validated runtime state.</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Compile-time or demo configuration bypasses reviewed runtime state | Reject pre-install reads and remove release-varying VITE/default branches. |
| Unknown feature broadens a journey | Project only closed RuntimeConfig fields and exact booleans. |
</threat_model>

<verification>
  <automated>bash -lc 'set -euo pipefail &amp;&amp; cd /Users/zhdeng/stoa-frontend &amp;&amp; node --version | grep -Eq "^v20\." &amp;&amp; node --test tests/release/runtime-env-projection.test.mjs &amp;&amp; npm exec -- tsc -b'</automated>
</verification>

<success_criteria>
- `env.ts` has one runtime-backed release truth.
- Node 20 tests and TypeScript pass.
- Production operations remain exact `NOT RUN`.
</success_criteria>

<output>Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-81-SUMMARY.md` when done.</output>
