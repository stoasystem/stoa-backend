---
phase: 474-deterministic-verification-and-gated-delivery
plan: 35
type: execute
wave: 21
depends_on: [474-34, 474-80, 474-93]
files_modified:
  - "evidence/phase-474/staging-delivery.json"
  - "evidence/phase-474/controlled-rollback.json"
autonomous: true
requirements: [V9QUAL-01, V9QUAL-02, V9QUAL-03, V9QUAL-04, V9QUAL-05, V9QUAL-06]
must_haves:
  truths:
    - "The same build-once set passes live staging backend/Web smoke."
    - "Deploy, smoke, and rollback jobs share the GitHub `staging` Environment while assuming distinct least-privilege deploy/read/rollback roles."
    - "A controlled staging failure automatically restores both previous verified pointers."
    - "Production release control remains DEFERRED_OUT_OF_SCOPE and no production job, role, trust, or call contributes to PASS."
  artifacts:
    - path: "evidence/phase-474/staging-delivery.json"
      provides: "Live staging delivery smoke and controlled rollback (D-03, D-05, D-07, D-08, D-14, D-15, D-16)"
    - path: "evidence/phase-474/controlled-rollback.json"
      provides: "Live staging delivery smoke and controlled rollback (D-03, D-05, D-07, D-08, D-14, D-15, D-16)"
  key_links:
    - from: "474-80"
      to: "evidence/phase-474/staging-delivery.json"
      via: "declared dependency and source-bound verification"
      pattern: 'staging\-delivery\.json'
---

<objective>
Live staging delivery smoke and controlled rollback (D-03, D-05, D-07, D-08, D-14, D-15, D-16).

Purpose: Prove the complete immutable set in live staging and the exact compensating action used for a controlled staging smoke failure.
Output: `evidence/phase-474/staging-delivery.json`, `evidence/phase-474/controlled-rollback.json`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `evidence/phase-474/staging-delivery.json` — Live staging delivery smoke and controlled rollback (D-03, D-05, D-07, D-08, D-14, D-15, D-16)
- `evidence/phase-474/controlled-rollback.json` — Live staging delivery smoke and controlled rollback (D-03, D-05, D-07, D-08, D-14, D-15, D-16)

## V9QUAL mapping

| Requirement | Plan evidence |
|---|---|
| V9QUAL-01..05 | The canonical exact-source formal gate remains the sole admission path before any staging job receives authority. |
| V9QUAL-06 | One build-once set is deployed, smoke-read, and restored through three distinct roles sharing the one `staging` Environment; production is deferred. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deploy smoke and roll back one immutable staging release</name>
  <files>evidence/phase-474/staging-delivery.json, evidence/phase-474/controlled-rollback.json</files>
  <behavior>
    - Green exact candidate deploys through StoaStagingDeployRole and smokes through StoaStagingReadRole.
    - An injected staging smoke failure invokes StoaStagingRollbackRole and restores both recorded previous pointers.
    - Missing gate, shared staging Environment, exact role trust/policy, identity, provider authority, smoke, or complete restoration blocks.
    - Any production control-plane node or provider target fails the contract rather than becoming an unrun obligation.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - docs/security/phase-474-workflow-policy.json
    - .github/workflows/deploy.yml
    - evidence/phase-474/environment-baseline-resolution.json
    - evidence/phase-474/protected-environments-resolution.json
    - evidence/phase-474/staging-substrate.json
    - evidence/phase-474/owner-staging-verification.json
    - evidence/phase-474/staging-delivery.json
    - evidence/phase-474/controlled-rollback.json
  </read_first>
  <action>Per D-03/D-05/D-06/D-07/D-08/D-14/D-15/D-16, first validate the Plan 474-80 owner receipt and its Plan 474-79/34 digests, then prove `.github/workflows/deploy.yml` contains dependency-closed `staging_deploy`, `staging_smoke`, and `staging_rollback` jobs that all declare `environment: staging` and name only `StoaStagingDeployRole`, `StoaStagingReadRole`, and `StoaStagingRollbackRole` respectively. From the three exact source receipts run the full two-clock/backend/Web/type/dependency/provenance gate, build each artifact once, and dispatch or execute the staging DAG against the provider-verified substrate. DeployRole may publish the manifest-bound Lambda versions, upload immutable Web/evidence bytes, and switch only staging aliases plus `/served-release.json`; persist exact previous and target alias/version and S3 VersionId/digest coordinates before transition. ReadRole performs read-only release identity/health/auth, real Web boot/API/runtime-config smoke, and provider readback. After a successful normal smoke, run the required controlled exercise by injecting a bounded staging smoke mismatch after both pointer updates; require the rollback job to assume only RollbackRole, consume the durable previous coordinates, restore API/weekly staging aliases plus Web descriptor/runtime-config VersionIds, then have ReadRole re-read and rehash every restored identity. Retain run/request/provider IDs, failed release identity, health result, rollback action/result, role session identities, and 90-day evidence across `staging-delivery.json` and `controlled-rollback.json`. No rebuild, out-of-band pointer edit, alternate Environment, extra role, or production target may contribute to PASS. Both receipts record `scope.production_release_control_plane=DEFERRED_OUT_OF_SCOPE`.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_delivery.py tests/test_delivery_workflow_contract.py -k "staging or smoke or compensat or rollback or restored or environment or role" &amp;&amp; .venv/bin/python scripts/release_gate.py verify --backend-ref evidence/phase-474/backend-source-ref.json --frontend-ref evidence/phase-474/frontend-source-ref.json --infra-ref evidence/phase-474/infra-source-ref.json --staging-substrate evidence/phase-474/staging-substrate.json --receipt-root evidence/phase-474 &amp;&amp; jq -e '"'"'.status == "PASS" and .environment == "staging" and .roles.deploy == "StoaStagingDeployRole" and .roles.read == "StoaStagingReadRole" and .scope.production_release_control_plane == "DEFERRED_OUT_OF_SCOPE"'"'"' evidence/phase-474/staging-delivery.json >/dev/null &amp;&amp; jq -e '"'"'.status == "ROLLED_BACK" and .environment == "staging" and .role == "StoaStagingRollbackRole" and .restored.backend == true and .restored.web == true and .scope.production_release_control_plane == "DEFERRED_OUT_OF_SCOPE"'"'"' evidence/phase-474/controlled-rollback.json >/dev/null'</automated>
  </verify>
  <acceptance>
    - One immutable backend/Web set passes every gate and live staging smoke.
    - The controlled failure restores both halves through the rollback role, and ReadRole proves exact restored identities.
    - No demo/intercepted acceptance, rebuilt artifact, alternate Environment/role, or production control plane contributes to PASS.
  </acceptance>
  <done>One immutable release deploys, smokes, and completes an exact two-pointer controlled rollback through the three least-privilege staging roles sharing one Environment; production is DEFERRED_OUT_OF_SCOPE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS V1-V14 Applicability

| ASVS | Control family | Disposition | Plan control |
|---|---|---|---|
| V1 | Architecture | apply | Keep the plan dependency-closed, source-bound, and independently verifiable. |
| V2 | Authentication | preserve | Do not introduce an alternate identity mechanism; smoke uses existing bounded auth. |
| V3 | Session Management | preserve | Do not broaden session lifetime or expose browser/session material in evidence. |
| V4 | Access Control | apply | Use least privilege and protected environment/OIDC boundaries before authority. |
| V5 | Validation | apply | Validate paths, refs, schemas, provider values, counts, and closed outcomes. |
| V6 | Cryptography | apply | Use SHA-256/AWS-managed encryption only; reject digest mismatch. |
| V7 | Error/Logging | apply | Emit stable redacted errors and identifiers without credentials or private payloads. |
| V8 | Data Protection | apply | Retain only required hashes, IDs, statuses, and policy evidence. |
| V9 | Communications | apply | Deny ambient network and constrain provider calls to reviewed endpoints/roles. |
| V10 | Malicious Code | apply | Use audited locks, immutable action SHAs, and tamper tests. |
| V11 | Business Logic | apply | Fail closed on missing prerequisites, stale identity, or partial outcome. |
| V12 | Files/Resources | apply | Use bounded paths, immutable blobs/objects, and resource limits. |
| V13 | API/Web Services | apply | Validate structured API/provider responses and preserve authorization semantics. |
| V14 | Configuration | apply | Use reviewed staging-only configuration with explicit production `DEFERRED_OUT_OF_SCOPE` state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-35-S | Spoofing | source/provider/role identity | high | mitigate/block | Require exact source, staging Environment, role session, account, region, and resource identity. |
| T-474-35-T | Tampering | staging deploy/smoke/rollback | high | mitigate/block | Bind artifacts, previous/target coordinates, commands, and provider readbacks; reject drift. |
| T-474-35-R | Repudiation | delivery and rollback receipts | high | mitigate/block | Retain canonical run/request/role/provider identities and digests. |
| T-474-35-I | Information Disclosure | diagnostics/evidence | medium | mitigate/block | Store redacted IDs, hashes, counts, and statuses only. |
| T-474-35-D | Denial of Service | bounded smoke/rollback | medium | mitigate/block | Bound exercise targets and fail closed without partial PASS. |
| T-474-35-E | Elevation of Privilege | three-role boundary | critical | mitigate/block | Enforce disjoint deploy/read/rollback roles sharing one exact staging subject and reject extra targets. |
| T-474-35-SC | Tampering | package/action supply chain | high | mitigate/block | Use only RESEARCH-approved packages and immutable reviewed Action SHAs; [SLOP] is forbidden. |
</threat_model>

<verification>
  <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_delivery.py tests/test_delivery_workflow_contract.py -k "staging or smoke or compensat or rollback or restored or environment or role" &amp;&amp; .venv/bin/python scripts/release_gate.py verify --backend-ref evidence/phase-474/backend-source-ref.json --frontend-ref evidence/phase-474/frontend-source-ref.json --infra-ref evidence/phase-474/infra-source-ref.json --staging-substrate evidence/phase-474/staging-substrate.json --receipt-root evidence/phase-474 &amp;&amp; jq -e '"'"'.status == "PASS" and .scope.production_release_control_plane == "DEFERRED_OUT_OF_SCOPE"'"'"' evidence/phase-474/staging-delivery.json >/dev/null &amp;&amp; jq -e '"'"'.status == "ROLLED_BACK" and .restored.backend == true and .restored.web == true and .scope.production_release_control_plane == "DEFERRED_OUT_OF_SCOPE"'"'"' evidence/phase-474/controlled-rollback.json >/dev/null'</automated>
</verification>

<success_criteria>
- The same build-once set passes live staging backend/Web smoke.
- A controlled staging failure automatically restores both previous verified pointers through the rollback role.
- Every task command is green and its evidence is source-bound.
- Production GitHub/AWS release-control work is `DEFERRED_OUT_OF_SCOPE` to a separate future phase or milestone.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-35-SUMMARY.md` when done.
</output>
