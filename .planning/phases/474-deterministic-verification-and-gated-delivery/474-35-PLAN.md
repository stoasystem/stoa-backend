---
phase: 474-deterministic-verification-and-gated-delivery
plan: 35
type: execute
wave: 19
depends_on: [474-29, 474-30, 474-31, 474-34]
files_modified:
  - "evidence/phase-474/controlled-rollback.json"
  - "evidence/phase-474/staging-delivery.json"
autonomous: true
requirements: [V9QUAL-01, V9QUAL-02, V9QUAL-03, V9QUAL-04, V9QUAL-05, V9QUAL-06]
must_haves:
  truths:
    - "The same build-once set passes live staging backend/Web smoke."
    - "A controlled nonproduction failure automatically restores both previous verified pointers."
  artifacts:
    - path: "evidence/phase-474/controlled-rollback.json"
      provides: "Live staging delivery smoke and controlled rollback"
    - path: "evidence/phase-474/staging-delivery.json"
      provides: "Live staging delivery smoke and controlled rollback"
  key_links:
    - from: "474-34"
      to: "evidence/phase-474/controlled-rollback.json"
      via: "declared dependency and immutable receipt"
      pattern: "controlled-rollback\.json"
---

<objective>
Live staging delivery smoke and controlled rollback (D-03, D-05, D-07, D-08, D-14, D-15, D-16).

Purpose: Prove the complete immutable set in live staging and the exact compensating action used for failed production smoke.
Output: `evidence/phase-474/controlled-rollback.json`, `evidence/phase-474/staging-delivery.json`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `evidence/phase-474/controlled-rollback.json` — Live staging delivery smoke and controlled rollback.
- `evidence/phase-474/staging-delivery.json` — Live staging delivery smoke and controlled rollback.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run the complete gate build once and prove live staging smoke</name>
  <files>evidence/phase-474/staging-delivery.json</files>
  <behavior>
    - Green exact candidate deploys and smokes in staging.
    - Missing gate, identity, provider authority, or smoke blocks.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
  </read_first>
  <action>From the three exact source receipts run the full two-clock/backend/Web/type/dependency/provenance gate, build each artifact once, upload immutable bytes, deploy the same manifest to the provider-verified staging substrate, and smoke backend release identity/health/auth plus real Web boot/API/runtime-config without interception. Retain run/request/provider IDs and read-back digests. Required staging authority or smoke cannot be NOT RUN; production remains exact NOT RUN per D-03/D-05/D-14. This task implements D-03, D-05, D-07, D-08, D-14, D-15, D-16.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python scripts/release_gate.py verify --backend-ref evidence/phase-474/backend-source-ref.json --frontend-ref evidence/phase-474/frontend-source-ref.json --infra-ref evidence/phase-474/infra-source-ref.json --staging-substrate evidence/phase-474/staging-substrate.json --receipt-root evidence/phase-474 &amp;&amp; jq -e '"'"'.status == "PASS" and .production_deploy == "NOT RUN"'"'"' evidence/phase-474/staging-delivery.json >/dev/null'</automated>
  </verify>
  <acceptance>
    - One immutable backend/Web set passes every gate and live staging smoke.
    - No demo/intercepted acceptance or rebuilt artifact contributes to PASS.
  </acceptance>
  <done>One immutable backend/Web set passes every gate and live staging smoke. No demo/intercepted acceptance or rebuilt artifact contributes to PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise controlled failed smoke and two-pointer compensation</name>
  <files>evidence/phase-474/controlled-rollback.json</files>
  <behavior>
    - Injected staging smoke failure triggers automatic two-pointer rollback.
    - Partial/mismatched restoration fails and remains visible.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
  </read_first>
  <action>Through the same staging role and immutable set, persist previous/target state, intentionally mismatch the bounded staging smoke after both pointer updates, and prove automatic compensation restores API/weekly aliases plus Web index/runtime-config VersionIds. Read back and rehash restored identities; retain failed release/run/request/health/action/result evidence and 90-day artifacts. Do not tamper out of band or call production; all production obligations remain exact NOT RUN per D-07/D-08/D-15/D-16. This task implements D-03, D-05, D-07, D-08, D-14, D-15, D-16.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m json.tool evidence/phase-474/controlled-rollback.json >/dev/null &amp;&amp; .venv/bin/python -m pytest -q tests/test_release_delivery.py -k "compensat or rollback or restored" &amp;&amp; jq -e '"'"'.status == "ROLLED_BACK" and .production_rollback == "NOT RUN"'"'"' evidence/phase-474/controlled-rollback.json >/dev/null'</automated>
  </verify>
  <acceptance>
    - The controlled staging failure restores both halves to the prior verified set.
    - Failure and compensation evidence is durable and production calls remain zero.
  </acceptance>
  <done>The controlled staging failure restores both halves to the prior verified set. Failure and compensation evidence is durable and production calls remain zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS / STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-35-01 | High | Tampering / Repudiation | Live staging delivery smoke and controlled rollback | mitigate/block | Bind exact source, inputs, command, result, and artifact identities; reject missing, duplicate, unknown, dirty, or mismatched values. |
| T-474-35-02 | High | Spoofing / Elevation | verification-to-delivery boundary | mitigate/block | Apply least privilege, protected environment checks, and dependency-closed eligibility before authority is exposed. |
| T-474-SC | High | Tampering | audited package installs | mitigate/block | Use only packages marked OK in RESEARCH.md Package Legitimacy Audit; [ASSUMED]/[SUS] requires a blocking human checkpoint and [SLOP] is forbidden. |
</threat_model>

<verification>
<automated>bash -lc '.venv/bin/python -m json.tool evidence/phase-474/controlled-rollback.json >/dev/null &amp;&amp; .venv/bin/python -m pytest -q tests/test_release_delivery.py -k "compensat or rollback or restored" &amp;&amp; jq -e '"'"'.status == "ROLLED_BACK" and .production_rollback == "NOT RUN"'"'"' evidence/phase-474/controlled-rollback.json >/dev/null'</automated>
</verification>

<success_criteria>
- The same build-once set passes live staging backend/Web smoke.
- A controlled nonproduction failure automatically restores both previous verified pointers.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless a later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-35-SUMMARY.md` when done.
</output>
