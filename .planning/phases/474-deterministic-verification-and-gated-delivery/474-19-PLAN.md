---
phase: 474-deterministic-verification-and-gated-delivery
plan: 19
type: execute
wave: 5
depends_on: [474-05, 474-18]
files_modified:
  - /Users/zhdeng/stoa-infra/app.py
  - /Users/zhdeng/stoa-infra/stacks/api_stack.py
  - /Users/zhdeng/stoa-infra/stacks/frontend_stack.py
  - /Users/zhdeng/stoa-infra/stacks/lambda_dist_guard.py
  - /Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py
  - /Users/zhdeng/stoa-infra/tests/test_release_topology.py
autonomous: true
requirements: [V9QUAL-06]
must_haves:
  truths:
    - "Both backend handlers have immutable versions and served staging/production aliases"
    - "Web assets use immutable release prefixes and versioned served index/runtime-config pointer keys"
    - "The stale Lambda dist bypass and destructive Web root behavior are absent"
    - "CDK synth/diff can prove no unintended existing-resource replacement before deployment"
  artifacts:
    - path: /Users/zhdeng/stoa-infra/stacks/api_stack.py
      provides: "Lambda published versions, aliases, and alias-backed API integrations"
    - path: /Users/zhdeng/stoa-infra/stacks/frontend_stack.py
      provides: "Versioned immutable Web releases and actually served atomic pointer keys"
    - path: /Users/zhdeng/stoa-infra/app.py
      provides: "Release stack composition and cross-stack outputs"
  key_links:
    - from: /Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py
      to: /Users/zhdeng/stoa-infra/stacks/api_stack.py
      via: "scoped alias/version ARNs"
    - from: /Users/zhdeng/stoa-infra/stacks/frontend_stack.py
      to: CloudFront
      via: "stable versioned `index.html` and `runtime-config.json` actually served"
---

<objective>
Add the minimum served Lambda alias and Web pointer topology and remove release bypasses.

Purpose: Ensure staging/production/rollback mutate only bounded pointers to immutable bytes, not unqualified functions or mutable Web roots.
Output: Version/alias APIs, versioned Web hosting/pointers, release stack wiring, no stale-dist override, and complete CDK assertions.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-18-SUMMARY.md
@/Users/zhdeng/stoa-infra/app.py
@/Users/zhdeng/stoa-infra/stacks/api_stack.py
@/Users/zhdeng/stoa-infra/stacks/frontend_stack.py
@/Users/zhdeng/stoa-infra/stacks/lambda_dist_guard.py
@/Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py
@/Users/zhdeng/stoa-infra/tests/test_release_topology.py
</context>

## Artifacts this phase produces

- New `ApiStack` properties `api_version`, `weekly_report_version`, `api_staging_alias`, `api_production_alias`, `weekly_staging_alias`, `weekly_production_alias`, and `staging_api_url`.
- Alias-backed `HttpLambdaIntegration` for staging and production plus alias-backed scheduled weekly report invocation; integrations never target `$LATEST`/unqualified functions.
- Removed `GithubBackendLambdaUpdatePolicy` direct-update authority.
- `FrontendStack` changes: versioned/retained private bucket, immutable `releases/<release-id>/` namespace contract, stable served `index.html` and `runtime-config.json` pointer keys with caching disabled, bounded invalidation paths, and outputs `WebBucketName`, `WebDistributionId`, `WebIndexPointerKey`, `WebRuntimeConfigPointerKey`.
- Removed `ALLOW_STALE_LAMBDA_DIST` branch; `verify_lambda_dist` always verifies exact 64-hex asset hash and manifest.
- `ReleaseDeliveryStack` inputs/permissions bound to exact alias ARNs, pointer keys, bucket/distribution, and transaction/evidence resources.
- `app.py` instantiation passing existing functions/buckets/distribution and preserving resource logical IDs.
- Expanded topology tests for versions/aliases, alias-backed integration/schedule, versioned retained Web bucket, served pointer/cache semantics, no `$LATEST`, no direct update policy, no destructive removal, and no stale bypass.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| immutable artifact -> Lambda version/alias | Wrong digest or unqualified update can substitute code. |
| immutable Web prefix -> served pointer/CloudFront | A passive pointer or mutable root can create split identity/races. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-19-01 | Critical | Tampering | Lambda code routing | mitigate/block | Published versions, CodeSha256/revision preconditions, alias-only integrations. |
| T-474-19-02 | Critical | Tampering | Web pointer | mitigate/block | Immutable prefix plus versioned actually served keys, preconditions, bounded invalidation. |
| T-474-19-03 | High | Elevation of privilege | stale/direct bypass | mitigate/block | Delete direct update policy and `ALLOW_STALE_LAMBDA_DIST`; assertion/source tests. |
| T-474-19-04 | High | Denial of service | CDK replacement | mitigate/block | Preserve logical IDs, require read-only inventory/diff before deployment, block replacements. |
| T-474-19-SC | High | Tampering | package installs | mitigate/block | No new package. |
</threat_model>

<tasks>

<task type="auto" id="474-19-01" tdd="true">
  <name>Publish backend versions and route staging/production through aliases</name>
  <files>/Users/zhdeng/stoa-infra/stacks/api_stack.py, /Users/zhdeng/stoa-infra/tests/test_release_topology.py</files>
  <read_first>
    - /Users/zhdeng/stoa-infra/stacks/api_stack.py
    - /Users/zhdeng/stoa-infra/tests/test_release_topology.py
    - /Users/zhdeng/stoa-infra/stacks/lambda_dist_guard.py
    - scripts/build_lambda_dist.py
  </read_first>
  <action>Create published versions and `staging`/`production` aliases for both functions from the exact verified asset. Add a staging HTTP API/integration and update the existing production integration/scheduler to aliases without replacing existing functions/gateway where avoidable. Export exact aliases/URL. Delete the direct `GithubBackendLambdaUpdatePolicy`. Tests assert alias targets are numbered versions, integrations/scheduler use aliases, both handler code digests originate from one asset, and no `UpdateFunctionCode` release policy remains.</action>
  <verify><automated>bash -lc 'cd /Users/zhdeng/stoa-infra && uv run pytest -q tests/test_release_topology.py -k "lambda or alias or api or schedule"'</automated></verify>
  <acceptance_criteria>
    - Both handlers expose staging/production aliases to numbered versions.
    - Served integrations/schedule use aliases, never `$LATEST`/unqualified functions.
    - Direct-update IAM policy is absent.
  </acceptance_criteria>
  <done>Backend routing is versioned and pointer-only.</done>
</task>

<task type="auto" id="474-19-02" tdd="true">
  <name>Make immutable Web prefixes and versioned served pointers deployable</name>
  <files>/Users/zhdeng/stoa-infra/stacks/frontend_stack.py, /Users/zhdeng/stoa-infra/tests/test_release_topology.py</files>
  <read_first>
    - /Users/zhdeng/stoa-infra/stacks/frontend_stack.py
    - /Users/zhdeng/stoa-infra/tests/test_release_topology.py
    - /Users/zhdeng/stoa-frontend/.github/workflows/deploy.yml
    - /Users/zhdeng/stoa-frontend/public/runtime-config.json.template
  </read_first>
  <action>Change the Web bucket to versioned, retained, non-destructive ownership while preserving the physical bucket/distribution. Define immutable release-prefix and stable served `index.html`/`runtime-config.json` pointer contracts; CloudFront must actually serve those keys with caching disabled while hashed assets stay immutable. Restrict writes by release roles to prefixes/pointer keys, require VersionId/digest preconditions in coordinator, and bound invalidations to `/index.html` and `/runtime-config.json`. Remove `DESTROY`, auto-delete, shared-root replacement assumptions, and any `sync --delete` policy surface.</action>
  <verify><automated>bash -lc 'cd /Users/zhdeng/stoa-infra && uv run pytest -q tests/test_release_topology.py -k "web or cloudfront or pointer or bucket"'</automated></verify>
  <acceptance_criteria>
    - Web bucket is private/versioned/retained and immutable release prefixes are explicit.
    - Stable pointer keys are actually served and cache-disabled; rollback VersionIds are possible.
    - No destructive root or passive-unused pointer design remains.
  </acceptance_criteria>
  <done>Web promotion can atomically switch bounded served keys to immutable build bytes.</done>
</task>

<task type="auto" id="474-19-03" tdd="true">
  <name>Wire release topology and remove the stale-dist bypass</name>
  <files>/Users/zhdeng/stoa-infra/app.py, /Users/zhdeng/stoa-infra/stacks/lambda_dist_guard.py, /Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py, /Users/zhdeng/stoa-infra/tests/test_release_topology.py</files>
  <read_first>
    - /Users/zhdeng/stoa-infra/app.py
    - /Users/zhdeng/stoa-infra/stacks/lambda_dist_guard.py
    - /Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py
    - /Users/zhdeng/stoa-infra/tests/test_release_topology.py
    - /Users/zhdeng/stoa-infra/stacks/api_stack.py
    - /Users/zhdeng/stoa-infra/stacks/frontend_stack.py
  </read_first>
  <action>Instantiate `ReleaseDeliveryStack` with existing/new exact resources and outputs; do not discover names manually. Remove the `ALLOW_STALE_LAMBDA_DIST` branch/message and always execute backend manifest verification with a validated 64-hex asset hash. Expand assertions/source test so bypass env/source, wildcard resources, unqualified Lambda, destructive Web storage, and missing outputs fail. Run synth and a read-only `cdk diff`; any replacement/destruction of existing production resources blocks deployment for explicit inventory resolution.</action>
  <verify><automated>bash -lc 'cd /Users/zhdeng/stoa-infra && uv run pytest -q tests/test_release_topology.py && uv run python app.py >/tmp/stoa-phase474-cdk-synth.log && ! rg "ALLOW_STALE_LAMBDA_DIST|allow-stale-lambda-dist-override" stacks app.py'</automated></verify>
  <acceptance_criteria>
    - All release resources wire through explicit constructs/outputs.
    - Stale-dist bypass strings/behavior are absent.
    - Synth passes; deployment is blocked on any unresolved replacement from live inventory/diff.
  </acceptance_criteria>
  <done>The minimum served topology is synthesized, tested, and bypass-free.</done>
</task>

</tasks>

<verification>
Run complete CDK assertions/synth, source search for bypass/direct/mutable patterns, and read-only diff. Require exact outputs, no `$LATEST`, no destructive bucket replacement, and alias/pointer-only release permissions.
</verification>

<success_criteria>
- Lambda and Web both expose immutable artifacts plus bounded served pointers.
- Direct update/stale dist/mutable Web root paths are absent.
- Topology is safe to inventory/diff before staging deployment.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-19-SUMMARY.md` when done.
</output>
