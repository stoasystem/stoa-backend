---
phase: 474-deterministic-verification-and-gated-delivery
plan: 34
type: execute
wave: 19
depends_on: [474-33, 474-77, 474-78, 474-93]
files_modified:
  - "dist/.stoa-build-manifest.json"
  - "dist/lambda.zip"
  - "evidence/phase-474/staging-substrate.json"
  - "/Users/zhdeng/stoa-infra/app.py"
  - "/Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py"
  - "/Users/zhdeng/stoa-infra/tests/test_release_topology.py"
autonomous: false
requirements: [V9QUAL-06]
must_haves:
  truths:
    - "A canonical source- and lock-bound backend Lambda dist manifest exists and verifies before CDK synth or diff begins."
    - "Only an additive StoaReleaseStaging diff bound to the safe baseline, exact infra/source refs, account, region, stack, and owner-confirmed digest may be applied."
    - "StoaReleaseStaging contains only staging stores, aliases, served Web pointer, durable rollback coordinates, and exactly three least-privilege staging roles with immutable provider readback identities."
    - "Staging manifests, artifacts, failed-smoke evidence, and rollback evidence have provider-verified retention of at least 90 days; the current and most recent verified known-good rollback sets are excluded indefinitely from lifecycle and Object Lock deletion and have no automatic deletion path."
    - "All three roles trust only repo:stoasystem/stoa-backend:environment:staging while deploy, read, and rollback permissions remain disjoint."
    - "Production release infrastructure is DEFERRED_OUT_OF_SCOPE and absent from the template, diff, IAM trust, and readback."
  artifacts:
    - path: "dist/.stoa-build-manifest.json"
      provides: "Canonical backend source/lock/runtime/artifact identity required before synth"
    - path: "dist/lambda.zip"
      provides: "Build-once backend Lambda bytes bound by the canonical manifest"
    - path: "evidence/phase-474/staging-substrate.json"
      provides: "Owner-confirmed additive StoaReleaseStaging apply plus provider readback of resources, 90-day retention, and indefinite protected rollback-set exclusions (D-05, D-14, D-15)"
    - path: "/Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py"
      provides: "StoaReleaseStaging resources and exact deploy/read/rollback role separation"
    - path: "/Users/zhdeng/stoa-infra/tests/test_release_topology.py"
      provides: "CDK assertions for exact staging resources, roles, OIDC trust, 90-day retention, indefinite protected rollback-set exclusions, and absence of production control plane"
  key_links:
    - from: "evidence/phase-474/environment-baseline-resolution.json"
      to: "evidence/phase-474/staging-substrate.json"
      via: "safe baseline digest plus exact source/dist/synth/diff/confirmation/readback chain"
      pattern: 'staging\-substrate\.json'
---

<objective>
Staging-only immutable CDK substrate (D-05, D-14, D-15).

Purpose: Generate and verify the backend build-once manifest, make the CDK source describe only StoaReleaseStaging with three exact staging roles, review an additive-only exact-source diff, require owner confirmation of its canonical digest, and then apply/read back only that staging substrate.
Output: the canonical backend dist, staging-only infra source/tests, and `evidence/phase-474/staging-substrate.json`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `dist/.stoa-build-manifest.json` — Canonical backend build identity generated and verified before synth.
- `dist/lambda.zip` — Immutable build-once backend Lambda bytes.
- `evidence/phase-474/staging-substrate.json` — Confirmation-digest-bound staging apply/readback receipt.
- `/Users/zhdeng/stoa-infra/app.py` — Exact `StoaReleaseStaging` stack wiring only.
- `/Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py` — Dedicated staging stores/pointer/aliases/coordinates and three exact staging roles.
- `/Users/zhdeng/stoa-infra/tests/test_release_topology.py` — Exact-resource, least-privilege, exact-subject, and no-production assertions.

## V9QUAL mapping

| Requirement | Plan evidence |
|---|---|
| V9QUAL-06 | The staging-only versioned release substrate is built from exact source, contains exactly three shared-subject least-privilege roles, is reviewed as an additive-only diff, explicitly owner-confirmed, applied once, and provider-read back while production remains DEFERRED_OUT_OF_SCOPE. |

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Build diff confirm apply and read back StoaReleaseStaging</name>
  <precondition>The Plan 474-33 baseline verifies as PASS with READY or READY_TO_BOOTSTRAP, the exact source-ref receipts validate, and `aws sts get-caller-identity --profile stoa` returns account `562923011260` for region `eu-central-2`.</precondition>
  <files>dist/.stoa-build-manifest.json, dist/lambda.zip, /Users/zhdeng/stoa-infra/app.py, /Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py, /Users/zhdeng/stoa-infra/tests/test_release_topology.py, evidence/phase-474/staging-substrate.json</files>
  <behavior>
    - The canonical backend dist manifest verifies before synth and binds exact source, uv.lock, runtime, handler inventory, artifact bytes, and clean-state identity.
    - A canonical PENDING receipt exposes one confirmation digest over baseline, source refs, manifest/ZIP, synthesized template, exact additive diff, account, region, and stack.
    - Only an owner response containing that exact digest permits staging apply and provider readback.
    - The synthesized template has exactly `StoaStagingDeployRole`, `StoaStagingReadRole`, and `StoaStagingRollbackRole`; each exact trust subject is staging and permissions are separated by responsibility.
    - The synthesized template and provider readback prove ordinary staging/failed evidence retention is at least 90 days and that the current plus most recent verified known-good rollback sets are indefinitely excluded from lifecycle/Object Lock deletion with no automatic deletion path.
    - Unsafe diff, drift, missing authority, wrong digest, extra role/trust, or any production target blocks without apply.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - docs/security/phase-474-workflow-policy.json
    - evidence/phase-474/final-source-handoff.json
    - evidence/phase-474/474-76-source-ref-resolution.json
    - evidence/phase-474/frontend-source-ref.json
    - evidence/phase-474/infra-source-ref.json
    - evidence/phase-474/environment-baseline-resolution.json
    - scripts/build_lambda_dist.py
    - /Users/zhdeng/stoa-infra/app.py
    - /Users/zhdeng/stoa-infra/stacks/release_delivery_stack.py
    - /Users/zhdeng/stoa-infra/tests/test_release_topology.py
  </read_first>
  <what-built>A canonical backend Lambda dist plus an exact-source additive-only `StoaReleaseStaging` synth/diff and PENDING confirmation receipt; after exact digest approval, the same task applies and provider-read backs only that staging substrate.</what-built>
  <action>Per D-05/D-06/D-14/D-15, validate the safe Plan 474-33 baseline, `final-source-handoff.json`, resolved frontend/infra source-ref receipts, clean ordinary-file worktrees, AWS account `562923011260`, region `eu-central-2`, and stack name `StoaReleaseStaging`. In the exact backend source workspace run the canonical `scripts/build_lambda_dist.py` path to create `dist/` and `dist/lambda.zip`, then run `--verify-only`; require the manifest to bind exact backend source/lock identities, Python 3.12 arm64 target, both handlers, distribution tree, ZIP SHA-256, boot smoke, and `source_git_dirty=false` before any infrastructure command. In the exact infra source, make `app.py` synthesize `StoaReleaseStaging` and make `release_delivery_stack.py` own only the staging Lambda versions/aliases or exact imported-function staging aliases, versioned staging Web objects, `/served-release.json`, staging CloudFront behavior/distribution, durable transaction/evidence stores, and previous-known-good coordinates. Configure every ordinary staging manifest, artifact, failed-smoke evidence object, and rollback evidence object for at least 90 days of retention. Represent the current release set and the most recent verified known-good rollback set as explicit protected coordinates, exclude both sets indefinitely from every lifecycle expiration rule and Object Lock deletion path, and provide no automatic deletion mechanism for either protected set; replacement of a protected coordinate may occur only after the successor is verified and the former current set becomes the new most-recent known-good set. Make `test_release_topology.py` fail unless the synthesized template proves the 90-day minimum, the two protected-set exclusions, Object Lock/versioning compatibility, and absence of any automatic delete path for protected sets. Replace the obsolete verify/upload/staging/production/rollback role set with exactly `StoaStagingDeployRole`, `StoaStagingReadRole`, and `StoaStagingRollbackRole`; every trust policy must require audience `sts.amazonaws.com` and exact subject `repo:stoasystem/stoa-backend:environment:staging`. Scope DeployRole to publishing the bound version, uploading immutable staging bytes/evidence, and moving staging aliases/pointer; scope ReadRole to provider/readback/smoke reads only; scope RollbackRole to alias/pointer restoration plus the reads needed to validate recorded previous coordinates, with controller-side digest/coordinate checks because IAM cannot constrain `UpdateAlias` to a receipt value. Remove production/ref-wide release identities and assert no production stack, alias target, IAM role, trust subject, prefix, or policy exists in this template. Run locked infra tests, `cdk synth StoaReleaseStaging`, and `cdk diff StoaReleaseStaging --no-change-set`; reject unknown resources, name collisions, Modify/Replace/Remove operations, replacement/deletion, any other stack diff, IAM broadening, retention/Object Lock/versioning weakening, or wrong source/account/region/stack. Canonicalize a PENDING receipt and confirmation digest over baseline/source/manifest/ZIP/template/diff/account/region/stack, present the complete additive resource list, and pause without a change set or provider mutation. Resume only when the owner returns the exact digest. After confirmation deploy only `StoaReleaseStaging`; independently read back every required resource, lifecycle/Object Lock/versioning configuration, protected-set exclusion, absence of automatic protected-set deletion, and the three role trusts/policies. Record these under `readback.retention.minimum_days`, `readback.retention.protected_sets`, `readback.retention.lifecycle_excluded`, `readback.retention.object_lock_delete_excluded`, and `readback.retention.automatic_delete_paths`, and write PASS only when the minimum is at least 90, the protected-set list is exactly `current` and `most_recent_verified_known_good`, both exclusion maps are true for both sets, automatic deletion paths are empty, and all provider state matches the confirmed digest. GitHub configuration and application deploy/smoke/rollback remain `NOT RUN` in this plan; production scope is `DEFERRED_OUT_OF_SCOPE`.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python scripts/build_lambda_dist.py --dist dist --verify-only &amp;&amp; (cd /Users/zhdeng/stoa-infra &amp;&amp; uv run pytest -q tests/test_release_topology.py &amp;&amp; uv run cdk synth StoaReleaseStaging &gt;/dev/null) &amp;&amp; .venv/bin/python scripts/release_environment.py verify-staging --baseline evidence/phase-474/environment-baseline-resolution.json --infra-ref evidence/phase-474/infra-source-ref.json --manifest dist/.stoa-build-manifest.json --receipt evidence/phase-474/staging-substrate.json --policy docs/security/phase-474-workflow-policy.json &amp;&amp; jq -e '"'"'.status == "PASS" and .stack.name == "StoaReleaseStaging" and .stack.account_id == "562923011260" and .stack.region == "eu-central-2" and .confirmation.status == "OWNER_CONFIRMED" and .readback.status == "PASS" and (.readback.roles | map(.name) | sort) == (["StoaStagingDeployRole","StoaStagingReadRole","StoaStagingRollbackRole"] | sort) and ([.readback.roles[].oidc_subject] | unique) == ["repo:stoasystem/stoa-backend:environment:staging"] and (.readback.retention.minimum_days &gt;= 90) and (.readback.retention.protected_sets | sort) == (["current","most_recent_verified_known_good"] | sort) and ([.readback.retention.lifecycle_excluded.current,.readback.retention.lifecycle_excluded.most_recent_verified_known_good,.readback.retention.object_lock_delete_excluded.current,.readback.retention.object_lock_delete_excluded.most_recent_verified_known_good] | all) and (.readback.retention.automatic_delete_paths | length) == 0 and .github_configuration == "NOT RUN" and .staging_deploy == "NOT RUN" and .staging_smoke == "NOT RUN" and .staging_rollback == "NOT RUN" and .scope.production_release_control_plane == "DEFERRED_OUT_OF_SCOPE"'"'"' evidence/phase-474/staging-substrate.json &gt;/dev/null'</automated>
  </verify>
  <how-to-verify>Inspect the PENDING receipt's exact account, region, stack, source-ref, backend manifest/ZIP, synthesized template, additive resource list, IAM/retention assertions, and diff SHA-256. Confirm ordinary staging and failed evidence is retained for at least 90 days, and confirm `current` plus `most_recent_verified_known_good` are indefinitely excluded from lifecycle/Object Lock deletion with zero automatic deletion paths. Reply `approve &lt;confirmation_sha256&gt;` only when every value matches and there is no replacement, deletion, unknown resource, IAM broadening, retention weakening, or production target. After apply, inspect the provider readback bound to the same digest.</how-to-verify>
  <resume-signal>Type `approve &lt;confirmation_sha256&gt;` for the exact displayed digest, or describe the discrepancy; any other approval leaves apply NOT RUN.</resume-signal>
  <acceptance>
    - The backend dist manifest is generated and verified before synth, and the confirmed diff is additive-only and exact-source/account/region/stack-bound.
    - StoaReleaseStaging is applied only after exact digest confirmation and every mandatory staging release/read/rollback resource is provider-read back.
    - Exactly three staging roles share the exact staging OIDC subject but have disjoint resource-scoped policies.
    - Provider readback proves a minimum retention of 90 days and exactly two indefinitely protected rollback sets, `current` and `most_recent_verified_known_good`, with lifecycle and Object Lock deletion exclusions and no automatic deletion path.
    - No GitHub policy configuration or staging application deploy/smoke/rollback occurs in this plan; production is absent and DEFERRED_OUT_OF_SCOPE.
  </acceptance>
  <done>The owner-confirmed additive StoaReleaseStaging substrate, exact 90-day minimum retention, indefinite protection of the current and most recent verified known-good rollback sets, absence of their automatic deletion paths, and exactly three staging roles are provider-verified against the canonical backend manifest and exact source baseline; application actions remain NOT RUN and production is DEFERRED_OUT_OF_SCOPE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS V1-V14 Applicability

| ASVS | Control family | Disposition | Plan control |
|---|---|---|---|
| V1 | Architecture | apply | Keep the plan dependency-closed, source-bound, and independently verifiable. |
| V2 | Authentication | preserve | Do not introduce an alternate identity mechanism; smoke uses existing bounded auth. |
| V3 | Session Management | preserve | Do not broaden session lifetime or expose browser/session material in evidence. |
| V4 | Access Control | apply | Use least privilege and protected environment/OIDC boundaries before authority. |
| V5 | Validation | apply | Validate paths, refs, schemas, provider values, counts, and closed outcomes. |
| V6 | Cryptography | apply | Use SHA-256/AWS-managed encryption only; reject digest mismatch. |
| V7 | Error/Logging | apply | Emit stable redacted errors and identifiers without credentials or private payloads. |
| V8 | Data Protection | apply | Retain required evidence for at least 90 days and exclude current plus most-recent verified known-good rollback sets indefinitely from automatic lifecycle/Object Lock deletion. |
| V9 | Communications | apply | Deny ambient network and constrain provider calls to reviewed endpoints/roles. |
| V10 | Malicious Code | apply | Use audited locks, immutable action SHAs, and tamper tests. |
| V11 | Business Logic | apply | Fail closed on missing prerequisites, stale identity, or partial outcome. |
| V12 | Files/Resources | apply | Use bounded paths, immutable blobs/objects, and resource limits. |
| V13 | API/Web Services | apply | Validate structured API/provider responses and preserve authorization semantics. |
| V14 | Configuration | apply | Use reviewed staging-only configuration with explicit production `DEFERRED_OUT_OF_SCOPE` state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-34-S | Spoofing | source/AWS/account identity | high | mitigate/block | Require exact source refs, manifest, account, region, stack, and authenticated AWS identity. |
| T-474-34-T | Tampering | manifest/diff/confirmation/readback chain | critical | mitigate/block | Bind canonical digests end to end and reject mismatch, drift, replacement, deletion, or unknown fields. |
| T-474-34-R | Repudiation | owner confirmation and provider receipt | high | mitigate/block | Retain ordinary evidence at least 90 days; retain exact confirmation identity and provider hashes; indefinitely exclude current and most-recent verified known-good rollback sets from automatic deletion. |
| T-474-34-I | Information Disclosure | diagnostics/evidence | medium | mitigate/block | Store redacted IDs, hashes, counts, and statuses only. |
| T-474-34-D | Denial of Service | bounded staging execution | medium | mitigate/block | Scope synth/diff/deploy/readback to one staging stack and fail closed without partial PASS. |
| T-474-34-E | Elevation of Privilege | staging role boundary | critical | mitigate/block | Require three exact-subject roles with disjoint deploy/read/rollback policies and reject every extra identity or target. |
| T-474-34-SC | Tampering | package/action supply chain | high | mitigate/block | Use locked source-approved tooling and immutable reviewed Action SHAs; [SLOP] is forbidden. |
</threat_model>

<verification>
  <automated>bash -lc '.venv/bin/python scripts/build_lambda_dist.py --dist dist --verify-only &amp;&amp; (cd /Users/zhdeng/stoa-infra &amp;&amp; uv run pytest -q tests/test_release_topology.py) &amp;&amp; .venv/bin/python scripts/release_environment.py verify-staging --baseline evidence/phase-474/environment-baseline-resolution.json --infra-ref evidence/phase-474/infra-source-ref.json --manifest dist/.stoa-build-manifest.json --receipt evidence/phase-474/staging-substrate.json --policy docs/security/phase-474-workflow-policy.json &amp;&amp; jq -e '"'"'.status == "PASS" and .confirmation.status == "OWNER_CONFIRMED" and .readback.status == "PASS" and (.readback.roles | length) == 3 and (.readback.retention.minimum_days &gt;= 90) and (.readback.retention.protected_sets | sort) == (["current","most_recent_verified_known_good"] | sort) and ([.readback.retention.lifecycle_excluded.current,.readback.retention.lifecycle_excluded.most_recent_verified_known_good,.readback.retention.object_lock_delete_excluded.current,.readback.retention.object_lock_delete_excluded.most_recent_verified_known_good] | all) and (.readback.retention.automatic_delete_paths | length) == 0 and .scope.production_release_control_plane == "DEFERRED_OUT_OF_SCOPE"'"'"' evidence/phase-474/staging-substrate.json &gt;/dev/null'</automated>
</verification>

<success_criteria>
- The canonical backend dist manifest verifies before synth and binds the exact build-once bytes.
- The applied StoaReleaseStaging diff is additive-only, owner-confirmed by exact digest, and provider-read back against exact source/account/region/stack identities.
- The staging stores, exactly three roles, aliases, served Web pointer, and rollback resources have immutable provider identities.
- Provider readback proves at least 90 days of ordinary evidence retention and indefinite automatic-deletion exclusion for the current and most recent verified known-good rollback sets.
- Production release infrastructure is absent and DEFERRED_OUT_OF_SCOPE.
- Every task command is green and its evidence is source-bound.
- Production GitHub/AWS release-control work is `DEFERRED_OUT_OF_SCOPE` to a separate future phase or milestone.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-34-SUMMARY.md` when done.
</output>
