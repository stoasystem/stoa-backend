---
phase: 474-deterministic-verification-and-gated-delivery
plan: 92
type: execute
wave: 11
depends_on: [474-91]
files_modified:
  - /Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
  - tests/test_infra_workflow_contract.py
autonomous: true
requirements: [V9QUAL-01, V9QUAL-02]
must_haves:
  truths:
    - "Infrastructure automation contains exactly one generic manual exact-ref caller of the backend-owned fixed formal aggregate."
    - "The infra workflow revision owns infra_sha and no OIDC, CDK diff/deploy, build, PR mutation, or alternate gate remains."
    - "The existing root .DS_Store remains the only approved local infra dirt exception and is not changed by this plan."
  artifacts:
    - path: /Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
      provides: "Thin manual infra formal-verification caller"
    - path: tests/test_infra_workflow_contract.py
      provides: "Closed portable infra workflow contract"
---

<objective>
Replace the unsafe infra diff/deploy workflow with one thin exact-ref caller of the fixed backend formal aggregate.

Purpose: close only the infra CI side of V9QUAL-01 while removing all provider and deployment authority from repository automation.
Output: one verification-only infra workflow and one backend-owned portable adversarial contract test. IaC source, source handoff, provider execution, production, mobile, and native remain outside this plan.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-91-SUMMARY.md
@/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace infra deployment with one exact formal caller</name>
  <files>/Users/zhdeng/stoa-infra/.github/workflows/deploy.yml, tests/test_infra_workflow_contract.py</files>
  <behavior>
    - Keep exactly one nonsymlink workflow file, manual `workflow_dispatch`, and required no-default lowercase full `backend_sha`, `frontend_sha`, and `infra_sha`; `infra_sha` must equal `github.sha`.
    - Use the exact Plan 90 action/runtime pins, sibling repository paths, no persistent credentials/caches, and exact post-checkout HEAD equality.
    - Reproduce the exact fail-closed Linux network/PID namespace preparation and private evidence-directory controls.
    - Invoke exactly backend candidate followed by formal; callers cannot select or duplicate children.
    - Reject push/PR triggers, multiple jobs, secrets/tokens, OIDC, AWS, PR comments, CDK, Node/npm, build/diff/deploy, artifacts, alternate tests, error swallowing, smoke, rollback, mobile, and native behavior.
    - Resolve the infra repository portably in local `stoa-infra` and formal snapshot `infra` layouts through a canonical project marker, rejecting zero/multiple/symlink roots.
    - Parse YAML with duplicate-key rejection and compare the complete workflow object so extra files, steps, jobs, or `if` bypasses fail locally.
  </behavior>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_infra_workflow_contract.py &amp;&amp; .venv/bin/ruff check tests/test_infra_workflow_contract.py &amp;&amp; git -C /Users/zhdeng/stoa-infra diff --check &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance>
    - Infra automation has one generic exact-ref candidate/formal runner and no direct provider or deployment path.
    - infra_sha is bound to the workflow revision; no inserted step can acquire mutation authority or weaken the gate.
    - Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` with zero calls.
  </acceptance>
  <done>The infra repository exposes only one thin exact-ref formal-verification workflow.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| Workflow executes stale infra source | `infra_sha == github.sha` plus exact checkout HEAD equality |
| Existing CDK diff/deploy authority survives | Complete one-file workflow equality and forbidden provider/deploy vocabulary |
| PR path leaks or mutates infrastructure data | Manual-only trigger, one job, read-only contents, no comments/token/OIDC |
| Local-only test path fails in formal snapshot | Canonical dual-layout project-marker resolver |
| Generic run is mistaken for approved source | Plan 93 externally machine-binds the final tuple; Plan 92 produces no admissible release evidence |
</threat_model>

<success_criteria>
- Infra dispatch requires one exact immutable three-repository source set.
- The infra-owned source SHA is inseparable from the workflow revision.
- No infra workflow builds, diffs, comments on, or mutates cloud, staging, production, mobile, or native state.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-92-SUMMARY.md` when done.
</output>
