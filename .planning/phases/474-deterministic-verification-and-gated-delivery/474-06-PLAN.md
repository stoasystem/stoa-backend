---
phase: 474-deterministic-verification-and-gated-delivery
plan: 06
type: execute
wave: 4
depends_on: [474-04]
files_modified:
  - src/stoa/security/route_authorization.py
  - src/stoa/security/admin_authorization.py
  - src/stoa/security/authorization.py
  - src/stoa/security/identity.py
  - src/stoa/security/jwks.py
  - src/stoa/security/tokens.py
  - src/stoa/security/public_auth_errors.py
  - src/stoa/security/route_inventory.py
  - src/stoa/security/reconciliation.py
autonomous: true
requirements: [V9QUAL-04]
must_haves:
  truths:
    - "Every security module in this plan has zero mypy errors under the exact full-repository command"
    - "Authorization, token, JWKS, route metadata, and reconciliation behavior remains deny-first"
    - "No broad Any, blanket ignore, module exclusion, or missing-import suppression is introduced per D-09/D-10"
  artifacts:
    - path: src/stoa/security/route_authorization.py
      provides: "Typed executable route-authorization metadata"
    - path: src/stoa/security/identity.py
      provides: "Typed issuer-subject and role identity boundary"
    - path: src/stoa/security/jwks.py
      provides: "Typed provider-key document boundary"
  key_links:
    - from: src/stoa/security/route_inventory.py
      to: src/stoa/security/route_authorization.py
      via: "closed metadata types instead of unchecked dictionaries"
---

<objective>
Repair the authorization and identity type foundation as the first full-mypy-zero slice per D-09 and D-10.

Purpose: Remove cascade-producing unchecked security/provider values without weakening the deny-first runtime contracts.
Output: Nine security modules clean under exact mypy and their focused authorization/authentication regression tests.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-04-SUMMARY.md
@pyproject.toml
@src/stoa/security/route_authorization.py
@src/stoa/security/admin_authorization.py
@src/stoa/security/authorization.py
@src/stoa/security/identity.py
@src/stoa/security/jwks.py
@src/stoa/security/tokens.py
@src/stoa/security/public_auth_errors.py
@src/stoa/security/route_inventory.py
@src/stoa/security/reconciliation.py
</context>

## Artifacts this phase produces

- New narrow types in existing security modules: `AuthorizationMetadata`, `AuthorizationDecision`, `ResolvedIdentity`, `JwksDocument`, `JwkRecord`, `VerifiedTokenClaims`, `PublicAuthFailure`, `RouteInventoryRow`, and `ReconciliationAction`.
- Narrow provider-facing `Protocol` definitions only where maintained stubs do not describe the consumed call shape.
- No new global config, baseline, ignore, exclusion, or broad `Any` surface.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| token/JWKS/provider values -> security policy | Unchecked provider dictionaries can spoof identity or widen authority. |
| route metadata -> inventory/policy | Missing typed fields can create a confused-deputy route. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-06-01 | Critical | Spoofing | identity/token/JWKS | mitigate/block | Closed TypedDict/model validation and maintained stubs/narrow Protocols. |
| T-474-06-02 | Critical | Elevation of privilege | authorization metadata | mitigate/block | Preserve deny-first union exhaustiveness and exact route regression tests. |
| T-474-06-03 | High | Tampering | mypy policy | mitigate/block | Exact scoped mypy command plus semantic guard; no ignores/Any/exclusions. |
| T-474-06-SC | High | Tampering | package installs | mitigate/block | No new package; use Plan 02 maintained stubs only. |
</threat_model>

<tasks>

<task type="auto" id="474-06-01" tdd="true">
  <name>Type executable authorization metadata and decisions</name>
  <files>src/stoa/security/route_authorization.py, src/stoa/security/admin_authorization.py, src/stoa/security/authorization.py</files>
  <read_first>
    - src/stoa/security/route_authorization.py
    - src/stoa/security/admin_authorization.py
    - src/stoa/security/authorization.py
    - tests/test_authorization_audit.py
    - tests/test_route_authorization_inventory.py
  </read_first>
  <action>Replace unchecked metadata/index/union access with the named closed types, typed constructors, exhaustive discriminants, and explicit optional handling. Preserve every capability, purpose, resource, concealment, and all-of decision. Fix the current `arg-type`, `index`, and `union-attr` errors at their source; do not cast away untrusted values or add ignores.</action>
  <verify><automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/security/route_authorization.py src/stoa/security/admin_authorization.py src/stoa/security/authorization.py && .venv/bin/python -m pytest -q tests/test_authorization_audit.py tests/test_route_authorization_inventory.py'</automated></verify>
  <acceptance_criteria>
    - The three files report zero mypy errors.
    - Focused authorization tests pass without policy/response weakening.
    - No new `Any`, `type: ignore`, unsafe cast, or unchecked dictionary fallback appears.
  </acceptance_criteria>
  <done>Authorization decisions and route metadata are statically closed and deny-first.</done>
</task>

<task type="auto" id="474-06-02" tdd="true">
  <name>Type identity, JWKS, tokens, and public authentication errors</name>
  <files>src/stoa/security/identity.py, src/stoa/security/jwks.py, src/stoa/security/tokens.py, src/stoa/security/public_auth_errors.py</files>
  <read_first>
    - src/stoa/security/identity.py
    - src/stoa/security/jwks.py
    - src/stoa/security/tokens.py
    - src/stoa/security/public_auth_errors.py
    - tests/test_identity_authorization.py
    - tests/test_public_auth_error_boundary.py
  </read_first>
  <action>Parse external JWT/JWKS/provider error values into `ResolvedIdentity`, `JwksDocument`, `JwkRecord`, `VerifiedTokenClaims`, and `PublicAuthFailure` before use. Use maintained stubs or a narrow local Protocol for only invoked provider methods. Preserve RS256 issuer/client/token-use validation, issuer-isolated cache behavior, redacted stable errors, and no email/claim privilege fallback. Resolve optional/union members explicitly.</action>
  <verify><automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/security/identity.py src/stoa/security/jwks.py src/stoa/security/tokens.py src/stoa/security/public_auth_errors.py && .venv/bin/python -m pytest -q tests/test_identity_authorization.py tests/test_public_auth_error_boundary.py'</automated></verify>
  <acceptance_criteria>
    - The four files report zero mypy errors and focused tests pass.
    - Provider data is validated/narrowed before attribute/index access.
    - Public errors remain closed and redact provider details.
  </acceptance_criteria>
  <done>Identity and authentication-provider boundaries are typed without authority broadening.</done>
</task>

<task type="auto" id="474-06-03" tdd="true">
  <name>Type route inventory and reconciliation actions</name>
  <files>src/stoa/security/route_inventory.py, src/stoa/security/reconciliation.py</files>
  <read_first>
    - src/stoa/security/route_inventory.py
    - src/stoa/security/reconciliation.py
    - src/stoa/security/route_authorization.py
    - tests/test_route_authorization_inventory.py
    - tests/test_privileged_identity_reconciliation.py
  </read_first>
  <action>Represent inventory rows and reconciliation actions with exact required fields and closed discriminants. Replace untyped annotation/container traversal, first-match dictionary assumptions, and optional mutation access with explicit helpers returning narrowed values. Preserve deterministic inventory ordering, fail-closed unknown routes, dry-run-first reconciliation, and immutable capability coordinates.</action>
  <verify><automated>bash -lc 'MYPYPATH=src:tests .venv/bin/python -m mypy --no-incremental --explicit-package-bases src/stoa/security/route_inventory.py src/stoa/security/reconciliation.py && .venv/bin/python -m pytest -q tests/test_route_authorization_inventory.py tests/test_privileged_identity_reconciliation.py; .venv/bin/ruff check src/stoa/security'</automated></verify>
  <acceptance_criteria>
    - Both files report zero mypy errors.
    - Inventory generation and reconciliation tests remain deterministic and deny-first.
    - Full security-package Ruff is zero.
  </acceptance_criteria>
  <done>The security foundation no longer contributes errors to the full mypy repair.</done>
</task>

</tasks>

<verification>
Run exact mypy on all nine owned modules, Ruff over `src/stoa/security`, and the focused authorization/identity/inventory/reconciliation suites.
</verification>

<success_criteria>
- All nine security modules are mypy zero.
- Runtime authorization/authentication semantics remain unchanged or stricter.
- D-09/D-10 forbidden suppression patterns are absent.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-06-SUMMARY.md` when done.
</output>
