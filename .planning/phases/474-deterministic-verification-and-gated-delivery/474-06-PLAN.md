---
phase: 474-deterministic-verification-and-gated-delivery
plan: 06
type: execute
wave: 3
depends_on: [474-02]
files_modified:
  - "schemas/release/release-manifest-v1.schema.json"
  - "scripts/build_lambda_dist.py"
  - "scripts/release_manifest.py"
  - "tests/test_lambda_dist_build.py"
  - "tests/test_release_manifest.py"
autonomous: true
requirements: [V9QUAL-06]
must_haves:
  truths:
    - "The manifest binds exact source, lock, runtime, gate, artifact, and config identities."
    - "The backend artifact is byte-reproducible and never rebuilt during promotion."
  artifacts:
    - path: "schemas/release/release-manifest-v1.schema.json"
      provides: "Cross-repository manifest and reproducible backend artifact"
    - path: "scripts/build_lambda_dist.py"
      provides: "Cross-repository manifest and reproducible backend artifact"
    - path: "scripts/release_manifest.py"
      provides: "Cross-repository manifest and reproducible backend artifact"
    - path: "tests/test_lambda_dist_build.py"
      provides: "Cross-repository manifest and reproducible backend artifact"
    - path: "tests/test_release_manifest.py"
      provides: "Cross-repository manifest and reproducible backend artifact"
  key_links:
    - from: "474-02"
      to: "schemas/release/release-manifest-v1.schema.json"
      via: "declared dependency and immutable receipt"
      pattern: 'release-manifest-v1\.schema\.json'
---

<objective>
Cross-repository manifest and reproducible backend artifact (D-13, D-14).

Purpose: Create the immutable cross-repository identity and reproducible backend bytes used by staging and any later promotion.
Output: `schemas/release/release-manifest-v1.schema.json`, `scripts/build_lambda_dist.py`, `scripts/release_manifest.py`, `tests/test_lambda_dist_build.py`, `tests/test_release_manifest.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `schemas/release/release-manifest-v1.schema.json` — Cross-repository manifest and reproducible backend artifact.
- `scripts/build_lambda_dist.py` — Cross-repository manifest and reproducible backend artifact.
- `scripts/release_manifest.py` — Cross-repository manifest and reproducible backend artifact.
- `tests/test_lambda_dist_build.py` — Cross-repository manifest and reproducible backend artifact.
- `tests/test_release_manifest.py` — Cross-repository manifest and reproducible backend artifact.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the closed release manifest and tamper matrix</name>
  <files>schemas/release/release-manifest-v1.schema.json, scripts/release_manifest.py, tests/test_release_manifest.py</files>
  <behavior>
    - Canonical stable inputs produce one release ID and digest.
    - Mutable or dirty identity is rejected.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
  </read_first>
  <action>Define and implement a closed canonical manifest binding execution-receipted backend/Web/infra commits, trees, locks, clean state, runtimes/platform, gate receipt/run IDs, artifact/config digests, and production exact NOT RUN. Derive a non-circular release ID from source/lock/runtime identities, then bind final bytes. Reject branches, tags, latest, research-time SHAs, unknown gates, dirty state, duplicate receipts, and any changed source/artifact/config per D-13/D-14. This task implements D-13, D-14.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_manifest.py &amp;&amp; .venv/bin/ruff check scripts/release_manifest.py tests/test_release_manifest.py'</automated>
  </verify>
  <acceptance>
    - One manifest identifies one exact three-repository candidate.
    - Any identity or byte tamper invalidates the manifest.
  </acceptance>
  <done>One manifest identifies one exact three-repository candidate. Any identity or byte tamper invalidates the manifest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build one normalized lock-bound Lambda artifact</name>
  <files>scripts/build_lambda_dist.py, tests/test_lambda_dist_build.py</files>
  <behavior>
    - Same input produces the same ZIP digest.
    - Stale or incompatible distributions fail validation.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - scripts/build_lambda_dist.py
    - tests/test_lambda_dist_build.py
  </read_first>
  <action>Extend the existing arm64 Python 3.12 builder: verify requirements.txt equals a fresh locked export, normalize ZIP order/timestamps/modes, bind source tree and handler inventory, hash final bytes, and boot-smoke imports in a compatible isolated runtime. Staging and production consume this digest; no later job rebuilds it per D-14. This task implements D-13, D-14.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_lambda_dist_build.py tests/test_release_manifest.py &amp;&amp; .venv/bin/ruff check scripts/build_lambda_dist.py tests/test_lambda_dist_build.py'</automated>
  </verify>
  <acceptance>
    - Repeated builds from the same source/lock are byte-identical.
    - Lock, source, handler, platform, archive, or boot drift fails.
  </acceptance>
  <done>Repeated builds from the same source/lock are byte-identical. Lock, source, handler, platform, archive, or boot drift fails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS / STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-06-01 | High | Tampering / Repudiation | Cross-repository manifest and reproducible backend artifact | mitigate/block | Bind exact source, inputs, command, result, and artifact identities; reject missing, duplicate, unknown, dirty, or mismatched values. |
| T-474-06-02 | High | Spoofing / Elevation | verification-to-delivery boundary | mitigate/block | Apply least privilege, protected environment checks, and dependency-closed eligibility before authority is exposed. |
| T-474-SC | High | Tampering | audited package installs | mitigate/block | Use only packages marked OK in RESEARCH.md Package Legitimacy Audit; [ASSUMED]/[SUS] requires a blocking human checkpoint and [SLOP] is forbidden. |
</threat_model>

<verification>
<automated>bash -lc '.venv/bin/python -m pytest -q tests/test_lambda_dist_build.py tests/test_release_manifest.py &amp;&amp; .venv/bin/ruff check scripts/build_lambda_dist.py tests/test_lambda_dist_build.py'</automated>
</verification>

<success_criteria>
- The manifest binds exact source, lock, runtime, gate, artifact, and config identities.
- The backend artifact is byte-reproducible and never rebuilt during promotion.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless a later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-06-SUMMARY.md` when done.
</output>
