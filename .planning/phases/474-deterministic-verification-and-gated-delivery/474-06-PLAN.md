---
phase: 474-deterministic-verification-and-gated-delivery
plan: 06
type: execute
wave: 3
depends_on: [474-02]
files_modified:
  - "schemas/release/release-manifest-v1.schema.json"
  - "scripts/release_manifest.py"
  - "tests/test_release_manifest.py"
  - "scripts/build_lambda_dist.py"
  - "tests/test_lambda_dist_build.py"
autonomous: true
requirements: [V9QUAL-06]
must_haves:
  truths:
    - "The manifest binds exact source, lock, runtime, gate, artifact, and config identities."
    - "The backend artifact is byte-reproducible and never rebuilt during promotion."
  artifacts:
    - path: "schemas/release/release-manifest-v1.schema.json"
      provides: "Cross-repository manifest and reproducible backend artifact (D-13, D-14)"
    - path: "scripts/release_manifest.py"
      provides: "Cross-repository manifest and reproducible backend artifact (D-13, D-14)"
    - path: "tests/test_release_manifest.py"
      provides: "Cross-repository manifest and reproducible backend artifact (D-13, D-14)"
    - path: "scripts/build_lambda_dist.py"
      provides: "Cross-repository manifest and reproducible backend artifact (D-13, D-14)"
    - path: "tests/test_lambda_dist_build.py"
      provides: "Cross-repository manifest and reproducible backend artifact (D-13, D-14)"
  key_links:
    - from: "474-02"
      to: "schemas/release/release-manifest-v1.schema.json"
      via: "declared dependency and source-bound verification"
      pattern: 'release\-manifest\-v1\.schema\.json'
---

<objective>
Cross-repository manifest and reproducible backend artifact (D-13, D-14).

Purpose: Create the immutable cross-repository identity and reproducible backend bytes used by staging and any later promotion.
Output: `schemas/release/release-manifest-v1.schema.json`, `scripts/release_manifest.py`, `tests/test_release_manifest.py`, `scripts/build_lambda_dist.py`, `tests/test_lambda_dist_build.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `schemas/release/release-manifest-v1.schema.json` — Cross-repository manifest and reproducible backend artifact (D-13, D-14)
- `scripts/release_manifest.py` — Cross-repository manifest and reproducible backend artifact (D-13, D-14)
- `tests/test_release_manifest.py` — Cross-repository manifest and reproducible backend artifact (D-13, D-14)
- `scripts/build_lambda_dist.py` — Cross-repository manifest and reproducible backend artifact (D-13, D-14)
- `tests/test_lambda_dist_build.py` — Cross-repository manifest and reproducible backend artifact (D-13, D-14)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the closed release manifest and tamper matrix</name>
  <files>schemas/release/release-manifest-v1.schema.json, scripts/release_manifest.py, tests/test_release_manifest.py</files>
  <behavior>
    - Canonical stable inputs produce one release ID and digest.
    - Mutable or dirty identity is rejected.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - schemas/release/release-manifest-v1.schema.json
    - scripts/release_manifest.py
    - tests/test_release_manifest.py
  </read_first>
  <action>Define and implement a closed canonical manifest binding execution-receipted backend/Web/infra commits, trees, locks, clean state, runtimes/platform, gate receipt/run IDs, artifact/config digests, and production exact NOT RUN. Derive a non-circular release ID from source/lock/runtime identities, then bind final bytes. Reject branches, tags, latest, research-time SHAs, unknown gates, dirty state, duplicate receipts, and any changed source/artifact/config per D-13/D-14. This task implements D-13, D-14.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_manifest.py &amp;&amp; .venv/bin/ruff check scripts/release_manifest.py tests/test_release_manifest.py'</automated>
  </verify>
  <acceptance>
    - One manifest identifies one exact three-repository candidate.
    - Any identity or byte tamper invalidates the manifest.
  </acceptance>
  <done>One manifest identifies one exact three-repository candidate. Any identity or byte tamper invalidates the manifest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build one normalized lock-bound Lambda artifact</name>
  <files>scripts/build_lambda_dist.py, tests/test_lambda_dist_build.py</files>
  <behavior>
    - Same input produces the same ZIP digest.
    - Stale or incompatible distributions fail validation.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - scripts/build_lambda_dist.py
    - tests/test_lambda_dist_build.py
  </read_first>
  <action>Extend the existing arm64 Python 3.12 builder: verify requirements.txt equals a fresh locked export, normalize ZIP order/timestamps/modes, bind source tree and handler inventory, hash final bytes, and boot-smoke imports in a compatible isolated runtime. Staging and production consume this digest; no later job rebuilds it per D-14. This task implements D-13, D-14.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_lambda_dist_build.py tests/test_release_manifest.py &amp;&amp; .venv/bin/ruff check scripts/build_lambda_dist.py tests/test_lambda_dist_build.py'</automated>
  </verify>
  <acceptance>
    - Repeated builds from the same source/lock are byte-identical.
    - Lock, source, handler, platform, archive, or boot drift fails.
  </acceptance>
  <done>Repeated builds from the same source/lock are byte-identical. Lock, source, handler, platform, archive, or boot drift fails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS V1-V14 Applicability

| ASVS | Control family | Disposition | Plan control |
|---|---|---|---|
| V1 | Architecture | apply | Keep the plan dependency-closed, source-bound, and independently verifiable. |
| V2 | Authentication | preserve | Do not introduce an alternate identity mechanism; smoke uses existing bounded auth. |
| V3 | Session Management | preserve | Do not broaden session lifetime or expose browser/session material in evidence. |
| V4 | Access Control | apply | Use least privilege and protected environment/OIDC boundaries before authority. |
| V5 | Validation | apply | Validate paths, refs, schemas, provider values, counts, and closed outcomes. |
| V6 | Cryptography | apply | Use SHA-256/AWS-managed encryption only; reject digest mismatch. |
| V7 | Error/Logging | apply | Emit stable redacted errors and identifiers without credentials or private payloads. |
| V8 | Data Protection | apply | Retain only required hashes, IDs, statuses, and policy evidence. |
| V9 | Communications | apply | Deny ambient network and constrain provider calls to reviewed endpoints/roles. |
| V10 | Malicious Code | apply | Use audited locks, immutable action SHAs, and tamper tests. |
| V11 | Business Logic | apply | Fail closed on missing prerequisites, stale identity, or partial outcome. |
| V12 | Files/Resources | apply | Use bounded paths, immutable blobs/objects, and resource limits. |
| V13 | API/Web Services | apply | Validate structured API/provider responses and preserve authorization semantics. |
| V14 | Configuration | apply | Use reviewed closed configuration with explicit production NOT RUN state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-474-06-S | Spoofing | source/provider identity | mitigate/block | Require exact execution-derived Git/provider identity and authorized actor/role. |
| T-474-06-T | Tampering | Cross-repository manifest and reproducible backend artifact (D-13, D-14) | mitigate/block | Bind inputs, commands, results, and artifacts; reject mismatch, drift, or unknown fields. |
| T-474-06-R | Repudiation | evidence receipt | mitigate/block | Retain canonical source-bound command/result/request identities. |
| T-474-06-I | Information Disclosure | diagnostics/evidence | mitigate/block | Store redacted IDs, hashes, counts, and statuses only. |
| T-474-06-D | Denial of Service | bounded execution | mitigate/block | Apply bounded time/resource scopes and fail closed without partial PASS. |
| T-474-06-E | Elevation of Privilege | verification-to-delivery boundary | mitigate/block | Use least privilege, dependency-closed eligibility, and protected environments. |
| T-474-06-SC | Tampering | package/action supply chain | mitigate/block | Use only RESEARCH-approved packages and immutable reviewed Action SHAs; [SLOP] is forbidden. |
</threat_model>

<verification>
  <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_manifest.py &amp;&amp; .venv/bin/ruff check scripts/release_manifest.py tests/test_release_manifest.py'</automated>
  <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_lambda_dist_build.py tests/test_release_manifest.py &amp;&amp; .venv/bin/ruff check scripts/build_lambda_dist.py tests/test_lambda_dist_build.py'</automated>
</verification>

<success_criteria>
- The manifest binds exact source, lock, runtime, gate, artifact, and config identities.
- The backend artifact is byte-reproducible and never rebuilt during promotion.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-06-SUMMARY.md` when done.
</output>
