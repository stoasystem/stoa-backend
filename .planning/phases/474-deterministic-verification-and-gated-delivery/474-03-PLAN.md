---
phase: 474-deterministic-verification-and-gated-delivery
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_phase473_evidence.py
  - tests/test_phase473_evidence_verifier.py
autonomous: true
requirements: [V9QUAL-07]
must_haves:
  truths:
    - "A later clean metadata HEAD can reverify the explicit Phase 473 publication"
    - "The publication is the candidate's single direct child and current HEAD descends from it"
    - "All four publication artifacts are read from immutable Git blobs and remain byte-identical"
  artifacts:
    - path: scripts/verify_phase473_evidence.py
      provides: "Explicit candidate/publication later-HEAD verifier"
    - path: tests/test_phase473_evidence_verifier.py
      provides: "Temporary-Git-history ancestry, path-cardinality, blob, and tamper tests"
  key_links:
    - from: scripts/verify_phase473_evidence.py
      to: docs/security/phase-473-evidence-manifest.json
      via: "git show/cat-file at explicit publication commit"
---

<objective>
Close the Phase 473 `final-head-publication-reverification` defect with immutable Git-blob and ancestry verification.

Purpose: Preserve the original four-file publication contract while making it callable from later clean metadata descendants.
Output: An explicit `--candidate/--publication` verifier and full synthetic Git-history regression matrix.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/473-student-content-privacy-and-practice-integrity/473-40-SUMMARY.md
@.planning/phases/473-student-content-privacy-and-practice-integrity/473-VERIFICATION.md
@scripts/verify_phase473_evidence.py
@tests/test_phase473_evidence_verifier.py
@docs/security/phase-473-evidence-manifest.json
</context>

## Artifacts this phase produces

- Extended CLI `verify-publication --candidate <sha> --publication <sha>`.
- New verifier symbols `git_blob_oid`, `git_blob_bytes`, `publication_parent`, `publication_changed_paths`, `is_ancestor`, and `verify_later_head_publication`.
- Exact error codes `publication_not_direct_child`, `publication_not_single_parent`, `publication_path_set_mismatch`, `head_not_descendant`, `publication_blob_missing`, `head_blob_mismatch`, and `manifest_blob_mismatch`.
- New tests for metadata-only descendants, merge publication, sideways ancestry, non-direct publication, extra/missing path, missing blob, manifest mismatch, and later artifact mutation.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI refs -> Git object database | Candidate/publication/HEAD relationships are untrusted. |
| worktree -> historical evidence | Current files may differ from immutable publication blobs. |
| manifest -> four artifacts | A manifest can reference wrong bytes or an incomplete path set. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-03-01 | High | Spoofing | publication identity | mitigate/block | Full SHAs, exactly one parent, and exact parent=candidate. |
| T-474-03-02 | Critical | Tampering | evidence blobs | mitigate/block | Read publication and HEAD with Git object commands; compare OIDs, bytes, sizes, and SHA-256. |
| T-474-03-03 | High | Repudiation | ancestry claim | mitigate/block | Require publication ancestor of current HEAD and exact four-path publication diff. |
| T-474-03-04 | High | Information disclosure | Git command errors | mitigate/block | Stable codes only; never echo artifact contents or environment values. |
| T-474-03-SC | High | Tampering | package installs | mitigate/block | No package install is planned. |

## ASVS 4.0.3 L1 Control Mapping

| ASVS L1 category | Applicability and concrete control | Verification |
|---|---|---|
| V1 Architecture | Applicable — exclusive file ownership, explicit dependencies, and fail-closed boundaries are part of this plan. | Task 03-01 automated command and this plan's focused acceptance criteria. |
| V2 Authentication | Applicable — exact identity/provider inputs remain authenticated and are exercised by focused tests. | Task 03-01 automated command and this plan's focused acceptance criteria. |
| V3 Session Management | Applicable — session/token state is closed and expiry/replay behavior remains tested. | Task 03-01 automated command and this plan's focused acceptance criteria. |
| V4 Access Control | Applicable — authorization/release authority remains deny-first; any High/Critical failure blocks completion. | Task 03-01 automated command and this plan's focused acceptance criteria. |
| V5 Validation | Applicable — untrusted records, command inputs, and result unions are narrowed before use; malformed values fail. | Task 03-01 automated command and this plan's focused acceptance criteria. |
| V6 Cryptography | Applicable — SHA-256/content identities are recomputed and never substitute for secret-key controls. | Task 03-01 automated command and this plan's focused acceptance criteria. |
| V7 Error Handling and Logging | Applicable — stable bounded errors/results are verified and private/provider payloads are not logged. | Task 03-01 automated command and this plan's focused acceptance criteria. |
| V8 Data Protection | Applicable — tests preserve redaction and prevent secret, token, student-content, or provider-payload disclosure. | Task 03-01 automated command and this plan's focused acceptance criteria. |
| V9 Communications | Applicable — real/provider/network boundaries require approved origins, denial, or immutable transport identity. | Task 03-01 automated command and this plan's focused acceptance criteria. |
| V10 Malicious Code | Applicable — no unreviewed package/action is added; the SC threat and locked dependency policy remain blocking. | Task 03-01 automated command and this plan's focused acceptance criteria. |
| V11 Business Logic | Applicable — focused tests preserve exact state, race, retry, idempotency, and fail-closed outcomes relevant to the owned files. | Task 03-01 automated command and this plan's focused acceptance criteria. |
| V12 Files and Resources | Applicable — every modified path is enumerated and artifact/evidence bytes or source files are bounded by exact ownership. | Task 03-01 automated command and this plan's focused acceptance criteria. |
| V13 API and Web Services | Applicable — request/CLI/workflow contracts are closed and verified end to end. | Task 03-01 automated command and this plan's focused acceptance criteria. |
| V14 Configuration | Applicable — exact tool/scope/configuration is verified and broad ignores, mutable refs, or permissive defaults are forbidden. | Task 03-01 automated command and this plan's focused acceptance criteria. |

Any failed applicable ASVS L1 control, or any High/Critical STRIDE threat without its named mitigation and test, blocks plan completion.
</threat_model>

<tasks>

<task type="auto" id="474-03-01" tdd="true">
  <name>Specify later-HEAD publication and Git-blob failure cases</name>
  <files>tests/test_phase473_evidence_verifier.py, scripts/verify_phase473_evidence.py</files>
  <behavior>
    - Explicit candidate plus single direct publication plus metadata-only descendant passes.
    - Merge, sideways, non-direct, extra/missing path, missing blob, changed blob, and manifest mismatch fail with stable codes.
    - The verifier never relies on publication being HEAD or reads publication truth from the worktree.
  </behavior>
  <read_first>
    - tests/test_phase473_evidence_verifier.py
    - scripts/verify_phase473_evidence.py
    - .planning/phases/473-student-content-privacy-and-practice-integrity/473-VERIFICATION.md
    - .planning/phases/473-student-content-privacy-and-practice-integrity/473-40-SUMMARY.md
    - docs/security/phase-473-evidence-manifest.json
  </read_first>
  <action>Create temporary Git repositories with an immutable candidate, one exact four-file publication, and later metadata commits. Add the positive later-HEAD case and each named negative case, asserting stable codes and no worktree-byte dependence. Preserve every existing receipt, coverage, privacy, and direct-publication test. RED must be the missing explicit publication interface, not collection failure.</action>
  <verify><automated>bash -lc 'set +e; .venv/bin/python -m pytest -q tests/test_phase473_evidence_verifier.py; rc=$?; test "$rc" -eq 1'</automated></verify>
  <acceptance_criteria>
    - Every ancestry/publication/blob mutation has an executable synthetic-Git test.
    - A metadata-only descendant is the sole new positive history shape.
    - Existing Phase 473 integrity tests remain collected.
  </acceptance_criteria>
  <done>The test suite expresses V9QUAL-07 without weakening the four-file publication boundary.</done>
</task>

<task type="auto" id="474-03-02" tdd="true">
  <name>Verify explicit publication commits from immutable blobs</name>
  <files>scripts/verify_phase473_evidence.py, tests/test_phase473_evidence_verifier.py</files>
  <behavior>
    - Git object reads and ancestry checks satisfy the complete positive/negative matrix.
    - Manifest byte counts/SHA-256 are recomputed from publication blobs.
    - HEAD equality proves later descendants did not modify any publication artifact.
  </behavior>
  <read_first>
    - scripts/verify_phase473_evidence.py
    - tests/test_phase473_evidence_verifier.py
    - docs/security/phase-473-evidence-manifest.json
  </read_first>
  <action>Add required `--publication` parsing and the named helper functions. Enforce one publication parent equal to candidate, exact four-path `diff-tree`, current HEAD ancestry, and clean worktree. For each required path, obtain publication/HEAD blob OIDs and bytes with `git cat-file`/`git show`, compare them, and recompute manifest byte/SHA-256 fields from publication bytes. Preserve candidate-bound capture semantics and all older commands. Map malformed refs and Git failures to stable codes without exposing content.</action>
  <verify><automated>bash -lc '.venv/bin/python -m pytest -q tests/test_phase473_evidence_verifier.py && .venv/bin/ruff check scripts/verify_phase473_evidence.py tests/test_phase473_evidence_verifier.py'</automated></verify>
  <acceptance_criteria>
    - Later metadata HEAD passes only with candidate `b43c71b...` and publication `5da6936...` or equivalent synthetic exact history.
    - All negative histories fail before evidence is accepted.
    - The real current clean HEAD can invoke the explicit historical publication command successfully.
  </acceptance_criteria>
  <done>Phase 473 publication integrity is reproducible from later clean metadata descendants.</done>
</task>

</tasks>

<verification>
Run the full verifier test module, targeted Ruff, and the real explicit candidate/publication command from current HEAD; confirm the exact four blobs match and no worktree content is used as publication truth.
</verification>

<success_criteria>
- V9QUAL-07 passes for a later clean metadata HEAD.
- Direct-child, single-parent, exact four-path, descendant, blob-equality, and manifest-hash rules all remain mandatory.
- Mutated, missing, merged, sideways, or worktree-substituted evidence fails closed.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-03-SUMMARY.md` when done.
</output>
