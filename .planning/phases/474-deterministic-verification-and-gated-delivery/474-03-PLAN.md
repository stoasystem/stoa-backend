---
phase: 474-deterministic-verification-and-gated-delivery
plan: 03
type: execute
wave: 3
depends_on: [474-02]
files_modified:
  - "pyproject.toml"
  - "uv.lock"
  - "requirements.txt"
  - "scripts/phase474_pytest_guard.py"
  - "tests/conftest.py"
  - "tests/test_deterministic_gate.py"
  - "scripts/release_gate.py"
autonomous: true
requirements: [V9QUAL-01, V9QUAL-02]
must_haves:
  truths:
    - "Two fresh Python 3.12 runs pass with identical collection and zero non-pass outcomes."
    - "Ambient AWS and network paths are denied at the process boundary."
  artifacts:
    - path: "pyproject.toml"
      provides: "Fresh hermetic Python verification (D-01, D-02, D-03, D-04)"
    - path: "uv.lock"
      provides: "Fresh hermetic Python verification (D-01, D-02, D-03, D-04)"
    - path: "requirements.txt"
      provides: "Fresh hermetic Python verification (D-01, D-02, D-03, D-04)"
    - path: "scripts/phase474_pytest_guard.py"
      provides: "Fresh hermetic Python verification (D-01, D-02, D-03, D-04)"
    - path: "tests/conftest.py"
      provides: "Fresh hermetic Python verification (D-01, D-02, D-03, D-04)"
    - path: "tests/test_deterministic_gate.py"
      provides: "Fresh hermetic Python verification (D-01, D-02, D-03, D-04)"
    - path: "scripts/release_gate.py"
      provides: "Fresh hermetic Python verification (D-01, D-02, D-03, D-04)"
  key_links:
    - from: "474-02"
      to: "pyproject.toml"
      via: "declared dependency and source-bound verification"
      pattern: 'pyproject\.toml'
---

<objective>
Fresh hermetic Python verification (D-01, D-02, D-03, D-04).

Purpose: Make the complete backend suite reproducible twice without ambient cloud, network, clock, or warm-environment influence.
Output: `pyproject.toml`, `uv.lock`, `requirements.txt`, `scripts/phase474_pytest_guard.py`, `tests/conftest.py`, `tests/test_deterministic_gate.py`, `scripts/release_gate.py`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `pyproject.toml` — Fresh hermetic Python verification (D-01, D-02, D-03, D-04)
- `uv.lock` — Fresh hermetic Python verification (D-01, D-02, D-03, D-04)
- `requirements.txt` — Fresh hermetic Python verification (D-01, D-02, D-03, D-04)
- `scripts/phase474_pytest_guard.py` — Fresh hermetic Python verification (D-01, D-02, D-03, D-04)
- `tests/conftest.py` — Fresh hermetic Python verification (D-01, D-02, D-03, D-04)
- `tests/test_deterministic_gate.py` — Fresh hermetic Python verification (D-01, D-02, D-03, D-04)
- `scripts/release_gate.py` — Fresh hermetic Python verification (D-01, D-02, D-03, D-04)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the approved hermetic toolchain and strict pytest guard</name>
  <files>pyproject.toml, uv.lock, requirements.txt, scripts/phase474_pytest_guard.py, tests/conftest.py, tests/test_deterministic_gate.py</files>
  <behavior>
    - Direct and subprocess network/AWS discovery attempts fail.
    - Skip, xfail, xpass, empty collection, and clock drift fail.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - pyproject.toml
    - uv.lock
    - requirements.txt
    - tests/conftest.py
    - scripts/phase474_pytest_guard.py
    - tests/test_deterministic_gate.py
  </read_first>
  <action>Use only packages approved by the research Package Legitimacy Audit. Lock exact Python 3.12 verification tooling, keep requirements.txt byte-equivalent to the frozen production export, and implement central fixed-clock/socket/AWS-denial fixtures plus strict node accounting. Record seed, clock, runtime, lock, collection SHA, and failed/error/skipped/xfail/xpass counts; empty or any non-pass collection fails per D-02/D-03/D-04. This task implements D-01, D-02, D-03, D-04.</action>
  <verify>
    <automated>bash -lc 'uv lock --check &amp;&amp; .venv/bin/python -m pytest -q tests/test_deterministic_gate.py'</automated>
  </verify>
  <acceptance>
    - The lock/export chain is deterministic and package-legitimacy approved.
    - The strict guard rejects all non-pass outcome categories and ambient access.
  </acceptance>
  <done>The lock/export chain is deterministic and package-legitimacy approved. The strict guard rejects all non-pass outcome categories and ambient access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run two distinct frozen Python 3.12 environments through the gate</name>
  <files>scripts/release_gate.py, tests/test_deterministic_gate.py</files>
  <behavior>
    - Standard and future clock runs bind the same source/lock/collection.
    - Unavailable OS isolation is exact NOT RUN, never PASS.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - scripts/release_gate.py
    - tests/test_deterministic_gate.py
  </read_first>
  <action>Create two different validated temporary UV_PROJECT_ENVIRONMENT paths, run uv sync --frozen --python 3.12 --extra dev, then execute identical complete-suite argv under an OS network-none boundary at 2026-07-01T12:00:00Z and 2035-01-15T12:00:00Z with seed 4740718. Deny credential mounts/proxies/metadata, compare collection identities, reject source/lock drift and warm environments. An incapable local host emits exact NOT RUN; CI must provide the boundary per D-02/D-04. This task implements D-01, D-02, D-03, D-04.</action>
  <verify>
    <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_deterministic_gate.py tests/test_release_gate.py &amp;&amp; .venv/bin/ruff check scripts/release_gate.py scripts/phase474_pytest_guard.py tests/conftest.py tests/test_deterministic_gate.py'</automated>
  </verify>
  <acceptance>
    - Both runs use separate fresh environments and differ only by clock.
    - Complete-suite evidence requires zero skip/xfail/xpass and identical collection.
  </acceptance>
  <done>Both runs use separate fresh environments and differ only by clock. Complete-suite evidence requires zero skip/xfail/xpass and identical collection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS V1-V14 Applicability

| ASVS | Control family | Disposition | Plan control |
|---|---|---|---|
| V1 | Architecture | apply | Keep the plan dependency-closed, source-bound, and independently verifiable. |
| V2 | Authentication | preserve | Do not introduce an alternate identity mechanism; smoke uses existing bounded auth. |
| V3 | Session Management | preserve | Do not broaden session lifetime or expose browser/session material in evidence. |
| V4 | Access Control | apply | Use least privilege and protected environment/OIDC boundaries before authority. |
| V5 | Validation | apply | Validate paths, refs, schemas, provider values, counts, and closed outcomes. |
| V6 | Cryptography | apply | Use SHA-256/AWS-managed encryption only; reject digest mismatch. |
| V7 | Error/Logging | apply | Emit stable redacted errors and identifiers without credentials or private payloads. |
| V8 | Data Protection | apply | Retain only required hashes, IDs, statuses, and policy evidence. |
| V9 | Communications | apply | Deny ambient network and constrain provider calls to reviewed endpoints/roles. |
| V10 | Malicious Code | apply | Use audited locks, immutable action SHAs, and tamper tests. |
| V11 | Business Logic | apply | Fail closed on missing prerequisites, stale identity, or partial outcome. |
| V12 | Files/Resources | apply | Use bounded paths, immutable blobs/objects, and resource limits. |
| V13 | API/Web Services | apply | Validate structured API/provider responses and preserve authorization semantics. |
| V14 | Configuration | apply | Use reviewed closed configuration with explicit production NOT RUN state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-474-03-S | Spoofing | source/provider identity | mitigate/block | Require exact execution-derived Git/provider identity and authorized actor/role. |
| T-474-03-T | Tampering | Fresh hermetic Python verification (D-01, D-02, D-03, D-04) | mitigate/block | Bind inputs, commands, results, and artifacts; reject mismatch, drift, or unknown fields. |
| T-474-03-R | Repudiation | evidence receipt | mitigate/block | Retain canonical source-bound command/result/request identities. |
| T-474-03-I | Information Disclosure | diagnostics/evidence | mitigate/block | Store redacted IDs, hashes, counts, and statuses only. |
| T-474-03-D | Denial of Service | bounded execution | mitigate/block | Apply bounded time/resource scopes and fail closed without partial PASS. |
| T-474-03-E | Elevation of Privilege | verification-to-delivery boundary | mitigate/block | Use least privilege, dependency-closed eligibility, and protected environments. |
| T-474-03-SC | Tampering | package/action supply chain | mitigate/block | Use only RESEARCH-approved packages and immutable reviewed Action SHAs; [SLOP] is forbidden. |
</threat_model>

<verification>
  <automated>bash -lc 'uv lock --check &amp;&amp; .venv/bin/python -m pytest -q tests/test_deterministic_gate.py'</automated>
  <automated>bash -lc '.venv/bin/python -m pytest -q tests/test_deterministic_gate.py tests/test_release_gate.py &amp;&amp; .venv/bin/ruff check scripts/release_gate.py scripts/phase474_pytest_guard.py tests/conftest.py tests/test_deterministic_gate.py'</automated>
</verification>

<success_criteria>
- Two fresh Python 3.12 runs pass with identical collection and zero non-pass outcomes.
- Ambient AWS and network paths are denied at the process boundary.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-03-SUMMARY.md` when done.
</output>
