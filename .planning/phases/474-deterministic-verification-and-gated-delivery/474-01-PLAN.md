---
phase: 474-deterministic-verification-and-gated-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "evidence/phase-474/candidate-identity.json"
  - "evidence/phase-474/candidate-preflight-blocker.json"
autonomous: false
requirements: [V9QUAL-01, V9QUAL-06, V9QUAL-07]
must_haves:
  truths:
    - "No Phase 474 edit begins from a dirty repository."
    - "The infra .DS_Store is owner-resolved rather than deleted by planning or execution."
  artifacts:
    - path: "evidence/phase-474/candidate-identity.json"
      provides: "Execution-time clean candidate preflight"
    - path: "evidence/phase-474/candidate-preflight-blocker.json"
      provides: "Execution-time clean candidate preflight"
  key_links:
    - from: "evidence/phase-474/candidate-identity.json"
      to: "evidence/phase-474/candidate-preflight-blocker.json"
      via: "closed task contract"
      pattern: 'candidate-preflight-blocker\.json'
---

<objective>
Execution-time clean candidate preflight (D-13, D-16).

Purpose: Prevent every later edit from silently adopting unrelated work and bind all work to fresh execution-time identities.
Output: `evidence/phase-474/candidate-identity.json`, `evidence/phase-474/candidate-preflight-blocker.json`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `evidence/phase-474/candidate-identity.json` — Execution-time clean candidate preflight.
- `evidence/phase-474/candidate-preflight-blocker.json` — Execution-time clean candidate preflight.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inspect all three repositories and retain a fail-closed preflight</name>
  <files>evidence/phase-474/candidate-preflight-blocker.json, evidence/phase-474/candidate-identity.json</files>
  <behavior>
    - Any dirty root blocks identity issuance.
    - Planning-time SHAs are never release identity.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
  </read_first>
  <action>Inspect the exact backend, frontend, and infra roots before any repository edit. Resolve execution-time HEAD, HEAD^{tree}, authoritative lock SHA-256, and porcelain-v1 including untracked files; never compare against research-time SHA constants. If any root is dirty, write only a redacted blocker receipt and do not create candidate-identity.json. The known infra .DS_Store must produce the blocker and must not be deleted, moved, ignored, staged, committed, or adopted by the executor. Only after all roots are clean may the command write the canonical candidate receipt per D-13. This task implements D-13, D-16.</action>
  <verify>
    <automated>bash -lc 'test -f evidence/phase-474/candidate-preflight-blocker.json || jq -e '"'"'.schema == "stoa.release.candidate-identity.v1" and ([.repositories[] | .clean] | all)'"'"' evidence/phase-474/candidate-identity.json >/dev/null'</automated>
  </verify>
  <acceptance>
    - Dirty state yields a bounded blocker receipt and zero repository mutation.
    - A clean run records execution-time commit/tree/lock/porcelain identities for all three roots.
  </acceptance>
  <done>Dirty state yields a bounded blocker receipt and zero repository mutation. A clean run records execution-time commit/tree/lock/porcelain identities for all three roots.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Resolve the pre-existing infra .DS_Store and approve the clean-start receipt</name>
  <files>evidence/phase-474/candidate-identity.json</files>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
  </read_first>
  <action>Pause because the pre-existing infra .DS_Store is outside Phase 474 ownership. The owner decides whether to retain it elsewhere, commit it intentionally outside this phase, or remove it. After the owner resolves it, rerun the automated preflight without reset/clean/checkout and issue candidate-identity.json only when all three porcelain results are empty. This task implements D-13, D-16.</action>
  <verify>
    <automated>bash -lc 'test -z "$(git status --porcelain=v1 --untracked-files=all -- . ":(exclude)evidence/phase-474/candidate-identity.json")" &amp;&amp; test -z "$(git -C /Users/zhdeng/stoa-frontend status --porcelain=v1 --untracked-files=all)" &amp;&amp; test -z "$(git -C /Users/zhdeng/stoa-infra status --porcelain=v1 --untracked-files=all)" &amp;&amp; jq -e '"'"'([.repositories[] | .clean] | all)'"'"' evidence/phase-474/candidate-identity.json >/dev/null'</automated>
  </verify>
  <how-to-verify>Review the exact three repository roots and bounded dirty-path list. Resolve /Users/zhdeng/stoa-infra/.DS_Store outside Phase 474, then type approved so the clean preflight can be rerun.</how-to-verify>
  <resume-signal>Type approved only when the automated check is green; otherwise describe the discrepancy.</resume-signal>
  <acceptance>
    - The owner, not the executor, resolves the undeclared infra file.
    - The approved receipt is derived from the now-clean execution-time state.
  </acceptance>
  <done>The owner, not the executor, resolves the undeclared infra file. The approved receipt is derived from the now-clean execution-time state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS / STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-01-01 | High | Tampering / Repudiation | Execution-time clean candidate preflight | mitigate/block | Bind exact source, inputs, command, result, and artifact identities; reject missing, duplicate, unknown, dirty, or mismatched values. |
| T-474-01-02 | High | Spoofing / Elevation | verification-to-delivery boundary | mitigate/block | Apply least privilege, protected environment checks, and dependency-closed eligibility before authority is exposed. |
| T-474-SC | High | Tampering | audited package installs | mitigate/block | Use only packages marked OK in RESEARCH.md Package Legitimacy Audit; [ASSUMED]/[SUS] requires a blocking human checkpoint and [SLOP] is forbidden. |
</threat_model>

<verification>
<automated>bash -lc 'test -z "$(git status --porcelain=v1 --untracked-files=all -- . ":(exclude)evidence/phase-474/candidate-identity.json")" &amp;&amp; test -z "$(git -C /Users/zhdeng/stoa-frontend status --porcelain=v1 --untracked-files=all)" &amp;&amp; test -z "$(git -C /Users/zhdeng/stoa-infra status --porcelain=v1 --untracked-files=all)" &amp;&amp; jq -e '"'"'([.repositories[] | .clean] | all)'"'"' evidence/phase-474/candidate-identity.json >/dev/null'</automated>
</verification>

<success_criteria>
- No Phase 474 edit begins from a dirty repository.
- The infra .DS_Store is owner-resolved rather than deleted by planning or execution.
- Every task command is green and its evidence is source-bound.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN` unless a later explicit operational approval exists.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-01-SUMMARY.md` when done.
</output>
