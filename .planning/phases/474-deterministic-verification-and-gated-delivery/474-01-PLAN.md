---
phase: 474-deterministic-verification-and-gated-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/release_gate.py
  - schemas/release/gate-receipt-v1.schema.json
  - tests/test_release_gate.py
autonomous: true
requirements: [V9QUAL-01, V9QUAL-02, V9QUAL-03, V9QUAL-04, V9QUAL-05, V9QUAL-06]
must_haves:
  truths:
    - "Local formal verification and CI resolve to one backend-owned command per D-01"
    - "A missing, duplicate, malformed, skipped, or failed obligation cannot produce PASS"
    - "Receipts bind exact candidate identities, commands, artifacts, runtimes, clocks, counts, and privacy results"
  artifacts:
    - path: scripts/release_gate.py
      provides: "Authoritative release-control CLI and canonical receipt primitives"
    - path: schemas/release/gate-receipt-v1.schema.json
      provides: "Closed machine contract for exact PASS, FAIL, and NOT RUN receipts"
    - path: tests/test_release_gate.py
      provides: "Tamper, omission, parity, and command-contract tests"
  key_links:
    - from: scripts/release_gate.py
      to: schemas/release/gate-receipt-v1.schema.json
      via: "schema validation before receipt acceptance"
    - from: tests/test_release_gate.py
      to: scripts/release_gate.py
      via: "module-level CLI and single-axis tamper tests"
---

<objective>
Define the one authoritative backend-owned local/CI gate and its fail-closed receipt contract per D-01 and D-03.

Purpose: Give every later verification, build, promotion, and rollback plan one immutable command/result vocabulary instead of duplicating policy in workflow YAML.
Output: `scripts/release_gate.py`, a closed receipt schema, and executable negative/positive contracts.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
@scripts/release_evidence.py
@scripts/verify_phase473_evidence.py
@tests/test_release_evidence.py
@tests/test_phase473_evidence_verifier.py
</context>

## Artifacts this phase produces

- New file `scripts/release_gate.py` with `GatePolicyError`, `GateExecutionError`, `GateStatus`, `canonical_json_bytes`, `sha256_bytes`, `validate_exact_candidate`, `validate_gate_receipt`, `write_gate_receipt`, `command_verify`, `command_self_test`, and testable `main(argv)`.
- New CLI `python scripts/release_gate.py verify --backend-ref <sha> --frontend-ref <sha> --infra-ref <sha> --receipt-root <path>`.
- New CLI `python scripts/release_gate.py self-test --receipt-root <path>`.
- New closed schema `schemas/release/gate-receipt-v1.schema.json` requiring `schema`, `gate_id`, exact candidate triplet, exact argv, UTC start/end, exit code, runtime/tool/lock/source identities, artifact byte/SHA-256 facts, closed outcome counts, privacy scan, status, and receipt digest.
- Status vocabulary `PASS|FAIL|NOT RUN`; `NOT RUN` requires a named obligation, owner, reason code, and prohibition on pass-equivalent counts.
- New test file `tests/test_release_gate.py` covering local/CI argv parity, canonical JSON stability, dirty/non-SHA candidates, missing/extra/duplicate obligations, digest drift, privacy matches, and PASS/FAIL/NOT RUN interpretation.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller -> gate CLI | Branch names, mutable refs, paths, and commands are untrusted release inputs. |
| subprocess -> receipt | Exit prose cannot be trusted without exact argv, byte digests, counts, and status policy. |
| receipt -> downstream workflow | A forged PASS could expose artifacts or credentials to deployment jobs. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-01-01 | High | Spoofing | candidate identity | mitigate/block | Accept only full exact backend/Web/infra commit SHAs and recompute source identity. |
| T-474-01-02 | Critical | Tampering | gate receipt | mitigate/block | Closed schema, canonical SHA-256 digest, exact artifact bytes, and single-axis tamper tests. |
| T-474-01-03 | High | Repudiation | command result | mitigate/block | Persist exact argv, UTC bounds, exit status, counts, and tool identities. |
| T-474-01-04 | High | Information disclosure | receipt output | mitigate/block | Closed privacy fields and denial tests for credentials, tokens, student content, object keys, and browser storage. |
| T-474-01-05 | Critical | Elevation of privilege | downstream gate | mitigate/block | Missing/non-PASS obligations return exit 2 and never expose artifact or deployment authority. |
| T-474-01-SC | High | Tampering | package installs | mitigate/block | No package install in this plan; if scope changes, only packages approved in the research legitimacy audit may be added. |

## ASVS 4.0.3 L1 Control Mapping

| ASVS L1 category | Applicability and concrete control | Verification |
|---|---|---|
| V1 Architecture | Applicable — exclusive file ownership, explicit dependencies, and fail-closed boundaries are part of this plan. | Task 01-01 automated command and this plan's focused acceptance criteria. |
| V2 Authentication | Applicable — exact identity/provider inputs remain authenticated and are exercised by focused tests. | Task 01-01 automated command and this plan's focused acceptance criteria. |
| V3 Session Management | Applicable — session/token state is closed and expiry/replay behavior remains tested. | Task 01-01 automated command and this plan's focused acceptance criteria. |
| V4 Access Control | Applicable — authorization/release authority remains deny-first; any High/Critical failure blocks completion. | Task 01-01 automated command and this plan's focused acceptance criteria. |
| V5 Validation | Applicable — untrusted records, command inputs, and result unions are narrowed before use; malformed values fail. | Task 01-01 automated command and this plan's focused acceptance criteria. |
| V6 Cryptography | Applicable — SHA-256/content identities are recomputed and never substitute for secret-key controls. | Task 01-01 automated command and this plan's focused acceptance criteria. |
| V7 Error Handling and Logging | Applicable — stable bounded errors/results are verified and private/provider payloads are not logged. | Task 01-01 automated command and this plan's focused acceptance criteria. |
| V8 Data Protection | Applicable — tests preserve redaction and prevent secret, token, student-content, or provider-payload disclosure. | Task 01-01 automated command and this plan's focused acceptance criteria. |
| V9 Communications | Applicable — real/provider/network boundaries require approved origins, denial, or immutable transport identity. | Task 01-01 automated command and this plan's focused acceptance criteria. |
| V10 Malicious Code | Applicable — no unreviewed package/action is added; the SC threat and locked dependency policy remain blocking. | Task 01-01 automated command and this plan's focused acceptance criteria. |
| V11 Business Logic | Applicable — focused tests preserve exact state, race, retry, idempotency, and fail-closed outcomes relevant to the owned files. | Task 01-01 automated command and this plan's focused acceptance criteria. |
| V12 Files and Resources | Applicable — every modified path is enumerated and artifact/evidence bytes or source files are bounded by exact ownership. | Task 01-01 automated command and this plan's focused acceptance criteria. |
| V13 API and Web Services | Applicable — request/CLI/workflow contracts are closed and verified end to end. | Task 01-01 automated command and this plan's focused acceptance criteria. |
| V14 Configuration | Applicable — exact tool/scope/configuration is verified and broad ignores, mutable refs, or permissive defaults are forbidden. | Task 01-01 automated command and this plan's focused acceptance criteria. |

Any failed applicable ASVS L1 control, or any High/Critical STRIDE threat without its named mitigation and test, blocks plan completion.
</threat_model>

<tasks>

<task type="auto" id="474-01-01" tdd="true">
  <name>Freeze the authoritative CLI and receipt rejection contract</name>
  <files>tests/test_release_gate.py, schemas/release/gate-receipt-v1.schema.json</files>
  <behavior>
    - Local and CI callers with the same exact refs resolve to byte-identical gate specifications.
    - Only full commit SHAs, closed gate IDs, complete identities, and canonical receipt bytes are accepted.
    - Missing, duplicate, unknown, skipped, failed, privacy-tainted, digest-drifted, or pass-shaped NOT RUN records fail.
  </behavior>
  <read_first>
    - tests/test_release_evidence.py
    - tests/test_phase473_evidence_verifier.py
    - scripts/release_evidence.py
    - scripts/verify_phase473_evidence.py
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - tests/test_release_gate.py
    - schemas/release/gate-receipt-v1.schema.json
  </read_first>
  <action>Write failing tests and the closed v1 schema before implementation. Require `verify` and `self-test`; exact backend/frontend/infra SHA inputs; canonical sorted compact JSON; SHA-256 over stable fields; exit `0` only for a complete PASS, exit `2` for policy rejection, and another nonzero code for unexpected execution failure. Require receipt keys named in the artifacts section, forbid additional properties, and make `NOT RUN` distinct from PASS per D-03. Parameterize single-field tampering for every identity/count/artifact/privacy/status field and prove CI cannot supply an alternate command graph per D-01.</action>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_release_gate.py</automated></verify>
  <acceptance_criteria>
    - The tests enumerate every required schema key and all three status values.
    - Every mutated or omitted identity, count, artifact, privacy, or status field is rejected.
    - Local/CI parity is asserted from one generated command specification, not string comments.
  </acceptance_criteria>
  <done>The executable contract is red only because the authoritative CLI is not yet implemented.</done>
</task>

<task type="auto" id="474-01-02" tdd="true">
  <name>Implement canonical gate primitives and fail-closed command dispatch</name>
  <files>scripts/release_gate.py, tests/test_release_gate.py</files>
  <behavior>
    - `main(argv)` is importable and produces deterministic results without workflow-specific behavior.
    - Policy failures emit a structured FAIL receipt and exit 2; unexpected failures never become policy PASS.
    - Receipt output contains no secret/private fields and changes digest after any stable-field mutation.
  </behavior>
  <read_first>
    - scripts/release_gate.py
    - tests/test_release_gate.py
    - schemas/release/gate-receipt-v1.schema.json
    - scripts/release_evidence.py
    - scripts/verify_phase473_evidence.py
  </read_first>
  <action>Implement the named symbols and CLI. Validate typed top-level objects, exact SHA/path inputs, closed gate inventories, canonical UTC timestamps, exact command arrays, and artifact metadata. Write sorted indented JSON plus newline for human files, compute receipt identity from compact canonical JSON, create parent directories, and never echo environment values. Keep orchestration hooks dependency-light and inject subprocess/Git/clock operations so later plans can test them. Do not add build, Web, AWS, or workflow policy here; expose typed registration points consumed by later plans.</action>
  <verify><automated>bash -lc '.venv/bin/python -m pytest -q tests/test_release_gate.py && .venv/bin/ruff check scripts/release_gate.py tests/test_release_gate.py'</automated></verify>
  <acceptance_criteria>
    - Both CLI commands exist and `main(argv)` is directly tested.
    - Canonical output and digest are byte-stable across two runs with identical injected inputs.
    - No branch, tag, `latest`, mutable path, skipped obligation, or privacy match can yield exit 0.
  </acceptance_criteria>
  <done>All formal callers share one deterministic gate/result vocabulary that fails closed.</done>
</task>

</tasks>

<verification>
Run the focused tests twice, compare generated receipt bytes, run Ruff on the new source/tests, and inspect `--help` to confirm only the checked-in authoritative command surface exists.
</verification>

<success_criteria>
- D-01 is executable: local and CI command specs are generated by the same code.
- D-03 is executable: non-pass and unavailable obligations cannot masquerade as PASS.
- Candidate, command, runtime, artifact, outcome, and privacy tampering are rejected with exit 2.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-01-SUMMARY.md` when done.
</output>
