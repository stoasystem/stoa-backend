---
phase: 474-deterministic-verification-and-gated-delivery
plan: 80
type: execute
wave: 20
depends_on: [474-34, 474-79]
files_modified:
  - "evidence/phase-474/owner-staging-verification.json"
autonomous: false
requirements: [V9QUAL-06]
must_haves:
  truths:
    - "The owner verifies the reviewer-free single-staging GitHub policy and provider-backed staging authority only after both Plan 474-79 and Plan 474-34 receipts independently PASS."
    - "The checkpoint verifies exactly three staging roles with one exact subject and grants no production authority; production remains DEFERRED_OUT_OF_SCOPE."
  artifacts:
    - path: "evidence/phase-474/owner-staging-verification.json"
      provides: "Owner verification of GitHub and staging evidence (D-03, D-05, D-06, D-15)"
  key_links:
    - from: "evidence/phase-474/protected-environments-resolution.json and evidence/phase-474/staging-substrate.json"
      to: "evidence/phase-474/owner-staging-verification.json"
      via: "two completed provider readbacks and one owner verification digest"
      pattern: 'owner\-staging\-verification\.json'
---

<objective>
Owner verification of GitHub and staging evidence (D-03, D-05, D-06, D-15).

Purpose: Require owner review only after both protected-environment readback and the staging-only substrate exist.
Output: `evidence/phase-474/owner-staging-verification.json`.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

## Artifacts this phase produces

- `evidence/phase-474/owner-staging-verification.json` — Owner verification of GitHub and staging evidence (D-03, D-05, D-06, D-15)

## V9QUAL mapping

| Requirement | Plan evidence |
|---|---|
| V9QUAL-06 | The owner verifies the exact single-staging GitHub/workflow and three-role AWS readbacks without converting that review into production authorization or eligibility. |

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Owner verifies protected environments and staging authority</name>
  <files>evidence/phase-474/owner-staging-verification.json</files>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - evidence/phase-474/owner-staging-verification.json
    - evidence/phase-474/environment-baseline-resolution.json
    - evidence/phase-474/protected-environments-resolution.json
    - evidence/phase-474/staging-substrate.json
  </read_first>
  <what-built>Two independently validated staging-only PASS provider readbacks joined into one digest-bound owner verification receipt, with production release control explicitly deferred.</what-built>
  <action>Per D-03/D-05/D-06/D-15, independently validate the Plan 474-79 `protected-environments-resolution.json` and Plan 474-34 `staging-substrate.json` against the same Plan 474-33 baseline and their exact policy/workflow/source/confirmation/readback digests; a PENDING, BLOCKED, mismatched, or partially read-back upstream artifact cannot reach this checkpoint. Create a digest-bound PENDING owner receipt from both completed PASS artifacts, then pause. The owner confirms exactly one `staging` Environment with branch `main` only, no tags, no reviewers, main protection and one active ruleset; separate deploy/smoke/rollback jobs all using `staging`; one exact OIDC subject; the canonical backend manifest and owner-confirmed additive `StoaReleaseStaging` diff; and exactly `StoaStagingDeployRole`, `StoaStagingReadRole`, and `StoaStagingRollbackRole` with disjoint permissions and the same exact staging subject. Require the production release control plane to be `DEFERRED_OUT_OF_SCOPE` and absent from both upstream artifacts. Approval records staging evidence verification only; it grants no production infrastructure, identity, eligibility, or mutation authority.</action>
  <verify>
    <automated>bash -lc 'jq -e '"'"'.status == "PASS" and (.readback.environments | map(.name)) == ["staging"] and (.readback.environments[0].reviewers | length) == 0 and .scope.production_release_control_plane == "DEFERRED_OUT_OF_SCOPE"'"'"' evidence/phase-474/protected-environments-resolution.json >/dev/null &amp;&amp; jq -e '"'"'.status == "PASS" and .confirmation.status == "OWNER_CONFIRMED" and .readback.status == "PASS" and (.readback.roles | length) == 3 and .scope.production_release_control_plane == "DEFERRED_OUT_OF_SCOPE"'"'"' evidence/phase-474/staging-substrate.json >/dev/null &amp;&amp; jq -e '"'"'.status == "OWNER_VERIFIED" and .github_authority == "CONFIRMED" and .staging_authority == "CONFIRMED" and .environment == "staging" and .manual_reviewers == [] and .scope.production_release_control_plane == "DEFERRED_OUT_OF_SCOPE"'"'"' evidence/phase-474/owner-staging-verification.json >/dev/null'</automated>
  </verify>
  <how-to-verify>Inspect both completed provider-linked resolutions and the generated owner receipt. Confirm exactly one reviewer-free `staging` Environment, main-only/no-tag policy, main protection/ruleset, one OIDC subject, three shared-Environment workflow jobs, the three exact least-privilege AWS roles, canonical backend manifest, confirmed additive staging diff, provider readback, and production `DEFERRED_OUT_OF_SCOPE`.</how-to-verify>
  <resume-signal>Type approved only if both upstream receipts and the generated owner receipt match; otherwise describe the discrepancy.</resume-signal>
  <acceptance>
    - The owner verifies the automated GitHub and staging evidence after both exist.
    - Production infrastructure, identity, trust, eligibility, and mutation are absent and DEFERRED_OUT_OF_SCOPE.
  </acceptance>
  <done>The owner verifies the staging-only GitHub and AWS evidence after both exist; production release control is absent and DEFERRED_OUT_OF_SCOPE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/evidence input → phase logic | Untrusted paths, refs, provider values, diagnostics, and receipts are validated before use. |
| verification → delivery authority | Artifacts, credentials, and mutation eligibility remain unavailable until all required gates pass. |

## ASVS V1-V14 Applicability

| ASVS | Control family | Disposition | Plan control |
|---|---|---|---|
| V1 | Architecture | apply | Keep the plan dependency-closed, source-bound, and independently verifiable. |
| V2 | Authentication | preserve | Do not introduce an alternate identity mechanism; smoke uses existing bounded auth. |
| V3 | Session Management | preserve | Do not broaden session lifetime or expose browser/session material in evidence. |
| V4 | Access Control | apply | Use least privilege and protected environment/OIDC boundaries before authority. |
| V5 | Validation | apply | Validate paths, refs, schemas, provider values, counts, and closed outcomes. |
| V6 | Cryptography | apply | Use SHA-256/AWS-managed encryption only; reject digest mismatch. |
| V7 | Error/Logging | apply | Emit stable redacted errors and identifiers without credentials or private payloads. |
| V8 | Data Protection | apply | Retain only required hashes, IDs, statuses, and policy evidence. |
| V9 | Communications | apply | Deny ambient network and constrain provider calls to reviewed endpoints/roles. |
| V10 | Malicious Code | apply | Use audited locks, immutable action SHAs, and tamper tests. |
| V11 | Business Logic | apply | Fail closed on missing prerequisites, stale identity, or partial outcome. |
| V12 | Files/Resources | apply | Use bounded paths, immutable blobs/objects, and resource limits. |
| V13 | API/Web Services | apply | Validate structured API/provider responses and preserve authorization semantics. |
| V14 | Configuration | apply | Use reviewed staging-only configuration with explicit production `DEFERRED_OUT_OF_SCOPE` state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-474-80-S | Spoofing | owner/source/provider identity | high | mitigate/block | Require exact owner and both independently validated provider receipts. |
| T-474-80-T | Tampering | joined owner verification evidence | high | mitigate/block | Bind baseline, GitHub resolution, staging receipt, confirmation, and readback digests. |
| T-474-80-R | Repudiation | owner verification receipt | high | mitigate/block | Retain canonical owner confirmation and upstream digest identities. |
| T-474-80-I | Information Disclosure | diagnostics/evidence | medium | mitigate/block | Store redacted IDs, hashes, counts, and statuses only. |
| T-474-80-D | Denial of Service | bounded owner verification | low | mitigate/block | Fail closed on incomplete upstream evidence without partial OWNER_VERIFIED state. |
| T-474-80-E | Elevation of Privilege | staging-only authority boundary | critical | mitigate/block | Owner verification confirms evidence only and cannot create a production identity, eligibility path, or action. |
| T-474-80-SC | Tampering | package/action supply chain | high | mitigate/block | Use only RESEARCH-approved packages and immutable reviewed Action SHAs; [SLOP] is forbidden. |
</threat_model>

<verification>
  <automated>bash -lc 'jq -e '"'"'.status == "PASS" and .scope.production_release_control_plane == "DEFERRED_OUT_OF_SCOPE"'"'"' evidence/phase-474/protected-environments-resolution.json >/dev/null &amp;&amp; jq -e '"'"'.status == "PASS" and .confirmation.status == "OWNER_CONFIRMED" and .readback.status == "PASS" and .scope.production_release_control_plane == "DEFERRED_OUT_OF_SCOPE"'"'"' evidence/phase-474/staging-substrate.json >/dev/null &amp;&amp; jq -e '"'"'.status == "OWNER_VERIFIED" and .github_authority == "CONFIRMED" and .staging_authority == "CONFIRMED" and .scope.production_release_control_plane == "DEFERRED_OUT_OF_SCOPE"'"'"' evidence/phase-474/owner-staging-verification.json >/dev/null'</automated>
</verification>

<success_criteria>
- The owner verifies both completed GitHub and staging provider readbacks against one immutable baseline and joined digest.
- The checkpoint grants no production authority and confirms production release control is DEFERRED_OUT_OF_SCOPE.
- Every task command is green and its evidence is source-bound.
- Production GitHub/AWS release-control work is `DEFERRED_OUT_OF_SCOPE` to a separate future phase or milestone.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-80-SUMMARY.md` when done.
</output>
