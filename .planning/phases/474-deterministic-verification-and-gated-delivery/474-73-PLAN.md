---
phase: 474-deterministic-verification-and-gated-delivery
plan: 73
type: execute
wave: 7
depends_on: [474-72]
files_modified:
  - "/Users/zhdeng/stoa-frontend/public/served-release.json.template"
  - "/Users/zhdeng/stoa-frontend/schemas/release/served-release-v1.schema.json"
  - "/Users/zhdeng/stoa-frontend/src/lib/servedRelease.ts"
  - "/Users/zhdeng/stoa-frontend/tests/release/served-release.test.mjs"
autonomous: true
requirements: [V9QUAL-03, V9QUAL-06]
must_haves:
  truths:
    - "The browser trusts one closed same-origin served-release descriptor, not unrelated build-time variables or mutable latest labels."
    - "The descriptor binds exact release identities and the runtime-config/Web object key, VersionId, and SHA-256 needed by later delivery evidence."
  artifacts:
    - path: "/Users/zhdeng/stoa-frontend/schemas/release/served-release-v1.schema.json"
      provides: "Closed served-release descriptor schema"
    - path: "/Users/zhdeng/stoa-frontend/src/lib/servedRelease.ts"
      provides: "Bounded descriptor loader and validator"
    - path: "/Users/zhdeng/stoa-frontend/tests/release/served-release.test.mjs"
      provides: "Descriptor tamper and trust-boundary tests"
  key_links:
    - from: "474-72"
      to: "/Users/zhdeng/stoa-frontend/src/lib/servedRelease.ts"
      via: "descriptor supplies exact RuntimeConfig validation expectations"
      pattern: 'servedRelease|runtimeConfig'
---

<objective>
Define the single served-release descriptor client trust contract (D-13, D-14, D-17, D-18).

Purpose: Give the Web client one bounded same-origin document that identifies the exact served release and exact immutable runtime-config/Web objects without initializing any application service.
Output: descriptor template, closed schema, dependency-free typed loader, and adversarial Node tests.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the served-release descriptor trust contract</name>
  <files>/Users/zhdeng/stoa-frontend/public/served-release.json.template, /Users/zhdeng/stoa-frontend/schemas/release/served-release-v1.schema.json, /Users/zhdeng/stoa-frontend/src/lib/servedRelease.ts, /Users/zhdeng/stoa-frontend/tests/release/served-release.test.mjs</files>
  <behavior>
    - A closed `stoa.web.served-release.v1` descriptor passes only with exact environment, releaseId, manifest SHA-256, frontend/backend artifact SHA-256, and runtime-config/Web key, VersionId, URL, and SHA-256 values.
    - Unknown fields, secrets, mobile/native/demo/auth-provider fields, unsafe or cross-origin URLs, mutable labels, malformed key/VersionId/SHA values, duplicate JSON keys, redirects, wrong media, and bodies over 16 KiB fail closed.
    - Fetch uses exact same-origin `/served-release.json`, `credentials: omit`, `cache: no-store`, and `redirect: error`; this plan does not start React, API, auth, monitoring, or contact services.
  </behavior>
  <read_first>
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-RESEARCH.md
    - .planning/phases/474-deterministic-verification-and-gated-delivery/474-PATTERNS.md
    - /Users/zhdeng/stoa-frontend/src/lib/runtimeConfig.ts
    - /Users/zhdeng/stoa-frontend/tests/release/runtime-config.test.mjs
    - /Users/zhdeng/stoa-frontend/public/served-release.json.template
    - /Users/zhdeng/stoa-frontend/schemas/release/served-release-v1.schema.json
    - /Users/zhdeng/stoa-frontend/src/lib/servedRelease.ts
    - /Users/zhdeng/stoa-frontend/tests/release/served-release.test.mjs
  </read_first>
  <action>Implement one dependency-free closed descriptor schema/loader. The descriptor is the only client trust input and carries the exact release identities required by `validateRuntimeConfig`, plus exact immutable runtime-config and Web entry object key, VersionId, same-origin URL, and SHA-256. Canonicalize and deep-freeze validated values; bound fetch to 16 KiB and refuse credentials, cache reuse, redirects, final-URL drift, duplicate keys, secret-like values, unsafe origins, mutable `latest`/branch identities, and any unknown field. Preserve `backend-api`; do not activate Amplify/Cognito and do not bootstrap services in this plan.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail &amp;&amp; cd /Users/zhdeng/stoa-frontend &amp;&amp; node --version | grep -Eq "^v20\." &amp;&amp; node --test tests/release/served-release.test.mjs &amp;&amp; npm exec -- tsc -b'</automated>
  </verify>
  <acceptance>
    - Schema, template, loader, and tests expose one closed served-release descriptor.
    - The descriptor binds exact immutable identities and contains no credential, alternate auth, demo, or native/mobile configuration.
  </acceptance>
  <done>One bounded served-release descriptor supplies exact immutable client trust inputs without starting any application service.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| same-origin descriptor response → client trust | Untrusted bytes become release authority only after bounded strict parsing and closed identity validation. |
| descriptor identity → runtime config | Exact release, key, VersionId, URL, and SHA-256 values must agree before configuration installation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-474-73-S | Spoofing | served release identity | mitigate/block | Require exact versioned release and object identities from one closed same-origin descriptor. |
| T-474-73-T | Tampering | descriptor bytes | mitigate/block | Strict JSON, closed keys, canonical identities, exact final URL, and SHA-256 binding fail closed. |
| T-474-73-I | Information Disclosure | descriptor values | mitigate/block | Permit only non-secret hashes, object identities, environment, and same-origin paths. |
| T-474-73-E | Elevation of Privilege | browser auth selection | mitigate/block | Preserve backend-api and exclude provider/native auth configuration. |
</threat_model>

<verification>
  <automated>bash -lc 'set -euo pipefail &amp;&amp; cd /Users/zhdeng/stoa-frontend &amp;&amp; node --version | grep -Eq "^v20\." &amp;&amp; node --test tests/release/served-release.test.mjs &amp;&amp; npm exec -- tsc -b'</automated>
</verification>

<success_criteria>
- One descriptor is the sole Web release trust input.
- Exact release, runtime-config, and Web object identities are closed and tamper-tested.
- No service bootstrap, alternate auth, native/mobile configuration, or provider mutation is introduced.
- Production infrastructure, deploy, smoke, and rollback remain exact `NOT RUN`.
</success_criteria>

<output>
Create `.planning/phases/474-deterministic-verification-and-gated-delivery/474-73-SUMMARY.md` when done.
</output>
