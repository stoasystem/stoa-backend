# Phase 41 Plan

**Phase:** 41 - Release Gate And v1.7 Audit
**Plan:** 41-01
**Status:** Complete

## Objective

Close v1.7 with a release gate that consolidates build, deploy, CDK, API, browser smoke, privacy, and final audit evidence.

## Tasks

1. Record backend/frontend deploy runs and deployed commit SHAs.
2. Rebuild/verify Lambda dist manifest and record deployed Lambda runtime state.
3. Run backend quality gates and frontend quality/e2e gates.
4. Run CDK diff and classify any output as infrastructure change, Lambda code asset drift, or no-op.
5. Verify production admin credential path metadata and Cognito `admins` group membership without exposing secret values.
6. Run production API checks for health, admin auth gate, authenticated export, bounds rejection, request IDs, and privacy denylist.
7. Run production browser smoke against `/admin/report-operations` with the long-lived admin credential path and block non-GET report operations calls.
8. Write release gate, live verification, final milestone audit, verification, and summary artifacts.
9. Mark VERIFY-01 and Phase 41 complete, then prepare v1.7 archive.

## Acceptance Mapping

| Success criterion | Evidence |
|-------------------|----------|
| Lambda build manifest and deploy evidence | `41-RELEASE-GATE.md` |
| Admin-only API checks with request IDs/bounds/privacy | `41-LIVE-VERIFICATION.md` |
| CDK diff/deploy evidence or N/A | `41-RELEASE-GATE.md` |
| Production browser smoke with long-lived admin credential path and no mutation | `41-LIVE-VERIFICATION.md` |
| Final audit and archive readiness | `41-MILESTONE-AUDIT.md` |

