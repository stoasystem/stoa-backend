# Phase 134 Plan: Role Route Contract Polish

**Phase:** 134
**Requirement:** I18N-02
**Status:** Planned
**Created:** 2026-06-11

## Objective

Add language-safe metadata and mobile-friendly contract evidence to selected student, parent, tutor, and admin adaptive learning routes without changing canonical API values.

## Tasks

1. Add a shared adaptive locale metadata helper.
2. Add locale metadata to memory, recommendation, assignment, assignment-list, and parent-progress responses.
3. Preserve canonical IDs/statuses/enum-like values/timestamps.
4. Extend adaptive tests for locale metadata and canonical stability across `de` and `en`.
5. Run focused adaptive/locale tests and ruff.

## Acceptance Criteria Mapping

| Criterion | Plan coverage |
|-----------|---------------|
| Selected role responses expose locale/language metadata. | Adaptive student, parent, tutor, and admin responses include `locale`. |
| Canonical values remain stable across locale preferences. | Tests compare student ID, role view, recommendation type/topic, and freshness under `de` and `en`. |
| Mobile-sensitive list/detail or summary contracts are bounded/documented. | Parent progress preserves active/completed assignment slices capped to five items; assignment lists carry count plus metadata. |
| Focused tests cover role visibility, canonical stability, and metadata. | `tests/test_adaptive_learning.py` assertions. |

## Verification

- `.venv/bin/python -m pytest tests/test_adaptive_learning.py tests/test_locale_preferences.py`
- `.venv/bin/python -m ruff check src/stoa/services/adaptive_learning_service.py src/stoa/routers/adaptive.py tests/test_adaptive_learning.py`
