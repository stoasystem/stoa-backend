---
phase: 472-privileged-identity-and-student-resource-authorization
plan: 22
type: execute
wave: 12
depends_on: [472-17, 472-18, 472-19, 472-20, 472-21]
autonomous: true
requirements: [V9AUTH-04, V9AUTH-05, V9ACCESS-01, V9ACCESS-02, V9ACCESS-03]
user_setup:
  - "Optional separately approved non-production Cognito sandbox configuration for external evidence; absent checks remain NOT RUN"
must_haves:
  truths:
    - "All six final review findings pass together under adversarial endpoint/state-machine tests and legitimate positive controls, including multi-target all-of authorization"
    - "The established Phase 472 gate, terminology gate, route inventory, client actions, and audit evidence remain deterministic"
    - "Phase 474/475 ownership and unavailable external Cognito evidence are reported exactly, never absorbed or inferred as passed"
  artifacts:
    - "docs/security/phase-472-evidence.md"
    - ".planning/phases/472-privileged-identity-and-student-resource-authorization/472-VALIDATION.md"
  key_links:
    - "Final evidence binds six reproduced findings to commands, tested source SHA, generated artifact digests, and redacted outcomes"
files_modified:
  - docs/security/phase-472-evidence.md
  - docs/security/route-authorization-inventory.json
  - docs/security/client-error-actions.json
  - .planning/phases/472-privileged-identity-and-student-resource-authorization/472-VALIDATION.md
---

# Plan 472-22: Six-finding integration, regression, and evidence gate

<objective>
Integrate Plans 17–21, independently reproduce closure of all six final Phase 472 findings, rerun the established security and generated-contract gates, and publish source-bound evidence without misrepresenting Phase 474/475 or unavailable external results.
</objective>

## Artifacts this phase produces

- Updated `472-VALIDATION.md` task map and actual command observations for Plans 17–22.
- Updated `phase-472-evidence.md` with CR-01, CR-02, WR-01..04 closure, exact commands/results/source SHA/digests, redacted representative outcomes, and explicit limitations.
- Regenerated route inventory and client action JSON only when their authoritative generators produce the checked content.

<must_haves>
Truths: all six findings close together with positive controls; earlier security contracts remain green; external/cross-phase limits remain honest. Artifacts: validation and source-bound evidence. Key link: evidence maps each final review finding through focused reproduction, full Phase 472 regression, generated contracts, and current source SHA.
</must_haves>

<threat_model>
ASVS L1 assets: release decision integrity, public identity ownership, all capability lineages, scalar and multi-target body authorization, account privacy, audit-key privacy, deterministic contracts, and evidence truthfulness. Trust boundaries: five implementation plans to combined tests; source/generators to checked artifacts; local doubles to external Cognito claims; Phase 472 results to Phase 474/475 ownership. Threats: fixes regress each other, blanket denial passes, a bulk command authorizes only its first/composite target, a later-target denial/audit failure still permits mutation, test filters omit a finding, route/client artifacts become stale, weak fixtures mask production validation, known failures are reassigned, external checks are inferred, or deferred teacher atomicity is claimed closed. Mitigations: named direct reproductions plus legitimate positives, registered-endpoint multi-target all-of/outage matrices, one combined no-skip gate, established 546-test family, deterministic generators, exact full-suite delta classification, source SHA/digests/timestamps, redaction canaries, explicit NOT RUN rows, and no production/provider mutation. Verification: CR-01/02 and WR-01..04 endpoint/state-machine probes, including collection order/partial/audit cases, terminology mutation gate, route/client checks, focused phase suite, full suite observation, and evidence inspection. Residual risk: six live Cognito checks can remain unavailable and block live rollout evidence even when local closure passes. Strict Settings fixture modernization remains Phase 474; teacher takeover/session/notification atomicity remains Phase 475/V9DATA-02.
</threat_model>

<tasks>

<task type="auto" id="472-22-01">
  <name>Run the combined six-finding adversarial closure gate</name>
  <files>tests/test_public_identity_lifecycle.py, tests/test_auth_account_lifecycle.py, tests/test_privileged_identity_reconciliation.py, tests/test_admin_authorization.py, tests/test_authorization_audit.py, tests/test_public_auth_error_boundary.py, tests/test_route_authorization_inventory.py</files>
  <read_first>
    - .planning/phases/472-privileged-identity-and-student-resource-authorization/472-REVIEW.md all six findings
    - .planning/phases/472-privileged-identity-and-student-resource-authorization/472-VERIFICATION.md direct probes and plan-ready closure
    - summaries and task verification output from Plans 472-17 through 472-21
    - all seven focused test modules named in this task
  </read_first>
  <action>Run one unfiltered local gate that independently exercises: provider-only and privileged existing-account registration zero-mutation plus legitimate command resume; already-confirmed resend command convergence and duplicate-email rejection; same-ID/different-coordinate grant revocation plus account restore non-revival; scalar typed body-target exact allow/wrong deny/distinct audit/outage; registered bulk/recovery/handoff/governance collection all-of authorization with first-allowed/later-denied, reversed order, duplicate, mixed-student, delimiter-collision, malformed/empty/over-limit, reference-only nested fields, and later-target audit-failure cases; forgot/reset known/unknown/disabled/privileged equivalence; and weak/default/duplicate/valid-rotation keyrings. For every collection negative, assert zero handler/service mutation for the whole command and redacted per-target evidence only; for positives, assert every member is authorized and durably evidenced before one effect. Require at least one legitimate positive for each feature family and mutation/effect sentinels so blanket denial cannot pass. No test may be skipped, xfailed, source-text-only, network-dependent, or weakened to accept prior behavior.</action>
  <verify>.venv/bin/python -m pytest -q tests/test_public_identity_lifecycle.py tests/test_auth_account_lifecycle.py tests/test_privileged_identity_reconciliation.py tests/test_admin_authorization.py tests/test_authorization_audit.py tests/test_public_auth_error_boundary.py tests/test_route_authorization_inventory.py</verify>
  <acceptance_criteria>
    - Every CR-01, CR-02, WR-01, WR-02, WR-03, and WR-04 named reproduction passes with a corresponding legitimate positive control.
    - Zero-mutation/effect assertions cover command/profile/binding/group/relationship/grant/admin service boundaries.
    - Multi-target registered endpoints cannot authorize only the first/composite target: every member has an all-of decision and durable redacted evidence before effects.
    - Later denial or later audit failure, in either input order, prevents the entire bulk/recovery/handoff/governance command from mutating.
    - Public responses and durable evidence contain no provider, account-state, raw target, email, key, token, content, object-key, or canary leakage.
    - Exactly one canonical role authorizes each Actor and active code uses `teacher` as the sole teacher-role vocabulary.
  </acceptance_criteria>
  <done>All six final findings close together under direct adversarial and positive local evidence.</done>
</task>

<task type="auto" id="472-22-02">
  <name>Run established Phase 472 regression and deterministic contract gates</name>
  <files>.planning/phases/472-privileged-identity-and-student-resource-authorization/472-VALIDATION.md, docs/security/route-authorization-inventory.json, docs/security/client-error-actions.json</files>
  <read_first>
    - .planning/phases/472-privileged-identity-and-student-resource-authorization/472-VALIDATION.md focused phase command and Plan 11–16 observations
    - scripts/generate_route_authorization_inventory.py
    - scripts/generate_client_error_actions.py
    - scripts/check_teacher_terminology.py
    - docs/security/tutor-term-allowlist.json exact historical/negative evidence allowlist
    - .planning/ROADMAP.md Phase 474 and 475 ownership boundaries
  </read_first>
  <action>Run the exact established focused Phase 472 gate from `472-VALIDATION.md`, extended only as necessary to include the seven final-finding modules from task 01. Run the semantic teacher terminology gate and its mutation controls. Regenerate route inventory and client actions twice, require byte-identical results and both `--check` commands, and update Validation with Plans 17–22, actual counts/times/status, source SHA, and artifact digests without rewriting historical Plan 01–16 evidence. Run the full suite as an observation. Require zero new Phase 472 failures; enumerate the exact Phase 474-owned strict Settings fixture set separately and do not edit, relax, absorb, or reassign it. Treat any additional failure as blocking.</action>
  <verify>.venv/bin/python -m pytest -q tests/test_auth_security.py tests/test_identity_authorization.py tests/test_client_error_actions.py tests/test_teacher_onboarding.py tests/test_teacher_terminology_gate.py tests/test_student_authorization_matrix.py tests/test_route_authorization_inventory.py tests/test_authorization_audit.py tests/test_public_auth_error_boundary.py tests/test_public_identity_lifecycle.py tests/test_notifications.py tests/test_websocket_notifications.py tests/test_admin_authorization.py tests/test_privileged_identity_reconciliation.py tests/test_provision_production_admin.py tests/test_auth_account_lifecycle.py tests/test_parent_children.py tests/test_questions.py tests/test_teacher_dispatch.py tests/test_adaptive_learning.py tests/test_curriculum_ops.py</verify>
  <acceptance_criteria>
    - The extended Phase 472 gate passes with all earlier issuer/client, one-role, invitation, capability, resource, outage, audit, and public-error controls intact.
    - Route inventory and client actions are byte-stable and match runtime/OpenAPI authoritative projections.
    - The semantic terminology gate consumes only exact historical/negative fixtures and rejects an injected active alias contract.
    - Full-suite result records exact failures and delta; no Phase 472 regression is hidden under Phase 474 ownership.
    - Validation maps Plans 17–22 while preserving every historical result and cross-phase boundary.
  </acceptance_criteria>
  <done>Earlier Phase 472 behavior and generated contracts remain green/deterministic with an honest full-suite delta.</done>
</task>

<task type="auto" id="472-22-03">
  <name>Publish source-bound closure and external-limitation evidence</name>
  <files>docs/security/phase-472-evidence.md</files>
  <read_first>
    - docs/security/phase-472-evidence.md existing G-01..G-05 format and limitation table
    - outputs from tasks 01-02
    - .planning/phases/472-privileged-identity-and-student-resource-authorization/472-VALIDATION.md Manual-Only Verifications
    - .planning/phases/472-privileged-identity-and-student-resource-authorization/472-USER-SETUP.md sandbox checklist
    - .planning/phases/472-privileged-identity-and-student-resource-authorization/472-REVIEW.md finding identifiers
  </read_first>
  <action>Update the evidence document with a final-review table for CR-01, CR-02, and WR-01..04. For each record the exact command, UTC timestamp, result/count, tested source SHA, redacted representative outcome, and applicable route/client artifact digest. Record the full-suite result and exact Phase 474 delta separately. Preserve all six external Cognito rows: unless separately approved configuration is actually available, mark each `NOT RUN — approval/configuration unavailable`; if approved, perform only the authorized non-production checks and record redacted IDs/cleanup. State that no AWS/network/provider/production mutation was performed by autonomous tasks and that Phase 475 teacher takeover atomicity remains open. Do not claim beta/production rollout approval.</action>
  <verify>.venv/bin/python -m pytest -q tests/test_public_identity_lifecycle.py tests/test_privileged_identity_reconciliation.py tests/test_admin_authorization.py tests/test_public_auth_error_boundary.py tests/test_authorization_audit.py && git diff --check</verify>
  <acceptance_criteria>
    - Evidence maps all six findings to current commands/results/SHA/digests and does not reuse stale Plan 16 counts as current proof.
    - Every external row is either an actually executed approved sandbox result or explicit NOT RUN, never an inferred pass.
    - Phase 474 fixture and Phase 475 transaction boundaries are explicit and not represented as Phase 472 fixes.
    - Evidence contains no password, token, raw provider subject/email/message, target/resource ID, student content, object key, audit key, or production identifier.
  </acceptance_criteria>
  <done>Release evidence is current, redacted, source-bound, and honest about every external/cross-phase limitation.</done>
</task>

</tasks>

<verification>
Run the combined six-finding gate, established/extended Phase 472 gate, terminology mutation gate, deterministic generators, and full-suite observation. Compare evidence to actual outputs/source SHA. External checks remain NOT RUN without separate approval; no production/provider mutation is implied.
</verification>

<success_criteria>
All six final Phase 472 blockers close together without regressing earlier controls, generated contracts are current, and evidence truthfully separates local closure from Phase 474/475 and unavailable live-provider work.
</success_criteria>
