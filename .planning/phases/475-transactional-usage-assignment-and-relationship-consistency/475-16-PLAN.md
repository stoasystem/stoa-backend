---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 16
type: execute
wave: 3
depends_on: [475-15]
files_modified:
  - src/stoa/jobs/reconcile_question_submissions.py
  - tests/test_phase475_question_reconciliation.py
autonomous: true
gap_closure: true
requirements: [V9DATA-01]
must_haves:
  truths:
    - "D-02: reconciliation accepts and emits only the opaque command digest; raw caller idempotency text never reappears in CLI/Lambda/job output."
  artifacts:
    - path: src/stoa/jobs/reconcile_question_submissions.py
      provides: "Opaque bounded reconciliation coordinate contract"
    - path: tests/test_phase475_question_reconciliation.py
      provides: "CLI/Lambda/output privacy and replay proof"
  key_links:
    - from: src/stoa/jobs/reconcile_question_submissions.py
      to: src/stoa/db/repositories/question_submission_repo.py
      via: "opaque command digest coordinate"
      pattern: "command.*digest"
---

<objective>
Close the reconciliation/output half of raw idempotency-key privacy per D-02.

Purpose: finish CR-08 outside the request path while preserving bounded explicit recovery.
Output: opaque CLI/Lambda coordinates and redacted reconciliation results.
</objective>

## Artifacts this phase produces

- Opaque `commandDigest` input/output contract for question reconciliation.
- CLI flag accepting only a 64-hex command digest rather than raw `--idempotency-key` text.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-03-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-15-SUMMARY.md
</context>

<tasks>

<task type="auto" id="475-16-01" tdd="true">
  <name>Make reconciliation coordinates opaque end to end</name>
  <files>src/stoa/jobs/reconcile_question_submissions.py, tests/test_phase475_question_reconciliation.py</files>
  <read_first>
    - src/stoa/jobs/reconcile_question_submissions.py coordinate model, Lambda parser, CLI flags, and result projection
    - src/stoa/db/repositories/question_submission_repo.py Plan 475-15 opaque digest validation and preview/apply signatures
    - tests/test_phase475_question_reconciliation.py CLI, Lambda, preview/apply, and output fixtures
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md CR-08 job-output evidence
  </read_first>
  <behavior>
    - CLI/Lambda reject raw or malformed coordinates and accept exactly one bounded student plus 64-hex command digest.
    - Preview/apply output contains opaque command/question identities and never the raw caller key canary.
  </behavior>
  <action>Replace public reconciliation names and parsing that invite raw `idempotencyKey`/`--idempotency-key` input with `commandDigest`/`--command-digest`, validate lowercase 64-hex before repository access, and pass that opaque value into the existing bounded preview/apply coordinate. Return only closed disposition, opaque `commandId`/question ID, versions, evidence digest, proposed action, and mutation count. Reject unknown fields and malformed coordinates without echoing supplied values. Preserve explicit single-coordinate execution and preview-by-default behavior; do not add an unbounded scan.</action>
  <acceptance_criteria>
    - `reconcile_question_submissions.py` has no public `idempotencyKey` or `--idempotency-key` input/output contract.
    - Malformed/raw-looking input fails before repository access and is absent from diagnostics.
    - Preview/apply/replay tests pass using the opaque digest and retain bounded single-coordinate behavior.
    - Job results contain no raw caller key, question text, provider result, or storage coordinate.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_question_reconciliation.py -k 'cli or lambda or privacy or preview or replay' && .venv/bin/ruff check src/stoa/jobs/reconcile_question_submissions.py tests/test_phase475_question_reconciliation.py</automated></verify>
  <done>CR-08 is closed across reconciliation: operational inputs and outputs use only the opaque durable command identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator/Lambda input to reconciliation | Operational coordinates can be logged or retained. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-16-01 | Information disclosure | reconciliation CLI/Lambda | mitigate | Accept and emit only validated opaque digests and closed fields. |
| T-475-16-02 | Denial of service | reconciliation discovery | mitigate | Keep one explicit bounded coordinate; no table scan. |
| T-475-SC | Tampering | dependency supply chain | accept | No package install is present. |
</threat_model>

<verification>
Run only the reconciliation CLI/Lambda/privacy/replay nodes plus Ruff.
</verification>

<success_criteria>
- No raw question idempotency input persists or appears in operational output.
- Bounded preview/apply recovery remains deterministic.
</success_criteria>

<output>Create `475-16-SUMMARY.md` when complete.</output>
