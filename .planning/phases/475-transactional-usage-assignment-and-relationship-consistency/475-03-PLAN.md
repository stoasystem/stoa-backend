---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 03
type: execute
wave: 3
depends_on: [475-01, 475-02]
files_modified:
  - src/stoa/db/repositories/question_submission_repo.py
  - src/stoa/services/usage_ledger_service.py
  - src/stoa/jobs/reconcile_question_submissions.py
  - tests/test_phase475_question_reconciliation.py
autonomous: true
requirements: [V9DATA-01]
must_haves:
  truths:
    - "D-01/D-03: recoverable submissions converge; only proven terminal failure restores question allowance and reverses the ledger."
    - "Successfully uploaded attachments remain durable library resources and storage quota is not refunded by question allowance reversal."
  artifacts:
    - path: src/stoa/jobs/reconcile_question_submissions.py
      provides: "Bounded preview/apply recovery for current and historical partial question state"
    - path: tests/test_phase475_question_reconciliation.py
      provides: "Recovery, terminal reversal, attachment-retention, and replay proof"
---

<objective>
Add bounded reconciliation and exact compensation for recoverable or historical question submissions.

Purpose: close partial records that predate the new transaction and finish the selected terminal-failure behavior.
Output: preview/apply reconciliation, processing recovery, one-time allowance/ledger reversal, and durable attachment preservation.
</objective>

## Artifacts this phase produces

- `QuestionReconciliationDisposition`, `preview_question_submission_reconciliation()`, `apply_question_submission_reconciliation()`, and `reverse_terminal_question_admission()`.
- CLI/job entry `src/stoa/jobs/reconcile_question_submissions.py` with `--preview` default and explicit `--apply`.
- New tests in `tests/test_phase475_question_reconciliation.py`.

<tasks>

<task type="auto" id="475-03-01">
  <name>Implement idempotent question reconciliation and terminal reversal</name>
  <files>src/stoa/db/repositories/question_submission_repo.py, src/stoa/services/usage_ledger_service.py, src/stoa/jobs/reconcile_question_submissions.py, tests/test_phase475_question_reconciliation.py</files>
  <read_first>
    - src/stoa/services/usage_ledger_service.py existing `reconcile_question_usage`
    - src/stoa/db/repositories/question_submission_repo.py admitted command state/version from Plans 475-01/02
    - src/stoa/db/repositories/attachment_repo.py durable attachment and association states
    - tests/test_usage_ledger.py current preview/apply reconciliation fixtures
    - tests/test_questions.py old partial-persistence case
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-01 and D-03
  </read_first>
  <action>Scan only bounded question command/ledger coordinates and classify committed, recoverable processing, legacy counter+ledger-without-question, and proven terminal failure. Preview performs zero writes and reports opaque command/question IDs, observed versions, and proposed action. Apply conditions every update on the previewed version/digest; changed rows are skipped. Recover a committed question/result when durable evidence exists. For a proven terminal command, atomically mark the command/question failed, decrement the exact question counter once without going below zero, and mark the ledger event `reversed` with reversal identity/time; do not delete or decrement storage for an already durable attachment. Repeated apply/reversal performs zero additional mutations. Unknown or conflicting legacy evidence remains report-only.</action>
  <acceptance_criteria>
    - Preview writes nothing and classifies all fixture states deterministically.
    - Apply repairs an unambiguous historical partial row and the second apply performs zero writes.
    - Terminal reversal decrements question usage exactly once and marks one ledger event reversed.
    - The attachment row/object and storage counter are byte-identical before and after question allowance reversal.
    - Changed-after-preview and conflicting histories are skipped/reported, never overwritten.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_question_reconciliation.py tests/test_usage_ledger.py && .venv/bin/ruff check src/stoa/jobs/reconcile_question_submissions.py src/stoa/db/repositories/question_submission_repo.py tests/test_phase475_question_reconciliation.py</automated></verify>
  <done>Recoverable and historical question states converge, while terminal compensation is exact, idempotent, and attachment-safe.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| Reconciler overwrites a live retry | Preview-bound version/digest conditions and changed-row skip |
| Compensation refunds twice | Durable reversal identity and conditional terminal transition |
| Question failure deletes reusable student content | Attachment/storage rows excluded from reversal transaction |
</threat_model>

<success_criteria>
- V9DATA-01 recovery and D-03 terminal compensation are independently reproducible and idempotent.
</success_criteria>

<output>Create `475-03-SUMMARY.md` after reconciliation tests pass.</output>
