---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stoa/services/rate_limit.py
  - src/stoa/routers/practice.py
  - tests/test_phase475_rate_limit.py
  - tests/test_conversations.py
  - tests/test_practice.py
autonomous: true
requirements: [V9DATA-04]
must_haves:
  truths:
    - "D-13: one admitted logical chat/hint request counts once; rejected and same-operation retried requests never increase the counter."
  artifacts:
    - path: src/stoa/services/rate_limit.py
      provides: "Capped idempotent logical-operation admission"
    - path: tests/test_phase475_rate_limit.py
      provides: "429 cap, concurrent final slot, provider retry, and day-boundary proof"
---

<objective>
Replace unconditional chat/hint increments with capped idempotent logical-operation admission.

Purpose: stop rejection amplification and provider-retry double counting.
Output: operation-bound counter transaction, typed outcomes, migrated call sites, and boundary tests.
</objective>

## Artifacts this phase produces

- `RateAdmissionDisposition`, `RateAdmissionResult`, `build_rate_operation_id()`, and `check_and_record_operation()`.
- Updated `check_and_record_chat()` and `check_and_record_hint()` requiring an operation identity.
- New test module `tests/test_phase475_rate_limit.py`.

<tasks>

<task type="auto" id="475-09-01">
  <name>Implement capped idempotent chat and hint admission</name>
  <files>src/stoa/services/rate_limit.py, src/stoa/routers/practice.py, tests/test_phase475_rate_limit.py, tests/test_conversations.py, tests/test_practice.py</files>
  <read_first>
    - src/stoa/services/rate_limit.py current `_increment_and_check`
    - src/stoa/routers/conversations.py message command/idempotency and chat counter call site
    - src/stoa/routers/practice.py hint request model/call site
    - src/stoa/db/repositories/attachment_repo.py message quota operation-row pattern
    - tests/test_phase473_message_command.py capped command claim and retry semantics
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-13
  </read_first>
  <action>Require a stable caller/domain operation ID for each logical chat or hint request. In one transaction put an operation row with owner, kind, UTC quota period, payload/resource digest, and admitted status, while updating the counter with `attribute_not_exists(count) OR count < :limit`. On contention, strongly read the operation: same digest returns the original admitted result without increment, changed digest is a structured conflict, and missing operation plus durable counter at limit returns 429. Preserve and regression-test the existing conversation message-command quota identity as the authoritative chat implementation; migrate the legacy `check_and_record_chat` adapter to the same operation contract without adding a second chat counter. Add an explicit hint idempotency field/identity rather than using challenge ID alone. An admitted request remains counted once if a downstream provider fails; retrying that same operation does not count again.</action>
  <acceptance_criteria>
    - Repeating 429 requests leaves the stored counter exactly equal to the configured limit.
    - Two concurrent distinct requests for the final slot produce one admitted and one rejected result.
    - Same-operation provider failure/retry remains one count; a distinct operation is evaluated separately.
    - Same key with changed digest conflicts without mutation.
    - UTC day change creates a new period/operation namespace without changing the prior day's row.
    - Existing conversation command and practice hint behavior remains functional.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_rate_limit.py tests/test_phase473_message_command.py tests/test_conversations.py tests/test_practice.py -k 'rate or quota or hint or message' && .venv/bin/ruff check src/stoa/services/rate_limit.py src/stoa/routers/practice.py tests/test_phase475_rate_limit.py</automated></verify>
  <done>Chat and hint frequency counters are capped and count each admitted logical request exactly once.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| Rejected retries inflate lockout | Conditional counter cap |
| Provider retry consumes another slot | Durable operation row and same-digest replay |
| Reused key changes request meaning | Payload/resource digest mismatch conflict |
</threat_model>

<success_criteria>
- V9DATA-04 and D-13 pass concurrent, retry, rejection, and day-boundary tests.
</success_criteria>

<output>Create `475-09-SUMMARY.md` after rate-limit and call-site tests pass.</output>
