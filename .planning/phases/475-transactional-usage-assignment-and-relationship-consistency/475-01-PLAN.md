---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stoa/db/repositories/question_submission_repo.py
  - src/stoa/services/usage_ledger_service.py
  - tests/test_phase475_question_admission.py
autonomous: true
requirements: [V9DATA-01]
must_haves:
  truths:
    - "D-02/D-04: one durable question submission identity binds one exact payload and admits quota, ledger, question, and attachment effects at most once."
    - "A same-payload retry returns the persisted command/result; a changed payload is a deterministic mismatch."
  artifacts:
    - path: src/stoa/db/repositories/question_submission_repo.py
      provides: "Atomic question-admission command and typed dispositions"
    - path: tests/test_phase475_question_admission.py
      provides: "Transaction-shape, concurrency, and ambiguous-timeout proof"
---

<objective>
Create the atomic lower-boundary primitive for one question admission.

Purpose: remove the counter-then-ledger-then-question partial-write sequence before changing route behavior.
Output: a durable payload-bound command, one capped transaction, typed replay outcomes, and lower-boundary tests.
</objective>

## Artifacts this phase produces

- New repository module `question_submission_repo.py`.
- `QuestionAdmissionDisposition`, `QuestionAdmissionResult`, `question_submission_fingerprint()`, `build_question_admission_transaction()`, `admit_question_submission()`, and `get_question_submission_command()`.
- `usage_ledger_service.build_question_usage_event()` as a pure item builder.
- New test module `tests/test_phase475_question_admission.py`.

<tasks>

<task type="auto" id="475-01-01">
  <name>Implement the atomic question-admission command</name>
  <files>src/stoa/db/repositories/question_submission_repo.py, src/stoa/services/usage_ledger_service.py, tests/test_phase475_question_admission.py</files>
  <read_first>
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-01 through D-04
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-RESEARCH.md durable command identity and question-admission findings
    - src/stoa/db/repositories/attachment_repo.py `build_question_attachment_transaction` and message-command disposition patterns
    - src/stoa/db/repositories/account_deletion_repo.py `transact`, serialization, and active-fence operations
    - src/stoa/db/repositories/question_repo.py `question_item` and current question keys
    - src/stoa/services/usage_ledger_service.py current question event schema
    - tests/test_phase473_message_command.py claim contention and commit-then-timeout analogs
  </read_first>
  <action>Create a question-specific command row keyed by student plus caller idempotency key, with a canonical SHA-256 fingerprint over normalized subject, exact UTF-8 original/corrected content, and ordered opaque attachment identity. Build one transaction containing the active account fence, command put, a single conditional counter update whose condition caps `count` below the resolved limit, one privacy-safe ledger put, one initial question put, and the existing prepared attachment operations when present. Persist deterministic question ID, quota period, counter value, ledger identity, attachment identity, created time, and `processing` command state. Classify same-fingerprint contention through a strong command read as ADMITTED/RESUME, changed fingerprint as PAYLOAD_MISMATCH, durable full counter as QUOTA_EXCEEDED, and nonconditional/ambiguous transport as RETRYABLE unless the strong read proves commit. Do not rely on DynamoDB's ten-minute client token as durable idempotency and do not place OCR or AI calls inside the transaction.</action>
  <acceptance_criteria>
    - The transaction contains one update for the question counter and never targets the same item twice.
    - Concurrent identical keys yield one admitted command, one question item, one ledger item, and one counter increment.
    - Same key with changed content returns PAYLOAD_MISMATCH before any new mutation.
    - A fake that commits then raises timeout is reconciled to RESUME; a pre-commit dependency failure is RETRYABLE.
    - Full quota returns QUOTA_EXCEEDED with the counter unchanged.
    - Ledger/event metadata contains no question text, OCR text, provider payload, or raw storage coordinate.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_question_admission.py && .venv/bin/ruff check src/stoa/db/repositories/question_submission_repo.py src/stoa/services/usage_ledger_service.py tests/test_phase475_question_admission.py</automated></verify>
  <done>The repository can atomically admit or replay one exact question submission without partial quota, ledger, question, or attachment state.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| Duplicate retry consumes paid usage twice | Durable command row, fingerprint, conditional counter update, and strong reread |
| Changed payload overwrites original result | Immutable fingerprint mismatch disposition |
| Transaction error leaks private coordinates | Typed redacted dispositions; raw cancellation reasons remain internal |
| Account deletion races question creation | Active account-fence condition in the same transaction |
</threat_model>

<success_criteria>
- V9DATA-01 has an executable atomic admission primitive with lower-boundary failure and concurrency proof.
</success_criteria>

<output>Create `475-01-SUMMARY.md` after the focused tests pass.</output>
