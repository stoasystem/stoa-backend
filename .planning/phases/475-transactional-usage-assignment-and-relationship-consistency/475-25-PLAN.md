---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 25
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stoa/db/repositories/account_deletion_repo.py
  - tests/test_phase475_deletion_discovery.py
autonomous: true
gap_closure: true
requirements: [V9DATA-02, V9DATA-03, V9DATA-07]
must_haves:
  truths:
    - "CR-10: strong base-table discovery finds exact cross-account parent, teacher, and actor identity references instead of certifying a false clean epoch."
    - "Discovery remains bounded, paginated, strongly consistent, exact-match only, and requires two later clean full-table epochs."
  artifacts:
    - path: src/stoa/db/repositories/account_deletion_repo.py
      provides: "Closed account identity-reference discovery registry"
    - path: tests/test_phase475_deletion_discovery.py
      provides: "Exact field, pagination, late-write, and two-clean-epoch discovery proof"
  key_links:
    - from: src/stoa/db/repositories/account_deletion_repo.py
      to: src/stoa/services/account_deletion_service.py
      via: "scan_owned_private_rows feeds every entity cleanup branch"
      pattern: "parent_id|teacher_id|actor_id"
---

<objective>
Close the CR-10 discovery blind spot without claiming a CONTEXT decision mapping.

Purpose: make parent, teacher, and notification identity references visible to the entity-specific cleanup plans that follow.
Output: one auditable exact-match discovery registry and focused strong-scan regression module.
</objective>

## Artifacts this phase produces

- A closed scalar/nested identity-reference registry for formal relationship, teacher question/session, and notification actor/metadata rows.
- A discovery test matrix that proves pagination and two later clean epochs cannot hide a late cross-account row.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-VERIFICATION.md
</context>

<tasks>

<task type="auto" id="475-25-01" tdd="true">
  <name>Discover every reviewed cross-account identity reference</name>
  <files>src/stoa/db/repositories/account_deletion_repo.py, tests/test_phase475_deletion_discovery.py</files>
  <read_first>
    - src/stoa/db/repositories/account_deletion_repo.py `scan_owned_private_rows`, `_targets_user`, and cursor validation
    - src/stoa/services/account_deletion_service.py `_run_base_branch` two-clean-epoch contract
    - src/stoa/db/repositories/user_repo.py formal forward/reverse binding schema
    - src/stoa/db/repositories/question_repo.py question and teacher-session schemas
    - src/stoa/db/repositories/notification_repo.py notification event/metadata schema
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md CR-10
  </read_first>
  <behavior>
    - Exact `parent_id`, `teacher_id`, or `actor_id` scalar references and the reviewed notification metadata spellings select the row.
    - Similar substrings, unrelated free text, and an identifier nested outside the closed registry do not select a row.
    - A late matching row between clean passes resets progress; completion occurs only after two later full strongly consistent clean epochs.
  </behavior>
  <action>Replace `_targets_user`'s partial ad hoc checks with a closed, entity-aware registry covering the exact persisted fields used by formal parent forward/reverse rows, teacher question/session rows, and notification actor/metadata rows. Keep exact normalized identifier equality; do not recursively scan arbitrary nested payloads or use substring matching. Preserve strong base-table pagination, repeating-cursor rejection, and bounded scans. Build the new test module first with a parameterized positive/negative field matrix plus cursor and late-write cases. This plan discovers CR-10 targets only; entity mutation belongs to 475-26 through 475-28.</action>
  <acceptance_criteria>
    - Every reviewed cross-account identity field has an explicit registry entry and positive test.
    - Near matches and unregistered payload text remain unselected.
    - Every scan request is strongly consistent and cursor-bounded.
    - Two later clean epochs are required after the last discovered row.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_deletion_discovery.py::test_cross_account_identity_registry_and_two_clean_epochs && .venv/bin/ruff check src/stoa/db/repositories/account_deletion_repo.py tests/test_phase475_deletion_discovery.py</automated></verify>
  <done>CR-10 discovery is closed and supplies deterministic inputs to each entity-level cleanup plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Heterogeneous table rows to deletion discovery | Other accounts own rows that still reference the deleting identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-25-01 | Information disclosure | identity discovery | mitigate | Closed exact field registry covers reviewed cross-account references. |
| T-475-25-02 | Tampering | clean-epoch seal | mitigate | Strong pagination and two later complete clean epochs remain mandatory. |
| T-475-SC | Tampering | dependency supply chain | accept | No package installation or manifest change. |
</threat_model>

<verification>Run the exact discovery node, then the inherited account-deletion seal tests.</verification>

<success_criteria>
- CR-10 rows cannot be invisible because they are owned by another account.
- No deferred billing, Web, native, AWS, provider, or deployment scope is introduced.
</success_criteria>

<output>Create `475-25-SUMMARY.md` when complete.</output>
