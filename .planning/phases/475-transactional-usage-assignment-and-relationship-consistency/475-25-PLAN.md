---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 25
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stoa/db/repositories/account_deletion_repo.py
  - tests/test_phase475_deletion_discovery.py
autonomous: true
gap_closure: true
requirements: [V9DATA-05, V9DATA-06, V9DATA-07]
must_haves:
  truths:
    - "D-12/D-13/D-14: strong deletion discovery finds every row owned by or referencing the account, including parent, teacher, and actor reference fields."
    - "Completion still requires two consecutive strongly consistent clean discovery epochs."
  artifacts:
    - path: src/stoa/db/repositories/account_deletion_repo.py
      provides: "Closed account ownership/reference predicate"
    - path: tests/test_phase475_deletion_discovery.py
      provides: "Per-field positive/negative discovery and two-clean-epoch proof"
  key_links:
    - from: src/stoa/db/repositories/account_deletion_repo.py
      to: "base-table strong scan"
      via: "closed `_targets_user` ownership/reference classifier"
      pattern: "parent_id|teacher_id|actor_id"
---

<objective>
Close deletion discovery over all account reference fields per D-12, D-13, and D-14.

Purpose: close CR-10 so deletion cannot certify completion while parent-, teacher-, or actor-referenced records remain.
Output: an auditable closed field predicate and two-clean-epoch regression tests.
</objective>

## Artifacts this phase produces

- Explicit deletion-target field registry covering owner, subject, parent, student, teacher, and actor references.
- Discovery tests that place the target in every field independently and reject similar non-target values.
- Completion test retaining the required two consecutive strong clean epochs.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-VERIFICATION.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
</context>

<tasks>

<task type="auto" id="475-25-01" tdd="true">
  <name>Complete the strong deletion ownership and reference predicate</name>
  <files>src/stoa/db/repositories/account_deletion_repo.py, tests/test_phase475_deletion_discovery.py</files>
  <read_first>
    - src/stoa/db/repositories/account_deletion_repo.py `_targets_user`, strong discovery scan, continuation handling, and clean-epoch completion
    - tests/test_phase473_account_deletion.py existing sweep behavior and fake table patterns
    - tests/test_phase473_account_deletion_seal.py completion/seal contract
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-12 through D-14
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md CR-10
  </read_first>
  <behavior>
    - A row is discovered when the target appears in each supported account ownership/reference field, including `parent_id`, `teacher_id`, and `actor_id`.
    - Similar text in unrelated fields or another account's exact identifier is not a match.
    - One clean strong scan cannot complete deletion; a second consecutive clean epoch can.
  </behavior>
  <action>Replace the partial ad hoc `_targets_user` checks with one explicit closed registry/predicate for all persisted account ownership and reference field spellings used by the backend, adding at minimum `parent_id`, `teacher_id`, and `actor_id` while retaining existing owner/student/user/subject keys and encoded primary-key ownership rules. Match exact normalized identifier values, not substrings or arbitrary nested text. Create a focused test module with a parameterized positive case for each supported field, negative near-match cases, pagination/continuation coverage, and a late-write-between-epochs case proving the existing two-consecutive-clean strong-scan invariant is preserved.</action>
  <acceptance_criteria>
    - The classifier explicitly includes parent, teacher, and actor reference fields.
    - Tests fail if any registered field is removed from discovery.
    - False-positive near matches are rejected.
    - Completion requires two consecutive strongly consistent clean epochs even with pagination and late writes.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_deletion_discovery.py tests/test_phase473_account_deletion.py tests/test_phase473_account_deletion_seal.py && .venv/bin/ruff check src/stoa/db/repositories/account_deletion_repo.py tests/test_phase475_deletion_discovery.py</automated></verify>
  <done>CR-10 is closed without weakening the D-12..D-14 strong-discovery and two-clean-epoch contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Persisted multi-entity rows to deletion certifier | Heterogeneous records encode account ownership in different fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-25-01 | Information disclosure | account deletion | mitigate | Closed exact-match reference registry prevents retained account-linked rows. |
| T-475-25-02 | Tampering | clean-epoch seal | mitigate | Two consecutive strong clean scans remain mandatory. |
| T-475-SC | Tampering | dependency supply chain | accept | No package or dependency manifest changes. |
</threat_model>

<verification>
Run the focused field-matrix suite plus the pre-existing account-deletion and seal suites.
</verification>

<success_criteria>
- No supported account relationship field can evade strong deletion discovery.
- D-12/D-13/D-14, V9DATA-05..07, and CR-10 have regression coverage.
</success_criteria>

<output>Create `475-25-SUMMARY.md` when complete.</output>
