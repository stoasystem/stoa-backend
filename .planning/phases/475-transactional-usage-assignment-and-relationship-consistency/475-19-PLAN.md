---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 19
type: execute
wave: 5
depends_on: [475-18]
files_modified:
  - src/stoa/db/repositories/question_submission_repo.py
  - src/stoa/routers/questions.py
  - tests/test_phase475_question_replay.py
autonomous: true
gap_closure: true
requirements: [V9DATA-01]
must_haves:
  truths:
    - "D-02/D-04: one strict replay entry validates command schema/state/owner/generation/digest/fingerprint/question identity and the strongly loaded question owner before returning private content."
  artifacts:
    - path: src/stoa/db/repositories/question_submission_repo.py
      provides: "Single strict replay classification and projection input"
    - path: tests/test_phase475_question_replay.py
      provides: "Foreign/corrupt/legacy/generation mismatch denial matrix"
  key_links:
    - from: src/stoa/routers/questions.py
      to: src/stoa/db/repositories/question_submission_repo.py
      via: "all preflight and attachment-race replay calls"
      pattern: "classify_.*replay"
---

<objective>
Make all question replay integrity- and ownership-safe per D-02/D-04.

Purpose: close CR-02 so corrupted or cross-student durable coordinates can never project another student's question.
Output: one strict replay API and adversarial route tests.
</objective>

## Artifacts this phase produces

- `classify_question_submission_replay(...)` strict repository entry point.
- Corrupt command, legacy schema, stale generation, foreign question, and changed fingerprint route tests.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-18-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
</context>

<tasks>

<task type="auto" id="475-19-01" tdd="true">
  <name>Route every question replay through strict classification</name>
  <files>src/stoa/db/repositories/question_submission_repo.py, src/stoa/routers/questions.py, tests/test_phase475_question_replay.py</files>
  <read_first>
    - src/stoa/routers/questions.py `_project_question_admission`, pre-reservation command branch, and attachment-reservation exception branch
    - src/stoa/db/repositories/question_submission_repo.py `_classify_command`, command key/schema, effect receipt, and active fence helpers
    - src/stoa/db/repositories/question_repo.py `get_question` returned shape
    - tests/test_phase475_question_replay.py current direct projection and route fixtures
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md CR-02
  </read_first>
  <behavior>
    - A command pointing at another student's real question is rejected indistinguishably from corrupt/unavailable replay and returns no question bytes.
    - Exact schema, digest, fingerprint, status, generation, question ID, and owner replay returns only the original processing/final result.
  </behavior>
  <action>Expose one repository replay classifier that strongly reads the command and current account fence, validates PK/SK/entity type/schema version, canonical student, opaque digest, payload fingerprint, allowed state, positive command version, exact account generation, and nonempty question identity, then strongly loads the question and validates its PK/SK/question ID/student owner/generation/schema/status against the command. Use it from every route preflight, attachment-reservation race, admission contention, and result projection branch; remove local fingerprint-only shortcuts and never trust `command.question_id` alone. Map mismatched payload to the existing safe new-submission 409 only when the command itself is otherwise authoritative; map corrupt/foreign/stale dependency state to the structured retry/unavailable contract with no private content or coordinate disclosure.</action>
  <acceptance_criteria>
    - All replay branches call the one strict repository entry point and no route branch constructs RESUME from a fingerprint comparison alone.
    - Foreign student/question, stale generation, malformed schema/version/status, wrong digest/key, and mismatched question identity return no question content.
    - Exact processing and completed replay still return the original durable question/result without provider or admission effects.
    - Changed payload on one valid authoritative command remains a structured D-04 mismatch.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_question_replay.py -k 'foreign or owner or schema or generation or replay or mismatch' && .venv/bin/ruff check src/stoa/db/repositories/question_submission_repo.py src/stoa/routers/questions.py tests/test_phase475_question_replay.py</automated></verify>
  <done>CR-02 is closed: no question replay can bypass strict command and loaded-question integrity/ownership checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Durable command to private question projection | Stored coordinates may be stale, corrupt, or foreign. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-19-01 | Information disclosure | replay projection | mitigate | Strong command/fence/question validation before returning any content. |
| T-475-19-02 | Tampering | stored command coordinates | mitigate | Validate closed schema, opaque digest, fingerprint, identity, generation, state, and versions. |
| T-475-SC | Tampering | dependency supply chain | accept | No package installation. |
</threat_model>

<verification>
Run the adversarial replay matrix and Ruff; retain structured/redacted errors.
</verification>

<success_criteria>
- CR-02 and strict replay portion of failed Truth 1 are closed.
- Foreign and random real identifiers remain indistinguishable at the public boundary.
</success_criteria>

<output>Create `475-19-SUMMARY.md` when complete.</output>
