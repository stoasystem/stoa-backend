---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 21
type: execute
wave: 4
depends_on: ["475-17"]
files_modified:
  - src/stoa/db/repositories/question_repo.py
  - src/stoa/routers/teachers.py
  - tests/test_phase475_teacher_takeover.py
autonomous: true
gap_closure: true
requirements: [V9DATA-02]
must_haves:
  truths:
    - "CR-04: teacher takeover commits only while the authenticated canonical `teacher` account remains active and unchanged."
    - "A suspended, deleted, role-changed, or concurrently edited teacher cannot win a takeover race."
  artifacts:
    - path: src/stoa/db/repositories/question_repo.py
      provides: "Teacher lifecycle/profile-version transaction fence"
    - path: tests/test_phase475_teacher_takeover.py
      provides: "Role, status, version, and suspension race proof"
  key_links:
    - from: src/stoa/routers/teachers.py
      to: src/stoa/db/repositories/question_repo.py
      via: "authorized teacher observation passed into the takeover transaction"
      pattern: "teacher.*profile.*version"
---

<objective>
Fence teacher takeover against the authoritative teacher account to close CR-04.

Purpose: close CR-04 so authorization checked before the write cannot become stale before ownership is committed.
Output: a transactional teacher account condition and adversarial lifecycle/version race tests.
</objective>

## Artifacts this phase produces

- Teacher-account key, canonical role/status, and profile-version conditions in the takeover transaction.
- Tests proving only an active `teacher` with the observed version can claim a question.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-VERIFICATION.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-17-SUMMARY.md
</context>

<tasks>

<task type="auto" id="475-21-01" tdd="true">
  <name>Add the active canonical-teacher transaction fence</name>
  <files>src/stoa/db/repositories/question_repo.py, src/stoa/routers/teachers.py, tests/test_phase475_teacher_takeover.py</files>
  <read_first>
    - src/stoa/routers/teachers.py `takeover`, `teacher_facts`, and current structured takeover errors
    - src/stoa/db/repositories/question_repo.py `claim_teacher_takeover` and transaction construction
    - tests/test_phase475_teacher_takeover.py in-memory transaction interpreter and takeover cases
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md CR-04
  </read_first>
  <behavior>
    - An active account whose canonical role is exactly `teacher` and profile version equals the authorized observation can take over once.
    - Suspension, deletion, canonical role change, or profile-version change between authorization and commit cancels the whole transaction.
    - Rejected races leave question owner, status/version, dispatch ownership, and notifications unchanged.
  </behavior>
  <action>Extend the takeover transaction with a `ConditionCheck` on the teacher profile row keyed by the authenticated principal. Require canonical `role = teacher`, active/non-deleted lifecycle state, and the profile version observed by the authorization facts; do not recognize aliases such as `tutor`, `instructor`, or pluralized roles. Pass the teacher account key and observed profile version from `teacher_facts` into the repository call. Map conditional failure to the existing structured/redacted takeover response without identifiers or storage details. Expand the in-memory transaction test adapter so the test exercises the real multi-item condition rather than a mocked repository result, and assert total rollback under each lifecycle race.</action>
  <acceptance_criteria>
    - The authoritative teacher profile participates in the same transaction as question/dispatch ownership.
    - Role comparison accepts only `teacher`; suspended, deleted, inactive, or version-stale profiles fail closed.
    - No failed teacher fence creates a notification or mutates any takeover artifact.
    - Public failures remain structured and coordinate-free.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_teacher_takeover.py && .venv/bin/ruff check src/stoa/db/repositories/question_repo.py src/stoa/routers/teachers.py tests/test_phase475_teacher_takeover.py</automated></verify>
  <done>CR-04 is closed with production and test evidence that takeover is atomic with active canonical-teacher authorization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authorized request to DynamoDB transaction | A previously valid teacher identity may change before commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-21-01 | Elevation of privilege | teacher takeover | mitigate | Canonical role, active lifecycle, and profile version are checked inside the ownership transaction. |
| T-475-21-02 | Tampering | concurrent account update | mitigate | Any stale observation cancels all transaction writes. |
| T-475-SC | Tampering | dependency supply chain | accept | No package or dependency manifest changes. |
</threat_model>

<verification>
Run the complete takeover suite and inspect the transaction request for a teacher-profile condition in the same `TransactWriteItems` call.
</verification>

<success_criteria>
- Only an unchanged active `teacher` can atomically claim work.
- V9DATA-02 and CR-04 are directly testable; D-08 remains the separate loser-identity concealment contract proved by the existing takeover suite.
</success_criteria>

<output>Create `475-21-SUMMARY.md` when complete.</output>
