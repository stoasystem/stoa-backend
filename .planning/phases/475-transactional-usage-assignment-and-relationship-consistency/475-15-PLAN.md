---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 15
type: execute
wave: 2
depends_on: [475-14]
files_modified:
  - src/stoa/db/repositories/question_submission_repo.py
  - src/stoa/services/usage_ledger_service.py
  - src/stoa/routers/questions.py
  - tests/test_phase475_question_admission.py
  - tests/test_phase475_question_replay.py
autonomous: true
gap_closure: true
requirements: [V9DATA-01]
must_haves:
  truths:
    - "D-02/D-04: raw caller idempotency text crosses the API boundary once, then only an opaque domain-separated digest reaches durable keys, rows, ledger metadata, responses, or diagnostics."
  artifacts:
    - path: src/stoa/db/repositories/question_submission_repo.py
      provides: "Opaque question command identity derivation and strict persisted schema"
    - path: tests/test_phase475_question_admission.py
      provides: "Arbitrary raw-key non-persistence and non-disclosure proof"
  key_links:
    - from: src/stoa/routers/questions.py
      to: src/stoa/db/repositories/question_submission_repo.py
      via: "question_submission_command_digest before admission/preflight"
      pattern: "question_submission_command_digest"
---

<objective>
Replace raw question idempotency persistence with one opaque durable identity per D-02/D-04.

Purpose: close CR-08 across command keys, command fields, usage ledger identity, and route replay without weakening exact payload mismatch detection.
Output: an opaque digest contract and arbitrary-canary privacy tests.
</objective>

## Artifacts this phase produces

- `question_submission_command_digest(student_id, caller_idempotency_key)`.
- Persisted `idempotency_digest` / opaque command identity schema; no raw caller key field.
- Tests that use an arbitrary email/question canary and inspect every transaction item, response, and emitted diagnostic.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-01-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-14-SUMMARY.md
</context>

<tasks>

<task type="auto" id="475-15-01" tdd="true">
  <name>Persist only an opaque question command identity</name>
  <files>src/stoa/db/repositories/question_submission_repo.py, src/stoa/services/usage_ledger_service.py, src/stoa/routers/questions.py, tests/test_phase475_question_admission.py, tests/test_phase475_question_replay.py</files>
  <read_first>
    - src/stoa/db/repositories/question_submission_repo.py command key, `_classify_command`, transaction builder, and admission command construction
    - src/stoa/services/usage_ledger_service.py `build_question_usage_event` and question event SK/event/idempotency fields
    - src/stoa/routers/questions.py preflight, usage-event, and admission arguments
    - tests/test_phase475_question_admission.py captured transaction operations
    - tests/test_phase475_question_replay.py public replay fixtures
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md CR-08
  </read_first>
  <behavior>
    - The same student and raw caller key derive the same lowercase 64-hex command digest; another student or key derives a different digest.
    - No persisted item, key, ledger field, route response, exception, or private event contains the arbitrary raw canary.
  </behavior>
  <action>Add a domain-separated, length-prefixed SHA-256 identity over canonical student ID and the validated raw caller key. Hash at the route boundary before preflight/admission, and make repository/ledger operations accept only the 64-hex opaque digest. Use the digest for `QUESTION_SUBMISSION#...`, `command_id`, ledger SK/event ID/idempotency metadata, and replay lookup; persist `idempotency_digest` rather than the raw key and make `_classify_command` reject malformed/legacy schema without echoing coordinates. Keep the separate payload fingerprint unchanged so same-key edited content remains a structured mismatch. Add a source/runtime canary assertion spanning captured transaction operations, command/ledger rows, public body, and diagnostics; do not rely on a fixed denylist alone.</action>
  <acceptance_criteria>
    - Durable command and ledger keys/fields contain one validated 64-hex digest and never contain raw caller key bytes.
    - Cross-student reuse of the same caller string produces a different durable identity.
    - Same-student same-key replay and changed-payload mismatch remain deterministic.
    - Arbitrary private-looking caller input is absent from captured operations, result bodies, logs, and exceptions.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_question_admission.py tests/test_phase475_question_replay.py -k 'privacy or idempotency or replay or mismatch' && .venv/bin/ruff check src/stoa/db/repositories/question_submission_repo.py src/stoa/services/usage_ledger_service.py src/stoa/routers/questions.py tests/test_phase475_question_admission.py tests/test_phase475_question_replay.py</automated></verify>
  <done>CR-08 is closed: raw question idempotency keys are API-only inputs and all durable/observable identities are opaque.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| API to DynamoDB/ledger | Caller-controlled text becomes a long-lived operation coordinate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-15-01 | Information disclosure | command and ledger identities | mitigate | Persist a domain-separated digest only and scan arbitrary canaries across all outputs. |
| T-475-15-02 | Tampering | replay identity | mitigate | Bind digest to canonical student ID and retain the independent exact payload fingerprint. |
| T-475-SC | Tampering | dependency supply chain | accept | No dependency or package installation occurs. |
</threat_model>

<verification>
Run lower-boundary transaction privacy tests and route replay tests, then Ruff the complete changed set.
</verification>

<success_criteria>
- CR-08 is covered by dynamic non-disclosure assertions, not a static known-value denylist.
- D-02/D-04 replay behavior remains intact with opaque durable coordinates.
</success_criteria>

<output>Create `475-15-SUMMARY.md` when complete.</output>
