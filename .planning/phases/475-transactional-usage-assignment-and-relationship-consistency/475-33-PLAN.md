---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 33
type: execute
wave: 8
depends_on: ["475-27", "475-28", "475-29", "475-30", "475-31", "475-32"]
files_modified:
  - scripts/verify_phase475.py
  - tests/test_phase475_evidence_verifier.py
autonomous: true
gap_closure: true
requirements: [V9DATA-01, V9DATA-02, V9DATA-03, V9DATA-04, V9DATA-05, V9DATA-06, V9DATA-07, V9DATA-08]
must_haves:
  truths:
    - "The Phase 475 evidence gate fails whenever mypy exits nonzero, regardless of changed-line classification."
    - "A PASS receipt records exit code zero and zero diagnostics for the complete immutable candidate runtime-file inventory."
  artifacts:
    - path: scripts/verify_phase475.py
      provides: "Fail-closed candidate-bound mypy gate"
    - path: tests/test_phase475_evidence_verifier.py
      provides: "Nonzero-exit, hidden-diagnostic, registry-drift, and clean-pass tests"
  key_links:
    - from: scripts/verify_phase475.py
      to: ".venv/bin/mypy"
      via: "candidate-derived complete runtime file argv and direct process exit disposition"
      pattern: "tool_exit_code.*0"
---

<objective>
Make Phase 475 type evidence fail closed on the real mypy process outcome.

Purpose: close WR-02 after plans 475-27 through 475-32 make the runtime inventory genuinely type-clean.
Output: an unfiltered source-bound mypy gate and adversarial verifier tests.
</objective>

## Artifacts this phase produces

- `MYPY-PHASE475` gate whose PASS requires tool exit code zero and no diagnostics.
- Verifier tests proving pre-existing, unchanged-line, malformed, and unparseable nonzero outcomes all fail.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-27-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-28-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-29-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-30-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-31-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-32-SUMMARY.md
</context>

<tasks>

<task type="auto" id="475-33-01" tdd="true">
  <name>Reject every nonzero mypy execution in checked evidence</name>
  <files>scripts/verify_phase475.py, tests/test_phase475_evidence_verifier.py</files>
  <read_first>
    - scripts/verify_phase475.py `_phase_runtime_files`, `_changed_lines`, `targeted_mypy`, `gate_registry`, receipt validation, and evidence rendering
    - tests/test_phase475_evidence_verifier.py current changed-line mypy tests and receipt tamper tests
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md WR-02
  </read_first>
  <behavior>
    - Mypy exit zero with zero diagnostics over the exact candidate runtime inventory produces PASS.
    - Any nonzero exit produces FAIL, including diagnostics outside changed lines, output with no parsed diagnostic, timeout/tool failure, or truncated/malformed output.
    - Missing, extra, duplicate, or reordered runtime registry input is rejected before execution/receipt acceptance.
  </behavior>
  <action>Remove changed-line diagnostic forgiveness from the mypy disposition. Retain candidate-derived runtime inventory equality and execute mypy once over all files, but make PASS require process return code zero and zero parsed diagnostics; preserve raw output only inside private capture artifacts and publish counts/hashes, not source lines. Rename the gate and result fields so they do not claim changed-line semantics. Reject tool exceptions, unexpected nonzero output, registry drift, and receipt/result drift. Update verifier unit tests with simulated unchanged-line diagnostics, nonzero/no-diagnostic output, malformed output, registry changes, and a true exit-zero result. Do not add exclusions or change mypy configuration.</action>
  <acceptance_criteria>
    - `targeted_mypy` cannot return PASS when `tool_exit_code != 0` or diagnostics exist.
    - The gate covers exactly every `.py` runtime file changed from `PHASE_BASE_SHA` to the candidate.
    - Public JSON/Markdown contains safe counts and hashes but no diagnostic source text or private values.
    - The real candidate runtime inventory exits mypy zero.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_evidence_verifier.py -k 'mypy' && candidate=$(git rev-parse HEAD) && files=$(git diff --name-only 901cb26626cb0f06b7f51a72b95e04aa4f7f4ebf "$candidate" -- src/stoa | rg '\.py$') && .venv/bin/mypy $files</automated></verify>
  <done>WR-02 is closed: checked evidence cannot report type PASS over any nonzero mypy execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Mypy subprocess to evidence receipt | Tool output and exit status are untrusted evidence inputs. |
| Candidate Git tree to file registry | An incomplete registry could hide affected runtime files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-33-01 | Repudiation | mypy receipt | mitigate | PASS requires real exit zero and zero diagnostics. |
| T-475-33-02 | Tampering | runtime file registry | mitigate | Exact candidate-derived inventory equality is enforced. |
| T-475-33-03 | Information disclosure | public evidence | mitigate | Publish counts/hashes only; raw output remains private capture data. |
| T-475-SC | Tampering | dependency supply chain | accept | No package or manifest changes. |
</threat_model>

<verification>
Run adversarial mypy verifier tests, then the unfiltered real mypy command over the candidate runtime inventory.
</verification>

<success_criteria>
- A nonzero mypy process can never be represented as PASS.
- All eight V9DATA requirements are checked against a genuinely type-clean candidate.
</success_criteria>

<output>Create `475-33-SUMMARY.md` when complete.</output>
