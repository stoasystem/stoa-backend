---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 17
type: execute
wave: 3
depends_on: [475-15]
files_modified:
  - src/stoa/db/repositories/question_repo.py
  - src/stoa/routers/questions.py
  - src/stoa/routers/teachers.py
  - src/stoa/services/teacher_dispatch_service.py
  - tests/test_phase475_question_state_cas.py
autonomous: true
gap_closure: true
requirements: [V9DATA-01, V9DATA-02]
must_haves:
  truths:
    - "D-01/D-05/D-07: every question writer compares the observed version and allowed source state, increments version once, and loses rather than overwriting a newer takeover or recovery transition."
  artifacts:
    - path: src/stoa/db/repositories/question_repo.py
      provides: "Closed state/version CAS mutation boundary for every question writer"
    - path: tests/test_phase475_question_state_cas.py
      provides: "Writer registry and AI/takeover interleaving proof"
  key_links:
    - from: src/stoa/routers/questions.py
      to: src/stoa/db/repositories/question_repo.py
      via: "observed snapshot and allowed-source transition"
      pattern: "update_.*question.*cas|expected_version"
---

<objective>
Make every question mutation versioned and state-constrained per D-01, D-05, and D-07.

Purpose: close CR-03 before provider completion and teacher lifecycle fencing build on the shared row.
Output: one typed CAS API, migrated writers, and adversarial interleaving tests.
</objective>

## Artifacts this phase produces

- `QuestionMutationDisposition` / `QuestionMutationResult` and a single state/version CAS mutation entry point.
- Closed question-writer registry covering OCR, AI completion, escalation/dispatch, feedback, teacher reply, and resolve.
- `tests/test_phase475_question_state_cas.py`.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-04-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-VERIFICATION.md
</context>

<tasks>

<task type="auto" id="475-17-01" tdd="true">
  <name>Fence every question writer with state and version CAS</name>
  <files>src/stoa/db/repositories/question_repo.py, src/stoa/routers/questions.py, src/stoa/routers/teachers.py, src/stoa/services/teacher_dispatch_service.py, tests/test_phase475_question_state_cas.py</files>
  <read_first>
    - src/stoa/db/repositories/question_repo.py `build_question_update_transaction`, `update_status`, `update_status_conditionally`, and takeover version update
    - src/stoa/routers/questions.py OCR, AI, request-teacher, and feedback writers
    - src/stoa/routers/teachers.py reply and resolve writers
    - src/stoa/services/teacher_dispatch_service.py dispatch/status writers
    - tests/test_phase475_teacher_takeover.py current takeover version assertions
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md CR-03
  </read_first>
  <behavior>
    - A write from version N succeeds only from its declared source status and stores version N+1.
    - AI/OCR/dispatch/reply/resolve work computed from an older snapshot cannot overwrite a committed takeover or later transition.
  </behavior>
  <action>Replace unconstrained `update_status` behavior with one typed repository mutation that requires the caller's observed question identity, student owner, positive version (including an explicit legacy-version initialization path), and a closed allowed-source-status set. Put status/version checks and the student active fence in the same transaction; update status/owned fields and increment version exactly once. Return APPLIED, STALE, INVALID_TRANSITION, or RETRYABLE after a strong reread without exposing coordinates. Migrate OCR enrichment, AI completion, student escalation, dispatch, feedback, teacher reply, and resolve to pass the snapshot/version observed before their work and the exact allowed transition. A stale same-state metadata writer must not replay against `teacher_active` or `resolved`. Add an AST/source-backed writer registry and barrier tests for AI completion versus takeover in both commit orders.</action>
  <acceptance_criteria>
    - Every production question Update contains owner, current status, and current version conditions plus one version increment.
    - No production call uses an unconstrained question status writer.
    - AI-first permits later takeover from the refreshed allowed state; takeover-first makes stale AI/OCR completion lose with owner/session unchanged.
    - Escalation, dispatch, feedback, reply, and resolve preserve their documented allowed state transitions and reject stale snapshots.
    - The writer registry fails on any new direct question mutation outside the reviewed CAS/takeover primitives.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_question_state_cas.py tests/test_phase475_teacher_takeover.py tests/test_teacher_dispatch.py tests/test_teacher_reply_sla.py && .venv/bin/ruff check src/stoa/db/repositories/question_repo.py src/stoa/routers/questions.py src/stoa/routers/teachers.py src/stoa/services/teacher_dispatch_service.py tests/test_phase475_question_state_cas.py</automated></verify>
  <done>CR-03 is closed: all question writers lose safely on stale state/version and cannot overwrite teacher takeover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Provider/worker result to question row | Delayed work crosses a concurrent ownership state machine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-17-01 | Tampering | question status writers | mitigate | Exact owner/status/version CAS and closed transition sets. |
| T-475-17-02 | Elevation of privilege | takeover ownership | mitigate | Stale AI/dispatch/reply cannot replace `teacher_active` owner/session. |
| T-475-SC | Tampering | dependency supply chain | accept | No dependency changes. |
</threat_model>

<verification>
Run the new CAS registry/race module plus takeover, dispatch, and SLA regressions.
</verification>

<success_criteria>
- CR-03 and the question-writer portion of failed Truths 1/2 are closed.
- The canonical role remains exactly `teacher`; no alias is introduced.
</success_criteria>

<output>Create `475-17-SUMMARY.md` when complete.</output>
