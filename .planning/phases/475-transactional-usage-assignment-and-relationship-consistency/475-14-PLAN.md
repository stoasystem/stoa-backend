---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stoa/models/question.py
  - src/stoa/routers/questions.py
  - tests/test_phase475_question_replay.py
autonomous: true
gap_closure: true
requirements: [V9DATA-01]
must_haves:
  truths:
    - "D-02: every retry-safe question POST requires a caller-retained idempotencyKey; a lost response cannot silently fall back to a new server UUID."
    - "D-04: malformed or missing submission identity fails through the same structured, coordinate-free public contract and performs no admission/provider effect."
  artifacts:
    - path: src/stoa/models/question.py
      provides: "Required bounded question idempotency input contract"
    - path: tests/test_phase475_question_replay.py
      provides: "Missing-key and lost-response retry contract proof"
  key_links:
    - from: src/stoa/models/question.py
      to: src/stoa/routers/questions.py
      via: "SubmitQuestionRequest.idempotency_key is used before question identity or effects"
      pattern: "idempotency_key"
---

<objective>
Require a replayable caller-owned question submission identity per D-02 and D-04.

Purpose: close WR-01 so a client that loses the first response can reproduce the same logical request instead of creating another quota/ledger/question operation.
Output: a required Web/backend request field, stable structured validation, and effect-free negative/retry tests.
</objective>

## Artifacts this phase produces

- Required `SubmitQuestionRequest.idempotency_key` / JSON `idempotencyKey` field.
- Regression nodes proving missing/blank keys stop before UUID generation, attachment reservation, admission, OCR, and AI.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-VERIFICATION.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
</context>

<tasks>

<task type="auto" id="475-14-01" tdd="true">
  <name>Require the durable question idempotency input</name>
  <files>src/stoa/models/question.py, src/stoa/routers/questions.py, tests/test_phase475_question_replay.py</files>
  <read_first>
    - src/stoa/models/question.py `SubmitQuestionRequest`
    - src/stoa/routers/questions.py `submit_question` through idempotency and `question_id` construction
    - tests/test_phase475_question_replay.py current replay request fixtures and effect counters
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-02 and D-04
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md WR-01
  </read_first>
  <behavior>
    - Missing, null, blank, shorter-than-8, longer-than-200, or unexpected question request fields produce a structured validation response before application effects.
    - A valid repeated `idempotencyKey` remains byte-identical at the route input and selects the original logical submission.
  </behavior>
  <action>Make JSON `idempotencyKey` required with the existing 8..200 bound and forbid a server-generated fallback derived from the fresh question UUID. Resolve and validate it before creating `question_id`, reserving an attachment, reading/incrementing quota, writing ledger/command/question rows, or calling OCR/AI. Preserve the established structured/redacted public error envelope (`code`, safe `message`, `correlationId` where the global handler supplies it) and do not expose raw request values. Update replay tests to use the exact public request boundary and assert every effect counter remains zero for missing or invalid keys; retain same-key retry and changed-payload behavior.</action>
  <acceptance_criteria>
    - `SubmitQuestionRequest.idempotency_key` has no default and JSON alias `idempotencyKey` remains the only accepted public name.
    - No branch in `submit_question` derives the idempotency identity from `question_id` or a fresh UUID.
    - Missing/invalid keys produce no attachment, admission, ledger, question, OCR, or AI call.
    - A valid lost-response retry still uses the same caller key and returns the original logical submission contract.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_question_replay.py -k 'idempotency or lost_response or missing' && .venv/bin/ruff check src/stoa/models/question.py src/stoa/routers/questions.py tests/test_phase475_question_replay.py</automated></verify>
  <done>WR-01 is closed: every accepted question submission has a client-reproducible identity and invalid identity input is effect-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Web client to question API | Untrusted idempotency input enters the durable command workflow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-14-01 | Tampering | `SubmitQuestionRequest` | mitigate | Require one bounded key and reject missing/extra input before effects. |
| T-475-14-02 | Denial of service | `submit_question` retry path | mitigate | Same caller identity reuses one durable command rather than consuming fresh quota. |
| T-475-SC | Tampering | dependency supply chain | accept | This plan installs no package and changes no dependency manifest. |
</threat_model>

<verification>
Run the focused replay contract plus Ruff; inspect the route to confirm validation precedes all effectful helpers.
</verification>

<success_criteria>
- The public backend contract makes retry identity mandatory without adding Web UI or native/mobile scope.
- D-02/D-04 and WR-01 have executable effect-free negative proof.
</success_criteria>

<output>Create `475-14-SUMMARY.md` when complete.</output>
