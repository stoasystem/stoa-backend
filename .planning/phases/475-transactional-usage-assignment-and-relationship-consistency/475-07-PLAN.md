---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 07
type: execute
wave: 3
depends_on: [475-06, 475-08]
files_modified:
  - src/stoa/db/repositories/user_repo.py
  - src/stoa/routers/admin.py
  - tests/test_phase475_parent_binding_reconciliation.py
  - tests/test_admin_authorization.py
autonomous: true
requirements: [V9DATA-03]
must_haves:
  truths:
    - "D-10/D-11: relationship repair previews before mutation, never auto-selects conflicting parents, rechecks versions, skips changed rows, and is idempotent."
  artifacts:
    - path: src/stoa/routers/admin.py
      provides: "Capability-protected preview/apply relationship repair contract"
    - path: tests/test_phase475_parent_binding_reconciliation.py
      provides: "Zero-write preview, changed-row skip, conflict, and replay proof"
---

<objective>
Add safe preview/apply reconciliation for historical parent-binding asymmetry.

Purpose: repair only unambiguous legacy state without broadening parent authorization or overwriting concurrent updates.
Output: bounded classification, version-bound apply, administrator-visible conflicts, and idempotent repair tests.
</objective>

## Artifacts this phase produces

- `ParentBindingRepairClassification`, `ParentBindingRepairPreview`, and `ParentBindingRepairApplyResponse` models.
- `preview_parent_binding_repair()` and `apply_parent_binding_repair()` repository/service functions.
- `POST /admin/parent-bindings/repair/preview` and explicit apply behavior on the existing repair route.
- New test module `tests/test_phase475_parent_binding_reconciliation.py`.

<tasks>

<task type="auto" id="475-07-01">
  <name>Implement version-bound preview/apply relationship reconciliation</name>
  <files>src/stoa/db/repositories/user_repo.py, src/stoa/routers/admin.py, tests/test_phase475_parent_binding_reconciliation.py, tests/test_admin_authorization.py</files>
  <read_first>
    - src/stoa/db/repositories/user_repo.py Plan 475-06 relationship transaction and strong reads
    - src/stoa/routers/admin.py parent-binding list/repair routes and target provider
    - src/stoa/routers/admin.py existing inline admin request/response model conventions
    - src/stoa/security/admin_authorization.py parent binding repair capability/target classification
    - tests/test_privileged_identity_reconciliation.py dry-run/apply/idempotency analog
    - tests/test_student_authorization_matrix.py one-sided/conflicting authorization behavior
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-10 and D-11
  </read_first>
  <action>Build a bounded preview that strongly reads the parent profile, student profile, forward row, and all reverse rows for the target pair and returns `consistent`, `repairable_missing_forward`, `repairable_missing_reverse`, `repairable_profile_projection`, `conflict`, or `skipped_invalid`, plus opaque pair ID and observed row versions/digests. Preview performs no writes. Apply accepts the preview identity, rechecks every coordinate/version, and calls the Plan 475-06 atomic relationship primitive only for an unambiguous same-parent repair. If any row changed, return `skipped_changed`; if rows name different parents, return `conflict` and write nothing. Repeated apply returns `already_consistent` with zero mutation. Preserve capability-scoped administrator authorization and durable audit evidence for preview and apply.</action>
  <acceptance_criteria>
    - Preview emits all classifications from fixtures and performs zero writes.
    - A missing one-sided row is repaired atomically only when every other coordinate agrees.
    - Different-parent conflict remains denied and unchanged.
    - Mutation between preview and apply returns `skipped_changed` and preserves new data.
    - Second apply performs zero writes and reports already consistent.
    - Unauthorized administrators cannot preview or apply before any relationship read/mutation.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_parent_binding_reconciliation.py tests/test_admin_authorization.py tests/test_student_authorization_matrix.py && .venv/bin/ruff check src/stoa/db/repositories/user_repo.py src/stoa/routers/admin.py tests/test_phase475_parent_binding_reconciliation.py</automated></verify>
  <done>Historical relationship repair is preview-first, conflict-safe, version-bound, audited, and idempotent.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| Preview silently mutates data | Pure strong-read preview and zero-write assertions |
| Apply overwrites changes after preview | Observed version/digest conditions and `skipped_changed` |
| Conflict repair grants wrong parent access | No automatic winner; conflict is report-only and auth stays denied |
</threat_model>

<success_criteria>
- V9DATA-03 historical repair satisfies D-10/D-11 without weakening Phase 472 authorization.
</success_criteria>

<output>Create `475-07-SUMMARY.md` after preview/apply and authorization tests pass.</output>
