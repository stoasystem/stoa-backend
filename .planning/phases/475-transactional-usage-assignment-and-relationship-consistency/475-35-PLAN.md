---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 35
type: execute
wave: 10
depends_on: ["475-24", "475-26", "475-34"]
files_modified:
  - docs/security/phase-475-evidence-results.json
  - docs/security/phase-475-evidence.md
autonomous: true
gap_closure: true
requirements: [V9DATA-01, V9DATA-02, V9DATA-03, V9DATA-04, V9DATA-05, V9DATA-06, V9DATA-07, V9DATA-08]
must_haves:
  truths:
    - "The final checked evidence is captured from one clean immutable Git candidate containing every functional, typing, and verifier fix."
    - "All local gates and all mapped D/V9DATA/CR/WR selectors pass with zero skip/xfail/xpass; external AWS/provider/deployment obligations remain exact later-phase `NOT RUN` entries."
    - "The publication commit changes exactly the two evidence files and remains verifiable at later HEAD."
  artifacts:
    - path: docs/security/phase-475-evidence-results.json
      provides: "Machine-readable candidate-bound Phase 475 receipts and coverage"
    - path: docs/security/phase-475-evidence.md
      provides: "Redacted human-readable checked evidence summary"
  key_links:
    - from: docs/security/phase-475-evidence-results.json
      to: "immutable candidate Git tree"
      via: "candidate SHA, exact argv, receipts, artifact hashes, and exhaustive source snapshot"
      pattern: "candidate_sha"
---

<objective>
Publish the final source-bound Phase 475 gap-closure evidence only after every functional and verifier fix is committed.

Purpose: replace the stale false-positive package with reproducible checked evidence for all local claims while preserving later-phase external ownership.
Output: exactly one JSON receipt package and one redacted Markdown report bound to the immutable candidate.
</objective>

## Artifacts this phase produces

- `docs/security/phase-475-evidence-results.json` with exact candidate/base identities, argv, exit codes, node receipts, hashes, complete source snapshot, and closed coverage.
- `docs/security/phase-475-evidence.md` with a redacted summary of local PASS and external `NOT RUN` obligations.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-VERIFICATION.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-24-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-26-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-34-SUMMARY.md
</context>

<tasks>

<task type="auto" id="475-35-01">
  <name>Capture, verify, and publish immutable final evidence</name>
  <files>docs/security/phase-475-evidence-results.json, docs/security/phase-475-evidence.md</files>
  <read_first>
    - scripts/verify_phase475.py capture, verify-capture, verify-publication commands and exact publication invariant
    - tests/test_phase475_evidence_verifier.py complete adversarial contract
    - docs/security/phase-475-evidence-results.json existing stale evidence package
    - docs/security/phase-475-evidence.md existing stale evidence narrative
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md every finding to close
  </read_first>
  <action>First require a clean worktree at the committed candidate containing plans 475-14 through 475-34 implementation summaries; set `candidate` to that exact HEAD and create the private capture directory with `mktemp -d`. Run `scripts/verify_phase475.py capture --candidate "$candidate" --capture-root "$capture_root"`, then `verify-capture` with the same immutable candidate and directory. Stop on any nonzero gate, skip/xfail/xpass, mypy diagnostic, source-snapshot omission, coverage drift, dirty-tree drift, or privacy match; never edit evidence into PASS. Persist only the generated JSON and Markdown, confirm they are the only publication diff, and commit exactly those two paths as the evidence publication commit whose parent is the candidate. Run `verify-publication` after that commit and preserve the private capture outside the repository only for the duration needed to verify. Do not call AWS, a live provider, or a deployment environment; retain exact `NOT RUN` rows owned by phases 479/480.</action>
  <acceptance_criteria>
    - Capture and verify-capture both succeed against the same clean immutable candidate SHA.
    - Every D-01..D-16, V9DATA-01..08, CR-01..CR-10, and WR-01..WR-04 selector resolves to observed passing nodes with no non-pass outcome.
    - Mypy receipt records tool exit code zero and zero diagnostics over the complete runtime inventory.
    - Source snapshot cardinality exactly equals the base-to-candidate path inventory with explicit deletion handling.
    - Publication commit changes exactly the two declared evidence files, has the candidate as parent, and passes `verify-publication`.
    - Evidence contains no raw answer, identity, idempotency value/digest, storage coordinate, provider diagnostic, or secret.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python scripts/verify_phase475.py verify-publication && .venv/bin/python -m pytest -q tests/test_phase475_evidence_verifier.py</automated></verify>
  <done>Phase 475 has final immutable source-bound local evidence for every gap, with external live obligations accurately retained for phases 479/480.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Clean candidate to private capture | Code or HEAD drift during execution invalidates every receipt. |
| Private capture to public evidence | Raw logs may contain sensitive values and cannot be copied into publication. |
| Candidate to publication commit | The evidence commit must contain exactly two generated artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-35-01 | Repudiation | candidate-bound receipts | mitigate | Clean HEAD checks before/after every gate plus immutable candidate SHA. |
| T-475-35-02 | Tampering | publication commit | mitigate | Exact two-path diff and candidate-parent verification. |
| T-475-35-03 | Information disclosure | evidence JSON/Markdown | mitigate | Denylist/privacy scan over both generated artifacts; publish safe counts/hashes only. |
| T-475-35-04 | Spoofing | external evidence status | mitigate | Exact later-phase owner and `NOT RUN` rows; no fabricated live execution. |
| T-475-SC | Tampering | dependency supply chain | accept | No package or dependency manifest changes. |
</threat_model>

<verification>
After the evidence-only publication commit, run `verify-publication` and the entire verifier adversarial suite.
</verification>

<success_criteria>
- The final evidence package is reproducible, candidate-bound, complete, redacted, and publication-immutable.
- No external AWS/provider/deployment claim is upgraded beyond `NOT RUN` in Phase 475.
</success_criteria>

<output>Create `475-35-SUMMARY.md` when complete.</output>
