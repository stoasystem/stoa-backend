---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 43
type: execute
wave: 9
depends_on: [475-42]
files_modified:
  - scripts/verify_phase475.py
  - tests/test_phase475_evidence_verifier.py
autonomous: true
gap_closure: true
requirements: [V9DATA-01, V9DATA-02, V9DATA-03, V9DATA-04, V9DATA-05, V9DATA-06, V9DATA-07, V9DATA-08]
must_haves:
  truths:
    - "Every base-to-candidate path appears exactly once in the source snapshot or verification fails."
    - "Added/modified/deleted/renamed/copied paths have explicit deterministic blob treatment; no failed Git read is silently skipped."
  artifacts:
    - path: scripts/verify_phase475.py
      provides: "Fail-closed exhaustive source snapshot"
    - path: tests/test_phase475_evidence_verifier.py
      provides: "Path status, blob failure, and cardinality drift tests"
  key_links:
    - from: "Git name-status inventory"
      to: "source_snapshot"
      via: "one normalized row per changed path with required blob hashes"
      pattern: "source_snapshot"
---

<objective>
Make the Phase 475 source snapshot exhaustive as an independent WR-03 fix.

Purpose: prevent deleted, unreadable, renamed, copied, or otherwise unclassified paths from vanishing from immutable evidence.
Output: deterministic fail-closed snapshot classification and focused adversarial tests.
</objective>

## Artifacts this phase produces

- Exact normalized name-status inventory and one-to-one snapshot cardinality assertion.
- Candidate/base blob hashing rules for every supported status.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-42-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-VERIFICATION.md
</context>

<tasks>

<task type="auto" id="475-43-01" tdd="true">
  <name>Fail closed on every source snapshot omission</name>
  <files>scripts/verify_phase475.py, tests/test_phase475_evidence_verifier.py</files>
  <read_first>
    - scripts/verify_phase475.py `_source_snapshot`, Git helpers, candidate/base validation, and publication receipt validation
    - tests/test_phase475_evidence_verifier.py source snapshot and Git failure tests
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md WR-03
  </read_first>
  <behavior>
    - Added/modified paths require a readable candidate blob and hash candidate bytes.
    - Deleted paths require candidate absence and a readable hashed base blob with explicit deleted status.
    - Rename/copy status is either represented deterministically with both endpoints/blobs or rejected; missing/extra/duplicate paths and blob read failure fail verification.
  </behavior>
  <action>Replace `_source_snapshot`'s silent `continue` with an exhaustive normalized `git diff --name-status` model. For added/modified paths require candidate blob read success; for deleted paths require candidate absence and base blob read success; for rename/copy entries either emit one deterministic record containing both paths and required blobs or reject unsupported ambiguity. Reject unrecognized status, failed blob reads, duplicate normalized paths, and any path/status/cardinality mismatch. Snapshot identity must be candidate/base bound and public-safe. Add focused tests for all statuses, unreadable blobs, malformed name-status, missing/extra/duplicate rows, and deterministic ordering. Do not change coverage registries in this plan.</action>
  <acceptance_criteria>
    - No changed path can silently disappear.
    - Snapshot rows and Git inventory are one-to-one and deterministically ordered.
    - Deletions bind the base blob and candidate absence explicitly.
    - All unsupported or malformed statuses fail closed.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_evidence_verifier.py::test_source_snapshot_is_exhaustive_for_all_git_statuses && .venv/bin/ruff check scripts/verify_phase475.py tests/test_phase475_evidence_verifier.py</automated></verify>
  <done>WR-03 source snapshot completeness is closed independently of coverage registration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git base/candidate objects to evidence snapshot | Missing or unreadable paths must invalidate evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-43-01 | Repudiation | source snapshot | mitigate | Exact status/path cardinality and required blob hashes. |
| T-475-43-02 | Tampering | renamed/deleted paths | mitigate | Explicit deterministic treatment or fail-closed rejection. |
| T-475-SC | Tampering | dependency supply chain | accept | No dependency changes. |
</threat_model>

<verification>Run the exact source-snapshot adversarial node plus the complete verifier suite.</verification>

<success_criteria>
- Snapshot completeness cannot be bypassed by Git status or blob-read behavior.
- This plan remains one root cause and does not mix coverage registry work.
</success_criteria>

<output>Create `475-43-SUMMARY.md` when complete.</output>
