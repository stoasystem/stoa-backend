---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 26
type: execute
wave: 3
depends_on: [475-23, 475-25]
files_modified:
  - src/stoa/db/repositories/account_deletion_repo.py
  - src/stoa/services/account_deletion_service.py
  - tests/test_phase475_deletion_relationship_scrub.py
autonomous: true
gap_closure: true
requirements: [V9DATA-03]
must_haves:
  truths:
    - "CR-10: deleting a parent scrubs both formal forward/reverse relationship identities and the student profile projection with entity-level CAS."
    - "A row changed after discovery is retried from a fresh strong read and deletion completes only after two later clean epochs."
  artifacts:
    - path: src/stoa/db/repositories/account_deletion_repo.py
      provides: "Formal relationship reference scrub/delete CAS"
    - path: tests/test_phase475_deletion_relationship_scrub.py
      provides: "Discovery-to-cleanup-to-two-clean-epoch relationship proof"
  key_links:
    - from: src/stoa/services/account_deletion_service.py
      to: src/stoa/db/repositories/account_deletion_repo.py
      via: "account_profile branch dispatches formal relationship rows to exact CAS cleanup"
      pattern: "parent_student_binding"
---

<objective>
Scrub formal parent relationship identity rows end to end for CR-10.

Purpose: prevent deleted parent identity from surviving in a student's reverse row or profile while preserving conflict/non-revival semantics.
Output: entity-specific relationship cleanup plus a real discovery, CAS-loss, retry, and two-clean-epoch test.
</objective>

## Artifacts this phase produces

- A relationship cleanup primitive conditioned on exact forward/reverse identity, status, version, and deleting-account generation.
- A branch integration test for both row directions and the student profile projection.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-23-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-25-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
</context>

<tasks>

<task type="auto" id="475-26-01" tdd="true">
  <name>CAS-scrub formal parent forward and reverse relations</name>
  <files>src/stoa/db/repositories/account_deletion_repo.py, src/stoa/services/account_deletion_service.py, tests/test_phase475_deletion_relationship_scrub.py</files>
  <read_first>
    - src/stoa/db/repositories/user_repo.py formal `parent_student_binding` forward/reverse schema and post-475-23 status/version contract
    - src/stoa/db/repositories/account_deletion_repo.py discovery, `scrub_parent_profile_child`, fence conditions, and transaction wrapper
    - src/stoa/services/account_deletion_service.py `_account_profile_branch` and `_run_base_branch`
    - tests/test_phase473_account_deletion_claim_fencing.py real profile scrub CAS fake
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md CR-10
  </read_first>
  <behavior>
    - Parent-owned forward and student-owned reverse rows naming the deleting parent are removed or minimized together with the student profile parent projection.
    - A version/status/identity change between discovery and cleanup performs no stale mutation, resets the epoch, and succeeds only after a fresh strong rediscovery.
    - Two full clean epochs after cleanup contain no raw parent identity in either relationship direction or profile.
  </behavior>
  <action>Add an entity-specific formal relationship cleanup path. Condition every mutation on the deletion fence generation and the discovered PK/SK, `entity_type`, parent/student IDs, status, relationship, and positive version; remove the two formal rows and scrub the student's parent projection without selecting a conflicting parent or writing `active`. Reuse the narrow profile CAS/retry contract and classify row-condition loss as retryable rather than clean. Wire `_account_profile_branch` to this primitive for `parent_student_binding` rows. The test must start from strong discovery, inject a real CAS loss, rerun cleanup, and drive the branch through two later clean epochs while asserting unrelated student profile fields remain byte-identical.</action>
  <acceptance_criteria>
    - Forward and reverse formal rows cannot retain the deleted parent ID.
    - Cleanup never revives or chooses among conflicting relationship rows.
    - CAS loss leaves the newer row/profile untouched and resets clean progress.
    - Completion requires two post-cleanup strong clean epochs.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_deletion_relationship_scrub.py::test_relationship_identity_scrub_retries_cas_then_requires_two_clean_epochs && .venv/bin/ruff check src/stoa/db/repositories/account_deletion_repo.py src/stoa/services/account_deletion_service.py tests/test_phase475_deletion_relationship_scrub.py</automated></verify>
  <done>CR-10 formal parent identity cleanup is functional, CAS-safe, retryable, and quiescence-proved under V9DATA-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Parent deletion to student-owned relationship rows | Deletion crosses account partitions and must not overwrite concurrent authority state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-26-01 | Tampering | relationship cleanup | mitigate | Exact row/version/status conditions plus deletion fence generation. |
| T-475-26-02 | Elevation of privilege | relationship state | mitigate | Cleanup removes authority and never writes active or selects a parent. |
| T-475-SC | Tampering | dependency supply chain | accept | No package or manifest change. |
</threat_model>

<verification>Run the focused real branch test plus existing relationship reconciliation and account-deletion seal suites.</verification>

<success_criteria>
- Deleted-parent identity is absent from both formal directions and the profile after two clean epochs.
- Concurrent row/profile changes are preserved or retried, never overwritten.
</success_criteria>

<output>Create `475-26-SUMMARY.md` when complete.</output>
