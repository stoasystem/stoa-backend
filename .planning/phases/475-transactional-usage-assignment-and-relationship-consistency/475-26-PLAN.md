---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 26
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase475_completed_deletion_replay.py
autonomous: true
gap_closure: true
requirements: [V9DATA-08]
must_haves:
  truths:
    - "D-15/D-16: a terminal deletion command replays one deterministic durable receipt."
    - "Replay cannot rerun deletion, produce a new completion timestamp, or expose storage/provider coordinates."
  artifacts:
    - path: tests/test_phase475_completed_deletion_replay.py
      provides: "Deterministic terminal receipt and zero-new-effect proof"
  key_links:
    - from: "completed deletion command"
      to: "public terminal response"
      via: "stored receipt replay rather than recomputation"
      pattern: "receipt"
---

<objective>
Lock the already-implemented terminal deletion replay contract to deterministic observable evidence per D-15 and D-16.

Purpose: preserve V9DATA-08 while gap fixes touch deletion discovery and the final verifier.
Output: a stronger source-level regression node for receipt equality, persistence, and zero replay effects.
</objective>

## Artifacts this phase produces

- A test-only proof that repeated completed-deletion calls return the same durable terminal receipt.
- Assertions that replay performs no new sweep, provider action, timestamp generation, or storage mutation.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-VERIFICATION.md
</context>

<tasks>

<task type="auto" id="475-26-01" tdd="true">
  <name>Prove deterministic terminal deletion receipt replay</name>
  <files>tests/test_phase475_completed_deletion_replay.py</files>
  <read_first>
    - tests/test_phase475_completed_deletion_replay.py current completion/replay fixture and response assertions
    - src/stoa/services/account_deletion_service.py completed-command replay branch and stored receipt construction
    - src/stoa/db/repositories/account_deletion_repo.py terminal command persistence fields
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-15 and D-16
  </read_first>
  <behavior>
    - First completion and every replay return deeply equal terminal response payloads from the persisted receipt.
    - Replay does not invoke discovery, deletion, provider cleanup, clock-generated completion, or a repository write.
    - The public receipt contains stable domain fields and no table key, bucket key, provider token, or internal exception.
  </behavior>
  <action>Strengthen the existing test at the real account-deletion service boundary. Capture the stored command receipt after completion, replay with a changed clock and effect spies that fail if invoked, and assert deep equality among the original response, stored receipt, and multiple replay responses. Assert the receipt's terminal status/count/completion identity stays stable and explicitly reject internal storage/provider coordinate fields. Do not modify production code in this plan; if the test exposes a regression, leave the plan failing so the executor reports the functional blocker rather than weakening the assertion.</action>
  <acceptance_criteria>
    - Original, persisted, and replayed terminal receipts are deeply equal.
    - Effect spies prove replay executes zero destructive/provider/storage writes.
    - Changed wall-clock time cannot change completion metadata.
    - The public receipt is structured and redacted.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_completed_deletion_replay.py && .venv/bin/ruff check tests/test_phase475_completed_deletion_replay.py</automated></verify>
  <done>V9DATA-08 remains proved at source level with deterministic, effect-free terminal replay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Persisted terminal command to public API | Internal deletion state is projected into a replayable public receipt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-26-01 | Repudiation | deletion receipt | mitigate | Deep equality proves one durable terminal outcome. |
| T-475-26-02 | Information disclosure | public receipt | mitigate | Negative assertions reject internal coordinates and provider tokens. |
| T-475-SC | Tampering | dependency supply chain | accept | Test-only plan; no package changes. |
</threat_model>

<verification>
Run the completed-deletion replay test independently before the aggregate Phase 475 evidence plan.
</verification>

<success_criteria>
- Terminal deletion replay is deterministic, durable, redacted, and effect-free.
- D-15/D-16 and V9DATA-08 retain explicit coverage.
</success_criteria>

<output>Create `475-26-SUMMARY.md` when complete.</output>
