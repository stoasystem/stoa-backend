---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 34
type: execute
wave: 9
depends_on: ["475-33"]
files_modified:
  - scripts/verify_phase475.py
  - tests/test_phase475_evidence_verifier.py
autonomous: true
gap_closure: true
requirements: [V9DATA-01, V9DATA-02, V9DATA-03, V9DATA-04, V9DATA-05, V9DATA-06, V9DATA-07, V9DATA-08]
must_haves:
  truths:
    - "Every path changed from the phase base to the immutable candidate appears exactly once in the source snapshot or causes evidence failure."
    - "Every D-01..D-16 decision, V9DATA-01..08 requirement, CR-01..CR-10 blocker, and WR-01..WR-04 warning is bound to observed passing test nodes."
  artifacts:
    - path: scripts/verify_phase475.py
      provides: "Exhaustive source snapshot and closed coverage registries"
    - path: tests/test_phase475_evidence_verifier.py
      provides: "Deleted/missing/unreadable path and coverage-registry drift tests"
  key_links:
    - from: "Git diff path inventory"
      to: "source_snapshot"
      via: "candidate-tree classification with no silent `git show` skip"
      pattern: "source_snapshot"
---

<objective>
Make the Phase 475 source snapshot exhaustive and bind every gap finding to passing source-level evidence.

Purpose: close WR-03 and prevent the final package from silently omitting changed paths or newly added gap tests.
Output: fail-closed snapshot classification, expanded gate registry, and exact decision/requirement/review coverage maps.
</objective>

## Artifacts this phase produces

- Exhaustive candidate path snapshot with explicit treatment for modified/added/deleted paths.
- Gate registry containing every test introduced by plans 475-14 through 475-26.
- Closed coverage sections for D-01..D-16, V9DATA-01..08, CR-01..CR-10, and WR-01..WR-04.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-VERIFICATION.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-33-SUMMARY.md
</context>

<tasks>

<task type="auto" id="475-34-01" tdd="true">
  <name>Fail closed on snapshot omission and close every evidence registry</name>
  <files>scripts/verify_phase475.py, tests/test_phase475_evidence_verifier.py</files>
  <read_first>
    - scripts/verify_phase475.py `PHASE475_MODULES`, gate registry, decision/requirement/follow-up contracts, `_source_snapshot`, `verify_coverage`, and public rendering
    - tests/test_phase475_evidence_verifier.py registry, snapshot, coverage, tamper, and privacy tests
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-01 through D-16
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md CR-01 through CR-10 and WR-01 through WR-04
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-VERIFICATION.md failed truths and external obligations
  </read_first>
  <behavior>
    - Every base-to-candidate path has one deterministic snapshot row; a candidate blob read failure or unclassified path fails verification.
    - Deleted paths are represented explicitly with base blob hash and deletion status rather than silently skipped.
    - Coverage verification rejects missing/extra IDs, selectors, skipped/xfail/xpass nodes, or selector-node drift across all decision, requirement, review, and inherited sections.
  </behavior>
  <action>Replace `_source_snapshot`'s silent `continue` with an exhaustive name-status inventory. For added/modified paths, require `git show candidate:path` success and hash candidate bytes; for deleted paths, require the candidate absence and hash the base blob with explicit `status: deleted`; reject renames/copies or other unhandled statuses unless represented deterministically with both sides. Assert snapshot path/status cardinality equals the Git diff inventory. Extend `PHASE475_MODULES`/gates to include the new question state-CAS, provider recovery, and deletion discovery tests and update the deletion gate accordingly. Add exact review finding contracts for CR-01..CR-10 and WR-01..WR-04, mapping each to the focused passing node(s), alongside complete D-01..D-16 and V9DATA-01..08 contracts. Keep AWS/provider/deployment evidence as exact `NOT RUN` rows owned only by phases 479/480. Extend tests for unreadable blobs, deletion rows, status/cardinality drift, missing/extra coverage IDs, skipped/xfail/xpass nodes, and public redaction.</action>
  <acceptance_criteria>
    - No changed path can disappear from `source_snapshot` because `git show` failed.
    - Snapshot rows are deterministic, candidate/base bound as appropriate, unique, and exhaustive.
    - All 16 decisions, 8 requirements, 10 blockers, and 4 warnings have exact selector coverage and observed passing nodes.
    - External AWS/provider/deployment obligations remain `NOT RUN` and owned by phases 479/480; no live call is attempted.
    - Public evidence remains structured/redacted and contains no raw identifiers, storage coordinates, provider diagnostics, or raw idempotency values.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_evidence_verifier.py && .venv/bin/ruff check scripts/verify_phase475.py tests/test_phase475_evidence_verifier.py</automated></verify>
  <done>WR-03 is closed and every source decision, requirement, review finding, and deferred external obligation has a fail-closed evidence mapping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git base/candidate trees to published snapshot | Missing/deleted/unreadable paths must not vanish from evidence. |
| Pytest node receipts to coverage claims | Registry manipulation could claim unobserved behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-34-01 | Repudiation | source snapshot | mitigate | Exact name-status cardinality and required blob hashes; no silent skip. |
| T-475-34-02 | Tampering | coverage registry | mitigate | Closed exact ID/selector/node sets reject omissions and additions. |
| T-475-34-03 | Information disclosure | evidence output | mitigate | Denylist and redaction tests cover JSON and Markdown publication. |
| T-475-SC | Tampering | dependency supply chain | accept | No package or manifest changes. |
</threat_model>

<verification>
Run the entire evidence-verifier adversarial suite and Ruff before capturing any final evidence.
</verification>

<success_criteria>
- Source snapshot and coverage mappings are exhaustive and fail closed.
- WR-03 and the complete multi-source/review audit are mechanically enforced.
</success_criteria>

<output>Create `475-34-SUMMARY.md` when complete.</output>
