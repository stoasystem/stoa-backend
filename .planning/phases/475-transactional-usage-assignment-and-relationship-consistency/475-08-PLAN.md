---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 08
type: execute
wave: 2
depends_on: [475-06]
files_modified:
  - src/stoa/db/repositories/user_repo.py
  - src/stoa/db/repositories/account_deletion_repo.py
  - tests/test_phase475_profile_version_cas.py
  - tests/test_phase473_account_deletion_claim_fencing.py
autonomous: true
requirements: [V9DATA-06]
must_haves:
  truths:
    - "D-12: ordinary parent profile writers and privacy scrub preserve unrelated latest fields; sensitive-field conflict is won by the scrub."
  artifacts:
    - path: src/stoa/db/repositories/user_repo.py
      provides: "Shared profile version/CAS-and-increment writer contract"
    - path: tests/test_phase475_profile_version_cas.py
      provides: "Real locale/preference writer versus real scrub race"
---

<objective>
Make every shared parent profile writer participate in one version/CAS discipline.

Purpose: turn Phase 473's scrub-side CAS into a real two-sided concurrency guarantee.
Output: versioned narrow writers, typed retry behavior, writer inventory, and real race proof.
</objective>

## Artifacts this phase produces

- `ProfileWriteDisposition`, `update_profile_fields_versioned()`, and `profile_update_operation()`.
- Closed `PROFILE_WRITER_REGISTRY` covering every `USER#*/PROFILE` mutation.
- New test module `tests/test_phase475_profile_version_cas.py`.

<tasks>

<task type="auto" id="475-08-01">
  <name>Unify parent profile writers and scrub under one version contract</name>
  <files>src/stoa/db/repositories/user_repo.py, src/stoa/db/repositories/account_deletion_repo.py, tests/test_phase475_profile_version_cas.py, tests/test_phase473_account_deletion_claim_fencing.py</files>
  <read_first>
    - src/stoa/db/repositories/user_repo.py every profile writer and `update_profile_fields`
    - src/stoa/db/repositories/account_deletion_repo.py `scrub_parent_profile_child`
    - tests/test_phase473_account_deletion_claim_fencing.py current synthetic concurrent preference test
    - src/stoa/routers/auth.py locale preference writer call site
    - src/stoa/routers/teachers.py availability writer call site and other callers found by `rg update_profile_fields`
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-12
  </read_first>
  <action>Inventory every mutation of `USER#{user_id}/PROFILE` and route shared writers through one narrow update that conditions on `attribute_exists`, active fence generation, and expected `version`, applies only caller-owned fields, and increments version exactly once. For callers without an observed version, perform a strong read and bounded retry on CAS loss; never replace the full profile. Keep `scrub_parent_profile_child` as a narrow sensitive-field remover conditioned on the observed version. When writer and scrub target unrelated fields, one retries and both changes survive; when both target the same sensitive child linkage, the scrubbed state remains absent and a stale ordinary writer cannot restore it. Add a source registry/test that fails if a profile writer bypasses the shared contract.</action>
  <acceptance_criteria>
    - Every registered profile writer conditions on and increments the same `version` field.
    - A barrier race using the real locale/preference writer and real child scrub preserves exact latest locale/preference bytes and removes only the target child data.
    - Same-sensitive-field race cannot restore scrubbed linkage.
    - CAS loss is retryable/bounded and never falls back to whole-row put/overwrite.
    - The writer registry detects a new direct profile mutation outside the shared contract.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_profile_version_cas.py tests/test_phase473_account_deletion_claim_fencing.py && .venv/bin/ruff check src/stoa/db/repositories/user_repo.py src/stoa/db/repositories/account_deletion_repo.py tests/test_phase475_profile_version_cas.py</automated></verify>
  <done>Normal profile updates and privacy scrub share a real CAS protocol that cannot lose unrelated concurrent values or revive scrubbed sensitive data.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| Stale scrub erases newer preferences | Shared row version, CAS loss, narrow retry |
| Stale writer revives removed child data | Same-field scrub priority and no whole-row replacement |
| Unregistered writer bypasses versioning | Closed source-backed profile writer registry test |
</threat_model>

<success_criteria>
- V9DATA-06 and follow-up `profile-version-cas` pass against real writers, not synthetic version changes.
</success_criteria>

<output>Create `475-08-SUMMARY.md` after profile race and registry tests pass.</output>
