---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 10
type: execute
wave: 2
depends_on: [475-09]
files_modified:
  - src/stoa/models/practice.py
  - src/stoa/db/repositories/practice_repo.py
  - src/stoa/services/practice_projection_service.py
  - src/stoa/routers/practice.py
  - tests/test_phase475_mistake_answer.py
autonomous: true
requirements: [V9DATA-05]
must_haves:
  truths:
    - "D-14: accepted wrong answers round-trip exactly within a display-safe bound; historical absence is explicit unknown and never replaced with the correct answer."
  artifacts:
    - path: src/stoa/services/practice_projection_service.py
      provides: "Bounded answer normalization and explicit legacy-unknown projection"
    - path: tests/test_phase475_mistake_answer.py
      provides: "Scalar/list/Unicode/bound/legacy round-trip proof"
---

<objective>
Complete the mistake-answer storage and legacy-unknown API contract.

Purpose: close the remaining small practice correctness gap without changing answer-reveal authorization.
Output: bounded canonical submitted answers, schema metadata, explicit unknown projection, and round-trip tests.
</objective>

## Artifacts this phase produces

- `PRACTICE_SUBMITTED_ANSWER_SCHEMA_VERSION`, `normalize_submitted_answer()`, and `LegacyAnswerState`.
- Nullable/typed `yourAnswer` plus `answerState` on mistake responses.
- New test module `tests/test_phase475_mistake_answer.py`.

<tasks>

<task type="auto" id="475-10-01">
  <name>Persist bounded answers and project legacy unknown explicitly</name>
  <files>src/stoa/models/practice.py, src/stoa/db/repositories/practice_repo.py, src/stoa/services/practice_projection_service.py, src/stoa/routers/practice.py, tests/test_phase475_mistake_answer.py</files>
  <read_first>
    - src/stoa/db/repositories/practice_repo.py `put_attempt`, `record_attempt`, and `get_mistakes`
    - src/stoa/services/practice_projection_service.py attempt result and answer-free preview boundaries
    - src/stoa/routers/practice.py submit and mistake routes
    - src/stoa/models/practice.py public response types
    - tests/test_phase473_practice_snapshot.py immutable receipt/mistake projection
    - tests/test_practice_privacy.py pre/post submission answer rules
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-14
  </read_first>
  <action>Define one deterministic JSON-safe submitted-answer normalization for supported scalar/list values, Unicode, and whitespace, with an explicit serialized byte/depth/item bound and rejection before attempt persistence when exceeded or unsupported. Persist the normalized value in both compatibility fields plus a schema version. Project current wrong attempts with `answerState="recorded"` and exact normalized `yourAnswer`. Project rows lacking both answer fields with `answerState="unknown_legacy"`, nullable `yourAnswer`, and the client message “当时提交的答案未保存”. Never substitute `standard_answer`, an empty string, or a guessed value. Preserve the Phase 473 rule that answer-bearing result data is available only after a durable attempt.</action>
  <acceptance_criteria>
    - Wrong scalar, list, and Unicode answers round-trip exactly after normalization.
    - Oversize/unsupported answers fail before `put_attempt` and expose no raw value in logs/errors.
    - Legacy missing fields return `unknown_legacy`, nullable answer, and the exact friendly message.
    - Correct answer is never copied into `yourAnswer`.
    - All pre-submission answer-free and privileged answer-access tests remain green.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_mistake_answer.py tests/test_phase473_practice_snapshot.py tests/test_practice_privacy.py tests/test_practice.py && .venv/bin/ruff check src/stoa/models/practice.py src/stoa/db/repositories/practice_repo.py src/stoa/services/practice_projection_service.py src/stoa/routers/practice.py tests/test_phase475_mistake_answer.py</automated></verify>
  <done>Mistake review accurately returns bounded recorded answers and safely labels historical unknowns.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| Oversize/deep answer becomes storage or rendering abuse | Canonical byte/depth/item bounds before persistence |
| Legacy row falsely claims student input | Explicit nullable `unknown_legacy` state |
| Correct answer leaks or is misattributed | Separate immutable recorded-answer and standard-answer fields/projections |
</threat_model>

<success_criteria>
- V9DATA-05 and D-14 pass complete safe round-trip and legacy tests.
</success_criteria>

<output>Create `475-10-SUMMARY.md` after practice regression tests pass.</output>
