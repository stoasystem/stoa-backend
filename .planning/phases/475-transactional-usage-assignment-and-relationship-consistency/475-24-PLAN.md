---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 24
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stoa/services/rate_limit.py
  - tests/test_phase475_rate_limit.py
autonomous: true
gap_closure: true
requirements: [V9DATA-04]
must_haves:
  truths:
    - "D-11: a committed rate-limit operation owns an immutable authoritative receipt."
    - "Every replay returns the original post-operation count and decision even after unrelated traffic changes the aggregate counter."
  artifacts:
    - path: src/stoa/services/rate_limit.py
      provides: "Counter CAS plus immutable per-operation receipt"
    - path: tests/test_phase475_rate_limit.py
      provides: "Intervening-traffic and concurrent-operation receipt proof"
  key_links:
    - from: "rate operation row"
      to: "aggregate counter row"
      via: "one transaction stores the exact counter outcome it conditionally commits"
      pattern: "counter_value_after"
---

<objective>
Return immutable rate-limit outcomes on replay per D-11.

Purpose: close CR-09 so replay does not report the current shared counter as if it were the original operation's result.
Output: expected-counter CAS, durable operation receipt, and concurrent/intervening traffic tests.
</objective>

## Artifacts this phase produces

- Per-operation receipt containing the committed decision, count, limit, period, and expiry.
- Retry loop that atomically binds the receipt to the exact aggregate counter transition.
- Tests proving replay stability after subsequent successful and rejected operations.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-VERIFICATION.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
</context>

<tasks>

<task type="auto" id="475-24-01" tdd="true">
  <name>Bind each rate operation to an immutable counter receipt</name>
  <files>src/stoa/services/rate_limit.py, tests/test_phase475_rate_limit.py</files>
  <read_first>
    - src/stoa/services/rate_limit.py operation-key builder, counter update transaction, duplicate path, and public result model
    - tests/test_phase475_rate_limit.py fake transaction behavior, concurrent calls, duplicate replay, and reset-boundary cases
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-11
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md CR-09
  </read_first>
  <behavior>
    - The first operation atomically increments from an expected counter and writes its exact post-operation receipt.
    - Same-operation replay reads only that receipt and returns the identical decision/count/limit/period/expiry after intervening traffic.
    - Concurrent different operations retry counter-CAS conflicts without double-counting any operation.
  </behavior>
  <action>Replace the capped additive update with a read/expected-value conditional transaction (including the absent-counter case) that creates the operation row and writes the exact next aggregate count together. Persist on the operation row the authoritative response fields, including decision and `counter_value_after`, rather than recomputing from the mutable counter during replay. On an operation-row duplicate, return its stored receipt; on a counter CAS loss with no matching operation row, reread and retry within the existing bounded policy. Preserve period/TTL semantics and structured fail-closed errors. Extend the fake transaction interpreter and tests for intervening accepted traffic, intervening rejected traffic, concurrent distinct operations, exact duplicate concurrency, and period rollover.</action>
  <acceptance_criteria>
    - Aggregate count advances at most once per operation ID.
    - Operation receipt and aggregate transition commit in the same transaction.
    - Replay returns byte-equivalent response fields from the original receipt regardless of later counter value.
    - No public error reveals DynamoDB keys or expressions.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_rate_limit.py && .venv/bin/ruff check src/stoa/services/rate_limit.py tests/test_phase475_rate_limit.py</automated></verify>
  <done>CR-09 is closed with a stable authoritative replay receipt under concurrency and intervening traffic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Rate-limited caller to shared counter | Concurrent untrusted operations contend for one aggregate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-24-01 | Tampering | counter update | mitigate | Expected-value CAS binds one immutable receipt to one aggregate transition. |
| T-475-24-02 | Repudiation | duplicate operation | mitigate | Replay returns the persisted authoritative operation receipt. |
| T-475-24-03 | Denial of service | CAS contention | mitigate | Bounded retries and fail-closed structured errors. |
| T-475-SC | Tampering | dependency supply chain | accept | No package or dependency manifest changes. |
</threat_model>

<verification>
Run the full Phase 475 rate-limit suite, including concurrency and intervening-traffic receipt assertions.
</verification>

<success_criteria>
- Each logical rate operation has one stable outcome independent of later counter state.
- D-11/V9DATA-04 and CR-09 are directly proved.
</success_criteria>

<output>Create `475-24-SUMMARY.md` when complete.</output>
