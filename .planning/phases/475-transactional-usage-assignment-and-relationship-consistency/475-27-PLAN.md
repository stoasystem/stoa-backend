---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 27
type: execute
wave: 5
depends_on: [475-21, 475-25, 475-26]
files_modified:
  - src/stoa/db/repositories/account_deletion_repo.py
  - src/stoa/services/account_deletion_service.py
  - tests/test_phase475_deletion_teacher_identity_scrub.py
autonomous: true
gap_closure: true
requirements: [V9DATA-02]
must_haves:
  truths:
    - "CR-10: deleting a teacher scrubs that identity from student-owned question and teacher-session state without deleting student content."
    - "Question/session cleanup is version-CAS guarded, retryable on races, and followed by two strong clean epochs."
  artifacts:
    - path: src/stoa/db/repositories/account_deletion_repo.py
      provides: "Teacher question/session reference scrub CAS"
    - path: tests/test_phase475_deletion_teacher_identity_scrub.py
      provides: "Teacher identity discovery-cleanup-quiescence proof"
  key_links:
    - from: src/stoa/services/account_deletion_service.py
      to: src/stoa/db/repositories/account_deletion_repo.py
      via: "question_ocr_session branch distinguishes owner deletion from teacher-reference scrub"
      pattern: "teacher_id|teacher_session"
---

<objective>
Scrub teacher identity from student-owned question/session records for CR-10.

Purpose: prevent teacher identity retention without destroying the student's question or reopening a completed takeover competition.
Output: question/session reference CAS cleanup and an adversarial end-to-end deletion branch test.
</objective>

## Artifacts this phase produces

- A teacher-reference cleanup transition for question rows and a strict tombstone/minimization path for teacher sessions.
- A CAS-loss and late-write test through strong discovery and two later clean epochs.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-21-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-25-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
</context>

<tasks>

<task type="auto" id="475-27-01" tdd="true">
  <name>CAS-scrub teacher question and session identity</name>
  <files>src/stoa/db/repositories/account_deletion_repo.py, src/stoa/services/account_deletion_service.py, tests/test_phase475_deletion_teacher_identity_scrub.py</files>
  <read_first>
    - src/stoa/db/repositories/question_repo.py question/session schema, post-475-17 version transitions, and takeover fields
    - src/stoa/db/repositories/account_deletion_repo.py `PRIVATE_QUESTION_SESSION_FIELDS`, tombstone allowlists, discovery, and transaction conditions
    - src/stoa/services/account_deletion_service.py `_question_ocr_session_branch` and `_run_base_branch`
    - tests/test_phase473_account_deletion.py question/session allowlist and two-clean-epoch tests
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md CR-10
  </read_first>
  <behavior>
    - A student-owned question retains its student content and identity but loses the deleting teacher's direct/history/session linkage under expected version/state CAS.
    - A teacher session naming the deleting teacher is minimized to the strict session tombstone and no raw teacher ID remains.
    - CAS loss or a late matching row resets progress; two subsequent strong clean epochs are required.
  </behavior>
  <action>Add an explicit cross-account teacher-reference scrub instead of sending every discovered row through the owner-deletion tombstone condition. For student-owned questions, condition on exact PK/SK, entity/schema, student owner, deleting `teacher_id` or reviewed teacher-history field, current status, and positive version; remove all reviewed teacher identity/session linkage, increment version once, and use a closed non-authorizing post-deletion status when the prior state depended on that teacher. Preserve question text, attachments, AI result, and student ownership. For teacher sessions, replace with the strict session tombstone under exact identity/version conditions. Treat CAS loss as retryable, then prove discovery, cleanup, one injected race, a late row, and two later clean epochs through the real branch.</action>
  <acceptance_criteria>
    - No raw deleting teacher ID survives in question, session, or reviewed teacher-history fields.
    - Student question content and attachment identities are not deleted by teacher deletion.
    - Cleanup cannot create a new owner/session or reopen takeover authority.
    - Version/CAS loss and late rows prevent premature clean completion.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_deletion_teacher_identity_scrub.py::test_teacher_identity_scrub_preserves_student_question_and_requires_two_clean_epochs && .venv/bin/ruff check src/stoa/db/repositories/account_deletion_repo.py src/stoa/services/account_deletion_service.py tests/test_phase475_deletion_teacher_identity_scrub.py</automated></verify>
  <done>CR-10 teacher question/session identity cleanup is safe, retryable, and quiescence-proved under V9DATA-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Teacher deletion to student-owned learning rows | Privacy cleanup must not destroy another user's retained content or create authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-27-01 | Tampering | question/session CAS | mitigate | Exact owner, identity, state, and version conditions precede field removal. |
| T-475-27-02 | Denial of service | student question | mitigate | Narrow reference scrub preserves student content and attachments. |
| T-475-SC | Tampering | dependency supply chain | accept | No package or manifest change. |
</threat_model>

<verification>Run the focused teacher-deletion branch node and existing takeover/deletion regressions.</verification>

<success_criteria>
- Teacher identity is absent after two clean epochs without losing student-owned question content.
- Concurrent question transitions cannot be overwritten by stale deletion cleanup.
</success_criteria>

<output>Create `475-27-SUMMARY.md` when complete.</output>
