---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 45
type: execute
wave: 11
depends_on: [475-44]
files_modified:
  - docs/security/phase-475-evidence-results.json
  - docs/security/phase-475-evidence.md
autonomous: true
gap_closure: true
requirements: [V9DATA-01, V9DATA-02, V9DATA-03, V9DATA-04, V9DATA-05, V9DATA-06, V9DATA-07, V9DATA-08]
must_haves:
  truths:
    - "Final checked evidence is captured from one clean immutable candidate containing every functional, type, verifier, snapshot, and coverage fix."
    - "All local gates and truthful D/V9DATA/CR/WR selectors pass with zero skip/xfail/xpass; external obligations remain exact later-phase NOT RUN."
    - "The publication commit changes exactly the two evidence files and remains verifiable from later HEAD."
  artifacts:
    - path: docs/security/phase-475-evidence-results.json
      provides: "Machine-readable immutable Phase 475 receipts and coverage"
    - path: docs/security/phase-475-evidence.md
      provides: "Redacted human-readable evidence summary"
  key_links:
    - from: docs/security/phase-475-evidence-results.json
      to: "immutable candidate Git tree"
      via: "candidate SHA, exact argv, receipts, hashes, exhaustive snapshot, and closed coverage"
      pattern: "candidate_sha"
---

<objective>
Publish final source-bound Phase 475 gap evidence after the complete dependency chain is committed.

Purpose: replace the stale false-positive package with reproducible local evidence while preserving later-phase live-system ownership.
Output: exactly one JSON package and one redacted Markdown report bound to one immutable candidate.
</objective>

## Artifacts this phase produces

- Candidate/base identities, exact argv/exit/node receipts, mypy-zero result, exhaustive source snapshot, and closed coverage.
- Exact `NOT RUN` rows for live AWS, provider effects, deployment, and production smoke.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-VERIFICATION.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-44-SUMMARY.md
</context>

<tasks>

<task type="auto" id="475-45-01">
  <name>Capture, verify, and publish immutable final evidence</name>
  <files>docs/security/phase-475-evidence-results.json, docs/security/phase-475-evidence.md</files>
  <read_first>
    - scripts/verify_phase475.py capture, verify-capture, verify-publication, privacy, snapshot, and coverage commands
    - tests/test_phase475_evidence_verifier.py complete adversarial verifier contract
    - docs/security/phase-475-evidence-results.json stale package to replace
    - docs/security/phase-475-evidence.md stale narrative to replace
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md all findings
  </read_first>
  <action>Require a clean committed HEAD containing implementation summaries for 475-14..44 and set that exact SHA as `candidate`. Create the private capture directory with `mktemp -d`; run `capture` then `verify-capture` against the same candidate. Stop on nonzero gates, skip/xfail/xpass, mypy diagnostics, source-snapshot omission, coverage drift, dirty-tree drift, or privacy match; never edit evidence into PASS. Persist only generated JSON/Markdown, prove they are the only publication diff, and commit exactly those paths with the candidate as the publication commit's sole parent. Run `verify-publication` afterward. Do not call AWS, live providers, deployment, or production; retain exact `NOT RUN` rows owned by 479/480.</action>
  <acceptance_criteria>
    - Capture and verify-capture succeed against one clean immutable candidate.
    - All 16 decisions, 8 requirements, 10 blockers, and 4 warnings resolve to observed passing nodes.
    - Mypy receipt records exit zero and zero diagnostics over the exact runtime inventory.
    - Source snapshot cardinality equals the complete base-to-candidate inventory.
    - Publication commit changes exactly two evidence files, has candidate as sole parent, and passes verify-publication.
    - Public artifacts contain no raw identity, answer, idempotency value/digest, storage coordinate, provider diagnostic, or secret.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python scripts/verify_phase475.py verify-publication && .venv/bin/python -m pytest -q tests/test_phase475_evidence_verifier.py</automated></verify>
  <done>Phase 475 has immutable complete local evidence for every gap, with live-system obligations accurately retained for 479/480.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Clean candidate to private capture | HEAD or worktree drift invalidates receipts. |
| Private capture to public evidence | Raw logs cannot enter publication. |
| Candidate to publication commit | Evidence publication must change exactly two generated files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-45-01 | Repudiation | candidate receipts | mitigate | Clean immutable SHA checks before/after every gate. |
| T-475-45-02 | Tampering | publication commit | mitigate | Exact two-path diff and sole-parent verification. |
| T-475-45-03 | Information disclosure | evidence files | mitigate | Opaque nodes plus private/public canary scans. |
| T-475-45-04 | Spoofing | external status | mitigate | Exact owner and NOT RUN rows; no live call. |
| T-475-SC | Tampering | dependency supply chain | accept | No dependency changes. |
</threat_model>

<verification>After the evidence-only commit, run verify-publication and the full verifier adversarial suite.</verification>

<success_criteria>
- Final publication transitively depends on relationship, teacher, deletion, question, rate, replay, type, mypy, snapshot, and coverage fixes.
- The package is reproducible, redacted, and never overclaims external execution.
</success_criteria>

<output>Create `475-45-SUMMARY.md` when complete.</output>
