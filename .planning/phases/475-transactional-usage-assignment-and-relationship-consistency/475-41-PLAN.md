---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 41
type: execute
wave: 2
depends_on: [475-29]
files_modified: [src/stoa/routers/auth.py]
autonomous: true
gap_closure: true
requirements: [V9DATA-08]
must_haves:
  truths:
    - "The auth router passes unfiltered mypy while preserving D-16 terminal deletion projection and zero-reschedule replay."
  artifacts:
    - path: src/stoa/routers/auth.py
      provides: "Type-correct account-deletion endpoint projection"
  key_links:
    - from: src/stoa/routers/auth.py
      to: src/stoa/services/account_deletion_service.py
      via: "terminal receipt `is_terminal` scheduling gate"
      pattern: "is_terminal|completedAt"
---

<objective>
Remove the auth router's real mypy diagnostics after D-16 replay proof.

Purpose: include the real terminal endpoint boundary in honest type evidence.
Output: one type-clean router file.
</objective>

## Artifacts this phase produces

- Explicit optional response-field narrowing at auth/account lifecycle boundaries.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-29-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
</context>

<tasks>
<task type="auto" id="475-41-01">
  <name>Eliminate auth router mypy diagnostics</name>
  <files>src/stoa/routers/auth.py</files>
  <read_first>
    - src/stoa/routers/auth.py complete file and current mypy output
    - tests/test_phase475_completed_deletion_replay.py terminal/pending endpoint contract
  </read_first>
  <action>Resolve every diagnostic with explicit optional text/time narrowing and typed response construction. Preserve canonical identity, terminal nested receipt authority, pending compatibility, D-16 zero scheduling on terminal replay, and structured/redacted errors. Do not add broad ignores, Any propagation, exclusions, config changes, or account-not-found fallbacks.</action>
  <acceptance_criteria>
    - Exact-file mypy exits zero.
    - Completed deletion replay and auth lifecycle suites pass.
    - Terminal/pending response and scheduling behavior is unchanged.
  </acceptance_criteria>
  <verify><automated>.venv/bin/mypy src/stoa/routers/auth.py && .venv/bin/python -m pytest -q tests/test_phase475_completed_deletion_replay.py tests/test_auth_account_lifecycle.py</automated></verify>
  <done>The auth router is mypy-clean with D-16/V9DATA-08 intact.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Verified identity/receipt to deletion API | Optional values must not fabricate terminal success or restart cleanup. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-41-01 | Repudiation | terminal projection | mitigate | Typed receipt plus real replay/zero-effect tests. |
| T-475-SC | Tampering | dependency supply chain | accept | No dependency changes. |
</threat_model>

<verification>Require exact-file mypy zero then real endpoint replay and auth lifecycle tests.</verification>
<success_criteria>- Auth routing is type-clean and terminal replay remains effect-free.</success_criteria>
<output>Create `475-41-SUMMARY.md` when complete.</output>
