---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 05
type: execute
wave: 3
depends_on: [475-04, 475-11]
files_modified:
  - src/stoa/services/notification_service.py
  - src/stoa/routers/teachers.py
  - tests/test_phase475_teacher_takeover_effect.py
autonomous: true
requirements: [V9DATA-02]
must_haves:
  truths:
    - "D-06/D-07: only the winner owns one takeover notification effect, and a post-claim failure is recovered without reopening competition or duplicating effects."
  artifacts:
    - path: tests/test_phase475_teacher_takeover_effect.py
      provides: "Claim-success/effect-failure/retry and exactly-one notification proof"
---

<objective>
Make the winning takeover notification a recoverable exactly-once effect.

Purpose: preserve confirmed teacher ownership when notification creation or delivery temporarily fails.
Output: deterministic takeover effect identity, enqueue/replay behavior, and failure-recovery tests.
</objective>

## Artifacts this phase produces

- `teacher_takeover_effect_id()` and `ensure_teacher_takeover_notification()`.
- New test module `tests/test_phase475_teacher_takeover_effect.py`.

<tasks>

<task type="auto" id="475-05-01">
  <name>Recover the winner's single takeover notification</name>
  <files>src/stoa/services/notification_service.py, src/stoa/routers/teachers.py, tests/test_phase475_teacher_takeover_effect.py</files>
  <read_first>
    - src/stoa/db/repositories/question_repo.py Plan 475-04 takeover result and persisted claim/session identity
    - src/stoa/services/notification_service.py `emit_teacher_takeover` and delivery-intent APIs
    - src/stoa/db/repositories/notification_repo.py deterministic intent registration/claim/completion keys
    - src/stoa/routers/teachers.py Plan 475-04 winner and replay branches
    - tests/test_notifications.py existing notification idempotency conventions
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-06 and D-07
  </read_first>
  <action>Derive one takeover notification operation ID from the persisted question claim/session identity and register it idempotently through the notification intent boundary. After a new claim or same-winner replay, call `ensure_teacher_takeover_notification`; if registration/delivery dependency fails, retain the winner/session and return the durable takeover result with recoverable effect status rather than reverting ownership. Retry reuses the same effect ID and converges the missing notification. A losing claim never invokes the ensure function. Reuse Plan 475-11 typed delivery outcomes so a transient begin failure remains retryable rather than canceled.</action>
  <acceptance_criteria>
    - Injected failure after claim leaves the same teacher owner and same session.
    - Healthy retry produces exactly one notification record/effect and returns the original takeover result.
    - Repeating the successful request produces zero additional notifications.
    - A losing teacher produces zero notification registration/delivery calls.
    - No recovery path reopens `escalated` status or changes the winner.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_teacher_takeover_effect.py tests/test_phase475_teacher_takeover.py tests/test_notifications.py -k 'takeover or delivery or notification' && .venv/bin/ruff check src/stoa/services/notification_service.py src/stoa/routers/teachers.py tests/test_phase475_teacher_takeover_effect.py</automated></verify>
  <done>A confirmed takeover converges one notification without duplicate sessions, notifications, or competition.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| Notification failure revokes valid ownership | Ownership transaction remains authoritative; effect is separately recoverable |
| Retry sends duplicate student notifications | Deterministic effect ID and idempotent delivery intent |
| Loser triggers notification | Ensure call is reachable only from WINNER/RESUME_SELF result |
</threat_model>

<success_criteria>
- V9DATA-02 proves one winner, one session, and one recoverable exactly-once notification.
</success_criteria>

<output>Create `475-05-SUMMARY.md` after effect-recovery tests pass.</output>
