---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 23
type: execute
wave: 2
depends_on: ["475-22"]
files_modified:
  - src/stoa/db/repositories/user_repo.py
  - src/stoa/routers/admin.py
  - tests/test_phase475_parent_binding_transaction.py
  - tests/test_phase475_parent_binding_reconciliation.py
autonomous: true
gap_closure: true
requirements: [V9DATA-03]
must_haves:
  truths:
    - "D-09/D-10: retries cannot revive a revoked or inactive relationship."
    - "Relationship status changes occur only through an authorized, version-fenced lifecycle transition."
  artifacts:
    - path: src/stoa/db/repositories/user_repo.py
      provides: "Status-aware relationship create/replay and lifecycle transition CAS"
    - path: src/stoa/routers/admin.py
      provides: "Authorized relationship status transition boundary"
  key_links:
    - from: src/stoa/routers/admin.py
      to: src/stoa/db/repositories/user_repo.py
      via: "canonical admin authorizes an expected-status/version transition"
      pattern: "relationship.*status"
---

<objective>
Make relationship status a non-revivable, authorization-fenced lifecycle per D-09 and D-10.

Purpose: close CR-06 so create retries and reconciliation never convert a revoked/inactive row back to active.
Output: status-aware create/replay rules, a guarded lifecycle transition path, and non-revival tests.
</objective>

## Artifacts this phase produces

- Create/replay conditions that distinguish active equality from revoked/inactive terminal history.
- Canonical-admin status transition with expected current status/version fencing.
- Retry and reconciliation tests proving revoked/inactive relationships stay non-active.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-VERIFICATION.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-22-SUMMARY.md
</context>

<tasks>

<task type="auto" id="475-23-01" tdd="true">
  <name>Prevent relationship retry revival and guard lifecycle transitions</name>
  <files>src/stoa/db/repositories/user_repo.py, src/stoa/routers/admin.py, tests/test_phase475_parent_binding_transaction.py, tests/test_phase475_parent_binding_reconciliation.py</files>
  <read_first>
    - src/stoa/db/repositories/user_repo.py existing relationship equality condition, conflict helper, update expression, and reconciliation
    - src/stoa/routers/admin.py canonical admin dependency and relationship management routes
    - tests/test_phase475_parent_binding_transaction.py same-pair replay and conflict cases
    - tests/test_phase475_parent_binding_reconciliation.py existing-row recovery cases
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md CR-06
  </read_first>
  <behavior>
    - Exact replay of an already active relationship is idempotent.
    - A create retry against revoked or inactive history returns a structured conflict and never writes `active`.
    - Only canonical `admin` can transition status, and stale expected status/version loses atomically.
  </behavior>
  <action>Include `status` in relationship identity/equality checks and in the conflict classifier. Remove any create/update expression that unconditionally assigns `active` to an existing revoked/inactive row. Treat only an already-active exact tuple as replay success; return the established structured/redacted conflict for non-active history. Add or tighten the backend admin lifecycle operation so only canonical `admin` may perform an explicit status transition guarded by expected status and relationship version, with no legacy role aliases and no public storage coordinates. Apply the same rules in reconciliation and assert the student profile pointer is not rewritten during a rejected revival.</action>
  <acceptance_criteria>
    - Same-pair active replay is stable and side-effect free.
    - Revoked/inactive rows remain unchanged under create retry and reconciliation.
    - Status changes require canonical `admin` authorization and a successful expected-status/version CAS.
    - Rejected operations use structured, redacted public errors.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_parent_binding_transaction.py tests/test_phase475_parent_binding_reconciliation.py -k 'status or revoked or inactive or replay or admin' && .venv/bin/ruff check src/stoa/db/repositories/user_repo.py src/stoa/routers/admin.py tests/test_phase475_parent_binding_transaction.py tests/test_phase475_parent_binding_reconciliation.py</automated></verify>
  <done>CR-06 is closed: retry paths preserve revoked/inactive history and lifecycle mutation is explicitly authorized and version-fenced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Relationship retry to persisted lifecycle | A duplicate create must not become authority to reactivate a revoked binding. |
| Admin request to lifecycle CAS | Untrusted status input attempts a privileged transition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-23-01 | Elevation of privilege | relationship retry | mitigate | Non-active history is a conflict, not an implicit reactivation. |
| T-475-23-02 | Tampering | relationship status | mitigate | Canonical-admin authorization plus expected status/version CAS. |
| T-475-SC | Tampering | dependency supply chain | accept | No package or dependency manifest changes. |
</threat_model>

<verification>
Run status-focused transaction and reconciliation tests and inspect that no create expression can overwrite non-active status.
</verification>

<success_criteria>
- Revocation/inactivation is monotonic across retries unless an authorized fenced transition explicitly changes it.
- D-09/D-10, V9DATA-03, and CR-06 are covered.
</success_criteria>

<output>Create `475-23-SUMMARY.md` when complete.</output>
