---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 22
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stoa/db/repositories/user_repo.py
  - tests/test_phase475_parent_binding_transaction.py
  - tests/test_phase475_parent_binding_reconciliation.py
autonomous: true
gap_closure: true
requirements: [V9DATA-03]
must_haves:
  truths:
    - "D-09/D-10: relationship creation commits only while both canonical `parent` and `student` profiles remain active and at the versions that authorization observed."
    - "A parent suspension/profile edit or student suspension/profile edit causes total rollback."
  artifacts:
    - path: src/stoa/db/repositories/user_repo.py
      provides: "Dual-account lifecycle and profile-version transaction fences"
    - path: tests/test_phase475_parent_binding_transaction.py
      provides: "Parent and student race matrix"
  key_links:
    - from: src/stoa/db/repositories/user_repo.py
      to: "parent and student PROFILE rows"
      via: "two authoritative ConditionCheck operations in the relationship transaction"
      pattern: "profile_version"
---

<objective>
Bind relationship creation to both participants' current account state per D-09 and D-10.

Purpose: close CR-05 so the parent's authorization cannot become stale while a student binding commits.
Output: symmetric parent/student lifecycle-version fences and real transaction rollback tests.
</objective>

## Artifacts this phase produces

- Parent and student profile conditions in the relationship transaction.
- Tests for suspension, deletion, role change, and profile-version races on either participant.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-VERIFICATION.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
</context>

<tasks>

<task type="auto" id="475-22-01" tdd="true">
  <name>Fence relationship writes with both active account versions</name>
  <files>src/stoa/db/repositories/user_repo.py, tests/test_phase475_parent_binding_transaction.py, tests/test_phase475_parent_binding_reconciliation.py</files>
  <read_first>
    - src/stoa/db/repositories/user_repo.py relationship transaction builder, lifecycle observations, and reconciliation path
    - tests/test_phase475_parent_binding_transaction.py transaction interpreter and current student fence assertions
    - tests/test_phase475_parent_binding_reconciliation.py replay/recovery fixtures
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-09 and D-10
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md CR-05
  </read_first>
  <behavior>
    - A canonical active `parent` and canonical active `student` at the observed versions bind atomically.
    - Parent or student suspension, deletion, role change, or version advance before commit cancels every relationship/profile write.
    - Reconciliation applies the same dual fence and cannot bypass it.
  </behavior>
  <action>Represent both authorization observations explicitly and add authoritative profile-row `ConditionCheck`s to the same relationship `TransactWriteItems`: parent must be canonical `parent`, active/non-deleted, and at its observed profile version; student must be canonical `student`, active/non-deleted, and at its observed profile version. Do not accept legacy role aliases. Ensure the command/reconciliation path carries both observations and cannot reconstruct a permissive fallback. Extend the transaction test adapter to evaluate both conditions and assert all-or-nothing rollback, including no relationship row, no student parent pointer change, and no command completion on either race.</action>
  <acceptance_criteria>
    - Both participant profile keys and observed versions are inputs to the relationship transaction.
    - Canonical roles are exactly `parent` and `student`.
    - Every parent-side and student-side lifecycle/version race leaves all artifacts unchanged.
    - Reconciliation cannot commit with stale or inactive participant facts.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_parent_binding_transaction.py tests/test_phase475_parent_binding_reconciliation.py && .venv/bin/ruff check src/stoa/db/repositories/user_repo.py tests/test_phase475_parent_binding_transaction.py tests/test_phase475_parent_binding_reconciliation.py</automated></verify>
  <done>CR-05 is closed: relationship authorization is fenced symmetrically at the atomic commit boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Parent API authorization to relationship transaction | Either account may change after authorization and before commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-22-01 | Elevation of privilege | parent binding | mitigate | Canonical parent and student roles plus active lifecycle are checked in-transaction. |
| T-475-22-02 | Tampering | concurrent profile update | mitigate | Observed profile versions fence both participants and cancel stale writes. |
| T-475-SC | Tampering | dependency supply chain | accept | No package or dependency manifest changes. |
</threat_model>

<verification>
Exercise all parent/student race combinations through the actual transaction request and reconciliation entry point.
</verification>

<success_criteria>
- Relationship creation cannot outlive either participant's authorization state.
- D-09/D-10, V9DATA-03, and CR-05 have executable evidence.
</success_criteria>

<output>Create `475-22-SUMMARY.md` when complete.</output>
