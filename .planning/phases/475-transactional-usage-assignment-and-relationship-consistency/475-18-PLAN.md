---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 18
type: execute
wave: 4
depends_on: [475-15, 475-17]
files_modified:
  - src/stoa/db/repositories/question_submission_repo.py
  - src/stoa/routers/questions.py
  - tests/test_phase475_question_effect_recovery.py
  - tests/test_phase475_question_replay.py
autonomous: true
gap_closure: true
requirements: [V9DATA-01]
must_haves:
  truths:
    - "D-01/D-02: OCR and AI effect intent/result are durable, and provider success followed by local question/command failure converges from the saved original result without another provider call."
  artifacts:
    - path: src/stoa/db/repositories/question_submission_repo.py
      provides: "Versioned question provider-effect intent/result/completion state machine"
    - path: tests/test_phase475_question_effect_recovery.py
      provides: "Real repository commit/failure/strong-replay recovery harness"
  key_links:
    - from: src/stoa/routers/questions.py
      to: src/stoa/db/repositories/question_submission_repo.py
      via: "begin effect, store result receipt, complete command and question"
      pattern: "question_.*effect"
---

<objective>
Durably receipt question provider effects and recover local completion per D-01/D-02.

Purpose: close CR-01 and replace WR-04's mocked proof with a real persistence-boundary failure matrix.
Output: effect state machine, conditional completion transaction, and source-real recovery tests.
</objective>

## Artifacts this phase produces

- `QuestionEffectKind`, `QuestionEffectDisposition`, and durable effect intent/result schema for OCR and AI.
- Conditional question-result plus submission-command completion transaction.
- `tests/test_phase475_question_effect_recovery.py` with commit-before-timeout and post-provider local-failure cases.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-02-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-17-SUMMARY.md
</context>

<tasks>

<task type="auto" id="475-18-01" tdd="true">
  <name>Recover OCR and AI success from durable effect receipts</name>
  <files>src/stoa/db/repositories/question_submission_repo.py, src/stoa/routers/questions.py, tests/test_phase475_question_effect_recovery.py, tests/test_phase475_question_replay.py</files>
  <read_first>
    - src/stoa/routers/questions.py `_build_question_content` and post-admission OCR/AI blocks
    - src/stoa/db/repositories/question_submission_repo.py command schema/version, admission, preview/apply completion
    - src/stoa/db/repositories/question_repo.py Plan 475-17 state/version CAS contract
    - tests/test_phase475_question_replay.py current monkeypatched lost-response test
    - tests/test_phase475_question_admission.py transaction-applying fake pattern
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md CR-01 and WR-04
  </read_first>
  <behavior>
    - Provider result is stored under the exact command/question/fingerprint/generation/effect identity before question completion is attempted.
    - If completion fails or its response is lost, same-key replay uses the stored result and performs one conditional completion with zero additional provider calls.
  </behavior>
  <action>Persist a versioned OCR/AI effect intent before each provider boundary using deterministic identities derived from the opaque command digest, fingerprint, question ID, owner generation, and effect kind. After success, persist a strict bounded private result receipt before updating the public question; reject malformed/foreign/stale receipts. Complete the question row and command in one transaction that checks command version/state/effect receipt and Plan 475-17 question version/state, writes the original OCR/AI result, and increments both versions. Model pre-provider dependency failure, terminal provider rejection, provider-outcome-unknown, result-receipt ambiguity, completion dependency failure, and completion-committed-response-lost as distinct closed states. Automatic replay may reuse a durable result receipt but must not blindly invoke a provider from an unknown/inflight state. Replace the principal replay proof with a fake that executes real repository operations and injects provider success followed by pre-commit failure, commit-before-timeout, and conditional loss.</action>
  <acceptance_criteria>
    - OCR/AI success plus local completion failure leaves a strong-readable result receipt bound to exact command/question owner/schema/version/generation.
    - Same-key replay completes from that receipt and invokes OCR/AI zero additional times.
    - Committed-but-timeout completion reconciles to the exact original final question.
    - Unknown/inflight provider outcome is never projected as success and never causes a blind duplicate provider call.
    - The coverage node executes real admission, effect-receipt, question update, and command-completion repository code rather than monkeypatching them away.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_question_effect_recovery.py tests/test_phase475_question_replay.py tests/test_phase475_question_admission.py && .venv/bin/ruff check src/stoa/db/repositories/question_submission_repo.py src/stoa/routers/questions.py tests/test_phase475_question_effect_recovery.py tests/test_phase475_question_replay.py</automated></verify>
  <done>CR-01 and WR-04 are closed: provider success is durably recoverable and proven through the real persistence boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| OCR/AI provider to durable state | External success can precede a failed/ambiguous local write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-18-01 | Tampering | effect receipt | mitigate | Bind schema, owner, generation, command, fingerprint, question, kind, and versions. |
| T-475-18-02 | Repudiation | ambiguous completion | mitigate | Strong-read receipt/command/question reconciliation with deterministic effect identity. |
| T-475-18-03 | Information disclosure | private provider result | mitigate | Store only in private rows and keep public errors structured/redacted. |
| T-475-SC | Tampering | dependency supply chain | accept | No new package is installed. |
</threat_model>

<verification>
Run the real-operation failure matrix, existing replay/admission tests, and Ruff.
</verification>

<success_criteria>
- Failed Truth 1's provider-success/local-write window converges safely.
- V9DATA-01 evidence no longer mocks away the persistence boundary.
</success_criteria>

<output>Create `475-18-SUMMARY.md` when complete.</output>
