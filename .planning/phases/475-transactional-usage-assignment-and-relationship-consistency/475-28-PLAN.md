---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 28
type: execute
wave: 2
depends_on: [475-25]
files_modified:
  - src/stoa/db/repositories/notification_repo.py
  - tests/test_phase475_deletion_notification_identity_scrub.py
autonomous: true
gap_closure: true
requirements: [V9DATA-07]
must_haves:
  truths:
    - "CR-10: notification rows owned by another account cannot retain the deleting actor or metadata identity."
    - "Reference cleanup preserves recipient/delivery state, uses exact CAS, retries races, and requires two later strong clean epochs."
  artifacts:
    - path: src/stoa/db/repositories/notification_repo.py
      provides: "Notification actor/metadata discovery and reference scrub CAS"
    - path: tests/test_phase475_deletion_notification_identity_scrub.py
      provides: "Notification discovery-cleanup-two-clean-epoch proof"
  key_links:
    - from: src/stoa/services/account_deletion_service.py
      to: src/stoa/db/repositories/notification_repo.py
      via: "notification_device_realtime branch consumes owner and reference matches"
      pattern: "scan_notification_private_rows|scrub_notification"
---

<objective>
Scrub notification actor and metadata identities end to end for CR-10.

Purpose: remove cross-account identity while preserving recipient-owned notification and accepted/unknown delivery evidence.
Output: reference-aware scan/scrub behavior and a real branch quiescence test.
</objective>

## Artifacts this phase produces

- Closed notification identity field classification for direct actor and reviewed metadata references.
- Narrow reference scrub distinct from full owner tombstoning.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-25-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-11-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
</context>

<tasks>

<task type="auto" id="475-28-01" tdd="true">
  <name>CAS-scrub notification actor and metadata identity</name>
  <files>src/stoa/db/repositories/notification_repo.py, tests/test_phase475_deletion_notification_identity_scrub.py</files>
  <read_first>
    - src/stoa/db/repositories/notification_repo.py private registry, `_targets_owner`, scan, scrub, tombstone, and version fields
    - src/stoa/services/account_deletion_service.py `_notification_device_realtime_branch` dirty/epoch logic
    - tests/test_phase473_notification_deletion.py strong scan, scrub, receipt, and branch fake patterns
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md CR-10
  </read_first>
  <behavior>
    - Direct `actor_id` and reviewed metadata actor/teacher/parent references select the row even when recipient ownership belongs to another account.
    - Reference-only cleanup removes the deleting identity while preserving recipient, event/effect identity, delivery status, and accepted/unknown external debt facts.
    - Version/CAS loss and a late notification reset progress; two subsequent strong clean epochs complete.
  </behavior>
  <action>Extend notification private discovery with a closed exact reference classifier for `actor_id` and reviewed metadata identity keys. Keep owner matches on the existing full-tombstone path. Add a separate reference-only scrub that conditions the deleting account fence plus exact PK/SK/entity/schema, matched identity, current version/status, and metadata digest; remove only direct/nested deleting-identity fields and increment version while preserving recipient/delivery/effect fields and external accepted/unknown status. Make row-condition loss retryable to the existing branch rather than clean. Test through `_notification_device_realtime_branch` with discovery, one CAS race, cleanup, a late row, and two later clean epochs.</action>
  <acceptance_criteria>
    - Cross-account actor/metadata matches are discovered and scrubbed.
    - Recipient and delivery/effect receipt fields remain unchanged for reference-only cleanup.
    - Malformed metadata or CAS loss fails closed without broad exception-to-complete conversion.
    - Two clean strong epochs follow the last matching notification.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_deletion_notification_identity_scrub.py::test_notification_identity_scrub_retries_cas_then_requires_two_clean_epochs && .venv/bin/ruff check src/stoa/db/repositories/notification_repo.py tests/test_phase475_deletion_notification_identity_scrub.py</automated></verify>
  <done>CR-10 notification actor/metadata cleanup is functional, CAS-safe, and quiescence-proved under V9DATA-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Deleted actor to recipient-owned notification | Identity removal must preserve the recipient's durable delivery state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-28-01 | Information disclosure | notification metadata | mitigate | Closed direct/nested identity scrub removes the deleting account reference. |
| T-475-28-02 | Tampering | delivery receipt | mitigate | Reference-only CAS preserves recipient/effect/status fields. |
| T-475-SC | Tampering | dependency supply chain | accept | No package or manifest change. |
</threat_model>

<verification>Run the focused notification deletion node plus inherited notification deletion and delivery recovery suites.</verification>

<success_criteria>
- No deleting identity remains in reviewed notification actor/metadata fields after two clean epochs.
- Delivery recovery and external-debt semantics remain unchanged.
</success_criteria>

<output>Create `475-28-SUMMARY.md` when complete.</output>
