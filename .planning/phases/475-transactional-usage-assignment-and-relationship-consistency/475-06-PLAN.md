---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stoa/db/repositories/user_repo.py
  - src/stoa/routers/admin.py
  - src/stoa/routers/auth.py
  - tests/test_phase475_parent_binding_transaction.py
  - tests/test_admin_authorization.py
autonomous: true
requirements: [V9DATA-03]
must_haves:
  truths:
    - "D-09/D-10: forward row, reverse row, and required student profile projection never commit one-sided and never overwrite a conflicting parent."
  artifacts:
    - path: src/stoa/db/repositories/user_repo.py
      provides: "Composed conditional parent-binding/profile transaction"
    - path: tests/test_phase475_parent_binding_transaction.py
      provides: "Failure, replay, conflict, and fail-closed authorization proof"
---

<objective>
Make parent/student binding plus required profile projection one conditional transaction.

Purpose: remove the remaining profile-then-binding partial-write window.
Output: one idempotent relationship write primitive and migrated registration/admin callers.
</objective>

## Artifacts this phase produces

- `ParentBindingDisposition`, `ParentBindingResult`, `build_parent_binding_transaction()`, and `put_parent_student_relationship()`.
- New test module `tests/test_phase475_parent_binding_transaction.py`.

<tasks>

<task type="auto" id="475-06-01">
  <name>Compose both relationship directions and profile link atomically</name>
  <files>src/stoa/db/repositories/user_repo.py, src/stoa/routers/admin.py, src/stoa/routers/auth.py, tests/test_phase475_parent_binding_transaction.py, tests/test_admin_authorization.py</files>
  <read_first>
    - src/stoa/db/repositories/user_repo.py `put_parent_student_binding`, `update_student_parent_link`, profile update helpers, and strong binding reads
    - src/stoa/routers/admin.py current `/parent-bindings/repair` write order and capability target
    - src/stoa/routers/auth.py registration binding call sites
    - src/stoa/security/authorization.py strict bidirectional parent authorization
    - tests/test_student_authorization_matrix.py asymmetric relationship denial
    - tests/test_admin_authorization.py current repair authorization/mutation order
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-09 and D-10
  </read_first>
  <action>Create one transaction containing the student's active account fence, conditional forward CHILD row, conditional reverse PARENT row, and narrow student PROFILE update for `parent_id`, `relationship`, and `parent_binding_status`. Identical active relationship replay is success with zero conflicting overwrite. Conditions permit absent or exact same parent/student/version identity but reject a different parent, student, or relationship. Migrate registration and admin repair away from separate `update_student_parent_link` then `put_parent_student_binding` calls. Keep strict bidirectional reads as the only authorization authority and map conflicts to a structured admin-safe result without auto-selecting either side.</action>
  <acceptance_criteria>
    - Failure injection at every operation leaves all three relationship projections unchanged.
    - Identical replay returns the original relationship and does not create a new version/history.
    - A conflicting parent remains unchanged and authorization stays denied.
    - Registration and admin repair use only `put_parent_student_relationship` for the logical write.
    - Existing parent authorization tests still require matching active forward and reverse rows.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_parent_binding_transaction.py tests/test_admin_authorization.py tests/test_student_authorization_matrix.py tests/test_parent_children.py -k 'binding or bidirectional or unrelated or repair' && .venv/bin/ruff check src/stoa/db/repositories/user_repo.py src/stoa/routers/admin.py src/stoa/routers/auth.py tests/test_phase475_parent_binding_transaction.py</automated></verify>
  <done>Parent relationship authority and its profile projection commit together or not at all, with conflicts preserved for administrator resolution.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| Half-written relationship grants access | Three projections and account fence share one transaction |
| Repair overwrites a different parent | Exact identity/version conditions; conflict is report-only |
| Legacy profile field broadens authority | Authorization continues to require both formal active rows |
</threat_model>

<success_criteria>
- V9DATA-03's normal relationship write cannot commit asymmetrically.
</success_criteria>

<output>Create `475-06-SUMMARY.md` after relationship transaction and authorization tests pass.</output>
