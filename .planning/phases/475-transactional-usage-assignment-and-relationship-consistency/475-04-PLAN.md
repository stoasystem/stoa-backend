---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 04
type: execute
wave: 2
depends_on: [475-01]
files_modified:
  - src/stoa/db/repositories/question_repo.py
  - src/stoa/routers/teachers.py
  - tests/test_phase475_teacher_takeover.py
  - tests/test_teacher_dispatch.py
autonomous: true
requirements: [V9DATA-02]
must_haves:
  truths:
    - "D-05/D-06: one atomic backend claim is the only winner and losing teachers create no owner/session/notification side effect."
    - "D-08: the losing public response does not disclose the winning teacher identity."
  artifacts:
    - path: src/stoa/db/repositories/question_repo.py
      provides: "Conditional teacher claim plus deterministic session transaction"
    - path: tests/test_phase475_teacher_takeover.py
      provides: "Barrier-synchronized one-winner proof"
---

<objective>
Make teacher takeover one atomic claim with one deterministic session.

Purpose: eliminate the read-then-update/session race before adding notification recovery.
Output: typed claim outcomes, one transaction, deterministic 409 loser, and barrier concurrency tests.
</objective>

## Artifacts this phase produces

- `TeacherTakeoverDisposition`, `TeacherTakeoverResult`, `teacher_takeover_claim_id()`, `teacher_session_id_for_claim()`, and `claim_teacher_takeover()`.
- New lower-boundary test module `tests/test_phase475_teacher_takeover.py`.

<tasks>

<task type="auto" id="475-04-01">
  <name>Implement atomic teacher claim and session</name>
  <files>src/stoa/db/repositories/question_repo.py, src/stoa/routers/teachers.py, tests/test_phase475_teacher_takeover.py, tests/test_teacher_dispatch.py</files>
  <read_first>
    - src/stoa/routers/teachers.py current `takeover` route and dispatch restrictions
    - src/stoa/db/repositories/question_repo.py conditional status and session helpers
    - src/stoa/services/teacher_reply_service.py SLA field calculation
    - tests/test_teacher_dispatch.py dispatch-owner behavior
    - tests/test_teacher_reply_sla.py takeover timestamps and SLA regression
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-05, D-06, and D-08
  </read_first>
  <action>Derive a durable claim identity and session ID from the question and winning claim rather than generating a new random session on every request. In one transaction, condition the question on `status=escalated`, current version, and any active dispatch owner/accept deadline; update owner/status/session/SLA fields and put the deterministic session with `attribute_not_exists`. On transaction contention, strongly read question/session: replay the same teacher's exact result, otherwise return ALREADY_CLAIMED without winner identity. Make the route return 200 for the winner/same-winner retry and a stable structured 409 action for every loser. Remove the unconditional `update_status` + independent `create_teacher_session` sequence. Do not emit notifications in this plan.</action>
  <acceptance_criteria>
    - Two barrier-synchronized teachers produce exactly one 200 and one deterministic 409.
    - Final state has one teacher owner and one session whose teacher matches the owner.
    - Same-winner retry returns the same session ID and performs no write.
    - Losing result/body/log has no winning teacher ID or name.
    - Stale/wrong dispatch owner cannot win, and existing dispatch/SLA regressions remain green.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_teacher_takeover.py tests/test_teacher_dispatch.py tests/test_teacher_reply_sla.py && .venv/bin/ruff check src/stoa/db/repositories/question_repo.py src/stoa/routers/teachers.py tests/test_phase475_teacher_takeover.py</automated></verify>
  <done>Teacher takeover has one atomic winner and session, deterministic loser behavior, and zero loser side effects.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| Two teachers both receive ownership | Conditional question transition and session put in one transaction |
| Loser learns another teacher identity | Generic structured conflict projection |
| Retry creates a second session | Deterministic session ID and same-winner strong replay |
</threat_model>

<success_criteria>
- V9DATA-02's owner/session concurrency boundary passes a real barrier test.
</success_criteria>

<output>Create `475-04-SUMMARY.md` after concurrency tests pass.</output>
