---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 29
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase475_completed_deletion_replay.py
autonomous: true
gap_closure: true
requirements: [V9DATA-08]
must_haves:
  truths:
    - "D-16: a completed deletion request replays the exact persisted terminal receipt and performs zero new cleanup or scheduling effects."
  artifacts:
    - path: tests/test_phase475_completed_deletion_replay.py
      provides: "Deterministic terminal receipt and zero-effect replay proof"
  key_links:
    - from: src/stoa/services/account_deletion_service.py
      to: src/stoa/routers/auth.py
      via: "stored terminal receipt projects through the endpoint scheduling gate"
      pattern: "is_terminal"
---

<objective>
Lock deterministic completed account-deletion replay to D-16.

Purpose: preserve the already implemented V9DATA-08 behavior while CR-10 deletion internals change.
Output: a stronger service/endpoint regression node; no production mutation in this plan.
</objective>

## Artifacts this phase produces

- Deep equality among first response, persisted nested receipt, and repeated terminal replays.
- Effect spies proving no scan, cleanup, provider, clock, scheduler, or repository write occurs on replay.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-12-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-VERIFICATION.md
</context>

<tasks>

<task type="auto" id="475-29-01" tdd="true">
  <name>Prove deterministic terminal deletion receipt replay</name>
  <files>tests/test_phase475_completed_deletion_replay.py</files>
  <read_first>
    - tests/test_phase475_completed_deletion_replay.py current real endpoint lost-response fixture
    - src/stoa/services/account_deletion_service.py terminal receipt validation and replay branch
    - src/stoa/routers/auth.py terminal scheduling gate
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-16
  </read_first>
  <behavior>
    - Original, stored, and multiple replayed terminal response payloads are deeply equal despite a changed clock.
    - Replay executes zero discovery, cleanup, provider, scheduler, or persistence effects.
    - Public receipt contains no storage/provider coordinate or internal exception data.
  </behavior>
  <action>Strengthen the existing real endpoint test: capture the nested stored receipt after completion, replay with a changed clock and fail-if-called spies for every branch/provider/scheduler/write boundary, and assert deep equality across the original response, stored receipt, and repeated response bodies. Assert terminal status/count/completion identity remains stable and reject internal table keys, provider tokens, and diagnostics. Do not change production code; if the assertion finds a regression, keep the test failing for functional follow-up.</action>
  <acceptance_criteria>
    - Repeated completed requests return one byte-stable durable outcome.
    - Changed wall-clock time cannot alter terminal metadata.
    - No replay effect spy is invoked.
    - Receipt remains structured and redacted.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_completed_deletion_replay.py::test_completed_deletion_replays_stored_receipt_without_new_effects && .venv/bin/ruff check tests/test_phase475_completed_deletion_replay.py</automated></verify>
  <done>D-16 and V9DATA-08 retain deterministic, durable, redacted, effect-free terminal replay evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Persisted terminal command to public API | A replay must project stored evidence without reopening destructive work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-29-01 | Repudiation | terminal receipt | mitigate | Deep equality binds first response, stored receipt, and replays. |
| T-475-29-02 | Information disclosure | public receipt | mitigate | Negative field assertions reject internal/provider coordinates. |
| T-475-SC | Tampering | dependency supply chain | accept | Test-only plan with no package changes. |
</threat_model>

<verification>Run the exact completed-replay node independently before aggregate capture.</verification>

<success_criteria>
- D-16 is the only CONTEXT mapping claimed by this plan.
- Terminal replay remains unchanged after CR-10 cleanup work.
</success_criteria>

<output>Create `475-29-SUMMARY.md` when complete.</output>
