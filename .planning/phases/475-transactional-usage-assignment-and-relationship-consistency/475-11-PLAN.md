---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stoa/db/repositories/notification_repo.py
  - src/stoa/services/notification_service.py
  - tests/test_phase475_delivery_begin.py
  - tests/test_phase473_delivery_intent_recovery.py
autonomous: true
requirements: [V9DATA-07]
must_haves:
  truths:
    - "D-15: only positively proven account deletion terminalizes delivery; conditional loss and transient dependency failure stay distinct and recoverable."
  artifacts:
    - path: src/stoa/db/repositories/notification_repo.py
      provides: "Typed delivery-begin outcomes"
    - path: tests/test_phase475_delivery_begin.py
      provides: "Dependency-failure/healthy-retry/exactly-once proof"
---

<objective>
Separate delivery-begin business conflicts from transient dependency failures.

Purpose: prevent database/provider outages from being permanently mislabeled as account deletion.
Output: typed repository taxonomy, correct service routing, and healthy-retry exactly-once tests.
</objective>

## Artifacts this phase produces

- `DeliveryBeginDisposition`, `DeliveryBeginResult`, and `classify_delivery_transaction_failure()`.
- New test module `tests/test_phase475_delivery_begin.py`.

<tasks>

<task type="auto" id="475-11-01">
  <name>Implement typed delivery-begin outcomes and recoverable dependency retry</name>
  <files>src/stoa/db/repositories/notification_repo.py, src/stoa/services/notification_service.py, tests/test_phase475_delivery_begin.py, tests/test_phase473_delivery_intent_recovery.py</files>
  <read_first>
    - src/stoa/db/repositories/notification_repo.py `begin_delivery_effect`, cancellation-reason helpers, and intent terminal states
    - src/stoa/services/notification_service.py `run_delivery_intent` sendable/begin/cancel branches
    - src/stoa/db/repositories/account_deletion_repo.py fence conflict and dependency exception behavior
    - tests/test_phase473_delivery_intent_recovery.py pre-effect/inflight/accepted crash recovery harness
    - tests/test_phase473_notification_deletion.py deletion race and zero-provider assertions
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-15
  </read_first>
  <action>Return a typed result from `begin_delivery_effect` that distinguishes BEGUN, CLAIM_LOST, PROVEN_ACCOUNT_DELETED, and DEPENDENCY_RETRY. Use ordered transaction cancellation reasons plus a strong authoritative fence/intent reread; never infer deletion from an unclassified `ClientError`, timeout, malformed dependency response, throttle, or broad `AccountDeletionConflict`. In `run_delivery_intent`, cancel only PROVEN_ACCOUNT_DELETED, return retryable conflict for CLAIM_LOST, and leave the claim/intent recoverable for DEPENDENCY_RETRY. Preserve the no-call window between durable BEGUN and provider invocation and existing acceptance-unknown semantics after provider mutation.</action>
  <acceptance_criteria>
    - Injected nonconditional failure below transaction begin returns retryable and does not set `canceled_account_deletion`.
    - Healthy retry reserves, begins, calls provider, and completes exactly once.
    - Proven deleted fence cancels terminally with zero provider calls.
    - Conditional claim/scope loss remains a retryable conflict and is not mislabeled deletion.
    - Existing pre-effect/inflight/provider-unknown recovery tests remain green.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_delivery_begin.py tests/test_phase473_delivery_intent_recovery.py tests/test_phase473_notification_deletion.py && .venv/bin/ruff check src/stoa/db/repositories/notification_repo.py src/stoa/services/notification_service.py tests/test_phase475_delivery_begin.py</automated></verify>
  <done>Delivery begin terminalizes only proven deletion and recovers cleanly from transient dependencies without duplicate provider effects.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| Database outage permanently drops notification | DEPENDENCY_RETRY typed outcome and preserved intent |
| Deleted account receives provider delivery | Strong fence proof before terminal cancellation/provider call |
| Retry calls provider twice | Durable begin state and existing acceptance-unknown recovery |
</threat_model>

<success_criteria>
- V9DATA-07 and `delivery-begin-dependency-classification` pass typed failure and healthy-retry proof.
</success_criteria>

<output>Create `475-11-SUMMARY.md` after delivery recovery tests pass.</output>
