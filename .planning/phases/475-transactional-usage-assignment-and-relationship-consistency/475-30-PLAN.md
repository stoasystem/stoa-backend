---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 30
type: execute
wave: 5
depends_on: ["475-21"]
files_modified:
  - src/stoa/routers/teachers.py
autonomous: true
gap_closure: true
requirements: [V9DATA-02]
must_haves:
  truths:
    - "The Phase 475 teacher router passes real mypy with exit code zero after the active-teacher fence is installed."
    - "Typing cleanup preserves canonical `teacher` authorization and atomic takeover behavior."
  artifacts:
    - path: src/stoa/routers/teachers.py
      provides: "Type-correct teacher API boundary"
  key_links:
    - from: src/stoa/routers/teachers.py
      to: src/stoa/db/repositories/question_repo.py
      via: "typed authorized teacher facts passed to takeover"
      pattern: "teacher_account|profile_version"
---

<objective>
Make the teacher API boundary pass unfiltered mypy after the lifecycle fence lands.

Purpose: remove teacher-route diagnostics without obscuring or weakening the CR-04 fix.
Output: a type-clean teacher router and preserved takeover/reply/resolve tests.
</objective>

## Artifacts this phase produces

- Mypy-clean `teachers.py` using precise authenticated teacher and question result types.
- Runtime proof that canonical-role and takeover CAS behavior is unchanged.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-21-SUMMARY.md
</context>

<tasks>

<task type="auto" id="475-30-01">
  <name>Eliminate teacher router mypy diagnostics after lifecycle fencing</name>
  <files>src/stoa/routers/teachers.py</files>
  <read_first>
    - src/stoa/routers/teachers.py full file with current mypy diagnostic locations
    - src/stoa/db/repositories/question_repo.py post-475-21 takeover signature/result
    - tests/test_phase475_teacher_takeover.py active-teacher race contract
    - tests/test_phase475_teacher_takeover_effect.py notification effect contract
    - mypy.ini or pyproject.toml active mypy configuration
  </read_first>
  <action>Resolve all mypy diagnostics in the teacher router with precise authorization-fact, request, repository-result, and response types plus explicit optional narrowing. Retain canonical role `teacher` only; do not add aliases or permissive defaults when teacher account/version facts are absent. Preserve structured/redacted public errors and every takeover/reply/resolve runtime branch. Do not use broad ignores, `Any` propagation, or configuration changes.</action>
  <acceptance_criteria>
    - Unfiltered mypy exits zero for `src/stoa/routers/teachers.py`.
    - Missing or stale teacher facts still fail closed.
    - Canonical `teacher` role checks and transaction-fence inputs from 475-21 are unchanged.
    - Takeover and notification-effect suites pass.
  </acceptance_criteria>
  <verify><automated>.venv/bin/mypy src/stoa/routers/teachers.py && .venv/bin/python -m pytest -q tests/test_phase475_teacher_takeover.py tests/test_phase475_teacher_takeover_effect.py</automated></verify>
  <done>The teacher API contributes zero diagnostics to final mypy while retaining the atomic authorization fence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Auth facts to teacher route | Optional or malformed facts must not be narrowed optimistically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-30-01 | Elevation of privilege | teacher authorization | mitigate | Explicit fail-closed narrowing and canonical `teacher` role retention. |
| T-475-30-02 | Information disclosure | teacher public errors | mitigate | Existing structured/redacted envelopes remain regression-tested. |
| T-475-SC | Tampering | dependency supply chain | accept | No package or manifest changes. |
</threat_model>

<verification>
Require exact-file mypy exit zero before takeover/effect regressions.
</verification>

<success_criteria>
- Teacher routing is type-clean and CR-04/V9DATA-02 semantics remain enforced.
</success_criteria>

<output>Create `475-30-SUMMARY.md` when complete.</output>
