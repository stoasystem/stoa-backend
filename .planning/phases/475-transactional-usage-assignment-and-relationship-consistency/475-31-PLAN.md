---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 31
type: execute
wave: 3
depends_on: ["475-23"]
files_modified:
  - src/stoa/routers/admin.py
autonomous: true
gap_closure: true
requirements: [V9DATA-03]
must_haves:
  truths:
    - "The Phase 475 admin router passes real mypy with exit code zero after relationship lifecycle authorization is installed."
    - "Typing cleanup preserves canonical `admin` authorization, expected-status/version CAS, and redacted errors."
  artifacts:
    - path: src/stoa/routers/admin.py
      provides: "Type-correct canonical-admin API boundary"
  key_links:
    - from: src/stoa/routers/admin.py
      to: src/stoa/db/repositories/user_repo.py
      via: "typed authorized lifecycle-transition request"
      pattern: "admin|relationship.*status"
---

<objective>
Make the admin API boundary pass unfiltered mypy after relationship lifecycle hardening.

Purpose: remove admin-route diagnostics without weakening CR-06's authorization and non-revival contract.
Output: a type-clean admin router with unchanged security behavior.
</objective>

## Artifacts this phase produces

- Mypy-clean `admin.py` with precise authorization and relationship transition types.
- Preserved canonical-admin and relationship lifecycle tests.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-REVIEW.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-23-SUMMARY.md
</context>

<tasks>

<task type="auto" id="475-31-01">
  <name>Eliminate admin router mypy diagnostics after lifecycle hardening</name>
  <files>src/stoa/routers/admin.py</files>
  <read_first>
    - src/stoa/routers/admin.py full file and current mypy diagnostic locations
    - src/stoa/db/repositories/user_repo.py post-475-23 relationship transition signatures/results
    - tests/test_admin_authorization.py canonical role denial/allow matrix
    - tests/test_phase475_parent_binding_transaction.py relationship status cases
    - mypy.ini or pyproject.toml active mypy configuration
  </read_first>
  <action>Resolve admin-router diagnostics with precise request, auth-fact, repository-result, and response types and explicit optional narrowing. Retain canonical role `admin` only, the expected current relationship status/version inputs, and structured/redacted errors. Do not introduce role aliases, permissive missing-fact defaults, broad type ignores, `Any` propagation, or mypy configuration changes.</action>
  <acceptance_criteria>
    - Unfiltered mypy exits zero for `src/stoa/routers/admin.py`.
    - Canonical `admin` remains the only privileged role accepted by affected routes.
    - Relationship status mutation retains its authorization and expected-status/version CAS.
    - Admin authorization and relationship status tests pass.
  </acceptance_criteria>
  <verify><automated>.venv/bin/mypy src/stoa/routers/admin.py && .venv/bin/python -m pytest -q tests/test_admin_authorization.py tests/test_phase475_parent_binding_transaction.py -k 'admin or status or revoked or inactive'</automated></verify>
  <done>The admin API contributes zero diagnostics to final mypy without weakening CR-06 or V9DATA-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Auth facts to privileged admin routes | Optional facts must not be narrowed into authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-475-31-01 | Elevation of privilege | admin authorization | mitigate | Explicit fail-closed narrowing preserves canonical `admin` only. |
| T-475-31-02 | Information disclosure | admin errors | mitigate | Existing redacted error contracts remain tested. |
| T-475-SC | Tampering | dependency supply chain | accept | No package or manifest changes. |
</threat_model>

<verification>
Require exact-file mypy exit zero followed by canonical-admin and lifecycle tests.
</verification>

<success_criteria>
- Admin routing is type-clean and the authorized relationship lifecycle remains non-revivable.
</success_criteria>

<output>Create `475-31-SUMMARY.md` when complete.</output>
