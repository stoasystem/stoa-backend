---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 02
type: execute
wave: 2
depends_on: [475-01]
files_modified:
  - src/stoa/models/question.py
  - src/stoa/routers/questions.py
  - tests/test_questions.py
  - tests/test_phase475_question_replay.py
autonomous: true
requirements: [V9DATA-01]
must_haves:
  truths:
    - "D-01/D-02: the route exposes one durable processing/original result and safe retries never re-upload or recharge."
    - "D-04: edited content under the same key receives a structured mismatch action requiring a new submission identity."
  artifacts:
    - path: src/stoa/routers/questions.py
      provides: "Atomic admission orchestration and processing/final replay projection"
    - path: tests/test_phase475_question_replay.py
      provides: "Route-level lost-response and payload-mismatch proof"
---

<objective>
Route all question submissions through the atomic admission command and expose its recoverable states.

Purpose: turn lower-boundary convergence into the selected student-visible API behavior.
Output: processing/final replay responses, structured mismatch/quota/retry errors, and no legacy write sequence.
</objective>

## Artifacts this phase produces

- `QuestionStatus.SUBMISSION_FAILED` and explicit processing projection using the existing `pending` initial state.
- `QuestionSubmissionErrorCode` values for payload mismatch and retryable admission failure.
- Route helper `_project_question_admission()` and focused route replay tests.

<tasks>

<task type="auto" id="475-02-01">
  <name>Migrate the question route to atomic admission and safe replay</name>
  <files>src/stoa/models/question.py, src/stoa/routers/questions.py, tests/test_questions.py, tests/test_phase475_question_replay.py</files>
  <read_first>
    - src/stoa/routers/questions.py complete current `submit_question` flow
    - src/stoa/models/question.py current request/status/response contracts
    - src/stoa/db/repositories/question_submission_repo.py Plan 475-01 dispositions and persisted command fields
    - src/stoa/services/attachment_service.py reserve/release/commit semantics
    - tests/test_questions.py current idempotency, quota, OCR, attachment, and persistence-failure cases
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-01, D-02, and D-04
  </read_first>
  <action>Replace `_check_daily_limit` plus separate `record_question_usage_event` and question put/attachment commit with Plan 475-01 admission. Prepare and validate the attachment, then atomically persist the initial `pending` question and command before OCR/AI effects. Project a committed initial question as the backend processing state; same-key retries return that exact question/result. Map PAYLOAD_MISMATCH to stable structured 409 with a client action to create a new submission, QUOTA_EXCEEDED to stable 429 without mutation, and RETRYABLE to a safe temporary-unavailable/try-again response. Continue OCR/AI only for the admitted owner; a resumed completed result does not invoke providers or attachment reservation again. Remove the old generic “already recorded; retry later” 409 and replace the old partial-state test with committed-before-timeout replay assertions.</action>
  <acceptance_criteria>
    - `submit_question` no longer calls the old independent counter/ledger/question sequence.
    - First success returns 201 with the durable question; same-key lost-response retry returns the same `question_id` and never calls quota, OCR, AI, or attachment mutation twice.
    - A pending question remains queryable/history-visible while later effects recover.
    - Changed payload receives structured 409 and the original question remains unchanged.
    - All existing ownership, attachment concealment, subject normalization, and answer/privacy tests remain green.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_questions.py tests/test_phase475_question_replay.py tests/test_attachment_security.py -k 'question or idempot or attachment' && .venv/bin/ruff check src/stoa/models/question.py src/stoa/routers/questions.py tests/test_phase475_question_replay.py</automated></verify>
  <done>The real API exposes a durable processing/original result and never duplicates admission effects on retry.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| UI sees false failure while commit succeeded | Strong command replay returns the durable pending/final question |
| Retry re-invokes OCR/AI or attachment consumption | Only ADMITTED owner starts effects; RESUME projects persisted state |
| Foreign/missing attachment becomes distinguishable | Preserve Phase 473 concealed attachment errors |
</threat_model>

<success_criteria>
- The public question route implements D-01, D-02, and D-04 over the Plan 475-01 transaction.
</success_criteria>

<output>Create `475-02-SUMMARY.md` after route and inherited regressions pass.</output>
