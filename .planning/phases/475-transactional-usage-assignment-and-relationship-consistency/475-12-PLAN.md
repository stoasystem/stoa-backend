---
phase: 475-transactional-usage-assignment-and-relationship-consistency
plan: 12
type: execute
wave: 2
depends_on: [475-06]
files_modified:
  - src/stoa/services/account_deletion_service.py
  - src/stoa/deps.py
  - src/stoa/routers/auth.py
  - tests/test_phase475_completed_deletion_replay.py
autonomous: true
requirements: [V9DATA-08]
must_haves:
  truths:
    - "D-16: identical completed deletion replay returns the stored deleted receipt and causes zero new cleanup, continuation, or conflict effects."
  artifacts:
    - path: src/stoa/services/account_deletion_service.py
      provides: "Authoritative stored terminal receipt projection"
    - path: tests/test_phase475_completed_deletion_replay.py
      provides: "Real endpoint lost-response replay with zero-effect counters"
---

<objective>
Replay the stored terminal account-deletion receipt through the real endpoint.

Purpose: make a lost successful deletion response recoverable after the active profile/binding is gone.
Output: terminal receipt validation/projection, no background rescheduling, and real dependency/route tests.
</objective>

## Artifacts this phase produces

- Extended `DeletionReceipt` with terminal/completed fields and `is_terminal`.
- `terminal_deletion_receipt()` validator/projector.
- New test module `tests/test_phase475_completed_deletion_replay.py`.

<tasks>

<task type="auto" id="475-12-01">
  <name>Return stored deleted receipt without reopening cleanup</name>
  <files>src/stoa/services/account_deletion_service.py, src/stoa/deps.py, src/stoa/routers/auth.py, tests/test_phase475_completed_deletion_replay.py</files>
  <read_first>
    - src/stoa/db/repositories/account_deletion_repo.py `begin_account_deletion`, identity lookup, and terminal `receipt` persistence
    - src/stoa/services/account_deletion_service.py `begin_or_replay_deletion` and `DeletionReceipt`
    - src/stoa/deps.py `get_deletion_command` active-binding/fallback lookup
    - src/stoa/routers/auth.py `DELETE /auth/me` background task scheduling
    - tests/test_auth_account_lifecycle.py deletion endpoint/dependency behavior
    - tests/test_phase473_account_deletion_seal.py terminal finalization contract
    - .planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md D-16
  </read_first>
  <action>When the identity fallback finds the same fingerprint command with `status=complete`, require and validate its stored nested receipt (`command_id`, `status=deleted`, `completed_at`) and project that exact terminal outcome rather than synthesizing `deletion_pending`/top-level state. Extend the public response with the stored completion timestamp only when terminal while retaining compatibility fields. In `delete_me`, schedule `continue_deletion_command` only for nonterminal accepted receipts; terminal replay returns immediately with no background task. Preserve fingerprint/identity mismatch as the existing concealed conflict and do not recreate an active profile, fence, or command.</action>
  <acceptance_criteria>
    - A real first request can finalize, lose its response, and an identical second `DELETE /auth/me` returns the same command ID, `deleted`, and stored completion time.
    - Replay performs zero branch handlers, cleanup calls, command claims, fence writes, and background task scheduling.
    - Missing/malformed stored terminal receipt fails closed as dependency/identity conflict without fabricating success.
    - Different fingerprint/verified identity remains a conflict.
    - Pending deletion replay still schedules continuation exactly once per accepted request behavior.
  </acceptance_criteria>
  <verify><automated>.venv/bin/python -m pytest -q tests/test_phase475_completed_deletion_replay.py tests/test_auth_account_lifecycle.py tests/test_phase473_account_deletion_seal.py && .venv/bin/ruff check src/stoa/services/account_deletion_service.py src/stoa/deps.py src/stoa/routers/auth.py tests/test_phase475_completed_deletion_replay.py</automated></verify>
  <done>The identical completed deletion request replays its stored terminal receipt with zero cleanup or command side effects.</done>
</task>

</tasks>

<threat_model>
| Threat | Control |
|---|---|
| Replay reopens destructive cleanup | Terminal receipt flag suppresses continuation/background task |
| Different identity obtains receipt | Existing issuer/subject/fingerprint binding remains mandatory |
| Malformed terminal row fabricates success | Strict stored-receipt validation and fail-closed projection |
</threat_model>

<success_criteria>
- V9DATA-08 and `completed-deletion-replay` pass through the real dependency and endpoint with zero additional effects.
</success_criteria>

<output>Create `475-12-SUMMARY.md` after terminal replay tests pass.</output>
