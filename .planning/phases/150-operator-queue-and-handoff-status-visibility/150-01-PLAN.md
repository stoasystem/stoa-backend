---
phase: 150-operator-queue-and-handoff-status-visibility
plan: 01
type: execute
wave: 1
depends_on:
  - 149-support-evidence-export-destination-integration
files_modified:
  - src/stoa/db/repositories/report_repo.py
  - src/stoa/services/support_destination_service.py
  - src/stoa/routers/admin.py
  - tests/test_admin_report_ops.py
autonomous: true
requirements:
  - SUPPORTINT-03
must_haves:
  truths:
    - "Admin-only list/detail APIs expose recent support handoff delivery lifecycle records with bounded filters and pagination."
    - "Operators can distinguish `created`, `queued`, `sent`, `failed`, `refused`, and `retried` status values and see provider/internal references where present."
    - "Retry behavior is explicit as read-only eligibility/status in Phase 150 and is unavailable for refused privacy-failed or unapproved destinations."
    - "Queue/status outputs are metadata-only and omit raw package sections, raw payloads, report artifacts, S3 keys, presigned URLs, authorization headers, cookies, tokens, and secrets."
  artifacts:
    - path: "src/stoa/db/repositories/report_repo.py"
      provides: "Recent support handoff delivery feed, detail, audit query, and scoped pagination token helpers"
      contains: "SUPPORT_HANDOFF_DELIVERY_FEED"
    - path: "src/stoa/services/support_destination_service.py"
      provides: "Metadata-only response shaping and retry eligibility helpers for support handoff delivery records"
      contains: "retry"
    - path: "src/stoa/routers/admin.py"
      provides: "Admin-only support handoff delivery list/detail endpoints"
      contains: "support-handoff-deliveries"
    - path: "tests/test_admin_report_ops.py"
      provides: "List/detail/retry visibility/privacy regressions for support handoff queue visibility"
      contains: "support_handoff_delivery_queue"
  key_links:
    - from: "src/stoa/services/support_destination_service.py"
      to: "src/stoa/db/repositories/report_repo.py"
      via: "delivery creation writes both point summary and recent feed rows"
      pattern: "put_support_handoff_delivery_record|SUPPORT_HANDOFF_DELIVERY_FEED"
    - from: "src/stoa/routers/admin.py"
      to: "src/stoa/db/repositories/report_repo.py"
      via: "list/detail routes delegate bounded query and exact delivery lookup"
      pattern: "list_support_handoff_delivery_summaries|get_support_handoff_delivery_record"
    - from: "src/stoa/routers/admin.py"
      to: "src/stoa/services/support_destination_service.py"
      via: "response shaping and retry metadata"
      pattern: "support_handoff_delivery_response|retry"
    - from: "tests/test_admin_report_ops.py"
      to: ".planning/phases/150-operator-queue-and-handoff-status-visibility/150-VALIDATION.md"
      via: "admin-only, filter, token, detail, retry, and metadata-only checks"
      pattern: "support_handoff_delivery_queue|support_handoff_delivery_detail"
---

<objective>
Implement Phase 150 operator queue and handoff status visibility required by SUPPORTINT-03.

Purpose: Add admin-only, bounded, metadata-only list/detail visibility for Phase 149 support handoff delivery lifecycle records, including retry eligibility/status without introducing retry mutation.
Output: Recent support handoff delivery list/detail APIs backed by a queryable feed read model, bounded audit detail, and focused regression tests.
</objective>

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/150-operator-queue-and-handoff-status-visibility/150-CONTEXT.md
@.planning/phases/150-operator-queue-and-handoff-status-visibility/150-RESEARCH.md
@.planning/phases/150-operator-queue-and-handoff-status-visibility/150-VALIDATION.md
@.planning/phases/149-support-evidence-export-destination-integration/149-01-SUMMARY.md
@.planning/phases/149-support-evidence-export-destination-integration/149-VERIFICATION.md
@src/stoa/services/support_destination_service.py
@src/stoa/db/repositories/report_repo.py
@src/stoa/routers/admin.py
@tests/test_admin_report_ops.py
</context>

## Artifacts This Phase Produces

- Recent support handoff delivery feed persistence/read helpers in `report_repo.py`.
- Metadata-only delivery response and retry visibility helpers in `support_destination_service.py`.
- Admin-only delivery queue list/detail endpoints in `admin.py`.
- Focused list/detail/retry visibility tests in `tests/test_admin_report_ops.py`.

<tasks>

<task type="auto">
  <name>Task 1: Add recent delivery feed and detail query helpers</name>
  <read_first>
    - `src/stoa/db/repositories/report_repo.py`
    - `.planning/phases/150-operator-queue-and-handoff-status-visibility/150-RESEARCH.md`
  </read_first>
  <files>src/stoa/db/repositories/report_repo.py</files>
  <action>Extend support handoff delivery persistence so every new delivery summary also writes a recent-first feed row under a stable partition such as `PK=SUPPORT_HANDOFF_DELIVERY_FEED` and `SK=SUMMARY#{created_at}#{delivery_id}`. Add `list_support_handoff_delivery_summaries()` using `query()` with `ScanIndexForward=False`, bounded `limit`, optional filters for `status`, `destination_mode`, `package_id`, `date_from`, and `date_to`, and `ExclusiveStartKey`. Add `list_support_handoff_delivery_audit_events()` mirroring existing audit query patterns. Add scoped `encode_support_handoff_delivery_page_token()` and `decode_support_handoff_delivery_page_token()` helpers for feed/audit tokens.</action>
  <acceptance_criteria>List queries are recent-first from a feed row, not arbitrary scan order. Detail audit queries are bounded by delivery partition and descending audit sort key. Invalid scoped tokens can be rejected before repo reads.</acceptance_criteria>
  <verify>./.venv/bin/pytest -q tests/test_admin_report_ops.py -k "support_handoff_delivery_queue or support_handoff_delivery_detail"</verify>
  <done>Repository supports recent list visibility and bounded detail audit timelines.</done>
</task>

<task type="auto">
  <name>Task 2: Add metadata-only response shaping and retry visibility</name>
  <read_first>
    - `src/stoa/services/support_destination_service.py`
    - `.planning/phases/150-operator-queue-and-handoff-status-visibility/150-RESEARCH.md`
  </read_first>
  <files>src/stoa/services/support_destination_service.py</files>
  <action>Add helpers that convert stored delivery rows and delivery audit events into API-safe response dictionaries. Include delivery ID, package ID, destination mode, status/lifecycle status, actor, created/updated timestamps, correlation ID, idempotency key, retry count, retryable flag, retry eligibility block, provider object reference, redacted refusal/failure reasons, privacy summary, evidence reference IDs, payload digest, and payload summary. Exclude DynamoDB keys, raw package sections, raw payloads, raw provider payloads, S3 keys, presigned URLs, authorization headers, cookies, tokens, and secrets. Add retry visibility logic only: no retry mutation route in Phase 150.</action>
  <acceptance_criteria>Both list and detail endpoints can share an allowlisted response shape. Refused privacy/unapproved records expose retry disabled with a redacted reason; retry mutation remains out of scope.</acceptance_criteria>
  <verify>./.venv/bin/pytest -q tests/test_admin_report_ops.py -k "retry_visibility or metadata_only or support_handoff_delivery_queue"</verify>
  <done>Operators see safe status/retry metadata without raw payload exposure or mutation semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add admin-only delivery queue list and detail endpoints</name>
  <read_first>
    - `src/stoa/routers/admin.py`
    - `src/stoa/db/repositories/report_repo.py`
    - `src/stoa/services/support_destination_service.py`
  </read_first>
  <files>src/stoa/routers/admin.py</files>
  <action>Add `GET /admin/reports/support-handoff-deliveries` with bounded `limit`, optional `status`, `destination_mode`, `package_id`, `date_from`, `date_to`, and `next_token`. Decode tokens before repo calls and return 400 on invalid tokens. Add `GET /admin/reports/support-handoff-deliveries/{delivery_id}` with bounded `audit_limit` and `audit_next_token`; return 404 for missing summaries and 400 for invalid audit tokens. Require admin role for both routes. Return typed or consistently shaped metadata-only responses with item counts and next tokens.</action>
  <acceptance_criteria>Non-admin requests fail before repo reads. List/detail filters and tokens are bounded and validated. Detail includes bounded audit metadata and audit pagination. No retry POST is added.</acceptance_criteria>
  <verify>./.venv/bin/pytest -q tests/test_admin_report_ops.py -k "support_handoff_delivery_queue or support_handoff_delivery_detail"</verify>
  <done>Operators have admin-only list/detail visibility into delivery lifecycle and recent activity.</done>
</task>

<task type="auto">
  <name>Task 4: Add focused queue/status visibility tests</name>
  <read_first>
    - `tests/test_admin_report_ops.py`
    - `.planning/phases/150-operator-queue-and-handoff-status-visibility/150-VALIDATION.md`
  </read_first>
  <files>tests/test_admin_report_ops.py</files>
  <action>Add tests for admin-only list/detail access, list filter passthrough, invalid list token, detail summary plus bounded audit events, missing detail 404, invalid detail audit token, read-only retry visibility, and metadata-only response invariants. Reuse existing monkeypatch style and privacy assertions. Keep all existing Phase 149 support handoff tests passing.</action>
  <acceptance_criteria>Tests prove SUPPORTINT-03 behavior and catch regressions that expose raw delivery payloads, package sections, private artifacts, provider payloads, headers, cookies, tokens, or secrets.</acceptance_criteria>
  <verify>./.venv/bin/pytest -q tests/test_admin_report_ops.py -k support_handoff</verify>
  <done>Automated coverage verifies list/detail/status/retry visibility and existing support handoff compatibility.</done>
</task>

</tasks>

<must_haves>
- Admin-only list/detail APIs expose bounded support handoff delivery lifecycle visibility.
- Recent list uses a delivery feed query path rather than relying on unordered table scans.
- Detail responses include bounded delivery audit events and pagination metadata.
- Retry visibility is explicit and read-only; refused privacy/unapproved records are not retryable.
- Outputs remain metadata-only and pass privacy denylist assertions.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin request -> queue/list route | Query filters and page tokens must not bypass admin auth, bounds, or token scope validation. |
| delivery storage -> API response | Stored metadata must be reduced through an allowlist before reaching operators. |
| retry status -> operator action | Retry eligibility must not imply a mutation path that does not exist in Phase 150. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-01 | Elevation of Privilege | list/detail routes | mitigate | Keep both endpoints behind `require_role(\"admin\")`; tests assert repo helpers are not called for non-admin users. |
| T-150-02 | Information Disclosure | list/detail responses | mitigate | Use allowlisted response shaping and privacy tests over summaries, details, and audit metadata. |
| T-150-03 | Tampering | pagination tokens | mitigate | Use scoped encode/decode helpers and return 400 before repo calls for invalid tokens. |
| T-150-04 | Repudiation | recent activity ordering | mitigate | Add recent-first feed query rows instead of unordered table scan semantics. |
| T-150-05 | Denial of Service | unbounded queue/audit queries | mitigate | Enforce FastAPI `limit`/`audit_limit` bounds and repository `Limit` values. |
| T-150-06 | Repudiation | retry ambiguity | mitigate | Expose read-only retry metadata and defer mutation until lifecycle transition semantics exist. |
</threat_model>

<verification>
- Plan structure gate: `node /Users/zhdeng/.codex/get-shit-done/bin/gsd-tools.cjs query verify.plan-structure .planning/phases/150-operator-queue-and-handoff-status-visibility/150-01-PLAN.md`
- Focused support handoff gate: `./.venv/bin/pytest -q tests/test_admin_report_ops.py -k support_handoff`
- Wider admin report ops gate: `./.venv/bin/pytest -q tests/test_admin_report_ops.py`
- Lint gate: `./.venv/bin/ruff check src/stoa/db/repositories/report_repo.py src/stoa/routers/admin.py src/stoa/services/support_destination_service.py tests/test_admin_report_ops.py`
</verification>

<success_criteria>
- `GET /admin/reports/support-handoff-deliveries` returns bounded metadata-only recent delivery records with filters and next token support.
- `GET /admin/reports/support-handoff-deliveries/{delivery_id}` returns one metadata-only delivery detail plus bounded delivery audit events.
- Operators can distinguish lifecycle statuses and see provider/internal references, refusal/failure reasons, privacy status, evidence references, payload digest, and retry eligibility.
- Privacy-failed and unapproved/refused deliveries are visibly non-retryable.
- Existing support handoff package and delivery behavior remains green.
</success_criteria>

<output>
Create `.planning/phases/150-operator-queue-and-handoff-status-visibility/150-01-SUMMARY.md` when done.
</output>
