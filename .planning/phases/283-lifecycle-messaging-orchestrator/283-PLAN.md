# Phase 283 Plan

- Add deterministic idempotency keys.
- Add gates for preference, opt-out, quiet hours, provider, template, destination, role, and stale state.
- Add retry/backoff and duplicate suppression contracts.
