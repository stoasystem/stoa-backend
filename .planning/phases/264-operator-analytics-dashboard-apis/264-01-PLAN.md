# Phase 264 Plan

1. Add `GET /admin/bi/dashboard`.
2. Compose aggregate sections for usage/quota, billing/provider, curriculum, teacher help/support, notifications, release smoke, and warehouse export.
3. Include blocker counts, support actions, partial/stale flags, and privacy metadata.
4. Test blocked states and redaction.
