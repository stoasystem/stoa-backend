---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 16
type: execute
wave: 4
depends_on: [476-15]
files_modified:
  - src/stoa/services/bedrock_token_count_service.py
  - src/stoa/services/ai_service.py
  - scripts/probe_bedrock_token_count.py
  - tests/test_bedrock_usage_evidence.py
  - docs/security/phase-476-bedrock-token-count-preflight.json
autonomous: true
requirements: [V9BILL-04]
threat_model:
  asvs_level: L1
  assets: ["Provider usage evidence, exact model identity, prompts/answers, and AWS credentials"]
  trust_boundaries: ["Application request into token count; Bedrock response into typed result; preflight into public evidence"]
  threats:
    - id: T-476-16-H
      severity: High
      stride: [Tampering, Repudiation, InformationDisclosure, ElevationOfPrivilege]
      description: "Estimated or malformed counts, unclassified calls, leaked content/credentials, or a mutating probe could corrupt allowance and evidence."
  mitigations: ["Exact provider count/usage; strict integers; closed caller inventory; count-only source-bound redacted preflight; fail-closed admission"]
  verification:
    automated: "PYTHONPATH=. .venv/bin/pytest -q tests/test_bedrock_usage_evidence.py && PYTHONPATH=. .venv/bin/python scripts/probe_bedrock_token_count.py verify --results docs/security/phase-476-bedrock-token-count-preflight.json"
    evidence: "Provider-shape, caller-classification, no-finalization, probe, source-binding, IAM, and redaction selectors"
  gate:
    open_high_allowed: false
    phase_gate: "scripts/verify_phase476_security_gate.py"
user_setup:
  - service: aws-bedrock-test
    why: "Prove exact configured-model token counting and InvokeModel usage shape before governed allowance admission."
    env_vars:
      - name: AWS_PROFILE
        source: "Approved non-production AWS profile with read/count access to the configured Bedrock inference profile"
      - name: AWS_REGION
        source: "Region used by the approved non-production Bedrock configuration"
must_haves:
  truths:
    - "D-18/D-22: the provider boundary returns validated content plus actual provider-reported input/output usage and never finalizes user debit itself."
    - "Admission uses the exact configured model/profile token-count operation; unavailable authoritative counting blocks governed use instead of estimating by characters."
    - "Every InvokeModel caller is classified as user-allowance or provider-cost-only."
  artifacts:
    - path: src/stoa/services/bedrock_token_count_service.py
      provides: "Exact-model input count adapter"
    - path: scripts/probe_bedrock_token_count.py
      provides: "Redacted no-content-leak configured-model/IAM probe"
    - path: tests/test_bedrock_usage_evidence.py
      provides: "Provider response shape and caller-classification proof"
    - path: docs/security/phase-476-bedrock-token-count-preflight.json
      provides: "Source/config/IAM-bound exact-model count capability receipt"
  key_links:
    - from: src/stoa/services/ai_service.py
      to: src/stoa/models/allowance.py
      via: "AIProviderResult embeds ProviderUsageEvidence"
      pattern: "ProviderUsageEvidence"
---

<objective>
Capture authoritative Bedrock input/output token evidence at the provider boundary.

Purpose: make final allowance accounting use provider facts, not request counts or local estimates.
Output: exact-model count adapter/probe, typed AI provider result, caller classification, and malformed-response tests.
</objective>

## Artifacts this phase produces

- New files `src/stoa/services/bedrock_token_count_service.py`, `scripts/probe_bedrock_token_count.py`, and `tests/test_bedrock_usage_evidence.py`.
- `AIProviderResult`, `AIInvocationClass`, `count_input_tokens()`, `parse_provider_usage()`, and `inventory_ai_invocation_classes()`.
- Evidence fields `model_id`, `inference_profile_id`, `provider_message_id_digest`, `input_tokens`, `output_tokens`, `stop_reason`, `effect_id`, and `observed_at`.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-15-SUMMARY.md
</context>

<tasks>

<task type="auto" id="476-16-01" tdd="true">
  <name>Return provider-reported Bedrock usage and prove token-count capability</name>
  <files>src/stoa/services/bedrock_token_count_service.py, src/stoa/services/ai_service.py, scripts/probe_bedrock_token_count.py, tests/test_bedrock_usage_evidence.py, docs/security/phase-476-bedrock-token-count-preflight.json</files>
  <read_first>
    - src/stoa/services/bedrock_token_count_service.py (new target; confirm absent)
    - src/stoa/services/ai_service.py all `invoke_model` call sites and response parsing
    - scripts/probe_bedrock_token_count.py (new target; confirm absent)
    - tests/test_bedrock_usage_evidence.py (new target; confirm absent)
    - src/stoa/models/allowance.py `ProviderUsageEvidence`
    - src/stoa/config.py Bedrock model/inference profile settings
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md Pattern 5 and Open Question 4
  </read_first>
  <behavior>
    - Exact configured request is counted through supported CountTokens/bedrock-mantle path before reservation.
    - InvokeModel response parses exact nonnegative `usage.input_tokens` and `usage.output_tokens`.
    - Malformed/missing usage fails governed invocation and never fabricates zero/estimate.
    - Inventory classifies answer/hint calls as user allowance and report/title/background calls explicitly provider-cost-only.
  </behavior>
  <action>Add a narrow exact-model token-count adapter selected from verified configured EU inference-profile semantics; do not use character heuristics or service-quota token burndown as user accounting. Change `ai_service` provider functions to return `AIProviderResult(content,usage)` with immutable count/model/correlation/stop/effect evidence while keeping prompt/answer bytes only in the content path. Parse exact ints excluding bool/fraction/negative. Create and run `PYTHONPATH=. .venv/bin/python scripts/probe_bedrock_token_count.py capture --results docs/security/phase-476-bedrock-token-count-preflight.json`; it checks current identity, exact model/profile count permission/endpoint, and response shape with synthetic non-private input and writes candidate source SHA, environment/model/profile digests, action name, exact synthetic counts, shape status, and correlation digest only. The probe exits nonzero on missing credentials, missing `bedrock-mantle:CountTokens` authority, endpoint/model/profile mismatch, malformed response, mock response, private-content canary, or source/config drift; it must never invoke generation or synthesize a receipt. Build a closed inventory for every InvokeModel caller and fail tests on unclassified additions.</action>
  <acceptance_criteria>
    - Valid response round-trips exact input/output counts; missing/malformed usage yields a typed retry/dependency error before finalization.
    - Probe cannot invoke generation or print credentials, raw request, prompt, response content, or full provider IDs.
    - A passing source/config/IAM-bound preflight receipt exists; Plans 17 and 18 remain blocked when it is absent, stale, mocked, or non-passing.
    - Unsupported/unavailable CountTokens blocks governed admission with stable `provider_token_count_unavailable`.
    - Caller inventory is exhaustive and separates user allowance from provider-cost-only effects.
    - `ai_service` contains no allowance counter mutation.
    - No ASVS L1 High threat remains unresolved; each listed mitigation has an observed passing selector for the source-bound phase gate.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_bedrock_usage_evidence.py &amp;&amp; .venv/bin/ruff check src/stoa/services/bedrock_token_count_service.py src/stoa/services/ai_service.py scripts/probe_bedrock_token_count.py tests/test_bedrock_usage_evidence.py &amp;&amp; PYTHONPATH=. .venv/bin/python scripts/probe_bedrock_token_count.py verify --results docs/security/phase-476-bedrock-token-count-preflight.json</automated></verify>
  <done>The provider seam supplies authoritative token evidence and an exact-model capability probe without finalizing debit or leaking content.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: provider usage evidence, model identity, prompts/answers, IAM credentials.
- Boundaries: application request → token count; Bedrock response → typed provider result; probe → public evidence.

**ASVS:** L1. **Severity:** High for every listed threat. Any unresolved High threat blocks this plan and the Phase 476 security gate.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Local estimate undercharges/overcharges | Tampering | Exact provider count/response usage only | Unavailable-path test |
| Malformed provider value trusted | Tampering | Exact runtime parsing | Bool/fraction/missing cases |
| Internal generation charges student | Repudiation | Closed caller classification inventory | AST inventory test |
| Prompt/answer/credential leaks | Information disclosure | Separate content/evidence and redacted probe | Canary scan |
| Probe mutates provider state | Elevation / Cost | Count-only dependency; generation fail-if-called | Probe unit test |
</threat_model>

<verification>
Focused tests cover provider shapes, classification, no-finalization, fail-closed counting, and probe redaction. Plan completion is blocked if any ASVS L1 High threat is open, unobserved, or source-unbound.
</verification>

<success_criteria>
- D-18 and the provider-evidence prerequisite of D-22 are implemented against the configured Bedrock model path.
</success_criteria>

<output>Create `476-16-SUMMARY.md` after verification.</output>
