---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 18
type: execute
wave: 5
depends_on: [476-15, 476-16]
files_modified:
  - src/stoa/routers/conversations.py
  - tests/test_conversation_token_finalization.py
autonomous: true
requirements: [V9BILL-04]
must_haves:
  truths:
    - "D-18/D-22: conversation answers and generated hints use the same actual-token reserve/evidence/durable-finalize rules as questions."
    - "Conversation command replay, regular/SSE transport, and hint retry cannot double debit."
    - "Title/background/report generation remains provider-cost-only and cannot consume a student allowance."
  artifacts:
    - path: tests/test_conversation_token_finalization.py
      provides: "Message/hint/transport replay and system-cost classification proof"
  key_links:
    - from: src/stoa/routers/conversations.py
      to: src/stoa/services/allowance_service.py
      via: "message/hint effect reservation and durable finalization"
      pattern: "reserve_token_allowance|finalize_token_allowance"
---

<objective>
Apply actual-token finalization to conversation and hint durable effects.

Purpose: prevent regular/SSE/hint retries and internal generations from corrupting student weekly allowance.
Output: message/hint wiring and transport/system-cost regression tests.
</objective>

## Artifacts this phase produces

- Conversation/hint fields `allowance_effect_id`, `provider_usage_evidence_id`, and `allowance_finalization_status`.
- New test file `tests/test_conversation_token_finalization.py`.
- Closed `AIInvocationClass` use at conversation answer, hint, and title call sites.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-15-SUMMARY.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-16-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md
</context>

<tasks>

<task type="auto" id="476-18-01" tdd="true">
  <name>Finalize conversation and hint tokens at durable result boundaries</name>
  <files>src/stoa/routers/conversations.py, tests/test_conversation_token_finalization.py</files>
  <read_first>
    - src/stoa/routers/conversations.py durable message command, regular/SSE projection, hint, and title generation paths
    - tests/test_conversation_token_finalization.py (new target; confirm absent)
    - tests/test_phase473_message_command.py durable conversation replay analog
    - src/stoa/services/allowance_service.py allowance APIs
    - src/stoa/services/ai_service.py classified provider results
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md D-18 and D-22
  </read_first>
  <behavior>
    - Regular and SSE retry one message effect and one debit.
    - Hint retry one hint effect and one debit.
    - Durable output survives disconnect and remains finalized.
    - Title/background generation records provider cost only.
  </behavior>
  <action>Bind message allowance identity to the existing durable message command and hint identity to the persisted attempt/challenge effect; never derive from transport request UUID. Count/reserve before each user-governed InvokeModel, persist actual provider usage, and finalize only with the validated durable assistant message/hint receipt that stable replay reads. Restore only on the closed terminal non-delivery path. Use identical regular/SSE projection and leave browser disconnect finalized after durable result. Mark conversation title and other internal generation calls explicitly provider-cost-only; they may record evidence but cannot reserve/finalize a student budget.</action>
  <acceptance_criteria>
    - Regular/SSE synchronized retry produces one provider call, message, effect, and debit.
    - Hint duplicate/retry produces one debit and exact durable hint result.
    - Validation/store/timeout/disconnect cases match D-22 outcomes.
    - Title generation changes provider-cost evidence only and leaves student counters unchanged.
    - Unclassified InvokeModel callers fail the Plan 16 inventory gate.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_conversation_token_finalization.py tests/test_phase473_message_command.py &amp;&amp; .venv/bin/ruff check src/stoa/routers/conversations.py tests/test_conversation_token_finalization.py</automated></verify>
  <done>Conversation answers and hints debit actual tokens once at durable delivery while internal generations never consume user allowance.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: message/hint result, student allowance, system-generation costs.
- Boundaries: regular/SSE/hint requests → durable effects; provider result → evidence; durable output → final debit.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Transport retry double debits | Repudiation | Durable command/effect identity independent of transport | Regular/SSE barrier tests |
| Internal title charges student | Tampering | Closed invocation class | Counter immutability test |
| Disconnect falsely restores | Tampering | Durable result controls finalization | Disconnect replay |
| Failed delivery consumes allowance | Repudiation | Terminal restore and unknown-state retention | Failure matrix |
| Conversation content leaks | Information disclosure | Evidence excludes history/message/hint text | Canary scan |
</threat_model>

<verification>
Tests cover regular, SSE, hint, title, retry, validation, storage, timeout, disconnect, and replay.
</verification>

<success_criteria>
- D-18/D-22 hold across all user-governed conversation and hint AI paths.
</success_criteria>

<output>Create `476-18-SUMMARY.md` after verification.</output>
