---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 09
type: execute
wave: 5
depends_on: [476-07, 476-08]
files_modified:
  - src/stoa/routers/parents.py
  - src/stoa/routers/admin.py
  - tests/test_billing_recheck_apis.py
autonomous: true
requirements: [V9BILL-02, V9BILL-04]
must_haves:
  truths:
    - "D-05/D-06: parent status starts confirming, blocks a new checkout while open, and recheck targets only the original opaque reference."
    - "D-08: admin sees safe lifecycle/timestamps/reason/redacted Stripe IDs and can recheck, but cannot mark paid."
    - "Parent A, students, and unauthorized admins cannot read/recheck/supersede Parent B's command."
  artifacts:
    - path: tests/test_billing_recheck_apis.py
      provides: "Ownership, authorization, closed route, and no-manual-success proof"
  key_links:
    - from: src/stoa/routers/parents.py
      to: src/stoa/services/billing_reconciliation_service.py
      via: "owner-authorized GET/recheck"
      pattern: "reconcile_checkout_command"
---

<objective>
Expose owner-safe parent and admin status/recheck APIs for one checkout command.

Purpose: provide the authoritative machine-actionable contract consumed by Web result/support views.
Output: closed status/recheck/supersession routes, redacted admin view, and authorization tests.
</objective>

## Artifacts this phase produces

- `ParentCheckoutStatusResponse`, `ParentCheckoutRecheckResponse`, `AdminCheckoutSupportResponse`, and `AdminCheckoutRecheckResponse`.
- Routes `GET /parents/me/subscription/checkout/{checkoutRef}`, `POST .../{checkoutRef}/recheck`, `POST .../{checkoutRef}/supersede`, `GET /admin/billing/checkouts/{checkoutRef}`, and `POST .../{checkoutRef}/recheck`.
- New test file `tests/test_billing_recheck_apis.py`.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-07-SUMMARY.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-08-SUMMARY.md
</context>

<tasks>

<task type="auto" id="476-09-01" tdd="true">
  <name>Publish parent/admin command status and recheck APIs</name>
  <files>src/stoa/routers/parents.py, src/stoa/routers/admin.py, tests/test_billing_recheck_apis.py</files>
  <read_first>
    - src/stoa/routers/parents.py current subscription/checkout/billing endpoints and parent dependency
    - src/stoa/routers/admin.py current billing support/readiness/refund route conventions
    - tests/test_billing_recheck_apis.py (new target; confirm absent)
    - src/stoa/services/billing_reconciliation_service.py result/projection contracts
    - src/stoa/services/subscription_service.py Plan 07 supersession operation
    - tests/test_admin_authorization.py admin capability/response analogs
  </read_first>
  <behavior>
    - Parent GET returns exactly confirming/active/not_completed/support_needed plus safe actions.
    - Parent/admin recheck invokes same-command reconciliation and no checkout-create provider.
    - Admin projection includes parent, target plan, timestamps, lifecycle dimensions, redacted IDs, and failure code only.
    - Cross-owner/student/unauthorized access is denied/concealed and no endpoint can set active manually.
  </behavior>
  <action>Add typed routes with `extra="forbid"` bodies and existing Actor/capability dependencies. Parent lookup must resolve opaque ref then verify authenticated parent ownership; reference entropy is not authorization. Map internal states to exactly D-11 outcomes. Recheck passes the already-loaded command to Plan 08 and cannot accept plan/beneficiary/callback/provider fields. Supersede requires explicit `confirmed=true` and Plan 07 preconditions. Admin read/recheck uses the existing billing-support capability, includes redacted provider suffixes and stable reason/action, and exposes no manual-success field or route; active can arise only from later signed provider convergence per D-08.</action>
  <acceptance_criteria>
    - Open parent command returns confirming and `newCheckoutAllowed=false`.
    - Recheck and admin recheck fail immediately if create/provider-mutation dependency is invoked.
    - Parent A cannot distinguish Parent B's real reference from random; student and wrong-capability admin are denied before repository/provider effects.
    - OpenAPI contains the five routes with closed request/response schemas and no callback/manual-paid input.
    - Static route inventory and tests find no retained “mark paid/success” admin operation.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_billing_recheck_apis.py tests/test_route_authorization_inventory.py &amp;&amp; .venv/bin/ruff check src/stoa/routers/parents.py src/stoa/routers/admin.py tests/test_billing_recheck_apis.py</automated></verify>
  <done>Parent and authorized support staff can inspect/recheck one command safely without broadening payment authority.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: command ownership, support data, reconciliation authority.
- Boundaries: authenticated parent/admin → API; opaque ref → repository; internal provider details → public response.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Cross-parent command enumeration | Information disclosure / Elevation | Owner check and concealed lookup response | Real-vs-random tests |
| Admin fabricates paid status | Elevation | Read/recheck-only route and no success input | OpenAPI/source gate |
| Recheck creates provider effect | Tampering | Reconciliation dependency lacks create | Fail-if-called test |
| PII/provider IDs leak | Information disclosure | Role-specific redaction and stable codes | Canary response/log scan |
| Recheck abuse causes DoS | DoS | Conditional lease and bounded response | Contention/rate cases |
</threat_model>

<verification>
Focused API tests cover valid roles, cross-owner references, schema extras, recheck idempotency, and absent manual activation.
</verification>

<success_criteria>
- D-05, D-06, D-08, and V9BILL-02 have a complete backend/Web handoff contract.
</success_criteria>

<output>Create `476-09-SUMMARY.md` after verification.</output>
