---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 03
type: execute
wave: 2
depends_on: [476-01, 476-02]
files_modified:
  - src/stoa/models/user.py
  - src/stoa/config.py
  - src/stoa/routers/auth.py
  - .env.example
  - tests/test_plan_identity_contract.py
autonomous: true
requirements: [V9BILL-01, V9BILL-04]
must_haves:
  truths:
    - "D-01/D-02: backend profile defaults, settings, price keys, plan catalog, and API types use the same four identities and the teacher term."
    - "No new account or plan configuration writes legacy free, standard, premium, or tutor_supported."
    - "All Stripe configuration added here remains sandbox/test capable and does not enable live charging."
  artifacts:
    - path: tests/test_plan_identity_contract.py
      provides: "Backend source/config/default identity closure"
  key_links:
    - from: src/stoa/models/user.py
      to: src/stoa/config.py
      via: "SubscriptionTier values and per-plan price/allowance settings"
      pattern: "FREE_TRIAL|TEACHER_SUPPORTED"
---

<objective>
Make the backend’s active account and configuration vocabulary one-to-one with the four Web products.

Purpose: remove the hidden three-tier model before commands, grants, migration, and evidence persist new state.
Output: canonical profile defaults, price/settings names, and a semantic identity gate.
</objective>

## Artifacts this phase produces

- `SubscriptionTier.FREE_TRIAL`, `.STUDENT`, `.TEACHER_SUPPORTED`, and `.FAMILY`.
- Settings `stripe_student_price_id`, `stripe_teacher_supported_price_id`, `stripe_family_price_id`, `free_trial_days`, `free_attachment_storage_bytes`, and `paid_attachment_storage_bytes`.
- Updated account initialization defaults and new test file `tests/test_plan_identity_contract.py`.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-01-SUMMARY.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-02-SUMMARY.md
</context>

<tasks>

<task type="auto" id="476-03-01" tdd="true">
  <name>Replace the active backend tier/config vocabulary</name>
  <files>src/stoa/models/user.py, src/stoa/config.py, src/stoa/routers/auth.py, .env.example, tests/test_plan_identity_contract.py</files>
  <read_first>
    - src/stoa/models/user.py `SubscriptionTier`
    - src/stoa/config.py daily-tier and Stripe settings
    - src/stoa/routers/auth.py profile initialization writes
    - .env.example Stripe and Web environment keys
    - tests/test_plan_identity_contract.py (new target; confirm absent)
    - src/stoa/models/billing.py `BillingPlanId`
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md D-01, D-02, D-15, D-19, and D-23
  </read_first>
  <behavior>
    - All new student/parent/admin profile defaults serialize `free_trial`.
    - Settings expose one price ID for each paid plan and no standard/premium price key.
    - Active enum/config/default AST inventory contains the four canonical values and no legacy tier aliases.
    - Live charges remain disabled by default.
  </behavior>
  <action>Change `SubscriptionTier` to the four D-02 identities and update profile initialization to `free_trial`; keep canonical role vocabulary unchanged. Replace standard/premium price settings with explicit student/teacher_supported/family Stripe Price IDs and retain `stripe_live_charges_enabled=False`. Introduce the locked 14-day trial and 5 GB/15 GB storage settings without daily request-count plan settings becoming the new allowance contract. Update `.env.example` with names only and no secret/sample live values. Add an AST/Settings test that distinguishes active plan identity from cosmetic English words and proves no active compatibility alias or tutor term is emitted.</action>
  <acceptance_criteria>
    - `SubscriptionTier` and `BillingPlanId` have byte-identical value sets.
    - New account fixtures persist `subscription_tier=free_trial`.
    - Settings validation requires three distinct paid Price IDs when checkout is enabled and rejects duplicate IDs.
    - Default/test settings cannot enable real live charges or accept `sk_live_`.
    - The focused semantic gate reports zero active legacy tier writes.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_plan_identity_contract.py &amp;&amp; .venv/bin/ruff check src/stoa/models/user.py src/stoa/config.py src/stoa/routers/auth.py tests/test_plan_identity_contract.py</automated></verify>
  <done>Every newly created backend account/configuration uses the four canonical product identities and sandbox-safe Stripe defaults.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: entitlement identity, Stripe Price mapping, account defaults, charge-mode control.
- Boundaries: environment → Settings; Settings → provider request; registration/provisioning → profile persistence.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Ambiguous tier mapping grants wrong plan | Elevation / Tampering | One enum value per product and distinct Price IDs | Set-equality and duplicate-ID tests |
| Live key/charge enabled during phase | Tampering / Financial harm | Default false plus `sk_live_` rejection | Settings negative tests |
| Legacy default revives hidden model | Tampering | All profile writers use free_trial | AST/runtime fixture inventory |
| Secret appears in example/evidence | Information disclosure | Names-only env documentation | Canary and prefix scan |
</threat_model>

<verification>
Focused tests prove enum/config/default closure without invoking Stripe or mutating production.
</verification>

<success_criteria>
- D-01 and D-02 hold across backend active types, settings, and account initialization.
</success_criteria>

<output>Create `476-03-SUMMARY.md` after verification.</output>
