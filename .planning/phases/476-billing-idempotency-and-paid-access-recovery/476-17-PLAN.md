---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 17
type: execute
wave: 5
depends_on: [476-15, 476-16]
files_modified:
  - src/stoa/routers/questions.py
  - tests/test_question_token_finalization.py
autonomous: true
requirements: [V9BILL-04]
must_haves:
  truths:
    - "D-22: question AI debit finalizes only with validated/safe durable readable result and stable replay."
    - "Terminal non-delivery restores user allowance but retains provider cost evidence; ambiguous outcomes remain reserved/recoverable."
    - "Browser disconnect after durable result keeps the final debit and replay returns the same result."
  artifacts:
    - path: tests/test_question_token_finalization.py
      provides: "Question reserve/provider/store/finalize/restore/disconnect matrix"
  key_links:
    - from: src/stoa/routers/questions.py
      to: src/stoa/services/allowance_service.py
      via: "question command effect identity reserves then finalizes with durable result"
      pattern: "reserve_token_allowance|finalize_token_allowance"
---

<objective>
Finalize weekly provider-token allowance at the durable question result boundary.

Purpose: prevent charging for unreplayable question answers while preserving real provider-cost evidence.
Output: question-path reservation/finalization/restoration wiring and deterministic failure tests.
</objective>

## Artifacts this phase produces

- Question effect integration fields `allowance_effect_id`, `provider_usage_evidence_id`, and `allowance_finalization_status`.
- New test file `tests/test_question_token_finalization.py`.
- Stable question responses/actions for `allowance_exhausted`, `provider_token_count_unavailable`, and recoverable in-flight finalization.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-15-SUMMARY.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-16-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-03-SUMMARY.md
</context>

<tasks>

<task type="auto" id="476-17-01" tdd="true">
  <name>Finalize question tokens only with a durable replayable result</name>
  <files>src/stoa/routers/questions.py, tests/test_question_token_finalization.py</files>
  <read_first>
    - src/stoa/routers/questions.py Phase 475 command/effect admission, AI completion, terminal proof, and replay paths
    - tests/test_question_token_finalization.py (new target; confirm absent)
    - tests/test_phase475_question_effect_recovery.py provider effect and terminal recovery analog
    - src/stoa/services/allowance_service.py reserve/observe/finalize/restore APIs
    - src/stoa/services/ai_service.py `AIProviderResult`
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md D-18 and D-22
  </read_first>
  <behavior>
    - Reserve before provider invocation; record provider evidence after response.
    - Finalization is atomic with/conditioned on durable validated readable question result.
    - Validation/storage terminal failure restores user allowance only; timeout/unknown remains recoverable.
    - Durable result plus lost HTTP response replays with unchanged final debit.
  </behavior>
  <action>Derive allowance effect identity from the existing opaque Phase 475 question command/effect coordinates and active plan/grant/week. Count/reserve input plus bounded output before InvokeModel. After `AIProviderResult`, persist provider usage evidence. Finalize exact actual input/output only in the same conditional transaction or exact durable-result receipt boundary that commits validated/safety-checked readable question output. On the existing closed terminal non-delivery proof, restore user reservation/debit once while retaining provider evidence/cost. Do not restore for timeout, throttle, malformed dependency response, unknown outcome, browser disconnect, or subjective quality. Replay must use the original effect/week/evidence.</action>
  <acceptance_criteria>
    - Failure injection covers before reserve, after reserve, after provider, validation failure, result-store failure, finalize commit timeout, HTTP disconnect, and replay.
    - Provider is never invoked when reservation is denied.
    - Final debit equals actual provider counts, not reserved max output or request count.
    - Terminal validation/storage failure restores availability and retains provider cost; ambiguous failure does neither finalize nor restore.
    - Disconnect-after-durable-result keeps one debit and exact result replay.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_question_token_finalization.py tests/test_phase475_question_effect_recovery.py &amp;&amp; .venv/bin/ruff check src/stoa/routers/questions.py tests/test_question_token_finalization.py</automated></verify>
  <done>Question AI usage is charged exactly once only when the validated result is durable/replayable, with correct restoration and provider-cost retention.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: question result, user allowance, provider cost, replay identity.
- Boundaries: question command → reservation; Bedrock result → evidence; durable result → finalization; HTTP transport → replay.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Unreplayable answer consumes allowance | Tampering / Repudiation | Finalize with durable result receipt only | Storage/validation failures |
| Retry double charges | Repudiation | Existing command/effect identity reused | Replay/disconnect tests |
| Ambiguous provider outcome refunded then rerun | Tampering / Financial harm | Unknown remains reserved/recoverable | Timeout cases |
| Provider evidence leaks content | Information disclosure | Evidence references counts/digests only | Canary scan |
</threat_model>

<verification>
The focused test names every reserve/provider/persist/finalize/transport boundary and asserts durable rows after injection.
</verification>

<success_criteria>
- D-22 is implemented for the question AI path with stable replay and provider-cost separation.
</success_criteria>

<output>Create `476-17-SUMMARY.md` after verification.</output>
