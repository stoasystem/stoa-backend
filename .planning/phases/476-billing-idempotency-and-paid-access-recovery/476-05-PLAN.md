---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 05
type: execute
wave: 2
depends_on: [476-01]
files_modified:
  - src/stoa/db/repositories/checkout_command_repo.py
  - tests/test_checkout_command_repo.py
autonomous: true
requirements: [V9BILL-01, V9BILL-02]
must_haves:
  truths:
    - "D-03: one parent-scoped logical key and immutable intent fingerprint create one durable command, open guard, and public lookup before Stripe."
    - "Identical contention replays the command; changed plan/beneficiaries returns an identity mismatch before provider mutation."
    - "An HTTP disconnect or provider ambiguity never releases the one-open-command guard."
  artifacts:
    - path: src/stoa/db/repositories/checkout_command_repo.py
      provides: "Command/guard/public lookup, provider lease, attachment, and strong replay primitives"
    - path: tests/test_checkout_command_repo.py
      provides: "Transaction, concurrency, lease, and ambiguous-commit proof"
  key_links:
    - from: src/stoa/db/repositories/checkout_command_repo.py
      to: src/stoa/models/billing.py
      via: "CheckoutIntent and CheckoutCommandState"
      pattern: "CheckoutIntent|CheckoutCommandState"
---

<objective>
Create the durable lower boundary that owns one checkout operation before any Stripe call.

Purpose: make duplicate/retry safety independent of browser lifetime and Stripe’s finite idempotency cache.
Output: transactional command/guard/lookup persistence, stable provider identity, lease/replay primitives, and concurrency tests.
</objective>

## Artifacts this phase produces

- New files `src/stoa/db/repositories/checkout_command_repo.py` and `tests/test_checkout_command_repo.py`.
- `CheckoutCommandDisposition`, `CheckoutCommandResult`, `checkout_intent_fingerprint()`, `register_checkout_command()`, `get_checkout_command_by_public_ref()`, `claim_provider_create()`, `attach_provider_session()`, `mark_provider_outcome_unknown()`, and `release_open_guard_for_terminal_command()`.
- Row entities `checkout_command.v1`, `checkout_open_guard.v1`, `checkout_public_lookup.v1`, and fields `command_version`, `intent_fingerprint`, `provider_key_digest`, `provider_effect_status`, and `lease_expires_at`.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-PATTERNS.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-01-SUMMARY.md
</context>

<tasks>

<task type="auto" id="476-05-01" tdd="true">
  <name>Implement the durable checkout command and one-open guard</name>
  <files>src/stoa/db/repositories/checkout_command_repo.py, tests/test_checkout_command_repo.py</files>
  <read_first>
    - src/stoa/db/repositories/checkout_command_repo.py (new target; confirm absent)
    - tests/test_checkout_command_repo.py (new target; confirm absent)
    - src/stoa/db/repositories/notification_repo.py `register_delivery_intent`, conditional replay, and strong-read helpers
    - src/stoa/services/notification_service.py register/recover/claim provider-effect flow
    - src/stoa/db/repositories/question_submission_repo.py durable command and ambiguous transaction classification
    - src/stoa/models/billing.py checkout contracts
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md D-03 and D-04
  </read_first>
  <behavior>
    - Concurrent identical registration creates one command/open guard/public lookup.
    - Same logical key plus same fingerprint replays; same key plus changed plan/beneficiaries mismatches.
    - Provider lease and call intent persist before effect and recover after expiry without changing provider key.
    - Attach commit-then-timeout reconciles from a strong read.
  </behavior>
  <action>Per D-03, derive one domain-separated command ID from authenticated parent identity plus the bounded caller idempotency key, one immutable fingerprint over parent, canonical plan, sorted explicit beneficiary IDs, Price/catalog version, and environment, one unrelated high-entropy public reference, and one versioned non-PII Stripe key digest. In one `TransactWriteItems`, conditionally create command, parent `CHECKOUT_OPEN` guard, and public lookup. On contention strongly read and validate schema/owner/fingerprint/version before replay. Add conditional provider-create lease/call-intent and session-attach transitions; preserve `provider_outcome_unknown` on ambiguity and never clear the guard on HTTP loss. Clear the guard only through a terminal-version-conditioned operation for active, proven not-completed, or proven superseded commands.</action>
  <acceptance_criteria>
    - Provider functions are absent from the repository and fail-if-called tests prove no network effect.
    - The registration transaction has exactly three distinct item targets and an active parent account-fence condition.
    - 20 synchronized identical callers produce one command; changed intent produces zero additional writes.
    - Raw parent ID/idempotency key never appears in public ref or provider key; provider key is stable and under 255 characters.
    - Lease expiry recovery retains command ID, fingerprint, and provider key; stale lease owners cannot attach a Session.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_checkout_command_repo.py &amp;&amp; .venv/bin/ruff check src/stoa/db/repositories/checkout_command_repo.py tests/test_checkout_command_repo.py</automated></verify>
  <done>One durable parent checkout command exists before Stripe and can be replayed/recovered without a second open operation.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: idempotency identity, one-open guard, command ownership, provider key.
- Boundaries: browser key/body → authenticated repository command; DynamoDB transaction → later provider caller; opaque ref → owner lookup.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Duplicate payable Sessions after race | Tampering / Repudiation | Parent guard and command atomically precede provider | Barrier concurrency test |
| Key reused with changed intent | Tampering | Immutable canonical fingerprint | Mismatch zero-effect test |
| Cross-parent opaque-ref access | Information disclosure / Elevation | Entropic ref plus owner validation | Cross-owner strong-read tests |
| Stale worker attaches wrong Session | Tampering | Lease generation and command-version condition | Lease takeover race |
| PII leaks through provider key | Information disclosure | Domain-separated digest only | Canary assertions |
</threat_model>

<verification>
Repository tests inject transaction contention, pre-commit failure, commit-then-timeout, lease expiry, and cross-owner lookup.
</verification>

<success_criteria>
- V9BILL-01 has an atomic local command/guard primitive that survives process and browser failure.
</success_criteria>

<output>Create `476-05-SUMMARY.md` after verification.</output>
