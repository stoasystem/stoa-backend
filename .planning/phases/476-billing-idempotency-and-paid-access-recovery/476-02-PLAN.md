---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stoa/config.py
  - src/stoa/services/billing_callback_service.py
  - .env.example
  - tests/test_billing_callback_urls.py
autonomous: true
requirements: [V9BILL-03]
must_haves:
  truths:
    - "D-09/D-12: the backend alone constructs complete Stripe return URLs from exact current-environment origins and one fixed result path."
    - "D-10: return URLs carry only a backend-generated opaque checkout reference and a non-authoritative return/cancel hint."
    - "Lookalikes, userinfo, encoded delimiters/backslashes, scheme-relative input, wrong ports, wildcards, query/fragment origins, arbitrary HTTPS origins, and request-derived origins fail closed."
  artifacts:
    - path: src/stoa/services/billing_callback_service.py
      provides: "Structural exact-origin parsing and fixed-path URL construction"
    - path: tests/test_billing_callback_urls.py
      provides: "Complete SEC-008 negative matrix"
  key_links:
    - from: src/stoa/services/billing_callback_service.py
      to: src/stoa/config.py
      via: "validated configured origin selected by environment"
      pattern: "stripe_checkout_web_origins"
---

<objective>
Close SEC-008 with server-owned, structurally parsed exact return URLs.

Purpose: remove all browser authority over Stripe callback origins and paths.
Output: validated environment policy, fixed result URL builder, and exact-origin adversarial tests.
</objective>

## Artifacts this phase produces

- New file `src/stoa/services/billing_callback_service.py` and test `tests/test_billing_callback_urls.py`.
- `BillingWebOriginPolicy`, `parse_exact_web_origin()`, and `build_checkout_return_urls()`.
- Settings `stripe_checkout_web_origins` and `stripe_checkout_result_path`.
- Environment examples for production/staging exact HTTPS origins and explicit local loopback origins/ports.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md
@docs/audit/full-project-audit.md
</context>

<tasks>

<task type="auto" id="476-02-01" tdd="true">
  <name>Implement exact-origin callback construction and its negative matrix</name>
  <files>src/stoa/config.py, src/stoa/services/billing_callback_service.py, .env.example, tests/test_billing_callback_urls.py</files>
  <read_first>
    - src/stoa/config.py current Stripe URL settings
    - src/stoa/services/billing_callback_service.py (new target; confirm absent)
    - .env.example current environment contract
    - tests/test_billing_callback_urls.py (new target; confirm absent)
    - src/stoa/services/subscription_service.py `_safe_url` and current checkout call
    - docs/audit/full-project-audit.md SEC-008
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md D-09, D-10, and D-12
  </read_first>
  <behavior>
    - Production/staging accept only configured exact HTTPS origins; development accepts only explicitly listed localhost/127.0.0.1/[::1] origin-port tuples.
    - Userinfo, path, query, fragment, wildcard, missing/extra port, lookalike host, encoded delimiter/backslash, scheme-relative, and request-derived values are rejected.
    - Success/cancel URLs use the fixed configured result path, opaque `checkoutRef`, and a non-authoritative `flow=return|cancel` hint.
  </behavior>
  <action>Replace full success/cancel URL settings with a list of current-environment exact origins and the fixed `/billing/checkout/result` path. Parse with `urllib.parse.urlsplit`; canonicalize scheme/host/explicit port without decoding into a second authority interpretation. Require HTTPS outside explicitly configured loopback development entries, forbid username/password/path/query/fragment/wildcards, and reject ambiguous encoded/backslash/netloc forms per D-12. `build_checkout_return_urls(checkout_ref, settings)` must select only configured policy data, never `Origin`, `Host`, forwarded headers, request URL, or a browser body field, and must append only `checkoutRef` plus non-authoritative flow per D-09/D-10. Fail application configuration rather than falling back to an inferred origin.</action>
  <acceptance_criteria>
    - The parameterized negative matrix contains production, staging, and local positive controls plus every bypass category named in D-12.
    - Browser-supplied `successUrl`/`cancelUrl` values are not accepted by the service API.
    - Both built URLs have the exact configured origin and fixed path; only opaque reference and flow differ.
    - No wildcard, arbitrary HTTPS acceptance, prefix comparison, `window.location.origin`, `request.headers`, or forwarded-host fallback exists.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_billing_callback_urls.py &amp;&amp; .venv/bin/ruff check src/stoa/config.py src/stoa/services/billing_callback_service.py tests/test_billing_callback_urls.py</automated></verify>
  <done>Checkout callbacks are server-built from exact environment configuration and every SEC-008 bypass class is rejected.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: redirect destination, opaque checkout reference, environment boundary.
- Boundaries: deployed configuration → URL policy; server → Stripe return URL; Stripe → browser redirect.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Lookalike/open redirect steals reference | Spoofing / Information disclosure | Structural exact-origin equality and fixed path | Exact negative matrix |
| Proxy/request headers select attacker origin | Tampering | Builder accepts Settings plus opaque ref only | Signature/source assertion |
| Encoded/userinfo/backslash parsing bypass | Spoofing | Reject ambiguous authority syntax before construction | Encoded delimiter cases |
| Wrong environment origin is used | Tampering | Environment-specific startup validation | Production/staging/local matrix |
</threat_model>

<verification>
The exact-origin matrix is the automated V9BILL-03 proof and contains explicit assertions for every D-12 case.
</verification>

<success_criteria>
- V9BILL-03 and SEC-008 are closed at the URL-construction boundary without browser-provided callback input.
</success_criteria>

<output>Create `476-02-SUMMARY.md` after verification.</output>
