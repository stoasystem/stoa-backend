---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 19
type: execute
wave: 8
depends_on: [476-13, 476-15, 476-17, 476-18]
files_modified:
  - src/stoa/services/teacher_support_allowance_service.py
  - src/stoa/routers/questions.py
  - src/stoa/routers/conversations.py
  - tests/test_teacher_support_allowances.py
autonomous: true
requirements: [V9BILL-04]
threat_model:
  asvs_level: L1
  assets: ["Teacher-support allowance, family shared scope, and queue/notification effects"]
  trust_boundaries: ["Student escalation into admission transaction; durable case identity into later messages/replies"]
  threats:
    - id: T-476-19-H
      severity: High
      stride: [Tampering, Repudiation, ElevationOfPrivilege, DenialOfService]
      description: "Retry, wrong family scope, final-slot races, denied downstream effects, or cross-family cases could overspend or expose support."
  mitigations: ["One case receipt; plan-version scope; counter CAS; admission before effects; exact grant/parent binding"]
  verification:
    automated: "PYTHONPATH=. .venv/bin/pytest -q tests/test_teacher_support_allowances.py"
    evidence: "Entry-point, replay, scope, concurrency, denial, relationship, and DST selectors"
  gate:
    open_high_allowed: false
    phase_gate: "scripts/verify_phase476_security_gate.py"
must_haves:
  truths:
    - "D-20: one successfully admitted durable support case consumes once; messages, replies, minutes, and retries consume zero additional cases."
    - "teacher_supported has two cases per beneficiary/week; family shares ten per family plan/grant version/week."
    - "D-21: support case windows use the same Zurich Monday calendar boundary and no rollover."
  artifacts:
    - path: src/stoa/services/teacher_support_allowance_service.py
      provides: "Case admission identity, plan scope, and exact-once counter orchestration"
    - path: tests/test_teacher_support_allowances.py
      provides: "Question/conversation/reply/retry/family-sharing proof"
  key_links:
    - from: src/stoa/services/teacher_support_allowance_service.py
      to: src/stoa/services/allowance_service.py
      via: "shared Zurich week and conditional effect counter"
      pattern: "zurich_week"
---

<objective>
Count teacher support by successfully admitted durable case, never by messages or time.

Purpose: implement D-20 across both question and conversation help entry points.
Output: plan-scoped admission service, route wiring, and cross-flow idempotency tests.
</objective>

## Artifacts this phase produces

- New files `src/stoa/services/teacher_support_allowance_service.py` and `tests/test_teacher_support_allowances.py`.
- `TeacherSupportScope`, `admit_teacher_support_case()`, and `get_teacher_support_projection()`.
- Entity `teacher_support_admission.v1` and fields `support_case_id`, `support_scope_id`, `week_identity`, `post_admission_count`, and `limit`.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-13-SUMMARY.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-15-SUMMARY.md
</context>

<tasks>

<task type="auto" id="476-19-01" tdd="true">
  <name>Debit one teacher-support case at durable admission</name>
  <files>src/stoa/services/teacher_support_allowance_service.py, src/stoa/routers/questions.py, src/stoa/routers/conversations.py, tests/test_teacher_support_allowances.py</files>
  <read_first>
    - src/stoa/services/teacher_support_allowance_service.py (new target; confirm absent)
    - src/stoa/routers/questions.py request-teacher durable case creation
    - src/stoa/routers/conversations.py teacher-help request/message paths
    - tests/test_teacher_support_allowances.py (new target; confirm absent)
    - src/stoa/services/allowance_service.py Zurich/effect/counter contracts
    - src/stoa/services/paid_entitlement_service.py active grant/scope resolution
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md D-20 and D-21
  </read_first>
  <behavior>
    - Successful first admission writes case plus one effect/counter debit atomically or not at all.
    - Retry and every later message/reply/resolve action preserve count.
    - Teacher-supported scopes per beneficiary at limit two; family scopes shared plan version at limit ten.
    - Free_trial/student/expired/grace-ended admission is denied before case/notification effects.
  </behavior>
  <action>Per D-20, derive the allowance effect from the durable support case ID created by each question/conversation escalation. For teacher_supported, scope the Zurich-week counter to beneficiary grant/version and limit 2. For family, scope it to parent subscription/grant plan version shared across selected beneficiaries and limit 10. Atomically persist first case admission with its operation receipt/counter CAS; only a successfully admitted case consumes. Route subsequent messages, teacher replies, assignment/takeover, resolution, and elapsed time around the existing case ID without allowance mutation. Deny before durable case/queue/notification effects when plan or quota does not permit admission.</action>
  <acceptance_criteria>
    - Question and conversation first-admission positive cases debit once; retry/message/reply/resolve tests leave count unchanged.
    - Two teacher_supported cases per beneficiary and ten family cases shared across three beneficiaries are exact.
    - Concurrent final-slot attempts have one winner and no count above limit.
    - Zurich DST/no-rollover behavior reuses Plan 15 window identity.
    - Denial creates no case, queue request, notification, assignment, or counter mutation.
    - No ASVS L1 High threat remains unresolved; each listed mitigation has an observed passing selector for the source-bound phase gate.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_teacher_support_allowances.py &amp;&amp; .venv/bin/ruff check src/stoa/services/teacher_support_allowance_service.py src/stoa/routers/questions.py src/stoa/routers/conversations.py tests/test_teacher_support_allowances.py</automated></verify>
  <done>Teacher support allowance counts exactly one admitted durable case under the correct beneficiary/family weekly scope.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: support-case allowance, family shared scope, queue/notification effects.
- Boundaries: student escalation → admission transaction; case ID → later messages/replies.

**ASVS:** L1. **Severity:** High for every listed threat. Any unresolved High threat blocks this plan and the Phase 476 security gate.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Retry/message double consumes | Repudiation | One operation receipt keyed by case ID | Cross-action tests |
| Family counter scoped per student | Tampering | Plan-version family scope | Three-beneficiary tests |
| Concurrent final slot overspends | Tampering | Counter CAS | Barrier test |
| Denied request still queues teacher | DoS / Elevation | Admission before downstream effects | Fail-if-called sentinels |
| Cross-family case affects quota | Elevation | Grant/parent/version binding | Cross-family tests |
</threat_model>

<verification>
Focused tests cover both case entry points, messages/replies, plan limits, shared scope, concurrency, denial, and DST. Plan completion is blocked if any ASVS L1 High threat is open, unobserved, or source-unbound.
</verification>

<success_criteria>
- D-20 and teacher-support portions of D-21/D-23 are implemented exactly.
</success_criteria>

<output>Create `476-19-SUMMARY.md` after verification.</output>
