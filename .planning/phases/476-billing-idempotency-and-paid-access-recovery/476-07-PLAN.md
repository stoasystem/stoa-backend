---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 07
type: execute
wave: 4
depends_on: [476-06]
files_modified:
  - src/stoa/services/subscription_service.py
  - src/stoa/db/repositories/checkout_command_repo.py
  - tests/test_billing_checkout_supersession.py
autonomous: true
requirements: [V9BILL-01, V9BILL-02]
threat_model:
  asvs_level: L1
  assets: ["Single payable Session, prior paid access, and explicit plan-change consent"]
  trust_boundaries: ["Parent confirmation into API; API into Stripe retrieve/expire; provider proof into guard transfer"]
  threats:
    - id: T-476-07-H
      severity: High
      stride: [Tampering, Repudiation, ElevationOfPrivilege, DenialOfService]
      description: "Unsafe supersession could leave two payable Sessions, replace a completed payment, cross owner scope, or erase prior access."
  mitigations: ["Owner/version checks; retrieve-expire-retrieve proof; unknown retains guard; prior grant immutability; atomic supersession"]
  verification:
    automated: "PYTHONPATH=. .venv/bin/pytest -q tests/test_billing_checkout_supersession.py tests/test_billing_checkout_commands.py"
    evidence: "Open/complete/unknown/concurrent supersession and prior-access byte-stability selectors"
  gate:
    open_high_allowed: false
    phase_gate: "scripts/verify_phase476_security_gate.py"
must_haves:
  truths:
    - "D-04: a confirmed plan change expires the old open Session where possible, proves it nonpayable, supersedes it, then creates a new command."
    - "At most one Session remains payable; complete/unknown old Sessions block replacement and reconcile instead."
    - "D-07: failed, canceled, or expired attempts leave the previous active plan and entitlements unchanged."
  artifacts:
    - path: tests/test_billing_checkout_supersession.py
      provides: "Retrieve-expire-proof-replace and race matrix"
  key_links:
    - from: src/stoa/services/subscription_service.py
      to: src/stoa/db/repositories/checkout_command_repo.py
      via: "version-conditioned expiration effect and supersession transaction"
      pattern: "supersede_checkout_command"
---

<objective>
Make an explicit changed-plan checkout safe without leaving two payable Sessions.

Purpose: preserve one-open-purchase invariant across parent-confirmed plan changes.
Output: retrieve-expire-supersede workflow and adversarial race tests.
</objective>

## Artifacts this phase produces

- `CheckoutSupersessionDisposition`, `claim_session_expiration()`, `record_session_expiration()`, and `supersede_checkout_command()`.
- `confirm_checkout_plan_change()` service operation.
- New test file `tests/test_billing_checkout_supersession.py`.
- Command fields `superseded_by_command_id`, `superseded_at`, and `expiration_effect_status`.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-06-SUMMARY.md
</context>

<tasks>

<task type="auto" id="476-07-01" tdd="true">
  <name>Implement retrieve-expire-supersede for confirmed plan changes</name>
  <files>src/stoa/services/subscription_service.py, src/stoa/db/repositories/checkout_command_repo.py, tests/test_billing_checkout_supersession.py</files>
  <read_first>
    - src/stoa/services/subscription_service.py Plan 06 create/resume flow and Stripe adapter
    - src/stoa/db/repositories/checkout_command_repo.py open guard, provider effect, and terminal operations
    - tests/test_billing_checkout_supersession.py (new target; confirm absent)
    - tests/test_billing_checkout_commands.py one-open invariant
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md Pattern 2
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md D-04 and D-07
  </read_first>
  <behavior>
    - Open old Session is expired once and must be retrieved as expired/nonpayable before guard transfer.
    - Complete Session never creates replacement; it enters reconciliation.
    - Unknown/retrieve failure keeps old guard and returns support-needed.
    - Concurrent supersession commands produce one new command/Session.
  </behavior>
  <action>Per D-04, add an explicit confirmed plan-change operation that accepts old `checkoutRef`, new logical key, new plan, and new beneficiaries under authenticated ownership. Retrieve the old Session. If open, persist an expiration-effect intent, call Stripe expiration once, then retrieve/prove `expired`; if complete, route to same-command reconciliation; if unknown/unavailable, retain guard and return support-needed. In one version-conditioned transaction mark old command superseded, transfer/replace the parent guard, and register the new command intent only after nonpayability proof. Preserve the previous active billing/grants throughout failed/canceled/expired attempts per D-07. Never infer confirmation from selecting a different card in the UI.</action>
  <acceptance_criteria>
    - Open/expired, complete, already-expired, provider-timeout, local-commit-timeout, and two concurrent superseders are tested.
    - No branch creates the new command before old Session nonpayability is durable.
    - Complete or unknown old Session produces zero new provider create calls.
    - Old active entitlement/billing projection remains byte-stable for canceled/expired replacement attempts.
    - Same supersession retry returns one new command and Session.
    - No ASVS L1 High threat remains unresolved; each listed mitigation has an observed passing selector for the source-bound phase gate.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_billing_checkout_supersession.py tests/test_billing_checkout_commands.py &amp;&amp; .venv/bin/ruff check src/stoa/services/subscription_service.py src/stoa/db/repositories/checkout_command_repo.py tests/test_billing_checkout_supersession.py</automated></verify>
  <done>A confirmed plan change cannot leave the parent with two payable Sessions or overwrite the prior active plan on failure.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: payable Session count, previous paid access, plan-change consent.
- Boundaries: parent confirmation → API; API → Stripe retrieve/expire; provider proof → guard transfer.

**ASVS:** L1. **Severity:** High for every listed threat. Any unresolved High threat blocks this plan and the Phase 476 security gate.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Two Sessions remain payable | Tampering / Financial harm | Retrieve-expire-retrieve proof before atomic guard transfer | Concurrent/race matrix |
| Stale caller supersedes another parent | Elevation | Owner/ref/version checks | Cross-owner tests |
| Complete Session wrongly replaced | Repudiation / Tampering | Complete routes to reconciliation only | Fail-if-create assertion |
| Provider timeout treated as absence | Tampering | Unknown retains original guard/support-needed | Timeout injection |
| Failed change removes paid access | DoS | Prior billing/grants immutable until provider-authoritative transition | Byte-stability assertions |
</threat_model>

<verification>
Supersession tests assert provider call order, guard ownership, exact Session payability, and unchanged prior entitlement. Plan completion is blocked if any ASVS L1 High threat is open, unobserved, or source-unbound.
</verification>

<success_criteria>
- D-04 and D-07 hold under concurrent and ambiguous provider outcomes.
</success_criteria>

<output>Create `476-07-SUMMARY.md` after verification.</output>
