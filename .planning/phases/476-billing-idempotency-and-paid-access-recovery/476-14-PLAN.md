---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 14
type: execute
wave: 8
depends_on: [476-04, 476-13]
files_modified:
  - src/stoa/services/free_trial_service.py
  - src/stoa/services/entitlement_service.py
  - src/stoa/routers/auth.py
  - tests/test_free_trial_window.py
autonomous: true
requirements: [V9BILL-04]
threat_model:
  asvs_level: L1
  assets: ["Immutable trial start/expiry, account/history availability, and governed-use admission"]
  trust_boundaries: ["Student activation into profile CAS; trial projection into AI/support admission"]
  threats:
    - id: T-476-14-H
      severity: High
      stride: [Tampering, ElevationOfPrivilege, DenialOfService]
      description: "Retries, missing history, or clock errors could reset trials, grant unproved free use, or deny preserved account/history access."
  mitigations: ["One conditional activation timestamp; migration-review fail closed; exact aware-time boundary; separate read and mutation entitlements"]
  verification:
    automated: "PYTHONPATH=. .venv/bin/pytest -q tests/test_free_trial_window.py"
    evidence: "Activation race, no-reset, missing-evidence, exact-expiry, and preserved-read selectors"
  gate:
    open_high_allowed: false
    phase_gate: "scripts/verify_phase476_security_gate.py"
must_haves:
  truths:
    - "D-23: free_trial starts once at first student learning-account activation and expires exactly 14 days later."
    - "Profile edits, relationship changes, retries, and paid/free transitions never reset the first activation timestamp."
    - "After expiry, account/history/parent viewing remain while new AI and teacher-support admission pauses and paid-plan action is returned."
  artifacts:
    - path: src/stoa/services/free_trial_service.py
      provides: "Immutable first-activation write and trial projection"
    - path: tests/test_free_trial_window.py
      provides: "Activation race, no-reset, expiry, and preserved-read proof"
  key_links:
    - from: src/stoa/routers/auth.py
      to: src/stoa/services/free_trial_service.py
      via: "successful first student activation writes immutable trial start"
      pattern: "activate_student_free_trial"
---

<objective>
Make the 14-day free trial immutable and enforce only new-use admission after expiry.

Purpose: prevent retries/profile changes from granting repeated trials or erasing learning access.
Output: first-activation primitive, entitlement projection, and time/race tests.
</objective>

## Artifacts this phase produces

- New files `src/stoa/services/free_trial_service.py` and `tests/test_free_trial_window.py`.
- `FreeTrialState`, `activate_student_free_trial()`, `get_free_trial_state()`, and `free_trial_allows_new_usage()`.
- Profile fields `first_student_activation_at`, `free_trial_expires_at`, and `free_trial_schema_version`.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-04-SUMMARY.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-13-SUMMARY.md
</context>

<tasks>

<task type="auto" id="476-14-01" tdd="true">
  <name>Persist and enforce the immutable 14-day free trial</name>
  <files>src/stoa/services/free_trial_service.py, src/stoa/services/entitlement_service.py, src/stoa/routers/auth.py, tests/test_free_trial_window.py</files>
  <read_first>
    - src/stoa/services/free_trial_service.py (new target; confirm absent)
    - src/stoa/services/entitlement_service.py free plan projection
    - src/stoa/routers/auth.py confirmation/activation lifecycle
    - tests/test_free_trial_window.py (new target; confirm absent)
    - src/stoa/db/repositories/user_repo.py profile CAS/account-fence pattern
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md D-23
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md Pitfall 9 and Open Question 5
  </read_first>
  <behavior>
    - First successful student activation conditionally writes one start and derived expiry.
    - Concurrent/replayed activation returns the same timestamps.
    - Trial expires at exact instant and blocks only new AI/support admission.
    - Missing historical activation evidence is review-required, never default-now.
  </behavior>
  <action>Per D-23, on the existing successful first student learning-account activation boundary, conditionally write one UTC `first_student_activation_at` and `free_trial_expires_at=start+14 calendar days` behind active account/profile version. Strongly replay existing valid timestamps on contention. Do not write from ordinary login, profile edit, relationship repair, entitlement read, or paid-plan transition. Update effective entitlement to project trial active/expired and stable `choose_paid_plan` action; keep read-only account, learning history, and parent views unaffected. Treat historical missing/malformed timestamps as migration review required and deny new free usage rather than granting 14 days from request time.</action>
  <acceptance_criteria>
    - Concurrent first activation yields one timestamp pair; later login/profile/binding/plan changes preserve exact bytes.
    - One microsecond before expiry permits admission and at expiry denies it.
    - Expired trial still authorizes account/history/parent read fixtures.
    - Missing historical evidence never calls clock-backed initialization from entitlement read.
    - No teacher-support or AI mutation occurs after expired admission denial.
    - No ASVS L1 High threat remains unresolved; each listed mitigation has an observed passing selector for the source-bound phase gate.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_free_trial_window.py &amp;&amp; .venv/bin/ruff check src/stoa/services/free_trial_service.py src/stoa/services/entitlement_service.py src/stoa/routers/auth.py tests/test_free_trial_window.py</automated></verify>
  <done>The free trial starts once, expires deterministically, and preserves account/history access while blocking only new governed use.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: trial start/expiry, account/history availability, new-use admission.
- Boundaries: identity activation → profile CAS; trial projection → AI/support admission.

**ASVS:** L1. **Severity:** High for every listed threat. Any unresolved High threat blocks this plan and the Phase 476 security gate.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Retry/profile edit resets trial | Tampering | Conditional immutable first timestamp | Race/no-reset tests |
| Historical missing timestamp grants new trial | Elevation | Review-required fail closed | Missing-evidence test |
| Expiry deletes/denies history | DoS | Separate read and mutation entitlements | Preserved-read tests |
| Clock boundary off-by-one | Tampering | Aware exact instants and time-machine | Before/at expiry cases |
</threat_model>

<verification>
Time-machine and concurrency tests prove immutable activation and exact admission boundary. Plan completion is blocked if any ASVS L1 High threat is open, unobserved, or source-unbound.
</verification>

<success_criteria>
- D-23 is fully implemented without accidental trial renewal or loss of historical access.
</success_criteria>

<output>Create `476-14-SUMMARY.md` after verification.</output>
