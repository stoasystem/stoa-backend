---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 08
type: execute
wave: 4
depends_on: [476-06]
files_modified:
  - src/stoa/services/billing_reconciliation_service.py
  - tests/test_billing_reconciliation.py
autonomous: true
requirements: [V9BILL-02]
must_haves:
  truths:
    - "D-06: recheck reconciles only the original command and cannot create a Session or charge attempt."
    - "Provider success/local attach failure, local success/HTTP timeout, duplicate retry, lease expiry, and delayed facts converge one support state."
    - "Uncertain provider outcome remains support-needed rather than being treated as nonpayment."
  artifacts:
    - path: src/stoa/services/billing_reconciliation_service.py
      provides: "Lease-bound same-command provider recheck and redacted projection"
    - path: tests/test_billing_reconciliation.py
      provides: "Deterministic failure-injection matrix"
  key_links:
    - from: src/stoa/services/billing_reconciliation_service.py
      to: src/stoa/db/repositories/checkout_command_repo.py
      via: "claim/retrieve/conditionally attach or classify same command"
      pattern: "get_checkout_command_by_public_ref|attach_provider_session"
---

<objective>
Recover one original checkout command across every provider/local partial-failure boundary.

Purpose: make V9BILL-02 deterministic before exposing recheck/status routes or webhook convergence.
Output: idempotent reconciliation lease, redacted support state, and required failure-injection matrix.
</objective>

## Artifacts this phase produces

- New files `src/stoa/services/billing_reconciliation_service.py` and `tests/test_billing_reconciliation.py`.
- `BillingReconciliationDisposition`, `BillingReconciliationResult`, `reconcile_checkout_command()`, and `project_checkout_support_state()`.
- Command fields `reconciliation_lease_generation`, `last_rechecked_at`, `reconciliation_reason`, and `failure_code`.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-06-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-03-SUMMARY.md
</context>

<tasks>

<task type="auto" id="476-08-01" tdd="true">
  <name>Implement same-command billing reconciliation with failure injection</name>
  <files>src/stoa/services/billing_reconciliation_service.py, tests/test_billing_reconciliation.py</files>
  <read_first>
    - src/stoa/services/billing_reconciliation_service.py (new target; confirm absent)
    - tests/test_billing_reconciliation.py (new target; confirm absent)
    - src/stoa/db/repositories/checkout_command_repo.py command/lease/attach interfaces
    - src/stoa/services/subscription_service.py Stripe retrieve/create adapter boundaries
    - src/stoa/services/usage_ledger_service.py reconciliation/support projection analog
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md Required Failure-Injection Matrix
  </read_first>
  <behavior>
    - Every recheck starts from stored command/provider intent and never calls Session.create.
    - Provider-created/local-missing Session is found and attached using stored opaque command reference/key evidence.
    - Local-attached/HTTP-lost state replays without provider mutation.
    - Lease contention and ambiguous provider read produce bounded retry/support-needed states.
  </behavior>
  <action>Implement a bounded conditional reconciliation lease over one owner-authorized command. Read stored provider intent/session lookup and retrieve Stripe objects using only persisted coordinates. Recover missing local attachment when exact sandbox Session/customer/Price/metadata match; replay already attached state; classify terminal expired/canceled/complete without activating entitlement; and retain `provider_outcome_unknown` when no authoritative proof exists. The function must have no provider-create dependency. Return stable codes/actions and only redacted provider suffixes/timestamps/failure class. Inject deterministic failures before command transaction, after command-before-Stripe, Stripe timeout, after Stripe-before-attach, after attach-before-HTTP, lease expiry, and repeated recheck.</action>
  <acceptance_criteria>
    - The exact required failure-injection matrix from RESEARCH.md has a named passing test for every checkout boundary.
    - Provider success/local failure attaches one Session; local success/response loss replays one command.
    - Fail-if-called create provider proves recheck cannot start a new charge/Session.
    - Lease contention does not double mutate and expired lease recovers once.
    - Support output contains lifecycle state, timestamps, safe action, redacted identifiers, and no key, URL, PII, or provider exception.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_billing_reconciliation.py &amp;&amp; .venv/bin/ruff check src/stoa/services/billing_reconciliation_service.py tests/test_billing_reconciliation.py</automated></verify>
  <done>Every specified checkout partial failure converges or remains honestly support-needed on the original command with zero new provider-create effects.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: original command identity, support truth, provider/local convergence.
- Boundaries: stored command → provider retrieval; provider object → local repair; internal diagnostics → support projection.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Recheck creates second payment attempt | Tampering / Financial harm | No create capability in service dependency | Fail-if-called sentinel |
| Provider object from another parent attaches | Elevation | Customer/Price/ref/environment/owner match | Mismatch matrix |
| Concurrent repair duplicates state | Tampering | Lease generation and conditional attach | Barrier tests |
| Unknown outcome is overclaimed terminal | Repudiation | Support-needed classification without guard release | Timeout/no-proof cases |
| Provider diagnostics leak | Information disclosure | Closed redacted projection | Canary scan |
</threat_model>

<verification>
Failure injection is deterministic and asserts durable rows/provider calls after every injected exception.
</verification>

<success_criteria>
- V9BILL-02 has an executable same-command recovery primitive for every required partial-failure class.
</success_criteria>

<output>Create `476-08-SUMMARY.md` after verification.</output>
