---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 13
type: execute
wave: 7
depends_on: [476-12]
files_modified:
  - src/stoa/services/paid_entitlement_service.py
  - src/stoa/services/entitlement_service.py
  - src/stoa/services/attachment_service.py
  - tests/test_paid_entitlement_transitions.py
autonomous: true
requirements: [V9BILL-02, V9BILL-04]
threat_model:
  asvs_level: L1
  assets: ["Paid period, grace deadline, learning history, grants, and attachment bytes"]
  trust_boundaries: ["Signed lifecycle facts into transitions; scheduler clock into effective plan; entitlement into upload admission"]
  threats:
    - id: T-476-13-H
      severity: High
      stride: [Tampering, Repudiation, DenialOfService]
      description: "Delayed/replayed transitions could shorten paid access, extend grace, delete content, remove prior grants, or repeat state changes."
  mitigations: ["Versioned period/grace identity; immutable deadline; admission-only storage enforcement; attempt/active separation; transition receipt"]
  verification:
    automated: "PYTHONPATH=. .venv/bin/pytest -q tests/test_paid_entitlement_transitions.py"
    evidence: "Time-travel, replay, recovery, no-delete, and prior-access stability selectors"
  gate:
    open_high_allowed: false
    phase_gate: "scripts/verify_phase476_security_gate.py"
must_haves:
  truths:
    - "D-07: failed/canceled/expired new purchase or upgrade ends only that attempt and preserves prior active plan/grants."
    - "D-16: cancel/downgrade applies at paid period end; renewal failure grants one fixed three-day grace then falls to free_trial if unresolved."
    - "Storage downgrade never deletes bytes; above 5 GB blocks only new uploads."
  artifacts:
    - path: tests/test_paid_entitlement_transitions.py
      provides: "Period-end, grace, recovery, no-delete, and monotonic replay proof"
  key_links:
    - from: src/stoa/services/paid_entitlement_service.py
      to: src/stoa/services/attachment_service.py
      via: "effective plan selects admission limit without deletion"
      pattern: "attachment_storage_limit"
---

<objective>
Implement monotonic cancellation, downgrade, renewal grace, recovery, and storage-retention transitions.

Purpose: preserve already-paid access/history while making delinquency outcomes deterministic.
Output: scheduled transitions, fixed grace identity, free fallback, upload-only blocking, and time-travel tests.
</objective>

## Artifacts this phase produces

- `PaidTransitionDisposition`, `schedule_period_end_transition()`, `start_renewal_grace()`, `clear_renewal_grace()`, and `apply_due_paid_transition()`.
- Entity `paid_transition.v1` and fields `transition_identity`, `effective_at`, `grace_started_at`, `grace_expires_at`, `target_plan`, and `transition_version`.
- New test file `tests/test_paid_entitlement_transitions.py`.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-12-SUMMARY.md
</context>

<tasks>

<task type="auto" id="476-13-01" tdd="true">
  <name>Implement period-end and grace paid-access transitions</name>
  <files>src/stoa/services/paid_entitlement_service.py, src/stoa/services/entitlement_service.py, src/stoa/services/attachment_service.py, tests/test_paid_entitlement_transitions.py</files>
  <read_first>
    - src/stoa/services/paid_entitlement_service.py Plan 12 grants/upgrades
    - src/stoa/services/entitlement_service.py effective plan and period projection
    - src/stoa/services/attachment_service.py upload quota admission/deletion behavior
    - tests/test_paid_entitlement_transitions.py (new target; confirm absent)
    - src/stoa/services/subscription_service.py current dunning/cancel period fields
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md D-07 and D-16
  </read_first>
  <behavior>
    - Cancel/downgrade leaves plan active before current_period_end and applies once at/after it.
    - Repeated payment-failed event creates one immutable 72-hour grace deadline; paid recovery clears it.
    - Unresolved grace falls to free_trial once and keeps account/history/grants as historical records.
    - Over-free-limit storage rejects only new upload reservation and invokes no object deletion.
  </behavior>
  <action>Per D-16, persist a scheduled transition keyed by subscription/plan version and exact provider period end; do not immediately change active grants for cancel/downgrade. Persist one grace identity/deadline from the first renewal-failure fact and never extend it on duplicate/delayed failures. A later matching paid invoice clears grace without resetting usage. At due time, a conditional transition updates effective plan/grants to target or free_trial exactly once while retaining account/history. Update attachment admission to resolve 5 GB free/15 GB paid from effective plan; if downgraded bytes exceed 5 GB, return a stable new-upload-blocked code and never call delete/decrement/recount existing objects.</action>
  <acceptance_criteria>
    - Time-machine tests cover one second before/at/after period end and grace end.
    - Duplicate/delayed failure cannot move the three-day deadline; recovery-before-expiry preserves paid access.
    - Failed/canceled checkout and failed upgrade leave prior plan/grants/counters byte-identical.
    - Downgrade over 5 GB retains every object/storage counter and blocks only the next new upload.
    - Duplicate scheduler/event processing produces one transition version.
    - No ASVS L1 High threat remains unresolved; each listed mitigation has an observed passing selector for the source-bound phase gate.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_paid_entitlement_transitions.py &amp;&amp; .venv/bin/ruff check src/stoa/services/paid_entitlement_service.py src/stoa/services/entitlement_service.py src/stoa/services/attachment_service.py tests/test_paid_entitlement_transitions.py</automated></verify>
  <done>Paid lifecycle transitions honor paid periods and grace, preserve history/storage, and cannot regress or duplicate under replay.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: paid period, grace deadline, learning history, attachment bytes.
- Boundaries: signed lifecycle facts → scheduled transition; scheduler clock → effective plan; entitlement → upload admission.

**ASVS:** L1. **Severity:** High for every listed threat. Any unresolved High threat blocks this plan and the Phase 476 security gate.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Delayed event shortens paid period | Tampering | Plan-version/period-end monotonic condition | Out-of-order time tests |
| Duplicate failure extends grace forever | Tampering | Immutable delinquency identity/deadline | Duplicate failure tests |
| Downgrade deletes customer content | DoS / Information loss | Admission-only storage enforcement | Delete fail-if-called |
| Failed upgrade removes prior access | DoS | Attempt separate from active grant | Byte-stability tests |
| Scheduler repeats transition | Repudiation | Conditional transition version/receipt | Replay tests |
</threat_model>

<verification>
Focused time-travel and failure tests prove period/grace boundaries, recovery, and zero deletion. Plan completion is blocked if any ASVS L1 High threat is open, unobserved, or source-unbound.
</verification>

<success_criteria>
- D-07 and D-16 hold for every new-purchase, upgrade, renewal, cancel, downgrade, and storage branch.
</success_criteria>

<output>Create `476-13-SUMMARY.md` after verification.</output>
