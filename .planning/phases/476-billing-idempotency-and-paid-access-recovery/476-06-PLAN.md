---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 06
type: execute
wave: 3
depends_on: [476-02, 476-03, 476-05]
files_modified:
  - src/stoa/services/subscription_service.py
  - src/stoa/routers/parents.py
  - tests/test_billing_checkout_commands.py
autonomous: true
requirements: [V9BILL-01, V9BILL-03]
threat_model:
  asvs_level: L1
  assets: ["Stripe Session uniqueness, parent funds, beneficiary scope, and checkout URL"]
  trust_boundaries: ["Browser into parent API; API into DynamoDB and Stripe sandbox"]
  threats:
    - id: T-476-06-H
      severity: High
      stride: [Spoofing, Tampering, Repudiation, InformationDisclosure, ElevationOfPrivilege]
      description: "Provider-first creation, unbound beneficiaries, browser redirects, live-mode confusion, or leaked URLs could create duplicate or unauthorized payment effects."
  mitigations: ["Command-first guard; stable Stripe key; fresh binding/cardinality checks; server callbacks; test-mode and redaction gates"]
  verification:
    automated: "PYTHONPATH=. .venv/bin/pytest -q tests/test_billing_checkout_commands.py"
    evidence: "Ordered provider, concurrency, timeout, beneficiary, callback, live-mode, and redaction selectors"
  gate:
    open_high_allowed: false
    phase_gate: "scripts/verify_phase476_security_gate.py"
must_haves:
  truths:
    - "D-03: refresh, duplicate click, response timeout, and identical retry return the same durable command and Stripe Session."
    - "D-09/D-10: checkout accepts plan and beneficiary IDs plus required idempotency identity, never browser callback URLs."
    - "D-14: command creation revalidates explicit active bound beneficiaries and plan cardinality before Stripe."
  artifacts:
    - path: tests/test_billing_checkout_commands.py
      provides: "Provider create ordering, idempotency propagation, retry, and beneficiary proof"
  key_links:
    - from: src/stoa/services/subscription_service.py
      to: src/stoa/db/repositories/checkout_command_repo.py
      via: "register/claim before Session.create; attach after response"
      pattern: "register_checkout_command.*claim_provider_create"
    - from: src/stoa/services/subscription_service.py
      to: src/stoa/services/billing_callback_service.py
      via: "server-built fixed return URLs"
      pattern: "build_checkout_return_urls"
---

<objective>
Route real parent checkout creation through the durable command and one stable Stripe sandbox request.

Purpose: close provider-first creation and browser callback authority.
Output: typed parent API, command-first Stripe orchestration, same-Session replay, and deterministic ordering tests.
</objective>

## Artifacts this phase produces

- `ParentCheckoutCommandCreate`, `ParentCheckoutCommandResponse`, and required `Idempotency-Key` route contract.
- `create_or_resume_checkout_command()` orchestration.
- Response fields `checkoutRef`, `commandState`, `checkoutUrl`, `safeActions`, `targetPlan`, and `beneficiaries`.
- New test file `tests/test_billing_checkout_commands.py`.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-02-SUMMARY.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-03-SUMMARY.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-05-SUMMARY.md
</context>

<tasks>

<task type="auto" id="476-06-01" tdd="true">
  <name>Create or replay one Stripe sandbox Checkout Session</name>
  <files>src/stoa/services/subscription_service.py, src/stoa/routers/parents.py, tests/test_billing_checkout_commands.py</files>
  <read_first>
    - src/stoa/services/subscription_service.py `create_checkout_session`, `_create_provider_checkout_session`, readiness, and price mapping
    - src/stoa/routers/parents.py `ParentCheckoutSessionCreate`, response model, and `/me/subscription/checkout`
    - tests/test_billing_checkout_commands.py (new target; confirm absent)
    - tests/test_subscription_operations.py current checkout/provider test analogs
    - src/stoa/db/repositories/checkout_command_repo.py public repository contract
    - src/stoa/services/billing_callback_service.py fixed URL builder
    - src/stoa/db/repositories/user_repo.py strict active parent/student binding reads
  </read_first>
  <behavior>
    - Durable registration and provider-call intent occur before `stripe.checkout.Session.create`.
    - Same operation under duplicate click, refresh, timeout, and lost HTTP response returns one Session ID/URL.
    - Changed payload under one key returns `checkout_idempotency_mismatch`; a different key while an open command exists returns `checkout_already_in_progress`.
    - Invalid/inactive/unbound beneficiary or wrong plan cardinality produces zero provider calls.
  </behavior>
  <action>Replace the current provider-first `create_checkout_session` path. Require a bounded `Idempotency-Key` header and body `{plan, beneficiaryIds}` with `extra="forbid"`; remove `successUrl` and `cancelUrl`. Revalidate the authenticated parent and every selected active bidirectional binding; require exactly one beneficiary for student/teacher_supported and one to three for family, and reject free_trial per D-01/D-14. Register/claim the durable command before Stripe. Build fixed URLs from Plan 02, pass the repository’s stable non-PII key through Stripe SDK `idempotency_key`, include only opaque command reference in metadata/client reference, require test key/test Prices and `livemode=false`, attach the returned Session conditionally, and replay the stored owner-visible URL only while payable. On provider timeout mark unknown and retry/retrieve the same key; never create a fresh command or key.</action>
  <acceptance_criteria>
    - Ordered fake proves command+guard+call intent are durable before first Stripe invocation.
    - Stripe receives identical parameters and one idempotency key on every ambiguous retry.
    - Concurrent API calls and HTTP-response loss return the same `checkoutRef`, Session ID, and checkout URL.
    - The route rejects callback fields, free_trial, invalid cardinality, inactive binding, cross-parent student, live key, live Price/object, and unknown body fields before provider mutation.
    - The response/logs omit parent/student raw IDs, Stripe secret, full provider key, and terminal checkout URL retention.
    - No ASVS L1 High threat remains unresolved; each listed mitigation has an observed passing selector for the source-bound phase gate.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_billing_checkout_commands.py &amp;&amp; .venv/bin/ruff check src/stoa/services/subscription_service.py src/stoa/routers/parents.py tests/test_billing_checkout_commands.py</automated></verify>
  <done>The authenticated Web contract creates or resumes one sandbox Session for one durable exact parent purchase intent.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: Stripe Session uniqueness, parent funds, beneficiary scope, checkout URL.
- Boundaries: browser → parent API; API → DynamoDB; API → Stripe sandbox.

**ASVS:** L1. **Severity:** High for every listed threat. Any unresolved High threat blocks this plan and the Phase 476 security gate.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Duplicate charge/session on retry | Tampering / Repudiation | Command-first guard and stable Stripe key | Concurrent/timeout tests |
| Parent buys access for unrelated student | Elevation | Fresh active bidirectional binding and cardinality checks | Cross-parent/inactive cases |
| Browser chooses redirect | Spoofing | Callback fields forbidden; server builder only | Extra-field and call-shape tests |
| Test/live confusion charges customer | Tampering / Financial harm | `sk_test_`, test Price, `livemode=false` checks | Live-mode refusal matrix |
| Checkout URL/identity leaks | Information disclosure | Owner-only response and redacted telemetry | Cross-owner/canary assertions |
</threat_model>

<verification>
The focused suite injects every command/provider/HTTP failure boundary and asserts one provider Session. Plan completion is blocked if any ASVS L1 High threat is open, unobserved, or source-unbound.
</verification>

<success_criteria>
- V9BILL-01 and V9BILL-03 are implemented on the parent checkout creation path.
</success_criteria>

<output>Create `476-06-SUMMARY.md` after verification.</output>
