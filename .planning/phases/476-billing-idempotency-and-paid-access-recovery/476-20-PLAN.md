---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 20
type: execute
wave: 8
depends_on: [476-13]
files_modified:
  - src/stoa/services/payment_reminder_service.py
  - src/stoa/services/notification_service.py
  - src/stoa/db/repositories/notification_repo.py
  - tests/test_payment_method_expiry_reminders.py
autonomous: true
requirements: [V9BILL-04]
must_haves:
  truths:
    - "D-24/D-25: parent and every explicit beneficiary receive the same safe reminder via in-app and verified-deliverable email, plus persistent Web state."
    - "D-26: expiry is Zurich month-end, reminder is seven local calendar days earlier, replacement clears old state, and method+month notifies at most once."
    - "D-27: recipient/channel failures are isolated and never change billing state; unusable email still receives in-app."
  artifacts:
    - path: src/stoa/services/payment_reminder_service.py
      provides: "Safe method projection, schedule identity, fan-out, replacement clearing"
    - path: tests/test_payment_method_expiry_reminders.py
      provides: "Month-end/DST/dedupe/deliverability/failure isolation proof"
  key_links:
    - from: src/stoa/services/payment_reminder_service.py
      to: src/stoa/services/notification_service.py
      via: "one delivery intent per recipient/channel"
      pattern: "register_delivery_intent"
---

<objective>
Deliver idempotent payment-method expiry reminders without card/secret leakage or batch-coupled failure.

Purpose: implement the family-wide notification and persistent reminder contract.
Output: safe payment projection, Zurich schedule, per-recipient/channel intents, clearing, and failure-isolation tests.
</objective>

## Artifacts this phase produces

- New files `src/stoa/services/payment_reminder_service.py` and `tests/test_payment_method_expiry_reminders.py`.
- `PaymentReminderDisposition`, `EmailEligibility`, `project_masked_payment_method()`, `payment_expiry_reminder_at()`, `resolve_billing_reminder_recipients()`, `run_payment_expiry_reminders()`, and `clear_replaced_payment_reminder()`.
- Entity `payment_expiry_reminder.v1` and fields `payment_method_digest`, `brand`, `last4`, `exp_month`, `exp_year`, `reminder_at`, `resolved_at`, and per-recipient/channel operation IDs.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-13-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md
</context>

<tasks>

<task type="auto" id="476-20-01" tdd="true">
  <name>Schedule and fan out idempotent payment-method expiry reminders</name>
  <files>src/stoa/services/payment_reminder_service.py, src/stoa/services/notification_service.py, src/stoa/db/repositories/notification_repo.py, tests/test_payment_method_expiry_reminders.py</files>
  <read_first>
    - src/stoa/services/payment_reminder_service.py (new target; confirm absent)
    - src/stoa/services/notification_service.py `run_authoritative_delivery`, email readiness, event creation
    - src/stoa/db/repositories/notification_repo.py delivery intent identity/claim/recovery
    - tests/test_payment_method_expiry_reminders.py (new target; confirm absent)
    - src/stoa/services/paid_entitlement_service.py explicit beneficiary grant reads
    - src/stoa/services/subscription_service.py payment method/invoice projection
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md D-24 through D-27
  </read_first>
  <behavior>
    - Reminder date equals Zurich month-end minus seven local calendar days across leap year/DST.
    - Same method digest+expiry month creates one logical reminder and independent recipient/channel intents.
    - Replacement resolves old persistent reminder; new method/month may notify once.
    - Invalid/unverified/bounced/suppressed/unknown email is skipped while in-app proceeds.
  </behavior>
  <action>Per D-26, resolve the subscription’s actual/default payment method through the provider adapter and persist only digest, brand, last4, exp month/year, source subscription digest, and observation version; compute expiry as the last calendar day in Europe/Zurich and subtract seven local calendar days, and key the logical reminder by method digest+year+month so replacement clears the old reminder and each method/month emits once. Per D-24 and D-25, fan out the same safe brand/last4/expiry reminder and persistent Web information to the parent plus current explicit beneficiary grant recipients, one durable operation per account/channel, with email plus in-app only. Per D-27, use a conservative email resolver—verified and positively deliverable only—while unknown/bounced/suppressed recipients remain in-app-only, and record each delivery failure independently without modifying billing or suppressing another intent.</action>
  <acceptance_criteria>
    - Leap year, month lengths, Zurich DST, exact seven-day boundary, before/at/after schedule are tested.
    - Repeated scheduler runs, provider duplicate updates, and delivery retries send each method/month/recipient/channel at most once.
    - Parent and all selected beneficiaries receive in-app; only eligible emails receive email.
    - One recipient/email provider failure leaves other intents successful and billing/grants byte-identical.
    - PAN/CVC/full provider IDs/secrets are rejected and absent from rows, notification payloads, logs, and responses.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_payment_method_expiry_reminders.py &amp;&amp; .venv/bin/ruff check src/stoa/services/payment_reminder_service.py src/stoa/services/notification_service.py src/stoa/db/repositories/notification_repo.py tests/test_payment_method_expiry_reminders.py</automated></verify>
  <done>Payment expiry reminders are safe, family-wide, calendar-correct, independently delivered, clearable, and at-most-once per method/month.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: masked payment summary, recipient family scope, delivery identity, billing state.
- Boundaries: Stripe payment method → safe projection; grants/profile → recipients; scheduler → notification intents; intent → SES/in-app.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Card/secret leakage | Information disclosure | Closed masked projection only | Forbidden-field/canary tests |
| Reminder sent cross-family | Elevation / Disclosure | Explicit grant recipient resolution | Cross-family tests |
| Duplicate reminders | Repudiation / DoS | Method+month logical identity and per-channel intents | Scheduler/retry tests |
| Invalid email blocks family | DoS | Independent intents and conservative resolver | Failure-isolation matrix |
| Reminder changes billing | Tampering | Notification path has no billing write capability | Byte-stability/fail-if-called |
</threat_model>

<verification>
Reminder tests cover calendar, idempotency, replacement, recipient scope, eligibility, partial failure, and redaction.
</verification>

<success_criteria>
- D-24 through D-27 are implemented without SMS/native push, live bulk send, card leakage, or billing mutation.
</success_criteria>

<output>Create `476-20-SUMMARY.md` after verification.</output>
