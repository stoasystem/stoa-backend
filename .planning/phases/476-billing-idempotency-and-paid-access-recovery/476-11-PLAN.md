---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 11
type: execute
wave: 5
depends_on: [476-06, 476-08, 476-10]
files_modified:
  - src/stoa/routers/billing.py
  - src/stoa/services/subscription_service.py
  - src/stoa/services/billing_reconciliation_service.py
  - tests/test_billing_webhook_convergence.py
autonomous: true
requirements: [V9BILL-02, V9BILL-04]
must_haves:
  truths:
    - "D-13: paid activation requires a valid Stripe signature plus matching first invoice paid and current subscription active."
    - "checkout.session.completed, browser return, invoice alone, subscription alone, and admin action remain confirming."
    - "D-17: duplicate, delayed, equal-time, and out-of-order events activate exactly once and never regress a newer entitlement."
  artifacts:
    - path: tests/test_billing_webhook_convergence.py
      provides: "Signature, API-version, duplicate/order, retrieval, and activation proof"
  key_links:
    - from: src/stoa/routers/billing.py
      to: src/stoa/db/repositories/billing_fact_repo.py
      via: "raw-body official signature verification then fact registration"
      pattern: "construct_event.*register_provider_event"
---

<objective>
Converge signed Stripe webhook facts into one authoritative paid-activation predicate.

Purpose: prevent redirect/session/admin shortcuts and unordered webhook regression.
Output: signature-authenticated fact processor, provider retrieval, joint activation guard, and full replay/order matrix.
</objective>

## Artifacts this phase produces

- `extract_invoice_subscription_id()`, `process_signed_billing_event()`, and `activation_predicate()`.
- Webhook response fields `signatureVerified`, `factDisposition`, `reconciliationDisposition`, and `activationDisposition`.
- New test file `tests/test_billing_webhook_convergence.py` and exact Stripe legacy/Basil invoice fixtures.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-08-SUMMARY.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-10-SUMMARY.md
</context>

<tasks>

<task type="auto" id="476-11-01" tdd="true">
  <name>Process signed unordered Stripe facts into exact-once activation</name>
  <files>src/stoa/routers/billing.py, src/stoa/services/subscription_service.py, src/stoa/services/billing_reconciliation_service.py, tests/test_billing_webhook_convergence.py</files>
  <read_first>
    - src/stoa/routers/billing.py raw-body webhook route
    - src/stoa/services/subscription_service.py `handle_stripe_webhook`, `_construct_provider_event`, `_provider_event_is_stale`, `_billing_transition`, and provider retrieval helpers
    - src/stoa/services/billing_reconciliation_service.py same-command reconciliation
    - tests/test_billing_webhook_convergence.py (new target; confirm absent)
    - tests/test_subscription_operations.py current signed/unsigned/event tests
    - src/stoa/db/repositories/billing_fact_repo.py fact/activation transaction contract
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md D-13 and D-17
  </read_first>
  <behavior>
    - Unsigned, wrong-secret, mutated-body, and live-mode events mutate nothing.
    - Each fact order and duplicate pattern converges one activation only after the joint invoice+subscription predicate.
    - `checkout.session.completed` alone remains confirming.
    - Legacy and `2025-03-31.basil` Invoice shapes resolve the exact matching subscription or fail closed.
  </behavior>
  <action>Per D-13, retain untouched raw request-body verification through `stripe.Webhook.construct_event` and remove unsigned fallback from any phase-closing path. After verification, register inbox/facts and retrieve the referenced current sandbox Invoice/Subscription when one predicate half is missing. Implement a version-aware Invoice subscription extractor for the pinned sandbox event-destination API version and legacy retained events. Require expected initial invoice ID paid=true, matching active subscription ID/customer/Price, immutable command metadata, explicit beneficiary intent, and `livemode=false`; only then invoke the Plan 10 atomic activation transaction. Per D-17, delete the global cross-object stale-event gate and converge duplicate, delayed, and out-of-order facts monotonically. Return 2xx for durable duplicates, retryable error for unpersisted dependency failure, and redacted processing dispositions.</action>
  <acceptance_criteria>
    - Named tests cover unsigned, wrong secret, body mutation, old timestamp, same/different duplicate IDs, equal timestamps, both event orders, stale subscription snapshot, live object, wrong customer/Price, legacy Invoice, and Basil Invoice.
    - Browser return and checkout completion cannot call activation.
    - Joint predicate produces one activation receipt under 20 duplicate/concurrent deliveries.
    - Provider retrieval mismatch cannot persist a grant or active projection.
    - Logs/responses/evidence contain no signature, secret, raw payload, checkout URL, PAN/CVC, or full provider ID.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_billing_webhook_convergence.py &amp;&amp; .venv/bin/ruff check src/stoa/routers/billing.py src/stoa/services/subscription_service.py src/stoa/services/billing_reconciliation_service.py tests/test_billing_webhook_convergence.py</automated></verify>
  <done>Only signed sandbox invoice-paid plus active-subscription proof can activate once; webhook replay/order cannot regress or duplicate state.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: webhook authenticity, paid-access activation, provider object binding.
- Boundaries: Internet/Stripe → raw webhook; verified event → provider retrieval; provider facts → DynamoDB activation.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Forged webhook activates access | Spoofing / Elevation | Official SDK over untouched raw body | Signature matrix |
| Replay/order duplicates or regresses grants | Tampering | Durable facts, semantic dedupe, activation receipt | Duplicate/reverse/equal-time tests |
| Foreign/live object activates | Elevation / Financial harm | Customer/Price/command/environment matches | Mismatch/live cases |
| API-version drift skips invoice binding | Tampering | Pinned version-aware extractor and fixtures | Legacy/Basil fixtures |
| Payload/secret leaks | Information disclosure | Closed redacted receipts | Canary scan |
</threat_model>

<verification>
The webhook convergence suite is the deterministic duplicate/delayed/out-of-order and provider-proof gate.
</verification>

<success_criteria>
- D-13/D-17 and the signed-webhook core of V9BILL-04 are implemented and adversarially proven.
</success_criteria>

<output>Create `476-11-SUMMARY.md` after verification.</output>
