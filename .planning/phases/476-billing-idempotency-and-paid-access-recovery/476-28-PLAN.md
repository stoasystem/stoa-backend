---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 28
type: execute
wave: 13
depends_on: [476-27]
files_modified:
  - /Users/zhdeng/stoa-frontend/tests/e2e/billing-paid-access.spec.ts
  - scripts/capture_phase476_sandbox_evidence.py
  - tests/test_phase476_sandbox_evidence.py
autonomous: true
requirements: [V9BILL-01, V9BILL-02, V9BILL-03, V9BILL-04]
threat_model:
  asvs_level: L1
  assets: ["Sandbox credentials, hosted payment journey, signed event evidence, grants, and allowances"]
  trust_boundaries: ["Browser into Stripe hosted Checkout; Stripe into signed webhook; backend into parent/admin Web; all systems into evidence collector"]
  threats:
    - id: T-476-28-H
      severity: High
      stride: [Spoofing, Tampering, Repudiation, InformationDisclosure]
      description: "Mock/live/unsigned/stale or secret-bearing evidence could overclaim paid activation, duplicate grants, charge customers, or leak sensitive data."
  mitigations: ["Plan 27 preflight; test-only hosted Checkout; real signed event delivery; exact-once version assertions; source-bound redacted evidence verifier"]
  verification:
    automated: "PYTHONPATH=. .venv/bin/pytest -q tests/test_phase476_sandbox_evidence.py && cd /Users/zhdeng/stoa-frontend && npm run test:e2e -- billing-paid-access.spec.ts --project=stripe-sandbox"
    evidence: "Hosted-domain, one-command/session, signed event order/replay, entitlement, role projection, no-live-charge, and redaction selectors"
  gate:
    open_high_allowed: false
    phase_gate: "scripts/verify_phase476_security_gate.py"
user_setup:
  - service: stripe-sandbox
    why: "Run the required hosted-checkout and signed-webhook test-mode journey without customer charges."
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Workbench sandbox API key; must begin sk_test_"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Stripe Workbench sandbox event destination signing secret"
      - name: STRIPE_STUDENT_PRICE_ID
        source: "Stripe sandbox recurring CHF 29 Student Price"
      - name: STRIPE_TEACHER_SUPPORTED_PRICE_ID
        source: "Stripe sandbox recurring CHF 89 Teacher-supported Price"
      - name: STRIPE_FAMILY_PRICE_ID
        source: "Stripe sandbox recurring CHF 149 Family Price"
      - name: STOA_STRIPE_SANDBOX_WEB_ORIGIN
        source: "Approved non-production Web origin configured in backend exact-origin settings"
    dashboard_config:
      - task: "Configure a sandbox event destination to the non-production webhook with the pinned API version and required checkout/invoice/subscription/payment-method events."
        location: "Stripe Workbench -> Webhooks/Event destinations"
must_haves:
  truths:
    - "A real browser reaches Stripe-hosted sandbox Checkout, returns by opaque reference, and signed webhooks activate parent/student entitlement and allowance exactly once."
    - "Duplicate/delayed/out-of-order signed events and same-command browser retries leave one Session, activation, grant set, allowance version, and support state."
    - "Parent/admin Web views explain the same operation; all evidence proves livemode=false and no production mutation/customer charge."
  artifacts:
    - path: /Users/zhdeng/stoa-frontend/tests/e2e/billing-paid-access.spec.ts
      provides: "Real hosted Checkout and parent/admin browser journey"
    - path: scripts/capture_phase476_sandbox_evidence.py
      provides: "Redacted command/provider/event/grant/allowance/browser evidence receipt"
    - path: tests/test_phase476_sandbox_evidence.py
      provides: "Evidence schema, redaction, and false-pass rejection"
  key_links:
    - from: /Users/zhdeng/stoa-frontend/tests/e2e/billing-paid-access.spec.ts
      to: scripts/capture_phase476_sandbox_evidence.py
      via: "one run/correlation identity binds browser/provider/backend observations"
      pattern: "phase476"
---

<objective>
Prove the complete paid-access journey in a real browser against Stripe sandbox and signed webhooks.

Purpose: close V9BILL-04 with non-mocked, redacted, exact-once evidence.
Output: hosted-checkout Playwright journey, evidence collector/verifier, and sandbox-only receipt.
</objective>

## Artifacts this phase produces

- New files `/Users/zhdeng/stoa-frontend/tests/e2e/billing-paid-access.spec.ts`, `scripts/capture_phase476_sandbox_evidence.py`, and `tests/test_phase476_sandbox_evidence.py`.
- Evidence schema `phase476.stripe_sandbox.v1`.
- Receipt fields `runId`, source SHAs, command/public-ref digests, Session/Event/Invoice/Subscription redacted IDs, event order/dedupe outcomes, activation/grant/allowance versions, parent/admin projection hashes, `livemode=false`, and explicit production-mutation/charge zeros.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-27-SUMMARY.md
@.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md
@/Users/zhdeng/stoa-frontend/AGENTS.md
</context>

<tasks>

<task type="auto" id="476-28-01" tdd="true">
  <name>Run and attest the real Stripe sandbox paid-access journey</name>
  <files>/Users/zhdeng/stoa-frontend/tests/e2e/billing-paid-access.spec.ts, scripts/capture_phase476_sandbox_evidence.py, tests/test_phase476_sandbox_evidence.py</files>
  <read_first>
    - /Users/zhdeng/stoa-frontend/tests/e2e/billing-paid-access.spec.ts (new target; confirm absent)
    - scripts/capture_phase476_sandbox_evidence.py (new target; confirm absent)
    - tests/test_phase476_sandbox_evidence.py (new target; confirm absent)
    - /Users/zhdeng/stoa-frontend/playwright.config.ts `stripe-sandbox` project
    - /Users/zhdeng/stoa-frontend/tests/e2e/billing-command-ui.spec.ts and billing-result-states.spec.ts
    - src/stoa/services/billing_reconciliation_service.py and billing fact/entitlement/allowance projections
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md Phase Requirements → Test Map and Sampling Rate
  </read_first>
  <behavior>
    - Parent selects beneficiaries/paid plan, one logical create reaches Stripe hosted test Checkout, and return remains confirming until signed joint proof.
    - Signed invoice/subscription facts activate exact grants/allowance once.
    - Duplicate/delayed/out-of-order events and refresh/retry/recheck remain one operation.
    - Parent result/billing and admin support views agree and reveal only allowed fields.
  </behavior>
  <action>Create a Playwright spec for the `stripe-sandbox` project using an approved sandbox parent plus active bound students and Stripe test payment method. Run Student, Teacher-supported, and Family in bounded isolated fixtures as needed to prove identity/cardinality while never using live/customer data. For the primary family journey, capture one browser key/ref, hosted Session, return confirming, signed webhook delivery, active plan/beneficiaries/weekly allowance, reminder-safe state, and admin redacted detail. Resend/reorder sandbox test events through authorized Workbench/API or the public destination, never handcrafted unsigned HTTP; prove one activation/grant/allowance version. The collector binds backend/frontend SHAs, preflight receipt, request/event IDs, local rows, screenshots/projection hashes, and no-live assertions while redacting secrets/full IDs/PII. Verifier rejects mocks, intercepted routes, unsigned events, missing joint proof, `livemode=true`, duplicate versions, source mismatch, or production mutation.</action>
  <acceptance_criteria>
    - Playwright runs with `--project=stripe-sandbox`, zero route interception, and reaches a Stripe-hosted test domain.
    - Refresh/timeout/repeat/recheck evidence shows one command and Session.
    - Signed duplicate/delayed/reversed event deliveries show one activation, expected grants, and one allowance version.
    - Parent/admin screenshots/projection hashes show matching state and redaction.
    - Receipt explicitly records `livemode=false`, sandbox Price/object modes, zero live charge, zero production mutation, and no secrets/PII.
    - No ASVS L1 High threat remains unresolved; each listed mitigation has an observed passing selector for the source-bound phase gate.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_phase476_sandbox_evidence.py &amp;&amp; cd /Users/zhdeng/stoa-frontend &amp;&amp; npm run test:e2e -- billing-paid-access.spec.ts --project=stripe-sandbox</automated></verify>
  <done>One immutable run proves the non-mocked Stripe sandbox browser→signed webhook→grant/allowance→parent/admin journey exactly once and without live charge.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: sandbox credentials, hosted payment journey, signed event evidence, grant/allowance truth.
- Boundaries: browser → Stripe hosted Checkout; Stripe → webhook; backend → Web/admin; all systems → evidence collector.

**ASVS:** L1. **Severity:** High for every listed threat. Any unresolved High threat blocks this plan and the Phase 476 security gate.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Mock evidence accepted as provider proof | Spoofing | Preflight/project/interception receipts | Verifier false-pass tests |
| Live customer charged | Tampering / Financial harm | Test keys/Prices/objects and `livemode=false` throughout | Receipt hard gate |
| Unsigned/replayed events overclaim activation | Spoofing / Tampering | Real signed delivery plus durable dedupe/version assertions | Event reorder/resend evidence |
| Evidence leaks secrets/PII | Information disclosure | Digests/redacted IDs and canary scan | Evidence verifier |
| Source/evidence mismatch | Repudiation | Exact backend/frontend SHAs and run IDs | Source-binding rejection |
</threat_model>

<verification>
Unit verification must pass locally; provider/browser command passes only with approved sandbox setup and may never fall back to mocks. Plan completion is blocked if any ASVS L1 High threat is open, unobserved, or source-unbound.
</verification>

<success_criteria>
- V9BILL-01 through V9BILL-04 have one integrated Stripe sandbox/browser proof with no live charge or production mutation.
</success_criteria>

<output>Create `476-28-SUMMARY.md` after verification.</output>
