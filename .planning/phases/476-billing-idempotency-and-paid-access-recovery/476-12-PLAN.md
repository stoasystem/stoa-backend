---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 12
type: execute
wave: 6
depends_on: [476-03, 476-11]
files_modified:
  - src/stoa/services/paid_entitlement_service.py
  - src/stoa/services/entitlement_service.py
  - tests/test_paid_entitlement_grants.py
autonomous: true
requirements: [V9BILL-02, V9BILL-04]
must_haves:
  truths:
    - "D-14: student/teacher_supported grant exactly one selected active bound student; family grants only one to three explicitly selected students."
    - "D-15: upgrade applies higher allowances/storage immediately without resetting current-week consumption or recounting stored objects."
    - "D-17: duplicate activation cannot duplicate grants or allowance versions."
  artifacts:
    - path: src/stoa/services/paid_entitlement_service.py
      provides: "Explicit beneficiary grant builder and monotonic activation/upgrade transition"
    - path: tests/test_paid_entitlement_grants.py
      provides: "Binding/cardinality/upgrade/duplicate proof"
  key_links:
    - from: src/stoa/services/paid_entitlement_service.py
      to: src/stoa/db/repositories/billing_fact_repo.py
      via: "grant operations supplied to atomic paid activation"
      pattern: "commit_paid_activation"
---

<objective>
Activate paid access only through explicit beneficiary grants and monotonic plan versions.

Purpose: prevent all-child inference and ensure upgrades preserve consumed allowance/storage.
Output: bounded grant/upgrade state machine, effective resolver integration, and exact-once tests.
</objective>

## Artifacts this phase produces

- New files `src/stoa/services/paid_entitlement_service.py` and `tests/test_paid_entitlement_grants.py`.
- `PaidGrantDisposition`, `validate_beneficiary_selection()`, `build_paid_activation_operations()`, `apply_paid_upgrade()`, and `get_active_beneficiary_grant()`.
- Entity `paid_beneficiary_grant.v1` and fields `grant_version`, `plan_version`, `subscription_id_digest`, `activated_at`, and `allowance_version`.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-11-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-CONTEXT.md
</context>

<tasks>

<task type="auto" id="476-12-01" tdd="true">
  <name>Publish explicit paid beneficiary grants and immediate upgrades</name>
  <files>src/stoa/services/paid_entitlement_service.py, src/stoa/services/entitlement_service.py, tests/test_paid_entitlement_grants.py</files>
  <read_first>
    - src/stoa/services/paid_entitlement_service.py (new target; confirm absent)
    - src/stoa/services/entitlement_service.py current binding/all-child billing resolution and storage limit
    - tests/test_paid_entitlement_grants.py (new target; confirm absent)
    - src/stoa/db/repositories/billing_fact_repo.py atomic activation operation contract
    - src/stoa/db/repositories/user_repo.py strict bidirectional relationship reads
    - src/stoa/services/attachment_service.py storage counter/limit behavior
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md D-14, D-15, and D-17
  </read_first>
  <behavior>
    - Exact selected active bindings only are granted; later/new children are not.
    - Duplicate activation replays one grant/version.
    - Upgrade changes plan/version/limit immediately while preserving current-week finalized/reserved counts and storage aggregate.
    - Inactive/changed binding between command and activation aborts the whole activation.
  </behavior>
  <action>Build grant operations from the command’s immutable sorted beneficiary IDs, revalidating each strict active bidirectional relationship immediately before commit. Student and teacher_supported require exactly one; family permits one to three. Bind grant rows to parent, student, command, subscription digest, plan, plan version, and account-fence/profile/relationship versions. Supply these operations to Plan 10’s one activation transaction. Change `resolve_student_entitlement` to require an exact active grant instead of scanning/inheriting every child. For a provider-authoritative paid upgrade, atomically publish a higher plan/version and allowance version while leaving current Zurich-week counters/reservations and attachment byte aggregate unchanged; only their resolved limits rise to D-15 values.</action>
  <acceptance_criteria>
    - One and three-beneficiary positive cases plus zero/four, duplicate, inactive, unbound, cross-parent, and relationship-race cases are tested.
    - Adding a child after family activation does not grant that child.
    - Duplicate activation leaves one grant per selected student and one allowance version.
    - Upgrade retains exact pre-upgrade consumed/reserved token/support counts and storage bytes.
    - Any binding/version condition loss leaves command, billing, all grants, and plan/allowance version unchanged.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_paid_entitlement_grants.py &amp;&amp; .venv/bin/ruff check src/stoa/services/paid_entitlement_service.py src/stoa/services/entitlement_service.py tests/test_paid_entitlement_grants.py</automated></verify>
  <done>Signed activation/upgrade yields only explicit, versioned beneficiary grants and never resets already-consumed resources.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: beneficiary authorization, paid plan version, usage/storage aggregates.
- Boundaries: checkout selection → current relationship proof; provider activation → grant transaction; grant → entitlement resolver.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Family silently grants future children | Elevation | Explicit immutable grant rows only | Add-child-after tests |
| Stale binding grants unrelated student | Elevation | Relationship/profile/fence version conditions | Race matrix |
| Duplicate event duplicates allowance | Tampering | Activation/grant/version receipt | Duplicate tests |
| Upgrade resets usage | Tampering / Repudiation | Limit/version update without counter writes | Byte-stable aggregate assertions |
| Cross-parent grant disclosure | Information disclosure | Owner-scoped exact lookup | Cross-parent cases |
</threat_model>

<verification>
Grant tests inspect the full transaction and effective entitlement for selected, unselected, raced, duplicate, and upgraded cases.
</verification>

<success_criteria>
- D-14, D-15, and D-17 are implemented for activation and upgrade.
</success_criteria>

<output>Create `476-12-SUMMARY.md` after verification.</output>
