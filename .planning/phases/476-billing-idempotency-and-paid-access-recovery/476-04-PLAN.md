---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 04
type: execute
wave: 3
depends_on: [476-03]
files_modified:
  - src/stoa/jobs/migrate_billing_plan_identity.py
  - scripts/provision_production_admin.py
  - tests/test_plan_identity_migration.py
  - docs/security/phase-476-plan-migration-preview.json
autonomous: true
requirements: [V9BILL-02, V9BILL-04]
threat_model:
  asvs_level: L1
  assets: ["Existing entitlements, beneficiary scope, billing history, trial history, and migration authority"]
  trust_boundaries: ["Legacy/provider evidence into preview; operator disposition into conditional non-production apply"]
  threats:
    - id: T-476-04-H
      severity: High
      stride: [Tampering, InformationDisclosure, ElevationOfPrivilege]
      description: "Guessed or stale legacy migration could broaden access, reset trials, overwrite newer billing, or leak provider/customer evidence."
  mitigations: ["Read-only source-bound preview; exact Price/beneficiary/trial evidence; review-required ambiguity; digest/version-conditioned non-production apply"]
  verification:
    automated: "PYTHONPATH=. .venv/bin/pytest -q tests/test_plan_identity_migration.py tests/test_plan_identity_contract.py"
    evidence: "Preview/apply/refusal tests and redacted migration-preview receipt"
  gate:
    open_high_allowed: false
    phase_gate: "scripts/verify_phase476_security_gate.py"
user_setup:
  - service: billing-migration-readonly
    why: "Inspect non-production legacy billing rows and Stripe sandbox Price/subscription evidence without guessing plan or trial history."
    env_vars:
      - name: STOA_BILLING_MIGRATION_ENVIRONMENT
        source: "Approved non-production environment identifier; production is rejected"
      - name: STRIPE_SECRET_KEY
        source: "Stripe sandbox read-only/test key; must begin sk_test_"
must_haves:
  truths:
    - "D-02: legacy billing/profile/config identities are migration inputs only and never remain active aliases."
    - "Ambiguous standard rows are reported as migration_review_required; the migration never guesses student versus family or broadens beneficiaries."
    - "Preview performs zero writes; apply is digest-bound, version-conditional, idempotent, non-production only, and preserves exact prior evidence for rollback."
  artifacts:
    - path: src/stoa/jobs/migrate_billing_plan_identity.py
      provides: "Read-only inventory/preview and explicit non-production apply"
    - path: tests/test_plan_identity_migration.py
      provides: "Ambiguity, changed-preview, replay, and production-refusal proof"
    - path: docs/security/phase-476-plan-migration-preview.json
      provides: "Redacted source-bound plan/trial migration inventory and unresolved-review gate"
  key_links:
    - from: src/stoa/jobs/migrate_billing_plan_identity.py
      to: src/stoa/models/billing.py
      via: "canonical BillingPlanId target validation"
      pattern: "BillingPlanId"
---

<objective>
Provide a safe preview/apply path from legacy tier rows to canonical plan identities.

Purpose: eliminate persisted ambiguity without inferring family access or authorizing production mutation.
Output: bounded migration job, review classification, conditional apply, and safety tests.
</objective>

## Artifacts this phase produces

- New files `src/stoa/jobs/migrate_billing_plan_identity.py` and `tests/test_plan_identity_migration.py`.
- Canonical `free_trial` initialization in `scripts/provision_production_admin.py`.
- `PlanMigrationDisposition`, `PlanMigrationPreview`, `preview_plan_identity_migration()`, `apply_plan_identity_migration()`, `lambda_handler()`, and CLI `--preview|--apply`.
- Persisted fields `plan_identity_schema_version`, `migration_evidence_digest`, `migration_source_plan`, and `migration_review_required`.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-03-SUMMARY.md
@.planning/phases/475-transactional-usage-assignment-and-relationship-consistency/475-03-SUMMARY.md
</context>

<tasks>

<task type="auto" id="476-04-01" tdd="true">
  <name>Implement digest-bound plan identity migration preview and apply</name>
  <files>src/stoa/jobs/migrate_billing_plan_identity.py, scripts/provision_production_admin.py, tests/test_plan_identity_migration.py, docs/security/phase-476-plan-migration-preview.json</files>
  <read_first>
    - src/stoa/jobs/migrate_billing_plan_identity.py (new target; confirm absent)
    - scripts/provision_production_admin.py profile bootstrap defaults
    - tests/test_plan_identity_migration.py (new target; confirm absent)
    - src/stoa/jobs/reconcile_question_submissions.py preview/apply analog
    - src/stoa/services/subscription_service.py billing/profile/provider fields
    - src/stoa/db/repositories/user_repo.py profile CAS/version contract
    - src/stoa/models/billing.py `BillingPlanId`
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md Runtime State Inventory and Pitfall 6
  </read_first>
  <behavior>
    - Preview classifies exact canonical, unambiguous provider-price match, ambiguous standard, changed evidence, malformed, and review-required rows with zero writes.
    - Apply accepts only a preview digest over exact row version/provider Price/subscription/beneficiary evidence.
    - Repeated apply is zero-write replay; changed source evidence is skipped and reported.
    - Production and live Stripe mutation are always refused.
  </behavior>
  <action>Build a bounded coordinate-driven preview/apply job. Determine a target only from canonical current value or an exact configured Stripe sandbox Price/subscription match plus explicit beneficiary grant evidence; never infer family from child count or map all `standard` globally. Return `migration_review_required` for unresolved `standard`, malformed provider evidence, or missing/contradictory historical first-activation evidence. Bind preview to strong whole-row/version and redacted provider-evidence digests. Run `PYTHONPATH=. .venv/bin/python -m stoa.jobs.migrate_billing_plan_identity preview --environment "$STOA_BILLING_MIGRATION_ENVIRONMENT" --output docs/security/phase-476-plan-migration-preview.json` against the approved non-production inventory; the command writes source SHA, configuration digest, classification counts, row/evidence digests, and unresolved dispositions only, never customer IDs, email, provider IDs, secrets, or card data. If read authority or evidence is unavailable, stop at the execution-time authentication gate and do not synthesize a passing receipt. Apply only with explicit `--apply`, matching digest, unchanged versions, non-production environment, and no provider mutation; require an evidence-bound operator disposition for each review-required row and fail closed while any required disposition is absent. Persist canonical target plus source/evidence/version metadata using conditional writes. Keep exact prior values in redacted migration history sufficient for controlled rollback without exposing Stripe secrets or card data. Close the remaining profile writer by changing the production-admin bootstrap default to canonical `free_trial`; this is a source/config identity correction only and must not execute provisioning or production mutation.</action>
  <acceptance_criteria>
    - Preview invokes no table update/transaction, Cognito mutation, or Stripe mutation.
    - Ambiguous `standard` and unknown historical trial dates never select a canonical paid plan or reset a trial.
    - The source-bound non-production preview receipt exists, passes its schema/redaction verifier, and leaves every unproved plan/trial row `migration_review_required`.
    - Apply and Phase 29 migration closure remain blocked until every required review row has an evidence-bound operator disposition.
    - Apply refuses production, live-mode objects, changed preview evidence, wrong digest, and missing explicit beneficiaries.
    - Commit-then-timeout replay reports applied with zero second write.
    - CLI defaults to preview and rejects unknown fields/coordinates before repository access.
    - No ASVS L1 High threat remains unresolved; each listed mitigation has an observed passing selector for the source-bound phase gate.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_plan_identity_migration.py tests/test_plan_identity_contract.py &amp;&amp; .venv/bin/ruff check src/stoa/jobs/migrate_billing_plan_identity.py scripts/provision_production_admin.py tests/test_plan_identity_migration.py &amp;&amp; PYTHONPATH=. .venv/bin/python -m stoa.jobs.migrate_billing_plan_identity verify-preview --results docs/security/phase-476-plan-migration-preview.json</automated></verify>
  <done>Legacy plan state can be inventoried and conditionally migrated without guesses, broadened access, live Stripe changes, or production writes.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: existing entitlements, beneficiary scope, profile/billing history, migration authority.
- Boundaries: stored legacy rows/provider read evidence → preview; operator preview digest → apply; job → DynamoDB/Cognito.

**ASVS:** L1. **Severity:** High for every listed threat. Any unresolved High threat blocks this plan and the Phase 476 security gate.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Standard is guessed as family | Elevation | Exact provider Price plus explicit beneficiary evidence; otherwise review | Ambiguity tests |
| Stale preview overwrites newer billing | Tampering | Whole-row/version digest and conditional apply | Changed-row race |
| Preview mutates customer data | Tampering | Separate read-only code path and fail-if-called sentinels | Zero-write preview tests |
| Production apply exceeds scope | Elevation / Financial harm | Hard environment/live-mode refusal | Production negative tests |
| Evidence leaks provider/customer identifiers | Information disclosure | Digests/redacted suffixes only | Canary scan |
</threat_model>

<verification>
Migration tests cover preview zero effects, apply conditions, ambiguous rows, timeout replay, and production/live refusal. Plan completion is blocked if any ASVS L1 High threat is open, unobserved, or source-unbound.
</verification>

<success_criteria>
- D-02 migration is executable and reversible without silently translating ambiguous legacy state.
</success_criteria>

<output>Create `476-04-SUMMARY.md` after verification.</output>
