---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 15
type: execute
wave: 3
depends_on: [476-01, 476-03]
files_modified:
  - src/stoa/db/repositories/allowance_repo.py
  - src/stoa/services/allowance_service.py
  - tests/test_token_allowances.py
autonomous: true
requirements: [V9BILL-04]
must_haves:
  truths:
    - "D-18/D-19: weekly input/output budgets use actual-provider token dimensions and exact locked values per canonical plan."
    - "D-21: weeks are Europe/Zurich Monday 00:00 boundaries across DST with no rollover."
    - "Reservations/finalizations are effect-idempotent, concurrency-safe, and preserve provider-cost evidence separately from user debit."
  artifacts:
    - path: src/stoa/db/repositories/allowance_repo.py
      provides: "Conditional reservation, provider observation, finalization, restoration, and support-case counters"
    - path: src/stoa/services/allowance_service.py
      provides: "Zurich windows, plan budgets, admission, and projections"
    - path: tests/test_token_allowances.py
      provides: "Budget, DST, overspend, replay, and evidence tests"
  key_links:
    - from: src/stoa/services/allowance_service.py
      to: src/stoa/db/repositories/allowance_repo.py
      via: "window/budget resolved before conditional reservation"
      pattern: "reserve_allowance"
---

<objective>
Create the weekly token allowance ledger and Zurich calendar-window semantics.

Purpose: replace request-count plan limits with provider-token budgets that cannot overspend under concurrency.
Output: conditional allowance repository/service and deterministic budget/DST tests.
</objective>

## Artifacts this phase produces

- New files `src/stoa/db/repositories/allowance_repo.py`, `src/stoa/services/allowance_service.py`, and `tests/test_token_allowances.py`.
- `zurich_week()`, `plan_allowance_budget()`, `reserve_token_allowance()`, `record_provider_usage()`, `finalize_token_allowance()`, `restore_user_allowance()`, and `get_allowance_projection()`.
- Entities `allowance_counter.v1`, `allowance_effect.v1`, and `provider_usage_evidence.v1`.
- Fields `finalized_input_tokens`, `finalized_output_tokens`, `reserved_input_tokens`, `reserved_output_tokens`, `provider_cost_input_tokens`, `provider_cost_output_tokens`, `restored_at`, and `week_identity`.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-01-SUMMARY.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-03-SUMMARY.md
</context>

<tasks>

<task type="auto" id="476-15-01" tdd="true">
  <name>Implement concurrent weekly token reservations and projections</name>
  <files>src/stoa/db/repositories/allowance_repo.py, src/stoa/services/allowance_service.py, tests/test_token_allowances.py</files>
  <read_first>
    - src/stoa/db/repositories/allowance_repo.py (new target; confirm absent)
    - src/stoa/services/allowance_service.py (new target; confirm absent)
    - tests/test_token_allowances.py (new target; confirm absent)
    - src/stoa/db/repositories/usage_ledger_repo.py deterministic keys/conditional persistence analog
    - src/stoa/services/usage_ledger_service.py idempotency/reconciliation projection analog
    - src/stoa/models/allowance.py Plan 01 contracts
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md D-18, D-19, D-21, and D-22
  </read_first>
  <behavior>
    - Budget values exactly match D-19 for all four plans and selected family beneficiary scope.
    - Zurich weeks start/end at local Monday midnight through both DST transitions.
    - Concurrent reservations never make finalized+reserved exceed either input or output budget.
    - Same effect replay returns original reservation/finalization/restoration without double count.
  </behavior>
  <action>Compute each window from `Europe/Zurich` local Monday dates, persist ISO week identity and converted UTC boundaries, and never derive next week by adding fixed UTC seconds. Resolve exactly D-19 budgets. Implement account-fenced conditional transactions for a payload-bound effect row plus aggregate counter CAS; reserve provider-counted input and bounded max output, record immutable provider usage/cost evidence, finalize exact actual counts, or restore only user reservation/debit while retaining provider-cost evidence per D-22. Keep prompts/answers out of evidence. Project input/output remaining and percentages separately for parent and exact redacted evidence for admin.</action>
  <acceptance_criteria>
    - Exact budget table and no-rollover behavior are tested.
    - Zurich spring/fall DST windows start/end at local Monday 00:00 and have correct non-fixed UTC durations.
    - Barrier tests show no input/output overspend and byte-stable replay after unrelated traffic.
    - Restoration removes user debit/reservation but preserves immutable provider usage/cost evidence.
    - Bool/fraction/negative/malformed persisted counts fail closed; evidence contains no content.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_token_allowances.py &amp;&amp; .venv/bin/ruff check src/stoa/db/repositories/allowance_repo.py src/stoa/services/allowance_service.py tests/test_token_allowances.py</automated></verify>
  <done>Weekly allowance admission is DST-correct, plan-correct, concurrent-safe, replayable, and separates user debit from provider cost.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: token budgets, reservation/finalization identity, provider-cost evidence.
- Boundaries: entitlement → budget; request estimate → reservation; provider usage → evidence/finalization; projection → parent/admin.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Concurrent overspend | Tampering | Counter CAS plus effect receipt | Barrier tests |
| DST assigns wrong week | Tampering / Repudiation | IANA local-calendar boundaries | DST cases |
| Retry double charges | Repudiation | Payload-bound effect identity | Replay tests |
| Restored allowance erases provider cost | Tampering | Separate immutable evidence | Restoration assertions |
| Learning content leaks in evidence | Information disclosure | Closed count/model/correlation schema | Canary scan |
</threat_model>

<verification>
Token allowance tests cover budget table, DST, concurrency, exact replay, restoration, and redaction.
</verification>

<success_criteria>
- D-18, D-19, D-21, and the repository half of D-22 are executable.
</success_criteria>

<output>Create `476-15-SUMMARY.md` after verification.</output>
