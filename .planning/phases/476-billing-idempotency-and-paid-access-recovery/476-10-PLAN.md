---
phase: 476-billing-idempotency-and-paid-access-recovery
plan: 10
type: execute
wave: 2
depends_on: [476-01]
files_modified:
  - src/stoa/db/repositories/billing_fact_repo.py
  - tests/test_billing_fact_repo.py
autonomous: true
requirements: [V9BILL-02, V9BILL-04]
threat_model:
  asvs_level: L1
  assets: ["Signed-event replay/order truth, activation receipt, and atomic grant/allowance state"]
  trust_boundaries: ["Verified Stripe event into fact repository; unordered facts into activation transaction"]
  threats:
    - id: T-476-10-H
      severity: High
      stride: [Tampering, Repudiation, InformationDisclosure, ElevationOfPrivilege]
      description: "Replay, out-of-order facts, partial transactions, foreign/live objects, or raw payload storage could duplicate or falsify paid access."
  mitigations: ["Event/semantic dedupe; object-monotonic facts; conditional transaction; customer/Price/environment binding; redacted fact schema"]
  verification:
    automated: "PYTHONPATH=. .venv/bin/pytest -q tests/test_billing_fact_repo.py"
    evidence: "Duplicate/order/CAS/transaction/mismatch/live/redaction selectors"
  gate:
    open_high_allowed: false
    phase_gate: "scripts/verify_phase476_security_gate.py"
must_haves:
  truths:
    - "D-13/D-17: signed provider events become idempotent facts, not a globally timestamp-ordered entitlement stream."
    - "Duplicate Event IDs and different Event IDs for the same semantic object/type cannot repeat activation."
    - "Invoice and subscription facts remain independently monotonic and can arrive in either order."
  artifacts:
    - path: src/stoa/db/repositories/billing_fact_repo.py
      provides: "Event inbox, semantic dedupe, object facts, reconciliation lease, and atomic activation transaction"
    - path: tests/test_billing_fact_repo.py
      provides: "Duplicate/order/CAS transaction proof"
  key_links:
    - from: src/stoa/db/repositories/billing_fact_repo.py
      to: src/stoa/models/billing.py
      via: "BillingFact and BillingFactKind persistence"
      pattern: "BillingFact"
---

<objective>
Create the fact-oriented persistence boundary for signed Stripe events and exact-once activation.

Purpose: remove the incorrect global `event.created` ordering assumption.
Output: durable inbox/dedupe/facts, conditional activation transaction builder, and ordering tests.
</objective>

## Artifacts this phase produces

- New files `src/stoa/db/repositories/billing_fact_repo.py` and `tests/test_billing_fact_repo.py`.
- `BillingEventDisposition`, `register_provider_event()`, `record_provider_fact()`, `load_activation_facts()`, `claim_fact_reconciliation()`, and `commit_paid_activation()`.
- Entities `billing_event_inbox.v1`, `billing_semantic_dedupe.v1`, `billing_object_fact.v1`, and `billing_activation_receipt.v1`.
- Fields `provider_event_id`, `semantic_identity`, `object_version`, `fact_observed_at`, `activation_version`, and `processing_result`.

<execution_context>
@/Users/zhdeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhdeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-CONTEXT.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-PATTERNS.md
@.planning/phases/476-billing-idempotency-and-paid-access-recovery/476-01-SUMMARY.md
</context>

<tasks>

<task type="auto" id="476-10-01" tdd="true">
  <name>Persist Stripe events as deduplicated object facts</name>
  <files>src/stoa/db/repositories/billing_fact_repo.py, tests/test_billing_fact_repo.py</files>
  <read_first>
    - src/stoa/db/repositories/billing_fact_repo.py (new target; confirm absent)
    - tests/test_billing_fact_repo.py (new target; confirm absent)
    - src/stoa/services/subscription_service.py current event dedupe, provider lookup, stale-event, and transition code
    - src/stoa/db/repositories/notification_repo.py conditional inbox/intent patterns
    - src/stoa/db/repositories/checkout_command_repo.py command/version/grant targets
    - src/stoa/models/billing.py fact/grant contracts
    - .planning/phases/476-billing-idempotency-and-paid-access-recovery/476-RESEARCH.md Pattern 3 and Pitfalls 3 through 5
  </read_first>
  <behavior>
    - Same Event ID is duplicate; different Event IDs with same type/object are semantic duplicates for side effects while retaining audit receipt.
    - Invoice-before-subscription and subscription-before-invoice yield the same fact set.
    - Older object snapshots cannot replace a newer object-version fact or regress an activation receipt.
    - Half-failed activation transaction persists nothing.
  </behavior>
  <action>Persist a redacted event inbox keyed by Stripe Event ID and semantic identity `(event.type,data.object.id)` plus object-specific facts keyed by object ID/version. Do not compare unrelated events through one global timestamp. Preserve receipt of semantic duplicates while suppressing repeated side effects. Add a bounded reconciliation lease. Build `commit_paid_activation` as one DynamoDB transaction conditioned on command/version/provider-customer/Price/test-mode and absent activation receipt; transaction targets command outcome, parent billing projection, explicit grant items supplied by the caller, plan/allowance version, and one activation receipt. Keep raw payload, signature, card data, checkout URL, and secrets outside persisted fact/evidence rows.</action>
  <acceptance_criteria>
    - Event-ID duplicate, semantic duplicate, equal timestamp, delayed older snapshot, and both invoice/subscription orders are explicit tests.
    - Global `last_provider_event_at` is not used to reject a semantically necessary fact.
    - Activation transaction has unique item targets and a conditional absent activation receipt.
    - Injected transaction failure leaves command/billing/grants/allowance/receipt unchanged.
    - Fact rows contain safe object digests/types/status/timestamps only and pass canary redaction.
    - No ASVS L1 High threat remains unresolved; each listed mitigation has an observed passing selector for the source-bound phase gate.
  </acceptance_criteria>
  <verify><automated>PYTHONPATH=. .venv/bin/pytest -q tests/test_billing_fact_repo.py &amp;&amp; .venv/bin/ruff check src/stoa/db/repositories/billing_fact_repo.py tests/test_billing_fact_repo.py</automated></verify>
  <done>Signed-event processing can persist unordered facts and later activate one exact transaction without duplicate/regressive side effects.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: webhook replay/order truth, activation receipt, grant/allowance atomicity.
- Boundaries: verified event → fact repository; unordered facts → activation transaction.

**ASVS:** L1. **Severity:** High for every listed threat. Any unresolved High threat blocks this plan and the Phase 476 security gate.

| Threat | STRIDE | Mitigation | Verification |
|---|---|---|---|
| Replayed event double activates | Tampering / Repudiation | Event and semantic dedupe plus activation receipt | Duplicate matrices |
| Out-of-order event regresses access | Tampering | Object-specific monotonic facts, no global clock ordering | Reverse/equal-time tests |
| Partial transaction grants without billing | Tampering | One conditional TransactWriteItems | Failure-at-boundary tests |
| Foreign/live provider object activates | Elevation / Financial harm | Command customer/Price/environment conditions | Mismatch/live cases |
| Event payload leaks PII/card data | Information disclosure | Redacted fact schema | Canary scan |
</threat_model>

<verification>
Repository tests prove duplicate/delayed/out-of-order semantics and all-or-nothing activation transaction shape. Plan completion is blocked if any ASVS L1 High threat is open, unobserved, or source-unbound.
</verification>

<success_criteria>
- D-17 has a durable fact/receipt mechanism ready for signed webhook orchestration.
</success_criteria>

<output>Create `476-10-SUMMARY.md` after verification.</output>
