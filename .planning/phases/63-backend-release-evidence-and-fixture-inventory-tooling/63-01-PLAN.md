---
plan_id: 63-01
phase: 63
phase_name: Backend Release Evidence And Fixture Inventory Tooling
status: complete
wave: 1
autonomous: true
files_modified:
  - src/stoa/services/release_evidence_service.py
  - src/stoa/routers/admin.py
  - scripts/release_evidence.py
  - tests/test_release_evidence.py
---

# Plan 63-01: Backend Release Evidence And Fixture Inventory Tooling

## Objective

Implement backend-side release evidence validation, safe-fixture inventory, mutation refusal checks, operator CLI commands, and admin-only read/validate endpoints.

## Tasks

1. Add a pure service for release evidence schema validation, privacy denylist scanning, redacted output, fixture inventory, and mutation refusal checks.
2. Add a CLI wrapper for `validate`, `fixture-status`, and `check-mutation`.
3. Add admin-only endpoints for evidence bundle validation and fixture status inspection.
4. Add focused tests for schema validation, redaction failure, fixture inventory sanitization, mutation refusal, CLI exit behavior, and admin-only route gating.
5. Run focused ruff and pytest checks.

## Verification

- `EVIDENCE-AUTO-02` is covered by validator, CLI, admin validate endpoint, and tests.
- `FIXTURE-02` backend inventory/refusal behavior is covered by fixture status service, admin endpoint, CLI, and tests.
- Private markers fail validation and are omitted from redacted output.
- Production mutation remains refusal-only and opt-in; no mutation is performed by Phase 63.
