---
phase: 473-student-content-privacy-and-practice-integrity
plan: 07
type: execute
wave: 4
depends_on: [473-02, 473-03, 473-04, 473-05, 473-06]
autonomous: true
requirements: [V9PRIV-01, V9PRIV-02, V9PRIV-03]
must_haves:
  truths:
    - "D-09 D-10 D-17: expired/invalid/abandoned upload objects remain unusable, cleanup is idempotent, durable history is preserved, and evidence is content/key-redacted"
    - "All D-01 through D-22 decisions and V9PRIV-01/02/03 pass together under ownership, file, lifecycle, quota, preview/result and privileged-scope matrices"
    - "Phase 475/476/478/479/480 boundaries and unavailable external S3/schedule evidence are recorded honestly rather than inferred"
  artifacts:
    - "src/stoa/jobs/upload_cleanup.py"
    - "docs/security/phase-473-evidence.md"
    - "Updated 473-VALIDATION.md observations and deterministic route/OpenAPI contracts"
  key_links:
    - "Cleanup queries only terminal/expired intents and conditionally confirms status before object deletion"
    - "Final evidence binds every requirement/decision to commands, results, source SHA and redacted artifacts"
files_modified:
  - src/stoa/db/repositories/attachment_repo.py
  - src/stoa/services/attachment_service.py
  - src/stoa/jobs/__init__.py
  - src/stoa/jobs/upload_cleanup.py
  - tests/test_attachment_security.py
  - tests/test_practice_privacy.py
  - tests/test_route_authorization_inventory.py
  - docs/security/route-authorization-inventory.json
  - docs/security/phase-473-evidence.md
  - .planning/phases/473-student-content-privacy-and-practice-integrity/473-VALIDATION.md
---

# Plan 473-07: Cleanup, combined privacy gate and source-bound evidence

<objective>
Complete Phase 473 with bounded idempotent upload cleanup, one combined adversarial regression/OpenAPI gate, and source-bound evidence that proves upload ownership and practice integrity without exposing student content or claiming unavailable external rollout checks.
</objective>

## Artifacts this phase produces

- `handler`, `cleanup_expired_uploads`, and `CleanupSummary` in `src/stoa/jobs/upload_cleanup.py`.
- Updated deterministic route authorization inventory for new upload/attachment/attempt/answer identifiers.
- `docs/security/phase-473-evidence.md` mapping SEC-003, SEC-005, BUG-001, V9PRIV-01/02/03 and D-01..D-22 to evidence.
- Completed Phase 473 validation task observations.

<must_haves>
Truths: D-09/D-10/D-17 and every cross-plan decision close together. Artifacts: cleanup handler, generated contract and evidence. Key links: final evidence is derived from actual combined commands and preserves later-phase ownership/NOT RUN limitations.
</must_haves>

<threat_model>
ASVS L1 assets: residual upload objects, durable history, final security claims, generated route contracts and evidence privacy. Trust boundaries: cleanup scan to object deletion; all plan outputs to combined tests; source/test results to evidence. Threats: cleanup deletes active/consumed/durable object, deletion failure revives upload, unbounded scan, one plan regresses another, route inventory misses new body/path IDs, full suite failure is hidden/reassigned, evidence includes keys/content/provider identifiers, or local fakes are claimed as live S3/schedule proof. Mitigations: bounded page/batch, conditional terminal recheck, idempotent delete/tombstone, combined no-skip gate, deterministic inventory regeneration, seeded canary scans, source SHA/digests, exact known Phase 474 delta and explicit NOT RUN rows. Verification: cleanup state matrix, full Phase 473 gate, Phase 472 authorization regression, generated `--check`, full-suite observation and evidence lint. Residual risk: external bucket policy, real presigned POST and scheduled Lambda/IaC remain unproved without separate approval/Phase 479–480 evidence.
</threat_model>

<tasks>

<task type="auto" id="473-07-01">
  <name>Implement bounded idempotent expired and invalid upload cleanup</name>
  <files>src/stoa/db/repositories/attachment_repo.py, src/stoa/services/attachment_service.py, src/stoa/jobs/__init__.py, src/stoa/jobs/upload_cleanup.py, tests/test_attachment_security.py</files>
  <read_first>
    - src/stoa/jobs/weekly_reports.py thin scheduled handler pattern
    - src/stoa/db/repositories/attachment_repo.py status/expiry/query operations from Plans 02–04
    - src/stoa/services/attachment_service.py invalid/expired transitions and durable reference logic
    - src/stoa/services/report_artifact_service.py delete failure handling
    - .planning/phases/473-student-content-privacy-and-practice-integrity/473-VALIDATION.md cleanup verification row
  </read_first>
  <action>Add a handler/service that processes a bounded page/batch of intents whose expiry is past or whose state is terminal invalid/expired. Before each object deletion, conditionally re-read/recheck owner-independent internal status/version and confirm no durable attachment references the object. Mark the record cleanup-pending/tombstoned as needed, call private S3 delete, then record cleanup complete; deletion failure keeps it non-consumable and retryable without restoring `validated`. Never delete active/consuming/consumed durable objects or conversation/question history. Return only counts by safe category and continuation token, with no IDs/keys/content. Make repeated and reordered invocations idempotent.</action>
  <verify>.venv/bin/python -m pytest -q tests/test_attachment_security.py -k 'cleanup or expired or invalid or abandoned or delete or durable or idempotent'</verify>
  <acceptance_criteria>
    - Expired/invalid/abandoned objects are deleted once or safely retried while their intents remain unusable.
    - Validated-within-expiry, consuming, consumed and durable referenced objects are never deleted.
    - S3 delete failure does not restore a state accepted by finalize/question/conversation flows.
    - Handler work is bounded by explicit batch/page limits and exposes only category counts/continuation.
    - Local handler tests use fake S3/table only; no ambient AWS or schedule claim occurs.
  </acceptance_criteria>
  <done>Unused unsafe objects converge toward deletion without risking durable student history.</done>
</task>

<task type="auto" id="473-07-02">
  <name>Run combined Phase 473 security gate and publish redacted evidence</name>
  <files>tests/test_attachment_security.py, tests/test_practice_privacy.py, tests/test_route_authorization_inventory.py, docs/security/route-authorization-inventory.json, docs/security/phase-473-evidence.md, .planning/phases/473-student-content-privacy-and-practice-integrity/473-VALIDATION.md</files>
  <read_first>
    - .planning/phases/473-student-content-privacy-and-practice-integrity/473-CONTEXT.md all D-01..D-22
    - .planning/phases/473-student-content-privacy-and-practice-integrity/473-RESEARCH.md Validation Architecture and threat model
    - all 473-01 through 473-06 SUMMARY.md files and verification outputs
    - scripts/generate_route_authorization_inventory.py and checked JSON workflow
    - docs/security/phase-472-evidence.md evidence format and limitations
    - .planning/ROADMAP.md Phase 475/476/478/479/480 ownership boundaries
  </read_first>
  <action>Run one unfiltered combined gate covering supported/boundary/malformed file fixtures; own/foreign/missing/expired/invalid/reused upload and saved attachment cases; quota free/paid/race behavior; normal/stream conversation history/reuse/deletion; question OCR owner/atomic/idempotent/redaction cases; every student preview family; attempt write-before-result; assigned/wrong/stale teacher, admin, student and parent answer matrices; cleanup; route inventory and OpenAPI response schemas. Seed raw key, OCR, document, answer, provider and student identifiers and assert absence from public bodies/logs/evidence. Regenerate route inventory twice, require byte-identical output and `--check`. Run the Phase 472 authorization regression subset and observe the full Python suite without weakening/reassigning the exact Phase 474-owned strict Settings failures. Update VALIDATION with actual UTC commands/counts/status and publish `phase-473-evidence.md` mapping findings/requirements/decisions to source SHA/digests. Mark real S3 POST and external schedule/IaC evidence NOT RUN unless separately approved/configured; perform no production mutation.</action>
  <verify>.venv/bin/python -m pytest -q tests/test_files.py tests/test_attachment_security.py tests/test_questions.py tests/test_conversations.py tests/test_practice.py tests/test_practice_privacy.py tests/test_curriculum_rollout.py tests/test_route_authorization_inventory.py tests/test_student_authorization_matrix.py</verify>
  <acceptance_criteria>
    - V9PRIV-01, V9PRIV-02 and V9PRIV-03 pass together with positive controls and zero-effect negative sentinels.
    - All D-01..D-22 decisions map to at least one passing automated command/evidence row.
    - Route inventory/OpenAPI are byte-stable and list every new upload/attachment/attempt/privileged-answer identifier with executable authorization metadata.
    - Evidence contains actual command/result/source SHA/digests and no seeded object key, file/OCR/answer content, provider payload, token or raw student identifier.
    - Full-suite delta and Phase 475/476/478/479/480 boundaries are explicit; unavailable live S3/schedule checks are NOT RUN, never inferred pass.
  </acceptance_criteria>
  <done>Phase 473 closure is combined, deterministic, redacted, source-bound and honest about external/later-phase limits.</done>
</task>

</tasks>

<verification>
Run cleanup tests, the exact combined Phase 473 command, deterministic inventory generation/check, Phase 472 authorization regression and full-suite observation. Inspect evidence and generated schemas for every seeded privacy canary.
</verification>

<success_criteria>
SEC-003, SEC-005 and BUG-001 are locally closed under one adversarial gate; student uploads/history and practice answers have safe contracts ready for later mobile implementation.
</success_criteria>
