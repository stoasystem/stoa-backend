---
phase: 473-student-content-privacy-and-practice-integrity
plan: 40
type: execute
wave: 33
depends_on: [473-39]
autonomous: true
requirements: [V9PRIV-02]
gap_closure: true
files_modified:
  - scripts/verify_phase473_evidence.py
  - tests/test_phase473_evidence_verifier.py
  - docs/security/phase-473-evidence-results.json
  - docs/security/phase-473-evidence.md
  - docs/security/phase-473-evidence-manifest.json
  - .planning/phases/473-student-content-privacy-and-practice-integrity/473-VALIDATION.md
must_haves:
  truths:
    - "The immutable Phase 473 evidence registry executes exact lower tests for both verifier blockers and all three reproduced warnings"
    - "Evidence binds one clean candidate SHA to raw log, JUnit, deterministic node manifest, exact argv, inventory artifacts, privacy denial, and independently recomputed counts/hashes"
    - "Two-worker deletion races, legacy/malformed delivery metadata, claimed-intent crash states, production timestamp validation, and parent-row CAS are observed rather than inferred from source strings"
    - "V9PRIV-02 and D-10/D-16/D-17 close only if all focused, inherited, inventory, lint, and full-suite gates pass with no skip/XFAIL/XPASS"
    - "Real S3, deployed cleanup/IaC, and production/deployed logs remain explicit NOT RUN obligations for Phases 479/480 and are not inferred from local doubles"
  artifacts:
    - "Updated strict evidence verifier and fixture-driven contract tests"
    - "Fresh immutable raw capture outside the repository and canonical checked results"
    - "Source-bound evidence, manifest, and Phase 473 validation publication"
    - "Finding/requirement/decision/inventory-to-node coverage for all final gaps"
  key_links:
    - "five finding IDs -> exact pytest node/lower fake -> raw receipt -> immutable candidate"
    - "checked boundary/private-store artifacts -> exact IDs/digests/selectors -> observed node coverage"
    - "candidate SHA -> results/evidence/validation hashes -> non-circular publication manifest"
    - "local evidence/external NOT RUN split -> independent phase verifier -> honest closure decision"
---

# Plan 473-40: Immutable evidence and aggregate re-verification

<objective>
Capture and publish fresh source-bound evidence that independently reproduces closure of CR-01, CR-02, WR-01, WR-02, and WR-03, then expose an exact re-verification contract for V9PRIV-02 and D-10/D-16/D-17 without overclaiming external systems.

Purpose: Replace the substantively stale Phase 473 evidence with one immutable candidate whose receipts prove the final lease, CAS, ownership, crash-recovery, and deletion-race behavior at real lower boundaries.
Output: Updated evidence verifier/tests, complete strict capture, checked results/evidence/manifest/validation, and source coverage audit.
</objective>

## Artifacts this phase produces

- New evidence gate IDs `P473-DELETION-CLAIM-FENCING`, `P473-DELIVERY-INTENT-RECOVERY`, `P473-PRIVATE-DELIVERY-FENCING`, and `P473-FINAL-GAP-REGRESSION`.
- New coverage dimensions `review_findings`, `gap_truths`, `claim_fence_nodes`, `delivery_scope_nodes`, and `crash_state_nodes` in `phase-473-evidence-results.json`.
- Fresh raw capture tree `/tmp/phase473-evidence/<candidate>/` containing logs, JUnit, node manifests, generated inventories, and checked result; only canonical redacted outputs are published.
- Updated `phase-473-evidence.md`, non-circular manifest, and `473-VALIDATION.md` bound to the candidate and exact artifact hashes.
- An explicit source audit showing every current finding covered and all deferred external obligations excluded from local closure.

<must_haves>
Truths: all five findings execute at lower boundaries, one clean SHA binds all receipts, no skipped/expected failures count, V9PRIV-02/decisions are node-covered, and external evidence remains honest. Artifacts: gate registry, raw capture, checked results/evidence/manifest/validation, and audit. Key links: finding to node, inventories to node, candidate to hashes, and evidence split to independent verification.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test collection -> claimed behavior | Collected high-level tests may not execute the declared lower repository/provider target. |
| command output -> evidence receipt | Console prose alone cannot prove exact argv, node outcomes, or source identity. |
| generated inventory -> coverage | Stale/missing IDs or selectors can make a green suite incomplete. |
| private/provider test data -> published evidence | Raw canaries, identifiers, payloads, coordinates, or exception detail must not enter artifacts. |
| local doubles -> external completion | Local tests cannot prove real S3, deployed scheduler/IaC, or production log behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-473-40-01 | Repudiation | gate receipt | mitigate | Exact argv, UTC bounds, clean SHA, raw log/JUnit/node bytes, and independently recomputed hashes/counts. |
| T-473-40-02 | Tampering | coverage map | mitigate | Join exact checked inventory IDs/digests/selectors to observed lower-fake nodes and fail on missing/extra mappings. |
| T-473-40-03 | Information disclosure | evidence publication | mitigate | Fixed denylist plus generated/raw/published privacy scan and redacted provider-neutral facts only. |
| T-473-40-04 | Spoofing | external evidence | mitigate | Explicit NOT RUN obligations and no local-to-live inference. |
| T-473-40-05 | Denial of service | partial capture | mitigate | Any gate/capture/publication failure invalidates the full observation set and requires a new clean candidate/full rerun. |
| T-473-40-SC | Tampering | package installs | mitigate | No package-manager install is planned; retain the package-legitimacy gate if scope changes. |
</threat_model>

<tasks>

<task type="auto" id="473-40-01" tdd="true">
  <name>Create failing evidence-registry and five-finding coverage contracts</name>
  <files>tests/test_phase473_evidence_verifier.py, scripts/verify_phase473_evidence.py</files>
  <behavior>
    - Registry includes exact focused gates for the three new test modules plus inherited deletion/notification and full-suite observations.
    - CR-01, CR-02, WR-01, WR-02, and WR-03 each map to exact observed nodes and declared lower fake targets.
    - V9PRIV-02 and D-10/D-16/D-17 require the new nodes plus current checked inventory coverage.
    - Missing, duplicate, skipped, XFAIL, XPASS, high-level-only, stale-inventory, or privacy-tainted receipts fail verification.
  </behavior>
  <read_first>
    - scripts/verify_phase473_evidence.py current gate registry, receipt verification, inventory composition, privacy denial, capture, and publication protocol
    - tests/test_phase473_evidence_verifier.py fixture-driven negative receipt/coverage/publication cases
    - docs/security/phase-473-boundary-inventory.json and docs/security/phase-473-private-store-inventory.json final Plan 473-39 selector maps
    - tests/test_phase473_account_deletion_claim_fencing.py exact CR-01/WR-01/WR-02 lower nodes
    - tests/test_phase473_delivery_intent_recovery.py exact WR-03 lower nodes
    - tests/test_phase473_private_delivery_fencing.py exact CR-02 lower nodes
    - .planning/phases/473-student-content-privacy-and-practice-integrity/473-VERIFICATION.md current blocked requirement/decisions and reproduced probes
  </read_first>
  <action>Extend fixture contracts before changing the verifier. Require dedicated strict pytest gates for the three new focused modules and one combined final-gap regression that also runs inherited account-deletion seal/deletion, notification deletion, notification, and WebSocket modules. Add exact coverage entries for each review finding, verifier gap truth, V9PRIV-02, and D-10/D-16/D-17; every entry names pytest node, expected lower fake/call/condition, and source/inventory row. Reject coverage satisfied only by collection or source strings. Add negative fixtures for a missing two-worker node, missing current-epoch assertion, missing legacy/malformed metadata case, missing provider-call counter, missing crash state, missing production constructor, missing parent CAS, duplicate node mapping, skipped/XFAIL/XPASS, wrong candidate, stale inventory digest, and private canary. Preserve all earlier V9PRIV/D-01 through D-22 coverage. RED exits exactly pytest code 1 without collection/import failure.</action>
  <acceptance_criteria>
    - All five findings and three failed decisions require exact new lower-bound nodes.
    - Negative fixtures prove high-level collection, stale inventory, skip/XFAIL/XPASS, and source-string assertions cannot satisfy coverage.
    - Earlier Phase 473 requirement/decision coverage remains required.
    - Failure is due only to missing registry/verifier behavior.
  </acceptance_criteria>
  <verify><automated>bash -lc 'set +e; .venv/bin/python -m pytest -q tests/test_phase473_evidence_verifier.py; rc=$?; test "$rc" -eq 1'</automated></verify>
  <done>The evidence verifier cannot pass until all five findings are observed through exact lower-bound receipts on one candidate.</done>
</task>

<task type="auto" id="473-40-02" tdd="true">
  <name>Implement strict final-gap gates and immutable capture verification</name>
  <files>scripts/verify_phase473_evidence.py, tests/test_phase473_evidence_verifier.py</files>
  <behavior>
    - Every gate runs from one clean immutable candidate with exact argv and strict node outcomes.
    - Generated boundary/private-store artifacts are reproduced and their IDs/digests/selectors join to observed nodes.
    - Five findings, V9PRIV-02, D-10/D-16/D-17, all V9PRIV requirements, and D-01 through D-22 have complete exact coverage.
    - Any failed or partial gate invalidates capture; privacy denial scans raw/generated/published candidates.
  </behavior>
  <read_first>
    - tests/test_phase473_evidence_verifier.py final RED fixtures from task 473-40-01
    - scripts/verify_phase473_evidence.py current `_deep_suite_modules`, registry, receipt, coverage, capture, and publication functions
    - docs/security/phase-473-boundary-inventory.json and private-store inventory exact selector schemas
    - tests/fixtures/phase473_evidence_denylist.txt fixed privacy seeds
  </read_first>
  <action>Add the four new gate IDs with exact argv and no ambient provider/network access. Require strict pytest node manifests to report setup/call/teardown success and reject skip, XFAIL, XPASS, missing, duplicate, or uncollected nodes. Extend coverage verification to map all five review IDs and gap truths to the exact focused nodes/lower fakes declared by Plan 473-39, and require the lower evidence facts: explicit `now_epoch`, claim/version/result digest, durable final set, UTC production constructor, parent row CAS conflict, pre-effect/inflight crash states, strong legacy owner join, sealed-global validation, and zero provider calls in every invalid/deletion-race case. Reproduce both generators into capture storage, check byte identity and semantic `--check`, and compose exact IDs/digests/17 branches. Preserve prior V9PRIV-01/02/03 and D-01 through D-22 coverage, Phase 472 authorization regression, targeted Ruff/diff checks, full pytest gate, and external obligations. Bind each receipt to clean candidate pre/post state, exact raw artifacts, hashes, UTC bounds, counts, argv, and privacy facts. Any failure removes/invalidates the candidate observation and requires the complete registry to rerun.</action>
  <acceptance_criteria>
    - Verifier tests pass all positive fixtures and reject every task-01 negative fixture.
    - Registry contains exact dedicated and combined final-gap gates plus inherited/full observations.
    - Coverage joins current inventory rows/selectors to observed lower nodes for all five findings.
    - Capture verification requires clean identical candidate state and zero privacy matches across raw/generated/published data.
    - External S3/IaC/production-log obligations remain explicit NOT RUN and separate from local success.
  </acceptance_criteria>
  <verify><automated>bash -lc 'set -euo pipefail; .venv/bin/python -m pytest -q tests/test_phase473_evidence_verifier.py; .venv/bin/ruff check scripts/verify_phase473_evidence.py tests/test_phase473_evidence_verifier.py'</automated></verify>
  <done>The strict verifier is capable of rejecting any incomplete, high-level-only, stale, tainted, or externally overclaimed final-gap evidence set.</done>
</task>

<task type="auto" id="473-40-03">
  <name>Capture one clean candidate, publish checked evidence, and expose re-verification</name>
  <files>docs/security/phase-473-evidence-results.json, docs/security/phase-473-evidence.md, docs/security/phase-473-evidence-manifest.json, .planning/phases/473-student-content-privacy-and-practice-integrity/473-VALIDATION.md</files>
  <read_first>
    - scripts/verify_phase473_evidence.py final registry/capture/publication contract from task 473-40-02
    - docs/security/phase-473-evidence-results.json current substantively stale receipts
    - docs/security/phase-473-evidence.md current human-readable findings/requirements/decisions/external split
    - docs/security/phase-473-evidence-manifest.json non-circular candidate/publication schema
    - .planning/phases/473-student-content-privacy-and-practice-integrity/473-VALIDATION.md current task/requirement/evidence mapping
    - .planning/phases/473-student-content-privacy-and-practice-integrity/473-VERIFICATION.md exact two blockers, three warnings, and deferred external boundaries
  </read_first>
  <action>After Plans 473-36 through 473-39 implementation/tests are committed and the worktree is clean, record that exact HEAD as the immutable tested candidate and capture the complete closed registry from gate one into `/tmp/phase473-evidence/<candidate>/`; do not reuse any receipt from the earlier candidate. If any gate, inventory, privacy, diff, or state check fails, discard the whole observation and create a new clean candidate after fixing the cause. Independently verify the capture twice, then publish canonical checked results, human evidence, and Phase 473 validation. Record exact focused/inherited/full counts, raw receipt hashes, candidate SHA, inventory digests/counts, finding-to-node mappings, requirement/decision coverage, zero privacy matches, and exact external NOT RUN obligations. The human evidence must explicitly show the two-worker unexpired/expired takeover/stale-write/stale-finalization matrix, production UTC timestamp rejection cases, parent concurrent-update conflict, pre-effect/inflight/post-acceptance crash matrix, legacy/missing/malformed/stale/global delivery matrix, and deletion-race zero-provider counters. Publish a non-circular manifest that hashes results/evidence/validation but not itself and verify the dirty draft. Then run `git add -- docs/security/phase-473-evidence-results.json docs/security/phase-473-evidence.md docs/security/phase-473-evidence-manifest.json .planning/phases/473-student-content-privacy-and-practice-integrity/473-VALIDATION.md`, require the staged name set to be exactly those four paths, and run `git commit -m "docs(473-40): publish immutable final-gap evidence" -- docs/security/phase-473-evidence-results.json docs/security/phase-473-evidence.md docs/security/phase-473-evidence-manifest.json .planning/phases/473-student-content-privacy-and-practice-integrity/473-VALIDATION.md`; no other path may be staged or committed. Only after that commit exists, derive `candidate=$(git rev-parse HEAD^)` and require it to equal the immutable candidate used by the raw capture and manifest, with the publication commit its single direct child. Re-run `verify-capture` for that candidate and clean-mode `verify-publication --candidate "$candidate"` without `--dirty`; the latter must recompute every manifest SHA-256 and byte count from the committed four files. Run `git diff --check "$candidate" HEAD`, require the committed candidate-to-HEAD diff to contain exactly those four paths, and require `git status --porcelain` to be empty. Any post-commit mismatch invalidates publication and requires a new candidate/full capture rather than amending or reusing receipts. Do not edit `473-VERIFICATION.md`; it remains the independent report and must be rerun after this clean publication exists.</action>
  <acceptance_criteria>
    - All gates pass on one clean candidate with strict node outcomes and independently verified raw receipts.
    - Published evidence gives exact source SHA, commands, counts, hashes, inventory coverage, five-finding nodes, and zero privacy matches.
    - V9PRIV-02 and D-10/D-16/D-17 map to the new lower evidence; V9PRIV-01/03 and D-01 through D-22 retain complete mappings.
    - The four publication files are committed together before the decisive integrity checks; no fifth path is present in the candidate-to-HEAD diff.
    - `HEAD^` is the immutable tested candidate, HEAD is its single direct publication child, and `verify-capture` succeeds for that exact parent.
    - Clean-mode `verify-publication` succeeds without `--dirty` and recomputes every manifest hash and byte count from committed files.
    - `git diff --check "$candidate" HEAD` passes, the exact four-path committed diff matches, and `git status --porcelain` is empty.
    - Real S3, deployed cleanup/IaC, and production/deployed logs remain NOT RUN under Phases 479/480.
  </acceptance_criteria>
  <verify><automated>bash -lc 'set -euo pipefail; candidate=$(git rev-parse HEAD^); capture_root=/tmp/phase473-evidence/$candidate; test "$(git rev-parse HEAD^)" = "$candidate"; test "$(git rev-list --count "$candidate"..HEAD)" -eq 1; .venv/bin/python scripts/verify_phase473_evidence.py verify-capture --candidate "$candidate" --capture-root "$capture_root"; .venv/bin/python scripts/verify_phase473_evidence.py verify-publication --candidate "$candidate"; git diff --check "$candidate" HEAD; expected=$(printf "%s\n" .planning/phases/473-student-content-privacy-and-practice-integrity/473-VALIDATION.md docs/security/phase-473-evidence-manifest.json docs/security/phase-473-evidence-results.json docs/security/phase-473-evidence.md | LC_ALL=C sort); actual=$(git diff --name-only "$candidate" HEAD | LC_ALL=C sort); test "$actual" = "$expected"; test -z "$(git status --porcelain)"'</automated></verify>
  <done>Fresh immutable evidence reproduces all five fixes, is committed as the exact clean four-file direct child of its tested candidate with verified hashes/byte counts and empty worktree, and is ready for independent Phase 473 verification without claiming unavailable external proof.</done>
</task>

</tasks>

## Multi-source coverage audit

| Source | ID | Feature/requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Student uploads and exercise previews cannot expose another user's content or answers, including safe cleanup/account closure | 473-36..473-40 | COVERED | Final cleanup and outbound effects now preserve the verified privacy goal under concurrency/crash. |
| REQ | V9PRIV-01 | Owner-bound OCR upload association | 473-01..473-35 | COVERED | Already locally satisfied; new plans preserve its deletion fence/evidence coverage. |
| REQ | V9PRIV-02 | Bounded lifecycle, stable errors, and safe failure cleanup | 473-36..473-40 | COVERED | The independently blocked requirement is the direct closure target. |
| REQ | V9PRIV-03 | Answer-free pre-submit practice contracts | 473-01..473-35 | COVERED | Already locally satisfied; retained in final evidence mapping. |
| RESEARCH | upload/attachment lifecycle | Opaque owner records, validation, atomic association, cleanup, and durable reuse | 473-01..473-35, 473-36 | COVERED | The new deletion claim/profile CAS repairs adjacent lifecycle cleanup proof without changing upload scope. |
| RESEARCH | provider/ownership threats | Foreign/private delivery, raw coordinate/content leakage, failure recovery, and negative matrices | 473-37..473-40 | COVERED | Crash-safe intent plus authoritative owner resolution and redacted evidence. |
| RESEARCH | validation architecture | Repository state machines, route/provider contracts, role/resource matrices, generated inventory/evidence | 473-36..473-40 | COVERED | Exact lower race/crash/legacy selectors are source-sealed and captured. |
| CONTEXT | D-01, D-02, D-03, D-04, D-05 | Exact supported upload types, bounds, and validation | 473-01..473-28 | COVERED | Existing implementation/evidence remains in the final registry. |
| CONTEXT | D-06, D-07, D-08, D-09 | Intent expiry, atomic consumption/replay, retry recovery, and unusable cleanup | 473-01..473-35, 473-36 | COVERED | Claim fencing strengthens safe cleanup; no scope reduction. |
| CONTEXT | D-10 | Durable attachments until conversation deletion/account closure | 473-36, 473-38, 473-40 | COVERED | Single-worker deletion and zero post-fence delivery restore account-closure integrity. |
| CONTEXT | D-11, D-12, D-13, D-14, D-15 | Quotas, reuse, Actor ownership, concealment, and `upload_expired` | 473-01..473-35 | COVERED | Existing local contracts remain regression/evidence inputs. |
| CONTEXT | D-16 | Stable distinct failure actions | 473-37, 473-38, 473-40 | COVERED | Closed delivery-owner/classification/cancel/unknown outcomes replace direct fallback and blind ambiguity. |
| CONTEXT | D-17 | No provider coordinates, OCR/private content, or foreign content in responses/logs/evidence | 473-36..473-40 | COVERED | Tokens/digests/status-only persistence and evidence privacy denial cover all new paths. |
| CONTEXT | D-18, D-19, D-20, D-21, D-22 | Attempt-gated answers, safe hints, answer-free previews, and scoped privileged reads | 473-01..473-35 | COVERED | Already verified locally and retained in final evidence mapping. |
| REVIEW/VERIFY | CR-01 | Current-time deletion lease plus opaque claim/version/CAS/durable final proof | 473-36, 473-39, 473-40 | COVERED | Includes unexpired/expired two-worker, stale-write, and stale-finalization tests. |
| REVIEW/VERIFY | CR-02 | Authoritative private delivery, sealed global exception, and intent/fence protection | 473-38, 473-39, 473-40 | COVERED | Includes missing/malformed/stale/legacy/global and deletion-race tests. |
| REVIEW/VERIFY | WR-01 | Valid production deletion timestamps | 473-36, 473-39, 473-40 | COVERED | Production constructor and repository rejection tests. |
| REVIEW/VERIFY | WR-02 | Parent-profile row CAS/narrow update | 473-36, 473-39, 473-40 | COVERED | Concurrent parent update remains intact and blocks clean epoch. |
| REVIEW/VERIFY | WR-03 | Safe claimed-intent crash recovery without blind retry | 473-37, 473-39, 473-40 | COVERED | Pre-effect reclaim and inflight ambiguity terminalization. |
| DEFERRED | Phase 479/480 | Real S3, deployed cleanup/IaC, and production/deployed logs | NONE | EXCLUDED | Explicitly deferred external obligations; not local gaps and never inferred from doubles. |

<verification>
Run verifier contract tests, capture every exact gate on one clean candidate, independently reverify raw receipts/inventories/coverage/privacy facts twice, publish the non-circular evidence set, verify dirty and clean publication state, and leave `473-VERIFICATION.md` untouched for the independent verifier rerun.
</verification>

<success_criteria>
- CR-01, CR-02, WR-01, WR-02, and WR-03 each have exact observed lower-bound evidence on one immutable candidate.
- V9PRIV-02 and D-10/D-16/D-17 have complete node/inventory coverage and no failed focused or inherited gate.
- Full pytest is recorded with strict no-skip/no-XFAIL/no-XPASS outcomes, not inferred from console prose.
- Published artifacts are hash-reproducible and contain zero private/provider-coordinate matches.
- External S3/IaC/production evidence remains honest NOT RUN work owned by Phases 479/480.
</success_criteria>

<output>
Create `.planning/phases/473-student-content-privacy-and-practice-integrity/473-40-SUMMARY.md` when done, then rerun independent Phase 473 verification.
</output>
