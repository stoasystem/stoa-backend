---
context: phase
phase: 477-web-authentication-and-contract-convergence
task: 2
total_tasks: 4
status: paused
last_updated: 2026-08-14T22:01:02.068Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through repository and test evidence.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Phase 474 prerequisite is incomplete — it remains 39/47 with eight retained plans and no independent verification — audit and close its actual gaps before resuming Phase 477.
- [ ] CONSTRAINT: Teacher invitation policy is superseded — public Teacher registration followed by administrator review is the latest human decision — do not restore invitation activation or `/teacher-access` as authority.
- [ ] CONSTRAINT: `teacher` is the only active role term — do not preserve `tutor` aliases, fields, routes, or fallback mappings in runtime contracts.
- [ ] CONSTRAINT: Isolated green tests are insufficient — real frontend/backend adapter contracts and infrastructure synthesis must be checked before claiming completion.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Green mock equals integrated success | Frontend component tests do not start the MSW server and cannot detect known live API mismatches. | blocking | Require OpenAPI/adapter contract tests and at least one non-intercepted integrated journey. |
| Old topology test equals deployability | Infra topology tests pass while `app.py` has an undefined symbol and still assembles a topology that conflicts with the locked staging-only authority. | blocking | Run lint plus CDK app/synth checks against explicit Phase 474 topology invariants. |
| Checkpoint equals approved context | Phase 477 has no final CONTEXT.md or PLAN.md. | advisory | Resume through `$gsd-discuss-phase 477`; never plan directly from this checkpoint. |

<current_state>
Phase 477 discussion is intentionally paused after two of four gray areas. No CONTEXT.md or PLAN.md exists. The next work is an audit, not implementation or continued discussion.
</current_state>

<completed_work>

Completed discussion areas:
- Login and session recovery policy is captured in `477-DISCUSS-CHECKPOINT.json`.
- Role policy is now public Student/Parent/Teacher registration, verified Teacher accounts entering `pending_review`, and administrator-only activation.
- Invitation activation and active `tutor` compatibility are explicitly rejected.
- Planning/evidence tracking was restored by removing the accidental `.planning/` and `evidence/` ignore rules.
</completed_work>

<remaining_work>

- Audit backend, frontend, and infra implementation against current planning authority and exact tested evidence.
- Classify every material gap as functionality, test coverage/evidence, security/control, or planning-authority drift.
- Phase 477 OpenAPI contract-authority discussion: not started.
- Phase 477 mock/real-service boundary discussion: not started.
- Do not write 477-CONTEXT.md or PLAN.md until the user explicitly resumes the phase after reviewing the audit.
</remaining_work>

<decisions_made>

- One `/login` surface supports password by default and login code as an alternate, with one authoritative session restoration path.
- Expiry uses one coalesced refresh attempt; failure clears local session and preserves only a safe return target.
- Explicit logout is all-device sign-out and provider revocation failure cannot be presented as success.
- Teacher registration uses the existing public registration system. Email verification leads to `pending_review`; only administrator approval activates `teacher`.
- Pending or rejected Teacher registrants receive no Teacher dashboard or student-resource authority.
- Delete invitation/token activation and all active `tutor` contracts; no silent migration or fallback role.
</decisions_made>

<blockers>
- Phase 474 release authority is not independently verified.
- Current planning text still contains the superseded invitation model.
- Known frontend/backend contract mismatches and an infra assembly defect make current green test subsets non-authoritative.
</blockers>

## Required Reading (in order)

1. `.planning/phases/477-web-authentication-and-contract-convergence/477-DISCUSS-CHECKPOINT.json` — latest human decisions and unresolved gray areas.
2. `.planning/v9.0-MILESTONE-AUDIT-2026-08-15.md` — current completed-work, test, infrastructure, integration, and security gap audit; decision `HOLD`.
3. `.planning/phases/474-deterministic-verification-and-gated-delivery/474-CONTEXT.md` — locked release and infrastructure boundary.
4. `.planning/phases/474-deterministic-verification-and-gated-delivery/474-34-PLAN.md` and retained successor plans — exact staging-only topology and evidence gates.
5. `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` — current milestone traceability, noting their Teacher invitation text is stale.

## Critical Anti-Patterns (do NOT repeat these)

- Treating a successful Git sync as semantic integration → rerun real cross-repository contract checks.
- Counting mocked frontend tests as backend reachability evidence → test generated/real adapters and non-intercepted paths.
- Treating provider or production controls as authorized by current infra source → preserve the Phase 474 staging-only, production-deferred boundary.
- Preserving `tutor` for convenience → fail closed and reconcile historical data outside ordinary runtime login.

## Infrastructure State

- Backend baseline before the planning-tracking commit: `12114eee`; no deployment occurred.
- Frontend last verified baseline: `996de48`; untracked `public/mockServiceWorker.js` was present.
- Infra last verified baseline: `83c4ec2`; untracked `.DS_Store` was present.
- Phase 474 authority: one `StoaReleaseStaging` stack and exactly three disjoint staging roles; production control-plane work is deferred out of scope.
- No servers, watchers, deployments, or external async jobs are running for this phase.

<context>
The user wants an evidence-backed inventory of what is truly implemented and tested, what merely has isolated test coverage, and what security or infrastructure controls remain absent or contradicted. Preserve the paused Phase 477 decisions but do not extend them during the audit.
</context>

<next_action>
Start with: review the August 15 audit and obtain an explicit closure-boundary decision. Do not resume Phase 477 automatically.
</next_action>
