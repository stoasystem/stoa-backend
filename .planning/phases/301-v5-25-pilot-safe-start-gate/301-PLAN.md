# Phase 301 Plan

- Add a safe-start gate contract.
- Combine blocker audit, provider activation/disablement, dry run, and launch-room rehearsal.
- Return `hold` by default for unresolved blockers and `start_limited_pilot` only when blockers are cleared or disabled.
