# Phase 265 Plan

1. Add `GET /admin/bi/alert-routing`.
2. Convert dashboard section states into low-cardinality route keys and dimensions.
3. Add runbook metadata for severity, owners, escalation, suppression, retry, and backfill.
4. Test that alert payloads do not include private identifiers or raw content.
