---
phase: 225
plan: 01
status: complete
---

# Plan 225-01: Admin Account Operations Console

## Goal

Give admins a practical support view for one parent account using the backend v5.9 account operations detail endpoint.

## Tasks

1. Add admin account operations types and API client.
   - Define `AdminAccountOperations` as the parent account operations model with billing events.
   - Add `getAdminParentAccountOperations(parentId, day?)`.
   - Add `adminQueryKeys.accountOperations(parentId, day)`.
   - Add `useAdminParentAccountOperationsQuery`.

2. Add shared rendering support.
   - Reuse parent support-state formatting helpers.
   - Add admin-specific billing event and detail components.

3. Add admin route/page.
   - Create `AdminAccountOperationsPage`.
   - Add `/admin/account-operations`.
   - Support query-string lookup by `parentId` and optional day.

4. Add subscription handoff.
   - Add links from selected request/billing contexts to `/admin/account-operations?parentId=...`.

5. Add focused e2e coverage.
   - Cover ready detail, attention/blocked detail, missing parent 404, API error, and subscription handoff link.

6. Verify and document.
   - Run frontend lint/build and focused e2e.
   - Write summary and verification.
   - Update roadmap, requirements, and state.

## Verification

- `npm run lint` — passed
- `npm run build` — passed with existing Vite large chunk warning
- `npx playwright test tests/e2e/admin-account-operations.spec.ts` — passed, 5 tests
