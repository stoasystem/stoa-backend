# Phase 61 Plan: v2.2 Release Gate And Safe Fixture Verification

## Tasks

1. Record backend/frontend commit SHAs and deploy workflow evidence.
2. Record Lambda runtime metadata and CDK diff classification.
3. Record current local quality gates.
4. Run production read-only API smoke with secret-backed admin auth.
5. Run production read-only browser smoke with a GET-only report-admin guard.
6. Run safe-fixture mutation smoke only if an explicit named non-customer fixture is provided.
7. Audit v2.2 requirements and record residual risks or blockers.

## Verification Commands

Backend:

- `.venv/bin/python -m ruff check src/stoa/services/report_artifact_edit_service.py src/stoa/db/repositories/report_repo.py src/stoa/routers/admin.py tests/test_admin_report_ops.py`
- `.venv/bin/python -m pytest tests/test_admin_report_ops.py tests/test_report_artifact_service.py -q`
- `.venv/bin/python -m pytest -q`

Frontend:

- `npm run lint -- src/services/admin/adminApi.ts src/hooks/admin/useAdminReportOperations.ts src/pages/admin/ReportOperationsPage.tsx tests/e2e/admin-report-operations.spec.ts`
- `npm run build`
- `npx playwright test tests/e2e/admin-report-operations.spec.ts`

Production:

- `uv run cdk diff --profile stoa-prod-admin`
- `/private/tmp/stoa_phase61_api_smoke.mjs`
- `/private/tmp/stoa_phase61_browser_smoke.mjs`
- `node scripts/report_artifact_safe_fixture_smoke.mjs` default refusal check

## Exit Criteria

Phase 61 can be marked complete only after named safe-fixture mutation smoke records edit, rollback, cleanup/restore evidence, request IDs, artifact version metadata, and a clean privacy denylist.
