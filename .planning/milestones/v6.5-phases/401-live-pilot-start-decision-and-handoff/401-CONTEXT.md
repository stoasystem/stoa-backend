# Phase 401 Context

## Phase Boundary

Run the v6.5 start gate and hand off either cohort start operations or blocker burn-down.

## Decisions

- v6.6 real user operations are allowed only when v6.5 returns `start_limited_pilot`.
- Held decisions produce a blocker package target instead of allowing real cohort operations.
