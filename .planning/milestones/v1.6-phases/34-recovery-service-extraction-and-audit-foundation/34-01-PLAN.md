# Phase 34 Plan: Shared Recovery Service And Audit Foundation

**Milestone:** v1.6 Report Recovery Operations Hardening
**Phase:** 34
**Plan:** 34-01
**Status:** Complete

## Scope

Deliver AUDIT-01 through AUDIT-04:

- Extract existing resend/retry recovery paths into a shared service.
- Write append-only audit events for successful, refused, and failed recovery attempts.
- Add conditional DynamoDB append helpers and audit pagination tokens.
- Add admin-only report-local and job-local audit timeline APIs.
- Verify metadata-only privacy boundaries in tests.

AUDIT-05 remains partially deferred to Phase 35 for cancelled async recovery paths.

## Tasks

1. Add `report_recovery_service` with shared single resend and generation retry behavior.
2. Preserve existing API semantics for status codes and mutable report summary updates.
3. Add DynamoDB audit helpers with report/job partition keys and conditional append writes.
4. Add scoped audit page tokens that accept only `AUDIT#` keys for report/job audit partitions.
5. Update admin routes to call the shared service and expose audit timeline endpoints.
6. Update unit tests for service extraction, audit writes, admin authorization, pagination, and redaction.

## Verification

- `uv run pytest tests/test_admin_report_ops.py tests/test_parent_children.py -q`
- `uv run ruff check src/stoa/services/report_recovery_service.py src/stoa/routers/admin.py src/stoa/db/repositories/report_repo.py tests/test_admin_report_ops.py tests/test_parent_children.py`
