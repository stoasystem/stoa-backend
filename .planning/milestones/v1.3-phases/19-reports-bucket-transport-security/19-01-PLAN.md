# Plan 19-01: Enforce HTTPS-Only Reports Bucket Access

**Phase:** 19 - Reports Bucket Transport Security
**Status:** Completed
**Requirements:** SEC-01, SEC-02, SEC-03

## Objective

Update the CDK reports bucket definition so S3 denies insecure transport, then produce source, synth/diff, and live bucket evidence that the change is policy-only and preserves the deployed private bucket posture.

## Tasks

1. Update the reports bucket CDK construct.
   - Add `enforce_ssl=True` to `StoaReportsBucket` if supported by the installed CDK library.
   - Preserve bucket name, public access block, encryption, access logging, retain policy, and existing exports.
   - Do not change image/log buckets unless synth shows a required compatibility issue.

2. Generate local CDK evidence.
   - Run a synth-style command from `/Users/zhdeng/stoa-infra`.
   - Inspect `StoaStorageStack.template.json` for an insecure-transport deny policy on the reports bucket.
   - Confirm the reports bucket logical resource still has `DeletionPolicy: Retain`, `UpdateReplacePolicy: Retain`, same bucket-name expression, public access block, encryption, and access logging.

3. Generate deployment-drift evidence.
   - Run `cdk diff StoaStorageStack StoaApiStack --profile stoa` if CDK CLI and AWS SSO are usable.
   - Confirm no reports bucket replacement is proposed.
   - Record expected policy-only changes separately from existing Lambda asset `Code.S3Key` drift.

4. Generate live AWS evidence.
   - Query bucket public access block and encryption for `stoa-reports-562923011260`.
   - Query bucket policy if present after deploy/diff context permits.
   - Record whether the live policy already contains the deny-insecure-transport statement or still awaits deploy.

5. Write execution artifacts.
   - Create `19-01-SUMMARY.md` with changed files, commands, outcomes, deviations, and next-phase readiness.
   - Create `19-VERIFICATION.md` with status, requirement mapping, evidence, and residual risks.
   - Update `.planning/ROADMAP.md` and `.planning/STATE.md` for Phase 19 completion.

## Verification

- `git diff --check`
- CDK synth/template inspection from `/Users/zhdeng/stoa-infra`
- `cdk diff StoaStorageStack StoaApiStack --profile stoa` when available
- AWS CLI live bucket checks when available:
  - `aws s3api get-public-access-block --bucket stoa-reports-562923011260 --profile stoa`
  - `aws s3api get-bucket-encryption --bucket stoa-reports-562923011260 --profile stoa`
  - `aws s3api get-bucket-policy --bucket stoa-reports-562923011260 --profile stoa`

## Acceptance Criteria

- SEC-01: CDK source/synth evidence shows HTTPS-only S3 access enforcement for the reports bucket.
- SEC-02: CDK diff/source evidence shows the deployed reports bucket is not replaced.
- SEC-03: Live or explicitly pending-deploy evidence records public access block and encryption remain enabled after hardening.
