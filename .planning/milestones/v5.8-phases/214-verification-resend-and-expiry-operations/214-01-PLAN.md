# Phase 214 Plan: Verification Resend And Expiry Operations

## Objective

Expose verification resend/confirm recovery behavior and support-visible status without storing verification codes.

## Tasks

1. Add bounded profile verification update helper in `user_repo`.
2. Add resend endpoint with cooldown/idempotency and provider throttle handling.
3. Add confirm endpoint with verified and expired state transitions.
4. Add admin support visibility endpoint for one account.
5. Cover resend, throttling/idempotency, expiry, and support visibility in focused tests.

## Verification Commands

- `.venv/bin/pytest tests/test_auth_account_lifecycle.py -q`
- `.venv/bin/ruff check src/stoa/services/account_verification_service.py src/stoa/db/repositories/user_repo.py src/stoa/routers/auth.py src/stoa/routers/admin.py tests/test_auth_account_lifecycle.py`
