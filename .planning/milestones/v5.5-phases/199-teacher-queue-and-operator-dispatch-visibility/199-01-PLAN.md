# Plan 199-01: Implement Teacher Queue And Operator Dispatch Visibility

## Goal

Expose teacher queue and operator dispatch health without leaking internal ranking details to students.

## Work Items

1. Decorate teacher queue items with dispatch and SLA summaries.
2. Filter non-stale dispatches assigned to other teachers from normal teacher queue views.
3. Add admin dispatch dashboard with queue age, load, timeout, reassignment, SLA risk, and no-candidate aggregates.
4. Keep dashboard payload content-safe.
5. Add focused tests for teacher queue filtering and admin dashboard aggregation.

## Verification

- `uv run pytest tests/test_teacher_dispatch.py tests/test_teacher_reply_sla.py -q`
- `uv run ruff check src/stoa/db/repositories/question_repo.py src/stoa/services/teacher_dispatch_service.py src/stoa/routers/questions.py src/stoa/routers/teachers.py src/stoa/routers/admin.py tests/test_teacher_dispatch.py tests/test_teacher_reply_sla.py`
