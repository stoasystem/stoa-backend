# Phase 60 Plan: Admin Artifact Rollback UI

## Tasks

1. Extend admin frontend API contracts with rollback action eligibility, preview/apply request types, response types, and endpoint clients.
2. Add React Query mutations for rollback preview/apply and invalidate report operation queries on success.
3. Add selected-report rollback UI controls using the existing admin report operations design pattern.
4. Reset stale rollback preview state when selecting another report.
5. Extend Playwright report operations coverage for rollback preview/apply and privacy denylist.
6. Run targeted frontend lint, production build, and Playwright regression.

## Verification

- `npm run lint -- src/services/admin/adminApi.ts src/hooks/admin/useAdminReportOperations.ts src/pages/admin/ReportOperationsPage.tsx tests/e2e/admin-report-operations.spec.ts`
- `npm run build`
- `npx playwright test tests/e2e/admin-report-operations.spec.ts`

## Exit Criteria

- UI-09 acceptance criteria are implemented and tested.
- No private artifact markers are rendered in rollback UI.
- Phase 61 can proceed to release/deploy/live verification.
