# Plan: Phase 94 Teacher Reply Composer And SLA Visibility UI

**Phase:** 94
**Milestone:** v3.1 Teacher Reply Quality And SLA Operations
**Status:** Complete
**Created:** 2026-06-08T11:20:00+0200

## Objective

Surface the Phase 93 rich teacher reply contract and SLA timing state in the active
teacher/tutor workflow and expose aggregate SLA stats in admin operations.

## Work Items

1. Extend the active `/tutors/me/help-requests` backend contract with SLA snapshots.
2. Allow tutor notes/replies to carry the Phase 93 `richContent` payload and persist the normalized rich metadata.
3. Add a teacher reply composer with paragraph, formula, list, and code block modes.
4. Render rich replies through allowlisted React components only.
5. Show SLA state in the tutor queue/detail views.
6. Add an admin dashboard card for aggregate teacher SLA metrics from `/admin/stats`.
7. Add Playwright coverage for formula display, SLA status, admin stats visibility, and private marker/raw HTML denial.

## Verification

- Backend:
  - `.venv/bin/ruff check src/stoa/routers/tutors.py tests/test_teacher_reply_sla.py`
  - `.venv/bin/pytest tests/test_teacher_reply_sla.py`
  - `.venv/bin/pytest` (`271 passed`)
- Frontend:
  - `npm run lint`
  - `npm run build`
  - `npx playwright test tests/e2e/tutor-workflow.spec.ts` (`2 passed`)
- Browser smoke:
  - In-app browser verified tutor queue SLA badge, tutor detail rich formula block/composer, and admin Teacher SLA card at `http://localhost:5174/` with demo fallback enabled.

## Production Safety

Phase 94 is local UI/API-contract work only. It does not deploy, does not mutate production
customer content, and does not require new CDK resources.
