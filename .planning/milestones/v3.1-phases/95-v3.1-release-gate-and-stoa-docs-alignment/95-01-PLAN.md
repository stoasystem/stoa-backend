# Plan: Phase 95 v3.1 Release Gate And STOA Docs Alignment

**Phase:** 95
**Milestone:** v3.1 Teacher Reply Quality And SLA Operations
**Status:** Complete
**Created:** 2026-06-08T13:15:00+0200

## Objective

Close v3.1 with backend/frontend quality gates, deploy evidence, production-safe smoke,
updated `stoa_docs` gap audit, and milestone audit.

## Verification

- Backend local full test suite: `.venv/bin/pytest` (`271 passed`).
- Backend focused lint:
  - `.venv/bin/ruff check src/stoa/routers/teachers.py src/stoa/routers/questions.py src/stoa/routers/admin.py src/stoa/services/teacher_reply_service.py tests/test_teacher_reply_sla.py`
  - `.venv/bin/ruff check src/stoa/routers/tutors.py tests/test_teacher_reply_sla.py`
- Frontend local gates:
  - `npm run lint`
  - `npm run build`
  - `npx playwright test tests/e2e/tutor-workflow.spec.ts` (`2 passed`)
- In-app browser smoke verified tutor queue SLA, tutor detail composer/formula rendering, and admin Teacher SLA card with local demo fallback.
- Backend deploy run `27133648881` completed successfully for backend commit `70cb8ae`.
- Frontend CI run `27133654195` completed successfully for frontend commit `b6c0fc4`.
- Frontend deploy run `27133654168` completed successfully for frontend commit `b6c0fc4`.
- Production API smoke:
  - `GET https://api.stoaedu.ch/health` returned `200`, request id `eo53hjssZicEPLQ=`.
  - Unauthenticated `GET https://api.stoaedu.ch/admin/stats` returned `401`, request id `eo55kjuJZicEPLQ=`.
- Production frontend smoke:
  - `HEAD https://app.stoaedu.ch/` returned `200`, `Last-Modified: Mon, 08 Jun 2026 11:11:16 GMT`.

## Production Safety

No production customer content mutation was performed. The teacher reply workflow was
not exercised through production writes because no named non-customer teacher-question
safe fixture and cleanup path was approved for this milestone. Production smoke remained
read-only/auth-gate only.

## Local Limitations

AWS CLI Lambda configuration reads were attempted but local AWS credentials were not
available (`NoCredentials`). Deploy evidence therefore relies on successful GitHub Actions
deploy runs and production HTTP smoke.
