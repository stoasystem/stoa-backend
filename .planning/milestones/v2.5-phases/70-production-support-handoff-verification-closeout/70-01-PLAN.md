# Phase 70 Plan: Production Support Handoff Verification Closeout

## Objective

Close the deferred v2.4 production verification gap with deploy evidence, runtime evidence, CDK classification, read-only production API smoke, read-only production browser smoke, and a final closeout audit.

## Tasks

1. Record backend and frontend deploy workflow run IDs, commit SHAs, job IDs, timestamps, and statuses.
2. Rebuild or verify Lambda build manifest and record live Lambda runtime metadata for `stoa-api` and `stoa-weekly-report`.
3. Run CDK diff with `stoa-prod-admin` and classify drift.
4. Run production read-only API smoke for support handoff auth, preview, `external_write` refusal, request IDs, and privacy denylist.
5. Run production read-only browser smoke for `/admin/report-operations` support handoff UI markers, route/auth, request guard, and visible privacy denylist.
6. Update v2.4 deferred verification evidence or cross-reference it from v2.5.
7. Complete final v2.5 audit and record residual risks/follow-ups.

## Verification Commands

Backend/local:

- `.venv/bin/python -m ruff check src/stoa/services/support_handoff_service.py src/stoa/routers/admin.py src/stoa/db/repositories/report_repo.py tests/test_admin_report_ops.py`
- `.venv/bin/python -m pytest tests/test_admin_report_ops.py -k "support_handoff or recovery_job_support_package or recovery_evidence" -q`
- `.venv/bin/python scripts/build_lambda_dist.py --verify-only`

Production:

- `uv run cdk diff --profile stoa-prod-admin`
- Production read-only API smoke for `/health` and `/admin/reports/support-handoff-package`.
- Production read-only browser smoke for `https://app.stoaedu.ch/admin/report-operations`.

## Exit Criteria

Phase 70 can be marked complete only when:

- `70-RELEASE-GATE.md` records deploy/runtime/CDK/local quality evidence.
- `70-LIVE-VERIFICATION.md` records production read-only API/browser smoke evidence with request IDs.
- `70-MILESTONE-AUDIT.md` confirms v2.4 production verification is resolved or records a concrete blocker.
