# Phase 44 Plan

**Phase:** 44 - Admin Generation Retry Job UI
**Plan:** 44-01
**Status:** Complete
**Created:** 2026-06-05

## Tasks

1. Extend frontend admin API bindings for `retry_generation` preview/create endpoints.
2. Add React Query hooks for generation retry preview/create mutations.
3. Convert the async recovery job panel from resend-only to a two-mode panel:
   - `Resend email` uses fixed `email_failed` filters.
   - `Retry generation` uses fixed `generation_failed` filters.
4. Render job type labels and generic success counts in the recovery jobs panel.
5. Extend Playwright admin report operations coverage for both job types and metadata-only rendering.
6. Run frontend lint, build, e2e, and backend regression checks.

## Safety Notes

- This phase changes local UI and tests only; no production mutation is executed.
- Production smoke is deferred to Phase 45 and must remain read-only.

