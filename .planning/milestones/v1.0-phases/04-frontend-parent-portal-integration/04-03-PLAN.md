---
phase: 04-frontend-parent-portal-integration
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - /Users/zhdeng/stoa-frontend/tests/e2e/parent-dashboard.spec.ts
  - .planning/phases/04-frontend-parent-portal-integration/04-VERIFICATION.md
autonomous: true
requirements:
  - FRONT-01
  - FRONT-02
  - FRONT-03
  - FRONT-04
  - FRONT-05
  - FRONT-06
user_setup:
  - "Frontend repository write access is required because the active Codex writable root is /Users/zhdeng/stoa-backend."
must_haves:
  truths:
    - "Focused verification proves the parent dashboard, child summary, history, and weekly report routes work against the local backend contract."
    - "Tests no longer depend on a hard-coded `user-student` path assertion for the dashboard-to-summary navigation."
    - "Build and lint pass after frontend contract changes."
  artifacts:
    - path: "/Users/zhdeng/stoa-frontend/tests/e2e/parent-dashboard.spec.ts"
      provides: "Focused parent portal flow verification"
      contains: "parent can view child summary"
    - path: ".planning/phases/04-frontend-parent-portal-integration/04-VERIFICATION.md"
      provides: "Recorded Phase 4 verification results"
      contains: "npm run build"
---

<objective>
Verify Phase 4 frontend integration and record the evidence for milestone continuation.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update focused parent Playwright flow</name>
  <action>Revise `tests/e2e/parent-dashboard.spec.ts` so it asserts the normal parent journey without hard-coding the child id. Cover dashboard child list, child summary, child history, and weekly report available or missing state. Keep the test focused and compatible with the local demo backend.</action>
  <verify>
    <automated>npx playwright test tests/e2e/parent-dashboard.spec.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run and record verification</name>
  <action>Run `npm run build`, `npm run lint`, and the focused parent Playwright test from `/Users/zhdeng/stoa-frontend`. Record results, any limitations, and remaining Phase 5 implications in `04-VERIFICATION.md`.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
    <automated>npx playwright test tests/e2e/parent-dashboard.spec.ts</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Frontend build and lint pass.
- Parent-focused Playwright flow passes or any failure is documented with a concrete blocker.
- Verification evidence is captured for Phase 5.
</success_criteria>

<output>
Create `.planning/phases/04-frontend-parent-portal-integration/04-03-SUMMARY.md` when done.
</output>
