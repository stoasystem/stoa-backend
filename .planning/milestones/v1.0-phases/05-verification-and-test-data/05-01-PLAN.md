---
phase: 05-verification-and-test-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/zhdeng/stoa-frontend/tests/e2e/parent-dashboard.spec.ts
autonomous: true
requirements:
  - TEST-06
  - TEST-07
  - TEST-08
  - TEST-09
user_setup:
  - "Frontend repository write access is required because the active Codex writable root is /Users/zhdeng/stoa-backend."
must_haves:
  truths:
    - "Frontend focused tests prove parent dashboard renders a real API child list response."
    - "Frontend focused tests prove parent dashboard renders no-child empty state."
    - "Frontend focused tests prove child report renders missing-report state."
    - "Frontend focused tests or request fixtures prove service paths use `/parents/me/...`."
  artifacts:
    - path: "/Users/zhdeng/stoa-frontend/tests/e2e/parent-dashboard.spec.ts"
      provides: "Focused parent frontend verification cases"
      contains: "No weekly report yet"
---

<objective>
Fill frontend verification gaps for dashboard no-child and missing-report behavior.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add no-child and missing-report frontend cases</name>
  <action>Extend the focused parent Playwright spec with route fixtures for an empty `/parents/me/children` response and a missing `/parents/me/children/{childId}/report` response. Keep the existing available-report journey. Use fixtures that explicitly target `/parents/me/...` URLs so service path alignment is asserted by the browser test.</action>
  <verify>
    <automated>npx playwright test tests/e2e/parent-dashboard.spec.ts</automated>
    <automated>npm run lint</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Parent Playwright spec passes with real-list, no-child, available-report, and missing-report cases.
- The test file demonstrates the frontend calls `/parents/me/...` routes.
</success_criteria>

<output>
Create `.planning/phases/05-verification-and-test-data/05-01-SUMMARY.md` when done.
</output>
