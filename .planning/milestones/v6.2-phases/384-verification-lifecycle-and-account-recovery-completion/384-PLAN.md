# Phase 384 Plan

- Add `verification_lifecycle_account_recovery_completion`.
- Represent tested and explicitly deferred account lifecycle surfaces.
- Keep evidence fields compatible with the private marker denylist.
