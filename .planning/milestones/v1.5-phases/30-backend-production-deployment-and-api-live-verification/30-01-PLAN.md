---
plan_id: 30-01
phase: 30
phase_name: Backend Production Deployment and API Live Verification
status: complete
type: verification
wave: 1
depends_on: []
requirements:
  - REL-01
  - REL-03
  - LIVE-01
  - LIVE-02
  - LIVE-04
  - VERIFY-02
requirements_addressed:
  - REL-01
  - REL-03
  - LIVE-01
  - LIVE-02
  - LIVE-04
  - VERIFY-02
files_modified:
  - .planning/phases/30-backend-production-deployment-and-api-live-verification/30-01-SUMMARY.md
  - .planning/phases/30-backend-production-deployment-and-api-live-verification/30-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
---

# Plan 30-01: Backend Production Deployment and API Live Verification

## Objective

Verify the backend production deployment state, Lambda runtime wiring, API health/auth gates, and CDK diff. Complete admin-auth list/detail only if a production admin token is available.

## Tasks

<task id="30-01-T1">
  <title>Verify AWS identity and Lambda runtime state</title>
  <read_first>
    <file>.planning/phases/30-backend-production-deployment-and-api-live-verification/30-CONTEXT.md</file>
    <file>.planning/phases/28-release-readiness-and-deployment-contract/28-RELEASE-READINESS.md</file>
  </read_first>
  <action>Run AWS STS identity and Lambda configuration checks for `stoa-api` and `stoa-weekly-report` using profile `stoa` and region `eu-central-2`. Record account, role ARN, function runtime, code size, last modified timestamp, state, last update status, code SHA, and `S3_REPORTS_BUCKET` value in `30-VERIFICATION.md`.</action>
  <acceptance_criteria>
    <criterion>`30-VERIFICATION.md` contains account `562923011260`.</criterion>
    <criterion>`30-VERIFICATION.md` contains `stoa-api`, `State=Active`, `LastUpdateStatus=Successful`, and `S3_REPORTS_BUCKET=stoa-reports-562923011260`.</criterion>
    <criterion>`30-VERIFICATION.md` contains `stoa-weekly-report`, `State=Active`, `LastUpdateStatus=Successful`, and `S3_REPORTS_BUCKET=stoa-reports-562923011260`.</criterion>
  </acceptance_criteria>
</task>

<task id="30-01-T2">
  <title>Verify backend tests, API health, and unauth rejection</title>
  <read_first>
    <file>tests/test_admin_report_ops.py</file>
    <file>tests/test_parent_children.py</file>
    <file>src/stoa/routers/admin.py</file>
  </read_first>
  <action>Run focused backend tests and focused ruff checks. Call `https://api.stoaedu.ch/health`, unauthenticated `https://api.stoaedu.ch/admin/reports/ops`, and invalid-token `https://api.stoaedu.ch/admin/reports/ops`. Record status codes and response bodies in `30-VERIFICATION.md`.</action>
  <acceptance_criteria>
    <criterion>`30-VERIFICATION.md` records `89 passed` for focused backend tests.</criterion>
    <criterion>`30-VERIFICATION.md` records focused ruff as passed.</criterion>
    <criterion>`30-VERIFICATION.md` records API health HTTP 200.</criterion>
    <criterion>`30-VERIFICATION.md` records unauthenticated admin report ops HTTP 401.</criterion>
    <criterion>`30-VERIFICATION.md` records invalid-token admin report ops HTTP 401.</criterion>
  </acceptance_criteria>
</task>

<task id="30-01-T3">
  <title>Verify CDK diff classification</title>
  <read_first>
    <file>.planning/phases/28-release-readiness-and-deployment-contract/28-RELEASE-READINESS.md</file>
    <file>/Users/zhdeng/stoa-infra/stacks/api_stack.py</file>
  </read_first>
  <action>Run `uv run cdk diff --all --profile stoa --context env=dev` from `/Users/zhdeng/stoa-infra`. Record warnings, stacks with no differences, stacks with differences, and whether differences are only expected Lambda `Code.S3Key` asset drift in `30-VERIFICATION.md`.</action>
  <acceptance_criteria>
    <criterion>`30-VERIFICATION.md` records the `Unknown option(s): --all` warning if present.</criterion>
    <criterion>`30-VERIFICATION.md` records Node v26 support warning if present.</criterion>
    <criterion>`30-VERIFICATION.md` records StoaApiStack Lambda `Code.S3Key` diff if present.</criterion>
    <criterion>`30-VERIFICATION.md` records no unexpected IAM, bucket, API route, DynamoDB, or policy drift if true.</criterion>
  </acceptance_criteria>
</task>

<task id="30-01-T4">
  <title>Attempt admin-auth read-only checks without mutation</title>
  <read_first>
    <file>src/stoa/routers/auth.py</file>
    <file>src/stoa/routers/admin.py</file>
    <file>/Users/zhdeng/stoa-frontend/src/services/admin/adminApi.ts</file>
  </read_first>
  <action>Attempt to obtain a production admin token only from already documented credentials or an existing admin browser/session source. If no token is available, record the blocker. If a token is available, call `GET /admin/reports/ops?limit=5`; if rows are returned, call one detail endpoint for a safe target and verify metadata-only response with no raw report HTML, private artifact keys, public URLs, presigned URLs, or direct S3 URL markers.</action>
  <acceptance_criteria>
    <criterion>`30-VERIFICATION.md` records admin token status as available or blocked.</criterion>
    <criterion>If blocked, `30-VERIFICATION.md` records the exact blocker and leaves LIVE-01 and LIVE-02 incomplete.</criterion>
    <criterion>If available, `30-VERIFICATION.md` records list/detail status and privacy marker results.</criterion>
  </acceptance_criteria>
</task>

## Threat Model

<threat_model>
  <threat id="TM-30-01" severity="high">
    <risk>Admin-auth API verification could be claimed without a real production admin token.</risk>
    <mitigation>Record LIVE-01 and LIVE-02 as incomplete unless a real production admin token is used for live API calls.</mitigation>
  </threat>
  <threat id="TM-30-02" severity="medium">
    <risk>Read-only verification could accidentally trigger report recovery mutations.</risk>
    <mitigation>Phase 30 permits only GET `/health`, GET `/admin/reports/ops`, and GET detail `/ops`; no POST recovery endpoints are allowed.</mitigation>
  </threat>
  <threat id="TM-30-03" severity="medium">
    <risk>Expected Lambda asset drift could hide unexpected IAM or storage drift.</risk>
    <mitigation>Classify CDK diff categories and block Phase 31 on unexpected IAM, bucket, API route, DynamoDB, or policy drift.</mitigation>
  </threat>
</threat_model>

## Must Haves

- AWS identity is verified.
- Both Lambdas are `Active` and `LastUpdateStatus=Successful`.
- Both Lambdas have `S3_REPORTS_BUCKET=stoa-reports-562923011260`.
- API health returns 200.
- Unauthenticated report operations access returns 401.
- Invalid-token report operations access returns 401.
- CDK diff is classified.
- Admin-auth list/detail is either verified or recorded as a blocking gap.

## Verification

- `aws sts get-caller-identity --profile stoa --region eu-central-2`
- `aws lambda get-function-configuration --function-name stoa-api --profile stoa --region eu-central-2`
- `aws lambda get-function-configuration --function-name stoa-weekly-report --profile stoa --region eu-central-2`
- `uv run pytest tests/test_admin_report_ops.py tests/test_parent_children.py`
- `uv run ruff check src/stoa/routers/admin.py tests/test_admin_report_ops.py tests/test_parent_children.py`
- `curl https://api.stoaedu.ch/health`
- `curl https://api.stoaedu.ch/admin/reports/ops`
- `uv run cdk diff --all --profile stoa --context env=dev`

## Artifacts This Phase Produces

- `.planning/phases/30-backend-production-deployment-and-api-live-verification/30-01-SUMMARY.md`
- `.planning/phases/30-backend-production-deployment-and-api-live-verification/30-VERIFICATION.md`

No runtime code symbols, API endpoints, classes, functions, database fields, IAM policies, or frontend routes are created by this phase.
